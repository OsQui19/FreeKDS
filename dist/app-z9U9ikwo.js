function _mergeNamespaces(n, m3) {
  for (var i2 = 0; i2 < m3.length; i2++) {
    const e2 = m3[i2];
    if (typeof e2 !== "string" && !Array.isArray(e2)) {
      for (const k2 in e2) {
        if (k2 !== "default" && !(k2 in n)) {
          const d3 = Object.getOwnPropertyDescriptor(e2, k2);
          if (d3) {
            Object.defineProperty(n, k2, d3.get ? d3 : {
              enumerable: true,
              get: () => e2[k2]
            });
          }
        }
      }
    }
  }
  return Object.freeze(Object.defineProperty(n, Symbol.toStringTag, { value: "Module" }));
}
(function polyfill() {
  const relList = document.createElement("link").relList;
  if (relList && relList.supports && relList.supports("modulepreload")) return;
  for (const link of document.querySelectorAll('link[rel="modulepreload"]')) processPreload(link);
  new MutationObserver((mutations) => {
    for (const mutation of mutations) {
      if (mutation.type !== "childList") continue;
      for (const node of mutation.addedNodes) if (node.tagName === "LINK" && node.rel === "modulepreload") processPreload(node);
    }
  }).observe(document, {
    childList: true,
    subtree: true
  });
  function getFetchOpts(link) {
    const fetchOpts = {};
    if (link.integrity) fetchOpts.integrity = link.integrity;
    if (link.referrerPolicy) fetchOpts.referrerPolicy = link.referrerPolicy;
    if (link.crossOrigin === "use-credentials") fetchOpts.credentials = "include";
    else if (link.crossOrigin === "anonymous") fetchOpts.credentials = "omit";
    else fetchOpts.credentials = "same-origin";
    return fetchOpts;
  }
  function processPreload(link) {
    if (link.ep) return;
    link.ep = true;
    const fetchOpts = getFetchOpts(link);
    fetch(link.href, fetchOpts);
  }
})();
var commonjsGlobal = typeof globalThis !== "undefined" ? globalThis : typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : {};
var jsxRuntime = { exports: {} };
var reactJsxRuntime_production_min = {};
var react = { exports: {} };
var react_production_min = {};
/**
 * @license React
 * react.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReact_production_min;
function requireReact_production_min() {
  if (hasRequiredReact_production_min) return react_production_min;
  hasRequiredReact_production_min = 1;
  var l = Symbol.for("react.element"), n = Symbol.for("react.portal"), p = Symbol.for("react.fragment"), q2 = Symbol.for("react.strict_mode"), r2 = Symbol.for("react.profiler"), t2 = Symbol.for("react.provider"), u2 = Symbol.for("react.context"), v = Symbol.for("react.forward_ref"), w2 = Symbol.for("react.suspense"), x2 = Symbol.for("react.memo"), y3 = Symbol.for("react.lazy"), z2 = Symbol.iterator;
  function A(a3) {
    if (null === a3 || "object" !== typeof a3) return null;
    a3 = z2 && a3[z2] || a3["@@iterator"];
    return "function" === typeof a3 ? a3 : null;
  }
  var B2 = { isMounted: function() {
    return false;
  }, enqueueForceUpdate: function() {
  }, enqueueReplaceState: function() {
  }, enqueueSetState: function() {
  } }, C = Object.assign, D2 = {};
  function E2(a3, b2, e2) {
    this.props = a3;
    this.context = b2;
    this.refs = D2;
    this.updater = e2 || B2;
  }
  E2.prototype.isReactComponent = {};
  E2.prototype.setState = function(a3, b2) {
    if ("object" !== typeof a3 && "function" !== typeof a3 && null != a3) throw Error("setState(...): takes an object of state variables to update or a function which returns an object of state variables.");
    this.updater.enqueueSetState(this, a3, b2, "setState");
  };
  E2.prototype.forceUpdate = function(a3) {
    this.updater.enqueueForceUpdate(this, a3, "forceUpdate");
  };
  function F() {
  }
  F.prototype = E2.prototype;
  function G2(a3, b2, e2) {
    this.props = a3;
    this.context = b2;
    this.refs = D2;
    this.updater = e2 || B2;
  }
  var H3 = G2.prototype = new F();
  H3.constructor = G2;
  C(H3, E2.prototype);
  H3.isPureReactComponent = true;
  var I2 = Array.isArray, J = Object.prototype.hasOwnProperty, K2 = { current: null }, L2 = { key: true, ref: true, __self: true, __source: true };
  function M3(a3, b2, e2) {
    var d3, c2 = {}, k2 = null, h3 = null;
    if (null != b2) for (d3 in void 0 !== b2.ref && (h3 = b2.ref), void 0 !== b2.key && (k2 = "" + b2.key), b2) J.call(b2, d3) && !L2.hasOwnProperty(d3) && (c2[d3] = b2[d3]);
    var g = arguments.length - 2;
    if (1 === g) c2.children = e2;
    else if (1 < g) {
      for (var f = Array(g), m3 = 0; m3 < g; m3++) f[m3] = arguments[m3 + 2];
      c2.children = f;
    }
    if (a3 && a3.defaultProps) for (d3 in g = a3.defaultProps, g) void 0 === c2[d3] && (c2[d3] = g[d3]);
    return { $$typeof: l, type: a3, key: k2, ref: h3, props: c2, _owner: K2.current };
  }
  function N(a3, b2) {
    return { $$typeof: l, type: a3.type, key: b2, ref: a3.ref, props: a3.props, _owner: a3._owner };
  }
  function O2(a3) {
    return "object" === typeof a3 && null !== a3 && a3.$$typeof === l;
  }
  function escape(a3) {
    var b2 = { "=": "=0", ":": "=2" };
    return "$" + a3.replace(/[=:]/g, function(a4) {
      return b2[a4];
    });
  }
  var P = /\/+/g;
  function Q2(a3, b2) {
    return "object" === typeof a3 && null !== a3 && null != a3.key ? escape("" + a3.key) : b2.toString(36);
  }
  function R2(a3, b2, e2, d3, c2) {
    var k2 = typeof a3;
    if ("undefined" === k2 || "boolean" === k2) a3 = null;
    var h3 = false;
    if (null === a3) h3 = true;
    else switch (k2) {
      case "string":
      case "number":
        h3 = true;
        break;
      case "object":
        switch (a3.$$typeof) {
          case l:
          case n:
            h3 = true;
        }
    }
    if (h3) return h3 = a3, c2 = c2(h3), a3 = "" === d3 ? "." + Q2(h3, 0) : d3, I2(c2) ? (e2 = "", null != a3 && (e2 = a3.replace(P, "$&/") + "/"), R2(c2, b2, e2, "", function(a4) {
      return a4;
    })) : null != c2 && (O2(c2) && (c2 = N(c2, e2 + (!c2.key || h3 && h3.key === c2.key ? "" : ("" + c2.key).replace(P, "$&/") + "/") + a3)), b2.push(c2)), 1;
    h3 = 0;
    d3 = "" === d3 ? "." : d3 + ":";
    if (I2(a3)) for (var g = 0; g < a3.length; g++) {
      k2 = a3[g];
      var f = d3 + Q2(k2, g);
      h3 += R2(k2, b2, e2, f, c2);
    }
    else if (f = A(a3), "function" === typeof f) for (a3 = f.call(a3), g = 0; !(k2 = a3.next()).done; ) k2 = k2.value, f = d3 + Q2(k2, g++), h3 += R2(k2, b2, e2, f, c2);
    else if ("object" === k2) throw b2 = String(a3), Error("Objects are not valid as a React child (found: " + ("[object Object]" === b2 ? "object with keys {" + Object.keys(a3).join(", ") + "}" : b2) + "). If you meant to render a collection of children, use an array instead.");
    return h3;
  }
  function S3(a3, b2, e2) {
    if (null == a3) return a3;
    var d3 = [], c2 = 0;
    R2(a3, d3, "", "", function(a4) {
      return b2.call(e2, a4, c2++);
    });
    return d3;
  }
  function T2(a3) {
    if (-1 === a3._status) {
      var b2 = a3._result;
      b2 = b2();
      b2.then(function(b3) {
        if (0 === a3._status || -1 === a3._status) a3._status = 1, a3._result = b3;
      }, function(b3) {
        if (0 === a3._status || -1 === a3._status) a3._status = 2, a3._result = b3;
      });
      -1 === a3._status && (a3._status = 0, a3._result = b2);
    }
    if (1 === a3._status) return a3._result.default;
    throw a3._result;
  }
  var U = { current: null }, V = { transition: null }, W = { ReactCurrentDispatcher: U, ReactCurrentBatchConfig: V, ReactCurrentOwner: K2 };
  function X2() {
    throw Error("act(...) is not supported in production builds of React.");
  }
  react_production_min.Children = { map: S3, forEach: function(a3, b2, e2) {
    S3(a3, function() {
      b2.apply(this, arguments);
    }, e2);
  }, count: function(a3) {
    var b2 = 0;
    S3(a3, function() {
      b2++;
    });
    return b2;
  }, toArray: function(a3) {
    return S3(a3, function(a4) {
      return a4;
    }) || [];
  }, only: function(a3) {
    if (!O2(a3)) throw Error("React.Children.only expected to receive a single React element child.");
    return a3;
  } };
  react_production_min.Component = E2;
  react_production_min.Fragment = p;
  react_production_min.Profiler = r2;
  react_production_min.PureComponent = G2;
  react_production_min.StrictMode = q2;
  react_production_min.Suspense = w2;
  react_production_min.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = W;
  react_production_min.act = X2;
  react_production_min.cloneElement = function(a3, b2, e2) {
    if (null === a3 || void 0 === a3) throw Error("React.cloneElement(...): The argument must be a React element, but you passed " + a3 + ".");
    var d3 = C({}, a3.props), c2 = a3.key, k2 = a3.ref, h3 = a3._owner;
    if (null != b2) {
      void 0 !== b2.ref && (k2 = b2.ref, h3 = K2.current);
      void 0 !== b2.key && (c2 = "" + b2.key);
      if (a3.type && a3.type.defaultProps) var g = a3.type.defaultProps;
      for (f in b2) J.call(b2, f) && !L2.hasOwnProperty(f) && (d3[f] = void 0 === b2[f] && void 0 !== g ? g[f] : b2[f]);
    }
    var f = arguments.length - 2;
    if (1 === f) d3.children = e2;
    else if (1 < f) {
      g = Array(f);
      for (var m3 = 0; m3 < f; m3++) g[m3] = arguments[m3 + 2];
      d3.children = g;
    }
    return { $$typeof: l, type: a3.type, key: c2, ref: k2, props: d3, _owner: h3 };
  };
  react_production_min.createContext = function(a3) {
    a3 = { $$typeof: u2, _currentValue: a3, _currentValue2: a3, _threadCount: 0, Provider: null, Consumer: null, _defaultValue: null, _globalName: null };
    a3.Provider = { $$typeof: t2, _context: a3 };
    return a3.Consumer = a3;
  };
  react_production_min.createElement = M3;
  react_production_min.createFactory = function(a3) {
    var b2 = M3.bind(null, a3);
    b2.type = a3;
    return b2;
  };
  react_production_min.createRef = function() {
    return { current: null };
  };
  react_production_min.forwardRef = function(a3) {
    return { $$typeof: v, render: a3 };
  };
  react_production_min.isValidElement = O2;
  react_production_min.lazy = function(a3) {
    return { $$typeof: y3, _payload: { _status: -1, _result: a3 }, _init: T2 };
  };
  react_production_min.memo = function(a3, b2) {
    return { $$typeof: x2, type: a3, compare: void 0 === b2 ? null : b2 };
  };
  react_production_min.startTransition = function(a3) {
    var b2 = V.transition;
    V.transition = {};
    try {
      a3();
    } finally {
      V.transition = b2;
    }
  };
  react_production_min.unstable_act = X2;
  react_production_min.useCallback = function(a3, b2) {
    return U.current.useCallback(a3, b2);
  };
  react_production_min.useContext = function(a3) {
    return U.current.useContext(a3);
  };
  react_production_min.useDebugValue = function() {
  };
  react_production_min.useDeferredValue = function(a3) {
    return U.current.useDeferredValue(a3);
  };
  react_production_min.useEffect = function(a3, b2) {
    return U.current.useEffect(a3, b2);
  };
  react_production_min.useId = function() {
    return U.current.useId();
  };
  react_production_min.useImperativeHandle = function(a3, b2, e2) {
    return U.current.useImperativeHandle(a3, b2, e2);
  };
  react_production_min.useInsertionEffect = function(a3, b2) {
    return U.current.useInsertionEffect(a3, b2);
  };
  react_production_min.useLayoutEffect = function(a3, b2) {
    return U.current.useLayoutEffect(a3, b2);
  };
  react_production_min.useMemo = function(a3, b2) {
    return U.current.useMemo(a3, b2);
  };
  react_production_min.useReducer = function(a3, b2, e2) {
    return U.current.useReducer(a3, b2, e2);
  };
  react_production_min.useRef = function(a3) {
    return U.current.useRef(a3);
  };
  react_production_min.useState = function(a3) {
    return U.current.useState(a3);
  };
  react_production_min.useSyncExternalStore = function(a3, b2, e2) {
    return U.current.useSyncExternalStore(a3, b2, e2);
  };
  react_production_min.useTransition = function() {
    return U.current.useTransition();
  };
  react_production_min.version = "18.3.1";
  return react_production_min;
}
var hasRequiredReact;
function requireReact() {
  if (hasRequiredReact) return react.exports;
  hasRequiredReact = 1;
  {
    react.exports = requireReact_production_min();
  }
  return react.exports;
}
/**
 * @license React
 * react-jsx-runtime.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReactJsxRuntime_production_min;
function requireReactJsxRuntime_production_min() {
  if (hasRequiredReactJsxRuntime_production_min) return reactJsxRuntime_production_min;
  hasRequiredReactJsxRuntime_production_min = 1;
  var f = requireReact(), k2 = Symbol.for("react.element"), l = Symbol.for("react.fragment"), m3 = Object.prototype.hasOwnProperty, n = f.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED.ReactCurrentOwner, p = { key: true, ref: true, __self: true, __source: true };
  function q2(c2, a3, g) {
    var b2, d3 = {}, e2 = null, h3 = null;
    void 0 !== g && (e2 = "" + g);
    void 0 !== a3.key && (e2 = "" + a3.key);
    void 0 !== a3.ref && (h3 = a3.ref);
    for (b2 in a3) m3.call(a3, b2) && !p.hasOwnProperty(b2) && (d3[b2] = a3[b2]);
    if (c2 && c2.defaultProps) for (b2 in a3 = c2.defaultProps, a3) void 0 === d3[b2] && (d3[b2] = a3[b2]);
    return { $$typeof: k2, type: c2, key: e2, ref: h3, props: d3, _owner: n.current };
  }
  reactJsxRuntime_production_min.Fragment = l;
  reactJsxRuntime_production_min.jsx = q2;
  reactJsxRuntime_production_min.jsxs = q2;
  return reactJsxRuntime_production_min;
}
var hasRequiredJsxRuntime;
function requireJsxRuntime() {
  if (hasRequiredJsxRuntime) return jsxRuntime.exports;
  hasRequiredJsxRuntime = 1;
  {
    jsxRuntime.exports = requireReactJsxRuntime_production_min();
  }
  return jsxRuntime.exports;
}
var jsxRuntimeExports = requireJsxRuntime();
const API_URL = "";
const originalFetch = window.fetch.bind(window);
window.fetch = (input, init) => {
  if (typeof input === "string" && input.startsWith("/") && API_URL) ;
  return originalFetch(input, init);
};
var reactExports = requireReact();
const React = /* @__PURE__ */ _mergeNamespaces({
  __proto__: null,
  default: reactExports
}, [reactExports]);
var client = {};
var reactDom = { exports: {} };
var reactDom_production_min = {};
var scheduler = { exports: {} };
var scheduler_production_min = {};
/**
 * @license React
 * scheduler.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredScheduler_production_min;
function requireScheduler_production_min() {
  if (hasRequiredScheduler_production_min) return scheduler_production_min;
  hasRequiredScheduler_production_min = 1;
  (function(exports) {
    function f(a3, b2) {
      var c2 = a3.length;
      a3.push(b2);
      a: for (; 0 < c2; ) {
        var d3 = c2 - 1 >>> 1, e2 = a3[d3];
        if (0 < g(e2, b2)) a3[d3] = b2, a3[c2] = e2, c2 = d3;
        else break a;
      }
    }
    function h3(a3) {
      return 0 === a3.length ? null : a3[0];
    }
    function k2(a3) {
      if (0 === a3.length) return null;
      var b2 = a3[0], c2 = a3.pop();
      if (c2 !== b2) {
        a3[0] = c2;
        a: for (var d3 = 0, e2 = a3.length, w2 = e2 >>> 1; d3 < w2; ) {
          var m3 = 2 * (d3 + 1) - 1, C = a3[m3], n = m3 + 1, x2 = a3[n];
          if (0 > g(C, c2)) n < e2 && 0 > g(x2, C) ? (a3[d3] = x2, a3[n] = c2, d3 = n) : (a3[d3] = C, a3[m3] = c2, d3 = m3);
          else if (n < e2 && 0 > g(x2, c2)) a3[d3] = x2, a3[n] = c2, d3 = n;
          else break a;
        }
      }
      return b2;
    }
    function g(a3, b2) {
      var c2 = a3.sortIndex - b2.sortIndex;
      return 0 !== c2 ? c2 : a3.id - b2.id;
    }
    if ("object" === typeof performance && "function" === typeof performance.now) {
      var l = performance;
      exports.unstable_now = function() {
        return l.now();
      };
    } else {
      var p = Date, q2 = p.now();
      exports.unstable_now = function() {
        return p.now() - q2;
      };
    }
    var r2 = [], t2 = [], u2 = 1, v = null, y3 = 3, z2 = false, A = false, B2 = false, D2 = "function" === typeof setTimeout ? setTimeout : null, E2 = "function" === typeof clearTimeout ? clearTimeout : null, F = "undefined" !== typeof setImmediate ? setImmediate : null;
    "undefined" !== typeof navigator && void 0 !== navigator.scheduling && void 0 !== navigator.scheduling.isInputPending && navigator.scheduling.isInputPending.bind(navigator.scheduling);
    function G2(a3) {
      for (var b2 = h3(t2); null !== b2; ) {
        if (null === b2.callback) k2(t2);
        else if (b2.startTime <= a3) k2(t2), b2.sortIndex = b2.expirationTime, f(r2, b2);
        else break;
        b2 = h3(t2);
      }
    }
    function H3(a3) {
      B2 = false;
      G2(a3);
      if (!A) if (null !== h3(r2)) A = true, I2(J);
      else {
        var b2 = h3(t2);
        null !== b2 && K2(H3, b2.startTime - a3);
      }
    }
    function J(a3, b2) {
      A = false;
      B2 && (B2 = false, E2(L2), L2 = -1);
      z2 = true;
      var c2 = y3;
      try {
        G2(b2);
        for (v = h3(r2); null !== v && (!(v.expirationTime > b2) || a3 && !M3()); ) {
          var d3 = v.callback;
          if ("function" === typeof d3) {
            v.callback = null;
            y3 = v.priorityLevel;
            var e2 = d3(v.expirationTime <= b2);
            b2 = exports.unstable_now();
            "function" === typeof e2 ? v.callback = e2 : v === h3(r2) && k2(r2);
            G2(b2);
          } else k2(r2);
          v = h3(r2);
        }
        if (null !== v) var w2 = true;
        else {
          var m3 = h3(t2);
          null !== m3 && K2(H3, m3.startTime - b2);
          w2 = false;
        }
        return w2;
      } finally {
        v = null, y3 = c2, z2 = false;
      }
    }
    var N = false, O2 = null, L2 = -1, P = 5, Q2 = -1;
    function M3() {
      return exports.unstable_now() - Q2 < P ? false : true;
    }
    function R2() {
      if (null !== O2) {
        var a3 = exports.unstable_now();
        Q2 = a3;
        var b2 = true;
        try {
          b2 = O2(true, a3);
        } finally {
          b2 ? S3() : (N = false, O2 = null);
        }
      } else N = false;
    }
    var S3;
    if ("function" === typeof F) S3 = function() {
      F(R2);
    };
    else if ("undefined" !== typeof MessageChannel) {
      var T2 = new MessageChannel(), U = T2.port2;
      T2.port1.onmessage = R2;
      S3 = function() {
        U.postMessage(null);
      };
    } else S3 = function() {
      D2(R2, 0);
    };
    function I2(a3) {
      O2 = a3;
      N || (N = true, S3());
    }
    function K2(a3, b2) {
      L2 = D2(function() {
        a3(exports.unstable_now());
      }, b2);
    }
    exports.unstable_IdlePriority = 5;
    exports.unstable_ImmediatePriority = 1;
    exports.unstable_LowPriority = 4;
    exports.unstable_NormalPriority = 3;
    exports.unstable_Profiling = null;
    exports.unstable_UserBlockingPriority = 2;
    exports.unstable_cancelCallback = function(a3) {
      a3.callback = null;
    };
    exports.unstable_continueExecution = function() {
      A || z2 || (A = true, I2(J));
    };
    exports.unstable_forceFrameRate = function(a3) {
      0 > a3 || 125 < a3 ? console.error("forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported") : P = 0 < a3 ? Math.floor(1e3 / a3) : 5;
    };
    exports.unstable_getCurrentPriorityLevel = function() {
      return y3;
    };
    exports.unstable_getFirstCallbackNode = function() {
      return h3(r2);
    };
    exports.unstable_next = function(a3) {
      switch (y3) {
        case 1:
        case 2:
        case 3:
          var b2 = 3;
          break;
        default:
          b2 = y3;
      }
      var c2 = y3;
      y3 = b2;
      try {
        return a3();
      } finally {
        y3 = c2;
      }
    };
    exports.unstable_pauseExecution = function() {
    };
    exports.unstable_requestPaint = function() {
    };
    exports.unstable_runWithPriority = function(a3, b2) {
      switch (a3) {
        case 1:
        case 2:
        case 3:
        case 4:
        case 5:
          break;
        default:
          a3 = 3;
      }
      var c2 = y3;
      y3 = a3;
      try {
        return b2();
      } finally {
        y3 = c2;
      }
    };
    exports.unstable_scheduleCallback = function(a3, b2, c2) {
      var d3 = exports.unstable_now();
      "object" === typeof c2 && null !== c2 ? (c2 = c2.delay, c2 = "number" === typeof c2 && 0 < c2 ? d3 + c2 : d3) : c2 = d3;
      switch (a3) {
        case 1:
          var e2 = -1;
          break;
        case 2:
          e2 = 250;
          break;
        case 5:
          e2 = 1073741823;
          break;
        case 4:
          e2 = 1e4;
          break;
        default:
          e2 = 5e3;
      }
      e2 = c2 + e2;
      a3 = { id: u2++, callback: b2, priorityLevel: a3, startTime: c2, expirationTime: e2, sortIndex: -1 };
      c2 > d3 ? (a3.sortIndex = c2, f(t2, a3), null === h3(r2) && a3 === h3(t2) && (B2 ? (E2(L2), L2 = -1) : B2 = true, K2(H3, c2 - d3))) : (a3.sortIndex = e2, f(r2, a3), A || z2 || (A = true, I2(J)));
      return a3;
    };
    exports.unstable_shouldYield = M3;
    exports.unstable_wrapCallback = function(a3) {
      var b2 = y3;
      return function() {
        var c2 = y3;
        y3 = b2;
        try {
          return a3.apply(this, arguments);
        } finally {
          y3 = c2;
        }
      };
    };
  })(scheduler_production_min);
  return scheduler_production_min;
}
var hasRequiredScheduler;
function requireScheduler() {
  if (hasRequiredScheduler) return scheduler.exports;
  hasRequiredScheduler = 1;
  {
    scheduler.exports = requireScheduler_production_min();
  }
  return scheduler.exports;
}
/**
 * @license React
 * react-dom.production.min.js
 *
 * Copyright (c) Facebook, Inc. and its affiliates.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE file in the root directory of this source tree.
 */
var hasRequiredReactDom_production_min;
function requireReactDom_production_min() {
  if (hasRequiredReactDom_production_min) return reactDom_production_min;
  hasRequiredReactDom_production_min = 1;
  var aa = requireReact(), ca = requireScheduler();
  function p(a3) {
    for (var b2 = "https://reactjs.org/docs/error-decoder.html?invariant=" + a3, c2 = 1; c2 < arguments.length; c2++) b2 += "&args[]=" + encodeURIComponent(arguments[c2]);
    return "Minified React error #" + a3 + "; visit " + b2 + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
  }
  var da = /* @__PURE__ */ new Set(), ea = {};
  function fa(a3, b2) {
    ha(a3, b2);
    ha(a3 + "Capture", b2);
  }
  function ha(a3, b2) {
    ea[a3] = b2;
    for (a3 = 0; a3 < b2.length; a3++) da.add(b2[a3]);
  }
  var ia = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement), ja = Object.prototype.hasOwnProperty, ka = /^[:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD][:A-Z_a-z\u00C0-\u00D6\u00D8-\u00F6\u00F8-\u02FF\u0370-\u037D\u037F-\u1FFF\u200C-\u200D\u2070-\u218F\u2C00-\u2FEF\u3001-\uD7FF\uF900-\uFDCF\uFDF0-\uFFFD\-.0-9\u00B7\u0300-\u036F\u203F-\u2040]*$/, la = {}, ma = {};
  function oa(a3) {
    if (ja.call(ma, a3)) return true;
    if (ja.call(la, a3)) return false;
    if (ka.test(a3)) return ma[a3] = true;
    la[a3] = true;
    return false;
  }
  function pa(a3, b2, c2, d3) {
    if (null !== c2 && 0 === c2.type) return false;
    switch (typeof b2) {
      case "function":
      case "symbol":
        return true;
      case "boolean":
        if (d3) return false;
        if (null !== c2) return !c2.acceptsBooleans;
        a3 = a3.toLowerCase().slice(0, 5);
        return "data-" !== a3 && "aria-" !== a3;
      default:
        return false;
    }
  }
  function qa(a3, b2, c2, d3) {
    if (null === b2 || "undefined" === typeof b2 || pa(a3, b2, c2, d3)) return true;
    if (d3) return false;
    if (null !== c2) switch (c2.type) {
      case 3:
        return !b2;
      case 4:
        return false === b2;
      case 5:
        return isNaN(b2);
      case 6:
        return isNaN(b2) || 1 > b2;
    }
    return false;
  }
  function v(a3, b2, c2, d3, e2, f, g) {
    this.acceptsBooleans = 2 === b2 || 3 === b2 || 4 === b2;
    this.attributeName = d3;
    this.attributeNamespace = e2;
    this.mustUseProperty = c2;
    this.propertyName = a3;
    this.type = b2;
    this.sanitizeURL = f;
    this.removeEmptyString = g;
  }
  var z2 = {};
  "children dangerouslySetInnerHTML defaultValue defaultChecked innerHTML suppressContentEditableWarning suppressHydrationWarning style".split(" ").forEach(function(a3) {
    z2[a3] = new v(a3, 0, false, a3, null, false, false);
  });
  [["acceptCharset", "accept-charset"], ["className", "class"], ["htmlFor", "for"], ["httpEquiv", "http-equiv"]].forEach(function(a3) {
    var b2 = a3[0];
    z2[b2] = new v(b2, 1, false, a3[1], null, false, false);
  });
  ["contentEditable", "draggable", "spellCheck", "value"].forEach(function(a3) {
    z2[a3] = new v(a3, 2, false, a3.toLowerCase(), null, false, false);
  });
  ["autoReverse", "externalResourcesRequired", "focusable", "preserveAlpha"].forEach(function(a3) {
    z2[a3] = new v(a3, 2, false, a3, null, false, false);
  });
  "allowFullScreen async autoFocus autoPlay controls default defer disabled disablePictureInPicture disableRemotePlayback formNoValidate hidden loop noModule noValidate open playsInline readOnly required reversed scoped seamless itemScope".split(" ").forEach(function(a3) {
    z2[a3] = new v(a3, 3, false, a3.toLowerCase(), null, false, false);
  });
  ["checked", "multiple", "muted", "selected"].forEach(function(a3) {
    z2[a3] = new v(a3, 3, true, a3, null, false, false);
  });
  ["capture", "download"].forEach(function(a3) {
    z2[a3] = new v(a3, 4, false, a3, null, false, false);
  });
  ["cols", "rows", "size", "span"].forEach(function(a3) {
    z2[a3] = new v(a3, 6, false, a3, null, false, false);
  });
  ["rowSpan", "start"].forEach(function(a3) {
    z2[a3] = new v(a3, 5, false, a3.toLowerCase(), null, false, false);
  });
  var ra = /[\-:]([a-z])/g;
  function sa(a3) {
    return a3[1].toUpperCase();
  }
  "accent-height alignment-baseline arabic-form baseline-shift cap-height clip-path clip-rule color-interpolation color-interpolation-filters color-profile color-rendering dominant-baseline enable-background fill-opacity fill-rule flood-color flood-opacity font-family font-size font-size-adjust font-stretch font-style font-variant font-weight glyph-name glyph-orientation-horizontal glyph-orientation-vertical horiz-adv-x horiz-origin-x image-rendering letter-spacing lighting-color marker-end marker-mid marker-start overline-position overline-thickness paint-order panose-1 pointer-events rendering-intent shape-rendering stop-color stop-opacity strikethrough-position strikethrough-thickness stroke-dasharray stroke-dashoffset stroke-linecap stroke-linejoin stroke-miterlimit stroke-opacity stroke-width text-anchor text-decoration text-rendering underline-position underline-thickness unicode-bidi unicode-range units-per-em v-alphabetic v-hanging v-ideographic v-mathematical vector-effect vert-adv-y vert-origin-x vert-origin-y word-spacing writing-mode xmlns:xlink x-height".split(" ").forEach(function(a3) {
    var b2 = a3.replace(
      ra,
      sa
    );
    z2[b2] = new v(b2, 1, false, a3, null, false, false);
  });
  "xlink:actuate xlink:arcrole xlink:role xlink:show xlink:title xlink:type".split(" ").forEach(function(a3) {
    var b2 = a3.replace(ra, sa);
    z2[b2] = new v(b2, 1, false, a3, "http://www.w3.org/1999/xlink", false, false);
  });
  ["xml:base", "xml:lang", "xml:space"].forEach(function(a3) {
    var b2 = a3.replace(ra, sa);
    z2[b2] = new v(b2, 1, false, a3, "http://www.w3.org/XML/1998/namespace", false, false);
  });
  ["tabIndex", "crossOrigin"].forEach(function(a3) {
    z2[a3] = new v(a3, 1, false, a3.toLowerCase(), null, false, false);
  });
  z2.xlinkHref = new v("xlinkHref", 1, false, "xlink:href", "http://www.w3.org/1999/xlink", true, false);
  ["src", "href", "action", "formAction"].forEach(function(a3) {
    z2[a3] = new v(a3, 1, false, a3.toLowerCase(), null, true, true);
  });
  function ta(a3, b2, c2, d3) {
    var e2 = z2.hasOwnProperty(b2) ? z2[b2] : null;
    if (null !== e2 ? 0 !== e2.type : d3 || !(2 < b2.length) || "o" !== b2[0] && "O" !== b2[0] || "n" !== b2[1] && "N" !== b2[1]) qa(b2, c2, e2, d3) && (c2 = null), d3 || null === e2 ? oa(b2) && (null === c2 ? a3.removeAttribute(b2) : a3.setAttribute(b2, "" + c2)) : e2.mustUseProperty ? a3[e2.propertyName] = null === c2 ? 3 === e2.type ? false : "" : c2 : (b2 = e2.attributeName, d3 = e2.attributeNamespace, null === c2 ? a3.removeAttribute(b2) : (e2 = e2.type, c2 = 3 === e2 || 4 === e2 && true === c2 ? "" : "" + c2, d3 ? a3.setAttributeNS(d3, b2, c2) : a3.setAttribute(b2, c2)));
  }
  var ua = aa.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED, va = Symbol.for("react.element"), wa = Symbol.for("react.portal"), ya = Symbol.for("react.fragment"), za = Symbol.for("react.strict_mode"), Aa = Symbol.for("react.profiler"), Ba = Symbol.for("react.provider"), Ca = Symbol.for("react.context"), Da = Symbol.for("react.forward_ref"), Ea = Symbol.for("react.suspense"), Fa = Symbol.for("react.suspense_list"), Ga = Symbol.for("react.memo"), Ha = Symbol.for("react.lazy");
  var Ia = Symbol.for("react.offscreen");
  var Ja = Symbol.iterator;
  function Ka(a3) {
    if (null === a3 || "object" !== typeof a3) return null;
    a3 = Ja && a3[Ja] || a3["@@iterator"];
    return "function" === typeof a3 ? a3 : null;
  }
  var A = Object.assign, La;
  function Ma(a3) {
    if (void 0 === La) try {
      throw Error();
    } catch (c2) {
      var b2 = c2.stack.trim().match(/\n( *(at )?)/);
      La = b2 && b2[1] || "";
    }
    return "\n" + La + a3;
  }
  var Na = false;
  function Oa(a3, b2) {
    if (!a3 || Na) return "";
    Na = true;
    var c2 = Error.prepareStackTrace;
    Error.prepareStackTrace = void 0;
    try {
      if (b2) if (b2 = function() {
        throw Error();
      }, Object.defineProperty(b2.prototype, "props", { set: function() {
        throw Error();
      } }), "object" === typeof Reflect && Reflect.construct) {
        try {
          Reflect.construct(b2, []);
        } catch (l) {
          var d3 = l;
        }
        Reflect.construct(a3, [], b2);
      } else {
        try {
          b2.call();
        } catch (l) {
          d3 = l;
        }
        a3.call(b2.prototype);
      }
      else {
        try {
          throw Error();
        } catch (l) {
          d3 = l;
        }
        a3();
      }
    } catch (l) {
      if (l && d3 && "string" === typeof l.stack) {
        for (var e2 = l.stack.split("\n"), f = d3.stack.split("\n"), g = e2.length - 1, h3 = f.length - 1; 1 <= g && 0 <= h3 && e2[g] !== f[h3]; ) h3--;
        for (; 1 <= g && 0 <= h3; g--, h3--) if (e2[g] !== f[h3]) {
          if (1 !== g || 1 !== h3) {
            do
              if (g--, h3--, 0 > h3 || e2[g] !== f[h3]) {
                var k2 = "\n" + e2[g].replace(" at new ", " at ");
                a3.displayName && k2.includes("<anonymous>") && (k2 = k2.replace("<anonymous>", a3.displayName));
                return k2;
              }
            while (1 <= g && 0 <= h3);
          }
          break;
        }
      }
    } finally {
      Na = false, Error.prepareStackTrace = c2;
    }
    return (a3 = a3 ? a3.displayName || a3.name : "") ? Ma(a3) : "";
  }
  function Pa(a3) {
    switch (a3.tag) {
      case 5:
        return Ma(a3.type);
      case 16:
        return Ma("Lazy");
      case 13:
        return Ma("Suspense");
      case 19:
        return Ma("SuspenseList");
      case 0:
      case 2:
      case 15:
        return a3 = Oa(a3.type, false), a3;
      case 11:
        return a3 = Oa(a3.type.render, false), a3;
      case 1:
        return a3 = Oa(a3.type, true), a3;
      default:
        return "";
    }
  }
  function Qa(a3) {
    if (null == a3) return null;
    if ("function" === typeof a3) return a3.displayName || a3.name || null;
    if ("string" === typeof a3) return a3;
    switch (a3) {
      case ya:
        return "Fragment";
      case wa:
        return "Portal";
      case Aa:
        return "Profiler";
      case za:
        return "StrictMode";
      case Ea:
        return "Suspense";
      case Fa:
        return "SuspenseList";
    }
    if ("object" === typeof a3) switch (a3.$$typeof) {
      case Ca:
        return (a3.displayName || "Context") + ".Consumer";
      case Ba:
        return (a3._context.displayName || "Context") + ".Provider";
      case Da:
        var b2 = a3.render;
        a3 = a3.displayName;
        a3 || (a3 = b2.displayName || b2.name || "", a3 = "" !== a3 ? "ForwardRef(" + a3 + ")" : "ForwardRef");
        return a3;
      case Ga:
        return b2 = a3.displayName || null, null !== b2 ? b2 : Qa(a3.type) || "Memo";
      case Ha:
        b2 = a3._payload;
        a3 = a3._init;
        try {
          return Qa(a3(b2));
        } catch (c2) {
        }
    }
    return null;
  }
  function Ra(a3) {
    var b2 = a3.type;
    switch (a3.tag) {
      case 24:
        return "Cache";
      case 9:
        return (b2.displayName || "Context") + ".Consumer";
      case 10:
        return (b2._context.displayName || "Context") + ".Provider";
      case 18:
        return "DehydratedFragment";
      case 11:
        return a3 = b2.render, a3 = a3.displayName || a3.name || "", b2.displayName || ("" !== a3 ? "ForwardRef(" + a3 + ")" : "ForwardRef");
      case 7:
        return "Fragment";
      case 5:
        return b2;
      case 4:
        return "Portal";
      case 3:
        return "Root";
      case 6:
        return "Text";
      case 16:
        return Qa(b2);
      case 8:
        return b2 === za ? "StrictMode" : "Mode";
      case 22:
        return "Offscreen";
      case 12:
        return "Profiler";
      case 21:
        return "Scope";
      case 13:
        return "Suspense";
      case 19:
        return "SuspenseList";
      case 25:
        return "TracingMarker";
      case 1:
      case 0:
      case 17:
      case 2:
      case 14:
      case 15:
        if ("function" === typeof b2) return b2.displayName || b2.name || null;
        if ("string" === typeof b2) return b2;
    }
    return null;
  }
  function Sa(a3) {
    switch (typeof a3) {
      case "boolean":
      case "number":
      case "string":
      case "undefined":
        return a3;
      case "object":
        return a3;
      default:
        return "";
    }
  }
  function Ta(a3) {
    var b2 = a3.type;
    return (a3 = a3.nodeName) && "input" === a3.toLowerCase() && ("checkbox" === b2 || "radio" === b2);
  }
  function Ua(a3) {
    var b2 = Ta(a3) ? "checked" : "value", c2 = Object.getOwnPropertyDescriptor(a3.constructor.prototype, b2), d3 = "" + a3[b2];
    if (!a3.hasOwnProperty(b2) && "undefined" !== typeof c2 && "function" === typeof c2.get && "function" === typeof c2.set) {
      var e2 = c2.get, f = c2.set;
      Object.defineProperty(a3, b2, { configurable: true, get: function() {
        return e2.call(this);
      }, set: function(a4) {
        d3 = "" + a4;
        f.call(this, a4);
      } });
      Object.defineProperty(a3, b2, { enumerable: c2.enumerable });
      return { getValue: function() {
        return d3;
      }, setValue: function(a4) {
        d3 = "" + a4;
      }, stopTracking: function() {
        a3._valueTracker = null;
        delete a3[b2];
      } };
    }
  }
  function Va(a3) {
    a3._valueTracker || (a3._valueTracker = Ua(a3));
  }
  function Wa(a3) {
    if (!a3) return false;
    var b2 = a3._valueTracker;
    if (!b2) return true;
    var c2 = b2.getValue();
    var d3 = "";
    a3 && (d3 = Ta(a3) ? a3.checked ? "true" : "false" : a3.value);
    a3 = d3;
    return a3 !== c2 ? (b2.setValue(a3), true) : false;
  }
  function Xa(a3) {
    a3 = a3 || ("undefined" !== typeof document ? document : void 0);
    if ("undefined" === typeof a3) return null;
    try {
      return a3.activeElement || a3.body;
    } catch (b2) {
      return a3.body;
    }
  }
  function Ya(a3, b2) {
    var c2 = b2.checked;
    return A({}, b2, { defaultChecked: void 0, defaultValue: void 0, value: void 0, checked: null != c2 ? c2 : a3._wrapperState.initialChecked });
  }
  function Za(a3, b2) {
    var c2 = null == b2.defaultValue ? "" : b2.defaultValue, d3 = null != b2.checked ? b2.checked : b2.defaultChecked;
    c2 = Sa(null != b2.value ? b2.value : c2);
    a3._wrapperState = { initialChecked: d3, initialValue: c2, controlled: "checkbox" === b2.type || "radio" === b2.type ? null != b2.checked : null != b2.value };
  }
  function ab(a3, b2) {
    b2 = b2.checked;
    null != b2 && ta(a3, "checked", b2, false);
  }
  function bb(a3, b2) {
    ab(a3, b2);
    var c2 = Sa(b2.value), d3 = b2.type;
    if (null != c2) if ("number" === d3) {
      if (0 === c2 && "" === a3.value || a3.value != c2) a3.value = "" + c2;
    } else a3.value !== "" + c2 && (a3.value = "" + c2);
    else if ("submit" === d3 || "reset" === d3) {
      a3.removeAttribute("value");
      return;
    }
    b2.hasOwnProperty("value") ? cb(a3, b2.type, c2) : b2.hasOwnProperty("defaultValue") && cb(a3, b2.type, Sa(b2.defaultValue));
    null == b2.checked && null != b2.defaultChecked && (a3.defaultChecked = !!b2.defaultChecked);
  }
  function db(a3, b2, c2) {
    if (b2.hasOwnProperty("value") || b2.hasOwnProperty("defaultValue")) {
      var d3 = b2.type;
      if (!("submit" !== d3 && "reset" !== d3 || void 0 !== b2.value && null !== b2.value)) return;
      b2 = "" + a3._wrapperState.initialValue;
      c2 || b2 === a3.value || (a3.value = b2);
      a3.defaultValue = b2;
    }
    c2 = a3.name;
    "" !== c2 && (a3.name = "");
    a3.defaultChecked = !!a3._wrapperState.initialChecked;
    "" !== c2 && (a3.name = c2);
  }
  function cb(a3, b2, c2) {
    if ("number" !== b2 || Xa(a3.ownerDocument) !== a3) null == c2 ? a3.defaultValue = "" + a3._wrapperState.initialValue : a3.defaultValue !== "" + c2 && (a3.defaultValue = "" + c2);
  }
  var eb = Array.isArray;
  function fb(a3, b2, c2, d3) {
    a3 = a3.options;
    if (b2) {
      b2 = {};
      for (var e2 = 0; e2 < c2.length; e2++) b2["$" + c2[e2]] = true;
      for (c2 = 0; c2 < a3.length; c2++) e2 = b2.hasOwnProperty("$" + a3[c2].value), a3[c2].selected !== e2 && (a3[c2].selected = e2), e2 && d3 && (a3[c2].defaultSelected = true);
    } else {
      c2 = "" + Sa(c2);
      b2 = null;
      for (e2 = 0; e2 < a3.length; e2++) {
        if (a3[e2].value === c2) {
          a3[e2].selected = true;
          d3 && (a3[e2].defaultSelected = true);
          return;
        }
        null !== b2 || a3[e2].disabled || (b2 = a3[e2]);
      }
      null !== b2 && (b2.selected = true);
    }
  }
  function gb(a3, b2) {
    if (null != b2.dangerouslySetInnerHTML) throw Error(p(91));
    return A({}, b2, { value: void 0, defaultValue: void 0, children: "" + a3._wrapperState.initialValue });
  }
  function hb(a3, b2) {
    var c2 = b2.value;
    if (null == c2) {
      c2 = b2.children;
      b2 = b2.defaultValue;
      if (null != c2) {
        if (null != b2) throw Error(p(92));
        if (eb(c2)) {
          if (1 < c2.length) throw Error(p(93));
          c2 = c2[0];
        }
        b2 = c2;
      }
      null == b2 && (b2 = "");
      c2 = b2;
    }
    a3._wrapperState = { initialValue: Sa(c2) };
  }
  function ib(a3, b2) {
    var c2 = Sa(b2.value), d3 = Sa(b2.defaultValue);
    null != c2 && (c2 = "" + c2, c2 !== a3.value && (a3.value = c2), null == b2.defaultValue && a3.defaultValue !== c2 && (a3.defaultValue = c2));
    null != d3 && (a3.defaultValue = "" + d3);
  }
  function jb(a3) {
    var b2 = a3.textContent;
    b2 === a3._wrapperState.initialValue && "" !== b2 && null !== b2 && (a3.value = b2);
  }
  function kb(a3) {
    switch (a3) {
      case "svg":
        return "http://www.w3.org/2000/svg";
      case "math":
        return "http://www.w3.org/1998/Math/MathML";
      default:
        return "http://www.w3.org/1999/xhtml";
    }
  }
  function lb(a3, b2) {
    return null == a3 || "http://www.w3.org/1999/xhtml" === a3 ? kb(b2) : "http://www.w3.org/2000/svg" === a3 && "foreignObject" === b2 ? "http://www.w3.org/1999/xhtml" : a3;
  }
  var mb, nb = (function(a3) {
    return "undefined" !== typeof MSApp && MSApp.execUnsafeLocalFunction ? function(b2, c2, d3, e2) {
      MSApp.execUnsafeLocalFunction(function() {
        return a3(b2, c2, d3, e2);
      });
    } : a3;
  })(function(a3, b2) {
    if ("http://www.w3.org/2000/svg" !== a3.namespaceURI || "innerHTML" in a3) a3.innerHTML = b2;
    else {
      mb = mb || document.createElement("div");
      mb.innerHTML = "<svg>" + b2.valueOf().toString() + "</svg>";
      for (b2 = mb.firstChild; a3.firstChild; ) a3.removeChild(a3.firstChild);
      for (; b2.firstChild; ) a3.appendChild(b2.firstChild);
    }
  });
  function ob(a3, b2) {
    if (b2) {
      var c2 = a3.firstChild;
      if (c2 && c2 === a3.lastChild && 3 === c2.nodeType) {
        c2.nodeValue = b2;
        return;
      }
    }
    a3.textContent = b2;
  }
  var pb = {
    animationIterationCount: true,
    aspectRatio: true,
    borderImageOutset: true,
    borderImageSlice: true,
    borderImageWidth: true,
    boxFlex: true,
    boxFlexGroup: true,
    boxOrdinalGroup: true,
    columnCount: true,
    columns: true,
    flex: true,
    flexGrow: true,
    flexPositive: true,
    flexShrink: true,
    flexNegative: true,
    flexOrder: true,
    gridArea: true,
    gridRow: true,
    gridRowEnd: true,
    gridRowSpan: true,
    gridRowStart: true,
    gridColumn: true,
    gridColumnEnd: true,
    gridColumnSpan: true,
    gridColumnStart: true,
    fontWeight: true,
    lineClamp: true,
    lineHeight: true,
    opacity: true,
    order: true,
    orphans: true,
    tabSize: true,
    widows: true,
    zIndex: true,
    zoom: true,
    fillOpacity: true,
    floodOpacity: true,
    stopOpacity: true,
    strokeDasharray: true,
    strokeDashoffset: true,
    strokeMiterlimit: true,
    strokeOpacity: true,
    strokeWidth: true
  }, qb = ["Webkit", "ms", "Moz", "O"];
  Object.keys(pb).forEach(function(a3) {
    qb.forEach(function(b2) {
      b2 = b2 + a3.charAt(0).toUpperCase() + a3.substring(1);
      pb[b2] = pb[a3];
    });
  });
  function rb(a3, b2, c2) {
    return null == b2 || "boolean" === typeof b2 || "" === b2 ? "" : c2 || "number" !== typeof b2 || 0 === b2 || pb.hasOwnProperty(a3) && pb[a3] ? ("" + b2).trim() : b2 + "px";
  }
  function sb(a3, b2) {
    a3 = a3.style;
    for (var c2 in b2) if (b2.hasOwnProperty(c2)) {
      var d3 = 0 === c2.indexOf("--"), e2 = rb(c2, b2[c2], d3);
      "float" === c2 && (c2 = "cssFloat");
      d3 ? a3.setProperty(c2, e2) : a3[c2] = e2;
    }
  }
  var tb = A({ menuitem: true }, { area: true, base: true, br: true, col: true, embed: true, hr: true, img: true, input: true, keygen: true, link: true, meta: true, param: true, source: true, track: true, wbr: true });
  function ub(a3, b2) {
    if (b2) {
      if (tb[a3] && (null != b2.children || null != b2.dangerouslySetInnerHTML)) throw Error(p(137, a3));
      if (null != b2.dangerouslySetInnerHTML) {
        if (null != b2.children) throw Error(p(60));
        if ("object" !== typeof b2.dangerouslySetInnerHTML || !("__html" in b2.dangerouslySetInnerHTML)) throw Error(p(61));
      }
      if (null != b2.style && "object" !== typeof b2.style) throw Error(p(62));
    }
  }
  function vb(a3, b2) {
    if (-1 === a3.indexOf("-")) return "string" === typeof b2.is;
    switch (a3) {
      case "annotation-xml":
      case "color-profile":
      case "font-face":
      case "font-face-src":
      case "font-face-uri":
      case "font-face-format":
      case "font-face-name":
      case "missing-glyph":
        return false;
      default:
        return true;
    }
  }
  var wb = null;
  function xb(a3) {
    a3 = a3.target || a3.srcElement || window;
    a3.correspondingUseElement && (a3 = a3.correspondingUseElement);
    return 3 === a3.nodeType ? a3.parentNode : a3;
  }
  var yb = null, zb = null, Ab = null;
  function Bb(a3) {
    if (a3 = Cb(a3)) {
      if ("function" !== typeof yb) throw Error(p(280));
      var b2 = a3.stateNode;
      b2 && (b2 = Db(b2), yb(a3.stateNode, a3.type, b2));
    }
  }
  function Eb(a3) {
    zb ? Ab ? Ab.push(a3) : Ab = [a3] : zb = a3;
  }
  function Fb() {
    if (zb) {
      var a3 = zb, b2 = Ab;
      Ab = zb = null;
      Bb(a3);
      if (b2) for (a3 = 0; a3 < b2.length; a3++) Bb(b2[a3]);
    }
  }
  function Gb(a3, b2) {
    return a3(b2);
  }
  function Hb() {
  }
  var Ib = false;
  function Jb(a3, b2, c2) {
    if (Ib) return a3(b2, c2);
    Ib = true;
    try {
      return Gb(a3, b2, c2);
    } finally {
      if (Ib = false, null !== zb || null !== Ab) Hb(), Fb();
    }
  }
  function Kb(a3, b2) {
    var c2 = a3.stateNode;
    if (null === c2) return null;
    var d3 = Db(c2);
    if (null === d3) return null;
    c2 = d3[b2];
    a: switch (b2) {
      case "onClick":
      case "onClickCapture":
      case "onDoubleClick":
      case "onDoubleClickCapture":
      case "onMouseDown":
      case "onMouseDownCapture":
      case "onMouseMove":
      case "onMouseMoveCapture":
      case "onMouseUp":
      case "onMouseUpCapture":
      case "onMouseEnter":
        (d3 = !d3.disabled) || (a3 = a3.type, d3 = !("button" === a3 || "input" === a3 || "select" === a3 || "textarea" === a3));
        a3 = !d3;
        break a;
      default:
        a3 = false;
    }
    if (a3) return null;
    if (c2 && "function" !== typeof c2) throw Error(p(231, b2, typeof c2));
    return c2;
  }
  var Lb = false;
  if (ia) try {
    var Mb = {};
    Object.defineProperty(Mb, "passive", { get: function() {
      Lb = true;
    } });
    window.addEventListener("test", Mb, Mb);
    window.removeEventListener("test", Mb, Mb);
  } catch (a3) {
    Lb = false;
  }
  function Nb(a3, b2, c2, d3, e2, f, g, h3, k2) {
    var l = Array.prototype.slice.call(arguments, 3);
    try {
      b2.apply(c2, l);
    } catch (m3) {
      this.onError(m3);
    }
  }
  var Ob = false, Pb = null, Qb = false, Rb = null, Sb = { onError: function(a3) {
    Ob = true;
    Pb = a3;
  } };
  function Tb(a3, b2, c2, d3, e2, f, g, h3, k2) {
    Ob = false;
    Pb = null;
    Nb.apply(Sb, arguments);
  }
  function Ub(a3, b2, c2, d3, e2, f, g, h3, k2) {
    Tb.apply(this, arguments);
    if (Ob) {
      if (Ob) {
        var l = Pb;
        Ob = false;
        Pb = null;
      } else throw Error(p(198));
      Qb || (Qb = true, Rb = l);
    }
  }
  function Vb(a3) {
    var b2 = a3, c2 = a3;
    if (a3.alternate) for (; b2.return; ) b2 = b2.return;
    else {
      a3 = b2;
      do
        b2 = a3, 0 !== (b2.flags & 4098) && (c2 = b2.return), a3 = b2.return;
      while (a3);
    }
    return 3 === b2.tag ? c2 : null;
  }
  function Wb(a3) {
    if (13 === a3.tag) {
      var b2 = a3.memoizedState;
      null === b2 && (a3 = a3.alternate, null !== a3 && (b2 = a3.memoizedState));
      if (null !== b2) return b2.dehydrated;
    }
    return null;
  }
  function Xb(a3) {
    if (Vb(a3) !== a3) throw Error(p(188));
  }
  function Yb(a3) {
    var b2 = a3.alternate;
    if (!b2) {
      b2 = Vb(a3);
      if (null === b2) throw Error(p(188));
      return b2 !== a3 ? null : a3;
    }
    for (var c2 = a3, d3 = b2; ; ) {
      var e2 = c2.return;
      if (null === e2) break;
      var f = e2.alternate;
      if (null === f) {
        d3 = e2.return;
        if (null !== d3) {
          c2 = d3;
          continue;
        }
        break;
      }
      if (e2.child === f.child) {
        for (f = e2.child; f; ) {
          if (f === c2) return Xb(e2), a3;
          if (f === d3) return Xb(e2), b2;
          f = f.sibling;
        }
        throw Error(p(188));
      }
      if (c2.return !== d3.return) c2 = e2, d3 = f;
      else {
        for (var g = false, h3 = e2.child; h3; ) {
          if (h3 === c2) {
            g = true;
            c2 = e2;
            d3 = f;
            break;
          }
          if (h3 === d3) {
            g = true;
            d3 = e2;
            c2 = f;
            break;
          }
          h3 = h3.sibling;
        }
        if (!g) {
          for (h3 = f.child; h3; ) {
            if (h3 === c2) {
              g = true;
              c2 = f;
              d3 = e2;
              break;
            }
            if (h3 === d3) {
              g = true;
              d3 = f;
              c2 = e2;
              break;
            }
            h3 = h3.sibling;
          }
          if (!g) throw Error(p(189));
        }
      }
      if (c2.alternate !== d3) throw Error(p(190));
    }
    if (3 !== c2.tag) throw Error(p(188));
    return c2.stateNode.current === c2 ? a3 : b2;
  }
  function Zb(a3) {
    a3 = Yb(a3);
    return null !== a3 ? $b(a3) : null;
  }
  function $b(a3) {
    if (5 === a3.tag || 6 === a3.tag) return a3;
    for (a3 = a3.child; null !== a3; ) {
      var b2 = $b(a3);
      if (null !== b2) return b2;
      a3 = a3.sibling;
    }
    return null;
  }
  var ac = ca.unstable_scheduleCallback, bc = ca.unstable_cancelCallback, cc = ca.unstable_shouldYield, dc = ca.unstable_requestPaint, B2 = ca.unstable_now, ec = ca.unstable_getCurrentPriorityLevel, fc = ca.unstable_ImmediatePriority, gc = ca.unstable_UserBlockingPriority, hc = ca.unstable_NormalPriority, ic = ca.unstable_LowPriority, jc = ca.unstable_IdlePriority, kc = null, lc = null;
  function mc(a3) {
    if (lc && "function" === typeof lc.onCommitFiberRoot) try {
      lc.onCommitFiberRoot(kc, a3, void 0, 128 === (a3.current.flags & 128));
    } catch (b2) {
    }
  }
  var oc = Math.clz32 ? Math.clz32 : nc, pc = Math.log, qc = Math.LN2;
  function nc(a3) {
    a3 >>>= 0;
    return 0 === a3 ? 32 : 31 - (pc(a3) / qc | 0) | 0;
  }
  var rc = 64, sc = 4194304;
  function tc(a3) {
    switch (a3 & -a3) {
      case 1:
        return 1;
      case 2:
        return 2;
      case 4:
        return 4;
      case 8:
        return 8;
      case 16:
        return 16;
      case 32:
        return 32;
      case 64:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return a3 & 4194240;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
      case 67108864:
        return a3 & 130023424;
      case 134217728:
        return 134217728;
      case 268435456:
        return 268435456;
      case 536870912:
        return 536870912;
      case 1073741824:
        return 1073741824;
      default:
        return a3;
    }
  }
  function uc(a3, b2) {
    var c2 = a3.pendingLanes;
    if (0 === c2) return 0;
    var d3 = 0, e2 = a3.suspendedLanes, f = a3.pingedLanes, g = c2 & 268435455;
    if (0 !== g) {
      var h3 = g & ~e2;
      0 !== h3 ? d3 = tc(h3) : (f &= g, 0 !== f && (d3 = tc(f)));
    } else g = c2 & ~e2, 0 !== g ? d3 = tc(g) : 0 !== f && (d3 = tc(f));
    if (0 === d3) return 0;
    if (0 !== b2 && b2 !== d3 && 0 === (b2 & e2) && (e2 = d3 & -d3, f = b2 & -b2, e2 >= f || 16 === e2 && 0 !== (f & 4194240))) return b2;
    0 !== (d3 & 4) && (d3 |= c2 & 16);
    b2 = a3.entangledLanes;
    if (0 !== b2) for (a3 = a3.entanglements, b2 &= d3; 0 < b2; ) c2 = 31 - oc(b2), e2 = 1 << c2, d3 |= a3[c2], b2 &= ~e2;
    return d3;
  }
  function vc(a3, b2) {
    switch (a3) {
      case 1:
      case 2:
      case 4:
        return b2 + 250;
      case 8:
      case 16:
      case 32:
      case 64:
      case 128:
      case 256:
      case 512:
      case 1024:
      case 2048:
      case 4096:
      case 8192:
      case 16384:
      case 32768:
      case 65536:
      case 131072:
      case 262144:
      case 524288:
      case 1048576:
      case 2097152:
        return b2 + 5e3;
      case 4194304:
      case 8388608:
      case 16777216:
      case 33554432:
      case 67108864:
        return -1;
      case 134217728:
      case 268435456:
      case 536870912:
      case 1073741824:
        return -1;
      default:
        return -1;
    }
  }
  function wc(a3, b2) {
    for (var c2 = a3.suspendedLanes, d3 = a3.pingedLanes, e2 = a3.expirationTimes, f = a3.pendingLanes; 0 < f; ) {
      var g = 31 - oc(f), h3 = 1 << g, k2 = e2[g];
      if (-1 === k2) {
        if (0 === (h3 & c2) || 0 !== (h3 & d3)) e2[g] = vc(h3, b2);
      } else k2 <= b2 && (a3.expiredLanes |= h3);
      f &= ~h3;
    }
  }
  function xc(a3) {
    a3 = a3.pendingLanes & -1073741825;
    return 0 !== a3 ? a3 : a3 & 1073741824 ? 1073741824 : 0;
  }
  function yc() {
    var a3 = rc;
    rc <<= 1;
    0 === (rc & 4194240) && (rc = 64);
    return a3;
  }
  function zc(a3) {
    for (var b2 = [], c2 = 0; 31 > c2; c2++) b2.push(a3);
    return b2;
  }
  function Ac(a3, b2, c2) {
    a3.pendingLanes |= b2;
    536870912 !== b2 && (a3.suspendedLanes = 0, a3.pingedLanes = 0);
    a3 = a3.eventTimes;
    b2 = 31 - oc(b2);
    a3[b2] = c2;
  }
  function Bc(a3, b2) {
    var c2 = a3.pendingLanes & ~b2;
    a3.pendingLanes = b2;
    a3.suspendedLanes = 0;
    a3.pingedLanes = 0;
    a3.expiredLanes &= b2;
    a3.mutableReadLanes &= b2;
    a3.entangledLanes &= b2;
    b2 = a3.entanglements;
    var d3 = a3.eventTimes;
    for (a3 = a3.expirationTimes; 0 < c2; ) {
      var e2 = 31 - oc(c2), f = 1 << e2;
      b2[e2] = 0;
      d3[e2] = -1;
      a3[e2] = -1;
      c2 &= ~f;
    }
  }
  function Cc(a3, b2) {
    var c2 = a3.entangledLanes |= b2;
    for (a3 = a3.entanglements; c2; ) {
      var d3 = 31 - oc(c2), e2 = 1 << d3;
      e2 & b2 | a3[d3] & b2 && (a3[d3] |= b2);
      c2 &= ~e2;
    }
  }
  var C = 0;
  function Dc(a3) {
    a3 &= -a3;
    return 1 < a3 ? 4 < a3 ? 0 !== (a3 & 268435455) ? 16 : 536870912 : 4 : 1;
  }
  var Ec, Fc, Gc, Hc, Ic, Jc = false, Kc = [], Lc = null, Mc = null, Nc = null, Oc = /* @__PURE__ */ new Map(), Pc = /* @__PURE__ */ new Map(), Qc = [], Rc = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset submit".split(" ");
  function Sc(a3, b2) {
    switch (a3) {
      case "focusin":
      case "focusout":
        Lc = null;
        break;
      case "dragenter":
      case "dragleave":
        Mc = null;
        break;
      case "mouseover":
      case "mouseout":
        Nc = null;
        break;
      case "pointerover":
      case "pointerout":
        Oc.delete(b2.pointerId);
        break;
      case "gotpointercapture":
      case "lostpointercapture":
        Pc.delete(b2.pointerId);
    }
  }
  function Tc(a3, b2, c2, d3, e2, f) {
    if (null === a3 || a3.nativeEvent !== f) return a3 = { blockedOn: b2, domEventName: c2, eventSystemFlags: d3, nativeEvent: f, targetContainers: [e2] }, null !== b2 && (b2 = Cb(b2), null !== b2 && Fc(b2)), a3;
    a3.eventSystemFlags |= d3;
    b2 = a3.targetContainers;
    null !== e2 && -1 === b2.indexOf(e2) && b2.push(e2);
    return a3;
  }
  function Uc(a3, b2, c2, d3, e2) {
    switch (b2) {
      case "focusin":
        return Lc = Tc(Lc, a3, b2, c2, d3, e2), true;
      case "dragenter":
        return Mc = Tc(Mc, a3, b2, c2, d3, e2), true;
      case "mouseover":
        return Nc = Tc(Nc, a3, b2, c2, d3, e2), true;
      case "pointerover":
        var f = e2.pointerId;
        Oc.set(f, Tc(Oc.get(f) || null, a3, b2, c2, d3, e2));
        return true;
      case "gotpointercapture":
        return f = e2.pointerId, Pc.set(f, Tc(Pc.get(f) || null, a3, b2, c2, d3, e2)), true;
    }
    return false;
  }
  function Vc(a3) {
    var b2 = Wc(a3.target);
    if (null !== b2) {
      var c2 = Vb(b2);
      if (null !== c2) {
        if (b2 = c2.tag, 13 === b2) {
          if (b2 = Wb(c2), null !== b2) {
            a3.blockedOn = b2;
            Ic(a3.priority, function() {
              Gc(c2);
            });
            return;
          }
        } else if (3 === b2 && c2.stateNode.current.memoizedState.isDehydrated) {
          a3.blockedOn = 3 === c2.tag ? c2.stateNode.containerInfo : null;
          return;
        }
      }
    }
    a3.blockedOn = null;
  }
  function Xc(a3) {
    if (null !== a3.blockedOn) return false;
    for (var b2 = a3.targetContainers; 0 < b2.length; ) {
      var c2 = Yc(a3.domEventName, a3.eventSystemFlags, b2[0], a3.nativeEvent);
      if (null === c2) {
        c2 = a3.nativeEvent;
        var d3 = new c2.constructor(c2.type, c2);
        wb = d3;
        c2.target.dispatchEvent(d3);
        wb = null;
      } else return b2 = Cb(c2), null !== b2 && Fc(b2), a3.blockedOn = c2, false;
      b2.shift();
    }
    return true;
  }
  function Zc(a3, b2, c2) {
    Xc(a3) && c2.delete(b2);
  }
  function $c() {
    Jc = false;
    null !== Lc && Xc(Lc) && (Lc = null);
    null !== Mc && Xc(Mc) && (Mc = null);
    null !== Nc && Xc(Nc) && (Nc = null);
    Oc.forEach(Zc);
    Pc.forEach(Zc);
  }
  function ad(a3, b2) {
    a3.blockedOn === b2 && (a3.blockedOn = null, Jc || (Jc = true, ca.unstable_scheduleCallback(ca.unstable_NormalPriority, $c)));
  }
  function bd(a3) {
    function b2(b3) {
      return ad(b3, a3);
    }
    if (0 < Kc.length) {
      ad(Kc[0], a3);
      for (var c2 = 1; c2 < Kc.length; c2++) {
        var d3 = Kc[c2];
        d3.blockedOn === a3 && (d3.blockedOn = null);
      }
    }
    null !== Lc && ad(Lc, a3);
    null !== Mc && ad(Mc, a3);
    null !== Nc && ad(Nc, a3);
    Oc.forEach(b2);
    Pc.forEach(b2);
    for (c2 = 0; c2 < Qc.length; c2++) d3 = Qc[c2], d3.blockedOn === a3 && (d3.blockedOn = null);
    for (; 0 < Qc.length && (c2 = Qc[0], null === c2.blockedOn); ) Vc(c2), null === c2.blockedOn && Qc.shift();
  }
  var cd = ua.ReactCurrentBatchConfig, dd = true;
  function ed(a3, b2, c2, d3) {
    var e2 = C, f = cd.transition;
    cd.transition = null;
    try {
      C = 1, fd(a3, b2, c2, d3);
    } finally {
      C = e2, cd.transition = f;
    }
  }
  function gd(a3, b2, c2, d3) {
    var e2 = C, f = cd.transition;
    cd.transition = null;
    try {
      C = 4, fd(a3, b2, c2, d3);
    } finally {
      C = e2, cd.transition = f;
    }
  }
  function fd(a3, b2, c2, d3) {
    if (dd) {
      var e2 = Yc(a3, b2, c2, d3);
      if (null === e2) hd(a3, b2, d3, id2, c2), Sc(a3, d3);
      else if (Uc(e2, a3, b2, c2, d3)) d3.stopPropagation();
      else if (Sc(a3, d3), b2 & 4 && -1 < Rc.indexOf(a3)) {
        for (; null !== e2; ) {
          var f = Cb(e2);
          null !== f && Ec(f);
          f = Yc(a3, b2, c2, d3);
          null === f && hd(a3, b2, d3, id2, c2);
          if (f === e2) break;
          e2 = f;
        }
        null !== e2 && d3.stopPropagation();
      } else hd(a3, b2, d3, null, c2);
    }
  }
  var id2 = null;
  function Yc(a3, b2, c2, d3) {
    id2 = null;
    a3 = xb(d3);
    a3 = Wc(a3);
    if (null !== a3) if (b2 = Vb(a3), null === b2) a3 = null;
    else if (c2 = b2.tag, 13 === c2) {
      a3 = Wb(b2);
      if (null !== a3) return a3;
      a3 = null;
    } else if (3 === c2) {
      if (b2.stateNode.current.memoizedState.isDehydrated) return 3 === b2.tag ? b2.stateNode.containerInfo : null;
      a3 = null;
    } else b2 !== a3 && (a3 = null);
    id2 = a3;
    return null;
  }
  function jd(a3) {
    switch (a3) {
      case "cancel":
      case "click":
      case "close":
      case "contextmenu":
      case "copy":
      case "cut":
      case "auxclick":
      case "dblclick":
      case "dragend":
      case "dragstart":
      case "drop":
      case "focusin":
      case "focusout":
      case "input":
      case "invalid":
      case "keydown":
      case "keypress":
      case "keyup":
      case "mousedown":
      case "mouseup":
      case "paste":
      case "pause":
      case "play":
      case "pointercancel":
      case "pointerdown":
      case "pointerup":
      case "ratechange":
      case "reset":
      case "resize":
      case "seeked":
      case "submit":
      case "touchcancel":
      case "touchend":
      case "touchstart":
      case "volumechange":
      case "change":
      case "selectionchange":
      case "textInput":
      case "compositionstart":
      case "compositionend":
      case "compositionupdate":
      case "beforeblur":
      case "afterblur":
      case "beforeinput":
      case "blur":
      case "fullscreenchange":
      case "focus":
      case "hashchange":
      case "popstate":
      case "select":
      case "selectstart":
        return 1;
      case "drag":
      case "dragenter":
      case "dragexit":
      case "dragleave":
      case "dragover":
      case "mousemove":
      case "mouseout":
      case "mouseover":
      case "pointermove":
      case "pointerout":
      case "pointerover":
      case "scroll":
      case "toggle":
      case "touchmove":
      case "wheel":
      case "mouseenter":
      case "mouseleave":
      case "pointerenter":
      case "pointerleave":
        return 4;
      case "message":
        switch (ec()) {
          case fc:
            return 1;
          case gc:
            return 4;
          case hc:
          case ic:
            return 16;
          case jc:
            return 536870912;
          default:
            return 16;
        }
      default:
        return 16;
    }
  }
  var kd = null, ld = null, md = null;
  function nd() {
    if (md) return md;
    var a3, b2 = ld, c2 = b2.length, d3, e2 = "value" in kd ? kd.value : kd.textContent, f = e2.length;
    for (a3 = 0; a3 < c2 && b2[a3] === e2[a3]; a3++) ;
    var g = c2 - a3;
    for (d3 = 1; d3 <= g && b2[c2 - d3] === e2[f - d3]; d3++) ;
    return md = e2.slice(a3, 1 < d3 ? 1 - d3 : void 0);
  }
  function od(a3) {
    var b2 = a3.keyCode;
    "charCode" in a3 ? (a3 = a3.charCode, 0 === a3 && 13 === b2 && (a3 = 13)) : a3 = b2;
    10 === a3 && (a3 = 13);
    return 32 <= a3 || 13 === a3 ? a3 : 0;
  }
  function pd() {
    return true;
  }
  function qd() {
    return false;
  }
  function rd(a3) {
    function b2(b3, d3, e2, f, g) {
      this._reactName = b3;
      this._targetInst = e2;
      this.type = d3;
      this.nativeEvent = f;
      this.target = g;
      this.currentTarget = null;
      for (var c2 in a3) a3.hasOwnProperty(c2) && (b3 = a3[c2], this[c2] = b3 ? b3(f) : f[c2]);
      this.isDefaultPrevented = (null != f.defaultPrevented ? f.defaultPrevented : false === f.returnValue) ? pd : qd;
      this.isPropagationStopped = qd;
      return this;
    }
    A(b2.prototype, { preventDefault: function() {
      this.defaultPrevented = true;
      var a4 = this.nativeEvent;
      a4 && (a4.preventDefault ? a4.preventDefault() : "unknown" !== typeof a4.returnValue && (a4.returnValue = false), this.isDefaultPrevented = pd);
    }, stopPropagation: function() {
      var a4 = this.nativeEvent;
      a4 && (a4.stopPropagation ? a4.stopPropagation() : "unknown" !== typeof a4.cancelBubble && (a4.cancelBubble = true), this.isPropagationStopped = pd);
    }, persist: function() {
    }, isPersistent: pd });
    return b2;
  }
  var sd = { eventPhase: 0, bubbles: 0, cancelable: 0, timeStamp: function(a3) {
    return a3.timeStamp || Date.now();
  }, defaultPrevented: 0, isTrusted: 0 }, td = rd(sd), ud = A({}, sd, { view: 0, detail: 0 }), vd = rd(ud), wd, xd, yd, Ad = A({}, ud, { screenX: 0, screenY: 0, clientX: 0, clientY: 0, pageX: 0, pageY: 0, ctrlKey: 0, shiftKey: 0, altKey: 0, metaKey: 0, getModifierState: zd, button: 0, buttons: 0, relatedTarget: function(a3) {
    return void 0 === a3.relatedTarget ? a3.fromElement === a3.srcElement ? a3.toElement : a3.fromElement : a3.relatedTarget;
  }, movementX: function(a3) {
    if ("movementX" in a3) return a3.movementX;
    a3 !== yd && (yd && "mousemove" === a3.type ? (wd = a3.screenX - yd.screenX, xd = a3.screenY - yd.screenY) : xd = wd = 0, yd = a3);
    return wd;
  }, movementY: function(a3) {
    return "movementY" in a3 ? a3.movementY : xd;
  } }), Bd = rd(Ad), Cd = A({}, Ad, { dataTransfer: 0 }), Dd = rd(Cd), Ed = A({}, ud, { relatedTarget: 0 }), Fd = rd(Ed), Gd = A({}, sd, { animationName: 0, elapsedTime: 0, pseudoElement: 0 }), Hd = rd(Gd), Id = A({}, sd, { clipboardData: function(a3) {
    return "clipboardData" in a3 ? a3.clipboardData : window.clipboardData;
  } }), Jd = rd(Id), Kd = A({}, sd, { data: 0 }), Ld = rd(Kd), Md = {
    Esc: "Escape",
    Spacebar: " ",
    Left: "ArrowLeft",
    Up: "ArrowUp",
    Right: "ArrowRight",
    Down: "ArrowDown",
    Del: "Delete",
    Win: "OS",
    Menu: "ContextMenu",
    Apps: "ContextMenu",
    Scroll: "ScrollLock",
    MozPrintableKey: "Unidentified"
  }, Nd = {
    8: "Backspace",
    9: "Tab",
    12: "Clear",
    13: "Enter",
    16: "Shift",
    17: "Control",
    18: "Alt",
    19: "Pause",
    20: "CapsLock",
    27: "Escape",
    32: " ",
    33: "PageUp",
    34: "PageDown",
    35: "End",
    36: "Home",
    37: "ArrowLeft",
    38: "ArrowUp",
    39: "ArrowRight",
    40: "ArrowDown",
    45: "Insert",
    46: "Delete",
    112: "F1",
    113: "F2",
    114: "F3",
    115: "F4",
    116: "F5",
    117: "F6",
    118: "F7",
    119: "F8",
    120: "F9",
    121: "F10",
    122: "F11",
    123: "F12",
    144: "NumLock",
    145: "ScrollLock",
    224: "Meta"
  }, Od = { Alt: "altKey", Control: "ctrlKey", Meta: "metaKey", Shift: "shiftKey" };
  function Pd(a3) {
    var b2 = this.nativeEvent;
    return b2.getModifierState ? b2.getModifierState(a3) : (a3 = Od[a3]) ? !!b2[a3] : false;
  }
  function zd() {
    return Pd;
  }
  var Qd = A({}, ud, { key: function(a3) {
    if (a3.key) {
      var b2 = Md[a3.key] || a3.key;
      if ("Unidentified" !== b2) return b2;
    }
    return "keypress" === a3.type ? (a3 = od(a3), 13 === a3 ? "Enter" : String.fromCharCode(a3)) : "keydown" === a3.type || "keyup" === a3.type ? Nd[a3.keyCode] || "Unidentified" : "";
  }, code: 0, location: 0, ctrlKey: 0, shiftKey: 0, altKey: 0, metaKey: 0, repeat: 0, locale: 0, getModifierState: zd, charCode: function(a3) {
    return "keypress" === a3.type ? od(a3) : 0;
  }, keyCode: function(a3) {
    return "keydown" === a3.type || "keyup" === a3.type ? a3.keyCode : 0;
  }, which: function(a3) {
    return "keypress" === a3.type ? od(a3) : "keydown" === a3.type || "keyup" === a3.type ? a3.keyCode : 0;
  } }), Rd = rd(Qd), Sd = A({}, Ad, { pointerId: 0, width: 0, height: 0, pressure: 0, tangentialPressure: 0, tiltX: 0, tiltY: 0, twist: 0, pointerType: 0, isPrimary: 0 }), Td = rd(Sd), Ud = A({}, ud, { touches: 0, targetTouches: 0, changedTouches: 0, altKey: 0, metaKey: 0, ctrlKey: 0, shiftKey: 0, getModifierState: zd }), Vd = rd(Ud), Wd = A({}, sd, { propertyName: 0, elapsedTime: 0, pseudoElement: 0 }), Xd = rd(Wd), Yd = A({}, Ad, {
    deltaX: function(a3) {
      return "deltaX" in a3 ? a3.deltaX : "wheelDeltaX" in a3 ? -a3.wheelDeltaX : 0;
    },
    deltaY: function(a3) {
      return "deltaY" in a3 ? a3.deltaY : "wheelDeltaY" in a3 ? -a3.wheelDeltaY : "wheelDelta" in a3 ? -a3.wheelDelta : 0;
    },
    deltaZ: 0,
    deltaMode: 0
  }), Zd = rd(Yd), $d = [9, 13, 27, 32], ae = ia && "CompositionEvent" in window, be = null;
  ia && "documentMode" in document && (be = document.documentMode);
  var ce = ia && "TextEvent" in window && !be, de = ia && (!ae || be && 8 < be && 11 >= be), ee = String.fromCharCode(32), fe = false;
  function ge(a3, b2) {
    switch (a3) {
      case "keyup":
        return -1 !== $d.indexOf(b2.keyCode);
      case "keydown":
        return 229 !== b2.keyCode;
      case "keypress":
      case "mousedown":
      case "focusout":
        return true;
      default:
        return false;
    }
  }
  function he(a3) {
    a3 = a3.detail;
    return "object" === typeof a3 && "data" in a3 ? a3.data : null;
  }
  var ie = false;
  function je(a3, b2) {
    switch (a3) {
      case "compositionend":
        return he(b2);
      case "keypress":
        if (32 !== b2.which) return null;
        fe = true;
        return ee;
      case "textInput":
        return a3 = b2.data, a3 === ee && fe ? null : a3;
      default:
        return null;
    }
  }
  function ke(a3, b2) {
    if (ie) return "compositionend" === a3 || !ae && ge(a3, b2) ? (a3 = nd(), md = ld = kd = null, ie = false, a3) : null;
    switch (a3) {
      case "paste":
        return null;
      case "keypress":
        if (!(b2.ctrlKey || b2.altKey || b2.metaKey) || b2.ctrlKey && b2.altKey) {
          if (b2.char && 1 < b2.char.length) return b2.char;
          if (b2.which) return String.fromCharCode(b2.which);
        }
        return null;
      case "compositionend":
        return de && "ko" !== b2.locale ? null : b2.data;
      default:
        return null;
    }
  }
  var le = { color: true, date: true, datetime: true, "datetime-local": true, email: true, month: true, number: true, password: true, range: true, search: true, tel: true, text: true, time: true, url: true, week: true };
  function me(a3) {
    var b2 = a3 && a3.nodeName && a3.nodeName.toLowerCase();
    return "input" === b2 ? !!le[a3.type] : "textarea" === b2 ? true : false;
  }
  function ne(a3, b2, c2, d3) {
    Eb(d3);
    b2 = oe(b2, "onChange");
    0 < b2.length && (c2 = new td("onChange", "change", null, c2, d3), a3.push({ event: c2, listeners: b2 }));
  }
  var pe = null, qe = null;
  function re(a3) {
    se(a3, 0);
  }
  function te(a3) {
    var b2 = ue(a3);
    if (Wa(b2)) return a3;
  }
  function ve(a3, b2) {
    if ("change" === a3) return b2;
  }
  var we = false;
  if (ia) {
    var xe;
    if (ia) {
      var ye = "oninput" in document;
      if (!ye) {
        var ze = document.createElement("div");
        ze.setAttribute("oninput", "return;");
        ye = "function" === typeof ze.oninput;
      }
      xe = ye;
    } else xe = false;
    we = xe && (!document.documentMode || 9 < document.documentMode);
  }
  function Ae() {
    pe && (pe.detachEvent("onpropertychange", Be), qe = pe = null);
  }
  function Be(a3) {
    if ("value" === a3.propertyName && te(qe)) {
      var b2 = [];
      ne(b2, qe, a3, xb(a3));
      Jb(re, b2);
    }
  }
  function Ce(a3, b2, c2) {
    "focusin" === a3 ? (Ae(), pe = b2, qe = c2, pe.attachEvent("onpropertychange", Be)) : "focusout" === a3 && Ae();
  }
  function De(a3) {
    if ("selectionchange" === a3 || "keyup" === a3 || "keydown" === a3) return te(qe);
  }
  function Ee(a3, b2) {
    if ("click" === a3) return te(b2);
  }
  function Fe(a3, b2) {
    if ("input" === a3 || "change" === a3) return te(b2);
  }
  function Ge(a3, b2) {
    return a3 === b2 && (0 !== a3 || 1 / a3 === 1 / b2) || a3 !== a3 && b2 !== b2;
  }
  var He = "function" === typeof Object.is ? Object.is : Ge;
  function Ie(a3, b2) {
    if (He(a3, b2)) return true;
    if ("object" !== typeof a3 || null === a3 || "object" !== typeof b2 || null === b2) return false;
    var c2 = Object.keys(a3), d3 = Object.keys(b2);
    if (c2.length !== d3.length) return false;
    for (d3 = 0; d3 < c2.length; d3++) {
      var e2 = c2[d3];
      if (!ja.call(b2, e2) || !He(a3[e2], b2[e2])) return false;
    }
    return true;
  }
  function Je(a3) {
    for (; a3 && a3.firstChild; ) a3 = a3.firstChild;
    return a3;
  }
  function Ke(a3, b2) {
    var c2 = Je(a3);
    a3 = 0;
    for (var d3; c2; ) {
      if (3 === c2.nodeType) {
        d3 = a3 + c2.textContent.length;
        if (a3 <= b2 && d3 >= b2) return { node: c2, offset: b2 - a3 };
        a3 = d3;
      }
      a: {
        for (; c2; ) {
          if (c2.nextSibling) {
            c2 = c2.nextSibling;
            break a;
          }
          c2 = c2.parentNode;
        }
        c2 = void 0;
      }
      c2 = Je(c2);
    }
  }
  function Le(a3, b2) {
    return a3 && b2 ? a3 === b2 ? true : a3 && 3 === a3.nodeType ? false : b2 && 3 === b2.nodeType ? Le(a3, b2.parentNode) : "contains" in a3 ? a3.contains(b2) : a3.compareDocumentPosition ? !!(a3.compareDocumentPosition(b2) & 16) : false : false;
  }
  function Me() {
    for (var a3 = window, b2 = Xa(); b2 instanceof a3.HTMLIFrameElement; ) {
      try {
        var c2 = "string" === typeof b2.contentWindow.location.href;
      } catch (d3) {
        c2 = false;
      }
      if (c2) a3 = b2.contentWindow;
      else break;
      b2 = Xa(a3.document);
    }
    return b2;
  }
  function Ne(a3) {
    var b2 = a3 && a3.nodeName && a3.nodeName.toLowerCase();
    return b2 && ("input" === b2 && ("text" === a3.type || "search" === a3.type || "tel" === a3.type || "url" === a3.type || "password" === a3.type) || "textarea" === b2 || "true" === a3.contentEditable);
  }
  function Oe(a3) {
    var b2 = Me(), c2 = a3.focusedElem, d3 = a3.selectionRange;
    if (b2 !== c2 && c2 && c2.ownerDocument && Le(c2.ownerDocument.documentElement, c2)) {
      if (null !== d3 && Ne(c2)) {
        if (b2 = d3.start, a3 = d3.end, void 0 === a3 && (a3 = b2), "selectionStart" in c2) c2.selectionStart = b2, c2.selectionEnd = Math.min(a3, c2.value.length);
        else if (a3 = (b2 = c2.ownerDocument || document) && b2.defaultView || window, a3.getSelection) {
          a3 = a3.getSelection();
          var e2 = c2.textContent.length, f = Math.min(d3.start, e2);
          d3 = void 0 === d3.end ? f : Math.min(d3.end, e2);
          !a3.extend && f > d3 && (e2 = d3, d3 = f, f = e2);
          e2 = Ke(c2, f);
          var g = Ke(
            c2,
            d3
          );
          e2 && g && (1 !== a3.rangeCount || a3.anchorNode !== e2.node || a3.anchorOffset !== e2.offset || a3.focusNode !== g.node || a3.focusOffset !== g.offset) && (b2 = b2.createRange(), b2.setStart(e2.node, e2.offset), a3.removeAllRanges(), f > d3 ? (a3.addRange(b2), a3.extend(g.node, g.offset)) : (b2.setEnd(g.node, g.offset), a3.addRange(b2)));
        }
      }
      b2 = [];
      for (a3 = c2; a3 = a3.parentNode; ) 1 === a3.nodeType && b2.push({ element: a3, left: a3.scrollLeft, top: a3.scrollTop });
      "function" === typeof c2.focus && c2.focus();
      for (c2 = 0; c2 < b2.length; c2++) a3 = b2[c2], a3.element.scrollLeft = a3.left, a3.element.scrollTop = a3.top;
    }
  }
  var Pe = ia && "documentMode" in document && 11 >= document.documentMode, Qe = null, Re = null, Se = null, Te = false;
  function Ue(a3, b2, c2) {
    var d3 = c2.window === c2 ? c2.document : 9 === c2.nodeType ? c2 : c2.ownerDocument;
    Te || null == Qe || Qe !== Xa(d3) || (d3 = Qe, "selectionStart" in d3 && Ne(d3) ? d3 = { start: d3.selectionStart, end: d3.selectionEnd } : (d3 = (d3.ownerDocument && d3.ownerDocument.defaultView || window).getSelection(), d3 = { anchorNode: d3.anchorNode, anchorOffset: d3.anchorOffset, focusNode: d3.focusNode, focusOffset: d3.focusOffset }), Se && Ie(Se, d3) || (Se = d3, d3 = oe(Re, "onSelect"), 0 < d3.length && (b2 = new td("onSelect", "select", null, b2, c2), a3.push({ event: b2, listeners: d3 }), b2.target = Qe)));
  }
  function Ve(a3, b2) {
    var c2 = {};
    c2[a3.toLowerCase()] = b2.toLowerCase();
    c2["Webkit" + a3] = "webkit" + b2;
    c2["Moz" + a3] = "moz" + b2;
    return c2;
  }
  var We = { animationend: Ve("Animation", "AnimationEnd"), animationiteration: Ve("Animation", "AnimationIteration"), animationstart: Ve("Animation", "AnimationStart"), transitionend: Ve("Transition", "TransitionEnd") }, Xe = {}, Ye = {};
  ia && (Ye = document.createElement("div").style, "AnimationEvent" in window || (delete We.animationend.animation, delete We.animationiteration.animation, delete We.animationstart.animation), "TransitionEvent" in window || delete We.transitionend.transition);
  function Ze(a3) {
    if (Xe[a3]) return Xe[a3];
    if (!We[a3]) return a3;
    var b2 = We[a3], c2;
    for (c2 in b2) if (b2.hasOwnProperty(c2) && c2 in Ye) return Xe[a3] = b2[c2];
    return a3;
  }
  var $e = Ze("animationend"), af = Ze("animationiteration"), bf = Ze("animationstart"), cf = Ze("transitionend"), df = /* @__PURE__ */ new Map(), ef = "abort auxClick cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(" ");
  function ff(a3, b2) {
    df.set(a3, b2);
    fa(b2, [a3]);
  }
  for (var gf = 0; gf < ef.length; gf++) {
    var hf = ef[gf], jf = hf.toLowerCase(), kf = hf[0].toUpperCase() + hf.slice(1);
    ff(jf, "on" + kf);
  }
  ff($e, "onAnimationEnd");
  ff(af, "onAnimationIteration");
  ff(bf, "onAnimationStart");
  ff("dblclick", "onDoubleClick");
  ff("focusin", "onFocus");
  ff("focusout", "onBlur");
  ff(cf, "onTransitionEnd");
  ha("onMouseEnter", ["mouseout", "mouseover"]);
  ha("onMouseLeave", ["mouseout", "mouseover"]);
  ha("onPointerEnter", ["pointerout", "pointerover"]);
  ha("onPointerLeave", ["pointerout", "pointerover"]);
  fa("onChange", "change click focusin focusout input keydown keyup selectionchange".split(" "));
  fa("onSelect", "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(" "));
  fa("onBeforeInput", ["compositionend", "keypress", "textInput", "paste"]);
  fa("onCompositionEnd", "compositionend focusout keydown keypress keyup mousedown".split(" "));
  fa("onCompositionStart", "compositionstart focusout keydown keypress keyup mousedown".split(" "));
  fa("onCompositionUpdate", "compositionupdate focusout keydown keypress keyup mousedown".split(" "));
  var lf = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(" "), mf = new Set("cancel close invalid load scroll toggle".split(" ").concat(lf));
  function nf(a3, b2, c2) {
    var d3 = a3.type || "unknown-event";
    a3.currentTarget = c2;
    Ub(d3, b2, void 0, a3);
    a3.currentTarget = null;
  }
  function se(a3, b2) {
    b2 = 0 !== (b2 & 4);
    for (var c2 = 0; c2 < a3.length; c2++) {
      var d3 = a3[c2], e2 = d3.event;
      d3 = d3.listeners;
      a: {
        var f = void 0;
        if (b2) for (var g = d3.length - 1; 0 <= g; g--) {
          var h3 = d3[g], k2 = h3.instance, l = h3.currentTarget;
          h3 = h3.listener;
          if (k2 !== f && e2.isPropagationStopped()) break a;
          nf(e2, h3, l);
          f = k2;
        }
        else for (g = 0; g < d3.length; g++) {
          h3 = d3[g];
          k2 = h3.instance;
          l = h3.currentTarget;
          h3 = h3.listener;
          if (k2 !== f && e2.isPropagationStopped()) break a;
          nf(e2, h3, l);
          f = k2;
        }
      }
    }
    if (Qb) throw a3 = Rb, Qb = false, Rb = null, a3;
  }
  function D2(a3, b2) {
    var c2 = b2[of];
    void 0 === c2 && (c2 = b2[of] = /* @__PURE__ */ new Set());
    var d3 = a3 + "__bubble";
    c2.has(d3) || (pf(b2, a3, 2, false), c2.add(d3));
  }
  function qf(a3, b2, c2) {
    var d3 = 0;
    b2 && (d3 |= 4);
    pf(c2, a3, d3, b2);
  }
  var rf = "_reactListening" + Math.random().toString(36).slice(2);
  function sf(a3) {
    if (!a3[rf]) {
      a3[rf] = true;
      da.forEach(function(b3) {
        "selectionchange" !== b3 && (mf.has(b3) || qf(b3, false, a3), qf(b3, true, a3));
      });
      var b2 = 9 === a3.nodeType ? a3 : a3.ownerDocument;
      null === b2 || b2[rf] || (b2[rf] = true, qf("selectionchange", false, b2));
    }
  }
  function pf(a3, b2, c2, d3) {
    switch (jd(b2)) {
      case 1:
        var e2 = ed;
        break;
      case 4:
        e2 = gd;
        break;
      default:
        e2 = fd;
    }
    c2 = e2.bind(null, b2, c2, a3);
    e2 = void 0;
    !Lb || "touchstart" !== b2 && "touchmove" !== b2 && "wheel" !== b2 || (e2 = true);
    d3 ? void 0 !== e2 ? a3.addEventListener(b2, c2, { capture: true, passive: e2 }) : a3.addEventListener(b2, c2, true) : void 0 !== e2 ? a3.addEventListener(b2, c2, { passive: e2 }) : a3.addEventListener(b2, c2, false);
  }
  function hd(a3, b2, c2, d3, e2) {
    var f = d3;
    if (0 === (b2 & 1) && 0 === (b2 & 2) && null !== d3) a: for (; ; ) {
      if (null === d3) return;
      var g = d3.tag;
      if (3 === g || 4 === g) {
        var h3 = d3.stateNode.containerInfo;
        if (h3 === e2 || 8 === h3.nodeType && h3.parentNode === e2) break;
        if (4 === g) for (g = d3.return; null !== g; ) {
          var k2 = g.tag;
          if (3 === k2 || 4 === k2) {
            if (k2 = g.stateNode.containerInfo, k2 === e2 || 8 === k2.nodeType && k2.parentNode === e2) return;
          }
          g = g.return;
        }
        for (; null !== h3; ) {
          g = Wc(h3);
          if (null === g) return;
          k2 = g.tag;
          if (5 === k2 || 6 === k2) {
            d3 = f = g;
            continue a;
          }
          h3 = h3.parentNode;
        }
      }
      d3 = d3.return;
    }
    Jb(function() {
      var d4 = f, e3 = xb(c2), g2 = [];
      a: {
        var h4 = df.get(a3);
        if (void 0 !== h4) {
          var k3 = td, n = a3;
          switch (a3) {
            case "keypress":
              if (0 === od(c2)) break a;
            case "keydown":
            case "keyup":
              k3 = Rd;
              break;
            case "focusin":
              n = "focus";
              k3 = Fd;
              break;
            case "focusout":
              n = "blur";
              k3 = Fd;
              break;
            case "beforeblur":
            case "afterblur":
              k3 = Fd;
              break;
            case "click":
              if (2 === c2.button) break a;
            case "auxclick":
            case "dblclick":
            case "mousedown":
            case "mousemove":
            case "mouseup":
            case "mouseout":
            case "mouseover":
            case "contextmenu":
              k3 = Bd;
              break;
            case "drag":
            case "dragend":
            case "dragenter":
            case "dragexit":
            case "dragleave":
            case "dragover":
            case "dragstart":
            case "drop":
              k3 = Dd;
              break;
            case "touchcancel":
            case "touchend":
            case "touchmove":
            case "touchstart":
              k3 = Vd;
              break;
            case $e:
            case af:
            case bf:
              k3 = Hd;
              break;
            case cf:
              k3 = Xd;
              break;
            case "scroll":
              k3 = vd;
              break;
            case "wheel":
              k3 = Zd;
              break;
            case "copy":
            case "cut":
            case "paste":
              k3 = Jd;
              break;
            case "gotpointercapture":
            case "lostpointercapture":
            case "pointercancel":
            case "pointerdown":
            case "pointermove":
            case "pointerout":
            case "pointerover":
            case "pointerup":
              k3 = Td;
          }
          var t2 = 0 !== (b2 & 4), J = !t2 && "scroll" === a3, x2 = t2 ? null !== h4 ? h4 + "Capture" : null : h4;
          t2 = [];
          for (var w2 = d4, u2; null !== w2; ) {
            u2 = w2;
            var F = u2.stateNode;
            5 === u2.tag && null !== F && (u2 = F, null !== x2 && (F = Kb(w2, x2), null != F && t2.push(tf(w2, F, u2))));
            if (J) break;
            w2 = w2.return;
          }
          0 < t2.length && (h4 = new k3(h4, n, null, c2, e3), g2.push({ event: h4, listeners: t2 }));
        }
      }
      if (0 === (b2 & 7)) {
        a: {
          h4 = "mouseover" === a3 || "pointerover" === a3;
          k3 = "mouseout" === a3 || "pointerout" === a3;
          if (h4 && c2 !== wb && (n = c2.relatedTarget || c2.fromElement) && (Wc(n) || n[uf])) break a;
          if (k3 || h4) {
            h4 = e3.window === e3 ? e3 : (h4 = e3.ownerDocument) ? h4.defaultView || h4.parentWindow : window;
            if (k3) {
              if (n = c2.relatedTarget || c2.toElement, k3 = d4, n = n ? Wc(n) : null, null !== n && (J = Vb(n), n !== J || 5 !== n.tag && 6 !== n.tag)) n = null;
            } else k3 = null, n = d4;
            if (k3 !== n) {
              t2 = Bd;
              F = "onMouseLeave";
              x2 = "onMouseEnter";
              w2 = "mouse";
              if ("pointerout" === a3 || "pointerover" === a3) t2 = Td, F = "onPointerLeave", x2 = "onPointerEnter", w2 = "pointer";
              J = null == k3 ? h4 : ue(k3);
              u2 = null == n ? h4 : ue(n);
              h4 = new t2(F, w2 + "leave", k3, c2, e3);
              h4.target = J;
              h4.relatedTarget = u2;
              F = null;
              Wc(e3) === d4 && (t2 = new t2(x2, w2 + "enter", n, c2, e3), t2.target = u2, t2.relatedTarget = J, F = t2);
              J = F;
              if (k3 && n) b: {
                t2 = k3;
                x2 = n;
                w2 = 0;
                for (u2 = t2; u2; u2 = vf(u2)) w2++;
                u2 = 0;
                for (F = x2; F; F = vf(F)) u2++;
                for (; 0 < w2 - u2; ) t2 = vf(t2), w2--;
                for (; 0 < u2 - w2; ) x2 = vf(x2), u2--;
                for (; w2--; ) {
                  if (t2 === x2 || null !== x2 && t2 === x2.alternate) break b;
                  t2 = vf(t2);
                  x2 = vf(x2);
                }
                t2 = null;
              }
              else t2 = null;
              null !== k3 && wf(g2, h4, k3, t2, false);
              null !== n && null !== J && wf(g2, J, n, t2, true);
            }
          }
        }
        a: {
          h4 = d4 ? ue(d4) : window;
          k3 = h4.nodeName && h4.nodeName.toLowerCase();
          if ("select" === k3 || "input" === k3 && "file" === h4.type) var na = ve;
          else if (me(h4)) if (we) na = Fe;
          else {
            na = De;
            var xa = Ce;
          }
          else (k3 = h4.nodeName) && "input" === k3.toLowerCase() && ("checkbox" === h4.type || "radio" === h4.type) && (na = Ee);
          if (na && (na = na(a3, d4))) {
            ne(g2, na, c2, e3);
            break a;
          }
          xa && xa(a3, h4, d4);
          "focusout" === a3 && (xa = h4._wrapperState) && xa.controlled && "number" === h4.type && cb(h4, "number", h4.value);
        }
        xa = d4 ? ue(d4) : window;
        switch (a3) {
          case "focusin":
            if (me(xa) || "true" === xa.contentEditable) Qe = xa, Re = d4, Se = null;
            break;
          case "focusout":
            Se = Re = Qe = null;
            break;
          case "mousedown":
            Te = true;
            break;
          case "contextmenu":
          case "mouseup":
          case "dragend":
            Te = false;
            Ue(g2, c2, e3);
            break;
          case "selectionchange":
            if (Pe) break;
          case "keydown":
          case "keyup":
            Ue(g2, c2, e3);
        }
        var $a;
        if (ae) b: {
          switch (a3) {
            case "compositionstart":
              var ba = "onCompositionStart";
              break b;
            case "compositionend":
              ba = "onCompositionEnd";
              break b;
            case "compositionupdate":
              ba = "onCompositionUpdate";
              break b;
          }
          ba = void 0;
        }
        else ie ? ge(a3, c2) && (ba = "onCompositionEnd") : "keydown" === a3 && 229 === c2.keyCode && (ba = "onCompositionStart");
        ba && (de && "ko" !== c2.locale && (ie || "onCompositionStart" !== ba ? "onCompositionEnd" === ba && ie && ($a = nd()) : (kd = e3, ld = "value" in kd ? kd.value : kd.textContent, ie = true)), xa = oe(d4, ba), 0 < xa.length && (ba = new Ld(ba, a3, null, c2, e3), g2.push({ event: ba, listeners: xa }), $a ? ba.data = $a : ($a = he(c2), null !== $a && (ba.data = $a))));
        if ($a = ce ? je(a3, c2) : ke(a3, c2)) d4 = oe(d4, "onBeforeInput"), 0 < d4.length && (e3 = new Ld("onBeforeInput", "beforeinput", null, c2, e3), g2.push({ event: e3, listeners: d4 }), e3.data = $a);
      }
      se(g2, b2);
    });
  }
  function tf(a3, b2, c2) {
    return { instance: a3, listener: b2, currentTarget: c2 };
  }
  function oe(a3, b2) {
    for (var c2 = b2 + "Capture", d3 = []; null !== a3; ) {
      var e2 = a3, f = e2.stateNode;
      5 === e2.tag && null !== f && (e2 = f, f = Kb(a3, c2), null != f && d3.unshift(tf(a3, f, e2)), f = Kb(a3, b2), null != f && d3.push(tf(a3, f, e2)));
      a3 = a3.return;
    }
    return d3;
  }
  function vf(a3) {
    if (null === a3) return null;
    do
      a3 = a3.return;
    while (a3 && 5 !== a3.tag);
    return a3 ? a3 : null;
  }
  function wf(a3, b2, c2, d3, e2) {
    for (var f = b2._reactName, g = []; null !== c2 && c2 !== d3; ) {
      var h3 = c2, k2 = h3.alternate, l = h3.stateNode;
      if (null !== k2 && k2 === d3) break;
      5 === h3.tag && null !== l && (h3 = l, e2 ? (k2 = Kb(c2, f), null != k2 && g.unshift(tf(c2, k2, h3))) : e2 || (k2 = Kb(c2, f), null != k2 && g.push(tf(c2, k2, h3))));
      c2 = c2.return;
    }
    0 !== g.length && a3.push({ event: b2, listeners: g });
  }
  var xf = /\r\n?/g, yf = /\u0000|\uFFFD/g;
  function zf(a3) {
    return ("string" === typeof a3 ? a3 : "" + a3).replace(xf, "\n").replace(yf, "");
  }
  function Af(a3, b2, c2) {
    b2 = zf(b2);
    if (zf(a3) !== b2 && c2) throw Error(p(425));
  }
  function Bf() {
  }
  var Cf = null, Df = null;
  function Ef(a3, b2) {
    return "textarea" === a3 || "noscript" === a3 || "string" === typeof b2.children || "number" === typeof b2.children || "object" === typeof b2.dangerouslySetInnerHTML && null !== b2.dangerouslySetInnerHTML && null != b2.dangerouslySetInnerHTML.__html;
  }
  var Ff = "function" === typeof setTimeout ? setTimeout : void 0, Gf = "function" === typeof clearTimeout ? clearTimeout : void 0, Hf = "function" === typeof Promise ? Promise : void 0, Jf = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof Hf ? function(a3) {
    return Hf.resolve(null).then(a3).catch(If);
  } : Ff;
  function If(a3) {
    setTimeout(function() {
      throw a3;
    });
  }
  function Kf(a3, b2) {
    var c2 = b2, d3 = 0;
    do {
      var e2 = c2.nextSibling;
      a3.removeChild(c2);
      if (e2 && 8 === e2.nodeType) if (c2 = e2.data, "/$" === c2) {
        if (0 === d3) {
          a3.removeChild(e2);
          bd(b2);
          return;
        }
        d3--;
      } else "$" !== c2 && "$?" !== c2 && "$!" !== c2 || d3++;
      c2 = e2;
    } while (c2);
    bd(b2);
  }
  function Lf(a3) {
    for (; null != a3; a3 = a3.nextSibling) {
      var b2 = a3.nodeType;
      if (1 === b2 || 3 === b2) break;
      if (8 === b2) {
        b2 = a3.data;
        if ("$" === b2 || "$!" === b2 || "$?" === b2) break;
        if ("/$" === b2) return null;
      }
    }
    return a3;
  }
  function Mf(a3) {
    a3 = a3.previousSibling;
    for (var b2 = 0; a3; ) {
      if (8 === a3.nodeType) {
        var c2 = a3.data;
        if ("$" === c2 || "$!" === c2 || "$?" === c2) {
          if (0 === b2) return a3;
          b2--;
        } else "/$" === c2 && b2++;
      }
      a3 = a3.previousSibling;
    }
    return null;
  }
  var Nf = Math.random().toString(36).slice(2), Of = "__reactFiber$" + Nf, Pf = "__reactProps$" + Nf, uf = "__reactContainer$" + Nf, of = "__reactEvents$" + Nf, Qf = "__reactListeners$" + Nf, Rf = "__reactHandles$" + Nf;
  function Wc(a3) {
    var b2 = a3[Of];
    if (b2) return b2;
    for (var c2 = a3.parentNode; c2; ) {
      if (b2 = c2[uf] || c2[Of]) {
        c2 = b2.alternate;
        if (null !== b2.child || null !== c2 && null !== c2.child) for (a3 = Mf(a3); null !== a3; ) {
          if (c2 = a3[Of]) return c2;
          a3 = Mf(a3);
        }
        return b2;
      }
      a3 = c2;
      c2 = a3.parentNode;
    }
    return null;
  }
  function Cb(a3) {
    a3 = a3[Of] || a3[uf];
    return !a3 || 5 !== a3.tag && 6 !== a3.tag && 13 !== a3.tag && 3 !== a3.tag ? null : a3;
  }
  function ue(a3) {
    if (5 === a3.tag || 6 === a3.tag) return a3.stateNode;
    throw Error(p(33));
  }
  function Db(a3) {
    return a3[Pf] || null;
  }
  var Sf = [], Tf = -1;
  function Uf(a3) {
    return { current: a3 };
  }
  function E2(a3) {
    0 > Tf || (a3.current = Sf[Tf], Sf[Tf] = null, Tf--);
  }
  function G2(a3, b2) {
    Tf++;
    Sf[Tf] = a3.current;
    a3.current = b2;
  }
  var Vf = {}, H3 = Uf(Vf), Wf = Uf(false), Xf = Vf;
  function Yf(a3, b2) {
    var c2 = a3.type.contextTypes;
    if (!c2) return Vf;
    var d3 = a3.stateNode;
    if (d3 && d3.__reactInternalMemoizedUnmaskedChildContext === b2) return d3.__reactInternalMemoizedMaskedChildContext;
    var e2 = {}, f;
    for (f in c2) e2[f] = b2[f];
    d3 && (a3 = a3.stateNode, a3.__reactInternalMemoizedUnmaskedChildContext = b2, a3.__reactInternalMemoizedMaskedChildContext = e2);
    return e2;
  }
  function Zf(a3) {
    a3 = a3.childContextTypes;
    return null !== a3 && void 0 !== a3;
  }
  function $f() {
    E2(Wf);
    E2(H3);
  }
  function ag(a3, b2, c2) {
    if (H3.current !== Vf) throw Error(p(168));
    G2(H3, b2);
    G2(Wf, c2);
  }
  function bg(a3, b2, c2) {
    var d3 = a3.stateNode;
    b2 = b2.childContextTypes;
    if ("function" !== typeof d3.getChildContext) return c2;
    d3 = d3.getChildContext();
    for (var e2 in d3) if (!(e2 in b2)) throw Error(p(108, Ra(a3) || "Unknown", e2));
    return A({}, c2, d3);
  }
  function cg(a3) {
    a3 = (a3 = a3.stateNode) && a3.__reactInternalMemoizedMergedChildContext || Vf;
    Xf = H3.current;
    G2(H3, a3);
    G2(Wf, Wf.current);
    return true;
  }
  function dg(a3, b2, c2) {
    var d3 = a3.stateNode;
    if (!d3) throw Error(p(169));
    c2 ? (a3 = bg(a3, b2, Xf), d3.__reactInternalMemoizedMergedChildContext = a3, E2(Wf), E2(H3), G2(H3, a3)) : E2(Wf);
    G2(Wf, c2);
  }
  var eg = null, fg = false, gg = false;
  function hg(a3) {
    null === eg ? eg = [a3] : eg.push(a3);
  }
  function ig(a3) {
    fg = true;
    hg(a3);
  }
  function jg() {
    if (!gg && null !== eg) {
      gg = true;
      var a3 = 0, b2 = C;
      try {
        var c2 = eg;
        for (C = 1; a3 < c2.length; a3++) {
          var d3 = c2[a3];
          do
            d3 = d3(true);
          while (null !== d3);
        }
        eg = null;
        fg = false;
      } catch (e2) {
        throw null !== eg && (eg = eg.slice(a3 + 1)), ac(fc, jg), e2;
      } finally {
        C = b2, gg = false;
      }
    }
    return null;
  }
  var kg = [], lg = 0, mg = null, ng = 0, og = [], pg = 0, qg = null, rg = 1, sg = "";
  function tg(a3, b2) {
    kg[lg++] = ng;
    kg[lg++] = mg;
    mg = a3;
    ng = b2;
  }
  function ug(a3, b2, c2) {
    og[pg++] = rg;
    og[pg++] = sg;
    og[pg++] = qg;
    qg = a3;
    var d3 = rg;
    a3 = sg;
    var e2 = 32 - oc(d3) - 1;
    d3 &= ~(1 << e2);
    c2 += 1;
    var f = 32 - oc(b2) + e2;
    if (30 < f) {
      var g = e2 - e2 % 5;
      f = (d3 & (1 << g) - 1).toString(32);
      d3 >>= g;
      e2 -= g;
      rg = 1 << 32 - oc(b2) + e2 | c2 << e2 | d3;
      sg = f + a3;
    } else rg = 1 << f | c2 << e2 | d3, sg = a3;
  }
  function vg(a3) {
    null !== a3.return && (tg(a3, 1), ug(a3, 1, 0));
  }
  function wg(a3) {
    for (; a3 === mg; ) mg = kg[--lg], kg[lg] = null, ng = kg[--lg], kg[lg] = null;
    for (; a3 === qg; ) qg = og[--pg], og[pg] = null, sg = og[--pg], og[pg] = null, rg = og[--pg], og[pg] = null;
  }
  var xg = null, yg = null, I2 = false, zg = null;
  function Ag(a3, b2) {
    var c2 = Bg(5, null, null, 0);
    c2.elementType = "DELETED";
    c2.stateNode = b2;
    c2.return = a3;
    b2 = a3.deletions;
    null === b2 ? (a3.deletions = [c2], a3.flags |= 16) : b2.push(c2);
  }
  function Cg(a3, b2) {
    switch (a3.tag) {
      case 5:
        var c2 = a3.type;
        b2 = 1 !== b2.nodeType || c2.toLowerCase() !== b2.nodeName.toLowerCase() ? null : b2;
        return null !== b2 ? (a3.stateNode = b2, xg = a3, yg = Lf(b2.firstChild), true) : false;
      case 6:
        return b2 = "" === a3.pendingProps || 3 !== b2.nodeType ? null : b2, null !== b2 ? (a3.stateNode = b2, xg = a3, yg = null, true) : false;
      case 13:
        return b2 = 8 !== b2.nodeType ? null : b2, null !== b2 ? (c2 = null !== qg ? { id: rg, overflow: sg } : null, a3.memoizedState = { dehydrated: b2, treeContext: c2, retryLane: 1073741824 }, c2 = Bg(18, null, null, 0), c2.stateNode = b2, c2.return = a3, a3.child = c2, xg = a3, yg = null, true) : false;
      default:
        return false;
    }
  }
  function Dg(a3) {
    return 0 !== (a3.mode & 1) && 0 === (a3.flags & 128);
  }
  function Eg(a3) {
    if (I2) {
      var b2 = yg;
      if (b2) {
        var c2 = b2;
        if (!Cg(a3, b2)) {
          if (Dg(a3)) throw Error(p(418));
          b2 = Lf(c2.nextSibling);
          var d3 = xg;
          b2 && Cg(a3, b2) ? Ag(d3, c2) : (a3.flags = a3.flags & -4097 | 2, I2 = false, xg = a3);
        }
      } else {
        if (Dg(a3)) throw Error(p(418));
        a3.flags = a3.flags & -4097 | 2;
        I2 = false;
        xg = a3;
      }
    }
  }
  function Fg(a3) {
    for (a3 = a3.return; null !== a3 && 5 !== a3.tag && 3 !== a3.tag && 13 !== a3.tag; ) a3 = a3.return;
    xg = a3;
  }
  function Gg(a3) {
    if (a3 !== xg) return false;
    if (!I2) return Fg(a3), I2 = true, false;
    var b2;
    (b2 = 3 !== a3.tag) && !(b2 = 5 !== a3.tag) && (b2 = a3.type, b2 = "head" !== b2 && "body" !== b2 && !Ef(a3.type, a3.memoizedProps));
    if (b2 && (b2 = yg)) {
      if (Dg(a3)) throw Hg(), Error(p(418));
      for (; b2; ) Ag(a3, b2), b2 = Lf(b2.nextSibling);
    }
    Fg(a3);
    if (13 === a3.tag) {
      a3 = a3.memoizedState;
      a3 = null !== a3 ? a3.dehydrated : null;
      if (!a3) throw Error(p(317));
      a: {
        a3 = a3.nextSibling;
        for (b2 = 0; a3; ) {
          if (8 === a3.nodeType) {
            var c2 = a3.data;
            if ("/$" === c2) {
              if (0 === b2) {
                yg = Lf(a3.nextSibling);
                break a;
              }
              b2--;
            } else "$" !== c2 && "$!" !== c2 && "$?" !== c2 || b2++;
          }
          a3 = a3.nextSibling;
        }
        yg = null;
      }
    } else yg = xg ? Lf(a3.stateNode.nextSibling) : null;
    return true;
  }
  function Hg() {
    for (var a3 = yg; a3; ) a3 = Lf(a3.nextSibling);
  }
  function Ig() {
    yg = xg = null;
    I2 = false;
  }
  function Jg(a3) {
    null === zg ? zg = [a3] : zg.push(a3);
  }
  var Kg = ua.ReactCurrentBatchConfig;
  function Lg(a3, b2, c2) {
    a3 = c2.ref;
    if (null !== a3 && "function" !== typeof a3 && "object" !== typeof a3) {
      if (c2._owner) {
        c2 = c2._owner;
        if (c2) {
          if (1 !== c2.tag) throw Error(p(309));
          var d3 = c2.stateNode;
        }
        if (!d3) throw Error(p(147, a3));
        var e2 = d3, f = "" + a3;
        if (null !== b2 && null !== b2.ref && "function" === typeof b2.ref && b2.ref._stringRef === f) return b2.ref;
        b2 = function(a4) {
          var b3 = e2.refs;
          null === a4 ? delete b3[f] : b3[f] = a4;
        };
        b2._stringRef = f;
        return b2;
      }
      if ("string" !== typeof a3) throw Error(p(284));
      if (!c2._owner) throw Error(p(290, a3));
    }
    return a3;
  }
  function Mg(a3, b2) {
    a3 = Object.prototype.toString.call(b2);
    throw Error(p(31, "[object Object]" === a3 ? "object with keys {" + Object.keys(b2).join(", ") + "}" : a3));
  }
  function Ng(a3) {
    var b2 = a3._init;
    return b2(a3._payload);
  }
  function Og(a3) {
    function b2(b3, c3) {
      if (a3) {
        var d4 = b3.deletions;
        null === d4 ? (b3.deletions = [c3], b3.flags |= 16) : d4.push(c3);
      }
    }
    function c2(c3, d4) {
      if (!a3) return null;
      for (; null !== d4; ) b2(c3, d4), d4 = d4.sibling;
      return null;
    }
    function d3(a4, b3) {
      for (a4 = /* @__PURE__ */ new Map(); null !== b3; ) null !== b3.key ? a4.set(b3.key, b3) : a4.set(b3.index, b3), b3 = b3.sibling;
      return a4;
    }
    function e2(a4, b3) {
      a4 = Pg(a4, b3);
      a4.index = 0;
      a4.sibling = null;
      return a4;
    }
    function f(b3, c3, d4) {
      b3.index = d4;
      if (!a3) return b3.flags |= 1048576, c3;
      d4 = b3.alternate;
      if (null !== d4) return d4 = d4.index, d4 < c3 ? (b3.flags |= 2, c3) : d4;
      b3.flags |= 2;
      return c3;
    }
    function g(b3) {
      a3 && null === b3.alternate && (b3.flags |= 2);
      return b3;
    }
    function h3(a4, b3, c3, d4) {
      if (null === b3 || 6 !== b3.tag) return b3 = Qg(c3, a4.mode, d4), b3.return = a4, b3;
      b3 = e2(b3, c3);
      b3.return = a4;
      return b3;
    }
    function k2(a4, b3, c3, d4) {
      var f2 = c3.type;
      if (f2 === ya) return m3(a4, b3, c3.props.children, d4, c3.key);
      if (null !== b3 && (b3.elementType === f2 || "object" === typeof f2 && null !== f2 && f2.$$typeof === Ha && Ng(f2) === b3.type)) return d4 = e2(b3, c3.props), d4.ref = Lg(a4, b3, c3), d4.return = a4, d4;
      d4 = Rg(c3.type, c3.key, c3.props, null, a4.mode, d4);
      d4.ref = Lg(a4, b3, c3);
      d4.return = a4;
      return d4;
    }
    function l(a4, b3, c3, d4) {
      if (null === b3 || 4 !== b3.tag || b3.stateNode.containerInfo !== c3.containerInfo || b3.stateNode.implementation !== c3.implementation) return b3 = Sg(c3, a4.mode, d4), b3.return = a4, b3;
      b3 = e2(b3, c3.children || []);
      b3.return = a4;
      return b3;
    }
    function m3(a4, b3, c3, d4, f2) {
      if (null === b3 || 7 !== b3.tag) return b3 = Tg(c3, a4.mode, d4, f2), b3.return = a4, b3;
      b3 = e2(b3, c3);
      b3.return = a4;
      return b3;
    }
    function q2(a4, b3, c3) {
      if ("string" === typeof b3 && "" !== b3 || "number" === typeof b3) return b3 = Qg("" + b3, a4.mode, c3), b3.return = a4, b3;
      if ("object" === typeof b3 && null !== b3) {
        switch (b3.$$typeof) {
          case va:
            return c3 = Rg(b3.type, b3.key, b3.props, null, a4.mode, c3), c3.ref = Lg(a4, null, b3), c3.return = a4, c3;
          case wa:
            return b3 = Sg(b3, a4.mode, c3), b3.return = a4, b3;
          case Ha:
            var d4 = b3._init;
            return q2(a4, d4(b3._payload), c3);
        }
        if (eb(b3) || Ka(b3)) return b3 = Tg(b3, a4.mode, c3, null), b3.return = a4, b3;
        Mg(a4, b3);
      }
      return null;
    }
    function r2(a4, b3, c3, d4) {
      var e3 = null !== b3 ? b3.key : null;
      if ("string" === typeof c3 && "" !== c3 || "number" === typeof c3) return null !== e3 ? null : h3(a4, b3, "" + c3, d4);
      if ("object" === typeof c3 && null !== c3) {
        switch (c3.$$typeof) {
          case va:
            return c3.key === e3 ? k2(a4, b3, c3, d4) : null;
          case wa:
            return c3.key === e3 ? l(a4, b3, c3, d4) : null;
          case Ha:
            return e3 = c3._init, r2(
              a4,
              b3,
              e3(c3._payload),
              d4
            );
        }
        if (eb(c3) || Ka(c3)) return null !== e3 ? null : m3(a4, b3, c3, d4, null);
        Mg(a4, c3);
      }
      return null;
    }
    function y3(a4, b3, c3, d4, e3) {
      if ("string" === typeof d4 && "" !== d4 || "number" === typeof d4) return a4 = a4.get(c3) || null, h3(b3, a4, "" + d4, e3);
      if ("object" === typeof d4 && null !== d4) {
        switch (d4.$$typeof) {
          case va:
            return a4 = a4.get(null === d4.key ? c3 : d4.key) || null, k2(b3, a4, d4, e3);
          case wa:
            return a4 = a4.get(null === d4.key ? c3 : d4.key) || null, l(b3, a4, d4, e3);
          case Ha:
            var f2 = d4._init;
            return y3(a4, b3, c3, f2(d4._payload), e3);
        }
        if (eb(d4) || Ka(d4)) return a4 = a4.get(c3) || null, m3(b3, a4, d4, e3, null);
        Mg(b3, d4);
      }
      return null;
    }
    function n(e3, g2, h4, k3) {
      for (var l2 = null, m4 = null, u2 = g2, w2 = g2 = 0, x2 = null; null !== u2 && w2 < h4.length; w2++) {
        u2.index > w2 ? (x2 = u2, u2 = null) : x2 = u2.sibling;
        var n2 = r2(e3, u2, h4[w2], k3);
        if (null === n2) {
          null === u2 && (u2 = x2);
          break;
        }
        a3 && u2 && null === n2.alternate && b2(e3, u2);
        g2 = f(n2, g2, w2);
        null === m4 ? l2 = n2 : m4.sibling = n2;
        m4 = n2;
        u2 = x2;
      }
      if (w2 === h4.length) return c2(e3, u2), I2 && tg(e3, w2), l2;
      if (null === u2) {
        for (; w2 < h4.length; w2++) u2 = q2(e3, h4[w2], k3), null !== u2 && (g2 = f(u2, g2, w2), null === m4 ? l2 = u2 : m4.sibling = u2, m4 = u2);
        I2 && tg(e3, w2);
        return l2;
      }
      for (u2 = d3(e3, u2); w2 < h4.length; w2++) x2 = y3(u2, e3, w2, h4[w2], k3), null !== x2 && (a3 && null !== x2.alternate && u2.delete(null === x2.key ? w2 : x2.key), g2 = f(x2, g2, w2), null === m4 ? l2 = x2 : m4.sibling = x2, m4 = x2);
      a3 && u2.forEach(function(a4) {
        return b2(e3, a4);
      });
      I2 && tg(e3, w2);
      return l2;
    }
    function t2(e3, g2, h4, k3) {
      var l2 = Ka(h4);
      if ("function" !== typeof l2) throw Error(p(150));
      h4 = l2.call(h4);
      if (null == h4) throw Error(p(151));
      for (var u2 = l2 = null, m4 = g2, w2 = g2 = 0, x2 = null, n2 = h4.next(); null !== m4 && !n2.done; w2++, n2 = h4.next()) {
        m4.index > w2 ? (x2 = m4, m4 = null) : x2 = m4.sibling;
        var t3 = r2(e3, m4, n2.value, k3);
        if (null === t3) {
          null === m4 && (m4 = x2);
          break;
        }
        a3 && m4 && null === t3.alternate && b2(e3, m4);
        g2 = f(t3, g2, w2);
        null === u2 ? l2 = t3 : u2.sibling = t3;
        u2 = t3;
        m4 = x2;
      }
      if (n2.done) return c2(
        e3,
        m4
      ), I2 && tg(e3, w2), l2;
      if (null === m4) {
        for (; !n2.done; w2++, n2 = h4.next()) n2 = q2(e3, n2.value, k3), null !== n2 && (g2 = f(n2, g2, w2), null === u2 ? l2 = n2 : u2.sibling = n2, u2 = n2);
        I2 && tg(e3, w2);
        return l2;
      }
      for (m4 = d3(e3, m4); !n2.done; w2++, n2 = h4.next()) n2 = y3(m4, e3, w2, n2.value, k3), null !== n2 && (a3 && null !== n2.alternate && m4.delete(null === n2.key ? w2 : n2.key), g2 = f(n2, g2, w2), null === u2 ? l2 = n2 : u2.sibling = n2, u2 = n2);
      a3 && m4.forEach(function(a4) {
        return b2(e3, a4);
      });
      I2 && tg(e3, w2);
      return l2;
    }
    function J(a4, d4, f2, h4) {
      "object" === typeof f2 && null !== f2 && f2.type === ya && null === f2.key && (f2 = f2.props.children);
      if ("object" === typeof f2 && null !== f2) {
        switch (f2.$$typeof) {
          case va:
            a: {
              for (var k3 = f2.key, l2 = d4; null !== l2; ) {
                if (l2.key === k3) {
                  k3 = f2.type;
                  if (k3 === ya) {
                    if (7 === l2.tag) {
                      c2(a4, l2.sibling);
                      d4 = e2(l2, f2.props.children);
                      d4.return = a4;
                      a4 = d4;
                      break a;
                    }
                  } else if (l2.elementType === k3 || "object" === typeof k3 && null !== k3 && k3.$$typeof === Ha && Ng(k3) === l2.type) {
                    c2(a4, l2.sibling);
                    d4 = e2(l2, f2.props);
                    d4.ref = Lg(a4, l2, f2);
                    d4.return = a4;
                    a4 = d4;
                    break a;
                  }
                  c2(a4, l2);
                  break;
                } else b2(a4, l2);
                l2 = l2.sibling;
              }
              f2.type === ya ? (d4 = Tg(f2.props.children, a4.mode, h4, f2.key), d4.return = a4, a4 = d4) : (h4 = Rg(f2.type, f2.key, f2.props, null, a4.mode, h4), h4.ref = Lg(a4, d4, f2), h4.return = a4, a4 = h4);
            }
            return g(a4);
          case wa:
            a: {
              for (l2 = f2.key; null !== d4; ) {
                if (d4.key === l2) if (4 === d4.tag && d4.stateNode.containerInfo === f2.containerInfo && d4.stateNode.implementation === f2.implementation) {
                  c2(a4, d4.sibling);
                  d4 = e2(d4, f2.children || []);
                  d4.return = a4;
                  a4 = d4;
                  break a;
                } else {
                  c2(a4, d4);
                  break;
                }
                else b2(a4, d4);
                d4 = d4.sibling;
              }
              d4 = Sg(f2, a4.mode, h4);
              d4.return = a4;
              a4 = d4;
            }
            return g(a4);
          case Ha:
            return l2 = f2._init, J(a4, d4, l2(f2._payload), h4);
        }
        if (eb(f2)) return n(a4, d4, f2, h4);
        if (Ka(f2)) return t2(a4, d4, f2, h4);
        Mg(a4, f2);
      }
      return "string" === typeof f2 && "" !== f2 || "number" === typeof f2 ? (f2 = "" + f2, null !== d4 && 6 === d4.tag ? (c2(a4, d4.sibling), d4 = e2(d4, f2), d4.return = a4, a4 = d4) : (c2(a4, d4), d4 = Qg(f2, a4.mode, h4), d4.return = a4, a4 = d4), g(a4)) : c2(a4, d4);
    }
    return J;
  }
  var Ug = Og(true), Vg = Og(false), Wg = Uf(null), Xg = null, Yg = null, Zg = null;
  function $g() {
    Zg = Yg = Xg = null;
  }
  function ah(a3) {
    var b2 = Wg.current;
    E2(Wg);
    a3._currentValue = b2;
  }
  function bh(a3, b2, c2) {
    for (; null !== a3; ) {
      var d3 = a3.alternate;
      (a3.childLanes & b2) !== b2 ? (a3.childLanes |= b2, null !== d3 && (d3.childLanes |= b2)) : null !== d3 && (d3.childLanes & b2) !== b2 && (d3.childLanes |= b2);
      if (a3 === c2) break;
      a3 = a3.return;
    }
  }
  function ch(a3, b2) {
    Xg = a3;
    Zg = Yg = null;
    a3 = a3.dependencies;
    null !== a3 && null !== a3.firstContext && (0 !== (a3.lanes & b2) && (dh = true), a3.firstContext = null);
  }
  function eh(a3) {
    var b2 = a3._currentValue;
    if (Zg !== a3) if (a3 = { context: a3, memoizedValue: b2, next: null }, null === Yg) {
      if (null === Xg) throw Error(p(308));
      Yg = a3;
      Xg.dependencies = { lanes: 0, firstContext: a3 };
    } else Yg = Yg.next = a3;
    return b2;
  }
  var fh = null;
  function gh(a3) {
    null === fh ? fh = [a3] : fh.push(a3);
  }
  function hh(a3, b2, c2, d3) {
    var e2 = b2.interleaved;
    null === e2 ? (c2.next = c2, gh(b2)) : (c2.next = e2.next, e2.next = c2);
    b2.interleaved = c2;
    return ih(a3, d3);
  }
  function ih(a3, b2) {
    a3.lanes |= b2;
    var c2 = a3.alternate;
    null !== c2 && (c2.lanes |= b2);
    c2 = a3;
    for (a3 = a3.return; null !== a3; ) a3.childLanes |= b2, c2 = a3.alternate, null !== c2 && (c2.childLanes |= b2), c2 = a3, a3 = a3.return;
    return 3 === c2.tag ? c2.stateNode : null;
  }
  var jh = false;
  function kh(a3) {
    a3.updateQueue = { baseState: a3.memoizedState, firstBaseUpdate: null, lastBaseUpdate: null, shared: { pending: null, interleaved: null, lanes: 0 }, effects: null };
  }
  function lh(a3, b2) {
    a3 = a3.updateQueue;
    b2.updateQueue === a3 && (b2.updateQueue = { baseState: a3.baseState, firstBaseUpdate: a3.firstBaseUpdate, lastBaseUpdate: a3.lastBaseUpdate, shared: a3.shared, effects: a3.effects });
  }
  function mh(a3, b2) {
    return { eventTime: a3, lane: b2, tag: 0, payload: null, callback: null, next: null };
  }
  function nh(a3, b2, c2) {
    var d3 = a3.updateQueue;
    if (null === d3) return null;
    d3 = d3.shared;
    if (0 !== (K2 & 2)) {
      var e2 = d3.pending;
      null === e2 ? b2.next = b2 : (b2.next = e2.next, e2.next = b2);
      d3.pending = b2;
      return ih(a3, c2);
    }
    e2 = d3.interleaved;
    null === e2 ? (b2.next = b2, gh(d3)) : (b2.next = e2.next, e2.next = b2);
    d3.interleaved = b2;
    return ih(a3, c2);
  }
  function oh(a3, b2, c2) {
    b2 = b2.updateQueue;
    if (null !== b2 && (b2 = b2.shared, 0 !== (c2 & 4194240))) {
      var d3 = b2.lanes;
      d3 &= a3.pendingLanes;
      c2 |= d3;
      b2.lanes = c2;
      Cc(a3, c2);
    }
  }
  function ph(a3, b2) {
    var c2 = a3.updateQueue, d3 = a3.alternate;
    if (null !== d3 && (d3 = d3.updateQueue, c2 === d3)) {
      var e2 = null, f = null;
      c2 = c2.firstBaseUpdate;
      if (null !== c2) {
        do {
          var g = { eventTime: c2.eventTime, lane: c2.lane, tag: c2.tag, payload: c2.payload, callback: c2.callback, next: null };
          null === f ? e2 = f = g : f = f.next = g;
          c2 = c2.next;
        } while (null !== c2);
        null === f ? e2 = f = b2 : f = f.next = b2;
      } else e2 = f = b2;
      c2 = { baseState: d3.baseState, firstBaseUpdate: e2, lastBaseUpdate: f, shared: d3.shared, effects: d3.effects };
      a3.updateQueue = c2;
      return;
    }
    a3 = c2.lastBaseUpdate;
    null === a3 ? c2.firstBaseUpdate = b2 : a3.next = b2;
    c2.lastBaseUpdate = b2;
  }
  function qh(a3, b2, c2, d3) {
    var e2 = a3.updateQueue;
    jh = false;
    var f = e2.firstBaseUpdate, g = e2.lastBaseUpdate, h3 = e2.shared.pending;
    if (null !== h3) {
      e2.shared.pending = null;
      var k2 = h3, l = k2.next;
      k2.next = null;
      null === g ? f = l : g.next = l;
      g = k2;
      var m3 = a3.alternate;
      null !== m3 && (m3 = m3.updateQueue, h3 = m3.lastBaseUpdate, h3 !== g && (null === h3 ? m3.firstBaseUpdate = l : h3.next = l, m3.lastBaseUpdate = k2));
    }
    if (null !== f) {
      var q2 = e2.baseState;
      g = 0;
      m3 = l = k2 = null;
      h3 = f;
      do {
        var r2 = h3.lane, y3 = h3.eventTime;
        if ((d3 & r2) === r2) {
          null !== m3 && (m3 = m3.next = {
            eventTime: y3,
            lane: 0,
            tag: h3.tag,
            payload: h3.payload,
            callback: h3.callback,
            next: null
          });
          a: {
            var n = a3, t2 = h3;
            r2 = b2;
            y3 = c2;
            switch (t2.tag) {
              case 1:
                n = t2.payload;
                if ("function" === typeof n) {
                  q2 = n.call(y3, q2, r2);
                  break a;
                }
                q2 = n;
                break a;
              case 3:
                n.flags = n.flags & -65537 | 128;
              case 0:
                n = t2.payload;
                r2 = "function" === typeof n ? n.call(y3, q2, r2) : n;
                if (null === r2 || void 0 === r2) break a;
                q2 = A({}, q2, r2);
                break a;
              case 2:
                jh = true;
            }
          }
          null !== h3.callback && 0 !== h3.lane && (a3.flags |= 64, r2 = e2.effects, null === r2 ? e2.effects = [h3] : r2.push(h3));
        } else y3 = { eventTime: y3, lane: r2, tag: h3.tag, payload: h3.payload, callback: h3.callback, next: null }, null === m3 ? (l = m3 = y3, k2 = q2) : m3 = m3.next = y3, g |= r2;
        h3 = h3.next;
        if (null === h3) if (h3 = e2.shared.pending, null === h3) break;
        else r2 = h3, h3 = r2.next, r2.next = null, e2.lastBaseUpdate = r2, e2.shared.pending = null;
      } while (1);
      null === m3 && (k2 = q2);
      e2.baseState = k2;
      e2.firstBaseUpdate = l;
      e2.lastBaseUpdate = m3;
      b2 = e2.shared.interleaved;
      if (null !== b2) {
        e2 = b2;
        do
          g |= e2.lane, e2 = e2.next;
        while (e2 !== b2);
      } else null === f && (e2.shared.lanes = 0);
      rh |= g;
      a3.lanes = g;
      a3.memoizedState = q2;
    }
  }
  function sh(a3, b2, c2) {
    a3 = b2.effects;
    b2.effects = null;
    if (null !== a3) for (b2 = 0; b2 < a3.length; b2++) {
      var d3 = a3[b2], e2 = d3.callback;
      if (null !== e2) {
        d3.callback = null;
        d3 = c2;
        if ("function" !== typeof e2) throw Error(p(191, e2));
        e2.call(d3);
      }
    }
  }
  var th = {}, uh = Uf(th), vh = Uf(th), wh = Uf(th);
  function xh(a3) {
    if (a3 === th) throw Error(p(174));
    return a3;
  }
  function yh(a3, b2) {
    G2(wh, b2);
    G2(vh, a3);
    G2(uh, th);
    a3 = b2.nodeType;
    switch (a3) {
      case 9:
      case 11:
        b2 = (b2 = b2.documentElement) ? b2.namespaceURI : lb(null, "");
        break;
      default:
        a3 = 8 === a3 ? b2.parentNode : b2, b2 = a3.namespaceURI || null, a3 = a3.tagName, b2 = lb(b2, a3);
    }
    E2(uh);
    G2(uh, b2);
  }
  function zh() {
    E2(uh);
    E2(vh);
    E2(wh);
  }
  function Ah(a3) {
    xh(wh.current);
    var b2 = xh(uh.current);
    var c2 = lb(b2, a3.type);
    b2 !== c2 && (G2(vh, a3), G2(uh, c2));
  }
  function Bh(a3) {
    vh.current === a3 && (E2(uh), E2(vh));
  }
  var L2 = Uf(0);
  function Ch(a3) {
    for (var b2 = a3; null !== b2; ) {
      if (13 === b2.tag) {
        var c2 = b2.memoizedState;
        if (null !== c2 && (c2 = c2.dehydrated, null === c2 || "$?" === c2.data || "$!" === c2.data)) return b2;
      } else if (19 === b2.tag && void 0 !== b2.memoizedProps.revealOrder) {
        if (0 !== (b2.flags & 128)) return b2;
      } else if (null !== b2.child) {
        b2.child.return = b2;
        b2 = b2.child;
        continue;
      }
      if (b2 === a3) break;
      for (; null === b2.sibling; ) {
        if (null === b2.return || b2.return === a3) return null;
        b2 = b2.return;
      }
      b2.sibling.return = b2.return;
      b2 = b2.sibling;
    }
    return null;
  }
  var Dh = [];
  function Eh() {
    for (var a3 = 0; a3 < Dh.length; a3++) Dh[a3]._workInProgressVersionPrimary = null;
    Dh.length = 0;
  }
  var Fh = ua.ReactCurrentDispatcher, Gh = ua.ReactCurrentBatchConfig, Hh = 0, M3 = null, N = null, O2 = null, Ih = false, Jh = false, Kh = 0, Lh = 0;
  function P() {
    throw Error(p(321));
  }
  function Mh(a3, b2) {
    if (null === b2) return false;
    for (var c2 = 0; c2 < b2.length && c2 < a3.length; c2++) if (!He(a3[c2], b2[c2])) return false;
    return true;
  }
  function Nh(a3, b2, c2, d3, e2, f) {
    Hh = f;
    M3 = b2;
    b2.memoizedState = null;
    b2.updateQueue = null;
    b2.lanes = 0;
    Fh.current = null === a3 || null === a3.memoizedState ? Oh : Ph;
    a3 = c2(d3, e2);
    if (Jh) {
      f = 0;
      do {
        Jh = false;
        Kh = 0;
        if (25 <= f) throw Error(p(301));
        f += 1;
        O2 = N = null;
        b2.updateQueue = null;
        Fh.current = Qh;
        a3 = c2(d3, e2);
      } while (Jh);
    }
    Fh.current = Rh;
    b2 = null !== N && null !== N.next;
    Hh = 0;
    O2 = N = M3 = null;
    Ih = false;
    if (b2) throw Error(p(300));
    return a3;
  }
  function Sh() {
    var a3 = 0 !== Kh;
    Kh = 0;
    return a3;
  }
  function Th() {
    var a3 = { memoizedState: null, baseState: null, baseQueue: null, queue: null, next: null };
    null === O2 ? M3.memoizedState = O2 = a3 : O2 = O2.next = a3;
    return O2;
  }
  function Uh() {
    if (null === N) {
      var a3 = M3.alternate;
      a3 = null !== a3 ? a3.memoizedState : null;
    } else a3 = N.next;
    var b2 = null === O2 ? M3.memoizedState : O2.next;
    if (null !== b2) O2 = b2, N = a3;
    else {
      if (null === a3) throw Error(p(310));
      N = a3;
      a3 = { memoizedState: N.memoizedState, baseState: N.baseState, baseQueue: N.baseQueue, queue: N.queue, next: null };
      null === O2 ? M3.memoizedState = O2 = a3 : O2 = O2.next = a3;
    }
    return O2;
  }
  function Vh(a3, b2) {
    return "function" === typeof b2 ? b2(a3) : b2;
  }
  function Wh(a3) {
    var b2 = Uh(), c2 = b2.queue;
    if (null === c2) throw Error(p(311));
    c2.lastRenderedReducer = a3;
    var d3 = N, e2 = d3.baseQueue, f = c2.pending;
    if (null !== f) {
      if (null !== e2) {
        var g = e2.next;
        e2.next = f.next;
        f.next = g;
      }
      d3.baseQueue = e2 = f;
      c2.pending = null;
    }
    if (null !== e2) {
      f = e2.next;
      d3 = d3.baseState;
      var h3 = g = null, k2 = null, l = f;
      do {
        var m3 = l.lane;
        if ((Hh & m3) === m3) null !== k2 && (k2 = k2.next = { lane: 0, action: l.action, hasEagerState: l.hasEagerState, eagerState: l.eagerState, next: null }), d3 = l.hasEagerState ? l.eagerState : a3(d3, l.action);
        else {
          var q2 = {
            lane: m3,
            action: l.action,
            hasEagerState: l.hasEagerState,
            eagerState: l.eagerState,
            next: null
          };
          null === k2 ? (h3 = k2 = q2, g = d3) : k2 = k2.next = q2;
          M3.lanes |= m3;
          rh |= m3;
        }
        l = l.next;
      } while (null !== l && l !== f);
      null === k2 ? g = d3 : k2.next = h3;
      He(d3, b2.memoizedState) || (dh = true);
      b2.memoizedState = d3;
      b2.baseState = g;
      b2.baseQueue = k2;
      c2.lastRenderedState = d3;
    }
    a3 = c2.interleaved;
    if (null !== a3) {
      e2 = a3;
      do
        f = e2.lane, M3.lanes |= f, rh |= f, e2 = e2.next;
      while (e2 !== a3);
    } else null === e2 && (c2.lanes = 0);
    return [b2.memoizedState, c2.dispatch];
  }
  function Xh(a3) {
    var b2 = Uh(), c2 = b2.queue;
    if (null === c2) throw Error(p(311));
    c2.lastRenderedReducer = a3;
    var d3 = c2.dispatch, e2 = c2.pending, f = b2.memoizedState;
    if (null !== e2) {
      c2.pending = null;
      var g = e2 = e2.next;
      do
        f = a3(f, g.action), g = g.next;
      while (g !== e2);
      He(f, b2.memoizedState) || (dh = true);
      b2.memoizedState = f;
      null === b2.baseQueue && (b2.baseState = f);
      c2.lastRenderedState = f;
    }
    return [f, d3];
  }
  function Yh() {
  }
  function Zh(a3, b2) {
    var c2 = M3, d3 = Uh(), e2 = b2(), f = !He(d3.memoizedState, e2);
    f && (d3.memoizedState = e2, dh = true);
    d3 = d3.queue;
    $h(ai.bind(null, c2, d3, a3), [a3]);
    if (d3.getSnapshot !== b2 || f || null !== O2 && O2.memoizedState.tag & 1) {
      c2.flags |= 2048;
      bi(9, ci.bind(null, c2, d3, e2, b2), void 0, null);
      if (null === Q2) throw Error(p(349));
      0 !== (Hh & 30) || di(c2, b2, e2);
    }
    return e2;
  }
  function di(a3, b2, c2) {
    a3.flags |= 16384;
    a3 = { getSnapshot: b2, value: c2 };
    b2 = M3.updateQueue;
    null === b2 ? (b2 = { lastEffect: null, stores: null }, M3.updateQueue = b2, b2.stores = [a3]) : (c2 = b2.stores, null === c2 ? b2.stores = [a3] : c2.push(a3));
  }
  function ci(a3, b2, c2, d3) {
    b2.value = c2;
    b2.getSnapshot = d3;
    ei(b2) && fi(a3);
  }
  function ai(a3, b2, c2) {
    return c2(function() {
      ei(b2) && fi(a3);
    });
  }
  function ei(a3) {
    var b2 = a3.getSnapshot;
    a3 = a3.value;
    try {
      var c2 = b2();
      return !He(a3, c2);
    } catch (d3) {
      return true;
    }
  }
  function fi(a3) {
    var b2 = ih(a3, 1);
    null !== b2 && gi(b2, a3, 1, -1);
  }
  function hi(a3) {
    var b2 = Th();
    "function" === typeof a3 && (a3 = a3());
    b2.memoizedState = b2.baseState = a3;
    a3 = { pending: null, interleaved: null, lanes: 0, dispatch: null, lastRenderedReducer: Vh, lastRenderedState: a3 };
    b2.queue = a3;
    a3 = a3.dispatch = ii.bind(null, M3, a3);
    return [b2.memoizedState, a3];
  }
  function bi(a3, b2, c2, d3) {
    a3 = { tag: a3, create: b2, destroy: c2, deps: d3, next: null };
    b2 = M3.updateQueue;
    null === b2 ? (b2 = { lastEffect: null, stores: null }, M3.updateQueue = b2, b2.lastEffect = a3.next = a3) : (c2 = b2.lastEffect, null === c2 ? b2.lastEffect = a3.next = a3 : (d3 = c2.next, c2.next = a3, a3.next = d3, b2.lastEffect = a3));
    return a3;
  }
  function ji() {
    return Uh().memoizedState;
  }
  function ki(a3, b2, c2, d3) {
    var e2 = Th();
    M3.flags |= a3;
    e2.memoizedState = bi(1 | b2, c2, void 0, void 0 === d3 ? null : d3);
  }
  function li(a3, b2, c2, d3) {
    var e2 = Uh();
    d3 = void 0 === d3 ? null : d3;
    var f = void 0;
    if (null !== N) {
      var g = N.memoizedState;
      f = g.destroy;
      if (null !== d3 && Mh(d3, g.deps)) {
        e2.memoizedState = bi(b2, c2, f, d3);
        return;
      }
    }
    M3.flags |= a3;
    e2.memoizedState = bi(1 | b2, c2, f, d3);
  }
  function mi(a3, b2) {
    return ki(8390656, 8, a3, b2);
  }
  function $h(a3, b2) {
    return li(2048, 8, a3, b2);
  }
  function ni(a3, b2) {
    return li(4, 2, a3, b2);
  }
  function oi(a3, b2) {
    return li(4, 4, a3, b2);
  }
  function pi(a3, b2) {
    if ("function" === typeof b2) return a3 = a3(), b2(a3), function() {
      b2(null);
    };
    if (null !== b2 && void 0 !== b2) return a3 = a3(), b2.current = a3, function() {
      b2.current = null;
    };
  }
  function qi(a3, b2, c2) {
    c2 = null !== c2 && void 0 !== c2 ? c2.concat([a3]) : null;
    return li(4, 4, pi.bind(null, b2, a3), c2);
  }
  function ri() {
  }
  function si(a3, b2) {
    var c2 = Uh();
    b2 = void 0 === b2 ? null : b2;
    var d3 = c2.memoizedState;
    if (null !== d3 && null !== b2 && Mh(b2, d3[1])) return d3[0];
    c2.memoizedState = [a3, b2];
    return a3;
  }
  function ti(a3, b2) {
    var c2 = Uh();
    b2 = void 0 === b2 ? null : b2;
    var d3 = c2.memoizedState;
    if (null !== d3 && null !== b2 && Mh(b2, d3[1])) return d3[0];
    a3 = a3();
    c2.memoizedState = [a3, b2];
    return a3;
  }
  function ui(a3, b2, c2) {
    if (0 === (Hh & 21)) return a3.baseState && (a3.baseState = false, dh = true), a3.memoizedState = c2;
    He(c2, b2) || (c2 = yc(), M3.lanes |= c2, rh |= c2, a3.baseState = true);
    return b2;
  }
  function vi(a3, b2) {
    var c2 = C;
    C = 0 !== c2 && 4 > c2 ? c2 : 4;
    a3(true);
    var d3 = Gh.transition;
    Gh.transition = {};
    try {
      a3(false), b2();
    } finally {
      C = c2, Gh.transition = d3;
    }
  }
  function wi() {
    return Uh().memoizedState;
  }
  function xi(a3, b2, c2) {
    var d3 = yi(a3);
    c2 = { lane: d3, action: c2, hasEagerState: false, eagerState: null, next: null };
    if (zi(a3)) Ai(b2, c2);
    else if (c2 = hh(a3, b2, c2, d3), null !== c2) {
      var e2 = R2();
      gi(c2, a3, d3, e2);
      Bi(c2, b2, d3);
    }
  }
  function ii(a3, b2, c2) {
    var d3 = yi(a3), e2 = { lane: d3, action: c2, hasEagerState: false, eagerState: null, next: null };
    if (zi(a3)) Ai(b2, e2);
    else {
      var f = a3.alternate;
      if (0 === a3.lanes && (null === f || 0 === f.lanes) && (f = b2.lastRenderedReducer, null !== f)) try {
        var g = b2.lastRenderedState, h3 = f(g, c2);
        e2.hasEagerState = true;
        e2.eagerState = h3;
        if (He(h3, g)) {
          var k2 = b2.interleaved;
          null === k2 ? (e2.next = e2, gh(b2)) : (e2.next = k2.next, k2.next = e2);
          b2.interleaved = e2;
          return;
        }
      } catch (l) {
      } finally {
      }
      c2 = hh(a3, b2, e2, d3);
      null !== c2 && (e2 = R2(), gi(c2, a3, d3, e2), Bi(c2, b2, d3));
    }
  }
  function zi(a3) {
    var b2 = a3.alternate;
    return a3 === M3 || null !== b2 && b2 === M3;
  }
  function Ai(a3, b2) {
    Jh = Ih = true;
    var c2 = a3.pending;
    null === c2 ? b2.next = b2 : (b2.next = c2.next, c2.next = b2);
    a3.pending = b2;
  }
  function Bi(a3, b2, c2) {
    if (0 !== (c2 & 4194240)) {
      var d3 = b2.lanes;
      d3 &= a3.pendingLanes;
      c2 |= d3;
      b2.lanes = c2;
      Cc(a3, c2);
    }
  }
  var Rh = { readContext: eh, useCallback: P, useContext: P, useEffect: P, useImperativeHandle: P, useInsertionEffect: P, useLayoutEffect: P, useMemo: P, useReducer: P, useRef: P, useState: P, useDebugValue: P, useDeferredValue: P, useTransition: P, useMutableSource: P, useSyncExternalStore: P, useId: P, unstable_isNewReconciler: false }, Oh = { readContext: eh, useCallback: function(a3, b2) {
    Th().memoizedState = [a3, void 0 === b2 ? null : b2];
    return a3;
  }, useContext: eh, useEffect: mi, useImperativeHandle: function(a3, b2, c2) {
    c2 = null !== c2 && void 0 !== c2 ? c2.concat([a3]) : null;
    return ki(
      4194308,
      4,
      pi.bind(null, b2, a3),
      c2
    );
  }, useLayoutEffect: function(a3, b2) {
    return ki(4194308, 4, a3, b2);
  }, useInsertionEffect: function(a3, b2) {
    return ki(4, 2, a3, b2);
  }, useMemo: function(a3, b2) {
    var c2 = Th();
    b2 = void 0 === b2 ? null : b2;
    a3 = a3();
    c2.memoizedState = [a3, b2];
    return a3;
  }, useReducer: function(a3, b2, c2) {
    var d3 = Th();
    b2 = void 0 !== c2 ? c2(b2) : b2;
    d3.memoizedState = d3.baseState = b2;
    a3 = { pending: null, interleaved: null, lanes: 0, dispatch: null, lastRenderedReducer: a3, lastRenderedState: b2 };
    d3.queue = a3;
    a3 = a3.dispatch = xi.bind(null, M3, a3);
    return [d3.memoizedState, a3];
  }, useRef: function(a3) {
    var b2 = Th();
    a3 = { current: a3 };
    return b2.memoizedState = a3;
  }, useState: hi, useDebugValue: ri, useDeferredValue: function(a3) {
    return Th().memoizedState = a3;
  }, useTransition: function() {
    var a3 = hi(false), b2 = a3[0];
    a3 = vi.bind(null, a3[1]);
    Th().memoizedState = a3;
    return [b2, a3];
  }, useMutableSource: function() {
  }, useSyncExternalStore: function(a3, b2, c2) {
    var d3 = M3, e2 = Th();
    if (I2) {
      if (void 0 === c2) throw Error(p(407));
      c2 = c2();
    } else {
      c2 = b2();
      if (null === Q2) throw Error(p(349));
      0 !== (Hh & 30) || di(d3, b2, c2);
    }
    e2.memoizedState = c2;
    var f = { value: c2, getSnapshot: b2 };
    e2.queue = f;
    mi(ai.bind(
      null,
      d3,
      f,
      a3
    ), [a3]);
    d3.flags |= 2048;
    bi(9, ci.bind(null, d3, f, c2, b2), void 0, null);
    return c2;
  }, useId: function() {
    var a3 = Th(), b2 = Q2.identifierPrefix;
    if (I2) {
      var c2 = sg;
      var d3 = rg;
      c2 = (d3 & ~(1 << 32 - oc(d3) - 1)).toString(32) + c2;
      b2 = ":" + b2 + "R" + c2;
      c2 = Kh++;
      0 < c2 && (b2 += "H" + c2.toString(32));
      b2 += ":";
    } else c2 = Lh++, b2 = ":" + b2 + "r" + c2.toString(32) + ":";
    return a3.memoizedState = b2;
  }, unstable_isNewReconciler: false }, Ph = {
    readContext: eh,
    useCallback: si,
    useContext: eh,
    useEffect: $h,
    useImperativeHandle: qi,
    useInsertionEffect: ni,
    useLayoutEffect: oi,
    useMemo: ti,
    useReducer: Wh,
    useRef: ji,
    useState: function() {
      return Wh(Vh);
    },
    useDebugValue: ri,
    useDeferredValue: function(a3) {
      var b2 = Uh();
      return ui(b2, N.memoizedState, a3);
    },
    useTransition: function() {
      var a3 = Wh(Vh)[0], b2 = Uh().memoizedState;
      return [a3, b2];
    },
    useMutableSource: Yh,
    useSyncExternalStore: Zh,
    useId: wi,
    unstable_isNewReconciler: false
  }, Qh = { readContext: eh, useCallback: si, useContext: eh, useEffect: $h, useImperativeHandle: qi, useInsertionEffect: ni, useLayoutEffect: oi, useMemo: ti, useReducer: Xh, useRef: ji, useState: function() {
    return Xh(Vh);
  }, useDebugValue: ri, useDeferredValue: function(a3) {
    var b2 = Uh();
    return null === N ? b2.memoizedState = a3 : ui(b2, N.memoizedState, a3);
  }, useTransition: function() {
    var a3 = Xh(Vh)[0], b2 = Uh().memoizedState;
    return [a3, b2];
  }, useMutableSource: Yh, useSyncExternalStore: Zh, useId: wi, unstable_isNewReconciler: false };
  function Ci(a3, b2) {
    if (a3 && a3.defaultProps) {
      b2 = A({}, b2);
      a3 = a3.defaultProps;
      for (var c2 in a3) void 0 === b2[c2] && (b2[c2] = a3[c2]);
      return b2;
    }
    return b2;
  }
  function Di(a3, b2, c2, d3) {
    b2 = a3.memoizedState;
    c2 = c2(d3, b2);
    c2 = null === c2 || void 0 === c2 ? b2 : A({}, b2, c2);
    a3.memoizedState = c2;
    0 === a3.lanes && (a3.updateQueue.baseState = c2);
  }
  var Ei = { isMounted: function(a3) {
    return (a3 = a3._reactInternals) ? Vb(a3) === a3 : false;
  }, enqueueSetState: function(a3, b2, c2) {
    a3 = a3._reactInternals;
    var d3 = R2(), e2 = yi(a3), f = mh(d3, e2);
    f.payload = b2;
    void 0 !== c2 && null !== c2 && (f.callback = c2);
    b2 = nh(a3, f, e2);
    null !== b2 && (gi(b2, a3, e2, d3), oh(b2, a3, e2));
  }, enqueueReplaceState: function(a3, b2, c2) {
    a3 = a3._reactInternals;
    var d3 = R2(), e2 = yi(a3), f = mh(d3, e2);
    f.tag = 1;
    f.payload = b2;
    void 0 !== c2 && null !== c2 && (f.callback = c2);
    b2 = nh(a3, f, e2);
    null !== b2 && (gi(b2, a3, e2, d3), oh(b2, a3, e2));
  }, enqueueForceUpdate: function(a3, b2) {
    a3 = a3._reactInternals;
    var c2 = R2(), d3 = yi(a3), e2 = mh(c2, d3);
    e2.tag = 2;
    void 0 !== b2 && null !== b2 && (e2.callback = b2);
    b2 = nh(a3, e2, d3);
    null !== b2 && (gi(b2, a3, d3, c2), oh(b2, a3, d3));
  } };
  function Fi(a3, b2, c2, d3, e2, f, g) {
    a3 = a3.stateNode;
    return "function" === typeof a3.shouldComponentUpdate ? a3.shouldComponentUpdate(d3, f, g) : b2.prototype && b2.prototype.isPureReactComponent ? !Ie(c2, d3) || !Ie(e2, f) : true;
  }
  function Gi(a3, b2, c2) {
    var d3 = false, e2 = Vf;
    var f = b2.contextType;
    "object" === typeof f && null !== f ? f = eh(f) : (e2 = Zf(b2) ? Xf : H3.current, d3 = b2.contextTypes, f = (d3 = null !== d3 && void 0 !== d3) ? Yf(a3, e2) : Vf);
    b2 = new b2(c2, f);
    a3.memoizedState = null !== b2.state && void 0 !== b2.state ? b2.state : null;
    b2.updater = Ei;
    a3.stateNode = b2;
    b2._reactInternals = a3;
    d3 && (a3 = a3.stateNode, a3.__reactInternalMemoizedUnmaskedChildContext = e2, a3.__reactInternalMemoizedMaskedChildContext = f);
    return b2;
  }
  function Hi(a3, b2, c2, d3) {
    a3 = b2.state;
    "function" === typeof b2.componentWillReceiveProps && b2.componentWillReceiveProps(c2, d3);
    "function" === typeof b2.UNSAFE_componentWillReceiveProps && b2.UNSAFE_componentWillReceiveProps(c2, d3);
    b2.state !== a3 && Ei.enqueueReplaceState(b2, b2.state, null);
  }
  function Ii(a3, b2, c2, d3) {
    var e2 = a3.stateNode;
    e2.props = c2;
    e2.state = a3.memoizedState;
    e2.refs = {};
    kh(a3);
    var f = b2.contextType;
    "object" === typeof f && null !== f ? e2.context = eh(f) : (f = Zf(b2) ? Xf : H3.current, e2.context = Yf(a3, f));
    e2.state = a3.memoizedState;
    f = b2.getDerivedStateFromProps;
    "function" === typeof f && (Di(a3, b2, f, c2), e2.state = a3.memoizedState);
    "function" === typeof b2.getDerivedStateFromProps || "function" === typeof e2.getSnapshotBeforeUpdate || "function" !== typeof e2.UNSAFE_componentWillMount && "function" !== typeof e2.componentWillMount || (b2 = e2.state, "function" === typeof e2.componentWillMount && e2.componentWillMount(), "function" === typeof e2.UNSAFE_componentWillMount && e2.UNSAFE_componentWillMount(), b2 !== e2.state && Ei.enqueueReplaceState(e2, e2.state, null), qh(a3, c2, e2, d3), e2.state = a3.memoizedState);
    "function" === typeof e2.componentDidMount && (a3.flags |= 4194308);
  }
  function Ji(a3, b2) {
    try {
      var c2 = "", d3 = b2;
      do
        c2 += Pa(d3), d3 = d3.return;
      while (d3);
      var e2 = c2;
    } catch (f) {
      e2 = "\nError generating stack: " + f.message + "\n" + f.stack;
    }
    return { value: a3, source: b2, stack: e2, digest: null };
  }
  function Ki(a3, b2, c2) {
    return { value: a3, source: null, stack: null != c2 ? c2 : null, digest: null != b2 ? b2 : null };
  }
  function Li(a3, b2) {
    try {
      console.error(b2.value);
    } catch (c2) {
      setTimeout(function() {
        throw c2;
      });
    }
  }
  var Mi = "function" === typeof WeakMap ? WeakMap : Map;
  function Ni(a3, b2, c2) {
    c2 = mh(-1, c2);
    c2.tag = 3;
    c2.payload = { element: null };
    var d3 = b2.value;
    c2.callback = function() {
      Oi || (Oi = true, Pi = d3);
      Li(a3, b2);
    };
    return c2;
  }
  function Qi(a3, b2, c2) {
    c2 = mh(-1, c2);
    c2.tag = 3;
    var d3 = a3.type.getDerivedStateFromError;
    if ("function" === typeof d3) {
      var e2 = b2.value;
      c2.payload = function() {
        return d3(e2);
      };
      c2.callback = function() {
        Li(a3, b2);
      };
    }
    var f = a3.stateNode;
    null !== f && "function" === typeof f.componentDidCatch && (c2.callback = function() {
      Li(a3, b2);
      "function" !== typeof d3 && (null === Ri ? Ri = /* @__PURE__ */ new Set([this]) : Ri.add(this));
      var c3 = b2.stack;
      this.componentDidCatch(b2.value, { componentStack: null !== c3 ? c3 : "" });
    });
    return c2;
  }
  function Si(a3, b2, c2) {
    var d3 = a3.pingCache;
    if (null === d3) {
      d3 = a3.pingCache = new Mi();
      var e2 = /* @__PURE__ */ new Set();
      d3.set(b2, e2);
    } else e2 = d3.get(b2), void 0 === e2 && (e2 = /* @__PURE__ */ new Set(), d3.set(b2, e2));
    e2.has(c2) || (e2.add(c2), a3 = Ti.bind(null, a3, b2, c2), b2.then(a3, a3));
  }
  function Ui(a3) {
    do {
      var b2;
      if (b2 = 13 === a3.tag) b2 = a3.memoizedState, b2 = null !== b2 ? null !== b2.dehydrated ? true : false : true;
      if (b2) return a3;
      a3 = a3.return;
    } while (null !== a3);
    return null;
  }
  function Vi(a3, b2, c2, d3, e2) {
    if (0 === (a3.mode & 1)) return a3 === b2 ? a3.flags |= 65536 : (a3.flags |= 128, c2.flags |= 131072, c2.flags &= -52805, 1 === c2.tag && (null === c2.alternate ? c2.tag = 17 : (b2 = mh(-1, 1), b2.tag = 2, nh(c2, b2, 1))), c2.lanes |= 1), a3;
    a3.flags |= 65536;
    a3.lanes = e2;
    return a3;
  }
  var Wi = ua.ReactCurrentOwner, dh = false;
  function Xi(a3, b2, c2, d3) {
    b2.child = null === a3 ? Vg(b2, null, c2, d3) : Ug(b2, a3.child, c2, d3);
  }
  function Yi(a3, b2, c2, d3, e2) {
    c2 = c2.render;
    var f = b2.ref;
    ch(b2, e2);
    d3 = Nh(a3, b2, c2, d3, f, e2);
    c2 = Sh();
    if (null !== a3 && !dh) return b2.updateQueue = a3.updateQueue, b2.flags &= -2053, a3.lanes &= ~e2, Zi(a3, b2, e2);
    I2 && c2 && vg(b2);
    b2.flags |= 1;
    Xi(a3, b2, d3, e2);
    return b2.child;
  }
  function $i(a3, b2, c2, d3, e2) {
    if (null === a3) {
      var f = c2.type;
      if ("function" === typeof f && !aj(f) && void 0 === f.defaultProps && null === c2.compare && void 0 === c2.defaultProps) return b2.tag = 15, b2.type = f, bj(a3, b2, f, d3, e2);
      a3 = Rg(c2.type, null, d3, b2, b2.mode, e2);
      a3.ref = b2.ref;
      a3.return = b2;
      return b2.child = a3;
    }
    f = a3.child;
    if (0 === (a3.lanes & e2)) {
      var g = f.memoizedProps;
      c2 = c2.compare;
      c2 = null !== c2 ? c2 : Ie;
      if (c2(g, d3) && a3.ref === b2.ref) return Zi(a3, b2, e2);
    }
    b2.flags |= 1;
    a3 = Pg(f, d3);
    a3.ref = b2.ref;
    a3.return = b2;
    return b2.child = a3;
  }
  function bj(a3, b2, c2, d3, e2) {
    if (null !== a3) {
      var f = a3.memoizedProps;
      if (Ie(f, d3) && a3.ref === b2.ref) if (dh = false, b2.pendingProps = d3 = f, 0 !== (a3.lanes & e2)) 0 !== (a3.flags & 131072) && (dh = true);
      else return b2.lanes = a3.lanes, Zi(a3, b2, e2);
    }
    return cj(a3, b2, c2, d3, e2);
  }
  function dj(a3, b2, c2) {
    var d3 = b2.pendingProps, e2 = d3.children, f = null !== a3 ? a3.memoizedState : null;
    if ("hidden" === d3.mode) if (0 === (b2.mode & 1)) b2.memoizedState = { baseLanes: 0, cachePool: null, transitions: null }, G2(ej, fj), fj |= c2;
    else {
      if (0 === (c2 & 1073741824)) return a3 = null !== f ? f.baseLanes | c2 : c2, b2.lanes = b2.childLanes = 1073741824, b2.memoizedState = { baseLanes: a3, cachePool: null, transitions: null }, b2.updateQueue = null, G2(ej, fj), fj |= a3, null;
      b2.memoizedState = { baseLanes: 0, cachePool: null, transitions: null };
      d3 = null !== f ? f.baseLanes : c2;
      G2(ej, fj);
      fj |= d3;
    }
    else null !== f ? (d3 = f.baseLanes | c2, b2.memoizedState = null) : d3 = c2, G2(ej, fj), fj |= d3;
    Xi(a3, b2, e2, c2);
    return b2.child;
  }
  function gj(a3, b2) {
    var c2 = b2.ref;
    if (null === a3 && null !== c2 || null !== a3 && a3.ref !== c2) b2.flags |= 512, b2.flags |= 2097152;
  }
  function cj(a3, b2, c2, d3, e2) {
    var f = Zf(c2) ? Xf : H3.current;
    f = Yf(b2, f);
    ch(b2, e2);
    c2 = Nh(a3, b2, c2, d3, f, e2);
    d3 = Sh();
    if (null !== a3 && !dh) return b2.updateQueue = a3.updateQueue, b2.flags &= -2053, a3.lanes &= ~e2, Zi(a3, b2, e2);
    I2 && d3 && vg(b2);
    b2.flags |= 1;
    Xi(a3, b2, c2, e2);
    return b2.child;
  }
  function hj(a3, b2, c2, d3, e2) {
    if (Zf(c2)) {
      var f = true;
      cg(b2);
    } else f = false;
    ch(b2, e2);
    if (null === b2.stateNode) ij(a3, b2), Gi(b2, c2, d3), Ii(b2, c2, d3, e2), d3 = true;
    else if (null === a3) {
      var g = b2.stateNode, h3 = b2.memoizedProps;
      g.props = h3;
      var k2 = g.context, l = c2.contextType;
      "object" === typeof l && null !== l ? l = eh(l) : (l = Zf(c2) ? Xf : H3.current, l = Yf(b2, l));
      var m3 = c2.getDerivedStateFromProps, q2 = "function" === typeof m3 || "function" === typeof g.getSnapshotBeforeUpdate;
      q2 || "function" !== typeof g.UNSAFE_componentWillReceiveProps && "function" !== typeof g.componentWillReceiveProps || (h3 !== d3 || k2 !== l) && Hi(b2, g, d3, l);
      jh = false;
      var r2 = b2.memoizedState;
      g.state = r2;
      qh(b2, d3, g, e2);
      k2 = b2.memoizedState;
      h3 !== d3 || r2 !== k2 || Wf.current || jh ? ("function" === typeof m3 && (Di(b2, c2, m3, d3), k2 = b2.memoizedState), (h3 = jh || Fi(b2, c2, h3, d3, r2, k2, l)) ? (q2 || "function" !== typeof g.UNSAFE_componentWillMount && "function" !== typeof g.componentWillMount || ("function" === typeof g.componentWillMount && g.componentWillMount(), "function" === typeof g.UNSAFE_componentWillMount && g.UNSAFE_componentWillMount()), "function" === typeof g.componentDidMount && (b2.flags |= 4194308)) : ("function" === typeof g.componentDidMount && (b2.flags |= 4194308), b2.memoizedProps = d3, b2.memoizedState = k2), g.props = d3, g.state = k2, g.context = l, d3 = h3) : ("function" === typeof g.componentDidMount && (b2.flags |= 4194308), d3 = false);
    } else {
      g = b2.stateNode;
      lh(a3, b2);
      h3 = b2.memoizedProps;
      l = b2.type === b2.elementType ? h3 : Ci(b2.type, h3);
      g.props = l;
      q2 = b2.pendingProps;
      r2 = g.context;
      k2 = c2.contextType;
      "object" === typeof k2 && null !== k2 ? k2 = eh(k2) : (k2 = Zf(c2) ? Xf : H3.current, k2 = Yf(b2, k2));
      var y3 = c2.getDerivedStateFromProps;
      (m3 = "function" === typeof y3 || "function" === typeof g.getSnapshotBeforeUpdate) || "function" !== typeof g.UNSAFE_componentWillReceiveProps && "function" !== typeof g.componentWillReceiveProps || (h3 !== q2 || r2 !== k2) && Hi(b2, g, d3, k2);
      jh = false;
      r2 = b2.memoizedState;
      g.state = r2;
      qh(b2, d3, g, e2);
      var n = b2.memoizedState;
      h3 !== q2 || r2 !== n || Wf.current || jh ? ("function" === typeof y3 && (Di(b2, c2, y3, d3), n = b2.memoizedState), (l = jh || Fi(b2, c2, l, d3, r2, n, k2) || false) ? (m3 || "function" !== typeof g.UNSAFE_componentWillUpdate && "function" !== typeof g.componentWillUpdate || ("function" === typeof g.componentWillUpdate && g.componentWillUpdate(d3, n, k2), "function" === typeof g.UNSAFE_componentWillUpdate && g.UNSAFE_componentWillUpdate(d3, n, k2)), "function" === typeof g.componentDidUpdate && (b2.flags |= 4), "function" === typeof g.getSnapshotBeforeUpdate && (b2.flags |= 1024)) : ("function" !== typeof g.componentDidUpdate || h3 === a3.memoizedProps && r2 === a3.memoizedState || (b2.flags |= 4), "function" !== typeof g.getSnapshotBeforeUpdate || h3 === a3.memoizedProps && r2 === a3.memoizedState || (b2.flags |= 1024), b2.memoizedProps = d3, b2.memoizedState = n), g.props = d3, g.state = n, g.context = k2, d3 = l) : ("function" !== typeof g.componentDidUpdate || h3 === a3.memoizedProps && r2 === a3.memoizedState || (b2.flags |= 4), "function" !== typeof g.getSnapshotBeforeUpdate || h3 === a3.memoizedProps && r2 === a3.memoizedState || (b2.flags |= 1024), d3 = false);
    }
    return jj(a3, b2, c2, d3, f, e2);
  }
  function jj(a3, b2, c2, d3, e2, f) {
    gj(a3, b2);
    var g = 0 !== (b2.flags & 128);
    if (!d3 && !g) return e2 && dg(b2, c2, false), Zi(a3, b2, f);
    d3 = b2.stateNode;
    Wi.current = b2;
    var h3 = g && "function" !== typeof c2.getDerivedStateFromError ? null : d3.render();
    b2.flags |= 1;
    null !== a3 && g ? (b2.child = Ug(b2, a3.child, null, f), b2.child = Ug(b2, null, h3, f)) : Xi(a3, b2, h3, f);
    b2.memoizedState = d3.state;
    e2 && dg(b2, c2, true);
    return b2.child;
  }
  function kj(a3) {
    var b2 = a3.stateNode;
    b2.pendingContext ? ag(a3, b2.pendingContext, b2.pendingContext !== b2.context) : b2.context && ag(a3, b2.context, false);
    yh(a3, b2.containerInfo);
  }
  function lj(a3, b2, c2, d3, e2) {
    Ig();
    Jg(e2);
    b2.flags |= 256;
    Xi(a3, b2, c2, d3);
    return b2.child;
  }
  var mj = { dehydrated: null, treeContext: null, retryLane: 0 };
  function nj(a3) {
    return { baseLanes: a3, cachePool: null, transitions: null };
  }
  function oj(a3, b2, c2) {
    var d3 = b2.pendingProps, e2 = L2.current, f = false, g = 0 !== (b2.flags & 128), h3;
    (h3 = g) || (h3 = null !== a3 && null === a3.memoizedState ? false : 0 !== (e2 & 2));
    if (h3) f = true, b2.flags &= -129;
    else if (null === a3 || null !== a3.memoizedState) e2 |= 1;
    G2(L2, e2 & 1);
    if (null === a3) {
      Eg(b2);
      a3 = b2.memoizedState;
      if (null !== a3 && (a3 = a3.dehydrated, null !== a3)) return 0 === (b2.mode & 1) ? b2.lanes = 1 : "$!" === a3.data ? b2.lanes = 8 : b2.lanes = 1073741824, null;
      g = d3.children;
      a3 = d3.fallback;
      return f ? (d3 = b2.mode, f = b2.child, g = { mode: "hidden", children: g }, 0 === (d3 & 1) && null !== f ? (f.childLanes = 0, f.pendingProps = g) : f = pj(g, d3, 0, null), a3 = Tg(a3, d3, c2, null), f.return = b2, a3.return = b2, f.sibling = a3, b2.child = f, b2.child.memoizedState = nj(c2), b2.memoizedState = mj, a3) : qj(b2, g);
    }
    e2 = a3.memoizedState;
    if (null !== e2 && (h3 = e2.dehydrated, null !== h3)) return rj(a3, b2, g, d3, h3, e2, c2);
    if (f) {
      f = d3.fallback;
      g = b2.mode;
      e2 = a3.child;
      h3 = e2.sibling;
      var k2 = { mode: "hidden", children: d3.children };
      0 === (g & 1) && b2.child !== e2 ? (d3 = b2.child, d3.childLanes = 0, d3.pendingProps = k2, b2.deletions = null) : (d3 = Pg(e2, k2), d3.subtreeFlags = e2.subtreeFlags & 14680064);
      null !== h3 ? f = Pg(h3, f) : (f = Tg(f, g, c2, null), f.flags |= 2);
      f.return = b2;
      d3.return = b2;
      d3.sibling = f;
      b2.child = d3;
      d3 = f;
      f = b2.child;
      g = a3.child.memoizedState;
      g = null === g ? nj(c2) : { baseLanes: g.baseLanes | c2, cachePool: null, transitions: g.transitions };
      f.memoizedState = g;
      f.childLanes = a3.childLanes & ~c2;
      b2.memoizedState = mj;
      return d3;
    }
    f = a3.child;
    a3 = f.sibling;
    d3 = Pg(f, { mode: "visible", children: d3.children });
    0 === (b2.mode & 1) && (d3.lanes = c2);
    d3.return = b2;
    d3.sibling = null;
    null !== a3 && (c2 = b2.deletions, null === c2 ? (b2.deletions = [a3], b2.flags |= 16) : c2.push(a3));
    b2.child = d3;
    b2.memoizedState = null;
    return d3;
  }
  function qj(a3, b2) {
    b2 = pj({ mode: "visible", children: b2 }, a3.mode, 0, null);
    b2.return = a3;
    return a3.child = b2;
  }
  function sj(a3, b2, c2, d3) {
    null !== d3 && Jg(d3);
    Ug(b2, a3.child, null, c2);
    a3 = qj(b2, b2.pendingProps.children);
    a3.flags |= 2;
    b2.memoizedState = null;
    return a3;
  }
  function rj(a3, b2, c2, d3, e2, f, g) {
    if (c2) {
      if (b2.flags & 256) return b2.flags &= -257, d3 = Ki(Error(p(422))), sj(a3, b2, g, d3);
      if (null !== b2.memoizedState) return b2.child = a3.child, b2.flags |= 128, null;
      f = d3.fallback;
      e2 = b2.mode;
      d3 = pj({ mode: "visible", children: d3.children }, e2, 0, null);
      f = Tg(f, e2, g, null);
      f.flags |= 2;
      d3.return = b2;
      f.return = b2;
      d3.sibling = f;
      b2.child = d3;
      0 !== (b2.mode & 1) && Ug(b2, a3.child, null, g);
      b2.child.memoizedState = nj(g);
      b2.memoizedState = mj;
      return f;
    }
    if (0 === (b2.mode & 1)) return sj(a3, b2, g, null);
    if ("$!" === e2.data) {
      d3 = e2.nextSibling && e2.nextSibling.dataset;
      if (d3) var h3 = d3.dgst;
      d3 = h3;
      f = Error(p(419));
      d3 = Ki(f, d3, void 0);
      return sj(a3, b2, g, d3);
    }
    h3 = 0 !== (g & a3.childLanes);
    if (dh || h3) {
      d3 = Q2;
      if (null !== d3) {
        switch (g & -g) {
          case 4:
            e2 = 2;
            break;
          case 16:
            e2 = 8;
            break;
          case 64:
          case 128:
          case 256:
          case 512:
          case 1024:
          case 2048:
          case 4096:
          case 8192:
          case 16384:
          case 32768:
          case 65536:
          case 131072:
          case 262144:
          case 524288:
          case 1048576:
          case 2097152:
          case 4194304:
          case 8388608:
          case 16777216:
          case 33554432:
          case 67108864:
            e2 = 32;
            break;
          case 536870912:
            e2 = 268435456;
            break;
          default:
            e2 = 0;
        }
        e2 = 0 !== (e2 & (d3.suspendedLanes | g)) ? 0 : e2;
        0 !== e2 && e2 !== f.retryLane && (f.retryLane = e2, ih(a3, e2), gi(d3, a3, e2, -1));
      }
      tj();
      d3 = Ki(Error(p(421)));
      return sj(a3, b2, g, d3);
    }
    if ("$?" === e2.data) return b2.flags |= 128, b2.child = a3.child, b2 = uj.bind(null, a3), e2._reactRetry = b2, null;
    a3 = f.treeContext;
    yg = Lf(e2.nextSibling);
    xg = b2;
    I2 = true;
    zg = null;
    null !== a3 && (og[pg++] = rg, og[pg++] = sg, og[pg++] = qg, rg = a3.id, sg = a3.overflow, qg = b2);
    b2 = qj(b2, d3.children);
    b2.flags |= 4096;
    return b2;
  }
  function vj(a3, b2, c2) {
    a3.lanes |= b2;
    var d3 = a3.alternate;
    null !== d3 && (d3.lanes |= b2);
    bh(a3.return, b2, c2);
  }
  function wj(a3, b2, c2, d3, e2) {
    var f = a3.memoizedState;
    null === f ? a3.memoizedState = { isBackwards: b2, rendering: null, renderingStartTime: 0, last: d3, tail: c2, tailMode: e2 } : (f.isBackwards = b2, f.rendering = null, f.renderingStartTime = 0, f.last = d3, f.tail = c2, f.tailMode = e2);
  }
  function xj(a3, b2, c2) {
    var d3 = b2.pendingProps, e2 = d3.revealOrder, f = d3.tail;
    Xi(a3, b2, d3.children, c2);
    d3 = L2.current;
    if (0 !== (d3 & 2)) d3 = d3 & 1 | 2, b2.flags |= 128;
    else {
      if (null !== a3 && 0 !== (a3.flags & 128)) a: for (a3 = b2.child; null !== a3; ) {
        if (13 === a3.tag) null !== a3.memoizedState && vj(a3, c2, b2);
        else if (19 === a3.tag) vj(a3, c2, b2);
        else if (null !== a3.child) {
          a3.child.return = a3;
          a3 = a3.child;
          continue;
        }
        if (a3 === b2) break a;
        for (; null === a3.sibling; ) {
          if (null === a3.return || a3.return === b2) break a;
          a3 = a3.return;
        }
        a3.sibling.return = a3.return;
        a3 = a3.sibling;
      }
      d3 &= 1;
    }
    G2(L2, d3);
    if (0 === (b2.mode & 1)) b2.memoizedState = null;
    else switch (e2) {
      case "forwards":
        c2 = b2.child;
        for (e2 = null; null !== c2; ) a3 = c2.alternate, null !== a3 && null === Ch(a3) && (e2 = c2), c2 = c2.sibling;
        c2 = e2;
        null === c2 ? (e2 = b2.child, b2.child = null) : (e2 = c2.sibling, c2.sibling = null);
        wj(b2, false, e2, c2, f);
        break;
      case "backwards":
        c2 = null;
        e2 = b2.child;
        for (b2.child = null; null !== e2; ) {
          a3 = e2.alternate;
          if (null !== a3 && null === Ch(a3)) {
            b2.child = e2;
            break;
          }
          a3 = e2.sibling;
          e2.sibling = c2;
          c2 = e2;
          e2 = a3;
        }
        wj(b2, true, c2, null, f);
        break;
      case "together":
        wj(b2, false, null, null, void 0);
        break;
      default:
        b2.memoizedState = null;
    }
    return b2.child;
  }
  function ij(a3, b2) {
    0 === (b2.mode & 1) && null !== a3 && (a3.alternate = null, b2.alternate = null, b2.flags |= 2);
  }
  function Zi(a3, b2, c2) {
    null !== a3 && (b2.dependencies = a3.dependencies);
    rh |= b2.lanes;
    if (0 === (c2 & b2.childLanes)) return null;
    if (null !== a3 && b2.child !== a3.child) throw Error(p(153));
    if (null !== b2.child) {
      a3 = b2.child;
      c2 = Pg(a3, a3.pendingProps);
      b2.child = c2;
      for (c2.return = b2; null !== a3.sibling; ) a3 = a3.sibling, c2 = c2.sibling = Pg(a3, a3.pendingProps), c2.return = b2;
      c2.sibling = null;
    }
    return b2.child;
  }
  function yj(a3, b2, c2) {
    switch (b2.tag) {
      case 3:
        kj(b2);
        Ig();
        break;
      case 5:
        Ah(b2);
        break;
      case 1:
        Zf(b2.type) && cg(b2);
        break;
      case 4:
        yh(b2, b2.stateNode.containerInfo);
        break;
      case 10:
        var d3 = b2.type._context, e2 = b2.memoizedProps.value;
        G2(Wg, d3._currentValue);
        d3._currentValue = e2;
        break;
      case 13:
        d3 = b2.memoizedState;
        if (null !== d3) {
          if (null !== d3.dehydrated) return G2(L2, L2.current & 1), b2.flags |= 128, null;
          if (0 !== (c2 & b2.child.childLanes)) return oj(a3, b2, c2);
          G2(L2, L2.current & 1);
          a3 = Zi(a3, b2, c2);
          return null !== a3 ? a3.sibling : null;
        }
        G2(L2, L2.current & 1);
        break;
      case 19:
        d3 = 0 !== (c2 & b2.childLanes);
        if (0 !== (a3.flags & 128)) {
          if (d3) return xj(a3, b2, c2);
          b2.flags |= 128;
        }
        e2 = b2.memoizedState;
        null !== e2 && (e2.rendering = null, e2.tail = null, e2.lastEffect = null);
        G2(L2, L2.current);
        if (d3) break;
        else return null;
      case 22:
      case 23:
        return b2.lanes = 0, dj(a3, b2, c2);
    }
    return Zi(a3, b2, c2);
  }
  var zj, Aj, Bj, Cj;
  zj = function(a3, b2) {
    for (var c2 = b2.child; null !== c2; ) {
      if (5 === c2.tag || 6 === c2.tag) a3.appendChild(c2.stateNode);
      else if (4 !== c2.tag && null !== c2.child) {
        c2.child.return = c2;
        c2 = c2.child;
        continue;
      }
      if (c2 === b2) break;
      for (; null === c2.sibling; ) {
        if (null === c2.return || c2.return === b2) return;
        c2 = c2.return;
      }
      c2.sibling.return = c2.return;
      c2 = c2.sibling;
    }
  };
  Aj = function() {
  };
  Bj = function(a3, b2, c2, d3) {
    var e2 = a3.memoizedProps;
    if (e2 !== d3) {
      a3 = b2.stateNode;
      xh(uh.current);
      var f = null;
      switch (c2) {
        case "input":
          e2 = Ya(a3, e2);
          d3 = Ya(a3, d3);
          f = [];
          break;
        case "select":
          e2 = A({}, e2, { value: void 0 });
          d3 = A({}, d3, { value: void 0 });
          f = [];
          break;
        case "textarea":
          e2 = gb(a3, e2);
          d3 = gb(a3, d3);
          f = [];
          break;
        default:
          "function" !== typeof e2.onClick && "function" === typeof d3.onClick && (a3.onclick = Bf);
      }
      ub(c2, d3);
      var g;
      c2 = null;
      for (l in e2) if (!d3.hasOwnProperty(l) && e2.hasOwnProperty(l) && null != e2[l]) if ("style" === l) {
        var h3 = e2[l];
        for (g in h3) h3.hasOwnProperty(g) && (c2 || (c2 = {}), c2[g] = "");
      } else "dangerouslySetInnerHTML" !== l && "children" !== l && "suppressContentEditableWarning" !== l && "suppressHydrationWarning" !== l && "autoFocus" !== l && (ea.hasOwnProperty(l) ? f || (f = []) : (f = f || []).push(l, null));
      for (l in d3) {
        var k2 = d3[l];
        h3 = null != e2 ? e2[l] : void 0;
        if (d3.hasOwnProperty(l) && k2 !== h3 && (null != k2 || null != h3)) if ("style" === l) if (h3) {
          for (g in h3) !h3.hasOwnProperty(g) || k2 && k2.hasOwnProperty(g) || (c2 || (c2 = {}), c2[g] = "");
          for (g in k2) k2.hasOwnProperty(g) && h3[g] !== k2[g] && (c2 || (c2 = {}), c2[g] = k2[g]);
        } else c2 || (f || (f = []), f.push(
          l,
          c2
        )), c2 = k2;
        else "dangerouslySetInnerHTML" === l ? (k2 = k2 ? k2.__html : void 0, h3 = h3 ? h3.__html : void 0, null != k2 && h3 !== k2 && (f = f || []).push(l, k2)) : "children" === l ? "string" !== typeof k2 && "number" !== typeof k2 || (f = f || []).push(l, "" + k2) : "suppressContentEditableWarning" !== l && "suppressHydrationWarning" !== l && (ea.hasOwnProperty(l) ? (null != k2 && "onScroll" === l && D2("scroll", a3), f || h3 === k2 || (f = [])) : (f = f || []).push(l, k2));
      }
      c2 && (f = f || []).push("style", c2);
      var l = f;
      if (b2.updateQueue = l) b2.flags |= 4;
    }
  };
  Cj = function(a3, b2, c2, d3) {
    c2 !== d3 && (b2.flags |= 4);
  };
  function Dj(a3, b2) {
    if (!I2) switch (a3.tailMode) {
      case "hidden":
        b2 = a3.tail;
        for (var c2 = null; null !== b2; ) null !== b2.alternate && (c2 = b2), b2 = b2.sibling;
        null === c2 ? a3.tail = null : c2.sibling = null;
        break;
      case "collapsed":
        c2 = a3.tail;
        for (var d3 = null; null !== c2; ) null !== c2.alternate && (d3 = c2), c2 = c2.sibling;
        null === d3 ? b2 || null === a3.tail ? a3.tail = null : a3.tail.sibling = null : d3.sibling = null;
    }
  }
  function S3(a3) {
    var b2 = null !== a3.alternate && a3.alternate.child === a3.child, c2 = 0, d3 = 0;
    if (b2) for (var e2 = a3.child; null !== e2; ) c2 |= e2.lanes | e2.childLanes, d3 |= e2.subtreeFlags & 14680064, d3 |= e2.flags & 14680064, e2.return = a3, e2 = e2.sibling;
    else for (e2 = a3.child; null !== e2; ) c2 |= e2.lanes | e2.childLanes, d3 |= e2.subtreeFlags, d3 |= e2.flags, e2.return = a3, e2 = e2.sibling;
    a3.subtreeFlags |= d3;
    a3.childLanes = c2;
    return b2;
  }
  function Ej(a3, b2, c2) {
    var d3 = b2.pendingProps;
    wg(b2);
    switch (b2.tag) {
      case 2:
      case 16:
      case 15:
      case 0:
      case 11:
      case 7:
      case 8:
      case 12:
      case 9:
      case 14:
        return S3(b2), null;
      case 1:
        return Zf(b2.type) && $f(), S3(b2), null;
      case 3:
        d3 = b2.stateNode;
        zh();
        E2(Wf);
        E2(H3);
        Eh();
        d3.pendingContext && (d3.context = d3.pendingContext, d3.pendingContext = null);
        if (null === a3 || null === a3.child) Gg(b2) ? b2.flags |= 4 : null === a3 || a3.memoizedState.isDehydrated && 0 === (b2.flags & 256) || (b2.flags |= 1024, null !== zg && (Fj(zg), zg = null));
        Aj(a3, b2);
        S3(b2);
        return null;
      case 5:
        Bh(b2);
        var e2 = xh(wh.current);
        c2 = b2.type;
        if (null !== a3 && null != b2.stateNode) Bj(a3, b2, c2, d3, e2), a3.ref !== b2.ref && (b2.flags |= 512, b2.flags |= 2097152);
        else {
          if (!d3) {
            if (null === b2.stateNode) throw Error(p(166));
            S3(b2);
            return null;
          }
          a3 = xh(uh.current);
          if (Gg(b2)) {
            d3 = b2.stateNode;
            c2 = b2.type;
            var f = b2.memoizedProps;
            d3[Of] = b2;
            d3[Pf] = f;
            a3 = 0 !== (b2.mode & 1);
            switch (c2) {
              case "dialog":
                D2("cancel", d3);
                D2("close", d3);
                break;
              case "iframe":
              case "object":
              case "embed":
                D2("load", d3);
                break;
              case "video":
              case "audio":
                for (e2 = 0; e2 < lf.length; e2++) D2(lf[e2], d3);
                break;
              case "source":
                D2("error", d3);
                break;
              case "img":
              case "image":
              case "link":
                D2(
                  "error",
                  d3
                );
                D2("load", d3);
                break;
              case "details":
                D2("toggle", d3);
                break;
              case "input":
                Za(d3, f);
                D2("invalid", d3);
                break;
              case "select":
                d3._wrapperState = { wasMultiple: !!f.multiple };
                D2("invalid", d3);
                break;
              case "textarea":
                hb(d3, f), D2("invalid", d3);
            }
            ub(c2, f);
            e2 = null;
            for (var g in f) if (f.hasOwnProperty(g)) {
              var h3 = f[g];
              "children" === g ? "string" === typeof h3 ? d3.textContent !== h3 && (true !== f.suppressHydrationWarning && Af(d3.textContent, h3, a3), e2 = ["children", h3]) : "number" === typeof h3 && d3.textContent !== "" + h3 && (true !== f.suppressHydrationWarning && Af(
                d3.textContent,
                h3,
                a3
              ), e2 = ["children", "" + h3]) : ea.hasOwnProperty(g) && null != h3 && "onScroll" === g && D2("scroll", d3);
            }
            switch (c2) {
              case "input":
                Va(d3);
                db(d3, f, true);
                break;
              case "textarea":
                Va(d3);
                jb(d3);
                break;
              case "select":
              case "option":
                break;
              default:
                "function" === typeof f.onClick && (d3.onclick = Bf);
            }
            d3 = e2;
            b2.updateQueue = d3;
            null !== d3 && (b2.flags |= 4);
          } else {
            g = 9 === e2.nodeType ? e2 : e2.ownerDocument;
            "http://www.w3.org/1999/xhtml" === a3 && (a3 = kb(c2));
            "http://www.w3.org/1999/xhtml" === a3 ? "script" === c2 ? (a3 = g.createElement("div"), a3.innerHTML = "<script><\/script>", a3 = a3.removeChild(a3.firstChild)) : "string" === typeof d3.is ? a3 = g.createElement(c2, { is: d3.is }) : (a3 = g.createElement(c2), "select" === c2 && (g = a3, d3.multiple ? g.multiple = true : d3.size && (g.size = d3.size))) : a3 = g.createElementNS(a3, c2);
            a3[Of] = b2;
            a3[Pf] = d3;
            zj(a3, b2, false, false);
            b2.stateNode = a3;
            a: {
              g = vb(c2, d3);
              switch (c2) {
                case "dialog":
                  D2("cancel", a3);
                  D2("close", a3);
                  e2 = d3;
                  break;
                case "iframe":
                case "object":
                case "embed":
                  D2("load", a3);
                  e2 = d3;
                  break;
                case "video":
                case "audio":
                  for (e2 = 0; e2 < lf.length; e2++) D2(lf[e2], a3);
                  e2 = d3;
                  break;
                case "source":
                  D2("error", a3);
                  e2 = d3;
                  break;
                case "img":
                case "image":
                case "link":
                  D2(
                    "error",
                    a3
                  );
                  D2("load", a3);
                  e2 = d3;
                  break;
                case "details":
                  D2("toggle", a3);
                  e2 = d3;
                  break;
                case "input":
                  Za(a3, d3);
                  e2 = Ya(a3, d3);
                  D2("invalid", a3);
                  break;
                case "option":
                  e2 = d3;
                  break;
                case "select":
                  a3._wrapperState = { wasMultiple: !!d3.multiple };
                  e2 = A({}, d3, { value: void 0 });
                  D2("invalid", a3);
                  break;
                case "textarea":
                  hb(a3, d3);
                  e2 = gb(a3, d3);
                  D2("invalid", a3);
                  break;
                default:
                  e2 = d3;
              }
              ub(c2, e2);
              h3 = e2;
              for (f in h3) if (h3.hasOwnProperty(f)) {
                var k2 = h3[f];
                "style" === f ? sb(a3, k2) : "dangerouslySetInnerHTML" === f ? (k2 = k2 ? k2.__html : void 0, null != k2 && nb(a3, k2)) : "children" === f ? "string" === typeof k2 ? ("textarea" !== c2 || "" !== k2) && ob(a3, k2) : "number" === typeof k2 && ob(a3, "" + k2) : "suppressContentEditableWarning" !== f && "suppressHydrationWarning" !== f && "autoFocus" !== f && (ea.hasOwnProperty(f) ? null != k2 && "onScroll" === f && D2("scroll", a3) : null != k2 && ta(a3, f, k2, g));
              }
              switch (c2) {
                case "input":
                  Va(a3);
                  db(a3, d3, false);
                  break;
                case "textarea":
                  Va(a3);
                  jb(a3);
                  break;
                case "option":
                  null != d3.value && a3.setAttribute("value", "" + Sa(d3.value));
                  break;
                case "select":
                  a3.multiple = !!d3.multiple;
                  f = d3.value;
                  null != f ? fb(a3, !!d3.multiple, f, false) : null != d3.defaultValue && fb(
                    a3,
                    !!d3.multiple,
                    d3.defaultValue,
                    true
                  );
                  break;
                default:
                  "function" === typeof e2.onClick && (a3.onclick = Bf);
              }
              switch (c2) {
                case "button":
                case "input":
                case "select":
                case "textarea":
                  d3 = !!d3.autoFocus;
                  break a;
                case "img":
                  d3 = true;
                  break a;
                default:
                  d3 = false;
              }
            }
            d3 && (b2.flags |= 4);
          }
          null !== b2.ref && (b2.flags |= 512, b2.flags |= 2097152);
        }
        S3(b2);
        return null;
      case 6:
        if (a3 && null != b2.stateNode) Cj(a3, b2, a3.memoizedProps, d3);
        else {
          if ("string" !== typeof d3 && null === b2.stateNode) throw Error(p(166));
          c2 = xh(wh.current);
          xh(uh.current);
          if (Gg(b2)) {
            d3 = b2.stateNode;
            c2 = b2.memoizedProps;
            d3[Of] = b2;
            if (f = d3.nodeValue !== c2) {
              if (a3 = xg, null !== a3) switch (a3.tag) {
                case 3:
                  Af(d3.nodeValue, c2, 0 !== (a3.mode & 1));
                  break;
                case 5:
                  true !== a3.memoizedProps.suppressHydrationWarning && Af(d3.nodeValue, c2, 0 !== (a3.mode & 1));
              }
            }
            f && (b2.flags |= 4);
          } else d3 = (9 === c2.nodeType ? c2 : c2.ownerDocument).createTextNode(d3), d3[Of] = b2, b2.stateNode = d3;
        }
        S3(b2);
        return null;
      case 13:
        E2(L2);
        d3 = b2.memoizedState;
        if (null === a3 || null !== a3.memoizedState && null !== a3.memoizedState.dehydrated) {
          if (I2 && null !== yg && 0 !== (b2.mode & 1) && 0 === (b2.flags & 128)) Hg(), Ig(), b2.flags |= 98560, f = false;
          else if (f = Gg(b2), null !== d3 && null !== d3.dehydrated) {
            if (null === a3) {
              if (!f) throw Error(p(318));
              f = b2.memoizedState;
              f = null !== f ? f.dehydrated : null;
              if (!f) throw Error(p(317));
              f[Of] = b2;
            } else Ig(), 0 === (b2.flags & 128) && (b2.memoizedState = null), b2.flags |= 4;
            S3(b2);
            f = false;
          } else null !== zg && (Fj(zg), zg = null), f = true;
          if (!f) return b2.flags & 65536 ? b2 : null;
        }
        if (0 !== (b2.flags & 128)) return b2.lanes = c2, b2;
        d3 = null !== d3;
        d3 !== (null !== a3 && null !== a3.memoizedState) && d3 && (b2.child.flags |= 8192, 0 !== (b2.mode & 1) && (null === a3 || 0 !== (L2.current & 1) ? 0 === T2 && (T2 = 3) : tj()));
        null !== b2.updateQueue && (b2.flags |= 4);
        S3(b2);
        return null;
      case 4:
        return zh(), Aj(a3, b2), null === a3 && sf(b2.stateNode.containerInfo), S3(b2), null;
      case 10:
        return ah(b2.type._context), S3(b2), null;
      case 17:
        return Zf(b2.type) && $f(), S3(b2), null;
      case 19:
        E2(L2);
        f = b2.memoizedState;
        if (null === f) return S3(b2), null;
        d3 = 0 !== (b2.flags & 128);
        g = f.rendering;
        if (null === g) if (d3) Dj(f, false);
        else {
          if (0 !== T2 || null !== a3 && 0 !== (a3.flags & 128)) for (a3 = b2.child; null !== a3; ) {
            g = Ch(a3);
            if (null !== g) {
              b2.flags |= 128;
              Dj(f, false);
              d3 = g.updateQueue;
              null !== d3 && (b2.updateQueue = d3, b2.flags |= 4);
              b2.subtreeFlags = 0;
              d3 = c2;
              for (c2 = b2.child; null !== c2; ) f = c2, a3 = d3, f.flags &= 14680066, g = f.alternate, null === g ? (f.childLanes = 0, f.lanes = a3, f.child = null, f.subtreeFlags = 0, f.memoizedProps = null, f.memoizedState = null, f.updateQueue = null, f.dependencies = null, f.stateNode = null) : (f.childLanes = g.childLanes, f.lanes = g.lanes, f.child = g.child, f.subtreeFlags = 0, f.deletions = null, f.memoizedProps = g.memoizedProps, f.memoizedState = g.memoizedState, f.updateQueue = g.updateQueue, f.type = g.type, a3 = g.dependencies, f.dependencies = null === a3 ? null : { lanes: a3.lanes, firstContext: a3.firstContext }), c2 = c2.sibling;
              G2(L2, L2.current & 1 | 2);
              return b2.child;
            }
            a3 = a3.sibling;
          }
          null !== f.tail && B2() > Gj && (b2.flags |= 128, d3 = true, Dj(f, false), b2.lanes = 4194304);
        }
        else {
          if (!d3) if (a3 = Ch(g), null !== a3) {
            if (b2.flags |= 128, d3 = true, c2 = a3.updateQueue, null !== c2 && (b2.updateQueue = c2, b2.flags |= 4), Dj(f, true), null === f.tail && "hidden" === f.tailMode && !g.alternate && !I2) return S3(b2), null;
          } else 2 * B2() - f.renderingStartTime > Gj && 1073741824 !== c2 && (b2.flags |= 128, d3 = true, Dj(f, false), b2.lanes = 4194304);
          f.isBackwards ? (g.sibling = b2.child, b2.child = g) : (c2 = f.last, null !== c2 ? c2.sibling = g : b2.child = g, f.last = g);
        }
        if (null !== f.tail) return b2 = f.tail, f.rendering = b2, f.tail = b2.sibling, f.renderingStartTime = B2(), b2.sibling = null, c2 = L2.current, G2(L2, d3 ? c2 & 1 | 2 : c2 & 1), b2;
        S3(b2);
        return null;
      case 22:
      case 23:
        return Hj(), d3 = null !== b2.memoizedState, null !== a3 && null !== a3.memoizedState !== d3 && (b2.flags |= 8192), d3 && 0 !== (b2.mode & 1) ? 0 !== (fj & 1073741824) && (S3(b2), b2.subtreeFlags & 6 && (b2.flags |= 8192)) : S3(b2), null;
      case 24:
        return null;
      case 25:
        return null;
    }
    throw Error(p(156, b2.tag));
  }
  function Ij(a3, b2) {
    wg(b2);
    switch (b2.tag) {
      case 1:
        return Zf(b2.type) && $f(), a3 = b2.flags, a3 & 65536 ? (b2.flags = a3 & -65537 | 128, b2) : null;
      case 3:
        return zh(), E2(Wf), E2(H3), Eh(), a3 = b2.flags, 0 !== (a3 & 65536) && 0 === (a3 & 128) ? (b2.flags = a3 & -65537 | 128, b2) : null;
      case 5:
        return Bh(b2), null;
      case 13:
        E2(L2);
        a3 = b2.memoizedState;
        if (null !== a3 && null !== a3.dehydrated) {
          if (null === b2.alternate) throw Error(p(340));
          Ig();
        }
        a3 = b2.flags;
        return a3 & 65536 ? (b2.flags = a3 & -65537 | 128, b2) : null;
      case 19:
        return E2(L2), null;
      case 4:
        return zh(), null;
      case 10:
        return ah(b2.type._context), null;
      case 22:
      case 23:
        return Hj(), null;
      case 24:
        return null;
      default:
        return null;
    }
  }
  var Jj = false, U = false, Kj = "function" === typeof WeakSet ? WeakSet : Set, V = null;
  function Lj(a3, b2) {
    var c2 = a3.ref;
    if (null !== c2) if ("function" === typeof c2) try {
      c2(null);
    } catch (d3) {
      W(a3, b2, d3);
    }
    else c2.current = null;
  }
  function Mj(a3, b2, c2) {
    try {
      c2();
    } catch (d3) {
      W(a3, b2, d3);
    }
  }
  var Nj = false;
  function Oj(a3, b2) {
    Cf = dd;
    a3 = Me();
    if (Ne(a3)) {
      if ("selectionStart" in a3) var c2 = { start: a3.selectionStart, end: a3.selectionEnd };
      else a: {
        c2 = (c2 = a3.ownerDocument) && c2.defaultView || window;
        var d3 = c2.getSelection && c2.getSelection();
        if (d3 && 0 !== d3.rangeCount) {
          c2 = d3.anchorNode;
          var e2 = d3.anchorOffset, f = d3.focusNode;
          d3 = d3.focusOffset;
          try {
            c2.nodeType, f.nodeType;
          } catch (F) {
            c2 = null;
            break a;
          }
          var g = 0, h3 = -1, k2 = -1, l = 0, m3 = 0, q2 = a3, r2 = null;
          b: for (; ; ) {
            for (var y3; ; ) {
              q2 !== c2 || 0 !== e2 && 3 !== q2.nodeType || (h3 = g + e2);
              q2 !== f || 0 !== d3 && 3 !== q2.nodeType || (k2 = g + d3);
              3 === q2.nodeType && (g += q2.nodeValue.length);
              if (null === (y3 = q2.firstChild)) break;
              r2 = q2;
              q2 = y3;
            }
            for (; ; ) {
              if (q2 === a3) break b;
              r2 === c2 && ++l === e2 && (h3 = g);
              r2 === f && ++m3 === d3 && (k2 = g);
              if (null !== (y3 = q2.nextSibling)) break;
              q2 = r2;
              r2 = q2.parentNode;
            }
            q2 = y3;
          }
          c2 = -1 === h3 || -1 === k2 ? null : { start: h3, end: k2 };
        } else c2 = null;
      }
      c2 = c2 || { start: 0, end: 0 };
    } else c2 = null;
    Df = { focusedElem: a3, selectionRange: c2 };
    dd = false;
    for (V = b2; null !== V; ) if (b2 = V, a3 = b2.child, 0 !== (b2.subtreeFlags & 1028) && null !== a3) a3.return = b2, V = a3;
    else for (; null !== V; ) {
      b2 = V;
      try {
        var n = b2.alternate;
        if (0 !== (b2.flags & 1024)) switch (b2.tag) {
          case 0:
          case 11:
          case 15:
            break;
          case 1:
            if (null !== n) {
              var t2 = n.memoizedProps, J = n.memoizedState, x2 = b2.stateNode, w2 = x2.getSnapshotBeforeUpdate(b2.elementType === b2.type ? t2 : Ci(b2.type, t2), J);
              x2.__reactInternalSnapshotBeforeUpdate = w2;
            }
            break;
          case 3:
            var u2 = b2.stateNode.containerInfo;
            1 === u2.nodeType ? u2.textContent = "" : 9 === u2.nodeType && u2.documentElement && u2.removeChild(u2.documentElement);
            break;
          case 5:
          case 6:
          case 4:
          case 17:
            break;
          default:
            throw Error(p(163));
        }
      } catch (F) {
        W(b2, b2.return, F);
      }
      a3 = b2.sibling;
      if (null !== a3) {
        a3.return = b2.return;
        V = a3;
        break;
      }
      V = b2.return;
    }
    n = Nj;
    Nj = false;
    return n;
  }
  function Pj(a3, b2, c2) {
    var d3 = b2.updateQueue;
    d3 = null !== d3 ? d3.lastEffect : null;
    if (null !== d3) {
      var e2 = d3 = d3.next;
      do {
        if ((e2.tag & a3) === a3) {
          var f = e2.destroy;
          e2.destroy = void 0;
          void 0 !== f && Mj(b2, c2, f);
        }
        e2 = e2.next;
      } while (e2 !== d3);
    }
  }
  function Qj(a3, b2) {
    b2 = b2.updateQueue;
    b2 = null !== b2 ? b2.lastEffect : null;
    if (null !== b2) {
      var c2 = b2 = b2.next;
      do {
        if ((c2.tag & a3) === a3) {
          var d3 = c2.create;
          c2.destroy = d3();
        }
        c2 = c2.next;
      } while (c2 !== b2);
    }
  }
  function Rj(a3) {
    var b2 = a3.ref;
    if (null !== b2) {
      var c2 = a3.stateNode;
      switch (a3.tag) {
        case 5:
          a3 = c2;
          break;
        default:
          a3 = c2;
      }
      "function" === typeof b2 ? b2(a3) : b2.current = a3;
    }
  }
  function Sj(a3) {
    var b2 = a3.alternate;
    null !== b2 && (a3.alternate = null, Sj(b2));
    a3.child = null;
    a3.deletions = null;
    a3.sibling = null;
    5 === a3.tag && (b2 = a3.stateNode, null !== b2 && (delete b2[Of], delete b2[Pf], delete b2[of], delete b2[Qf], delete b2[Rf]));
    a3.stateNode = null;
    a3.return = null;
    a3.dependencies = null;
    a3.memoizedProps = null;
    a3.memoizedState = null;
    a3.pendingProps = null;
    a3.stateNode = null;
    a3.updateQueue = null;
  }
  function Tj(a3) {
    return 5 === a3.tag || 3 === a3.tag || 4 === a3.tag;
  }
  function Uj(a3) {
    a: for (; ; ) {
      for (; null === a3.sibling; ) {
        if (null === a3.return || Tj(a3.return)) return null;
        a3 = a3.return;
      }
      a3.sibling.return = a3.return;
      for (a3 = a3.sibling; 5 !== a3.tag && 6 !== a3.tag && 18 !== a3.tag; ) {
        if (a3.flags & 2) continue a;
        if (null === a3.child || 4 === a3.tag) continue a;
        else a3.child.return = a3, a3 = a3.child;
      }
      if (!(a3.flags & 2)) return a3.stateNode;
    }
  }
  function Vj(a3, b2, c2) {
    var d3 = a3.tag;
    if (5 === d3 || 6 === d3) a3 = a3.stateNode, b2 ? 8 === c2.nodeType ? c2.parentNode.insertBefore(a3, b2) : c2.insertBefore(a3, b2) : (8 === c2.nodeType ? (b2 = c2.parentNode, b2.insertBefore(a3, c2)) : (b2 = c2, b2.appendChild(a3)), c2 = c2._reactRootContainer, null !== c2 && void 0 !== c2 || null !== b2.onclick || (b2.onclick = Bf));
    else if (4 !== d3 && (a3 = a3.child, null !== a3)) for (Vj(a3, b2, c2), a3 = a3.sibling; null !== a3; ) Vj(a3, b2, c2), a3 = a3.sibling;
  }
  function Wj(a3, b2, c2) {
    var d3 = a3.tag;
    if (5 === d3 || 6 === d3) a3 = a3.stateNode, b2 ? c2.insertBefore(a3, b2) : c2.appendChild(a3);
    else if (4 !== d3 && (a3 = a3.child, null !== a3)) for (Wj(a3, b2, c2), a3 = a3.sibling; null !== a3; ) Wj(a3, b2, c2), a3 = a3.sibling;
  }
  var X2 = null, Xj = false;
  function Yj(a3, b2, c2) {
    for (c2 = c2.child; null !== c2; ) Zj(a3, b2, c2), c2 = c2.sibling;
  }
  function Zj(a3, b2, c2) {
    if (lc && "function" === typeof lc.onCommitFiberUnmount) try {
      lc.onCommitFiberUnmount(kc, c2);
    } catch (h3) {
    }
    switch (c2.tag) {
      case 5:
        U || Lj(c2, b2);
      case 6:
        var d3 = X2, e2 = Xj;
        X2 = null;
        Yj(a3, b2, c2);
        X2 = d3;
        Xj = e2;
        null !== X2 && (Xj ? (a3 = X2, c2 = c2.stateNode, 8 === a3.nodeType ? a3.parentNode.removeChild(c2) : a3.removeChild(c2)) : X2.removeChild(c2.stateNode));
        break;
      case 18:
        null !== X2 && (Xj ? (a3 = X2, c2 = c2.stateNode, 8 === a3.nodeType ? Kf(a3.parentNode, c2) : 1 === a3.nodeType && Kf(a3, c2), bd(a3)) : Kf(X2, c2.stateNode));
        break;
      case 4:
        d3 = X2;
        e2 = Xj;
        X2 = c2.stateNode.containerInfo;
        Xj = true;
        Yj(a3, b2, c2);
        X2 = d3;
        Xj = e2;
        break;
      case 0:
      case 11:
      case 14:
      case 15:
        if (!U && (d3 = c2.updateQueue, null !== d3 && (d3 = d3.lastEffect, null !== d3))) {
          e2 = d3 = d3.next;
          do {
            var f = e2, g = f.destroy;
            f = f.tag;
            void 0 !== g && (0 !== (f & 2) ? Mj(c2, b2, g) : 0 !== (f & 4) && Mj(c2, b2, g));
            e2 = e2.next;
          } while (e2 !== d3);
        }
        Yj(a3, b2, c2);
        break;
      case 1:
        if (!U && (Lj(c2, b2), d3 = c2.stateNode, "function" === typeof d3.componentWillUnmount)) try {
          d3.props = c2.memoizedProps, d3.state = c2.memoizedState, d3.componentWillUnmount();
        } catch (h3) {
          W(c2, b2, h3);
        }
        Yj(a3, b2, c2);
        break;
      case 21:
        Yj(a3, b2, c2);
        break;
      case 22:
        c2.mode & 1 ? (U = (d3 = U) || null !== c2.memoizedState, Yj(a3, b2, c2), U = d3) : Yj(a3, b2, c2);
        break;
      default:
        Yj(a3, b2, c2);
    }
  }
  function ak(a3) {
    var b2 = a3.updateQueue;
    if (null !== b2) {
      a3.updateQueue = null;
      var c2 = a3.stateNode;
      null === c2 && (c2 = a3.stateNode = new Kj());
      b2.forEach(function(b3) {
        var d3 = bk.bind(null, a3, b3);
        c2.has(b3) || (c2.add(b3), b3.then(d3, d3));
      });
    }
  }
  function ck(a3, b2) {
    var c2 = b2.deletions;
    if (null !== c2) for (var d3 = 0; d3 < c2.length; d3++) {
      var e2 = c2[d3];
      try {
        var f = a3, g = b2, h3 = g;
        a: for (; null !== h3; ) {
          switch (h3.tag) {
            case 5:
              X2 = h3.stateNode;
              Xj = false;
              break a;
            case 3:
              X2 = h3.stateNode.containerInfo;
              Xj = true;
              break a;
            case 4:
              X2 = h3.stateNode.containerInfo;
              Xj = true;
              break a;
          }
          h3 = h3.return;
        }
        if (null === X2) throw Error(p(160));
        Zj(f, g, e2);
        X2 = null;
        Xj = false;
        var k2 = e2.alternate;
        null !== k2 && (k2.return = null);
        e2.return = null;
      } catch (l) {
        W(e2, b2, l);
      }
    }
    if (b2.subtreeFlags & 12854) for (b2 = b2.child; null !== b2; ) dk(b2, a3), b2 = b2.sibling;
  }
  function dk(a3, b2) {
    var c2 = a3.alternate, d3 = a3.flags;
    switch (a3.tag) {
      case 0:
      case 11:
      case 14:
      case 15:
        ck(b2, a3);
        ek(a3);
        if (d3 & 4) {
          try {
            Pj(3, a3, a3.return), Qj(3, a3);
          } catch (t2) {
            W(a3, a3.return, t2);
          }
          try {
            Pj(5, a3, a3.return);
          } catch (t2) {
            W(a3, a3.return, t2);
          }
        }
        break;
      case 1:
        ck(b2, a3);
        ek(a3);
        d3 & 512 && null !== c2 && Lj(c2, c2.return);
        break;
      case 5:
        ck(b2, a3);
        ek(a3);
        d3 & 512 && null !== c2 && Lj(c2, c2.return);
        if (a3.flags & 32) {
          var e2 = a3.stateNode;
          try {
            ob(e2, "");
          } catch (t2) {
            W(a3, a3.return, t2);
          }
        }
        if (d3 & 4 && (e2 = a3.stateNode, null != e2)) {
          var f = a3.memoizedProps, g = null !== c2 ? c2.memoizedProps : f, h3 = a3.type, k2 = a3.updateQueue;
          a3.updateQueue = null;
          if (null !== k2) try {
            "input" === h3 && "radio" === f.type && null != f.name && ab(e2, f);
            vb(h3, g);
            var l = vb(h3, f);
            for (g = 0; g < k2.length; g += 2) {
              var m3 = k2[g], q2 = k2[g + 1];
              "style" === m3 ? sb(e2, q2) : "dangerouslySetInnerHTML" === m3 ? nb(e2, q2) : "children" === m3 ? ob(e2, q2) : ta(e2, m3, q2, l);
            }
            switch (h3) {
              case "input":
                bb(e2, f);
                break;
              case "textarea":
                ib(e2, f);
                break;
              case "select":
                var r2 = e2._wrapperState.wasMultiple;
                e2._wrapperState.wasMultiple = !!f.multiple;
                var y3 = f.value;
                null != y3 ? fb(e2, !!f.multiple, y3, false) : r2 !== !!f.multiple && (null != f.defaultValue ? fb(
                  e2,
                  !!f.multiple,
                  f.defaultValue,
                  true
                ) : fb(e2, !!f.multiple, f.multiple ? [] : "", false));
            }
            e2[Pf] = f;
          } catch (t2) {
            W(a3, a3.return, t2);
          }
        }
        break;
      case 6:
        ck(b2, a3);
        ek(a3);
        if (d3 & 4) {
          if (null === a3.stateNode) throw Error(p(162));
          e2 = a3.stateNode;
          f = a3.memoizedProps;
          try {
            e2.nodeValue = f;
          } catch (t2) {
            W(a3, a3.return, t2);
          }
        }
        break;
      case 3:
        ck(b2, a3);
        ek(a3);
        if (d3 & 4 && null !== c2 && c2.memoizedState.isDehydrated) try {
          bd(b2.containerInfo);
        } catch (t2) {
          W(a3, a3.return, t2);
        }
        break;
      case 4:
        ck(b2, a3);
        ek(a3);
        break;
      case 13:
        ck(b2, a3);
        ek(a3);
        e2 = a3.child;
        e2.flags & 8192 && (f = null !== e2.memoizedState, e2.stateNode.isHidden = f, !f || null !== e2.alternate && null !== e2.alternate.memoizedState || (fk = B2()));
        d3 & 4 && ak(a3);
        break;
      case 22:
        m3 = null !== c2 && null !== c2.memoizedState;
        a3.mode & 1 ? (U = (l = U) || m3, ck(b2, a3), U = l) : ck(b2, a3);
        ek(a3);
        if (d3 & 8192) {
          l = null !== a3.memoizedState;
          if ((a3.stateNode.isHidden = l) && !m3 && 0 !== (a3.mode & 1)) for (V = a3, m3 = a3.child; null !== m3; ) {
            for (q2 = V = m3; null !== V; ) {
              r2 = V;
              y3 = r2.child;
              switch (r2.tag) {
                case 0:
                case 11:
                case 14:
                case 15:
                  Pj(4, r2, r2.return);
                  break;
                case 1:
                  Lj(r2, r2.return);
                  var n = r2.stateNode;
                  if ("function" === typeof n.componentWillUnmount) {
                    d3 = r2;
                    c2 = r2.return;
                    try {
                      b2 = d3, n.props = b2.memoizedProps, n.state = b2.memoizedState, n.componentWillUnmount();
                    } catch (t2) {
                      W(d3, c2, t2);
                    }
                  }
                  break;
                case 5:
                  Lj(r2, r2.return);
                  break;
                case 22:
                  if (null !== r2.memoizedState) {
                    gk(q2);
                    continue;
                  }
              }
              null !== y3 ? (y3.return = r2, V = y3) : gk(q2);
            }
            m3 = m3.sibling;
          }
          a: for (m3 = null, q2 = a3; ; ) {
            if (5 === q2.tag) {
              if (null === m3) {
                m3 = q2;
                try {
                  e2 = q2.stateNode, l ? (f = e2.style, "function" === typeof f.setProperty ? f.setProperty("display", "none", "important") : f.display = "none") : (h3 = q2.stateNode, k2 = q2.memoizedProps.style, g = void 0 !== k2 && null !== k2 && k2.hasOwnProperty("display") ? k2.display : null, h3.style.display = rb("display", g));
                } catch (t2) {
                  W(a3, a3.return, t2);
                }
              }
            } else if (6 === q2.tag) {
              if (null === m3) try {
                q2.stateNode.nodeValue = l ? "" : q2.memoizedProps;
              } catch (t2) {
                W(a3, a3.return, t2);
              }
            } else if ((22 !== q2.tag && 23 !== q2.tag || null === q2.memoizedState || q2 === a3) && null !== q2.child) {
              q2.child.return = q2;
              q2 = q2.child;
              continue;
            }
            if (q2 === a3) break a;
            for (; null === q2.sibling; ) {
              if (null === q2.return || q2.return === a3) break a;
              m3 === q2 && (m3 = null);
              q2 = q2.return;
            }
            m3 === q2 && (m3 = null);
            q2.sibling.return = q2.return;
            q2 = q2.sibling;
          }
        }
        break;
      case 19:
        ck(b2, a3);
        ek(a3);
        d3 & 4 && ak(a3);
        break;
      case 21:
        break;
      default:
        ck(
          b2,
          a3
        ), ek(a3);
    }
  }
  function ek(a3) {
    var b2 = a3.flags;
    if (b2 & 2) {
      try {
        a: {
          for (var c2 = a3.return; null !== c2; ) {
            if (Tj(c2)) {
              var d3 = c2;
              break a;
            }
            c2 = c2.return;
          }
          throw Error(p(160));
        }
        switch (d3.tag) {
          case 5:
            var e2 = d3.stateNode;
            d3.flags & 32 && (ob(e2, ""), d3.flags &= -33);
            var f = Uj(a3);
            Wj(a3, f, e2);
            break;
          case 3:
          case 4:
            var g = d3.stateNode.containerInfo, h3 = Uj(a3);
            Vj(a3, h3, g);
            break;
          default:
            throw Error(p(161));
        }
      } catch (k2) {
        W(a3, a3.return, k2);
      }
      a3.flags &= -3;
    }
    b2 & 4096 && (a3.flags &= -4097);
  }
  function hk(a3, b2, c2) {
    V = a3;
    ik(a3);
  }
  function ik(a3, b2, c2) {
    for (var d3 = 0 !== (a3.mode & 1); null !== V; ) {
      var e2 = V, f = e2.child;
      if (22 === e2.tag && d3) {
        var g = null !== e2.memoizedState || Jj;
        if (!g) {
          var h3 = e2.alternate, k2 = null !== h3 && null !== h3.memoizedState || U;
          h3 = Jj;
          var l = U;
          Jj = g;
          if ((U = k2) && !l) for (V = e2; null !== V; ) g = V, k2 = g.child, 22 === g.tag && null !== g.memoizedState ? jk(e2) : null !== k2 ? (k2.return = g, V = k2) : jk(e2);
          for (; null !== f; ) V = f, ik(f), f = f.sibling;
          V = e2;
          Jj = h3;
          U = l;
        }
        kk(a3);
      } else 0 !== (e2.subtreeFlags & 8772) && null !== f ? (f.return = e2, V = f) : kk(a3);
    }
  }
  function kk(a3) {
    for (; null !== V; ) {
      var b2 = V;
      if (0 !== (b2.flags & 8772)) {
        var c2 = b2.alternate;
        try {
          if (0 !== (b2.flags & 8772)) switch (b2.tag) {
            case 0:
            case 11:
            case 15:
              U || Qj(5, b2);
              break;
            case 1:
              var d3 = b2.stateNode;
              if (b2.flags & 4 && !U) if (null === c2) d3.componentDidMount();
              else {
                var e2 = b2.elementType === b2.type ? c2.memoizedProps : Ci(b2.type, c2.memoizedProps);
                d3.componentDidUpdate(e2, c2.memoizedState, d3.__reactInternalSnapshotBeforeUpdate);
              }
              var f = b2.updateQueue;
              null !== f && sh(b2, f, d3);
              break;
            case 3:
              var g = b2.updateQueue;
              if (null !== g) {
                c2 = null;
                if (null !== b2.child) switch (b2.child.tag) {
                  case 5:
                    c2 = b2.child.stateNode;
                    break;
                  case 1:
                    c2 = b2.child.stateNode;
                }
                sh(b2, g, c2);
              }
              break;
            case 5:
              var h3 = b2.stateNode;
              if (null === c2 && b2.flags & 4) {
                c2 = h3;
                var k2 = b2.memoizedProps;
                switch (b2.type) {
                  case "button":
                  case "input":
                  case "select":
                  case "textarea":
                    k2.autoFocus && c2.focus();
                    break;
                  case "img":
                    k2.src && (c2.src = k2.src);
                }
              }
              break;
            case 6:
              break;
            case 4:
              break;
            case 12:
              break;
            case 13:
              if (null === b2.memoizedState) {
                var l = b2.alternate;
                if (null !== l) {
                  var m3 = l.memoizedState;
                  if (null !== m3) {
                    var q2 = m3.dehydrated;
                    null !== q2 && bd(q2);
                  }
                }
              }
              break;
            case 19:
            case 17:
            case 21:
            case 22:
            case 23:
            case 25:
              break;
            default:
              throw Error(p(163));
          }
          U || b2.flags & 512 && Rj(b2);
        } catch (r2) {
          W(b2, b2.return, r2);
        }
      }
      if (b2 === a3) {
        V = null;
        break;
      }
      c2 = b2.sibling;
      if (null !== c2) {
        c2.return = b2.return;
        V = c2;
        break;
      }
      V = b2.return;
    }
  }
  function gk(a3) {
    for (; null !== V; ) {
      var b2 = V;
      if (b2 === a3) {
        V = null;
        break;
      }
      var c2 = b2.sibling;
      if (null !== c2) {
        c2.return = b2.return;
        V = c2;
        break;
      }
      V = b2.return;
    }
  }
  function jk(a3) {
    for (; null !== V; ) {
      var b2 = V;
      try {
        switch (b2.tag) {
          case 0:
          case 11:
          case 15:
            var c2 = b2.return;
            try {
              Qj(4, b2);
            } catch (k2) {
              W(b2, c2, k2);
            }
            break;
          case 1:
            var d3 = b2.stateNode;
            if ("function" === typeof d3.componentDidMount) {
              var e2 = b2.return;
              try {
                d3.componentDidMount();
              } catch (k2) {
                W(b2, e2, k2);
              }
            }
            var f = b2.return;
            try {
              Rj(b2);
            } catch (k2) {
              W(b2, f, k2);
            }
            break;
          case 5:
            var g = b2.return;
            try {
              Rj(b2);
            } catch (k2) {
              W(b2, g, k2);
            }
        }
      } catch (k2) {
        W(b2, b2.return, k2);
      }
      if (b2 === a3) {
        V = null;
        break;
      }
      var h3 = b2.sibling;
      if (null !== h3) {
        h3.return = b2.return;
        V = h3;
        break;
      }
      V = b2.return;
    }
  }
  var lk = Math.ceil, mk = ua.ReactCurrentDispatcher, nk = ua.ReactCurrentOwner, ok = ua.ReactCurrentBatchConfig, K2 = 0, Q2 = null, Y2 = null, Z = 0, fj = 0, ej = Uf(0), T2 = 0, pk = null, rh = 0, qk = 0, rk = 0, sk = null, tk = null, fk = 0, Gj = Infinity, uk = null, Oi = false, Pi = null, Ri = null, vk = false, wk = null, xk = 0, yk = 0, zk = null, Ak = -1, Bk = 0;
  function R2() {
    return 0 !== (K2 & 6) ? B2() : -1 !== Ak ? Ak : Ak = B2();
  }
  function yi(a3) {
    if (0 === (a3.mode & 1)) return 1;
    if (0 !== (K2 & 2) && 0 !== Z) return Z & -Z;
    if (null !== Kg.transition) return 0 === Bk && (Bk = yc()), Bk;
    a3 = C;
    if (0 !== a3) return a3;
    a3 = window.event;
    a3 = void 0 === a3 ? 16 : jd(a3.type);
    return a3;
  }
  function gi(a3, b2, c2, d3) {
    if (50 < yk) throw yk = 0, zk = null, Error(p(185));
    Ac(a3, c2, d3);
    if (0 === (K2 & 2) || a3 !== Q2) a3 === Q2 && (0 === (K2 & 2) && (qk |= c2), 4 === T2 && Ck(a3, Z)), Dk(a3, d3), 1 === c2 && 0 === K2 && 0 === (b2.mode & 1) && (Gj = B2() + 500, fg && jg());
  }
  function Dk(a3, b2) {
    var c2 = a3.callbackNode;
    wc(a3, b2);
    var d3 = uc(a3, a3 === Q2 ? Z : 0);
    if (0 === d3) null !== c2 && bc(c2), a3.callbackNode = null, a3.callbackPriority = 0;
    else if (b2 = d3 & -d3, a3.callbackPriority !== b2) {
      null != c2 && bc(c2);
      if (1 === b2) 0 === a3.tag ? ig(Ek.bind(null, a3)) : hg(Ek.bind(null, a3)), Jf(function() {
        0 === (K2 & 6) && jg();
      }), c2 = null;
      else {
        switch (Dc(d3)) {
          case 1:
            c2 = fc;
            break;
          case 4:
            c2 = gc;
            break;
          case 16:
            c2 = hc;
            break;
          case 536870912:
            c2 = jc;
            break;
          default:
            c2 = hc;
        }
        c2 = Fk(c2, Gk.bind(null, a3));
      }
      a3.callbackPriority = b2;
      a3.callbackNode = c2;
    }
  }
  function Gk(a3, b2) {
    Ak = -1;
    Bk = 0;
    if (0 !== (K2 & 6)) throw Error(p(327));
    var c2 = a3.callbackNode;
    if (Hk() && a3.callbackNode !== c2) return null;
    var d3 = uc(a3, a3 === Q2 ? Z : 0);
    if (0 === d3) return null;
    if (0 !== (d3 & 30) || 0 !== (d3 & a3.expiredLanes) || b2) b2 = Ik(a3, d3);
    else {
      b2 = d3;
      var e2 = K2;
      K2 |= 2;
      var f = Jk();
      if (Q2 !== a3 || Z !== b2) uk = null, Gj = B2() + 500, Kk(a3, b2);
      do
        try {
          Lk();
          break;
        } catch (h3) {
          Mk(a3, h3);
        }
      while (1);
      $g();
      mk.current = f;
      K2 = e2;
      null !== Y2 ? b2 = 0 : (Q2 = null, Z = 0, b2 = T2);
    }
    if (0 !== b2) {
      2 === b2 && (e2 = xc(a3), 0 !== e2 && (d3 = e2, b2 = Nk(a3, e2)));
      if (1 === b2) throw c2 = pk, Kk(a3, 0), Ck(a3, d3), Dk(a3, B2()), c2;
      if (6 === b2) Ck(a3, d3);
      else {
        e2 = a3.current.alternate;
        if (0 === (d3 & 30) && !Ok(e2) && (b2 = Ik(a3, d3), 2 === b2 && (f = xc(a3), 0 !== f && (d3 = f, b2 = Nk(a3, f))), 1 === b2)) throw c2 = pk, Kk(a3, 0), Ck(a3, d3), Dk(a3, B2()), c2;
        a3.finishedWork = e2;
        a3.finishedLanes = d3;
        switch (b2) {
          case 0:
          case 1:
            throw Error(p(345));
          case 2:
            Pk(a3, tk, uk);
            break;
          case 3:
            Ck(a3, d3);
            if ((d3 & 130023424) === d3 && (b2 = fk + 500 - B2(), 10 < b2)) {
              if (0 !== uc(a3, 0)) break;
              e2 = a3.suspendedLanes;
              if ((e2 & d3) !== d3) {
                R2();
                a3.pingedLanes |= a3.suspendedLanes & e2;
                break;
              }
              a3.timeoutHandle = Ff(Pk.bind(null, a3, tk, uk), b2);
              break;
            }
            Pk(a3, tk, uk);
            break;
          case 4:
            Ck(a3, d3);
            if ((d3 & 4194240) === d3) break;
            b2 = a3.eventTimes;
            for (e2 = -1; 0 < d3; ) {
              var g = 31 - oc(d3);
              f = 1 << g;
              g = b2[g];
              g > e2 && (e2 = g);
              d3 &= ~f;
            }
            d3 = e2;
            d3 = B2() - d3;
            d3 = (120 > d3 ? 120 : 480 > d3 ? 480 : 1080 > d3 ? 1080 : 1920 > d3 ? 1920 : 3e3 > d3 ? 3e3 : 4320 > d3 ? 4320 : 1960 * lk(d3 / 1960)) - d3;
            if (10 < d3) {
              a3.timeoutHandle = Ff(Pk.bind(null, a3, tk, uk), d3);
              break;
            }
            Pk(a3, tk, uk);
            break;
          case 5:
            Pk(a3, tk, uk);
            break;
          default:
            throw Error(p(329));
        }
      }
    }
    Dk(a3, B2());
    return a3.callbackNode === c2 ? Gk.bind(null, a3) : null;
  }
  function Nk(a3, b2) {
    var c2 = sk;
    a3.current.memoizedState.isDehydrated && (Kk(a3, b2).flags |= 256);
    a3 = Ik(a3, b2);
    2 !== a3 && (b2 = tk, tk = c2, null !== b2 && Fj(b2));
    return a3;
  }
  function Fj(a3) {
    null === tk ? tk = a3 : tk.push.apply(tk, a3);
  }
  function Ok(a3) {
    for (var b2 = a3; ; ) {
      if (b2.flags & 16384) {
        var c2 = b2.updateQueue;
        if (null !== c2 && (c2 = c2.stores, null !== c2)) for (var d3 = 0; d3 < c2.length; d3++) {
          var e2 = c2[d3], f = e2.getSnapshot;
          e2 = e2.value;
          try {
            if (!He(f(), e2)) return false;
          } catch (g) {
            return false;
          }
        }
      }
      c2 = b2.child;
      if (b2.subtreeFlags & 16384 && null !== c2) c2.return = b2, b2 = c2;
      else {
        if (b2 === a3) break;
        for (; null === b2.sibling; ) {
          if (null === b2.return || b2.return === a3) return true;
          b2 = b2.return;
        }
        b2.sibling.return = b2.return;
        b2 = b2.sibling;
      }
    }
    return true;
  }
  function Ck(a3, b2) {
    b2 &= ~rk;
    b2 &= ~qk;
    a3.suspendedLanes |= b2;
    a3.pingedLanes &= ~b2;
    for (a3 = a3.expirationTimes; 0 < b2; ) {
      var c2 = 31 - oc(b2), d3 = 1 << c2;
      a3[c2] = -1;
      b2 &= ~d3;
    }
  }
  function Ek(a3) {
    if (0 !== (K2 & 6)) throw Error(p(327));
    Hk();
    var b2 = uc(a3, 0);
    if (0 === (b2 & 1)) return Dk(a3, B2()), null;
    var c2 = Ik(a3, b2);
    if (0 !== a3.tag && 2 === c2) {
      var d3 = xc(a3);
      0 !== d3 && (b2 = d3, c2 = Nk(a3, d3));
    }
    if (1 === c2) throw c2 = pk, Kk(a3, 0), Ck(a3, b2), Dk(a3, B2()), c2;
    if (6 === c2) throw Error(p(345));
    a3.finishedWork = a3.current.alternate;
    a3.finishedLanes = b2;
    Pk(a3, tk, uk);
    Dk(a3, B2());
    return null;
  }
  function Qk(a3, b2) {
    var c2 = K2;
    K2 |= 1;
    try {
      return a3(b2);
    } finally {
      K2 = c2, 0 === K2 && (Gj = B2() + 500, fg && jg());
    }
  }
  function Rk(a3) {
    null !== wk && 0 === wk.tag && 0 === (K2 & 6) && Hk();
    var b2 = K2;
    K2 |= 1;
    var c2 = ok.transition, d3 = C;
    try {
      if (ok.transition = null, C = 1, a3) return a3();
    } finally {
      C = d3, ok.transition = c2, K2 = b2, 0 === (K2 & 6) && jg();
    }
  }
  function Hj() {
    fj = ej.current;
    E2(ej);
  }
  function Kk(a3, b2) {
    a3.finishedWork = null;
    a3.finishedLanes = 0;
    var c2 = a3.timeoutHandle;
    -1 !== c2 && (a3.timeoutHandle = -1, Gf(c2));
    if (null !== Y2) for (c2 = Y2.return; null !== c2; ) {
      var d3 = c2;
      wg(d3);
      switch (d3.tag) {
        case 1:
          d3 = d3.type.childContextTypes;
          null !== d3 && void 0 !== d3 && $f();
          break;
        case 3:
          zh();
          E2(Wf);
          E2(H3);
          Eh();
          break;
        case 5:
          Bh(d3);
          break;
        case 4:
          zh();
          break;
        case 13:
          E2(L2);
          break;
        case 19:
          E2(L2);
          break;
        case 10:
          ah(d3.type._context);
          break;
        case 22:
        case 23:
          Hj();
      }
      c2 = c2.return;
    }
    Q2 = a3;
    Y2 = a3 = Pg(a3.current, null);
    Z = fj = b2;
    T2 = 0;
    pk = null;
    rk = qk = rh = 0;
    tk = sk = null;
    if (null !== fh) {
      for (b2 = 0; b2 < fh.length; b2++) if (c2 = fh[b2], d3 = c2.interleaved, null !== d3) {
        c2.interleaved = null;
        var e2 = d3.next, f = c2.pending;
        if (null !== f) {
          var g = f.next;
          f.next = e2;
          d3.next = g;
        }
        c2.pending = d3;
      }
      fh = null;
    }
    return a3;
  }
  function Mk(a3, b2) {
    do {
      var c2 = Y2;
      try {
        $g();
        Fh.current = Rh;
        if (Ih) {
          for (var d3 = M3.memoizedState; null !== d3; ) {
            var e2 = d3.queue;
            null !== e2 && (e2.pending = null);
            d3 = d3.next;
          }
          Ih = false;
        }
        Hh = 0;
        O2 = N = M3 = null;
        Jh = false;
        Kh = 0;
        nk.current = null;
        if (null === c2 || null === c2.return) {
          T2 = 1;
          pk = b2;
          Y2 = null;
          break;
        }
        a: {
          var f = a3, g = c2.return, h3 = c2, k2 = b2;
          b2 = Z;
          h3.flags |= 32768;
          if (null !== k2 && "object" === typeof k2 && "function" === typeof k2.then) {
            var l = k2, m3 = h3, q2 = m3.tag;
            if (0 === (m3.mode & 1) && (0 === q2 || 11 === q2 || 15 === q2)) {
              var r2 = m3.alternate;
              r2 ? (m3.updateQueue = r2.updateQueue, m3.memoizedState = r2.memoizedState, m3.lanes = r2.lanes) : (m3.updateQueue = null, m3.memoizedState = null);
            }
            var y3 = Ui(g);
            if (null !== y3) {
              y3.flags &= -257;
              Vi(y3, g, h3, f, b2);
              y3.mode & 1 && Si(f, l, b2);
              b2 = y3;
              k2 = l;
              var n = b2.updateQueue;
              if (null === n) {
                var t2 = /* @__PURE__ */ new Set();
                t2.add(k2);
                b2.updateQueue = t2;
              } else n.add(k2);
              break a;
            } else {
              if (0 === (b2 & 1)) {
                Si(f, l, b2);
                tj();
                break a;
              }
              k2 = Error(p(426));
            }
          } else if (I2 && h3.mode & 1) {
            var J = Ui(g);
            if (null !== J) {
              0 === (J.flags & 65536) && (J.flags |= 256);
              Vi(J, g, h3, f, b2);
              Jg(Ji(k2, h3));
              break a;
            }
          }
          f = k2 = Ji(k2, h3);
          4 !== T2 && (T2 = 2);
          null === sk ? sk = [f] : sk.push(f);
          f = g;
          do {
            switch (f.tag) {
              case 3:
                f.flags |= 65536;
                b2 &= -b2;
                f.lanes |= b2;
                var x2 = Ni(f, k2, b2);
                ph(f, x2);
                break a;
              case 1:
                h3 = k2;
                var w2 = f.type, u2 = f.stateNode;
                if (0 === (f.flags & 128) && ("function" === typeof w2.getDerivedStateFromError || null !== u2 && "function" === typeof u2.componentDidCatch && (null === Ri || !Ri.has(u2)))) {
                  f.flags |= 65536;
                  b2 &= -b2;
                  f.lanes |= b2;
                  var F = Qi(f, h3, b2);
                  ph(f, F);
                  break a;
                }
            }
            f = f.return;
          } while (null !== f);
        }
        Sk(c2);
      } catch (na) {
        b2 = na;
        Y2 === c2 && null !== c2 && (Y2 = c2 = c2.return);
        continue;
      }
      break;
    } while (1);
  }
  function Jk() {
    var a3 = mk.current;
    mk.current = Rh;
    return null === a3 ? Rh : a3;
  }
  function tj() {
    if (0 === T2 || 3 === T2 || 2 === T2) T2 = 4;
    null === Q2 || 0 === (rh & 268435455) && 0 === (qk & 268435455) || Ck(Q2, Z);
  }
  function Ik(a3, b2) {
    var c2 = K2;
    K2 |= 2;
    var d3 = Jk();
    if (Q2 !== a3 || Z !== b2) uk = null, Kk(a3, b2);
    do
      try {
        Tk();
        break;
      } catch (e2) {
        Mk(a3, e2);
      }
    while (1);
    $g();
    K2 = c2;
    mk.current = d3;
    if (null !== Y2) throw Error(p(261));
    Q2 = null;
    Z = 0;
    return T2;
  }
  function Tk() {
    for (; null !== Y2; ) Uk(Y2);
  }
  function Lk() {
    for (; null !== Y2 && !cc(); ) Uk(Y2);
  }
  function Uk(a3) {
    var b2 = Vk(a3.alternate, a3, fj);
    a3.memoizedProps = a3.pendingProps;
    null === b2 ? Sk(a3) : Y2 = b2;
    nk.current = null;
  }
  function Sk(a3) {
    var b2 = a3;
    do {
      var c2 = b2.alternate;
      a3 = b2.return;
      if (0 === (b2.flags & 32768)) {
        if (c2 = Ej(c2, b2, fj), null !== c2) {
          Y2 = c2;
          return;
        }
      } else {
        c2 = Ij(c2, b2);
        if (null !== c2) {
          c2.flags &= 32767;
          Y2 = c2;
          return;
        }
        if (null !== a3) a3.flags |= 32768, a3.subtreeFlags = 0, a3.deletions = null;
        else {
          T2 = 6;
          Y2 = null;
          return;
        }
      }
      b2 = b2.sibling;
      if (null !== b2) {
        Y2 = b2;
        return;
      }
      Y2 = b2 = a3;
    } while (null !== b2);
    0 === T2 && (T2 = 5);
  }
  function Pk(a3, b2, c2) {
    var d3 = C, e2 = ok.transition;
    try {
      ok.transition = null, C = 1, Wk(a3, b2, c2, d3);
    } finally {
      ok.transition = e2, C = d3;
    }
    return null;
  }
  function Wk(a3, b2, c2, d3) {
    do
      Hk();
    while (null !== wk);
    if (0 !== (K2 & 6)) throw Error(p(327));
    c2 = a3.finishedWork;
    var e2 = a3.finishedLanes;
    if (null === c2) return null;
    a3.finishedWork = null;
    a3.finishedLanes = 0;
    if (c2 === a3.current) throw Error(p(177));
    a3.callbackNode = null;
    a3.callbackPriority = 0;
    var f = c2.lanes | c2.childLanes;
    Bc(a3, f);
    a3 === Q2 && (Y2 = Q2 = null, Z = 0);
    0 === (c2.subtreeFlags & 2064) && 0 === (c2.flags & 2064) || vk || (vk = true, Fk(hc, function() {
      Hk();
      return null;
    }));
    f = 0 !== (c2.flags & 15990);
    if (0 !== (c2.subtreeFlags & 15990) || f) {
      f = ok.transition;
      ok.transition = null;
      var g = C;
      C = 1;
      var h3 = K2;
      K2 |= 4;
      nk.current = null;
      Oj(a3, c2);
      dk(c2, a3);
      Oe(Df);
      dd = !!Cf;
      Df = Cf = null;
      a3.current = c2;
      hk(c2);
      dc();
      K2 = h3;
      C = g;
      ok.transition = f;
    } else a3.current = c2;
    vk && (vk = false, wk = a3, xk = e2);
    f = a3.pendingLanes;
    0 === f && (Ri = null);
    mc(c2.stateNode);
    Dk(a3, B2());
    if (null !== b2) for (d3 = a3.onRecoverableError, c2 = 0; c2 < b2.length; c2++) e2 = b2[c2], d3(e2.value, { componentStack: e2.stack, digest: e2.digest });
    if (Oi) throw Oi = false, a3 = Pi, Pi = null, a3;
    0 !== (xk & 1) && 0 !== a3.tag && Hk();
    f = a3.pendingLanes;
    0 !== (f & 1) ? a3 === zk ? yk++ : (yk = 0, zk = a3) : yk = 0;
    jg();
    return null;
  }
  function Hk() {
    if (null !== wk) {
      var a3 = Dc(xk), b2 = ok.transition, c2 = C;
      try {
        ok.transition = null;
        C = 16 > a3 ? 16 : a3;
        if (null === wk) var d3 = false;
        else {
          a3 = wk;
          wk = null;
          xk = 0;
          if (0 !== (K2 & 6)) throw Error(p(331));
          var e2 = K2;
          K2 |= 4;
          for (V = a3.current; null !== V; ) {
            var f = V, g = f.child;
            if (0 !== (V.flags & 16)) {
              var h3 = f.deletions;
              if (null !== h3) {
                for (var k2 = 0; k2 < h3.length; k2++) {
                  var l = h3[k2];
                  for (V = l; null !== V; ) {
                    var m3 = V;
                    switch (m3.tag) {
                      case 0:
                      case 11:
                      case 15:
                        Pj(8, m3, f);
                    }
                    var q2 = m3.child;
                    if (null !== q2) q2.return = m3, V = q2;
                    else for (; null !== V; ) {
                      m3 = V;
                      var r2 = m3.sibling, y3 = m3.return;
                      Sj(m3);
                      if (m3 === l) {
                        V = null;
                        break;
                      }
                      if (null !== r2) {
                        r2.return = y3;
                        V = r2;
                        break;
                      }
                      V = y3;
                    }
                  }
                }
                var n = f.alternate;
                if (null !== n) {
                  var t2 = n.child;
                  if (null !== t2) {
                    n.child = null;
                    do {
                      var J = t2.sibling;
                      t2.sibling = null;
                      t2 = J;
                    } while (null !== t2);
                  }
                }
                V = f;
              }
            }
            if (0 !== (f.subtreeFlags & 2064) && null !== g) g.return = f, V = g;
            else b: for (; null !== V; ) {
              f = V;
              if (0 !== (f.flags & 2048)) switch (f.tag) {
                case 0:
                case 11:
                case 15:
                  Pj(9, f, f.return);
              }
              var x2 = f.sibling;
              if (null !== x2) {
                x2.return = f.return;
                V = x2;
                break b;
              }
              V = f.return;
            }
          }
          var w2 = a3.current;
          for (V = w2; null !== V; ) {
            g = V;
            var u2 = g.child;
            if (0 !== (g.subtreeFlags & 2064) && null !== u2) u2.return = g, V = u2;
            else b: for (g = w2; null !== V; ) {
              h3 = V;
              if (0 !== (h3.flags & 2048)) try {
                switch (h3.tag) {
                  case 0:
                  case 11:
                  case 15:
                    Qj(9, h3);
                }
              } catch (na) {
                W(h3, h3.return, na);
              }
              if (h3 === g) {
                V = null;
                break b;
              }
              var F = h3.sibling;
              if (null !== F) {
                F.return = h3.return;
                V = F;
                break b;
              }
              V = h3.return;
            }
          }
          K2 = e2;
          jg();
          if (lc && "function" === typeof lc.onPostCommitFiberRoot) try {
            lc.onPostCommitFiberRoot(kc, a3);
          } catch (na) {
          }
          d3 = true;
        }
        return d3;
      } finally {
        C = c2, ok.transition = b2;
      }
    }
    return false;
  }
  function Xk(a3, b2, c2) {
    b2 = Ji(c2, b2);
    b2 = Ni(a3, b2, 1);
    a3 = nh(a3, b2, 1);
    b2 = R2();
    null !== a3 && (Ac(a3, 1, b2), Dk(a3, b2));
  }
  function W(a3, b2, c2) {
    if (3 === a3.tag) Xk(a3, a3, c2);
    else for (; null !== b2; ) {
      if (3 === b2.tag) {
        Xk(b2, a3, c2);
        break;
      } else if (1 === b2.tag) {
        var d3 = b2.stateNode;
        if ("function" === typeof b2.type.getDerivedStateFromError || "function" === typeof d3.componentDidCatch && (null === Ri || !Ri.has(d3))) {
          a3 = Ji(c2, a3);
          a3 = Qi(b2, a3, 1);
          b2 = nh(b2, a3, 1);
          a3 = R2();
          null !== b2 && (Ac(b2, 1, a3), Dk(b2, a3));
          break;
        }
      }
      b2 = b2.return;
    }
  }
  function Ti(a3, b2, c2) {
    var d3 = a3.pingCache;
    null !== d3 && d3.delete(b2);
    b2 = R2();
    a3.pingedLanes |= a3.suspendedLanes & c2;
    Q2 === a3 && (Z & c2) === c2 && (4 === T2 || 3 === T2 && (Z & 130023424) === Z && 500 > B2() - fk ? Kk(a3, 0) : rk |= c2);
    Dk(a3, b2);
  }
  function Yk(a3, b2) {
    0 === b2 && (0 === (a3.mode & 1) ? b2 = 1 : (b2 = sc, sc <<= 1, 0 === (sc & 130023424) && (sc = 4194304)));
    var c2 = R2();
    a3 = ih(a3, b2);
    null !== a3 && (Ac(a3, b2, c2), Dk(a3, c2));
  }
  function uj(a3) {
    var b2 = a3.memoizedState, c2 = 0;
    null !== b2 && (c2 = b2.retryLane);
    Yk(a3, c2);
  }
  function bk(a3, b2) {
    var c2 = 0;
    switch (a3.tag) {
      case 13:
        var d3 = a3.stateNode;
        var e2 = a3.memoizedState;
        null !== e2 && (c2 = e2.retryLane);
        break;
      case 19:
        d3 = a3.stateNode;
        break;
      default:
        throw Error(p(314));
    }
    null !== d3 && d3.delete(b2);
    Yk(a3, c2);
  }
  var Vk;
  Vk = function(a3, b2, c2) {
    if (null !== a3) if (a3.memoizedProps !== b2.pendingProps || Wf.current) dh = true;
    else {
      if (0 === (a3.lanes & c2) && 0 === (b2.flags & 128)) return dh = false, yj(a3, b2, c2);
      dh = 0 !== (a3.flags & 131072) ? true : false;
    }
    else dh = false, I2 && 0 !== (b2.flags & 1048576) && ug(b2, ng, b2.index);
    b2.lanes = 0;
    switch (b2.tag) {
      case 2:
        var d3 = b2.type;
        ij(a3, b2);
        a3 = b2.pendingProps;
        var e2 = Yf(b2, H3.current);
        ch(b2, c2);
        e2 = Nh(null, b2, d3, a3, e2, c2);
        var f = Sh();
        b2.flags |= 1;
        "object" === typeof e2 && null !== e2 && "function" === typeof e2.render && void 0 === e2.$$typeof ? (b2.tag = 1, b2.memoizedState = null, b2.updateQueue = null, Zf(d3) ? (f = true, cg(b2)) : f = false, b2.memoizedState = null !== e2.state && void 0 !== e2.state ? e2.state : null, kh(b2), e2.updater = Ei, b2.stateNode = e2, e2._reactInternals = b2, Ii(b2, d3, a3, c2), b2 = jj(null, b2, d3, true, f, c2)) : (b2.tag = 0, I2 && f && vg(b2), Xi(null, b2, e2, c2), b2 = b2.child);
        return b2;
      case 16:
        d3 = b2.elementType;
        a: {
          ij(a3, b2);
          a3 = b2.pendingProps;
          e2 = d3._init;
          d3 = e2(d3._payload);
          b2.type = d3;
          e2 = b2.tag = Zk(d3);
          a3 = Ci(d3, a3);
          switch (e2) {
            case 0:
              b2 = cj(null, b2, d3, a3, c2);
              break a;
            case 1:
              b2 = hj(null, b2, d3, a3, c2);
              break a;
            case 11:
              b2 = Yi(null, b2, d3, a3, c2);
              break a;
            case 14:
              b2 = $i(null, b2, d3, Ci(d3.type, a3), c2);
              break a;
          }
          throw Error(p(
            306,
            d3,
            ""
          ));
        }
        return b2;
      case 0:
        return d3 = b2.type, e2 = b2.pendingProps, e2 = b2.elementType === d3 ? e2 : Ci(d3, e2), cj(a3, b2, d3, e2, c2);
      case 1:
        return d3 = b2.type, e2 = b2.pendingProps, e2 = b2.elementType === d3 ? e2 : Ci(d3, e2), hj(a3, b2, d3, e2, c2);
      case 3:
        a: {
          kj(b2);
          if (null === a3) throw Error(p(387));
          d3 = b2.pendingProps;
          f = b2.memoizedState;
          e2 = f.element;
          lh(a3, b2);
          qh(b2, d3, null, c2);
          var g = b2.memoizedState;
          d3 = g.element;
          if (f.isDehydrated) if (f = { element: d3, isDehydrated: false, cache: g.cache, pendingSuspenseBoundaries: g.pendingSuspenseBoundaries, transitions: g.transitions }, b2.updateQueue.baseState = f, b2.memoizedState = f, b2.flags & 256) {
            e2 = Ji(Error(p(423)), b2);
            b2 = lj(a3, b2, d3, c2, e2);
            break a;
          } else if (d3 !== e2) {
            e2 = Ji(Error(p(424)), b2);
            b2 = lj(a3, b2, d3, c2, e2);
            break a;
          } else for (yg = Lf(b2.stateNode.containerInfo.firstChild), xg = b2, I2 = true, zg = null, c2 = Vg(b2, null, d3, c2), b2.child = c2; c2; ) c2.flags = c2.flags & -3 | 4096, c2 = c2.sibling;
          else {
            Ig();
            if (d3 === e2) {
              b2 = Zi(a3, b2, c2);
              break a;
            }
            Xi(a3, b2, d3, c2);
          }
          b2 = b2.child;
        }
        return b2;
      case 5:
        return Ah(b2), null === a3 && Eg(b2), d3 = b2.type, e2 = b2.pendingProps, f = null !== a3 ? a3.memoizedProps : null, g = e2.children, Ef(d3, e2) ? g = null : null !== f && Ef(d3, f) && (b2.flags |= 32), gj(a3, b2), Xi(a3, b2, g, c2), b2.child;
      case 6:
        return null === a3 && Eg(b2), null;
      case 13:
        return oj(a3, b2, c2);
      case 4:
        return yh(b2, b2.stateNode.containerInfo), d3 = b2.pendingProps, null === a3 ? b2.child = Ug(b2, null, d3, c2) : Xi(a3, b2, d3, c2), b2.child;
      case 11:
        return d3 = b2.type, e2 = b2.pendingProps, e2 = b2.elementType === d3 ? e2 : Ci(d3, e2), Yi(a3, b2, d3, e2, c2);
      case 7:
        return Xi(a3, b2, b2.pendingProps, c2), b2.child;
      case 8:
        return Xi(a3, b2, b2.pendingProps.children, c2), b2.child;
      case 12:
        return Xi(a3, b2, b2.pendingProps.children, c2), b2.child;
      case 10:
        a: {
          d3 = b2.type._context;
          e2 = b2.pendingProps;
          f = b2.memoizedProps;
          g = e2.value;
          G2(Wg, d3._currentValue);
          d3._currentValue = g;
          if (null !== f) if (He(f.value, g)) {
            if (f.children === e2.children && !Wf.current) {
              b2 = Zi(a3, b2, c2);
              break a;
            }
          } else for (f = b2.child, null !== f && (f.return = b2); null !== f; ) {
            var h3 = f.dependencies;
            if (null !== h3) {
              g = f.child;
              for (var k2 = h3.firstContext; null !== k2; ) {
                if (k2.context === d3) {
                  if (1 === f.tag) {
                    k2 = mh(-1, c2 & -c2);
                    k2.tag = 2;
                    var l = f.updateQueue;
                    if (null !== l) {
                      l = l.shared;
                      var m3 = l.pending;
                      null === m3 ? k2.next = k2 : (k2.next = m3.next, m3.next = k2);
                      l.pending = k2;
                    }
                  }
                  f.lanes |= c2;
                  k2 = f.alternate;
                  null !== k2 && (k2.lanes |= c2);
                  bh(
                    f.return,
                    c2,
                    b2
                  );
                  h3.lanes |= c2;
                  break;
                }
                k2 = k2.next;
              }
            } else if (10 === f.tag) g = f.type === b2.type ? null : f.child;
            else if (18 === f.tag) {
              g = f.return;
              if (null === g) throw Error(p(341));
              g.lanes |= c2;
              h3 = g.alternate;
              null !== h3 && (h3.lanes |= c2);
              bh(g, c2, b2);
              g = f.sibling;
            } else g = f.child;
            if (null !== g) g.return = f;
            else for (g = f; null !== g; ) {
              if (g === b2) {
                g = null;
                break;
              }
              f = g.sibling;
              if (null !== f) {
                f.return = g.return;
                g = f;
                break;
              }
              g = g.return;
            }
            f = g;
          }
          Xi(a3, b2, e2.children, c2);
          b2 = b2.child;
        }
        return b2;
      case 9:
        return e2 = b2.type, d3 = b2.pendingProps.children, ch(b2, c2), e2 = eh(e2), d3 = d3(e2), b2.flags |= 1, Xi(a3, b2, d3, c2), b2.child;
      case 14:
        return d3 = b2.type, e2 = Ci(d3, b2.pendingProps), e2 = Ci(d3.type, e2), $i(a3, b2, d3, e2, c2);
      case 15:
        return bj(a3, b2, b2.type, b2.pendingProps, c2);
      case 17:
        return d3 = b2.type, e2 = b2.pendingProps, e2 = b2.elementType === d3 ? e2 : Ci(d3, e2), ij(a3, b2), b2.tag = 1, Zf(d3) ? (a3 = true, cg(b2)) : a3 = false, ch(b2, c2), Gi(b2, d3, e2), Ii(b2, d3, e2, c2), jj(null, b2, d3, true, a3, c2);
      case 19:
        return xj(a3, b2, c2);
      case 22:
        return dj(a3, b2, c2);
    }
    throw Error(p(156, b2.tag));
  };
  function Fk(a3, b2) {
    return ac(a3, b2);
  }
  function $k(a3, b2, c2, d3) {
    this.tag = a3;
    this.key = c2;
    this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
    this.index = 0;
    this.ref = null;
    this.pendingProps = b2;
    this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
    this.mode = d3;
    this.subtreeFlags = this.flags = 0;
    this.deletions = null;
    this.childLanes = this.lanes = 0;
    this.alternate = null;
  }
  function Bg(a3, b2, c2, d3) {
    return new $k(a3, b2, c2, d3);
  }
  function aj(a3) {
    a3 = a3.prototype;
    return !(!a3 || !a3.isReactComponent);
  }
  function Zk(a3) {
    if ("function" === typeof a3) return aj(a3) ? 1 : 0;
    if (void 0 !== a3 && null !== a3) {
      a3 = a3.$$typeof;
      if (a3 === Da) return 11;
      if (a3 === Ga) return 14;
    }
    return 2;
  }
  function Pg(a3, b2) {
    var c2 = a3.alternate;
    null === c2 ? (c2 = Bg(a3.tag, b2, a3.key, a3.mode), c2.elementType = a3.elementType, c2.type = a3.type, c2.stateNode = a3.stateNode, c2.alternate = a3, a3.alternate = c2) : (c2.pendingProps = b2, c2.type = a3.type, c2.flags = 0, c2.subtreeFlags = 0, c2.deletions = null);
    c2.flags = a3.flags & 14680064;
    c2.childLanes = a3.childLanes;
    c2.lanes = a3.lanes;
    c2.child = a3.child;
    c2.memoizedProps = a3.memoizedProps;
    c2.memoizedState = a3.memoizedState;
    c2.updateQueue = a3.updateQueue;
    b2 = a3.dependencies;
    c2.dependencies = null === b2 ? null : { lanes: b2.lanes, firstContext: b2.firstContext };
    c2.sibling = a3.sibling;
    c2.index = a3.index;
    c2.ref = a3.ref;
    return c2;
  }
  function Rg(a3, b2, c2, d3, e2, f) {
    var g = 2;
    d3 = a3;
    if ("function" === typeof a3) aj(a3) && (g = 1);
    else if ("string" === typeof a3) g = 5;
    else a: switch (a3) {
      case ya:
        return Tg(c2.children, e2, f, b2);
      case za:
        g = 8;
        e2 |= 8;
        break;
      case Aa:
        return a3 = Bg(12, c2, b2, e2 | 2), a3.elementType = Aa, a3.lanes = f, a3;
      case Ea:
        return a3 = Bg(13, c2, b2, e2), a3.elementType = Ea, a3.lanes = f, a3;
      case Fa:
        return a3 = Bg(19, c2, b2, e2), a3.elementType = Fa, a3.lanes = f, a3;
      case Ia:
        return pj(c2, e2, f, b2);
      default:
        if ("object" === typeof a3 && null !== a3) switch (a3.$$typeof) {
          case Ba:
            g = 10;
            break a;
          case Ca:
            g = 9;
            break a;
          case Da:
            g = 11;
            break a;
          case Ga:
            g = 14;
            break a;
          case Ha:
            g = 16;
            d3 = null;
            break a;
        }
        throw Error(p(130, null == a3 ? a3 : typeof a3, ""));
    }
    b2 = Bg(g, c2, b2, e2);
    b2.elementType = a3;
    b2.type = d3;
    b2.lanes = f;
    return b2;
  }
  function Tg(a3, b2, c2, d3) {
    a3 = Bg(7, a3, d3, b2);
    a3.lanes = c2;
    return a3;
  }
  function pj(a3, b2, c2, d3) {
    a3 = Bg(22, a3, d3, b2);
    a3.elementType = Ia;
    a3.lanes = c2;
    a3.stateNode = { isHidden: false };
    return a3;
  }
  function Qg(a3, b2, c2) {
    a3 = Bg(6, a3, null, b2);
    a3.lanes = c2;
    return a3;
  }
  function Sg(a3, b2, c2) {
    b2 = Bg(4, null !== a3.children ? a3.children : [], a3.key, b2);
    b2.lanes = c2;
    b2.stateNode = { containerInfo: a3.containerInfo, pendingChildren: null, implementation: a3.implementation };
    return b2;
  }
  function al(a3, b2, c2, d3, e2) {
    this.tag = b2;
    this.containerInfo = a3;
    this.finishedWork = this.pingCache = this.current = this.pendingChildren = null;
    this.timeoutHandle = -1;
    this.callbackNode = this.pendingContext = this.context = null;
    this.callbackPriority = 0;
    this.eventTimes = zc(0);
    this.expirationTimes = zc(-1);
    this.entangledLanes = this.finishedLanes = this.mutableReadLanes = this.expiredLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
    this.entanglements = zc(0);
    this.identifierPrefix = d3;
    this.onRecoverableError = e2;
    this.mutableSourceEagerHydrationData = null;
  }
  function bl(a3, b2, c2, d3, e2, f, g, h3, k2) {
    a3 = new al(a3, b2, c2, h3, k2);
    1 === b2 ? (b2 = 1, true === f && (b2 |= 8)) : b2 = 0;
    f = Bg(3, null, null, b2);
    a3.current = f;
    f.stateNode = a3;
    f.memoizedState = { element: d3, isDehydrated: c2, cache: null, transitions: null, pendingSuspenseBoundaries: null };
    kh(f);
    return a3;
  }
  function cl(a3, b2, c2) {
    var d3 = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
    return { $$typeof: wa, key: null == d3 ? null : "" + d3, children: a3, containerInfo: b2, implementation: c2 };
  }
  function dl(a3) {
    if (!a3) return Vf;
    a3 = a3._reactInternals;
    a: {
      if (Vb(a3) !== a3 || 1 !== a3.tag) throw Error(p(170));
      var b2 = a3;
      do {
        switch (b2.tag) {
          case 3:
            b2 = b2.stateNode.context;
            break a;
          case 1:
            if (Zf(b2.type)) {
              b2 = b2.stateNode.__reactInternalMemoizedMergedChildContext;
              break a;
            }
        }
        b2 = b2.return;
      } while (null !== b2);
      throw Error(p(171));
    }
    if (1 === a3.tag) {
      var c2 = a3.type;
      if (Zf(c2)) return bg(a3, c2, b2);
    }
    return b2;
  }
  function el(a3, b2, c2, d3, e2, f, g, h3, k2) {
    a3 = bl(c2, d3, true, a3, e2, f, g, h3, k2);
    a3.context = dl(null);
    c2 = a3.current;
    d3 = R2();
    e2 = yi(c2);
    f = mh(d3, e2);
    f.callback = void 0 !== b2 && null !== b2 ? b2 : null;
    nh(c2, f, e2);
    a3.current.lanes = e2;
    Ac(a3, e2, d3);
    Dk(a3, d3);
    return a3;
  }
  function fl(a3, b2, c2, d3) {
    var e2 = b2.current, f = R2(), g = yi(e2);
    c2 = dl(c2);
    null === b2.context ? b2.context = c2 : b2.pendingContext = c2;
    b2 = mh(f, g);
    b2.payload = { element: a3 };
    d3 = void 0 === d3 ? null : d3;
    null !== d3 && (b2.callback = d3);
    a3 = nh(e2, b2, g);
    null !== a3 && (gi(a3, e2, g, f), oh(a3, e2, g));
    return g;
  }
  function gl(a3) {
    a3 = a3.current;
    if (!a3.child) return null;
    switch (a3.child.tag) {
      case 5:
        return a3.child.stateNode;
      default:
        return a3.child.stateNode;
    }
  }
  function hl(a3, b2) {
    a3 = a3.memoizedState;
    if (null !== a3 && null !== a3.dehydrated) {
      var c2 = a3.retryLane;
      a3.retryLane = 0 !== c2 && c2 < b2 ? c2 : b2;
    }
  }
  function il(a3, b2) {
    hl(a3, b2);
    (a3 = a3.alternate) && hl(a3, b2);
  }
  function jl() {
    return null;
  }
  var kl = "function" === typeof reportError ? reportError : function(a3) {
    console.error(a3);
  };
  function ll(a3) {
    this._internalRoot = a3;
  }
  ml.prototype.render = ll.prototype.render = function(a3) {
    var b2 = this._internalRoot;
    if (null === b2) throw Error(p(409));
    fl(a3, b2, null, null);
  };
  ml.prototype.unmount = ll.prototype.unmount = function() {
    var a3 = this._internalRoot;
    if (null !== a3) {
      this._internalRoot = null;
      var b2 = a3.containerInfo;
      Rk(function() {
        fl(null, a3, null, null);
      });
      b2[uf] = null;
    }
  };
  function ml(a3) {
    this._internalRoot = a3;
  }
  ml.prototype.unstable_scheduleHydration = function(a3) {
    if (a3) {
      var b2 = Hc();
      a3 = { blockedOn: null, target: a3, priority: b2 };
      for (var c2 = 0; c2 < Qc.length && 0 !== b2 && b2 < Qc[c2].priority; c2++) ;
      Qc.splice(c2, 0, a3);
      0 === c2 && Vc(a3);
    }
  };
  function nl(a3) {
    return !(!a3 || 1 !== a3.nodeType && 9 !== a3.nodeType && 11 !== a3.nodeType);
  }
  function ol(a3) {
    return !(!a3 || 1 !== a3.nodeType && 9 !== a3.nodeType && 11 !== a3.nodeType && (8 !== a3.nodeType || " react-mount-point-unstable " !== a3.nodeValue));
  }
  function pl() {
  }
  function ql(a3, b2, c2, d3, e2) {
    if (e2) {
      if ("function" === typeof d3) {
        var f = d3;
        d3 = function() {
          var a4 = gl(g);
          f.call(a4);
        };
      }
      var g = el(b2, d3, a3, 0, null, false, false, "", pl);
      a3._reactRootContainer = g;
      a3[uf] = g.current;
      sf(8 === a3.nodeType ? a3.parentNode : a3);
      Rk();
      return g;
    }
    for (; e2 = a3.lastChild; ) a3.removeChild(e2);
    if ("function" === typeof d3) {
      var h3 = d3;
      d3 = function() {
        var a4 = gl(k2);
        h3.call(a4);
      };
    }
    var k2 = bl(a3, 0, false, null, null, false, false, "", pl);
    a3._reactRootContainer = k2;
    a3[uf] = k2.current;
    sf(8 === a3.nodeType ? a3.parentNode : a3);
    Rk(function() {
      fl(b2, k2, c2, d3);
    });
    return k2;
  }
  function rl(a3, b2, c2, d3, e2) {
    var f = c2._reactRootContainer;
    if (f) {
      var g = f;
      if ("function" === typeof e2) {
        var h3 = e2;
        e2 = function() {
          var a4 = gl(g);
          h3.call(a4);
        };
      }
      fl(b2, g, a3, e2);
    } else g = ql(c2, b2, a3, e2, d3);
    return gl(g);
  }
  Ec = function(a3) {
    switch (a3.tag) {
      case 3:
        var b2 = a3.stateNode;
        if (b2.current.memoizedState.isDehydrated) {
          var c2 = tc(b2.pendingLanes);
          0 !== c2 && (Cc(b2, c2 | 1), Dk(b2, B2()), 0 === (K2 & 6) && (Gj = B2() + 500, jg()));
        }
        break;
      case 13:
        Rk(function() {
          var b3 = ih(a3, 1);
          if (null !== b3) {
            var c3 = R2();
            gi(b3, a3, 1, c3);
          }
        }), il(a3, 1);
    }
  };
  Fc = function(a3) {
    if (13 === a3.tag) {
      var b2 = ih(a3, 134217728);
      if (null !== b2) {
        var c2 = R2();
        gi(b2, a3, 134217728, c2);
      }
      il(a3, 134217728);
    }
  };
  Gc = function(a3) {
    if (13 === a3.tag) {
      var b2 = yi(a3), c2 = ih(a3, b2);
      if (null !== c2) {
        var d3 = R2();
        gi(c2, a3, b2, d3);
      }
      il(a3, b2);
    }
  };
  Hc = function() {
    return C;
  };
  Ic = function(a3, b2) {
    var c2 = C;
    try {
      return C = a3, b2();
    } finally {
      C = c2;
    }
  };
  yb = function(a3, b2, c2) {
    switch (b2) {
      case "input":
        bb(a3, c2);
        b2 = c2.name;
        if ("radio" === c2.type && null != b2) {
          for (c2 = a3; c2.parentNode; ) c2 = c2.parentNode;
          c2 = c2.querySelectorAll("input[name=" + JSON.stringify("" + b2) + '][type="radio"]');
          for (b2 = 0; b2 < c2.length; b2++) {
            var d3 = c2[b2];
            if (d3 !== a3 && d3.form === a3.form) {
              var e2 = Db(d3);
              if (!e2) throw Error(p(90));
              Wa(d3);
              bb(d3, e2);
            }
          }
        }
        break;
      case "textarea":
        ib(a3, c2);
        break;
      case "select":
        b2 = c2.value, null != b2 && fb(a3, !!c2.multiple, b2, false);
    }
  };
  Gb = Qk;
  Hb = Rk;
  var sl = { usingClientEntryPoint: false, Events: [Cb, ue, Db, Eb, Fb, Qk] }, tl = { findFiberByHostInstance: Wc, bundleType: 0, version: "18.3.1", rendererPackageName: "react-dom" };
  var ul = { bundleType: tl.bundleType, version: tl.version, rendererPackageName: tl.rendererPackageName, rendererConfig: tl.rendererConfig, overrideHookState: null, overrideHookStateDeletePath: null, overrideHookStateRenamePath: null, overrideProps: null, overridePropsDeletePath: null, overridePropsRenamePath: null, setErrorHandler: null, setSuspenseHandler: null, scheduleUpdate: null, currentDispatcherRef: ua.ReactCurrentDispatcher, findHostInstanceByFiber: function(a3) {
    a3 = Zb(a3);
    return null === a3 ? null : a3.stateNode;
  }, findFiberByHostInstance: tl.findFiberByHostInstance || jl, findHostInstancesForRefresh: null, scheduleRefresh: null, scheduleRoot: null, setRefreshHandler: null, getCurrentFiber: null, reconcilerVersion: "18.3.1-next-f1338f8080-20240426" };
  if ("undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) {
    var vl = __REACT_DEVTOOLS_GLOBAL_HOOK__;
    if (!vl.isDisabled && vl.supportsFiber) try {
      kc = vl.inject(ul), lc = vl;
    } catch (a3) {
    }
  }
  reactDom_production_min.__SECRET_INTERNALS_DO_NOT_USE_OR_YOU_WILL_BE_FIRED = sl;
  reactDom_production_min.createPortal = function(a3, b2) {
    var c2 = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
    if (!nl(b2)) throw Error(p(200));
    return cl(a3, b2, null, c2);
  };
  reactDom_production_min.createRoot = function(a3, b2) {
    if (!nl(a3)) throw Error(p(299));
    var c2 = false, d3 = "", e2 = kl;
    null !== b2 && void 0 !== b2 && (true === b2.unstable_strictMode && (c2 = true), void 0 !== b2.identifierPrefix && (d3 = b2.identifierPrefix), void 0 !== b2.onRecoverableError && (e2 = b2.onRecoverableError));
    b2 = bl(a3, 1, false, null, null, c2, false, d3, e2);
    a3[uf] = b2.current;
    sf(8 === a3.nodeType ? a3.parentNode : a3);
    return new ll(b2);
  };
  reactDom_production_min.findDOMNode = function(a3) {
    if (null == a3) return null;
    if (1 === a3.nodeType) return a3;
    var b2 = a3._reactInternals;
    if (void 0 === b2) {
      if ("function" === typeof a3.render) throw Error(p(188));
      a3 = Object.keys(a3).join(",");
      throw Error(p(268, a3));
    }
    a3 = Zb(b2);
    a3 = null === a3 ? null : a3.stateNode;
    return a3;
  };
  reactDom_production_min.flushSync = function(a3) {
    return Rk(a3);
  };
  reactDom_production_min.hydrate = function(a3, b2, c2) {
    if (!ol(b2)) throw Error(p(200));
    return rl(null, a3, b2, true, c2);
  };
  reactDom_production_min.hydrateRoot = function(a3, b2, c2) {
    if (!nl(a3)) throw Error(p(405));
    var d3 = null != c2 && c2.hydratedSources || null, e2 = false, f = "", g = kl;
    null !== c2 && void 0 !== c2 && (true === c2.unstable_strictMode && (e2 = true), void 0 !== c2.identifierPrefix && (f = c2.identifierPrefix), void 0 !== c2.onRecoverableError && (g = c2.onRecoverableError));
    b2 = el(b2, null, a3, 1, null != c2 ? c2 : null, e2, false, f, g);
    a3[uf] = b2.current;
    sf(a3);
    if (d3) for (a3 = 0; a3 < d3.length; a3++) c2 = d3[a3], e2 = c2._getVersion, e2 = e2(c2._source), null == b2.mutableSourceEagerHydrationData ? b2.mutableSourceEagerHydrationData = [c2, e2] : b2.mutableSourceEagerHydrationData.push(
      c2,
      e2
    );
    return new ml(b2);
  };
  reactDom_production_min.render = function(a3, b2, c2) {
    if (!ol(b2)) throw Error(p(200));
    return rl(null, a3, b2, false, c2);
  };
  reactDom_production_min.unmountComponentAtNode = function(a3) {
    if (!ol(a3)) throw Error(p(40));
    return a3._reactRootContainer ? (Rk(function() {
      rl(null, null, a3, false, function() {
        a3._reactRootContainer = null;
        a3[uf] = null;
      });
    }), true) : false;
  };
  reactDom_production_min.unstable_batchedUpdates = Qk;
  reactDom_production_min.unstable_renderSubtreeIntoContainer = function(a3, b2, c2, d3) {
    if (!ol(c2)) throw Error(p(200));
    if (null == a3 || void 0 === a3._reactInternals) throw Error(p(38));
    return rl(a3, b2, c2, false, d3);
  };
  reactDom_production_min.version = "18.3.1-next-f1338f8080-20240426";
  return reactDom_production_min;
}
var hasRequiredReactDom;
function requireReactDom() {
  if (hasRequiredReactDom) return reactDom.exports;
  hasRequiredReactDom = 1;
  function checkDCE() {
    if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
      return;
    }
    try {
      __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
    } catch (err) {
      console.error(err);
    }
  }
  {
    checkDCE();
    reactDom.exports = requireReactDom_production_min();
  }
  return reactDom.exports;
}
var hasRequiredClient;
function requireClient() {
  if (hasRequiredClient) return client;
  hasRequiredClient = 1;
  var m3 = requireReactDom();
  {
    client.createRoot = m3.createRoot;
    client.hydrateRoot = m3.hydrateRoot;
  }
  return client;
}
var clientExports = requireClient();
const scriptRel = "modulepreload";
const assetsURL = function(dep) {
  return "/" + dep;
};
const seen = {};
const __vitePreload = function preload(baseModule, deps, importerUrl) {
  let promise = Promise.resolve();
  if (deps && deps.length > 0) {
    let allSettled = function(promises$2) {
      return Promise.all(promises$2.map((p) => Promise.resolve(p).then((value$1) => ({
        status: "fulfilled",
        value: value$1
      }), (reason) => ({
        status: "rejected",
        reason
      }))));
    };
    document.getElementsByTagName("link");
    const cspNonceMeta = document.querySelector("meta[property=csp-nonce]");
    const cspNonce = cspNonceMeta?.nonce || cspNonceMeta?.getAttribute("nonce");
    promise = allSettled(deps.map((dep) => {
      dep = assetsURL(dep);
      if (dep in seen) return;
      seen[dep] = true;
      const isCss = dep.endsWith(".css");
      const cssSelector = isCss ? '[rel="stylesheet"]' : "";
      if (document.querySelector(`link[href="${dep}"]${cssSelector}`)) return;
      const link = document.createElement("link");
      link.rel = isCss ? "stylesheet" : scriptRel;
      if (!isCss) link.as = "script";
      link.crossOrigin = "";
      link.href = dep;
      if (cspNonce) link.setAttribute("nonce", cspNonce);
      document.head.appendChild(link);
      if (isCss) return new Promise((res, rej) => {
        link.addEventListener("load", res);
        link.addEventListener("error", () => rej(/* @__PURE__ */ new Error(`Unable to preload CSS for ${dep}`)));
      });
    }));
  }
  function handlePreloadError(err$2) {
    const e$1 = new Event("vite:preloadError", { cancelable: true });
    e$1.payload = err$2;
    window.dispatchEvent(e$1);
    if (!e$1.defaultPrevented) throw err$2;
  }
  return promise.then((res) => {
    for (const item of res || []) {
      if (item.status !== "rejected") continue;
      handlePreloadError(item.reason);
    }
    return baseModule().catch(handlePreloadError);
  });
};
var reactDomExports = requireReactDom();
/**
 * @remix-run/router v1.23.0
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */
function _extends$3() {
  _extends$3 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i2 = 1; i2 < arguments.length; i2++) {
      var source = arguments[i2];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$3.apply(this, arguments);
}
var Action$1;
(function(Action2) {
  Action2["Pop"] = "POP";
  Action2["Push"] = "PUSH";
  Action2["Replace"] = "REPLACE";
})(Action$1 || (Action$1 = {}));
const PopStateEventType = "popstate";
function createBrowserHistory(options) {
  if (options === void 0) {
    options = {};
  }
  function createBrowserLocation(window2, globalHistory) {
    let {
      pathname,
      search,
      hash: hash2
    } = window2.location;
    return createLocation(
      "",
      {
        pathname,
        search,
        hash: hash2
      },
      // state defaults to `null` because `window.history.state` does
      globalHistory.state && globalHistory.state.usr || null,
      globalHistory.state && globalHistory.state.key || "default"
    );
  }
  function createBrowserHref(window2, to) {
    return typeof to === "string" ? to : createPath(to);
  }
  return getUrlBasedHistory(createBrowserLocation, createBrowserHref, null, options);
}
function invariant(value, message) {
  if (value === false || value === null || typeof value === "undefined") {
    throw new Error(message);
  }
}
function warning(cond, message) {
  if (!cond) {
    if (typeof console !== "undefined") console.warn(message);
    try {
      throw new Error(message);
    } catch (e2) {
    }
  }
}
function createKey() {
  return Math.random().toString(36).substr(2, 8);
}
function getHistoryState(location, index2) {
  return {
    usr: location.state,
    key: location.key,
    idx: index2
  };
}
function createLocation(current, to, state, key) {
  if (state === void 0) {
    state = null;
  }
  let location = _extends$3({
    pathname: typeof current === "string" ? current : current.pathname,
    search: "",
    hash: ""
  }, typeof to === "string" ? parsePath(to) : to, {
    state,
    // TODO: This could be cleaned up.  push/replace should probably just take
    // full Locations now and avoid the need to run through this flow at all
    // But that's a pretty big refactor to the current test suite so going to
    // keep as is for the time being and just let any incoming keys take precedence
    key: to && to.key || key || createKey()
  });
  return location;
}
function createPath(_ref) {
  let {
    pathname = "/",
    search = "",
    hash: hash2 = ""
  } = _ref;
  if (search && search !== "?") pathname += search.charAt(0) === "?" ? search : "?" + search;
  if (hash2 && hash2 !== "#") pathname += hash2.charAt(0) === "#" ? hash2 : "#" + hash2;
  return pathname;
}
function parsePath(path) {
  let parsedPath = {};
  if (path) {
    let hashIndex = path.indexOf("#");
    if (hashIndex >= 0) {
      parsedPath.hash = path.substr(hashIndex);
      path = path.substr(0, hashIndex);
    }
    let searchIndex = path.indexOf("?");
    if (searchIndex >= 0) {
      parsedPath.search = path.substr(searchIndex);
      path = path.substr(0, searchIndex);
    }
    if (path) {
      parsedPath.pathname = path;
    }
  }
  return parsedPath;
}
function getUrlBasedHistory(getLocation, createHref, validateLocation, options) {
  if (options === void 0) {
    options = {};
  }
  let {
    window: window2 = document.defaultView,
    v5Compat = false
  } = options;
  let globalHistory = window2.history;
  let action = Action$1.Pop;
  let listener = null;
  let index2 = getIndex();
  if (index2 == null) {
    index2 = 0;
    globalHistory.replaceState(_extends$3({}, globalHistory.state, {
      idx: index2
    }), "");
  }
  function getIndex() {
    let state = globalHistory.state || {
      idx: null
    };
    return state.idx;
  }
  function handlePop() {
    action = Action$1.Pop;
    let nextIndex = getIndex();
    let delta = nextIndex == null ? null : nextIndex - index2;
    index2 = nextIndex;
    if (listener) {
      listener({
        action,
        location: history.location,
        delta
      });
    }
  }
  function push(to, state) {
    action = Action$1.Push;
    let location = createLocation(history.location, to, state);
    index2 = getIndex() + 1;
    let historyState = getHistoryState(location, index2);
    let url = history.createHref(location);
    try {
      globalHistory.pushState(historyState, "", url);
    } catch (error) {
      if (error instanceof DOMException && error.name === "DataCloneError") {
        throw error;
      }
      window2.location.assign(url);
    }
    if (v5Compat && listener) {
      listener({
        action,
        location: history.location,
        delta: 1
      });
    }
  }
  function replace(to, state) {
    action = Action$1.Replace;
    let location = createLocation(history.location, to, state);
    index2 = getIndex();
    let historyState = getHistoryState(location, index2);
    let url = history.createHref(location);
    globalHistory.replaceState(historyState, "", url);
    if (v5Compat && listener) {
      listener({
        action,
        location: history.location,
        delta: 0
      });
    }
  }
  function createURL(to) {
    let base = window2.location.origin !== "null" ? window2.location.origin : window2.location.href;
    let href = typeof to === "string" ? to : createPath(to);
    href = href.replace(/ $/, "%20");
    invariant(base, "No window.location.(origin|href) available to create URL for href: " + href);
    return new URL(href, base);
  }
  let history = {
    get action() {
      return action;
    },
    get location() {
      return getLocation(window2, globalHistory);
    },
    listen(fn3) {
      if (listener) {
        throw new Error("A history only accepts one active listener");
      }
      window2.addEventListener(PopStateEventType, handlePop);
      listener = fn3;
      return () => {
        window2.removeEventListener(PopStateEventType, handlePop);
        listener = null;
      };
    },
    createHref(to) {
      return createHref(window2, to);
    },
    createURL,
    encodeLocation(to) {
      let url = createURL(to);
      return {
        pathname: url.pathname,
        search: url.search,
        hash: url.hash
      };
    },
    push,
    replace,
    go(n) {
      return globalHistory.go(n);
    }
  };
  return history;
}
var ResultType;
(function(ResultType2) {
  ResultType2["data"] = "data";
  ResultType2["deferred"] = "deferred";
  ResultType2["redirect"] = "redirect";
  ResultType2["error"] = "error";
})(ResultType || (ResultType = {}));
function matchRoutes(routes, locationArg, basename) {
  if (basename === void 0) {
    basename = "/";
  }
  return matchRoutesImpl(routes, locationArg, basename);
}
function matchRoutesImpl(routes, locationArg, basename, allowPartial) {
  let location = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
  let pathname = stripBasename(location.pathname || "/", basename);
  if (pathname == null) {
    return null;
  }
  let branches = flattenRoutes(routes);
  rankRouteBranches(branches);
  let matches2 = null;
  for (let i2 = 0; matches2 == null && i2 < branches.length; ++i2) {
    let decoded = decodePath(pathname);
    matches2 = matchRouteBranch(branches[i2], decoded);
  }
  return matches2;
}
function flattenRoutes(routes, branches, parentsMeta, parentPath) {
  if (branches === void 0) {
    branches = [];
  }
  if (parentsMeta === void 0) {
    parentsMeta = [];
  }
  if (parentPath === void 0) {
    parentPath = "";
  }
  let flattenRoute = (route2, index2, relativePath) => {
    let meta = {
      relativePath: relativePath === void 0 ? route2.path || "" : relativePath,
      caseSensitive: route2.caseSensitive === true,
      childrenIndex: index2,
      route: route2
    };
    if (meta.relativePath.startsWith("/")) {
      invariant(meta.relativePath.startsWith(parentPath), 'Absolute route path "' + meta.relativePath + '" nested under path ' + ('"' + parentPath + '" is not valid. An absolute child route path ') + "must start with the combined path of all its parent routes.");
      meta.relativePath = meta.relativePath.slice(parentPath.length);
    }
    let path = joinPaths([parentPath, meta.relativePath]);
    let routesMeta = parentsMeta.concat(meta);
    if (route2.children && route2.children.length > 0) {
      invariant(
        // Our types know better, but runtime JS may not!
        // @ts-expect-error
        route2.index !== true,
        "Index routes must not have child routes. Please remove " + ('all child routes from route path "' + path + '".')
      );
      flattenRoutes(route2.children, branches, routesMeta, path);
    }
    if (route2.path == null && !route2.index) {
      return;
    }
    branches.push({
      path,
      score: computeScore(path, route2.index),
      routesMeta
    });
  };
  routes.forEach((route2, index2) => {
    var _route$path;
    if (route2.path === "" || !((_route$path = route2.path) != null && _route$path.includes("?"))) {
      flattenRoute(route2, index2);
    } else {
      for (let exploded of explodeOptionalSegments(route2.path)) {
        flattenRoute(route2, index2, exploded);
      }
    }
  });
  return branches;
}
function explodeOptionalSegments(path) {
  let segments = path.split("/");
  if (segments.length === 0) return [];
  let [first, ...rest] = segments;
  let isOptional = first.endsWith("?");
  let required = first.replace(/\?$/, "");
  if (rest.length === 0) {
    return isOptional ? [required, ""] : [required];
  }
  let restExploded = explodeOptionalSegments(rest.join("/"));
  let result = [];
  result.push(...restExploded.map((subpath) => subpath === "" ? required : [required, subpath].join("/")));
  if (isOptional) {
    result.push(...restExploded);
  }
  return result.map((exploded) => path.startsWith("/") && exploded === "" ? "/" : exploded);
}
function rankRouteBranches(branches) {
  branches.sort((a3, b2) => a3.score !== b2.score ? b2.score - a3.score : compareIndexes(a3.routesMeta.map((meta) => meta.childrenIndex), b2.routesMeta.map((meta) => meta.childrenIndex)));
}
const paramRe = /^:[\w-]+$/;
const dynamicSegmentValue = 3;
const indexRouteValue = 2;
const emptySegmentValue = 1;
const staticSegmentValue = 10;
const splatPenalty = -2;
const isSplat = (s3) => s3 === "*";
function computeScore(path, index2) {
  let segments = path.split("/");
  let initialScore = segments.length;
  if (segments.some(isSplat)) {
    initialScore += splatPenalty;
  }
  if (index2) {
    initialScore += indexRouteValue;
  }
  return segments.filter((s3) => !isSplat(s3)).reduce((score, segment) => score + (paramRe.test(segment) ? dynamicSegmentValue : segment === "" ? emptySegmentValue : staticSegmentValue), initialScore);
}
function compareIndexes(a3, b2) {
  let siblings2 = a3.length === b2.length && a3.slice(0, -1).every((n, i2) => n === b2[i2]);
  return siblings2 ? (
    // If two routes are siblings, we should try to match the earlier sibling
    // first. This allows people to have fine-grained control over the matching
    // behavior by simply putting routes with identical paths in the order they
    // want them tried.
    a3[a3.length - 1] - b2[b2.length - 1]
  ) : (
    // Otherwise, it doesn't really make sense to rank non-siblings by index,
    // so they sort equally.
    0
  );
}
function matchRouteBranch(branch, pathname, allowPartial) {
  let {
    routesMeta
  } = branch;
  let matchedParams = {};
  let matchedPathname = "/";
  let matches2 = [];
  for (let i2 = 0; i2 < routesMeta.length; ++i2) {
    let meta = routesMeta[i2];
    let end2 = i2 === routesMeta.length - 1;
    let remainingPathname = matchedPathname === "/" ? pathname : pathname.slice(matchedPathname.length) || "/";
    let match2 = matchPath({
      path: meta.relativePath,
      caseSensitive: meta.caseSensitive,
      end: end2
    }, remainingPathname);
    let route2 = meta.route;
    if (!match2) {
      return null;
    }
    Object.assign(matchedParams, match2.params);
    matches2.push({
      // TODO: Can this as be avoided?
      params: matchedParams,
      pathname: joinPaths([matchedPathname, match2.pathname]),
      pathnameBase: normalizePathname(joinPaths([matchedPathname, match2.pathnameBase])),
      route: route2
    });
    if (match2.pathnameBase !== "/") {
      matchedPathname = joinPaths([matchedPathname, match2.pathnameBase]);
    }
  }
  return matches2;
}
function matchPath(pattern, pathname) {
  if (typeof pattern === "string") {
    pattern = {
      path: pattern,
      caseSensitive: false,
      end: true
    };
  }
  let [matcher, compiledParams] = compilePath(pattern.path, pattern.caseSensitive, pattern.end);
  let match2 = pathname.match(matcher);
  if (!match2) return null;
  let matchedPathname = match2[0];
  let pathnameBase = matchedPathname.replace(/(.)\/+$/, "$1");
  let captureGroups = match2.slice(1);
  let params = compiledParams.reduce((memo, _ref, index2) => {
    let {
      paramName,
      isOptional
    } = _ref;
    if (paramName === "*") {
      let splatValue = captureGroups[index2] || "";
      pathnameBase = matchedPathname.slice(0, matchedPathname.length - splatValue.length).replace(/(.)\/+$/, "$1");
    }
    const value = captureGroups[index2];
    if (isOptional && !value) {
      memo[paramName] = void 0;
    } else {
      memo[paramName] = (value || "").replace(/%2F/g, "/");
    }
    return memo;
  }, {});
  return {
    params,
    pathname: matchedPathname,
    pathnameBase,
    pattern
  };
}
function compilePath(path, caseSensitive, end2) {
  if (caseSensitive === void 0) {
    caseSensitive = false;
  }
  if (end2 === void 0) {
    end2 = true;
  }
  warning(path === "*" || !path.endsWith("*") || path.endsWith("/*"), 'Route path "' + path + '" will be treated as if it were ' + ('"' + path.replace(/\*$/, "/*") + '" because the `*` character must ') + "always follow a `/` in the pattern. To get rid of this warning, " + ('please change the route path to "' + path.replace(/\*$/, "/*") + '".'));
  let params = [];
  let regexpSource = "^" + path.replace(/\/*\*?$/, "").replace(/^\/*/, "/").replace(/[\\.*+^${}|()[\]]/g, "\\$&").replace(/\/:([\w-]+)(\?)?/g, (_, paramName, isOptional) => {
    params.push({
      paramName,
      isOptional: isOptional != null
    });
    return isOptional ? "/?([^\\/]+)?" : "/([^\\/]+)";
  });
  if (path.endsWith("*")) {
    params.push({
      paramName: "*"
    });
    regexpSource += path === "*" || path === "/*" ? "(.*)$" : "(?:\\/(.+)|\\/*)$";
  } else if (end2) {
    regexpSource += "\\/*$";
  } else if (path !== "" && path !== "/") {
    regexpSource += "(?:(?=\\/|$))";
  } else ;
  let matcher = new RegExp(regexpSource, caseSensitive ? void 0 : "i");
  return [matcher, params];
}
function decodePath(value) {
  try {
    return value.split("/").map((v) => decodeURIComponent(v).replace(/\//g, "%2F")).join("/");
  } catch (error) {
    warning(false, 'The URL path "' + value + '" could not be decoded because it is is a malformed URL segment. This is probably due to a bad percent ' + ("encoding (" + error + ")."));
    return value;
  }
}
function stripBasename(pathname, basename) {
  if (basename === "/") return pathname;
  if (!pathname.toLowerCase().startsWith(basename.toLowerCase())) {
    return null;
  }
  let startIndex = basename.endsWith("/") ? basename.length - 1 : basename.length;
  let nextChar = pathname.charAt(startIndex);
  if (nextChar && nextChar !== "/") {
    return null;
  }
  return pathname.slice(startIndex) || "/";
}
function resolvePath(to, fromPathname) {
  if (fromPathname === void 0) {
    fromPathname = "/";
  }
  let {
    pathname: toPathname,
    search = "",
    hash: hash2 = ""
  } = typeof to === "string" ? parsePath(to) : to;
  let pathname = toPathname ? toPathname.startsWith("/") ? toPathname : resolvePathname(toPathname, fromPathname) : fromPathname;
  return {
    pathname,
    search: normalizeSearch(search),
    hash: normalizeHash(hash2)
  };
}
function resolvePathname(relativePath, fromPathname) {
  let segments = fromPathname.replace(/\/+$/, "").split("/");
  let relativeSegments = relativePath.split("/");
  relativeSegments.forEach((segment) => {
    if (segment === "..") {
      if (segments.length > 1) segments.pop();
    } else if (segment !== ".") {
      segments.push(segment);
    }
  });
  return segments.length > 1 ? segments.join("/") : "/";
}
function getInvalidPathError(char, field, dest, path) {
  return "Cannot include a '" + char + "' character in a manually specified " + ("`to." + field + "` field [" + JSON.stringify(path) + "].  Please separate it out to the ") + ("`to." + dest + "` field. Alternatively you may provide the full path as ") + 'a string in <Link to="..."> and the router will parse it for you.';
}
function getPathContributingMatches(matches2) {
  return matches2.filter((match2, index2) => index2 === 0 || match2.route.path && match2.route.path.length > 0);
}
function getResolveToMatches(matches2, v7_relativeSplatPath) {
  let pathMatches = getPathContributingMatches(matches2);
  if (v7_relativeSplatPath) {
    return pathMatches.map((match2, idx) => idx === pathMatches.length - 1 ? match2.pathname : match2.pathnameBase);
  }
  return pathMatches.map((match2) => match2.pathnameBase);
}
function resolveTo(toArg, routePathnames, locationPathname, isPathRelative) {
  if (isPathRelative === void 0) {
    isPathRelative = false;
  }
  let to;
  if (typeof toArg === "string") {
    to = parsePath(toArg);
  } else {
    to = _extends$3({}, toArg);
    invariant(!to.pathname || !to.pathname.includes("?"), getInvalidPathError("?", "pathname", "search", to));
    invariant(!to.pathname || !to.pathname.includes("#"), getInvalidPathError("#", "pathname", "hash", to));
    invariant(!to.search || !to.search.includes("#"), getInvalidPathError("#", "search", "hash", to));
  }
  let isEmptyPath = toArg === "" || to.pathname === "";
  let toPathname = isEmptyPath ? "/" : to.pathname;
  let from;
  if (toPathname == null) {
    from = locationPathname;
  } else {
    let routePathnameIndex = routePathnames.length - 1;
    if (!isPathRelative && toPathname.startsWith("..")) {
      let toSegments = toPathname.split("/");
      while (toSegments[0] === "..") {
        toSegments.shift();
        routePathnameIndex -= 1;
      }
      to.pathname = toSegments.join("/");
    }
    from = routePathnameIndex >= 0 ? routePathnames[routePathnameIndex] : "/";
  }
  let path = resolvePath(to, from);
  let hasExplicitTrailingSlash = toPathname && toPathname !== "/" && toPathname.endsWith("/");
  let hasCurrentTrailingSlash = (isEmptyPath || toPathname === ".") && locationPathname.endsWith("/");
  if (!path.pathname.endsWith("/") && (hasExplicitTrailingSlash || hasCurrentTrailingSlash)) {
    path.pathname += "/";
  }
  return path;
}
const joinPaths = (paths) => paths.join("/").replace(/\/\/+/g, "/");
const normalizePathname = (pathname) => pathname.replace(/\/+$/, "").replace(/^\/*/, "/");
const normalizeSearch = (search) => !search || search === "?" ? "" : search.startsWith("?") ? search : "?" + search;
const normalizeHash = (hash2) => !hash2 || hash2 === "#" ? "" : hash2.startsWith("#") ? hash2 : "#" + hash2;
function isRouteErrorResponse(error) {
  return error != null && typeof error.status === "number" && typeof error.statusText === "string" && typeof error.internal === "boolean" && "data" in error;
}
const validMutationMethodsArr = ["post", "put", "patch", "delete"];
new Set(validMutationMethodsArr);
const validRequestMethodsArr = ["get", ...validMutationMethodsArr];
new Set(validRequestMethodsArr);
/**
 * React Router v6.30.1
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */
function _extends$2() {
  _extends$2 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i2 = 1; i2 < arguments.length; i2++) {
      var source = arguments[i2];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$2.apply(this, arguments);
}
const DataRouterContext = /* @__PURE__ */ reactExports.createContext(null);
const DataRouterStateContext = /* @__PURE__ */ reactExports.createContext(null);
const NavigationContext = /* @__PURE__ */ reactExports.createContext(null);
const LocationContext = /* @__PURE__ */ reactExports.createContext(null);
const RouteContext = /* @__PURE__ */ reactExports.createContext({
  outlet: null,
  matches: [],
  isDataRoute: false
});
const RouteErrorContext = /* @__PURE__ */ reactExports.createContext(null);
function useHref(to, _temp) {
  let {
    relative
  } = _temp === void 0 ? {} : _temp;
  !useInRouterContext() ? invariant(false) : void 0;
  let {
    basename,
    navigator: navigator2
  } = reactExports.useContext(NavigationContext);
  let {
    hash: hash2,
    pathname,
    search
  } = useResolvedPath(to, {
    relative
  });
  let joinedPathname = pathname;
  if (basename !== "/") {
    joinedPathname = pathname === "/" ? basename : joinPaths([basename, pathname]);
  }
  return navigator2.createHref({
    pathname: joinedPathname,
    search,
    hash: hash2
  });
}
function useInRouterContext() {
  return reactExports.useContext(LocationContext) != null;
}
function useLocation() {
  !useInRouterContext() ? invariant(false) : void 0;
  return reactExports.useContext(LocationContext).location;
}
function useIsomorphicLayoutEffect$1(cb) {
  let isStatic = reactExports.useContext(NavigationContext).static;
  if (!isStatic) {
    reactExports.useLayoutEffect(cb);
  }
}
function useNavigate() {
  let {
    isDataRoute
  } = reactExports.useContext(RouteContext);
  return isDataRoute ? useNavigateStable() : useNavigateUnstable();
}
function useNavigateUnstable() {
  !useInRouterContext() ? invariant(false) : void 0;
  let dataRouterContext = reactExports.useContext(DataRouterContext);
  let {
    basename,
    future,
    navigator: navigator2
  } = reactExports.useContext(NavigationContext);
  let {
    matches: matches2
  } = reactExports.useContext(RouteContext);
  let {
    pathname: locationPathname
  } = useLocation();
  let routePathnamesJson = JSON.stringify(getResolveToMatches(matches2, future.v7_relativeSplatPath));
  let activeRef = reactExports.useRef(false);
  useIsomorphicLayoutEffect$1(() => {
    activeRef.current = true;
  });
  let navigate2 = reactExports.useCallback(function(to, options) {
    if (options === void 0) {
      options = {};
    }
    if (!activeRef.current) return;
    if (typeof to === "number") {
      navigator2.go(to);
      return;
    }
    let path = resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, options.relative === "path");
    if (dataRouterContext == null && basename !== "/") {
      path.pathname = path.pathname === "/" ? basename : joinPaths([basename, path.pathname]);
    }
    (!!options.replace ? navigator2.replace : navigator2.push)(path, options.state, options);
  }, [basename, navigator2, routePathnamesJson, locationPathname, dataRouterContext]);
  return navigate2;
}
const OutletContext = /* @__PURE__ */ reactExports.createContext(null);
function useOutlet(context2) {
  let outlet = reactExports.useContext(RouteContext).outlet;
  if (outlet) {
    return /* @__PURE__ */ reactExports.createElement(OutletContext.Provider, {
      value: context2
    }, outlet);
  }
  return outlet;
}
function useParams() {
  let {
    matches: matches2
  } = reactExports.useContext(RouteContext);
  let routeMatch = matches2[matches2.length - 1];
  return routeMatch ? routeMatch.params : {};
}
function useResolvedPath(to, _temp2) {
  let {
    relative
  } = _temp2 === void 0 ? {} : _temp2;
  let {
    future
  } = reactExports.useContext(NavigationContext);
  let {
    matches: matches2
  } = reactExports.useContext(RouteContext);
  let {
    pathname: locationPathname
  } = useLocation();
  let routePathnamesJson = JSON.stringify(getResolveToMatches(matches2, future.v7_relativeSplatPath));
  return reactExports.useMemo(() => resolveTo(to, JSON.parse(routePathnamesJson), locationPathname, relative === "path"), [to, routePathnamesJson, locationPathname, relative]);
}
function useRoutes(routes, locationArg) {
  return useRoutesImpl(routes, locationArg);
}
function useRoutesImpl(routes, locationArg, dataRouterState, future) {
  !useInRouterContext() ? invariant(false) : void 0;
  let {
    navigator: navigator2
  } = reactExports.useContext(NavigationContext);
  let {
    matches: parentMatches
  } = reactExports.useContext(RouteContext);
  let routeMatch = parentMatches[parentMatches.length - 1];
  let parentParams = routeMatch ? routeMatch.params : {};
  routeMatch ? routeMatch.pathname : "/";
  let parentPathnameBase = routeMatch ? routeMatch.pathnameBase : "/";
  routeMatch && routeMatch.route;
  let locationFromContext = useLocation();
  let location;
  if (locationArg) {
    var _parsedLocationArg$pa;
    let parsedLocationArg = typeof locationArg === "string" ? parsePath(locationArg) : locationArg;
    !(parentPathnameBase === "/" || ((_parsedLocationArg$pa = parsedLocationArg.pathname) == null ? void 0 : _parsedLocationArg$pa.startsWith(parentPathnameBase))) ? invariant(false) : void 0;
    location = parsedLocationArg;
  } else {
    location = locationFromContext;
  }
  let pathname = location.pathname || "/";
  let remainingPathname = pathname;
  if (parentPathnameBase !== "/") {
    let parentSegments = parentPathnameBase.replace(/^\//, "").split("/");
    let segments = pathname.replace(/^\//, "").split("/");
    remainingPathname = "/" + segments.slice(parentSegments.length).join("/");
  }
  let matches2 = matchRoutes(routes, {
    pathname: remainingPathname
  });
  let renderedMatches = _renderMatches(matches2 && matches2.map((match2) => Object.assign({}, match2, {
    params: Object.assign({}, parentParams, match2.params),
    pathname: joinPaths([
      parentPathnameBase,
      // Re-encode pathnames that were decoded inside matchRoutes
      navigator2.encodeLocation ? navigator2.encodeLocation(match2.pathname).pathname : match2.pathname
    ]),
    pathnameBase: match2.pathnameBase === "/" ? parentPathnameBase : joinPaths([
      parentPathnameBase,
      // Re-encode pathnames that were decoded inside matchRoutes
      navigator2.encodeLocation ? navigator2.encodeLocation(match2.pathnameBase).pathname : match2.pathnameBase
    ])
  })), parentMatches, dataRouterState, future);
  if (locationArg && renderedMatches) {
    return /* @__PURE__ */ reactExports.createElement(LocationContext.Provider, {
      value: {
        location: _extends$2({
          pathname: "/",
          search: "",
          hash: "",
          state: null,
          key: "default"
        }, location),
        navigationType: Action$1.Pop
      }
    }, renderedMatches);
  }
  return renderedMatches;
}
function DefaultErrorComponent() {
  let error = useRouteError();
  let message = isRouteErrorResponse(error) ? error.status + " " + error.statusText : error instanceof Error ? error.message : JSON.stringify(error);
  let stack = error instanceof Error ? error.stack : null;
  let lightgrey = "rgba(200,200,200, 0.5)";
  let preStyles = {
    padding: "0.5rem",
    backgroundColor: lightgrey
  };
  let devInfo = null;
  return /* @__PURE__ */ reactExports.createElement(reactExports.Fragment, null, /* @__PURE__ */ reactExports.createElement("h2", null, "Unexpected Application Error!"), /* @__PURE__ */ reactExports.createElement("h3", {
    style: {
      fontStyle: "italic"
    }
  }, message), stack ? /* @__PURE__ */ reactExports.createElement("pre", {
    style: preStyles
  }, stack) : null, devInfo);
}
const defaultErrorElement = /* @__PURE__ */ reactExports.createElement(DefaultErrorComponent, null);
class RenderErrorBoundary extends reactExports.Component {
  constructor(props) {
    super(props);
    this.state = {
      location: props.location,
      revalidation: props.revalidation,
      error: props.error
    };
  }
  static getDerivedStateFromError(error) {
    return {
      error
    };
  }
  static getDerivedStateFromProps(props, state) {
    if (state.location !== props.location || state.revalidation !== "idle" && props.revalidation === "idle") {
      return {
        error: props.error,
        location: props.location,
        revalidation: props.revalidation
      };
    }
    return {
      error: props.error !== void 0 ? props.error : state.error,
      location: state.location,
      revalidation: props.revalidation || state.revalidation
    };
  }
  componentDidCatch(error, errorInfo) {
    console.error("React Router caught the following error during render", error, errorInfo);
  }
  render() {
    return this.state.error !== void 0 ? /* @__PURE__ */ reactExports.createElement(RouteContext.Provider, {
      value: this.props.routeContext
    }, /* @__PURE__ */ reactExports.createElement(RouteErrorContext.Provider, {
      value: this.state.error,
      children: this.props.component
    })) : this.props.children;
  }
}
function RenderedRoute(_ref) {
  let {
    routeContext,
    match: match2,
    children
  } = _ref;
  let dataRouterContext = reactExports.useContext(DataRouterContext);
  if (dataRouterContext && dataRouterContext.static && dataRouterContext.staticContext && (match2.route.errorElement || match2.route.ErrorBoundary)) {
    dataRouterContext.staticContext._deepestRenderedBoundaryId = match2.route.id;
  }
  return /* @__PURE__ */ reactExports.createElement(RouteContext.Provider, {
    value: routeContext
  }, children);
}
function _renderMatches(matches2, parentMatches, dataRouterState, future) {
  var _dataRouterState;
  if (parentMatches === void 0) {
    parentMatches = [];
  }
  if (dataRouterState === void 0) {
    dataRouterState = null;
  }
  if (future === void 0) {
    future = null;
  }
  if (matches2 == null) {
    var _future;
    if (!dataRouterState) {
      return null;
    }
    if (dataRouterState.errors) {
      matches2 = dataRouterState.matches;
    } else if ((_future = future) != null && _future.v7_partialHydration && parentMatches.length === 0 && !dataRouterState.initialized && dataRouterState.matches.length > 0) {
      matches2 = dataRouterState.matches;
    } else {
      return null;
    }
  }
  let renderedMatches = matches2;
  let errors = (_dataRouterState = dataRouterState) == null ? void 0 : _dataRouterState.errors;
  if (errors != null) {
    let errorIndex = renderedMatches.findIndex((m3) => m3.route.id && (errors == null ? void 0 : errors[m3.route.id]) !== void 0);
    !(errorIndex >= 0) ? invariant(false) : void 0;
    renderedMatches = renderedMatches.slice(0, Math.min(renderedMatches.length, errorIndex + 1));
  }
  let renderFallback = false;
  let fallbackIndex = -1;
  if (dataRouterState && future && future.v7_partialHydration) {
    for (let i2 = 0; i2 < renderedMatches.length; i2++) {
      let match2 = renderedMatches[i2];
      if (match2.route.HydrateFallback || match2.route.hydrateFallbackElement) {
        fallbackIndex = i2;
      }
      if (match2.route.id) {
        let {
          loaderData,
          errors: errors2
        } = dataRouterState;
        let needsToRunLoader = match2.route.loader && loaderData[match2.route.id] === void 0 && (!errors2 || errors2[match2.route.id] === void 0);
        if (match2.route.lazy || needsToRunLoader) {
          renderFallback = true;
          if (fallbackIndex >= 0) {
            renderedMatches = renderedMatches.slice(0, fallbackIndex + 1);
          } else {
            renderedMatches = [renderedMatches[0]];
          }
          break;
        }
      }
    }
  }
  return renderedMatches.reduceRight((outlet, match2, index2) => {
    let error;
    let shouldRenderHydrateFallback = false;
    let errorElement = null;
    let hydrateFallbackElement = null;
    if (dataRouterState) {
      error = errors && match2.route.id ? errors[match2.route.id] : void 0;
      errorElement = match2.route.errorElement || defaultErrorElement;
      if (renderFallback) {
        if (fallbackIndex < 0 && index2 === 0) {
          warningOnce("route-fallback");
          shouldRenderHydrateFallback = true;
          hydrateFallbackElement = null;
        } else if (fallbackIndex === index2) {
          shouldRenderHydrateFallback = true;
          hydrateFallbackElement = match2.route.hydrateFallbackElement || null;
        }
      }
    }
    let matches22 = parentMatches.concat(renderedMatches.slice(0, index2 + 1));
    let getChildren = () => {
      let children;
      if (error) {
        children = errorElement;
      } else if (shouldRenderHydrateFallback) {
        children = hydrateFallbackElement;
      } else if (match2.route.Component) {
        children = /* @__PURE__ */ reactExports.createElement(match2.route.Component, null);
      } else if (match2.route.element) {
        children = match2.route.element;
      } else {
        children = outlet;
      }
      return /* @__PURE__ */ reactExports.createElement(RenderedRoute, {
        match: match2,
        routeContext: {
          outlet,
          matches: matches22,
          isDataRoute: dataRouterState != null
        },
        children
      });
    };
    return dataRouterState && (match2.route.ErrorBoundary || match2.route.errorElement || index2 === 0) ? /* @__PURE__ */ reactExports.createElement(RenderErrorBoundary, {
      location: dataRouterState.location,
      revalidation: dataRouterState.revalidation,
      component: errorElement,
      error,
      children: getChildren(),
      routeContext: {
        outlet: null,
        matches: matches22,
        isDataRoute: true
      }
    }) : getChildren();
  }, null);
}
var DataRouterHook$1 = /* @__PURE__ */ (function(DataRouterHook2) {
  DataRouterHook2["UseBlocker"] = "useBlocker";
  DataRouterHook2["UseRevalidator"] = "useRevalidator";
  DataRouterHook2["UseNavigateStable"] = "useNavigate";
  return DataRouterHook2;
})(DataRouterHook$1 || {});
var DataRouterStateHook$1 = /* @__PURE__ */ (function(DataRouterStateHook2) {
  DataRouterStateHook2["UseBlocker"] = "useBlocker";
  DataRouterStateHook2["UseLoaderData"] = "useLoaderData";
  DataRouterStateHook2["UseActionData"] = "useActionData";
  DataRouterStateHook2["UseRouteError"] = "useRouteError";
  DataRouterStateHook2["UseNavigation"] = "useNavigation";
  DataRouterStateHook2["UseRouteLoaderData"] = "useRouteLoaderData";
  DataRouterStateHook2["UseMatches"] = "useMatches";
  DataRouterStateHook2["UseRevalidator"] = "useRevalidator";
  DataRouterStateHook2["UseNavigateStable"] = "useNavigate";
  DataRouterStateHook2["UseRouteId"] = "useRouteId";
  return DataRouterStateHook2;
})(DataRouterStateHook$1 || {});
function useDataRouterContext$1(hookName) {
  let ctx = reactExports.useContext(DataRouterContext);
  !ctx ? invariant(false) : void 0;
  return ctx;
}
function useDataRouterState(hookName) {
  let state = reactExports.useContext(DataRouterStateContext);
  !state ? invariant(false) : void 0;
  return state;
}
function useRouteContext(hookName) {
  let route2 = reactExports.useContext(RouteContext);
  !route2 ? invariant(false) : void 0;
  return route2;
}
function useCurrentRouteId(hookName) {
  let route2 = useRouteContext();
  let thisRoute = route2.matches[route2.matches.length - 1];
  !thisRoute.route.id ? invariant(false) : void 0;
  return thisRoute.route.id;
}
function useRouteError() {
  var _state$errors;
  let error = reactExports.useContext(RouteErrorContext);
  let state = useDataRouterState();
  let routeId = useCurrentRouteId();
  if (error !== void 0) {
    return error;
  }
  return (_state$errors = state.errors) == null ? void 0 : _state$errors[routeId];
}
function useNavigateStable() {
  let {
    router
  } = useDataRouterContext$1(DataRouterHook$1.UseNavigateStable);
  let id2 = useCurrentRouteId(DataRouterStateHook$1.UseNavigateStable);
  let activeRef = reactExports.useRef(false);
  useIsomorphicLayoutEffect$1(() => {
    activeRef.current = true;
  });
  let navigate2 = reactExports.useCallback(function(to, options) {
    if (options === void 0) {
      options = {};
    }
    if (!activeRef.current) return;
    if (typeof to === "number") {
      router.navigate(to);
    } else {
      router.navigate(to, _extends$2({
        fromRouteId: id2
      }, options));
    }
  }, [router, id2]);
  return navigate2;
}
const alreadyWarned$1 = {};
function warningOnce(key, cond, message) {
  if (!alreadyWarned$1[key]) {
    alreadyWarned$1[key] = true;
  }
}
function logV6DeprecationWarnings(renderFuture, routerFuture) {
  if ((renderFuture == null ? void 0 : renderFuture.v7_startTransition) === void 0) ;
  if ((renderFuture == null ? void 0 : renderFuture.v7_relativeSplatPath) === void 0 && true) ;
}
function Navigate(_ref4) {
  let {
    to,
    replace: replace2,
    state,
    relative
  } = _ref4;
  !useInRouterContext() ? invariant(false) : void 0;
  let {
    future,
    static: isStatic
  } = reactExports.useContext(NavigationContext);
  let {
    matches: matches2
  } = reactExports.useContext(RouteContext);
  let {
    pathname: locationPathname
  } = useLocation();
  let navigate2 = useNavigate();
  let path = resolveTo(to, getResolveToMatches(matches2, future.v7_relativeSplatPath), locationPathname, relative === "path");
  let jsonPath = JSON.stringify(path);
  reactExports.useEffect(() => navigate2(JSON.parse(jsonPath), {
    replace: replace2,
    state,
    relative
  }), [navigate2, jsonPath, relative, replace2, state]);
  return null;
}
function Outlet(props) {
  return useOutlet(props.context);
}
function Route(_props) {
  invariant(false);
}
function Router(_ref5) {
  let {
    basename: basenameProp = "/",
    children = null,
    location: locationProp,
    navigationType = Action$1.Pop,
    navigator: navigator2,
    static: staticProp = false,
    future
  } = _ref5;
  !!useInRouterContext() ? invariant(false) : void 0;
  let basename = basenameProp.replace(/^\/*/, "/");
  let navigationContext = reactExports.useMemo(() => ({
    basename,
    navigator: navigator2,
    static: staticProp,
    future: _extends$2({
      v7_relativeSplatPath: false
    }, future)
  }), [basename, future, navigator2, staticProp]);
  if (typeof locationProp === "string") {
    locationProp = parsePath(locationProp);
  }
  let {
    pathname = "/",
    search = "",
    hash: hash2 = "",
    state = null,
    key = "default"
  } = locationProp;
  let locationContext = reactExports.useMemo(() => {
    let trailingPathname = stripBasename(pathname, basename);
    if (trailingPathname == null) {
      return null;
    }
    return {
      location: {
        pathname: trailingPathname,
        search,
        hash: hash2,
        state,
        key
      },
      navigationType
    };
  }, [basename, pathname, search, hash2, state, key, navigationType]);
  if (locationContext == null) {
    return null;
  }
  return /* @__PURE__ */ reactExports.createElement(NavigationContext.Provider, {
    value: navigationContext
  }, /* @__PURE__ */ reactExports.createElement(LocationContext.Provider, {
    children,
    value: locationContext
  }));
}
function Routes(_ref6) {
  let {
    children,
    location
  } = _ref6;
  return useRoutes(createRoutesFromChildren(children), location);
}
new Promise(() => {
});
function createRoutesFromChildren(children, parentPath) {
  if (parentPath === void 0) {
    parentPath = [];
  }
  let routes = [];
  reactExports.Children.forEach(children, (element, index2) => {
    if (!/* @__PURE__ */ reactExports.isValidElement(element)) {
      return;
    }
    let treePath = [...parentPath, index2];
    if (element.type === reactExports.Fragment) {
      routes.push.apply(routes, createRoutesFromChildren(element.props.children, treePath));
      return;
    }
    !(element.type === Route) ? invariant(false) : void 0;
    !(!element.props.index || !element.props.children) ? invariant(false) : void 0;
    let route2 = {
      id: element.props.id || treePath.join("-"),
      caseSensitive: element.props.caseSensitive,
      element: element.props.element,
      Component: element.props.Component,
      index: element.props.index,
      path: element.props.path,
      loader: element.props.loader,
      action: element.props.action,
      errorElement: element.props.errorElement,
      ErrorBoundary: element.props.ErrorBoundary,
      hasErrorBoundary: element.props.ErrorBoundary != null || element.props.errorElement != null,
      shouldRevalidate: element.props.shouldRevalidate,
      handle: element.props.handle,
      lazy: element.props.lazy
    };
    if (element.props.children) {
      route2.children = createRoutesFromChildren(element.props.children, treePath);
    }
    routes.push(route2);
  });
  return routes;
}
/**
 * React Router DOM v6.30.1
 *
 * Copyright (c) Remix Software Inc.
 *
 * This source code is licensed under the MIT license found in the
 * LICENSE.md file in the root directory of this source tree.
 *
 * @license MIT
 */
function _extends$1() {
  _extends$1 = Object.assign ? Object.assign.bind() : function(target) {
    for (var i2 = 1; i2 < arguments.length; i2++) {
      var source = arguments[i2];
      for (var key in source) {
        if (Object.prototype.hasOwnProperty.call(source, key)) {
          target[key] = source[key];
        }
      }
    }
    return target;
  };
  return _extends$1.apply(this, arguments);
}
function _objectWithoutPropertiesLoose$8(source, excluded) {
  if (source == null) return {};
  var target = {};
  var sourceKeys = Object.keys(source);
  var key, i2;
  for (i2 = 0; i2 < sourceKeys.length; i2++) {
    key = sourceKeys[i2];
    if (excluded.indexOf(key) >= 0) continue;
    target[key] = source[key];
  }
  return target;
}
function isModifiedEvent$1(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
function shouldProcessLinkClick(event, target) {
  return event.button === 0 && // Ignore everything but left clicks
  (!target || target === "_self") && // Let browser handle "target=_blank" etc.
  !isModifiedEvent$1(event);
}
const _excluded$f = ["onClick", "relative", "reloadDocument", "replace", "state", "target", "to", "preventScrollReset", "viewTransition"], _excluded2$2 = ["aria-current", "caseSensitive", "className", "end", "style", "to", "viewTransition", "children"];
const REACT_ROUTER_VERSION = "6";
try {
  window.__reactRouterVersion = REACT_ROUTER_VERSION;
} catch (e2) {
}
const ViewTransitionContext = /* @__PURE__ */ reactExports.createContext({
  isTransitioning: false
});
const START_TRANSITION = "startTransition";
const startTransitionImpl = React[START_TRANSITION];
function BrowserRouter(_ref4) {
  let {
    basename,
    children,
    future,
    window: window2
  } = _ref4;
  let historyRef = reactExports.useRef();
  if (historyRef.current == null) {
    historyRef.current = createBrowserHistory({
      window: window2,
      v5Compat: true
    });
  }
  let history = historyRef.current;
  let [state, setStateImpl] = reactExports.useState({
    action: history.action,
    location: history.location
  });
  let {
    v7_startTransition
  } = future || {};
  let setState = reactExports.useCallback((newState) => {
    v7_startTransition && startTransitionImpl ? startTransitionImpl(() => setStateImpl(newState)) : setStateImpl(newState);
  }, [setStateImpl, v7_startTransition]);
  reactExports.useLayoutEffect(() => history.listen(setState), [history, setState]);
  reactExports.useEffect(() => logV6DeprecationWarnings(future), [future]);
  return /* @__PURE__ */ reactExports.createElement(Router, {
    basename,
    children,
    location: state.location,
    navigationType: state.action,
    navigator: history,
    future
  });
}
const isBrowser = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
const ABSOLUTE_URL_REGEX = /^(?:[a-z][a-z0-9+.-]*:|\/\/)/i;
const Link = /* @__PURE__ */ reactExports.forwardRef(function LinkWithRef(_ref7, ref) {
  let {
    onClick,
    relative,
    reloadDocument,
    replace: replace2,
    state,
    target,
    to,
    preventScrollReset,
    viewTransition
  } = _ref7, rest = _objectWithoutPropertiesLoose$8(_ref7, _excluded$f);
  let {
    basename
  } = reactExports.useContext(NavigationContext);
  let absoluteHref;
  let isExternal = false;
  if (typeof to === "string" && ABSOLUTE_URL_REGEX.test(to)) {
    absoluteHref = to;
    if (isBrowser) {
      try {
        let currentUrl = new URL(window.location.href);
        let targetUrl = to.startsWith("//") ? new URL(currentUrl.protocol + to) : new URL(to);
        let path = stripBasename(targetUrl.pathname, basename);
        if (targetUrl.origin === currentUrl.origin && path != null) {
          to = path + targetUrl.search + targetUrl.hash;
        } else {
          isExternal = true;
        }
      } catch (e2) {
      }
    }
  }
  let href = useHref(to, {
    relative
  });
  let internalOnClick = useLinkClickHandler(to, {
    replace: replace2,
    state,
    target,
    preventScrollReset,
    relative,
    viewTransition
  });
  function handleClick(event) {
    if (onClick) onClick(event);
    if (!event.defaultPrevented) {
      internalOnClick(event);
    }
  }
  return (
    // eslint-disable-next-line jsx-a11y/anchor-has-content
    /* @__PURE__ */ reactExports.createElement("a", _extends$1({}, rest, {
      href: absoluteHref || href,
      onClick: isExternal || reloadDocument ? onClick : handleClick,
      ref,
      target
    }))
  );
});
const NavLink$1 = /* @__PURE__ */ reactExports.forwardRef(function NavLinkWithRef(_ref8, ref) {
  let {
    "aria-current": ariaCurrentProp = "page",
    caseSensitive = false,
    className: classNameProp = "",
    end: end2 = false,
    style: styleProp,
    to,
    viewTransition,
    children
  } = _ref8, rest = _objectWithoutPropertiesLoose$8(_ref8, _excluded2$2);
  let path = useResolvedPath(to, {
    relative: rest.relative
  });
  let location = useLocation();
  let routerState = reactExports.useContext(DataRouterStateContext);
  let {
    navigator: navigator2,
    basename
  } = reactExports.useContext(NavigationContext);
  let isTransitioning = routerState != null && // Conditional usage is OK here because the usage of a data router is static
  // eslint-disable-next-line react-hooks/rules-of-hooks
  useViewTransitionState(path) && viewTransition === true;
  let toPathname = navigator2.encodeLocation ? navigator2.encodeLocation(path).pathname : path.pathname;
  let locationPathname = location.pathname;
  let nextLocationPathname = routerState && routerState.navigation && routerState.navigation.location ? routerState.navigation.location.pathname : null;
  if (!caseSensitive) {
    locationPathname = locationPathname.toLowerCase();
    nextLocationPathname = nextLocationPathname ? nextLocationPathname.toLowerCase() : null;
    toPathname = toPathname.toLowerCase();
  }
  if (nextLocationPathname && basename) {
    nextLocationPathname = stripBasename(nextLocationPathname, basename) || nextLocationPathname;
  }
  const endSlashPosition = toPathname !== "/" && toPathname.endsWith("/") ? toPathname.length - 1 : toPathname.length;
  let isActive = locationPathname === toPathname || !end2 && locationPathname.startsWith(toPathname) && locationPathname.charAt(endSlashPosition) === "/";
  let isPending = nextLocationPathname != null && (nextLocationPathname === toPathname || !end2 && nextLocationPathname.startsWith(toPathname) && nextLocationPathname.charAt(toPathname.length) === "/");
  let renderProps = {
    isActive,
    isPending,
    isTransitioning
  };
  let ariaCurrent = isActive ? ariaCurrentProp : void 0;
  let className;
  if (typeof classNameProp === "function") {
    className = classNameProp(renderProps);
  } else {
    className = [classNameProp, isActive ? "active" : null, isPending ? "pending" : null, isTransitioning ? "transitioning" : null].filter(Boolean).join(" ");
  }
  let style2 = typeof styleProp === "function" ? styleProp(renderProps) : styleProp;
  return /* @__PURE__ */ reactExports.createElement(Link, _extends$1({}, rest, {
    "aria-current": ariaCurrent,
    className,
    ref,
    style: style2,
    to,
    viewTransition
  }), typeof children === "function" ? children(renderProps) : children);
});
var DataRouterHook;
(function(DataRouterHook2) {
  DataRouterHook2["UseScrollRestoration"] = "useScrollRestoration";
  DataRouterHook2["UseSubmit"] = "useSubmit";
  DataRouterHook2["UseSubmitFetcher"] = "useSubmitFetcher";
  DataRouterHook2["UseFetcher"] = "useFetcher";
  DataRouterHook2["useViewTransitionState"] = "useViewTransitionState";
})(DataRouterHook || (DataRouterHook = {}));
var DataRouterStateHook;
(function(DataRouterStateHook2) {
  DataRouterStateHook2["UseFetcher"] = "useFetcher";
  DataRouterStateHook2["UseFetchers"] = "useFetchers";
  DataRouterStateHook2["UseScrollRestoration"] = "useScrollRestoration";
})(DataRouterStateHook || (DataRouterStateHook = {}));
function useDataRouterContext(hookName) {
  let ctx = reactExports.useContext(DataRouterContext);
  !ctx ? invariant(false) : void 0;
  return ctx;
}
function useLinkClickHandler(to, _temp) {
  let {
    target,
    replace: replaceProp,
    state,
    preventScrollReset,
    relative,
    viewTransition
  } = _temp === void 0 ? {} : _temp;
  let navigate2 = useNavigate();
  let location = useLocation();
  let path = useResolvedPath(to, {
    relative
  });
  return reactExports.useCallback((event) => {
    if (shouldProcessLinkClick(event, target)) {
      event.preventDefault();
      let replace2 = replaceProp !== void 0 ? replaceProp : createPath(location) === createPath(path);
      navigate2(to, {
        replace: replace2,
        state,
        preventScrollReset,
        relative,
        viewTransition
      });
    }
  }, [location, navigate2, path, replaceProp, state, target, to, preventScrollReset, relative, viewTransition]);
}
function useViewTransitionState(to, opts) {
  if (opts === void 0) {
    opts = {};
  }
  let vtContext = reactExports.useContext(ViewTransitionContext);
  !(vtContext != null) ? invariant(false) : void 0;
  let {
    basename
  } = useDataRouterContext(DataRouterHook.useViewTransitionState);
  let path = useResolvedPath(to, {
    relative: opts.relative
  });
  if (!vtContext.isTransitioning) {
    return false;
  }
  let currentPath = stripBasename(vtContext.currentLocation.pathname, basename) || vtContext.currentLocation.pathname;
  let nextPath = stripBasename(vtContext.nextLocation.pathname, basename) || vtContext.nextLocation.pathname;
  return matchPath(path.pathname, nextPath) != null || matchPath(path.pathname, currentPath) != null;
}
const light = { "colors": { "background": "#f8f8f8", "text": "#000000", "primary": "#4f46e5", "accent": "#a78bfa" }, "fonts": { "body": "Arial, sans-serif" } };
const dark = { "colors": { "background": "#1e2030", "text": "#e5e5e5", "primary": "#0084ff", "accent": "#6699ff" }, "fonts": { "body": "Arial, sans-serif" } };
const themeConfig = {
  light,
  dark
};
const ThemeContext$1 = reactExports.createContext({
  themeName: "light",
  setThemeName: () => {
  },
  toggleTheme: () => {
  }
});
function ThemeProviderWithStyled({ children }) {
  const [StyledThemeProvider, setStyledThemeProvider] = reactExports.useState(null);
  const [themeName, setThemeName] = reactExports.useState(() => {
    try {
      return localStorage.getItem("theme") || "light";
    } catch {
      return "light";
    }
  });
  const [themes] = reactExports.useState(themeConfig);
  reactExports.useEffect(() => {
    let active = true;
    __vitePreload(() => import("./styled-components.browser.esm-CNtzB2--.js"), true ? [] : void 0).then((mod) => {
      if (active) setStyledThemeProvider(() => mod.ThemeProvider);
    }).catch(() => {
      if (active) setStyledThemeProvider(null);
    });
    return () => {
      active = false;
    };
  }, []);
  const toggleTheme = () => setThemeName((prev2) => prev2 === "light" ? "dark" : "light");
  reactExports.useEffect(() => {
    try {
      localStorage.setItem("theme", themeName);
    } catch {
    }
  }, [themeName]);
  const currentTheme = themes[themeName];
  if (!currentTheme) {
    console.error(`Theme '${themeName}' not found`);
  }
  const value = { themeName, setThemeName, toggleTheme };
  const ProviderComponent = StyledThemeProvider || FallbackThemeProvider;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeContext$1.Provider, { value, children: /* @__PURE__ */ jsxRuntimeExports.jsx(ProviderComponent, { theme: currentTheme || themes.light, children }) });
}
function FallbackThemeProvider({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children });
}
const ThemeProvider = ThemeProviderWithStyled;
function useTheme() {
  return reactExports.useContext(ThemeContext$1);
}
const tokenCache = {};
function buildKey(options = {}) {
  const { stationId, screenId } = options;
  return `${stationId || "base"}-${screenId || "base"}`;
}
async function resolveTokens(options = {}) {
  const key = buildKey(options);
  if (tokenCache[key]) return tokenCache[key];
  const params = new URLSearchParams();
  if (options.stationId) params.append("stationId", options.stationId);
  if (options.screenId) params.append("screenId", options.screenId);
  const qs = params.toString();
  let tokens = {};
  try {
    const res = await fetch(`/api/tokens${qs ? `?${qs}` : ""}`);
    tokens = res.ok ? await res.json() : {};
  } catch (err) {
    console.error(err);
  }
  tokenCache[key] = tokens;
  return tokens;
}
function clearTokenCache() {
  Object.keys(tokenCache).forEach((key) => delete tokenCache[key]);
}
function setVars(prefix, obj, vars) {
  Object.entries(obj || {}).forEach(([key, val]) => {
    const name2 = prefix ? `${prefix}-${key}` : key;
    if (val && typeof val === "object" && !Array.isArray(val)) {
      if ("$value" in val) {
        vars[`--token-${name2}`] = String(val.$value);
      } else {
        setVars(name2, val, vars);
      }
    }
  });
}
function TokenCSSVariables() {
  reactExports.useEffect(() => {
    let active = true;
    (async () => {
      try {
        const tokens = await resolveTokens() || {};
        if (!active) return;
        const vars = {};
        setVars("", tokens, vars);
        const root = document.documentElement;
        Object.entries(vars).forEach(([k2, v]) => root.style.setProperty(k2, v));
      } catch {
      }
    })();
    return () => {
      active = false;
    };
  }, []);
  return null;
}
var classnames = { exports: {} };
/*!
	Copyright (c) 2018 Jed Watson.
	Licensed under the MIT License (MIT), see
	http://jedwatson.github.io/classnames
*/
var hasRequiredClassnames;
function requireClassnames() {
  if (hasRequiredClassnames) return classnames.exports;
  hasRequiredClassnames = 1;
  (function(module) {
    (function() {
      var hasOwn = {}.hasOwnProperty;
      function classNames() {
        var classes = "";
        for (var i2 = 0; i2 < arguments.length; i2++) {
          var arg = arguments[i2];
          if (arg) {
            classes = appendClass(classes, parseValue(arg));
          }
        }
        return classes;
      }
      function parseValue(arg) {
        if (typeof arg === "string" || typeof arg === "number") {
          return arg;
        }
        if (typeof arg !== "object") {
          return "";
        }
        if (Array.isArray(arg)) {
          return classNames.apply(null, arg);
        }
        if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) {
          return arg.toString();
        }
        var classes = "";
        for (var key in arg) {
          if (hasOwn.call(arg, key) && arg[key]) {
            classes = appendClass(classes, key);
          }
        }
        return classes;
      }
      function appendClass(value, newClass) {
        if (!newClass) {
          return value;
        }
        if (value) {
          return value + " " + newClass;
        }
        return value + newClass;
      }
      if (module.exports) {
        classNames.default = classNames;
        module.exports = classNames;
      } else {
        window.classNames = classNames;
      }
    })();
  })(classnames);
  return classnames.exports;
}
var classnamesExports = requireClassnames();
function _extends() {
  return _extends = Object.assign ? Object.assign.bind() : function(n) {
    for (var e2 = 1; e2 < arguments.length; e2++) {
      var t2 = arguments[e2];
      for (var r2 in t2) ({}).hasOwnProperty.call(t2, r2) && (n[r2] = t2[r2]);
    }
    return n;
  }, _extends.apply(null, arguments);
}
function _objectWithoutPropertiesLoose$7(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (-1 !== e2.indexOf(n)) continue;
    t2[n] = r2[n];
  }
  return t2;
}
var browser = { exports: {} };
var hasRequiredBrowser;
function requireBrowser() {
  if (hasRequiredBrowser) return browser.exports;
  hasRequiredBrowser = 1;
  var invariant2 = function(condition, format2, a3, b2, c2, d3, e2, f) {
    if (!condition) {
      var error;
      if (format2 === void 0) {
        error = new Error(
          "Minified exception occurred; use the non-minified dev environment for the full error message and additional helpful warnings."
        );
      } else {
        var args = [a3, b2, c2, d3, e2, f];
        var argIndex = 0;
        error = new Error(
          format2.replace(/%s/g, function() {
            return args[argIndex++];
          })
        );
        error.name = "Invariant Violation";
      }
      error.framesToPop = 1;
      throw error;
    }
  };
  browser.exports = invariant2;
  browser.exports.default = invariant2;
  browser.exports.__esModule = true;
  return browser.exports;
}
var browserExports = requireBrowser();
var noop$4 = function noop2() {
};
function uncontrolledPropTypes(controlledValues, displayName) {
  var propTypes2 = {};
  Object.keys(controlledValues).forEach(function(prop) {
    propTypes2[defaultKey(prop)] = noop$4;
  });
  return propTypes2;
}
function isProp(props, prop) {
  return props[prop] !== void 0;
}
function defaultKey(key) {
  return "default" + key.charAt(0).toUpperCase() + key.substr(1);
}
function canAcceptRef(component) {
  return !!component && (typeof component !== "function" || component.prototype && component.prototype.isReactComponent);
}
function _toPropertyKey(arg) {
  var key = _toPrimitive(arg, "string");
  return typeof key === "symbol" ? key : String(key);
}
function _toPrimitive(input, hint) {
  if (typeof input !== "object" || input === null) return input;
  var prim = input[Symbol.toPrimitive];
  if (prim !== void 0) {
    var res = prim.call(input, hint);
    if (typeof res !== "object") return res;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return String(input);
}
function useUncontrolledProp(propValue, defaultValue2, handler) {
  var wasPropRef = reactExports.useRef(propValue !== void 0);
  var _useState = reactExports.useState(defaultValue2), stateValue = _useState[0], setState = _useState[1];
  var isProp2 = propValue !== void 0;
  var wasProp = wasPropRef.current;
  wasPropRef.current = isProp2;
  if (!isProp2 && wasProp && stateValue !== defaultValue2) {
    setState(defaultValue2);
  }
  return [isProp2 ? propValue : stateValue, reactExports.useCallback(function(value) {
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }
    if (handler) handler.apply(void 0, [value].concat(args));
    setState(value);
  }, [handler])];
}
function useUncontrolled(props, config2) {
  return Object.keys(config2).reduce(function(result, fieldName) {
    var _extends2;
    var _ref = result, defaultValue2 = _ref[defaultKey(fieldName)], propsValue = _ref[fieldName], rest = _objectWithoutPropertiesLoose$7(_ref, [defaultKey(fieldName), fieldName].map(_toPropertyKey));
    var handlerName = config2[fieldName];
    var _useUncontrolledProp = useUncontrolledProp(propsValue, defaultValue2, props[handlerName]), value = _useUncontrolledProp[0], handler = _useUncontrolledProp[1];
    return _extends({}, rest, (_extends2 = {}, _extends2[fieldName] = value, _extends2[handlerName] = handler, _extends2));
  }, props);
}
function _setPrototypeOf(t2, e2) {
  return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t3, e3) {
    return t3.__proto__ = e3, t3;
  }, _setPrototypeOf(t2, e2);
}
function _inheritsLoose(t2, o) {
  t2.prototype = Object.create(o.prototype), t2.prototype.constructor = t2, _setPrototypeOf(t2, o);
}
function componentWillMount() {
  var state = this.constructor.getDerivedStateFromProps(this.props, this.state);
  if (state !== null && state !== void 0) {
    this.setState(state);
  }
}
function componentWillReceiveProps(nextProps) {
  function updater(prevState) {
    var state = this.constructor.getDerivedStateFromProps(nextProps, prevState);
    return state !== null && state !== void 0 ? state : null;
  }
  this.setState(updater.bind(this));
}
function componentWillUpdate(nextProps, nextState) {
  try {
    var prevProps = this.props;
    var prevState = this.state;
    this.props = nextProps;
    this.state = nextState;
    this.__reactInternalSnapshotFlag = true;
    this.__reactInternalSnapshot = this.getSnapshotBeforeUpdate(
      prevProps,
      prevState
    );
  } finally {
    this.props = prevProps;
    this.state = prevState;
  }
}
componentWillMount.__suppressDeprecationWarning = true;
componentWillReceiveProps.__suppressDeprecationWarning = true;
componentWillUpdate.__suppressDeprecationWarning = true;
function polyfill2(Component) {
  var prototype = Component.prototype;
  if (!prototype || !prototype.isReactComponent) {
    throw new Error("Can only polyfill class components");
  }
  if (typeof Component.getDerivedStateFromProps !== "function" && typeof prototype.getSnapshotBeforeUpdate !== "function") {
    return Component;
  }
  var foundWillMountName = null;
  var foundWillReceivePropsName = null;
  var foundWillUpdateName = null;
  if (typeof prototype.componentWillMount === "function") {
    foundWillMountName = "componentWillMount";
  } else if (typeof prototype.UNSAFE_componentWillMount === "function") {
    foundWillMountName = "UNSAFE_componentWillMount";
  }
  if (typeof prototype.componentWillReceiveProps === "function") {
    foundWillReceivePropsName = "componentWillReceiveProps";
  } else if (typeof prototype.UNSAFE_componentWillReceiveProps === "function") {
    foundWillReceivePropsName = "UNSAFE_componentWillReceiveProps";
  }
  if (typeof prototype.componentWillUpdate === "function") {
    foundWillUpdateName = "componentWillUpdate";
  } else if (typeof prototype.UNSAFE_componentWillUpdate === "function") {
    foundWillUpdateName = "UNSAFE_componentWillUpdate";
  }
  if (foundWillMountName !== null || foundWillReceivePropsName !== null || foundWillUpdateName !== null) {
    var componentName = Component.displayName || Component.name;
    var newApiName = typeof Component.getDerivedStateFromProps === "function" ? "getDerivedStateFromProps()" : "getSnapshotBeforeUpdate()";
    throw Error(
      "Unsafe legacy lifecycles will not be called for components using new component APIs.\n\n" + componentName + " uses " + newApiName + " but also contains the following legacy lifecycles:" + (foundWillMountName !== null ? "\n  " + foundWillMountName : "") + (foundWillReceivePropsName !== null ? "\n  " + foundWillReceivePropsName : "") + (foundWillUpdateName !== null ? "\n  " + foundWillUpdateName : "") + "\n\nThe above lifecycles should be removed. Learn more about this warning here:\nhttps://fb.me/react-async-component-lifecycle-hooks"
    );
  }
  if (typeof Component.getDerivedStateFromProps === "function") {
    prototype.componentWillMount = componentWillMount;
    prototype.componentWillReceiveProps = componentWillReceiveProps;
  }
  if (typeof prototype.getSnapshotBeforeUpdate === "function") {
    if (typeof prototype.componentDidUpdate !== "function") {
      throw new Error(
        "Cannot polyfill getSnapshotBeforeUpdate() for components that do not define componentDidUpdate() on the prototype"
      );
    }
    prototype.componentWillUpdate = componentWillUpdate;
    var componentDidUpdate = prototype.componentDidUpdate;
    prototype.componentDidUpdate = function componentDidUpdatePolyfill(prevProps, prevState, maybeSnapshot) {
      var snapshot = this.__reactInternalSnapshotFlag ? this.__reactInternalSnapshot : maybeSnapshot;
      componentDidUpdate.call(this, prevProps, prevState, snapshot);
    };
  }
  return Component;
}
var _jsxFileName = "/Users/jquense/src/uncontrollable/src/uncontrollable.js";
function uncontrollable(Component, controlledValues, methods) {
  if (methods === void 0) {
    methods = [];
  }
  var displayName = Component.displayName || Component.name || "Component";
  var canAcceptRef$1 = canAcceptRef(Component);
  var controlledProps = Object.keys(controlledValues);
  var PROPS_TO_OMIT = controlledProps.map(defaultKey);
  !(canAcceptRef$1 || !methods.length) ? browserExports(false) : void 0;
  var UncontrolledComponent = /* @__PURE__ */ (function(_React$Component) {
    _inheritsLoose(UncontrolledComponent2, _React$Component);
    function UncontrolledComponent2() {
      var _this;
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      _this = _React$Component.call.apply(_React$Component, [this].concat(args)) || this;
      _this.handlers = /* @__PURE__ */ Object.create(null);
      controlledProps.forEach(function(propName) {
        var handlerName = controlledValues[propName];
        var handleChange = function handleChange2(value) {
          if (_this.props[handlerName]) {
            var _this$props;
            _this._notifying = true;
            for (var _len2 = arguments.length, args2 = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
              args2[_key2 - 1] = arguments[_key2];
            }
            (_this$props = _this.props)[handlerName].apply(_this$props, [value].concat(args2));
            _this._notifying = false;
          }
          if (!_this.unmounted) _this.setState(function(_ref) {
            var _extends2;
            var values2 = _ref.values;
            return {
              values: _extends(/* @__PURE__ */ Object.create(null), values2, (_extends2 = {}, _extends2[propName] = value, _extends2))
            };
          });
        };
        _this.handlers[handlerName] = handleChange;
      });
      if (methods.length) _this.attachRef = function(ref) {
        _this.inner = ref;
      };
      var values = /* @__PURE__ */ Object.create(null);
      controlledProps.forEach(function(key) {
        values[key] = _this.props[defaultKey(key)];
      });
      _this.state = {
        values,
        prevProps: {}
      };
      return _this;
    }
    var _proto = UncontrolledComponent2.prototype;
    _proto.shouldComponentUpdate = function shouldComponentUpdate() {
      return !this._notifying;
    };
    UncontrolledComponent2.getDerivedStateFromProps = function getDerivedStateFromProps(props, _ref2) {
      var values = _ref2.values, prevProps = _ref2.prevProps;
      var nextState = {
        values: _extends(/* @__PURE__ */ Object.create(null), values),
        prevProps: {}
      };
      controlledProps.forEach(function(key) {
        nextState.prevProps[key] = props[key];
        if (!isProp(props, key) && isProp(prevProps, key)) {
          nextState.values[key] = props[defaultKey(key)];
        }
      });
      return nextState;
    };
    _proto.componentWillUnmount = function componentWillUnmount() {
      this.unmounted = true;
    };
    _proto.render = function render() {
      var _this2 = this;
      var _this$props2 = this.props, innerRef = _this$props2.innerRef, props = _objectWithoutPropertiesLoose$7(_this$props2, ["innerRef"]);
      PROPS_TO_OMIT.forEach(function(prop) {
        delete props[prop];
      });
      var newProps = {};
      controlledProps.forEach(function(propName) {
        var propValue = _this2.props[propName];
        newProps[propName] = propValue !== void 0 ? propValue : _this2.state.values[propName];
      });
      return reactExports.createElement(Component, _extends({}, props, newProps, this.handlers, {
        ref: innerRef || this.attachRef
      }));
    };
    return UncontrolledComponent2;
  })(reactExports.Component);
  polyfill2(UncontrolledComponent);
  UncontrolledComponent.displayName = "Uncontrolled(" + displayName + ")";
  UncontrolledComponent.propTypes = _extends({
    innerRef: function innerRef() {
    }
  }, uncontrolledPropTypes(controlledValues));
  methods.forEach(function(method) {
    UncontrolledComponent.prototype[method] = function $proxiedMethod() {
      var _this$inner;
      return (_this$inner = this.inner)[method].apply(_this$inner, arguments);
    };
  });
  var WrappedComponent = UncontrolledComponent;
  if (reactExports.forwardRef) {
    WrappedComponent = reactExports.forwardRef(function(props, ref) {
      return reactExports.createElement(UncontrolledComponent, _extends({}, props, {
        innerRef: ref,
        __source: {
          fileName: _jsxFileName,
          lineNumber: 128
        },
        __self: this
      }));
    });
    WrappedComponent.propTypes = UncontrolledComponent.propTypes;
  }
  WrappedComponent.ControlledComponent = Component;
  WrappedComponent.deferControlTo = function(newComponent, additions, nextMethods) {
    if (additions === void 0) {
      additions = {};
    }
    return uncontrollable(newComponent, _extends({}, controlledValues, additions), nextMethods);
  };
  return WrappedComponent;
}
const DEFAULT_BREAKPOINTS = ["xxl", "xl", "lg", "md", "sm", "xs"];
const DEFAULT_MIN_BREAKPOINT = "xs";
const ThemeContext = /* @__PURE__ */ reactExports.createContext({
  prefixes: {},
  breakpoints: DEFAULT_BREAKPOINTS,
  minBreakpoint: DEFAULT_MIN_BREAKPOINT
});
const {
  Consumer,
  Provider
} = ThemeContext;
function useBootstrapPrefix(prefix, defaultPrefix) {
  const {
    prefixes
  } = reactExports.useContext(ThemeContext);
  return prefix || prefixes[defaultPrefix] || defaultPrefix;
}
function ownerDocument$1(node) {
  return node && node.ownerDocument || document;
}
function ownerWindow(node) {
  var doc = ownerDocument$1(node);
  return doc && doc.defaultView || window;
}
function getComputedStyle$1(node, psuedoElement) {
  return ownerWindow(node).getComputedStyle(node, psuedoElement);
}
var rUpper = /([A-Z])/g;
function hyphenate(string) {
  return string.replace(rUpper, "-$1").toLowerCase();
}
var msPattern = /^ms-/;
function hyphenateStyleName(string) {
  return hyphenate(string).replace(msPattern, "-ms-");
}
var supportedTransforms = /^((translate|rotate|scale)(X|Y|Z|3d)?|matrix(3d)?|perspective|skew(X|Y)?)$/i;
function isTransform(value) {
  return !!(value && supportedTransforms.test(value));
}
function style(node, property) {
  var css = "";
  var transforms = "";
  if (typeof property === "string") {
    return node.style.getPropertyValue(hyphenateStyleName(property)) || getComputedStyle$1(node).getPropertyValue(hyphenateStyleName(property));
  }
  Object.keys(property).forEach(function(key) {
    var value = property[key];
    if (!value && value !== 0) {
      node.style.removeProperty(hyphenateStyleName(key));
    } else if (isTransform(key)) {
      transforms += key + "(" + value + ") ";
    } else {
      css += hyphenateStyleName(key) + ": " + value + ";";
    }
  });
  if (transforms) {
    css += "transform: " + transforms + ";";
  }
  node.style.cssText += ";" + css;
}
var propTypes$2 = { exports: {} };
var ReactPropTypesSecret_1;
var hasRequiredReactPropTypesSecret;
function requireReactPropTypesSecret() {
  if (hasRequiredReactPropTypesSecret) return ReactPropTypesSecret_1;
  hasRequiredReactPropTypesSecret = 1;
  var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
  ReactPropTypesSecret_1 = ReactPropTypesSecret;
  return ReactPropTypesSecret_1;
}
var factoryWithThrowingShims;
var hasRequiredFactoryWithThrowingShims;
function requireFactoryWithThrowingShims() {
  if (hasRequiredFactoryWithThrowingShims) return factoryWithThrowingShims;
  hasRequiredFactoryWithThrowingShims = 1;
  var ReactPropTypesSecret = /* @__PURE__ */ requireReactPropTypesSecret();
  function emptyFunction() {
  }
  function emptyFunctionWithReset() {
  }
  emptyFunctionWithReset.resetWarningCache = emptyFunction;
  factoryWithThrowingShims = function() {
    function shim(props, propName, componentName, location, propFullName, secret) {
      if (secret === ReactPropTypesSecret) {
        return;
      }
      var err = new Error(
        "Calling PropTypes validators directly is not supported by the `prop-types` package. Use PropTypes.checkPropTypes() to call them. Read more at http://fb.me/use-check-prop-types"
      );
      err.name = "Invariant Violation";
      throw err;
    }
    shim.isRequired = shim;
    function getShim() {
      return shim;
    }
    var ReactPropTypes = {
      array: shim,
      bigint: shim,
      bool: shim,
      func: shim,
      number: shim,
      object: shim,
      string: shim,
      symbol: shim,
      any: shim,
      arrayOf: getShim,
      element: shim,
      elementType: shim,
      instanceOf: getShim,
      node: shim,
      objectOf: getShim,
      oneOf: getShim,
      oneOfType: getShim,
      shape: getShim,
      exact: getShim,
      checkPropTypes: emptyFunctionWithReset,
      resetWarningCache: emptyFunction
    };
    ReactPropTypes.PropTypes = ReactPropTypes;
    return ReactPropTypes;
  };
  return factoryWithThrowingShims;
}
var hasRequiredPropTypes$1;
function requirePropTypes$1() {
  if (hasRequiredPropTypes$1) return propTypes$2.exports;
  hasRequiredPropTypes$1 = 1;
  {
    propTypes$2.exports = /* @__PURE__ */ requireFactoryWithThrowingShims()();
  }
  return propTypes$2.exports;
}
var propTypesExports = /* @__PURE__ */ requirePropTypes$1();
const config = {
  disabled: false
};
const TransitionGroupContext = reactExports.createContext(null);
var forceReflow = function forceReflow2(node) {
  return node.scrollTop;
};
var UNMOUNTED = "unmounted";
var EXITED = "exited";
var ENTERING = "entering";
var ENTERED = "entered";
var EXITING = "exiting";
var Transition = /* @__PURE__ */ (function(_React$Component) {
  _inheritsLoose(Transition2, _React$Component);
  function Transition2(props, context2) {
    var _this;
    _this = _React$Component.call(this, props, context2) || this;
    var parentGroup = context2;
    var appear = parentGroup && !parentGroup.isMounting ? props.enter : props.appear;
    var initialStatus;
    _this.appearStatus = null;
    if (props.in) {
      if (appear) {
        initialStatus = EXITED;
        _this.appearStatus = ENTERING;
      } else {
        initialStatus = ENTERED;
      }
    } else {
      if (props.unmountOnExit || props.mountOnEnter) {
        initialStatus = UNMOUNTED;
      } else {
        initialStatus = EXITED;
      }
    }
    _this.state = {
      status: initialStatus
    };
    _this.nextCallback = null;
    return _this;
  }
  Transition2.getDerivedStateFromProps = function getDerivedStateFromProps(_ref, prevState) {
    var nextIn = _ref.in;
    if (nextIn && prevState.status === UNMOUNTED) {
      return {
        status: EXITED
      };
    }
    return null;
  };
  var _proto = Transition2.prototype;
  _proto.componentDidMount = function componentDidMount() {
    this.updateStatus(true, this.appearStatus);
  };
  _proto.componentDidUpdate = function componentDidUpdate(prevProps) {
    var nextStatus = null;
    if (prevProps !== this.props) {
      var status = this.state.status;
      if (this.props.in) {
        if (status !== ENTERING && status !== ENTERED) {
          nextStatus = ENTERING;
        }
      } else {
        if (status === ENTERING || status === ENTERED) {
          nextStatus = EXITING;
        }
      }
    }
    this.updateStatus(false, nextStatus);
  };
  _proto.componentWillUnmount = function componentWillUnmount() {
    this.cancelNextCallback();
  };
  _proto.getTimeouts = function getTimeouts() {
    var timeout2 = this.props.timeout;
    var exit, enter, appear;
    exit = enter = appear = timeout2;
    if (timeout2 != null && typeof timeout2 !== "number") {
      exit = timeout2.exit;
      enter = timeout2.enter;
      appear = timeout2.appear !== void 0 ? timeout2.appear : enter;
    }
    return {
      exit,
      enter,
      appear
    };
  };
  _proto.updateStatus = function updateStatus(mounting, nextStatus) {
    if (mounting === void 0) {
      mounting = false;
    }
    if (nextStatus !== null) {
      this.cancelNextCallback();
      if (nextStatus === ENTERING) {
        if (this.props.unmountOnExit || this.props.mountOnEnter) {
          var node = this.props.nodeRef ? this.props.nodeRef.current : reactDomExports.findDOMNode(this);
          if (node) forceReflow(node);
        }
        this.performEnter(mounting);
      } else {
        this.performExit();
      }
    } else if (this.props.unmountOnExit && this.state.status === EXITED) {
      this.setState({
        status: UNMOUNTED
      });
    }
  };
  _proto.performEnter = function performEnter(mounting) {
    var _this2 = this;
    var enter = this.props.enter;
    var appearing = this.context ? this.context.isMounting : mounting;
    var _ref2 = this.props.nodeRef ? [appearing] : [reactDomExports.findDOMNode(this), appearing], maybeNode = _ref2[0], maybeAppearing = _ref2[1];
    var timeouts = this.getTimeouts();
    var enterTimeout = appearing ? timeouts.appear : timeouts.enter;
    if (!mounting && !enter || config.disabled) {
      this.safeSetState({
        status: ENTERED
      }, function() {
        _this2.props.onEntered(maybeNode);
      });
      return;
    }
    this.props.onEnter(maybeNode, maybeAppearing);
    this.safeSetState({
      status: ENTERING
    }, function() {
      _this2.props.onEntering(maybeNode, maybeAppearing);
      _this2.onTransitionEnd(enterTimeout, function() {
        _this2.safeSetState({
          status: ENTERED
        }, function() {
          _this2.props.onEntered(maybeNode, maybeAppearing);
        });
      });
    });
  };
  _proto.performExit = function performExit() {
    var _this3 = this;
    var exit = this.props.exit;
    var timeouts = this.getTimeouts();
    var maybeNode = this.props.nodeRef ? void 0 : reactDomExports.findDOMNode(this);
    if (!exit || config.disabled) {
      this.safeSetState({
        status: EXITED
      }, function() {
        _this3.props.onExited(maybeNode);
      });
      return;
    }
    this.props.onExit(maybeNode);
    this.safeSetState({
      status: EXITING
    }, function() {
      _this3.props.onExiting(maybeNode);
      _this3.onTransitionEnd(timeouts.exit, function() {
        _this3.safeSetState({
          status: EXITED
        }, function() {
          _this3.props.onExited(maybeNode);
        });
      });
    });
  };
  _proto.cancelNextCallback = function cancelNextCallback() {
    if (this.nextCallback !== null) {
      this.nextCallback.cancel();
      this.nextCallback = null;
    }
  };
  _proto.safeSetState = function safeSetState(nextState, callback) {
    callback = this.setNextCallback(callback);
    this.setState(nextState, callback);
  };
  _proto.setNextCallback = function setNextCallback(callback) {
    var _this4 = this;
    var active = true;
    this.nextCallback = function(event) {
      if (active) {
        active = false;
        _this4.nextCallback = null;
        callback(event);
      }
    };
    this.nextCallback.cancel = function() {
      active = false;
    };
    return this.nextCallback;
  };
  _proto.onTransitionEnd = function onTransitionEnd(timeout2, handler) {
    this.setNextCallback(handler);
    var node = this.props.nodeRef ? this.props.nodeRef.current : reactDomExports.findDOMNode(this);
    var doesNotHaveTimeoutOrListener = timeout2 == null && !this.props.addEndListener;
    if (!node || doesNotHaveTimeoutOrListener) {
      setTimeout(this.nextCallback, 0);
      return;
    }
    if (this.props.addEndListener) {
      var _ref3 = this.props.nodeRef ? [this.nextCallback] : [node, this.nextCallback], maybeNode = _ref3[0], maybeNextCallback = _ref3[1];
      this.props.addEndListener(maybeNode, maybeNextCallback);
    }
    if (timeout2 != null) {
      setTimeout(this.nextCallback, timeout2);
    }
  };
  _proto.render = function render() {
    var status = this.state.status;
    if (status === UNMOUNTED) {
      return null;
    }
    var _this$props = this.props, children = _this$props.children;
    _this$props.in;
    _this$props.mountOnEnter;
    _this$props.unmountOnExit;
    _this$props.appear;
    _this$props.enter;
    _this$props.exit;
    _this$props.timeout;
    _this$props.addEndListener;
    _this$props.onEnter;
    _this$props.onEntering;
    _this$props.onEntered;
    _this$props.onExit;
    _this$props.onExiting;
    _this$props.onExited;
    _this$props.nodeRef;
    var childProps = _objectWithoutPropertiesLoose$7(_this$props, ["children", "in", "mountOnEnter", "unmountOnExit", "appear", "enter", "exit", "timeout", "addEndListener", "onEnter", "onEntering", "onEntered", "onExit", "onExiting", "onExited", "nodeRef"]);
    return (
      // allows for nested Transitions
      /* @__PURE__ */ reactExports.createElement(TransitionGroupContext.Provider, {
        value: null
      }, typeof children === "function" ? children(status, childProps) : reactExports.cloneElement(reactExports.Children.only(children), childProps))
    );
  };
  return Transition2;
})(reactExports.Component);
Transition.contextType = TransitionGroupContext;
Transition.propTypes = {};
function noop$3() {
}
Transition.defaultProps = {
  in: false,
  mountOnEnter: false,
  unmountOnExit: false,
  appear: false,
  enter: true,
  exit: true,
  onEnter: noop$3,
  onEntering: noop$3,
  onEntered: noop$3,
  onExit: noop$3,
  onExiting: noop$3,
  onExited: noop$3
};
Transition.UNMOUNTED = UNMOUNTED;
Transition.EXITED = EXITED;
Transition.ENTERING = ENTERING;
Transition.ENTERED = ENTERED;
Transition.EXITING = EXITING;
function isEscKey(e2) {
  return e2.code === "Escape" || e2.keyCode === 27;
}
function getReactVersion() {
  const parts = reactExports.version.split(".");
  return {
    major: +parts[0],
    minor: +parts[1],
    patch: +parts[2]
  };
}
function getChildRef(element) {
  if (!element || typeof element === "function") {
    return null;
  }
  const {
    major
  } = getReactVersion();
  const childRef = major >= 19 ? element.props.ref : element.ref;
  return childRef;
}
const canUseDOM$1 = !!(typeof window !== "undefined" && window.document && window.document.createElement);
var optionsSupported = false;
var onceSupported = false;
try {
  var options = {
    get passive() {
      return optionsSupported = true;
    },
    get once() {
      return onceSupported = optionsSupported = true;
    }
  };
  if (canUseDOM$1) {
    window.addEventListener("test", options, options);
    window.removeEventListener("test", options, true);
  }
} catch (e2) {
}
function addEventListener$1(node, eventName, handler, options) {
  if (options && typeof options !== "boolean" && !onceSupported) {
    var once = options.once, capture = options.capture;
    var wrappedHandler = handler;
    if (!onceSupported && once) {
      wrappedHandler = handler.__once || function onceHandler(event) {
        this.removeEventListener(eventName, onceHandler, capture);
        handler.call(this, event);
      };
      handler.__once = wrappedHandler;
    }
    node.addEventListener(eventName, wrappedHandler, optionsSupported ? options : capture);
  }
  node.addEventListener(eventName, handler, options);
}
function removeEventListener(node, eventName, handler, options) {
  var capture = options && typeof options !== "boolean" ? options.capture : options;
  node.removeEventListener(eventName, handler, capture);
  if (handler.__once) {
    node.removeEventListener(eventName, handler.__once, capture);
  }
}
function listen(node, eventName, handler, options) {
  addEventListener$1(node, eventName, handler, options);
  return function() {
    removeEventListener(node, eventName, handler, options);
  };
}
function triggerEvent(node, eventName, bubbles, cancelable) {
  if (bubbles === void 0) {
    bubbles = false;
  }
  if (cancelable === void 0) {
    cancelable = true;
  }
  if (node) {
    var event = document.createEvent("HTMLEvents");
    event.initEvent(eventName, bubbles, cancelable);
    node.dispatchEvent(event);
  }
}
function parseDuration$1(node) {
  var str = style(node, "transitionDuration") || "";
  var mult = str.indexOf("ms") === -1 ? 1e3 : 1;
  return parseFloat(str) * mult;
}
function emulateTransitionEnd(element, duration2, padding) {
  if (padding === void 0) {
    padding = 5;
  }
  var called = false;
  var handle = setTimeout(function() {
    if (!called) triggerEvent(element, "transitionend", true);
  }, duration2 + padding);
  var remove2 = listen(element, "transitionend", function() {
    called = true;
  }, {
    once: true
  });
  return function() {
    clearTimeout(handle);
    remove2();
  };
}
function transitionEnd(element, handler, duration2, padding) {
  if (duration2 == null) duration2 = parseDuration$1(element) || 0;
  var removeEmulate = emulateTransitionEnd(element, duration2, padding);
  var remove2 = listen(element, "transitionend", handler);
  return function() {
    removeEmulate();
    remove2();
  };
}
function parseDuration(node, property) {
  const str = style(node, property) || "";
  const mult = str.indexOf("ms") === -1 ? 1e3 : 1;
  return parseFloat(str) * mult;
}
function transitionEndListener(element, handler) {
  const duration2 = parseDuration(element, "transitionDuration");
  const delay = parseDuration(element, "transitionDelay");
  const remove2 = transitionEnd(element, (e2) => {
    if (e2.target === element) {
      remove2();
      handler(e2);
    }
  }, duration2 + delay);
}
function createChainedFunction(...funcs) {
  return funcs.filter((f) => f != null).reduce((acc, f) => {
    if (typeof f !== "function") {
      throw new Error("Invalid Argument Type, must only provide functions, undefined, or null.");
    }
    if (acc === null) return f;
    return function chainedFunction(...args) {
      acc.apply(this, args);
      f.apply(this, args);
    };
  }, null);
}
function triggerBrowserReflow(node) {
  node.offsetHeight;
}
const toFnRef$1 = (ref) => !ref || typeof ref === "function" ? ref : (value) => {
  ref.current = value;
};
function mergeRefs$1(refA, refB) {
  const a3 = toFnRef$1(refA);
  const b2 = toFnRef$1(refB);
  return (value) => {
    if (a3) a3(value);
    if (b2) b2(value);
  };
}
function useMergedRefs$1(refA, refB) {
  return reactExports.useMemo(() => mergeRefs$1(refA, refB), [refA, refB]);
}
function safeFindDOMNode$1(componentOrElement) {
  if (componentOrElement && "setState" in componentOrElement) {
    return reactDomExports.findDOMNode(componentOrElement);
  }
  return componentOrElement != null ? componentOrElement : null;
}
const TransitionWrapper = /* @__PURE__ */ reactExports.forwardRef(({
  onEnter,
  onEntering,
  onEntered,
  onExit,
  onExiting,
  onExited,
  addEndListener,
  children,
  childRef,
  ...props
}, ref) => {
  const nodeRef = reactExports.useRef(null);
  const mergedRef = useMergedRefs$1(nodeRef, childRef);
  const attachRef = (r2) => {
    mergedRef(safeFindDOMNode$1(r2));
  };
  const normalize = (callback) => (param) => {
    if (callback && nodeRef.current) {
      callback(nodeRef.current, param);
    }
  };
  const handleEnter = reactExports.useCallback(normalize(onEnter), [onEnter]);
  const handleEntering = reactExports.useCallback(normalize(onEntering), [onEntering]);
  const handleEntered = reactExports.useCallback(normalize(onEntered), [onEntered]);
  const handleExit = reactExports.useCallback(normalize(onExit), [onExit]);
  const handleExiting = reactExports.useCallback(normalize(onExiting), [onExiting]);
  const handleExited = reactExports.useCallback(normalize(onExited), [onExited]);
  const handleAddEndListener = reactExports.useCallback(normalize(addEndListener), [addEndListener]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Transition, {
    ref,
    ...props,
    onEnter: handleEnter,
    onEntered: handleEntered,
    onEntering: handleEntering,
    onExit: handleExit,
    onExited: handleExited,
    onExiting: handleExiting,
    addEndListener: handleAddEndListener,
    nodeRef,
    children: typeof children === "function" ? (status, innerProps) => (
      // TODO: Types for RTG missing innerProps, so need to cast.
      children(status, {
        ...innerProps,
        ref: attachRef
      })
    ) : /* @__PURE__ */ reactExports.cloneElement(children, {
      ref: attachRef
    })
  });
});
TransitionWrapper.displayName = "TransitionWrapper";
const MARGINS = {
  height: ["marginTop", "marginBottom"],
  width: ["marginLeft", "marginRight"]
};
function getDefaultDimensionValue(dimension, elem) {
  const offset2 = `offset${dimension[0].toUpperCase()}${dimension.slice(1)}`;
  const value = elem[offset2];
  const margins = MARGINS[dimension];
  return value + // @ts-expect-error TODO
  parseInt(style(elem, margins[0]), 10) + // @ts-expect-error TODO
  parseInt(style(elem, margins[1]), 10);
}
const collapseStyles = {
  [EXITED]: "collapse",
  [EXITING]: "collapsing",
  [ENTERING]: "collapsing",
  [ENTERED]: "collapse show"
};
const Collapse = /* @__PURE__ */ reactExports.forwardRef(({
  onEnter,
  onEntering,
  onEntered,
  onExit,
  onExiting,
  className,
  children,
  dimension = "height",
  in: inProp = false,
  timeout = 300,
  mountOnEnter = false,
  unmountOnExit = false,
  appear = false,
  getDimensionValue = getDefaultDimensionValue,
  ...props
}, ref) => {
  const computedDimension = typeof dimension === "function" ? dimension() : dimension;
  const handleEnter = reactExports.useMemo(() => createChainedFunction((elem) => {
    elem.style[computedDimension] = "0";
  }, onEnter), [computedDimension, onEnter]);
  const handleEntering = reactExports.useMemo(() => createChainedFunction((elem) => {
    const scroll = `scroll${computedDimension[0].toUpperCase()}${computedDimension.slice(1)}`;
    elem.style[computedDimension] = `${elem[scroll]}px`;
  }, onEntering), [computedDimension, onEntering]);
  const handleEntered = reactExports.useMemo(() => createChainedFunction((elem) => {
    elem.style[computedDimension] = null;
  }, onEntered), [computedDimension, onEntered]);
  const handleExit = reactExports.useMemo(() => createChainedFunction((elem) => {
    elem.style[computedDimension] = `${getDimensionValue(computedDimension, elem)}px`;
    triggerBrowserReflow(elem);
  }, onExit), [onExit, getDimensionValue, computedDimension]);
  const handleExiting = reactExports.useMemo(() => createChainedFunction((elem) => {
    elem.style[computedDimension] = null;
  }, onExiting), [computedDimension, onExiting]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TransitionWrapper, {
    ref,
    addEndListener: transitionEndListener,
    ...props,
    "aria-expanded": props.role ? inProp : null,
    onEnter: handleEnter,
    onEntering: handleEntering,
    onEntered: handleEntered,
    onExit: handleExit,
    onExiting: handleExiting,
    childRef: getChildRef(children),
    in: inProp,
    timeout,
    mountOnEnter,
    unmountOnExit,
    appear,
    children: (state, innerProps) => /* @__PURE__ */ reactExports.cloneElement(children, {
      ...innerProps,
      className: classnamesExports(className, children.props.className, collapseStyles[state], computedDimension === "width" && "collapse-horizontal")
    })
  });
});
Collapse.displayName = "Collapse";
function useCommittedRef$1(value) {
  const ref = reactExports.useRef(value);
  reactExports.useEffect(() => {
    ref.current = value;
  }, [value]);
  return ref;
}
function useEventCallback$1(fn3) {
  const ref = useCommittedRef$1(fn3);
  return reactExports.useCallback(function(...args) {
    return ref.current && ref.current(...args);
  }, [ref]);
}
const divWithClassName = ((className) => (
  // eslint-disable-next-line react/display-name
  /* @__PURE__ */ reactExports.forwardRef((p, ref) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
    ...p,
    ref,
    className: classnamesExports(p.className, className)
  }))
));
function useCommittedRef(value) {
  const ref = reactExports.useRef(value);
  reactExports.useEffect(() => {
    ref.current = value;
  }, [value]);
  return ref;
}
function useEventCallback(fn3) {
  const ref = useCommittedRef(fn3);
  return reactExports.useCallback(function(...args) {
    return ref.current && ref.current(...args);
  }, [ref]);
}
function useMounted$1() {
  const mounted = reactExports.useRef(true);
  const isMounted = reactExports.useRef(() => mounted.current);
  reactExports.useEffect(() => {
    mounted.current = true;
    return () => {
      mounted.current = false;
    };
  }, []);
  return isMounted.current;
}
function usePrevious$1(value) {
  const ref = reactExports.useRef(null);
  reactExports.useEffect(() => {
    ref.current = value;
  });
  return ref.current;
}
const isReactNative$1 = typeof global !== "undefined" && // @ts-ignore
global.navigator && // @ts-ignore
global.navigator.product === "ReactNative";
const isDOM$1 = typeof document !== "undefined";
const useIsomorphicEffect$1 = isDOM$1 || isReactNative$1 ? reactExports.useLayoutEffect : reactExports.useEffect;
const _excluded$e = ["as", "disabled"];
function _objectWithoutPropertiesLoose$6(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
function isTrivialHref$1(href) {
  return !href || href.trim() === "#";
}
function useButtonProps({
  tagName,
  disabled,
  href,
  target,
  rel,
  role,
  onClick,
  tabIndex = 0,
  type
}) {
  if (!tagName) {
    if (href != null || target != null || rel != null) {
      tagName = "a";
    } else {
      tagName = "button";
    }
  }
  const meta = {
    tagName
  };
  if (tagName === "button") {
    return [{
      type: type || "button",
      disabled
    }, meta];
  }
  const handleClick = (event) => {
    if (disabled || tagName === "a" && isTrivialHref$1(href)) {
      event.preventDefault();
    }
    if (disabled) {
      event.stopPropagation();
      return;
    }
    onClick == null ? void 0 : onClick(event);
  };
  const handleKeyDown = (event) => {
    if (event.key === " ") {
      event.preventDefault();
      handleClick(event);
    }
  };
  if (tagName === "a") {
    href || (href = "#");
    if (disabled) {
      href = void 0;
    }
  }
  return [{
    role: role != null ? role : "button",
    // explicitly undefined so that it overrides the props disabled in a spread
    // e.g. <Tag {...props} {...hookProps} />
    disabled: void 0,
    tabIndex: disabled ? void 0 : tabIndex,
    href,
    target: tagName === "a" ? target : void 0,
    "aria-disabled": !disabled ? void 0 : disabled,
    rel: tagName === "a" ? rel : void 0,
    onClick: handleClick,
    onKeyDown: handleKeyDown
  }, meta];
}
const Button$1 = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    as: asProp,
    disabled
  } = _ref, props = _objectWithoutPropertiesLoose$6(_ref, _excluded$e);
  const [buttonProps, {
    tagName: Component
  }] = useButtonProps(Object.assign({
    tagName: asProp,
    disabled
  }, props));
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, Object.assign({}, props, buttonProps, {
    ref
  }));
});
Button$1.displayName = "Button";
const _excluded$d = ["onKeyDown"];
function _objectWithoutPropertiesLoose$5(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
function isTrivialHref(href) {
  return !href || href.trim() === "#";
}
const Anchor = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    onKeyDown
  } = _ref, props = _objectWithoutPropertiesLoose$5(_ref, _excluded$d);
  const [buttonProps] = useButtonProps(Object.assign({
    tagName: "a"
  }, props));
  const handleKeyDown = useEventCallback((e2) => {
    buttonProps.onKeyDown(e2);
    onKeyDown == null ? void 0 : onKeyDown(e2);
  });
  if (isTrivialHref(props.href) || props.role === "button") {
    return /* @__PURE__ */ jsxRuntimeExports.jsx("a", Object.assign({
      ref
    }, props, buttonProps, {
      onKeyDown: handleKeyDown
    }));
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx("a", Object.assign({
    ref
  }, props, {
    onKeyDown
  }));
});
Anchor.displayName = "Anchor";
const fadeStyles$1 = {
  [ENTERING]: "show",
  [ENTERED]: "show"
};
const Fade = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  children,
  transitionClasses = {},
  onEnter,
  ...rest
}, ref) => {
  const props = {
    in: false,
    timeout: 300,
    mountOnEnter: false,
    unmountOnExit: false,
    appear: false,
    ...rest
  };
  const handleEnter = reactExports.useCallback((node, isAppearing) => {
    triggerBrowserReflow(node);
    onEnter == null || onEnter(node, isAppearing);
  }, [onEnter]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TransitionWrapper, {
    ref,
    addEndListener: transitionEndListener,
    ...props,
    onEnter: handleEnter,
    childRef: getChildRef(children),
    children: (status, innerProps) => /* @__PURE__ */ reactExports.cloneElement(children, {
      ...innerProps,
      className: classnamesExports("fade", className, children.props.className, fadeStyles$1[status], transitionClasses[status])
    })
  });
});
Fade.displayName = "Fade";
const propTypes$1 = {
  /** An accessible label indicating the relevant information about the Close Button. */
  "aria-label": propTypesExports.string,
  /** A callback fired after the Close Button is clicked. */
  onClick: propTypesExports.func,
  /**
   * Render different color variant for the button.
   *
   * Omitting this will render the default dark color.
   */
  variant: propTypesExports.oneOf(["white"])
};
const CloseButton = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  variant,
  "aria-label": ariaLabel = "Close",
  ...props
}, ref) => /* @__PURE__ */ jsxRuntimeExports.jsx("button", {
  ref,
  type: "button",
  className: classnamesExports("btn-close", variant && `btn-close-${variant}`, className),
  "aria-label": ariaLabel,
  ...props
}));
CloseButton.displayName = "CloseButton";
CloseButton.propTypes = propTypes$1;
const Button = /* @__PURE__ */ reactExports.forwardRef(({
  as,
  bsPrefix,
  variant = "primary",
  size: size2,
  active = false,
  disabled = false,
  className,
  ...props
}, ref) => {
  const prefix = useBootstrapPrefix(bsPrefix, "btn");
  const [buttonProps, {
    tagName
  }] = useButtonProps({
    tagName: as,
    disabled,
    ...props
  });
  const Component = tagName;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ...buttonProps,
    ...props,
    ref,
    disabled,
    className: classnamesExports(className, prefix, active && "active", variant && `${prefix}-${variant}`, size2 && `${prefix}-${size2}`, props.href && disabled && "disabled")
  });
});
Button.displayName = "Button";
const context$1 = /* @__PURE__ */ reactExports.createContext(null);
context$1.displayName = "CardHeaderContext";
function useMounted() {
  const mounted = reactExports.useRef(true);
  const isMounted = reactExports.useRef(() => mounted.current);
  reactExports.useEffect(() => {
    mounted.current = true;
    return () => {
      mounted.current = false;
    };
  }, []);
  return isMounted.current;
}
function useUpdatedRef$1(value) {
  const valueRef = reactExports.useRef(value);
  valueRef.current = value;
  return valueRef;
}
function useWillUnmount$1(fn3) {
  const onUnmount = useUpdatedRef$1(fn3);
  reactExports.useEffect(() => () => onUnmount.current(), []);
}
const MAX_DELAY_MS = 2 ** 31 - 1;
function setChainedTimeout(handleRef, fn3, timeoutAtMs) {
  const delayMs = timeoutAtMs - Date.now();
  handleRef.current = delayMs <= MAX_DELAY_MS ? setTimeout(fn3, delayMs) : setTimeout(() => setChainedTimeout(handleRef, fn3, timeoutAtMs), MAX_DELAY_MS);
}
function useTimeout() {
  const isMounted = useMounted();
  const handleRef = reactExports.useRef();
  useWillUnmount$1(() => clearTimeout(handleRef.current));
  return reactExports.useMemo(() => {
    const clear2 = () => clearTimeout(handleRef.current);
    function set(fn3, delayMs = 0) {
      if (!isMounted()) return;
      clear2();
      if (delayMs <= MAX_DELAY_MS) {
        handleRef.current = setTimeout(fn3, delayMs);
      } else {
        setChainedTimeout(handleRef, fn3, Date.now() + delayMs);
      }
    }
    return {
      set,
      clear: clear2,
      handleRef
    };
  }, []);
}
const Container = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  fluid = false,
  // Need to define the default "as" during prop destructuring to be compatible with styled-components github.com/react-bootstrap/react-bootstrap/issues/3595
  as: Component = "div",
  className,
  ...props
}, ref) => {
  const prefix = useBootstrapPrefix(bsPrefix, "container");
  const suffix = typeof fluid === "string" ? `-${fluid}` : "-fluid";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    ...props,
    className: classnamesExports(className, fluid ? `${prefix}${suffix}` : prefix)
  });
});
Container.displayName = "Container";
var toArray$1 = Function.prototype.bind.call(Function.prototype.call, [].slice);
function qsa(element, selector) {
  return toArray$1(element.querySelectorAll(selector));
}
function useForceUpdate() {
  const [, dispatch] = reactExports.useReducer((revision) => revision + 1, 0);
  return dispatch;
}
var top = "top";
var bottom = "bottom";
var right = "right";
var left = "left";
var auto = "auto";
var basePlacements = [top, bottom, right, left];
var start = "start";
var end = "end";
var clippingParents = "clippingParents";
var viewport = "viewport";
var popper = "popper";
var reference = "reference";
var variationPlacements = /* @__PURE__ */ basePlacements.reduce(function(acc, placement) {
  return acc.concat([placement + "-" + start, placement + "-" + end]);
}, []);
var placements = /* @__PURE__ */ [].concat(basePlacements, [auto]).reduce(function(acc, placement) {
  return acc.concat([placement, placement + "-" + start, placement + "-" + end]);
}, []);
var beforeRead = "beforeRead";
var read = "read";
var afterRead = "afterRead";
var beforeMain = "beforeMain";
var main$5 = "main";
var afterMain = "afterMain";
var beforeWrite = "beforeWrite";
var write = "write";
var afterWrite = "afterWrite";
var modifierPhases = [beforeRead, read, afterRead, beforeMain, main$5, afterMain, beforeWrite, write, afterWrite];
function getBasePlacement(placement) {
  return placement.split("-")[0];
}
function getWindow$1(node) {
  if (node == null) {
    return window;
  }
  if (node.toString() !== "[object Window]") {
    var ownerDocument2 = node.ownerDocument;
    return ownerDocument2 ? ownerDocument2.defaultView || window : window;
  }
  return node;
}
function isElement(node) {
  var OwnElement = getWindow$1(node).Element;
  return node instanceof OwnElement || node instanceof Element;
}
function isHTMLElement$2(node) {
  var OwnElement = getWindow$1(node).HTMLElement;
  return node instanceof OwnElement || node instanceof HTMLElement;
}
function isShadowRoot(node) {
  if (typeof ShadowRoot === "undefined") {
    return false;
  }
  var OwnElement = getWindow$1(node).ShadowRoot;
  return node instanceof OwnElement || node instanceof ShadowRoot;
}
var max$1 = Math.max;
var min$1 = Math.min;
var round = Math.round;
function getUAString() {
  var uaData = navigator.userAgentData;
  if (uaData != null && uaData.brands && Array.isArray(uaData.brands)) {
    return uaData.brands.map(function(item) {
      return item.brand + "/" + item.version;
    }).join(" ");
  }
  return navigator.userAgent;
}
function isLayoutViewport() {
  return !/^((?!chrome|android).)*safari/i.test(getUAString());
}
function getBoundingClientRect(element, includeScale, isFixedStrategy) {
  if (includeScale === void 0) {
    includeScale = false;
  }
  if (isFixedStrategy === void 0) {
    isFixedStrategy = false;
  }
  var clientRect = element.getBoundingClientRect();
  var scaleX = 1;
  var scaleY = 1;
  if (includeScale && isHTMLElement$2(element)) {
    scaleX = element.offsetWidth > 0 ? round(clientRect.width) / element.offsetWidth || 1 : 1;
    scaleY = element.offsetHeight > 0 ? round(clientRect.height) / element.offsetHeight || 1 : 1;
  }
  var _ref = isElement(element) ? getWindow$1(element) : window, visualViewport = _ref.visualViewport;
  var addVisualOffsets = !isLayoutViewport() && isFixedStrategy;
  var x2 = (clientRect.left + (addVisualOffsets && visualViewport ? visualViewport.offsetLeft : 0)) / scaleX;
  var y3 = (clientRect.top + (addVisualOffsets && visualViewport ? visualViewport.offsetTop : 0)) / scaleY;
  var width = clientRect.width / scaleX;
  var height2 = clientRect.height / scaleY;
  return {
    width,
    height: height2,
    top: y3,
    right: x2 + width,
    bottom: y3 + height2,
    left: x2,
    x: x2,
    y: y3
  };
}
function getLayoutRect(element) {
  var clientRect = getBoundingClientRect(element);
  var width = element.offsetWidth;
  var height2 = element.offsetHeight;
  if (Math.abs(clientRect.width - width) <= 1) {
    width = clientRect.width;
  }
  if (Math.abs(clientRect.height - height2) <= 1) {
    height2 = clientRect.height;
  }
  return {
    x: element.offsetLeft,
    y: element.offsetTop,
    width,
    height: height2
  };
}
function contains$1(parent, child) {
  var rootNode = child.getRootNode && child.getRootNode();
  if (parent.contains(child)) {
    return true;
  } else if (rootNode && isShadowRoot(rootNode)) {
    var next = child;
    do {
      if (next && parent.isSameNode(next)) {
        return true;
      }
      next = next.parentNode || next.host;
    } while (next);
  }
  return false;
}
function getNodeName(element) {
  return element ? (element.nodeName || "").toLowerCase() : null;
}
function getComputedStyle(element) {
  return getWindow$1(element).getComputedStyle(element);
}
function isTableElement(element) {
  return ["table", "td", "th"].indexOf(getNodeName(element)) >= 0;
}
function getDocumentElement(element) {
  return ((isElement(element) ? element.ownerDocument : (
    // $FlowFixMe[prop-missing]
    element.document
  )) || window.document).documentElement;
}
function getParentNode(element) {
  if (getNodeName(element) === "html") {
    return element;
  }
  return (
    // this is a quicker (but less type safe) way to save quite some bytes from the bundle
    // $FlowFixMe[incompatible-return]
    // $FlowFixMe[prop-missing]
    element.assignedSlot || // step into the shadow DOM of the parent of a slotted node
    element.parentNode || // DOM Element detected
    (isShadowRoot(element) ? element.host : null) || // ShadowRoot detected
    // $FlowFixMe[incompatible-call]: HTMLElement is a Node
    getDocumentElement(element)
  );
}
function getTrueOffsetParent(element) {
  if (!isHTMLElement$2(element) || // https://github.com/popperjs/popper-core/issues/837
  getComputedStyle(element).position === "fixed") {
    return null;
  }
  return element.offsetParent;
}
function getContainingBlock(element) {
  var isFirefox = /firefox/i.test(getUAString());
  var isIE = /Trident/i.test(getUAString());
  if (isIE && isHTMLElement$2(element)) {
    var elementCss = getComputedStyle(element);
    if (elementCss.position === "fixed") {
      return null;
    }
  }
  var currentNode = getParentNode(element);
  if (isShadowRoot(currentNode)) {
    currentNode = currentNode.host;
  }
  while (isHTMLElement$2(currentNode) && ["html", "body"].indexOf(getNodeName(currentNode)) < 0) {
    var css = getComputedStyle(currentNode);
    if (css.transform !== "none" || css.perspective !== "none" || css.contain === "paint" || ["transform", "perspective"].indexOf(css.willChange) !== -1 || isFirefox && css.willChange === "filter" || isFirefox && css.filter && css.filter !== "none") {
      return currentNode;
    } else {
      currentNode = currentNode.parentNode;
    }
  }
  return null;
}
function getOffsetParent(element) {
  var window2 = getWindow$1(element);
  var offsetParent2 = getTrueOffsetParent(element);
  while (offsetParent2 && isTableElement(offsetParent2) && getComputedStyle(offsetParent2).position === "static") {
    offsetParent2 = getTrueOffsetParent(offsetParent2);
  }
  if (offsetParent2 && (getNodeName(offsetParent2) === "html" || getNodeName(offsetParent2) === "body" && getComputedStyle(offsetParent2).position === "static")) {
    return window2;
  }
  return offsetParent2 || getContainingBlock(element) || window2;
}
function getMainAxisFromPlacement(placement) {
  return ["top", "bottom"].indexOf(placement) >= 0 ? "x" : "y";
}
function within(min2, value, max2) {
  return max$1(min2, min$1(value, max2));
}
function withinMaxClamp(min2, value, max2) {
  var v = within(min2, value, max2);
  return v > max2 ? max2 : v;
}
function getFreshSideObject() {
  return {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0
  };
}
function mergePaddingObject(paddingObject) {
  return Object.assign({}, getFreshSideObject(), paddingObject);
}
function expandToHashMap(value, keys) {
  return keys.reduce(function(hashMap, key) {
    hashMap[key] = value;
    return hashMap;
  }, {});
}
var toPaddingObject = function toPaddingObject2(padding, state) {
  padding = typeof padding === "function" ? padding(Object.assign({}, state.rects, {
    placement: state.placement
  })) : padding;
  return mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
};
function arrow(_ref) {
  var _state$modifiersData$;
  var state = _ref.state, name2 = _ref.name, options = _ref.options;
  var arrowElement = state.elements.arrow;
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var basePlacement = getBasePlacement(state.placement);
  var axis = getMainAxisFromPlacement(basePlacement);
  var isVertical = [left, right].indexOf(basePlacement) >= 0;
  var len = isVertical ? "height" : "width";
  if (!arrowElement || !popperOffsets2) {
    return;
  }
  var paddingObject = toPaddingObject(options.padding, state);
  var arrowRect = getLayoutRect(arrowElement);
  var minProp = axis === "y" ? top : left;
  var maxProp = axis === "y" ? bottom : right;
  var endDiff = state.rects.reference[len] + state.rects.reference[axis] - popperOffsets2[axis] - state.rects.popper[len];
  var startDiff = popperOffsets2[axis] - state.rects.reference[axis];
  var arrowOffsetParent = getOffsetParent(arrowElement);
  var clientSize = arrowOffsetParent ? axis === "y" ? arrowOffsetParent.clientHeight || 0 : arrowOffsetParent.clientWidth || 0 : 0;
  var centerToReference = endDiff / 2 - startDiff / 2;
  var min2 = paddingObject[minProp];
  var max2 = clientSize - arrowRect[len] - paddingObject[maxProp];
  var center = clientSize / 2 - arrowRect[len] / 2 + centerToReference;
  var offset2 = within(min2, center, max2);
  var axisProp = axis;
  state.modifiersData[name2] = (_state$modifiersData$ = {}, _state$modifiersData$[axisProp] = offset2, _state$modifiersData$.centerOffset = offset2 - center, _state$modifiersData$);
}
function effect$1(_ref2) {
  var state = _ref2.state, options = _ref2.options;
  var _options$element = options.element, arrowElement = _options$element === void 0 ? "[data-popper-arrow]" : _options$element;
  if (arrowElement == null) {
    return;
  }
  if (typeof arrowElement === "string") {
    arrowElement = state.elements.popper.querySelector(arrowElement);
    if (!arrowElement) {
      return;
    }
  }
  if (!contains$1(state.elements.popper, arrowElement)) {
    return;
  }
  state.elements.arrow = arrowElement;
}
const arrow$1 = {
  name: "arrow",
  enabled: true,
  phase: "main",
  fn: arrow,
  effect: effect$1,
  requires: ["popperOffsets"],
  requiresIfExists: ["preventOverflow"]
};
function getVariation(placement) {
  return placement.split("-")[1];
}
var unsetSides = {
  top: "auto",
  right: "auto",
  bottom: "auto",
  left: "auto"
};
function roundOffsetsByDPR(_ref, win) {
  var x2 = _ref.x, y3 = _ref.y;
  var dpr = win.devicePixelRatio || 1;
  return {
    x: round(x2 * dpr) / dpr || 0,
    y: round(y3 * dpr) / dpr || 0
  };
}
function mapToStyles(_ref2) {
  var _Object$assign2;
  var popper2 = _ref2.popper, popperRect = _ref2.popperRect, placement = _ref2.placement, variation = _ref2.variation, offsets = _ref2.offsets, position2 = _ref2.position, gpuAcceleration = _ref2.gpuAcceleration, adaptive = _ref2.adaptive, roundOffsets = _ref2.roundOffsets, isFixed2 = _ref2.isFixed;
  var _offsets$x = offsets.x, x2 = _offsets$x === void 0 ? 0 : _offsets$x, _offsets$y = offsets.y, y3 = _offsets$y === void 0 ? 0 : _offsets$y;
  var _ref3 = typeof roundOffsets === "function" ? roundOffsets({
    x: x2,
    y: y3
  }) : {
    x: x2,
    y: y3
  };
  x2 = _ref3.x;
  y3 = _ref3.y;
  var hasX = offsets.hasOwnProperty("x");
  var hasY = offsets.hasOwnProperty("y");
  var sideX = left;
  var sideY = top;
  var win = window;
  if (adaptive) {
    var offsetParent2 = getOffsetParent(popper2);
    var heightProp = "clientHeight";
    var widthProp = "clientWidth";
    if (offsetParent2 === getWindow$1(popper2)) {
      offsetParent2 = getDocumentElement(popper2);
      if (getComputedStyle(offsetParent2).position !== "static" && position2 === "absolute") {
        heightProp = "scrollHeight";
        widthProp = "scrollWidth";
      }
    }
    offsetParent2 = offsetParent2;
    if (placement === top || (placement === left || placement === right) && variation === end) {
      sideY = bottom;
      var offsetY = isFixed2 && offsetParent2 === win && win.visualViewport ? win.visualViewport.height : (
        // $FlowFixMe[prop-missing]
        offsetParent2[heightProp]
      );
      y3 -= offsetY - popperRect.height;
      y3 *= gpuAcceleration ? 1 : -1;
    }
    if (placement === left || (placement === top || placement === bottom) && variation === end) {
      sideX = right;
      var offsetX = isFixed2 && offsetParent2 === win && win.visualViewport ? win.visualViewport.width : (
        // $FlowFixMe[prop-missing]
        offsetParent2[widthProp]
      );
      x2 -= offsetX - popperRect.width;
      x2 *= gpuAcceleration ? 1 : -1;
    }
  }
  var commonStyles = Object.assign({
    position: position2
  }, adaptive && unsetSides);
  var _ref4 = roundOffsets === true ? roundOffsetsByDPR({
    x: x2,
    y: y3
  }, getWindow$1(popper2)) : {
    x: x2,
    y: y3
  };
  x2 = _ref4.x;
  y3 = _ref4.y;
  if (gpuAcceleration) {
    var _Object$assign;
    return Object.assign({}, commonStyles, (_Object$assign = {}, _Object$assign[sideY] = hasY ? "0" : "", _Object$assign[sideX] = hasX ? "0" : "", _Object$assign.transform = (win.devicePixelRatio || 1) <= 1 ? "translate(" + x2 + "px, " + y3 + "px)" : "translate3d(" + x2 + "px, " + y3 + "px, 0)", _Object$assign));
  }
  return Object.assign({}, commonStyles, (_Object$assign2 = {}, _Object$assign2[sideY] = hasY ? y3 + "px" : "", _Object$assign2[sideX] = hasX ? x2 + "px" : "", _Object$assign2.transform = "", _Object$assign2));
}
function computeStyles(_ref5) {
  var state = _ref5.state, options = _ref5.options;
  var _options$gpuAccelerat = options.gpuAcceleration, gpuAcceleration = _options$gpuAccelerat === void 0 ? true : _options$gpuAccelerat, _options$adaptive = options.adaptive, adaptive = _options$adaptive === void 0 ? true : _options$adaptive, _options$roundOffsets = options.roundOffsets, roundOffsets = _options$roundOffsets === void 0 ? true : _options$roundOffsets;
  var commonStyles = {
    placement: getBasePlacement(state.placement),
    variation: getVariation(state.placement),
    popper: state.elements.popper,
    popperRect: state.rects.popper,
    gpuAcceleration,
    isFixed: state.options.strategy === "fixed"
  };
  if (state.modifiersData.popperOffsets != null) {
    state.styles.popper = Object.assign({}, state.styles.popper, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.popperOffsets,
      position: state.options.strategy,
      adaptive,
      roundOffsets
    })));
  }
  if (state.modifiersData.arrow != null) {
    state.styles.arrow = Object.assign({}, state.styles.arrow, mapToStyles(Object.assign({}, commonStyles, {
      offsets: state.modifiersData.arrow,
      position: "absolute",
      adaptive: false,
      roundOffsets
    })));
  }
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-placement": state.placement
  });
}
const computeStyles$1 = {
  name: "computeStyles",
  enabled: true,
  phase: "beforeWrite",
  fn: computeStyles,
  data: {}
};
var passive = {
  passive: true
};
function effect(_ref) {
  var state = _ref.state, instance = _ref.instance, options = _ref.options;
  var _options$scroll = options.scroll, scroll = _options$scroll === void 0 ? true : _options$scroll, _options$resize = options.resize, resize = _options$resize === void 0 ? true : _options$resize;
  var window2 = getWindow$1(state.elements.popper);
  var scrollParents = [].concat(state.scrollParents.reference, state.scrollParents.popper);
  if (scroll) {
    scrollParents.forEach(function(scrollParent2) {
      scrollParent2.addEventListener("scroll", instance.update, passive);
    });
  }
  if (resize) {
    window2.addEventListener("resize", instance.update, passive);
  }
  return function() {
    if (scroll) {
      scrollParents.forEach(function(scrollParent2) {
        scrollParent2.removeEventListener("scroll", instance.update, passive);
      });
    }
    if (resize) {
      window2.removeEventListener("resize", instance.update, passive);
    }
  };
}
const eventListeners = {
  name: "eventListeners",
  enabled: true,
  phase: "write",
  fn: function fn() {
  },
  effect,
  data: {}
};
var hash$1 = {
  left: "right",
  right: "left",
  bottom: "top",
  top: "bottom"
};
function getOppositePlacement(placement) {
  return placement.replace(/left|right|bottom|top/g, function(matched) {
    return hash$1[matched];
  });
}
var hash = {
  start: "end",
  end: "start"
};
function getOppositeVariationPlacement(placement) {
  return placement.replace(/start|end/g, function(matched) {
    return hash[matched];
  });
}
function getWindowScroll(node) {
  var win = getWindow$1(node);
  var scrollLeft2 = win.pageXOffset;
  var scrollTop2 = win.pageYOffset;
  return {
    scrollLeft: scrollLeft2,
    scrollTop: scrollTop2
  };
}
function getWindowScrollBarX(element) {
  return getBoundingClientRect(getDocumentElement(element)).left + getWindowScroll(element).scrollLeft;
}
function getViewportRect(element, strategy) {
  var win = getWindow$1(element);
  var html = getDocumentElement(element);
  var visualViewport = win.visualViewport;
  var width = html.clientWidth;
  var height2 = html.clientHeight;
  var x2 = 0;
  var y3 = 0;
  if (visualViewport) {
    width = visualViewport.width;
    height2 = visualViewport.height;
    var layoutViewport = isLayoutViewport();
    if (layoutViewport || !layoutViewport && strategy === "fixed") {
      x2 = visualViewport.offsetLeft;
      y3 = visualViewport.offsetTop;
    }
  }
  return {
    width,
    height: height2,
    x: x2 + getWindowScrollBarX(element),
    y: y3
  };
}
function getDocumentRect(element) {
  var _element$ownerDocumen;
  var html = getDocumentElement(element);
  var winScroll = getWindowScroll(element);
  var body = (_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body;
  var width = max$1(html.scrollWidth, html.clientWidth, body ? body.scrollWidth : 0, body ? body.clientWidth : 0);
  var height2 = max$1(html.scrollHeight, html.clientHeight, body ? body.scrollHeight : 0, body ? body.clientHeight : 0);
  var x2 = -winScroll.scrollLeft + getWindowScrollBarX(element);
  var y3 = -winScroll.scrollTop;
  if (getComputedStyle(body || html).direction === "rtl") {
    x2 += max$1(html.clientWidth, body ? body.clientWidth : 0) - width;
  }
  return {
    width,
    height: height2,
    x: x2,
    y: y3
  };
}
function isScrollParent(element) {
  var _getComputedStyle = getComputedStyle(element), overflow = _getComputedStyle.overflow, overflowX = _getComputedStyle.overflowX, overflowY = _getComputedStyle.overflowY;
  return /auto|scroll|overlay|hidden/.test(overflow + overflowY + overflowX);
}
function getScrollParent(node) {
  if (["html", "body", "#document"].indexOf(getNodeName(node)) >= 0) {
    return node.ownerDocument.body;
  }
  if (isHTMLElement$2(node) && isScrollParent(node)) {
    return node;
  }
  return getScrollParent(getParentNode(node));
}
function listScrollParents(element, list) {
  var _element$ownerDocumen;
  if (list === void 0) {
    list = [];
  }
  var scrollParent2 = getScrollParent(element);
  var isBody = scrollParent2 === ((_element$ownerDocumen = element.ownerDocument) == null ? void 0 : _element$ownerDocumen.body);
  var win = getWindow$1(scrollParent2);
  var target = isBody ? [win].concat(win.visualViewport || [], isScrollParent(scrollParent2) ? scrollParent2 : []) : scrollParent2;
  var updatedList = list.concat(target);
  return isBody ? updatedList : (
    // $FlowFixMe[incompatible-call]: isBody tells us target will be an HTMLElement here
    updatedList.concat(listScrollParents(getParentNode(target)))
  );
}
function rectToClientRect(rect) {
  return Object.assign({}, rect, {
    left: rect.x,
    top: rect.y,
    right: rect.x + rect.width,
    bottom: rect.y + rect.height
  });
}
function getInnerBoundingClientRect(element, strategy) {
  var rect = getBoundingClientRect(element, false, strategy === "fixed");
  rect.top = rect.top + element.clientTop;
  rect.left = rect.left + element.clientLeft;
  rect.bottom = rect.top + element.clientHeight;
  rect.right = rect.left + element.clientWidth;
  rect.width = element.clientWidth;
  rect.height = element.clientHeight;
  rect.x = rect.left;
  rect.y = rect.top;
  return rect;
}
function getClientRectFromMixedType(element, clippingParent, strategy) {
  return clippingParent === viewport ? rectToClientRect(getViewportRect(element, strategy)) : isElement(clippingParent) ? getInnerBoundingClientRect(clippingParent, strategy) : rectToClientRect(getDocumentRect(getDocumentElement(element)));
}
function getClippingParents(element) {
  var clippingParents2 = listScrollParents(getParentNode(element));
  var canEscapeClipping = ["absolute", "fixed"].indexOf(getComputedStyle(element).position) >= 0;
  var clipperElement = canEscapeClipping && isHTMLElement$2(element) ? getOffsetParent(element) : element;
  if (!isElement(clipperElement)) {
    return [];
  }
  return clippingParents2.filter(function(clippingParent) {
    return isElement(clippingParent) && contains$1(clippingParent, clipperElement) && getNodeName(clippingParent) !== "body";
  });
}
function getClippingRect(element, boundary, rootBoundary, strategy) {
  var mainClippingParents = boundary === "clippingParents" ? getClippingParents(element) : [].concat(boundary);
  var clippingParents2 = [].concat(mainClippingParents, [rootBoundary]);
  var firstClippingParent = clippingParents2[0];
  var clippingRect = clippingParents2.reduce(function(accRect, clippingParent) {
    var rect = getClientRectFromMixedType(element, clippingParent, strategy);
    accRect.top = max$1(rect.top, accRect.top);
    accRect.right = min$1(rect.right, accRect.right);
    accRect.bottom = min$1(rect.bottom, accRect.bottom);
    accRect.left = max$1(rect.left, accRect.left);
    return accRect;
  }, getClientRectFromMixedType(element, firstClippingParent, strategy));
  clippingRect.width = clippingRect.right - clippingRect.left;
  clippingRect.height = clippingRect.bottom - clippingRect.top;
  clippingRect.x = clippingRect.left;
  clippingRect.y = clippingRect.top;
  return clippingRect;
}
function computeOffsets(_ref) {
  var reference2 = _ref.reference, element = _ref.element, placement = _ref.placement;
  var basePlacement = placement ? getBasePlacement(placement) : null;
  var variation = placement ? getVariation(placement) : null;
  var commonX = reference2.x + reference2.width / 2 - element.width / 2;
  var commonY = reference2.y + reference2.height / 2 - element.height / 2;
  var offsets;
  switch (basePlacement) {
    case top:
      offsets = {
        x: commonX,
        y: reference2.y - element.height
      };
      break;
    case bottom:
      offsets = {
        x: commonX,
        y: reference2.y + reference2.height
      };
      break;
    case right:
      offsets = {
        x: reference2.x + reference2.width,
        y: commonY
      };
      break;
    case left:
      offsets = {
        x: reference2.x - element.width,
        y: commonY
      };
      break;
    default:
      offsets = {
        x: reference2.x,
        y: reference2.y
      };
  }
  var mainAxis = basePlacement ? getMainAxisFromPlacement(basePlacement) : null;
  if (mainAxis != null) {
    var len = mainAxis === "y" ? "height" : "width";
    switch (variation) {
      case start:
        offsets[mainAxis] = offsets[mainAxis] - (reference2[len] / 2 - element[len] / 2);
        break;
      case end:
        offsets[mainAxis] = offsets[mainAxis] + (reference2[len] / 2 - element[len] / 2);
        break;
    }
  }
  return offsets;
}
function detectOverflow(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, _options$placement = _options.placement, placement = _options$placement === void 0 ? state.placement : _options$placement, _options$strategy = _options.strategy, strategy = _options$strategy === void 0 ? state.strategy : _options$strategy, _options$boundary = _options.boundary, boundary = _options$boundary === void 0 ? clippingParents : _options$boundary, _options$rootBoundary = _options.rootBoundary, rootBoundary = _options$rootBoundary === void 0 ? viewport : _options$rootBoundary, _options$elementConte = _options.elementContext, elementContext = _options$elementConte === void 0 ? popper : _options$elementConte, _options$altBoundary = _options.altBoundary, altBoundary = _options$altBoundary === void 0 ? false : _options$altBoundary, _options$padding = _options.padding, padding = _options$padding === void 0 ? 0 : _options$padding;
  var paddingObject = mergePaddingObject(typeof padding !== "number" ? padding : expandToHashMap(padding, basePlacements));
  var altContext = elementContext === popper ? reference : popper;
  var popperRect = state.rects.popper;
  var element = state.elements[altBoundary ? altContext : elementContext];
  var clippingClientRect = getClippingRect(isElement(element) ? element : element.contextElement || getDocumentElement(state.elements.popper), boundary, rootBoundary, strategy);
  var referenceClientRect = getBoundingClientRect(state.elements.reference);
  var popperOffsets2 = computeOffsets({
    reference: referenceClientRect,
    element: popperRect,
    placement
  });
  var popperClientRect = rectToClientRect(Object.assign({}, popperRect, popperOffsets2));
  var elementClientRect = elementContext === popper ? popperClientRect : referenceClientRect;
  var overflowOffsets = {
    top: clippingClientRect.top - elementClientRect.top + paddingObject.top,
    bottom: elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom,
    left: clippingClientRect.left - elementClientRect.left + paddingObject.left,
    right: elementClientRect.right - clippingClientRect.right + paddingObject.right
  };
  var offsetData = state.modifiersData.offset;
  if (elementContext === popper && offsetData) {
    var offset2 = offsetData[placement];
    Object.keys(overflowOffsets).forEach(function(key) {
      var multiply = [right, bottom].indexOf(key) >= 0 ? 1 : -1;
      var axis = [top, bottom].indexOf(key) >= 0 ? "y" : "x";
      overflowOffsets[key] += offset2[axis] * multiply;
    });
  }
  return overflowOffsets;
}
function computeAutoPlacement(state, options) {
  if (options === void 0) {
    options = {};
  }
  var _options = options, placement = _options.placement, boundary = _options.boundary, rootBoundary = _options.rootBoundary, padding = _options.padding, flipVariations = _options.flipVariations, _options$allowedAutoP = _options.allowedAutoPlacements, allowedAutoPlacements = _options$allowedAutoP === void 0 ? placements : _options$allowedAutoP;
  var variation = getVariation(placement);
  var placements$1 = variation ? flipVariations ? variationPlacements : variationPlacements.filter(function(placement2) {
    return getVariation(placement2) === variation;
  }) : basePlacements;
  var allowedPlacements = placements$1.filter(function(placement2) {
    return allowedAutoPlacements.indexOf(placement2) >= 0;
  });
  if (allowedPlacements.length === 0) {
    allowedPlacements = placements$1;
  }
  var overflows = allowedPlacements.reduce(function(acc, placement2) {
    acc[placement2] = detectOverflow(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding
    })[getBasePlacement(placement2)];
    return acc;
  }, {});
  return Object.keys(overflows).sort(function(a3, b2) {
    return overflows[a3] - overflows[b2];
  });
}
function getExpandedFallbackPlacements(placement) {
  if (getBasePlacement(placement) === auto) {
    return [];
  }
  var oppositePlacement = getOppositePlacement(placement);
  return [getOppositeVariationPlacement(placement), oppositePlacement, getOppositeVariationPlacement(oppositePlacement)];
}
function flip(_ref) {
  var state = _ref.state, options = _ref.options, name2 = _ref.name;
  if (state.modifiersData[name2]._skip) {
    return;
  }
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? true : _options$altAxis, specifiedFallbackPlacements = options.fallbackPlacements, padding = options.padding, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, _options$flipVariatio = options.flipVariations, flipVariations = _options$flipVariatio === void 0 ? true : _options$flipVariatio, allowedAutoPlacements = options.allowedAutoPlacements;
  var preferredPlacement = state.options.placement;
  var basePlacement = getBasePlacement(preferredPlacement);
  var isBasePlacement = basePlacement === preferredPlacement;
  var fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipVariations ? [getOppositePlacement(preferredPlacement)] : getExpandedFallbackPlacements(preferredPlacement));
  var placements2 = [preferredPlacement].concat(fallbackPlacements).reduce(function(acc, placement2) {
    return acc.concat(getBasePlacement(placement2) === auto ? computeAutoPlacement(state, {
      placement: placement2,
      boundary,
      rootBoundary,
      padding,
      flipVariations,
      allowedAutoPlacements
    }) : placement2);
  }, []);
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var checksMap = /* @__PURE__ */ new Map();
  var makeFallbackChecks = true;
  var firstFittingPlacement = placements2[0];
  for (var i2 = 0; i2 < placements2.length; i2++) {
    var placement = placements2[i2];
    var _basePlacement = getBasePlacement(placement);
    var isStartVariation = getVariation(placement) === start;
    var isVertical = [top, bottom].indexOf(_basePlacement) >= 0;
    var len = isVertical ? "width" : "height";
    var overflow = detectOverflow(state, {
      placement,
      boundary,
      rootBoundary,
      altBoundary,
      padding
    });
    var mainVariationSide = isVertical ? isStartVariation ? right : left : isStartVariation ? bottom : top;
    if (referenceRect[len] > popperRect[len]) {
      mainVariationSide = getOppositePlacement(mainVariationSide);
    }
    var altVariationSide = getOppositePlacement(mainVariationSide);
    var checks = [];
    if (checkMainAxis) {
      checks.push(overflow[_basePlacement] <= 0);
    }
    if (checkAltAxis) {
      checks.push(overflow[mainVariationSide] <= 0, overflow[altVariationSide] <= 0);
    }
    if (checks.every(function(check) {
      return check;
    })) {
      firstFittingPlacement = placement;
      makeFallbackChecks = false;
      break;
    }
    checksMap.set(placement, checks);
  }
  if (makeFallbackChecks) {
    var numberOfChecks = flipVariations ? 3 : 1;
    var _loop = function _loop2(_i2) {
      var fittingPlacement = placements2.find(function(placement2) {
        var checks2 = checksMap.get(placement2);
        if (checks2) {
          return checks2.slice(0, _i2).every(function(check) {
            return check;
          });
        }
      });
      if (fittingPlacement) {
        firstFittingPlacement = fittingPlacement;
        return "break";
      }
    };
    for (var _i = numberOfChecks; _i > 0; _i--) {
      var _ret = _loop(_i);
      if (_ret === "break") break;
    }
  }
  if (state.placement !== firstFittingPlacement) {
    state.modifiersData[name2]._skip = true;
    state.placement = firstFittingPlacement;
    state.reset = true;
  }
}
const flip$1 = {
  name: "flip",
  enabled: true,
  phase: "main",
  fn: flip,
  requiresIfExists: ["offset"],
  data: {
    _skip: false
  }
};
function getSideOffsets(overflow, rect, preventedOffsets) {
  if (preventedOffsets === void 0) {
    preventedOffsets = {
      x: 0,
      y: 0
    };
  }
  return {
    top: overflow.top - rect.height - preventedOffsets.y,
    right: overflow.right - rect.width + preventedOffsets.x,
    bottom: overflow.bottom - rect.height + preventedOffsets.y,
    left: overflow.left - rect.width - preventedOffsets.x
  };
}
function isAnySideFullyClipped(overflow) {
  return [top, right, bottom, left].some(function(side) {
    return overflow[side] >= 0;
  });
}
function hide(_ref) {
  var state = _ref.state, name2 = _ref.name;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var preventedOffsets = state.modifiersData.preventOverflow;
  var referenceOverflow = detectOverflow(state, {
    elementContext: "reference"
  });
  var popperAltOverflow = detectOverflow(state, {
    altBoundary: true
  });
  var referenceClippingOffsets = getSideOffsets(referenceOverflow, referenceRect);
  var popperEscapeOffsets = getSideOffsets(popperAltOverflow, popperRect, preventedOffsets);
  var isReferenceHidden = isAnySideFullyClipped(referenceClippingOffsets);
  var hasPopperEscaped = isAnySideFullyClipped(popperEscapeOffsets);
  state.modifiersData[name2] = {
    referenceClippingOffsets,
    popperEscapeOffsets,
    isReferenceHidden,
    hasPopperEscaped
  };
  state.attributes.popper = Object.assign({}, state.attributes.popper, {
    "data-popper-reference-hidden": isReferenceHidden,
    "data-popper-escaped": hasPopperEscaped
  });
}
const hide$1 = {
  name: "hide",
  enabled: true,
  phase: "main",
  requiresIfExists: ["preventOverflow"],
  fn: hide
};
function distanceAndSkiddingToXY(placement, rects, offset2) {
  var basePlacement = getBasePlacement(placement);
  var invertDistance = [left, top].indexOf(basePlacement) >= 0 ? -1 : 1;
  var _ref = typeof offset2 === "function" ? offset2(Object.assign({}, rects, {
    placement
  })) : offset2, skidding = _ref[0], distance = _ref[1];
  skidding = skidding || 0;
  distance = (distance || 0) * invertDistance;
  return [left, right].indexOf(basePlacement) >= 0 ? {
    x: distance,
    y: skidding
  } : {
    x: skidding,
    y: distance
  };
}
function offset$1(_ref2) {
  var state = _ref2.state, options = _ref2.options, name2 = _ref2.name;
  var _options$offset = options.offset, offset2 = _options$offset === void 0 ? [0, 0] : _options$offset;
  var data = placements.reduce(function(acc, placement) {
    acc[placement] = distanceAndSkiddingToXY(placement, state.rects, offset2);
    return acc;
  }, {});
  var _data$state$placement = data[state.placement], x2 = _data$state$placement.x, y3 = _data$state$placement.y;
  if (state.modifiersData.popperOffsets != null) {
    state.modifiersData.popperOffsets.x += x2;
    state.modifiersData.popperOffsets.y += y3;
  }
  state.modifiersData[name2] = data;
}
const offset$2 = {
  name: "offset",
  enabled: true,
  phase: "main",
  requires: ["popperOffsets"],
  fn: offset$1
};
function popperOffsets(_ref) {
  var state = _ref.state, name2 = _ref.name;
  state.modifiersData[name2] = computeOffsets({
    reference: state.rects.reference,
    element: state.rects.popper,
    placement: state.placement
  });
}
const popperOffsets$1 = {
  name: "popperOffsets",
  enabled: true,
  phase: "read",
  fn: popperOffsets,
  data: {}
};
function getAltAxis(axis) {
  return axis === "x" ? "y" : "x";
}
function preventOverflow(_ref) {
  var state = _ref.state, options = _ref.options, name2 = _ref.name;
  var _options$mainAxis = options.mainAxis, checkMainAxis = _options$mainAxis === void 0 ? true : _options$mainAxis, _options$altAxis = options.altAxis, checkAltAxis = _options$altAxis === void 0 ? false : _options$altAxis, boundary = options.boundary, rootBoundary = options.rootBoundary, altBoundary = options.altBoundary, padding = options.padding, _options$tether = options.tether, tether = _options$tether === void 0 ? true : _options$tether, _options$tetherOffset = options.tetherOffset, tetherOffset = _options$tetherOffset === void 0 ? 0 : _options$tetherOffset;
  var overflow = detectOverflow(state, {
    boundary,
    rootBoundary,
    padding,
    altBoundary
  });
  var basePlacement = getBasePlacement(state.placement);
  var variation = getVariation(state.placement);
  var isBasePlacement = !variation;
  var mainAxis = getMainAxisFromPlacement(basePlacement);
  var altAxis = getAltAxis(mainAxis);
  var popperOffsets2 = state.modifiersData.popperOffsets;
  var referenceRect = state.rects.reference;
  var popperRect = state.rects.popper;
  var tetherOffsetValue = typeof tetherOffset === "function" ? tetherOffset(Object.assign({}, state.rects, {
    placement: state.placement
  })) : tetherOffset;
  var normalizedTetherOffsetValue = typeof tetherOffsetValue === "number" ? {
    mainAxis: tetherOffsetValue,
    altAxis: tetherOffsetValue
  } : Object.assign({
    mainAxis: 0,
    altAxis: 0
  }, tetherOffsetValue);
  var offsetModifierState = state.modifiersData.offset ? state.modifiersData.offset[state.placement] : null;
  var data = {
    x: 0,
    y: 0
  };
  if (!popperOffsets2) {
    return;
  }
  if (checkMainAxis) {
    var _offsetModifierState$;
    var mainSide = mainAxis === "y" ? top : left;
    var altSide = mainAxis === "y" ? bottom : right;
    var len = mainAxis === "y" ? "height" : "width";
    var offset2 = popperOffsets2[mainAxis];
    var min2 = offset2 + overflow[mainSide];
    var max2 = offset2 - overflow[altSide];
    var additive = tether ? -popperRect[len] / 2 : 0;
    var minLen = variation === start ? referenceRect[len] : popperRect[len];
    var maxLen = variation === start ? -popperRect[len] : -referenceRect[len];
    var arrowElement = state.elements.arrow;
    var arrowRect = tether && arrowElement ? getLayoutRect(arrowElement) : {
      width: 0,
      height: 0
    };
    var arrowPaddingObject = state.modifiersData["arrow#persistent"] ? state.modifiersData["arrow#persistent"].padding : getFreshSideObject();
    var arrowPaddingMin = arrowPaddingObject[mainSide];
    var arrowPaddingMax = arrowPaddingObject[altSide];
    var arrowLen = within(0, referenceRect[len], arrowRect[len]);
    var minOffset = isBasePlacement ? referenceRect[len] / 2 - additive - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis : minLen - arrowLen - arrowPaddingMin - normalizedTetherOffsetValue.mainAxis;
    var maxOffset = isBasePlacement ? -referenceRect[len] / 2 + additive + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis : maxLen + arrowLen + arrowPaddingMax + normalizedTetherOffsetValue.mainAxis;
    var arrowOffsetParent = state.elements.arrow && getOffsetParent(state.elements.arrow);
    var clientOffset = arrowOffsetParent ? mainAxis === "y" ? arrowOffsetParent.clientTop || 0 : arrowOffsetParent.clientLeft || 0 : 0;
    var offsetModifierValue = (_offsetModifierState$ = offsetModifierState == null ? void 0 : offsetModifierState[mainAxis]) != null ? _offsetModifierState$ : 0;
    var tetherMin = offset2 + minOffset - offsetModifierValue - clientOffset;
    var tetherMax = offset2 + maxOffset - offsetModifierValue;
    var preventedOffset = within(tether ? min$1(min2, tetherMin) : min2, offset2, tether ? max$1(max2, tetherMax) : max2);
    popperOffsets2[mainAxis] = preventedOffset;
    data[mainAxis] = preventedOffset - offset2;
  }
  if (checkAltAxis) {
    var _offsetModifierState$2;
    var _mainSide = mainAxis === "x" ? top : left;
    var _altSide = mainAxis === "x" ? bottom : right;
    var _offset = popperOffsets2[altAxis];
    var _len = altAxis === "y" ? "height" : "width";
    var _min = _offset + overflow[_mainSide];
    var _max = _offset - overflow[_altSide];
    var isOriginSide = [top, left].indexOf(basePlacement) !== -1;
    var _offsetModifierValue = (_offsetModifierState$2 = offsetModifierState == null ? void 0 : offsetModifierState[altAxis]) != null ? _offsetModifierState$2 : 0;
    var _tetherMin = isOriginSide ? _min : _offset - referenceRect[_len] - popperRect[_len] - _offsetModifierValue + normalizedTetherOffsetValue.altAxis;
    var _tetherMax = isOriginSide ? _offset + referenceRect[_len] + popperRect[_len] - _offsetModifierValue - normalizedTetherOffsetValue.altAxis : _max;
    var _preventedOffset = tether && isOriginSide ? withinMaxClamp(_tetherMin, _offset, _tetherMax) : within(tether ? _tetherMin : _min, _offset, tether ? _tetherMax : _max);
    popperOffsets2[altAxis] = _preventedOffset;
    data[altAxis] = _preventedOffset - _offset;
  }
  state.modifiersData[name2] = data;
}
const preventOverflow$1 = {
  name: "preventOverflow",
  enabled: true,
  phase: "main",
  fn: preventOverflow,
  requiresIfExists: ["offset"]
};
function getHTMLElementScroll(element) {
  return {
    scrollLeft: element.scrollLeft,
    scrollTop: element.scrollTop
  };
}
function getNodeScroll(node) {
  if (node === getWindow$1(node) || !isHTMLElement$2(node)) {
    return getWindowScroll(node);
  } else {
    return getHTMLElementScroll(node);
  }
}
function isElementScaled(element) {
  var rect = element.getBoundingClientRect();
  var scaleX = round(rect.width) / element.offsetWidth || 1;
  var scaleY = round(rect.height) / element.offsetHeight || 1;
  return scaleX !== 1 || scaleY !== 1;
}
function getCompositeRect(elementOrVirtualElement, offsetParent2, isFixed2) {
  if (isFixed2 === void 0) {
    isFixed2 = false;
  }
  var isOffsetParentAnElement = isHTMLElement$2(offsetParent2);
  var offsetParentIsScaled = isHTMLElement$2(offsetParent2) && isElementScaled(offsetParent2);
  var documentElement = getDocumentElement(offsetParent2);
  var rect = getBoundingClientRect(elementOrVirtualElement, offsetParentIsScaled, isFixed2);
  var scroll = {
    scrollLeft: 0,
    scrollTop: 0
  };
  var offsets = {
    x: 0,
    y: 0
  };
  if (isOffsetParentAnElement || !isOffsetParentAnElement && !isFixed2) {
    if (getNodeName(offsetParent2) !== "body" || // https://github.com/popperjs/popper-core/issues/1078
    isScrollParent(documentElement)) {
      scroll = getNodeScroll(offsetParent2);
    }
    if (isHTMLElement$2(offsetParent2)) {
      offsets = getBoundingClientRect(offsetParent2, true);
      offsets.x += offsetParent2.clientLeft;
      offsets.y += offsetParent2.clientTop;
    } else if (documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
  }
  return {
    x: rect.left + scroll.scrollLeft - offsets.x,
    y: rect.top + scroll.scrollTop - offsets.y,
    width: rect.width,
    height: rect.height
  };
}
function order(modifiers) {
  var map = /* @__PURE__ */ new Map();
  var visited = /* @__PURE__ */ new Set();
  var result = [];
  modifiers.forEach(function(modifier) {
    map.set(modifier.name, modifier);
  });
  function sort(modifier) {
    visited.add(modifier.name);
    var requires = [].concat(modifier.requires || [], modifier.requiresIfExists || []);
    requires.forEach(function(dep) {
      if (!visited.has(dep)) {
        var depModifier = map.get(dep);
        if (depModifier) {
          sort(depModifier);
        }
      }
    });
    result.push(modifier);
  }
  modifiers.forEach(function(modifier) {
    if (!visited.has(modifier.name)) {
      sort(modifier);
    }
  });
  return result;
}
function orderModifiers(modifiers) {
  var orderedModifiers = order(modifiers);
  return modifierPhases.reduce(function(acc, phase) {
    return acc.concat(orderedModifiers.filter(function(modifier) {
      return modifier.phase === phase;
    }));
  }, []);
}
function debounce(fn3) {
  var pending;
  return function() {
    if (!pending) {
      pending = new Promise(function(resolve) {
        Promise.resolve().then(function() {
          pending = void 0;
          resolve(fn3());
        });
      });
    }
    return pending;
  };
}
function mergeByName(modifiers) {
  var merged = modifiers.reduce(function(merged2, current) {
    var existing = merged2[current.name];
    merged2[current.name] = existing ? Object.assign({}, existing, current, {
      options: Object.assign({}, existing.options, current.options),
      data: Object.assign({}, existing.data, current.data)
    }) : current;
    return merged2;
  }, {});
  return Object.keys(merged).map(function(key) {
    return merged[key];
  });
}
var DEFAULT_OPTIONS = {
  placement: "bottom",
  modifiers: [],
  strategy: "absolute"
};
function areValidElements() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  return !args.some(function(element) {
    return !(element && typeof element.getBoundingClientRect === "function");
  });
}
function popperGenerator(generatorOptions) {
  if (generatorOptions === void 0) {
    generatorOptions = {};
  }
  var _generatorOptions = generatorOptions, _generatorOptions$def = _generatorOptions.defaultModifiers, defaultModifiers = _generatorOptions$def === void 0 ? [] : _generatorOptions$def, _generatorOptions$def2 = _generatorOptions.defaultOptions, defaultOptions2 = _generatorOptions$def2 === void 0 ? DEFAULT_OPTIONS : _generatorOptions$def2;
  return function createPopper2(reference2, popper2, options) {
    if (options === void 0) {
      options = defaultOptions2;
    }
    var state = {
      placement: "bottom",
      orderedModifiers: [],
      options: Object.assign({}, DEFAULT_OPTIONS, defaultOptions2),
      modifiersData: {},
      elements: {
        reference: reference2,
        popper: popper2
      },
      attributes: {},
      styles: {}
    };
    var effectCleanupFns = [];
    var isDestroyed = false;
    var instance = {
      state,
      setOptions: function setOptions(setOptionsAction) {
        var options2 = typeof setOptionsAction === "function" ? setOptionsAction(state.options) : setOptionsAction;
        cleanupModifierEffects();
        state.options = Object.assign({}, defaultOptions2, state.options, options2);
        state.scrollParents = {
          reference: isElement(reference2) ? listScrollParents(reference2) : reference2.contextElement ? listScrollParents(reference2.contextElement) : [],
          popper: listScrollParents(popper2)
        };
        var orderedModifiers = orderModifiers(mergeByName([].concat(defaultModifiers, state.options.modifiers)));
        state.orderedModifiers = orderedModifiers.filter(function(m3) {
          return m3.enabled;
        });
        runModifierEffects();
        return instance.update();
      },
      // Sync update – it will always be executed, even if not necessary. This
      // is useful for low frequency updates where sync behavior simplifies the
      // logic.
      // For high frequency updates (e.g. `resize` and `scroll` events), always
      // prefer the async Popper#update method
      forceUpdate: function forceUpdate() {
        if (isDestroyed) {
          return;
        }
        var _state$elements = state.elements, reference3 = _state$elements.reference, popper3 = _state$elements.popper;
        if (!areValidElements(reference3, popper3)) {
          return;
        }
        state.rects = {
          reference: getCompositeRect(reference3, getOffsetParent(popper3), state.options.strategy === "fixed"),
          popper: getLayoutRect(popper3)
        };
        state.reset = false;
        state.placement = state.options.placement;
        state.orderedModifiers.forEach(function(modifier) {
          return state.modifiersData[modifier.name] = Object.assign({}, modifier.data);
        });
        for (var index2 = 0; index2 < state.orderedModifiers.length; index2++) {
          if (state.reset === true) {
            state.reset = false;
            index2 = -1;
            continue;
          }
          var _state$orderedModifie = state.orderedModifiers[index2], fn3 = _state$orderedModifie.fn, _state$orderedModifie2 = _state$orderedModifie.options, _options = _state$orderedModifie2 === void 0 ? {} : _state$orderedModifie2, name2 = _state$orderedModifie.name;
          if (typeof fn3 === "function") {
            state = fn3({
              state,
              options: _options,
              name: name2,
              instance
            }) || state;
          }
        }
      },
      // Async and optimistically optimized update – it will not be executed if
      // not necessary (debounced to run at most once-per-tick)
      update: debounce(function() {
        return new Promise(function(resolve) {
          instance.forceUpdate();
          resolve(state);
        });
      }),
      destroy: function destroy() {
        cleanupModifierEffects();
        isDestroyed = true;
      }
    };
    if (!areValidElements(reference2, popper2)) {
      return instance;
    }
    instance.setOptions(options).then(function(state2) {
      if (!isDestroyed && options.onFirstUpdate) {
        options.onFirstUpdate(state2);
      }
    });
    function runModifierEffects() {
      state.orderedModifiers.forEach(function(_ref) {
        var name2 = _ref.name, _ref$options = _ref.options, options2 = _ref$options === void 0 ? {} : _ref$options, effect3 = _ref.effect;
        if (typeof effect3 === "function") {
          var cleanupFn = effect3({
            state,
            name: name2,
            instance,
            options: options2
          });
          var noopFn = function noopFn2() {
          };
          effectCleanupFns.push(cleanupFn || noopFn);
        }
      });
    }
    function cleanupModifierEffects() {
      effectCleanupFns.forEach(function(fn3) {
        return fn3();
      });
      effectCleanupFns = [];
    }
    return instance;
  };
}
function contains(context2, node) {
  if (context2.contains) return context2.contains(node);
  if (context2.compareDocumentPosition) return context2 === node || !!(context2.compareDocumentPosition(node) & 16);
}
var warning_1;
var hasRequiredWarning;
function requireWarning() {
  if (hasRequiredWarning) return warning_1;
  hasRequiredWarning = 1;
  var warning2 = function() {
  };
  warning_1 = warning2;
  return warning_1;
}
var warningExports = requireWarning();
const SelectableContext = /* @__PURE__ */ reactExports.createContext(null);
const makeEventKey = (eventKey, href = null) => {
  if (eventKey != null) return String(eventKey);
  return href || null;
};
const NavContext = /* @__PURE__ */ reactExports.createContext(null);
NavContext.displayName = "NavContext";
const ATTRIBUTE_PREFIX = `data-rr-ui-`;
const PROPERTY_PREFIX = `rrUi`;
function dataAttr(property) {
  return `${ATTRIBUTE_PREFIX}${property}`;
}
function dataProp(property) {
  return `${PROPERTY_PREFIX}${property}`;
}
const Context$1 = /* @__PURE__ */ reactExports.createContext(canUseDOM$1 ? window : void 0);
Context$1.Provider;
function useWindow() {
  return reactExports.useContext(Context$1);
}
const isReactNative = typeof global !== "undefined" && // @ts-ignore
global.navigator && // @ts-ignore
global.navigator.product === "ReactNative";
const isDOM = typeof document !== "undefined";
const useIsomorphicEffect = isDOM || isReactNative ? reactExports.useLayoutEffect : reactExports.useEffect;
const context = /* @__PURE__ */ reactExports.createContext(null);
context.displayName = "NavbarContext";
const toFnRef = (ref) => !ref || typeof ref === "function" ? ref : (value) => {
  ref.current = value;
};
function mergeRefs(refA, refB) {
  const a3 = toFnRef(refA);
  const b2 = toFnRef(refB);
  return (value) => {
    if (a3) a3(value);
    if (b2) b2(value);
  };
}
function useMergedRefs(refA, refB) {
  return reactExports.useMemo(() => mergeRefs(refA, refB), [refA, refB]);
}
const TabContext = /* @__PURE__ */ reactExports.createContext(null);
const _excluded$c = ["as", "active", "eventKey"];
function _objectWithoutPropertiesLoose$4(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
function useNavItem({
  key,
  onClick,
  active,
  id: id2,
  role,
  disabled
}) {
  const parentOnSelect = reactExports.useContext(SelectableContext);
  const navContext = reactExports.useContext(NavContext);
  const tabContext = reactExports.useContext(TabContext);
  let isActive = active;
  const props = {
    role
  };
  if (navContext) {
    if (!role && navContext.role === "tablist") props.role = "tab";
    const contextControllerId = navContext.getControllerId(key != null ? key : null);
    const contextControlledId = navContext.getControlledId(key != null ? key : null);
    props[dataAttr("event-key")] = key;
    props.id = contextControllerId || id2;
    isActive = active == null && key != null ? navContext.activeKey === key : active;
    if (isActive || !(tabContext != null && tabContext.unmountOnExit) && !(tabContext != null && tabContext.mountOnEnter)) props["aria-controls"] = contextControlledId;
  }
  if (props.role === "tab") {
    props["aria-selected"] = isActive;
    if (!isActive) {
      props.tabIndex = -1;
    }
    if (disabled) {
      props.tabIndex = -1;
      props["aria-disabled"] = true;
    }
  }
  props.onClick = useEventCallback((e2) => {
    if (disabled) return;
    onClick == null ? void 0 : onClick(e2);
    if (key == null) {
      return;
    }
    if (parentOnSelect && !e2.isPropagationStopped()) {
      parentOnSelect(key, e2);
    }
  });
  return [props, {
    isActive
  }];
}
const NavItem$2 = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    as: Component = Button$1,
    active,
    eventKey
  } = _ref, options = _objectWithoutPropertiesLoose$4(_ref, _excluded$c);
  const [props, meta] = useNavItem(Object.assign({
    key: makeEventKey(eventKey, options.href),
    active
  }, options));
  props[dataAttr("active")] = meta.isActive;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, Object.assign({}, options, props, {
    ref
  }));
});
NavItem$2.displayName = "NavItem";
const _excluded$b = ["as", "onSelect", "activeKey", "role", "onKeyDown"];
function _objectWithoutPropertiesLoose$3(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
const noop$2 = () => {
};
const EVENT_KEY_ATTR = dataAttr("event-key");
const Nav$2 = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    // Need to define the default "as" during prop destructuring to be compatible with styled-components github.com/react-bootstrap/react-bootstrap/issues/3595
    as: Component = "div",
    onSelect,
    activeKey,
    role,
    onKeyDown
  } = _ref, props = _objectWithoutPropertiesLoose$3(_ref, _excluded$b);
  const forceUpdate = useForceUpdate();
  const needsRefocusRef = reactExports.useRef(false);
  const parentOnSelect = reactExports.useContext(SelectableContext);
  const tabContext = reactExports.useContext(TabContext);
  let getControlledId, getControllerId;
  if (tabContext) {
    role = role || "tablist";
    activeKey = tabContext.activeKey;
    getControlledId = tabContext.getControlledId;
    getControllerId = tabContext.getControllerId;
  }
  const listNode = reactExports.useRef(null);
  const getNextActiveTab = (offset2) => {
    const currentListNode = listNode.current;
    if (!currentListNode) return null;
    const items = qsa(currentListNode, `[${EVENT_KEY_ATTR}]:not([aria-disabled=true])`);
    const activeChild = currentListNode.querySelector("[aria-selected=true]");
    if (!activeChild || activeChild !== document.activeElement) return null;
    const index2 = items.indexOf(activeChild);
    if (index2 === -1) return null;
    let nextIndex = index2 + offset2;
    if (nextIndex >= items.length) nextIndex = 0;
    if (nextIndex < 0) nextIndex = items.length - 1;
    return items[nextIndex];
  };
  const handleSelect = (key, event) => {
    if (key == null) return;
    onSelect == null ? void 0 : onSelect(key, event);
    parentOnSelect == null ? void 0 : parentOnSelect(key, event);
  };
  const handleKeyDown = (event) => {
    onKeyDown == null ? void 0 : onKeyDown(event);
    if (!tabContext) {
      return;
    }
    let nextActiveChild;
    switch (event.key) {
      case "ArrowLeft":
      case "ArrowUp":
        nextActiveChild = getNextActiveTab(-1);
        break;
      case "ArrowRight":
      case "ArrowDown":
        nextActiveChild = getNextActiveTab(1);
        break;
      default:
        return;
    }
    if (!nextActiveChild) return;
    event.preventDefault();
    handleSelect(nextActiveChild.dataset[dataProp("EventKey")] || null, event);
    needsRefocusRef.current = true;
    forceUpdate();
  };
  reactExports.useEffect(() => {
    if (listNode.current && needsRefocusRef.current) {
      const activeChild = listNode.current.querySelector(`[${EVENT_KEY_ATTR}][aria-selected=true]`);
      activeChild == null ? void 0 : activeChild.focus();
    }
    needsRefocusRef.current = false;
  });
  const mergedRef = useMergedRefs(ref, listNode);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(SelectableContext.Provider, {
    value: handleSelect,
    children: /* @__PURE__ */ jsxRuntimeExports.jsx(NavContext.Provider, {
      value: {
        role,
        // used by NavLink to determine it's role
        activeKey: makeEventKey(activeKey),
        getControlledId: getControlledId || noop$2,
        getControllerId: getControllerId || noop$2
      },
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(Component, Object.assign({}, props, {
        onKeyDown: handleKeyDown,
        ref: mergedRef,
        role
      }))
    })
  });
});
Nav$2.displayName = "Nav";
const BaseNav = Object.assign(Nav$2, {
  Item: NavItem$2
});
var size;
function scrollbarSize(recalc) {
  if (!size && size !== 0 || recalc) {
    if (canUseDOM$1) {
      var scrollDiv = document.createElement("div");
      scrollDiv.style.position = "absolute";
      scrollDiv.style.top = "-9999px";
      scrollDiv.style.width = "50px";
      scrollDiv.style.height = "50px";
      scrollDiv.style.overflow = "scroll";
      document.body.appendChild(scrollDiv);
      size = scrollDiv.offsetWidth - scrollDiv.clientWidth;
      document.body.removeChild(scrollDiv);
    }
  }
  return size;
}
function useCallbackRef() {
  return reactExports.useState(null);
}
function activeElement(doc) {
  if (doc === void 0) {
    doc = ownerDocument$1();
  }
  try {
    var active = doc.activeElement;
    if (!active || !active.nodeName) return null;
    return active;
  } catch (e2) {
    return doc.body;
  }
}
function useUpdatedRef(value) {
  const valueRef = reactExports.useRef(value);
  valueRef.current = value;
  return valueRef;
}
function useWillUnmount(fn3) {
  const onUnmount = useUpdatedRef(fn3);
  reactExports.useEffect(() => () => onUnmount.current(), []);
}
function getBodyScrollbarWidth(ownerDocument2 = document) {
  const window2 = ownerDocument2.defaultView;
  return Math.abs(window2.innerWidth - ownerDocument2.documentElement.clientWidth);
}
const OPEN_DATA_ATTRIBUTE = dataAttr("modal-open");
class ModalManager {
  constructor({
    ownerDocument: ownerDocument2,
    handleContainerOverflow = true,
    isRTL = false
  } = {}) {
    this.handleContainerOverflow = handleContainerOverflow;
    this.isRTL = isRTL;
    this.modals = [];
    this.ownerDocument = ownerDocument2;
  }
  getScrollbarWidth() {
    return getBodyScrollbarWidth(this.ownerDocument);
  }
  getElement() {
    return (this.ownerDocument || document).body;
  }
  setModalAttributes(_modal) {
  }
  removeModalAttributes(_modal) {
  }
  setContainerStyle(containerState) {
    const style$1 = {
      overflow: "hidden"
    };
    const paddingProp = this.isRTL ? "paddingLeft" : "paddingRight";
    const container = this.getElement();
    containerState.style = {
      overflow: container.style.overflow,
      [paddingProp]: container.style[paddingProp]
    };
    if (containerState.scrollBarWidth) {
      style$1[paddingProp] = `${parseInt(style(container, paddingProp) || "0", 10) + containerState.scrollBarWidth}px`;
    }
    container.setAttribute(OPEN_DATA_ATTRIBUTE, "");
    style(container, style$1);
  }
  reset() {
    [...this.modals].forEach((m3) => this.remove(m3));
  }
  removeContainerStyle(containerState) {
    const container = this.getElement();
    container.removeAttribute(OPEN_DATA_ATTRIBUTE);
    Object.assign(container.style, containerState.style);
  }
  add(modal) {
    let modalIdx = this.modals.indexOf(modal);
    if (modalIdx !== -1) {
      return modalIdx;
    }
    modalIdx = this.modals.length;
    this.modals.push(modal);
    this.setModalAttributes(modal);
    if (modalIdx !== 0) {
      return modalIdx;
    }
    this.state = {
      scrollBarWidth: this.getScrollbarWidth(),
      style: {}
    };
    if (this.handleContainerOverflow) {
      this.setContainerStyle(this.state);
    }
    return modalIdx;
  }
  remove(modal) {
    const modalIdx = this.modals.indexOf(modal);
    if (modalIdx === -1) {
      return;
    }
    this.modals.splice(modalIdx, 1);
    if (!this.modals.length && this.handleContainerOverflow) {
      this.removeContainerStyle(this.state);
    }
    this.removeModalAttributes(modal);
  }
  isTopModal(modal) {
    return !!this.modals.length && this.modals[this.modals.length - 1] === modal;
  }
}
const resolveContainerRef$1 = (ref, document2) => {
  if (!canUseDOM$1) return null;
  if (ref == null) return (document2 || ownerDocument$1()).body;
  if (typeof ref === "function") ref = ref();
  if (ref && "current" in ref) ref = ref.current;
  if (ref && ("nodeType" in ref || ref.getBoundingClientRect)) return ref;
  return null;
};
function useWaitForDOMRef$1(ref, onResolved) {
  const window2 = useWindow();
  const [resolvedRef, setRef] = reactExports.useState(() => resolveContainerRef$1(ref, window2 == null ? void 0 : window2.document));
  if (!resolvedRef) {
    const earlyRef = resolveContainerRef$1(ref);
    if (earlyRef) setRef(earlyRef);
  }
  reactExports.useEffect(() => {
  }, [onResolved, resolvedRef]);
  reactExports.useEffect(() => {
    const nextRef = resolveContainerRef$1(ref);
    if (nextRef !== resolvedRef) {
      setRef(nextRef);
    }
  }, [ref, resolvedRef]);
  return resolvedRef;
}
function NoopTransition({
  children,
  in: inProp,
  onExited,
  mountOnEnter,
  unmountOnExit
}) {
  const ref = reactExports.useRef(null);
  const hasEnteredRef = reactExports.useRef(inProp);
  const handleExited = useEventCallback(onExited);
  reactExports.useEffect(() => {
    if (inProp) hasEnteredRef.current = true;
    else {
      handleExited(ref.current);
    }
  }, [inProp, handleExited]);
  const combinedRef = useMergedRefs(ref, getChildRef(children));
  const child = /* @__PURE__ */ reactExports.cloneElement(children, {
    ref: combinedRef
  });
  if (inProp) return child;
  if (unmountOnExit) {
    return null;
  }
  if (!hasEnteredRef.current && mountOnEnter) {
    return null;
  }
  return child;
}
const _excluded$a = ["onEnter", "onEntering", "onEntered", "onExit", "onExiting", "onExited", "addEndListener", "children"];
function _objectWithoutPropertiesLoose$2(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
function useRTGTransitionProps(_ref) {
  let {
    onEnter,
    onEntering,
    onEntered,
    onExit,
    onExiting,
    onExited,
    addEndListener,
    children
  } = _ref, props = _objectWithoutPropertiesLoose$2(_ref, _excluded$a);
  const nodeRef = reactExports.useRef(null);
  const mergedRef = useMergedRefs(nodeRef, getChildRef(children));
  const normalize = (callback) => (param) => {
    if (callback && nodeRef.current) {
      callback(nodeRef.current, param);
    }
  };
  const handleEnter = reactExports.useCallback(normalize(onEnter), [onEnter]);
  const handleEntering = reactExports.useCallback(normalize(onEntering), [onEntering]);
  const handleEntered = reactExports.useCallback(normalize(onEntered), [onEntered]);
  const handleExit = reactExports.useCallback(normalize(onExit), [onExit]);
  const handleExiting = reactExports.useCallback(normalize(onExiting), [onExiting]);
  const handleExited = reactExports.useCallback(normalize(onExited), [onExited]);
  const handleAddEndListener = reactExports.useCallback(normalize(addEndListener), [addEndListener]);
  return Object.assign({}, props, {
    nodeRef
  }, onEnter && {
    onEnter: handleEnter
  }, onEntering && {
    onEntering: handleEntering
  }, onEntered && {
    onEntered: handleEntered
  }, onExit && {
    onExit: handleExit
  }, onExiting && {
    onExiting: handleExiting
  }, onExited && {
    onExited: handleExited
  }, addEndListener && {
    addEndListener: handleAddEndListener
  }, {
    children: typeof children === "function" ? (status, innerProps) => (
      // TODO: Types for RTG missing innerProps, so need to cast.
      children(status, Object.assign({}, innerProps, {
        ref: mergedRef
      }))
    ) : /* @__PURE__ */ reactExports.cloneElement(children, {
      ref: mergedRef
    })
  });
}
const _excluded$9 = ["component"];
function _objectWithoutPropertiesLoose$1(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
const RTGTransition = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    component: Component
  } = _ref, props = _objectWithoutPropertiesLoose$1(_ref, _excluded$9);
  const transitionProps = useRTGTransitionProps(props);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, Object.assign({
    ref
  }, transitionProps));
});
function useTransition({
  in: inProp,
  onTransition
}) {
  const ref = reactExports.useRef(null);
  const isInitialRef = reactExports.useRef(true);
  const handleTransition = useEventCallback(onTransition);
  useIsomorphicEffect$1(() => {
    if (!ref.current) {
      return void 0;
    }
    let stale = false;
    handleTransition({
      in: inProp,
      element: ref.current,
      initial: isInitialRef.current,
      isStale: () => stale
    });
    return () => {
      stale = true;
    };
  }, [inProp, handleTransition]);
  useIsomorphicEffect$1(() => {
    isInitialRef.current = false;
    return () => {
      isInitialRef.current = true;
    };
  }, []);
  return ref;
}
function ImperativeTransition({
  children,
  in: inProp,
  onExited,
  onEntered,
  transition
}) {
  const [exited, setExited] = reactExports.useState(!inProp);
  if (inProp && exited) {
    setExited(false);
  }
  const ref = useTransition({
    in: !!inProp,
    onTransition: (options) => {
      const onFinish = () => {
        if (options.isStale()) return;
        if (options.in) {
          onEntered == null ? void 0 : onEntered(options.element, options.initial);
        } else {
          setExited(true);
          onExited == null ? void 0 : onExited(options.element);
        }
      };
      Promise.resolve(transition(options)).then(onFinish, (error) => {
        if (!options.in) setExited(true);
        throw error;
      });
    }
  });
  const combinedRef = useMergedRefs(ref, getChildRef(children));
  return exited && !inProp ? null : /* @__PURE__ */ reactExports.cloneElement(children, {
    ref: combinedRef
  });
}
function renderTransition(component, runTransition, props) {
  if (component) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(RTGTransition, Object.assign({}, props, {
      component
    }));
  }
  if (runTransition) {
    return /* @__PURE__ */ jsxRuntimeExports.jsx(ImperativeTransition, Object.assign({}, props, {
      transition: runTransition
    }));
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(NoopTransition, Object.assign({}, props));
}
const _excluded$8 = ["show", "role", "className", "style", "children", "backdrop", "keyboard", "onBackdropClick", "onEscapeKeyDown", "transition", "runTransition", "backdropTransition", "runBackdropTransition", "autoFocus", "enforceFocus", "restoreFocus", "restoreFocusOptions", "renderDialog", "renderBackdrop", "manager", "container", "onShow", "onHide", "onExit", "onExited", "onExiting", "onEnter", "onEntering", "onEntered"];
function _objectWithoutPropertiesLoose(r2, e2) {
  if (null == r2) return {};
  var t2 = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e2.indexOf(n) >= 0) continue;
    t2[n] = r2[n];
  }
  return t2;
}
let manager;
function getManager(window2) {
  if (!manager) manager = new ModalManager({
    ownerDocument: window2 == null ? void 0 : window2.document
  });
  return manager;
}
function useModalManager(provided) {
  const window2 = useWindow();
  const modalManager = provided || getManager(window2);
  const modal = reactExports.useRef({
    dialog: null,
    backdrop: null
  });
  return Object.assign(modal.current, {
    add: () => modalManager.add(modal.current),
    remove: () => modalManager.remove(modal.current),
    isTopModal: () => modalManager.isTopModal(modal.current),
    setDialogRef: reactExports.useCallback((ref) => {
      modal.current.dialog = ref;
    }, []),
    setBackdropRef: reactExports.useCallback((ref) => {
      modal.current.backdrop = ref;
    }, [])
  });
}
const Modal = /* @__PURE__ */ reactExports.forwardRef((_ref, ref) => {
  let {
    show = false,
    role = "dialog",
    className,
    style: style2,
    children,
    backdrop = true,
    keyboard = true,
    onBackdropClick,
    onEscapeKeyDown,
    transition,
    runTransition,
    backdropTransition,
    runBackdropTransition,
    autoFocus = true,
    enforceFocus = true,
    restoreFocus = true,
    restoreFocusOptions,
    renderDialog,
    renderBackdrop = (props) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", Object.assign({}, props)),
    manager: providedManager,
    container: containerRef,
    onShow,
    onHide: onHide2 = () => {
    },
    onExit,
    onExited,
    onExiting,
    onEnter,
    onEntering,
    onEntered
  } = _ref, rest = _objectWithoutPropertiesLoose(_ref, _excluded$8);
  const ownerWindow2 = useWindow();
  const container = useWaitForDOMRef$1(containerRef);
  const modal = useModalManager(providedManager);
  const isMounted = useMounted$1();
  const prevShow = usePrevious$1(show);
  const [exited, setExited] = reactExports.useState(!show);
  const lastFocusRef = reactExports.useRef(null);
  reactExports.useImperativeHandle(ref, () => modal, [modal]);
  if (canUseDOM$1 && !prevShow && show) {
    lastFocusRef.current = activeElement(ownerWindow2 == null ? void 0 : ownerWindow2.document);
  }
  if (show && exited) {
    setExited(false);
  }
  const handleShow = useEventCallback(() => {
    modal.add();
    removeKeydownListenerRef.current = listen(document, "keydown", handleDocumentKeyDown);
    removeFocusListenerRef.current = listen(
      document,
      "focus",
      // the timeout is necessary b/c this will run before the new modal is mounted
      // and so steals focus from it
      () => setTimeout(handleEnforceFocus),
      true
    );
    if (onShow) {
      onShow();
    }
    if (autoFocus) {
      var _modal$dialog$ownerDo, _modal$dialog;
      const currentActiveElement = activeElement((_modal$dialog$ownerDo = (_modal$dialog = modal.dialog) == null ? void 0 : _modal$dialog.ownerDocument) != null ? _modal$dialog$ownerDo : ownerWindow2 == null ? void 0 : ownerWindow2.document);
      if (modal.dialog && currentActiveElement && !contains(modal.dialog, currentActiveElement)) {
        lastFocusRef.current = currentActiveElement;
        modal.dialog.focus();
      }
    }
  });
  const handleHide = useEventCallback(() => {
    modal.remove();
    removeKeydownListenerRef.current == null ? void 0 : removeKeydownListenerRef.current();
    removeFocusListenerRef.current == null ? void 0 : removeFocusListenerRef.current();
    if (restoreFocus) {
      var _lastFocusRef$current;
      (_lastFocusRef$current = lastFocusRef.current) == null ? void 0 : _lastFocusRef$current.focus == null ? void 0 : _lastFocusRef$current.focus(restoreFocusOptions);
      lastFocusRef.current = null;
    }
  });
  reactExports.useEffect(() => {
    if (!show || !container) return;
    handleShow();
  }, [
    show,
    container,
    /* should never change: */
    handleShow
  ]);
  reactExports.useEffect(() => {
    if (!exited) return;
    handleHide();
  }, [exited, handleHide]);
  useWillUnmount(() => {
    handleHide();
  });
  const handleEnforceFocus = useEventCallback(() => {
    if (!enforceFocus || !isMounted() || !modal.isTopModal()) {
      return;
    }
    const currentActiveElement = activeElement(ownerWindow2 == null ? void 0 : ownerWindow2.document);
    if (modal.dialog && currentActiveElement && !contains(modal.dialog, currentActiveElement)) {
      modal.dialog.focus();
    }
  });
  const handleBackdropClick = useEventCallback((e2) => {
    if (e2.target !== e2.currentTarget) {
      return;
    }
    onBackdropClick == null ? void 0 : onBackdropClick(e2);
    if (backdrop === true) {
      onHide2();
    }
  });
  const handleDocumentKeyDown = useEventCallback((e2) => {
    if (keyboard && isEscKey(e2) && modal.isTopModal()) {
      onEscapeKeyDown == null ? void 0 : onEscapeKeyDown(e2);
      if (!e2.defaultPrevented) {
        onHide2();
      }
    }
  });
  const removeFocusListenerRef = reactExports.useRef();
  const removeKeydownListenerRef = reactExports.useRef();
  const handleHidden = (...args) => {
    setExited(true);
    onExited == null ? void 0 : onExited(...args);
  };
  if (!container) {
    return null;
  }
  const dialogProps = Object.assign({
    role,
    ref: modal.setDialogRef,
    // apparently only works on the dialog role element
    "aria-modal": role === "dialog" ? true : void 0
  }, rest, {
    style: style2,
    className,
    tabIndex: -1
  });
  let dialog = renderDialog ? renderDialog(dialogProps) : /* @__PURE__ */ jsxRuntimeExports.jsx("div", Object.assign({}, dialogProps, {
    children: /* @__PURE__ */ reactExports.cloneElement(children, {
      role: "document"
    })
  }));
  dialog = renderTransition(transition, runTransition, {
    unmountOnExit: true,
    mountOnEnter: true,
    appear: true,
    in: !!show,
    onExit,
    onExiting,
    onExited: handleHidden,
    onEnter,
    onEntering,
    onEntered,
    children: dialog
  });
  let backdropElement = null;
  if (backdrop) {
    backdropElement = renderBackdrop({
      ref: modal.setBackdropRef,
      onClick: handleBackdropClick
    });
    backdropElement = renderTransition(backdropTransition, runBackdropTransition, {
      in: !!show,
      appear: true,
      mountOnEnter: true,
      unmountOnExit: true,
      children: backdropElement
    });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, {
    children: /* @__PURE__ */ reactDomExports.createPortal(/* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
      children: [backdropElement, dialog]
    }), container)
  });
});
Modal.displayName = "Modal";
const BaseModal = Object.assign(Modal, {
  Manager: ModalManager
});
function hasClass(element, className) {
  if (element.classList) return !!className && element.classList.contains(className);
  return (" " + (element.className.baseVal || element.className) + " ").indexOf(" " + className + " ") !== -1;
}
function addClass(element, className) {
  if (element.classList) element.classList.add(className);
  else if (!hasClass(element, className)) if (typeof element.className === "string") element.className = element.className + " " + className;
  else element.setAttribute("class", (element.className && element.className.baseVal || "") + " " + className);
}
function replaceClassName(origClass, classToRemove) {
  return origClass.replace(new RegExp("(^|\\s)" + classToRemove + "(?:\\s|$)", "g"), "$1").replace(/\s+/g, " ").replace(/^\s*|\s*$/g, "");
}
function removeClass(element, className) {
  if (element.classList) {
    element.classList.remove(className);
  } else if (typeof element.className === "string") {
    element.className = replaceClassName(element.className, className);
  } else {
    element.setAttribute("class", replaceClassName(element.className && element.className.baseVal || "", className));
  }
}
const Selector = {
  FIXED_CONTENT: ".fixed-top, .fixed-bottom, .is-fixed, .sticky-top",
  STICKY_CONTENT: ".sticky-top",
  NAVBAR_TOGGLER: ".navbar-toggler"
};
class BootstrapModalManager extends ModalManager {
  adjustAndStore(prop, element, adjust) {
    const actual = element.style[prop];
    element.dataset[prop] = actual;
    style(element, {
      [prop]: `${parseFloat(style(element, prop)) + adjust}px`
    });
  }
  restore(prop, element) {
    const value = element.dataset[prop];
    if (value !== void 0) {
      delete element.dataset[prop];
      style(element, {
        [prop]: value
      });
    }
  }
  setContainerStyle(containerState) {
    super.setContainerStyle(containerState);
    const container = this.getElement();
    addClass(container, "modal-open");
    if (!containerState.scrollBarWidth) return;
    const paddingProp = this.isRTL ? "paddingLeft" : "paddingRight";
    const marginProp = this.isRTL ? "marginLeft" : "marginRight";
    qsa(container, Selector.FIXED_CONTENT).forEach((el) => this.adjustAndStore(paddingProp, el, containerState.scrollBarWidth));
    qsa(container, Selector.STICKY_CONTENT).forEach((el) => this.adjustAndStore(marginProp, el, -containerState.scrollBarWidth));
    qsa(container, Selector.NAVBAR_TOGGLER).forEach((el) => this.adjustAndStore(marginProp, el, containerState.scrollBarWidth));
  }
  removeContainerStyle(containerState) {
    super.removeContainerStyle(containerState);
    const container = this.getElement();
    removeClass(container, "modal-open");
    const paddingProp = this.isRTL ? "paddingLeft" : "paddingRight";
    const marginProp = this.isRTL ? "marginLeft" : "marginRight";
    qsa(container, Selector.FIXED_CONTENT).forEach((el) => this.restore(paddingProp, el));
    qsa(container, Selector.STICKY_CONTENT).forEach((el) => this.restore(marginProp, el));
    qsa(container, Selector.NAVBAR_TOGGLER).forEach((el) => this.restore(marginProp, el));
  }
}
let sharedManager;
function getSharedManager(options) {
  if (!sharedManager) sharedManager = new BootstrapModalManager(options);
  return sharedManager;
}
const ModalContext = /* @__PURE__ */ reactExports.createContext({
  onHide() {
  }
});
const AbstractModalHeader = /* @__PURE__ */ reactExports.forwardRef(({
  closeLabel = "Close",
  closeVariant,
  closeButton = false,
  onHide: onHide2,
  children,
  ...props
}, ref) => {
  const context2 = reactExports.useContext(ModalContext);
  const handleClick = useEventCallback$1(() => {
    context2 == null || context2.onHide();
    onHide2 == null || onHide2();
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", {
    ref,
    ...props,
    children: [children, closeButton && /* @__PURE__ */ jsxRuntimeExports.jsx(CloseButton, {
      "aria-label": closeLabel,
      variant: closeVariant,
      onClick: handleClick
    })]
  });
});
AbstractModalHeader.displayName = "AbstractModalHeader";
const NavItem$1 = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  bsPrefix,
  as: Component = "div",
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "nav-item");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    className: classnamesExports(className, bsPrefix),
    ...props
  });
});
NavItem$1.displayName = "NavItem";
const NavLink = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  as: Component = Anchor,
  active,
  eventKey,
  disabled = false,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "nav-link");
  const [navItemProps, meta] = useNavItem({
    key: makeEventKey(eventKey, props.href),
    active,
    disabled,
    ...props
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ...props,
    ...navItemProps,
    ref,
    disabled,
    className: classnamesExports(className, bsPrefix, disabled && "disabled", meta.isActive && "active")
  });
});
NavLink.displayName = "NavLink";
const Nav = /* @__PURE__ */ reactExports.forwardRef((uncontrolledProps, ref) => {
  const {
    as = "div",
    bsPrefix: initialBsPrefix,
    variant,
    fill = false,
    justify = false,
    navbar,
    navbarScroll,
    className,
    activeKey,
    ...props
  } = useUncontrolled(uncontrolledProps, {
    activeKey: "onSelect"
  });
  const bsPrefix = useBootstrapPrefix(initialBsPrefix, "nav");
  let navbarBsPrefix;
  let cardHeaderBsPrefix;
  let isNavbar = false;
  const navbarContext = reactExports.useContext(context);
  const cardHeaderContext = reactExports.useContext(context$1);
  if (navbarContext) {
    navbarBsPrefix = navbarContext.bsPrefix;
    isNavbar = navbar == null ? true : navbar;
  } else if (cardHeaderContext) {
    ({
      cardHeaderBsPrefix
    } = cardHeaderContext);
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(BaseNav, {
    as,
    ref,
    activeKey,
    className: classnamesExports(className, {
      [bsPrefix]: !isNavbar,
      [`${navbarBsPrefix}-nav`]: isNavbar,
      [`${navbarBsPrefix}-nav-scroll`]: isNavbar && navbarScroll,
      [`${cardHeaderBsPrefix}-${variant}`]: !!cardHeaderBsPrefix,
      [`${bsPrefix}-${variant}`]: !!variant,
      [`${bsPrefix}-fill`]: fill,
      [`${bsPrefix}-justified`]: justify
    }),
    ...props
  });
});
Nav.displayName = "Nav";
const Nav$1 = Object.assign(Nav, {
  Item: NavItem$1,
  Link: NavLink
});
const NavbarBrand = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  as,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "navbar-brand");
  const Component = as || (props.href ? "a" : "span");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ...props,
    ref,
    className: classnamesExports(className, bsPrefix)
  });
});
NavbarBrand.displayName = "NavbarBrand";
const NavbarCollapse = /* @__PURE__ */ reactExports.forwardRef(({
  children,
  bsPrefix,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "navbar-collapse");
  const context$12 = reactExports.useContext(context);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Collapse, {
    in: !!(context$12 && context$12.expanded),
    ...props,
    children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
      ref,
      className: bsPrefix,
      children
    })
  });
});
NavbarCollapse.displayName = "NavbarCollapse";
const NavbarToggle = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  children,
  label = "Toggle navigation",
  // Need to define the default "as" during prop destructuring to be compatible with styled-components github.com/react-bootstrap/react-bootstrap/issues/3595
  as: Component = "button",
  onClick,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "navbar-toggler");
  const {
    onToggle,
    expanded
  } = reactExports.useContext(context) || {};
  const handleClick = useEventCallback$1((e2) => {
    if (onClick) onClick(e2);
    if (onToggle) onToggle();
  });
  if (Component === "button") {
    props.type = "button";
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ...props,
    ref,
    onClick: handleClick,
    "aria-label": label,
    className: classnamesExports(className, bsPrefix, !expanded && "collapsed"),
    children: children || /* @__PURE__ */ jsxRuntimeExports.jsx("span", {
      className: `${bsPrefix}-icon`
    })
  });
});
NavbarToggle.displayName = "NavbarToggle";
const matchersByWindow = /* @__PURE__ */ new WeakMap();
const getMatcher = (query, targetWindow) => {
  if (!query || !targetWindow) return void 0;
  const matchers = matchersByWindow.get(targetWindow) || /* @__PURE__ */ new Map();
  matchersByWindow.set(targetWindow, matchers);
  let mql = matchers.get(query);
  if (!mql) {
    mql = targetWindow.matchMedia(query);
    mql.refCount = 0;
    matchers.set(mql.media, mql);
  }
  return mql;
};
function useMediaQuery(query, targetWindow = typeof window === "undefined" ? void 0 : window) {
  const mql = getMatcher(query, targetWindow);
  const [matches2, setMatches] = reactExports.useState(() => mql ? mql.matches : false);
  useIsomorphicEffect(() => {
    let mql2 = getMatcher(query, targetWindow);
    if (!mql2) {
      return setMatches(false);
    }
    let matchers = matchersByWindow.get(targetWindow);
    const handleChange = () => {
      setMatches(mql2.matches);
    };
    mql2.refCount++;
    mql2.addListener(handleChange);
    handleChange();
    return () => {
      mql2.removeListener(handleChange);
      mql2.refCount--;
      if (mql2.refCount <= 0) {
        matchers == null ? void 0 : matchers.delete(mql2.media);
      }
      mql2 = void 0;
    };
  }, [query]);
  return matches2;
}
function createBreakpointHook(breakpointValues) {
  const names = Object.keys(breakpointValues);
  function and(query, next) {
    if (query === next) {
      return next;
    }
    return query ? `${query} and ${next}` : next;
  }
  function getNext(breakpoint) {
    return names[Math.min(names.indexOf(breakpoint) + 1, names.length - 1)];
  }
  function getMaxQuery(breakpoint) {
    const next = getNext(breakpoint);
    let value = breakpointValues[next];
    if (typeof value === "number") value = `${value - 0.2}px`;
    else value = `calc(${value} - 0.2px)`;
    return `(max-width: ${value})`;
  }
  function getMinQuery(breakpoint) {
    let value = breakpointValues[breakpoint];
    if (typeof value === "number") {
      value = `${value}px`;
    }
    return `(min-width: ${value})`;
  }
  function useBreakpoint2(breakpointOrMap, direction, window2) {
    let breakpointMap;
    if (typeof breakpointOrMap === "object") {
      breakpointMap = breakpointOrMap;
      window2 = direction;
      direction = true;
    } else {
      direction = direction || true;
      breakpointMap = {
        [breakpointOrMap]: direction
      };
    }
    let query = reactExports.useMemo(() => Object.entries(breakpointMap).reduce((query2, [key, direction2]) => {
      if (direction2 === "up" || direction2 === true) {
        query2 = and(query2, getMinQuery(key));
      }
      if (direction2 === "down" || direction2 === true) {
        query2 = and(query2, getMaxQuery(key));
      }
      return query2;
    }, ""), [JSON.stringify(breakpointMap)]);
    return useMediaQuery(query, window2);
  }
  return useBreakpoint2;
}
const useBreakpoint = createBreakpointHook({
  xs: 0,
  sm: 576,
  md: 768,
  lg: 992,
  xl: 1200,
  xxl: 1400
});
const OffcanvasBody = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  bsPrefix,
  as: Component = "div",
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "offcanvas-body");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    className: classnamesExports(className, bsPrefix),
    ...props
  });
});
OffcanvasBody.displayName = "OffcanvasBody";
const transitionStyles = {
  [ENTERING]: "show",
  [ENTERED]: "show"
};
const OffcanvasToggling = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  children,
  in: inProp = false,
  mountOnEnter = false,
  unmountOnExit = false,
  appear = false,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "offcanvas");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(TransitionWrapper, {
    ref,
    addEndListener: transitionEndListener,
    in: inProp,
    mountOnEnter,
    unmountOnExit,
    appear,
    ...props,
    childRef: getChildRef(children),
    children: (status, innerProps) => /* @__PURE__ */ reactExports.cloneElement(children, {
      ...innerProps,
      className: classnamesExports(className, children.props.className, (status === ENTERING || status === EXITING) && `${bsPrefix}-toggling`, transitionStyles[status])
    })
  });
});
OffcanvasToggling.displayName = "OffcanvasToggling";
const OffcanvasHeader = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  closeLabel = "Close",
  closeButton = false,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "offcanvas-header");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(AbstractModalHeader, {
    ref,
    ...props,
    className: classnamesExports(className, bsPrefix),
    closeLabel,
    closeButton
  });
});
OffcanvasHeader.displayName = "OffcanvasHeader";
const DivStyledAsH5 = divWithClassName("h5");
const OffcanvasTitle = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  bsPrefix,
  as: Component = DivStyledAsH5,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "offcanvas-title");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    className: classnamesExports(className, bsPrefix),
    ...props
  });
});
OffcanvasTitle.displayName = "OffcanvasTitle";
function DialogTransition(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(OffcanvasToggling, {
    ...props
  });
}
function BackdropTransition(props) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Fade, {
    ...props
  });
}
const Offcanvas = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  children,
  "aria-labelledby": ariaLabelledby,
  placement = "start",
  responsive,
  /* BaseModal props */
  show = false,
  backdrop = true,
  keyboard = true,
  scroll = false,
  onEscapeKeyDown,
  onShow,
  onHide: onHide2,
  container,
  autoFocus = true,
  enforceFocus = true,
  restoreFocus = true,
  restoreFocusOptions,
  onEntered,
  onExit,
  onExiting,
  onEnter,
  onEntering,
  onExited,
  backdropClassName,
  manager: propsManager,
  renderStaticNode = false,
  ...props
}, ref) => {
  const modalManager = reactExports.useRef();
  bsPrefix = useBootstrapPrefix(bsPrefix, "offcanvas");
  const [showOffcanvas, setShowOffcanvas] = reactExports.useState(false);
  const handleHide = useEventCallback$1(onHide2);
  const hideResponsiveOffcanvas = useBreakpoint(responsive || "xs", "up");
  reactExports.useEffect(() => {
    setShowOffcanvas(responsive ? show && !hideResponsiveOffcanvas : show);
  }, [show, responsive, hideResponsiveOffcanvas]);
  const modalContext = reactExports.useMemo(() => ({
    onHide: handleHide
  }), [handleHide]);
  function getModalManager() {
    if (propsManager) return propsManager;
    if (scroll) {
      if (!modalManager.current) modalManager.current = new BootstrapModalManager({
        handleContainerOverflow: false
      });
      return modalManager.current;
    }
    return getSharedManager();
  }
  const handleEnter = (node, ...args) => {
    if (node) node.style.visibility = "visible";
    onEnter == null || onEnter(node, ...args);
  };
  const handleExited = (node, ...args) => {
    if (node) node.style.visibility = "";
    onExited == null || onExited(...args);
  };
  const renderBackdrop = reactExports.useCallback((backdropProps) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
    ...backdropProps,
    className: classnamesExports(`${bsPrefix}-backdrop`, backdropClassName)
  }), [backdropClassName, bsPrefix]);
  const renderDialog = (dialogProps) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
    ...dialogProps,
    ...props,
    className: classnamesExports(className, responsive ? `${bsPrefix}-${responsive}` : bsPrefix, `${bsPrefix}-${placement}`),
    "aria-labelledby": ariaLabelledby,
    children
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, {
    children: [!showOffcanvas && (responsive || renderStaticNode) && renderDialog({}), /* @__PURE__ */ jsxRuntimeExports.jsx(ModalContext.Provider, {
      value: modalContext,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(BaseModal, {
        show: showOffcanvas,
        ref,
        backdrop,
        container,
        keyboard,
        autoFocus,
        enforceFocus: enforceFocus && !scroll,
        restoreFocus,
        restoreFocusOptions,
        onEscapeKeyDown,
        onShow,
        onHide: handleHide,
        onEnter: handleEnter,
        onEntering,
        onEntered,
        onExit,
        onExiting,
        onExited: handleExited,
        manager: getModalManager(),
        transition: DialogTransition,
        backdropTransition: BackdropTransition,
        renderBackdrop,
        renderDialog
      })
    })]
  });
});
Offcanvas.displayName = "Offcanvas";
const Offcanvas$1 = Object.assign(Offcanvas, {
  Body: OffcanvasBody,
  Header: OffcanvasHeader,
  Title: OffcanvasTitle
});
const NavbarOffcanvas = /* @__PURE__ */ reactExports.forwardRef(({
  onHide: onHide2,
  ...props
}, ref) => {
  const context$12 = reactExports.useContext(context);
  const handleHide = useEventCallback$1(() => {
    context$12 == null || context$12.onToggle == null || context$12.onToggle();
    onHide2 == null || onHide2();
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Offcanvas$1, {
    ref,
    show: !!(context$12 != null && context$12.expanded),
    ...props,
    renderStaticNode: true,
    onHide: handleHide
  });
});
NavbarOffcanvas.displayName = "NavbarOffcanvas";
const NavbarText = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  bsPrefix,
  as: Component = "span",
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "navbar-text");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    className: classnamesExports(className, bsPrefix),
    ...props
  });
});
NavbarText.displayName = "NavbarText";
const Navbar = /* @__PURE__ */ reactExports.forwardRef((props, ref) => {
  const {
    bsPrefix: initialBsPrefix,
    expand = true,
    variant = "light",
    bg,
    fixed,
    sticky,
    className,
    // Need to define the default "as" during prop destructuring to be compatible with styled-components github.com/react-bootstrap/react-bootstrap/issues/3595
    as: Component = "nav",
    expanded,
    onToggle,
    onSelect,
    collapseOnSelect = false,
    ...controlledProps
  } = useUncontrolled(props, {
    expanded: "onToggle"
  });
  const bsPrefix = useBootstrapPrefix(initialBsPrefix, "navbar");
  const handleCollapse = reactExports.useCallback((...args) => {
    onSelect == null || onSelect(...args);
    if (collapseOnSelect && expanded) {
      onToggle == null || onToggle(false);
    }
  }, [onSelect, collapseOnSelect, expanded, onToggle]);
  if (controlledProps.role === void 0 && Component !== "nav") {
    controlledProps.role = "navigation";
  }
  let expandClass = `${bsPrefix}-expand`;
  if (typeof expand === "string") expandClass = `${expandClass}-${expand}`;
  const navbarContext = reactExports.useMemo(() => ({
    onToggle: () => onToggle == null ? void 0 : onToggle(!expanded),
    bsPrefix,
    expanded: !!expanded,
    expand
  }), [bsPrefix, expanded, expand, onToggle]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(context.Provider, {
    value: navbarContext,
    children: /* @__PURE__ */ jsxRuntimeExports.jsx(SelectableContext.Provider, {
      value: handleCollapse,
      children: /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
        ref,
        ...controlledProps,
        className: classnamesExports(className, bsPrefix, expand && expandClass, variant && `${bsPrefix}-${variant}`, bg && `bg-${bg}`, sticky && `sticky-${sticky}`, fixed && `fixed-${fixed}`)
      })
    })
  });
});
Navbar.displayName = "Navbar";
const Navbar$1 = Object.assign(Navbar, {
  Brand: NavbarBrand,
  Collapse: NavbarCollapse,
  Offcanvas: NavbarOffcanvas,
  Text: NavbarText,
  Toggle: NavbarToggle
});
const fadeStyles = {
  [ENTERING]: "showing",
  [EXITING]: "showing show"
};
const ToastFade = /* @__PURE__ */ reactExports.forwardRef((props, ref) => /* @__PURE__ */ jsxRuntimeExports.jsx(Fade, {
  ...props,
  ref,
  transitionClasses: fadeStyles
}));
ToastFade.displayName = "ToastFade";
const ToastContext = /* @__PURE__ */ reactExports.createContext({
  onClose() {
  }
});
const ToastHeader = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  closeLabel = "Close",
  closeVariant,
  closeButton = true,
  className,
  children,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast-header");
  const context2 = reactExports.useContext(ToastContext);
  const handleClick = useEventCallback$1((e2) => {
    context2 == null || context2.onClose == null || context2.onClose(e2);
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", {
    ref,
    ...props,
    className: classnamesExports(bsPrefix, className),
    children: [children, closeButton && /* @__PURE__ */ jsxRuntimeExports.jsx(CloseButton, {
      "aria-label": closeLabel,
      variant: closeVariant,
      onClick: handleClick,
      "data-dismiss": "toast"
    })]
  });
});
ToastHeader.displayName = "ToastHeader";
const ToastBody = /* @__PURE__ */ reactExports.forwardRef(({
  className,
  bsPrefix,
  as: Component = "div",
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast-body");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    className: classnamesExports(className, bsPrefix),
    ...props
  });
});
ToastBody.displayName = "ToastBody";
const Toast = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  className,
  transition: Transition2 = ToastFade,
  show = true,
  animation = true,
  delay = 5e3,
  autohide = false,
  onClose,
  onEntered,
  onExit,
  onExiting,
  onEnter,
  onEntering,
  onExited,
  bg,
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast");
  const delayRef = reactExports.useRef(delay);
  const onCloseRef = reactExports.useRef(onClose);
  reactExports.useEffect(() => {
    delayRef.current = delay;
    onCloseRef.current = onClose;
  }, [delay, onClose]);
  const autohideTimeout = useTimeout();
  const autohideToast = !!(autohide && show);
  const autohideFunc = reactExports.useCallback(() => {
    if (autohideToast) {
      onCloseRef.current == null || onCloseRef.current();
    }
  }, [autohideToast]);
  reactExports.useEffect(() => {
    autohideTimeout.set(autohideFunc, delayRef.current);
  }, [autohideTimeout, autohideFunc]);
  const toastContext = reactExports.useMemo(() => ({
    onClose
  }), [onClose]);
  const hasAnimation = !!(Transition2 && animation);
  const toast = /* @__PURE__ */ jsxRuntimeExports.jsx("div", {
    ...props,
    ref,
    className: classnamesExports(bsPrefix, className, bg && `bg-${bg}`, !hasAnimation && (show ? "show" : "hide")),
    role: "alert",
    "aria-live": "assertive",
    "aria-atomic": "true"
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToastContext.Provider, {
    value: toastContext,
    children: hasAnimation && Transition2 ? /* @__PURE__ */ jsxRuntimeExports.jsx(Transition2, {
      in: show,
      onEnter,
      onEntering,
      onEntered,
      onExit,
      onExiting,
      onExited,
      unmountOnExit: true,
      children: toast
    }) : toast
  });
});
Toast.displayName = "Toast";
const Toast$1 = Object.assign(Toast, {
  Body: ToastBody,
  Header: ToastHeader
});
const positionClasses = {
  "top-start": "top-0 start-0",
  "top-center": "top-0 start-50 translate-middle-x",
  "top-end": "top-0 end-0",
  "middle-start": "top-50 start-0 translate-middle-y",
  "middle-center": "top-50 start-50 translate-middle",
  "middle-end": "top-50 end-0 translate-middle-y",
  "bottom-start": "bottom-0 start-0",
  "bottom-center": "bottom-0 start-50 translate-middle-x",
  "bottom-end": "bottom-0 end-0"
};
const ToastContainer = /* @__PURE__ */ reactExports.forwardRef(({
  bsPrefix,
  position: position2,
  containerPosition,
  className,
  // Need to define the default "as" during prop destructuring to be compatible with styled-components github.com/react-bootstrap/react-bootstrap/issues/3595
  as: Component = "div",
  ...props
}, ref) => {
  bsPrefix = useBootstrapPrefix(bsPrefix, "toast-container");
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {
    ref,
    ...props,
    className: classnamesExports(bsPrefix, position2 && positionClasses[position2], containerPosition && `position-${containerPosition}`, className)
  });
});
ToastContainer.displayName = "ToastContainer";
const DEFAULT_TOKENS$1 = {
  color: {
    surface: { $value: "#f8f9fa" },
    text: { $value: "#212529" },
    accent: { $value: "#0d6efd" }
  }
};
function AppNavbar() {
  const { themeName, toggleTheme } = useTheme();
  const { plugins } = usePlugins();
  const [tokens, setTokens] = reactExports.useState(DEFAULT_TOKENS$1);
  const [error, setError] = reactExports.useState(false);
  reactExports.useEffect(() => {
    let isMounted = true;
    const loadTokens = async () => {
      const t2 = await resolveTokens() || {};
      const merged = {
        ...DEFAULT_TOKENS$1,
        ...t2,
        color: { ...DEFAULT_TOKENS$1.color, ...t2?.color || {} }
      };
      if (isMounted) {
        setTokens(merged);
        setError(Object.keys(t2).length === 0);
      }
    };
    loadTokens();
    return () => {
      isMounted = false;
    };
  }, []);
  const surface = tokens.color?.surface?.$value ?? DEFAULT_TOKENS$1.color.surface.$value;
  const text2 = tokens.color?.text?.$value ?? DEFAULT_TOKENS$1.color.text.$value;
  const accent = tokens.color?.accent?.$value ?? DEFAULT_TOKENS$1.color.accent.$value;
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Navbar$1, { expand: "lg", className: "mb-3", style: { backgroundColor: surface, color: text2 }, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Container, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Navbar$1.Brand, { as: Link, to: "/", style: { color: text2 }, children: "FreeKDS" }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ms-2 text-warning small", children: "Default theme applied" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Navbar$1.Toggle, { "aria-controls": "app-navbar" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Navbar$1.Collapse, { id: "app-navbar", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs(Nav$1, { className: "me-auto", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(Nav$1.Link, { as: Link, to: "/dashboard", children: "Dashboard" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(Nav$1.Link, { as: Link, to: "/admin", children: "Admin" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        Button,
        {
          onClick: toggleTheme,
          style: { backgroundColor: accent, borderColor: accent },
          children: themeName === "light" ? "Dark Mode" : "Light Mode"
        }
      )
    ] })
  ] }) });
}
const DEFAULT_TOKENS = {
  color: {
    surface: { $value: "#f8f9fa" },
    text: { $value: "#212529" }
  }
};
function AppFooter() {
  const [tokens, setTokens] = reactExports.useState(DEFAULT_TOKENS);
  const [error, setError] = reactExports.useState(false);
  reactExports.useEffect(() => {
    let isMounted = true;
    const loadTokens = async () => {
      const t2 = await resolveTokens() || {};
      const merged = {
        ...DEFAULT_TOKENS,
        ...t2,
        color: { ...DEFAULT_TOKENS.color, ...t2?.color || {} }
      };
      if (isMounted) {
        setTokens(merged);
        setError(Object.keys(t2).length === 0);
      }
    };
    loadTokens();
    return () => {
      isMounted = false;
    };
  }, []);
  const background = tokens.color?.surface?.$value ?? DEFAULT_TOKENS.color.surface.$value;
  const text2 = tokens.color?.text?.$value ?? DEFAULT_TOKENS.color.text.$value;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(
    "footer",
    {
      style: { backgroundColor: background, color: text2 },
      className: "text-center py-3 mt-auto",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("small", { children: [
          "© ",
          (/* @__PURE__ */ new Date()).getFullYear(),
          " FreeKDS"
        ] }),
        error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-warning small", children: "Default theme applied" })
      ]
    }
  );
}
function BaseLayout({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex flex-column min-vh-100", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(TokenCSSVariables, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(AppNavbar, {}),
    children ? children : /* @__PURE__ */ jsxRuntimeExports.jsx(Outlet, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(AppFooter, {})
  ] }) });
}
function LayoutWrap({ children }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(PluginProvider, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(BaseLayout, { children }) });
}
const $schema$4 = "../../../schemas/plugin-manifest.schema@1.1.0.json";
const id$4 = "feature-flags";
const name$4 = "Feature Flags";
const version$4 = "1.0.0";
const main$4 = "./index.js";
const route$4 = "/admin/flags";
const license$4 = "MIT";
const coreAPI$4 = "1.0.0";
const activationEvents$4 = ["onStartupFinished", "onShiftChange"];
const contributes$4 = { "actions": [], "routes": [], "transforms": [], "shortcuts": [], "adminPanels": [{ "id": "feature-flags", "title": "Feature Flags", "requiredDomains": [], "requiredScopes": [], "latency": "interactive" }] };
const __vite_glob_0_0 = {
  $schema: $schema$4,
  id: id$4,
  name: name$4,
  version: version$4,
  main: main$4,
  route: route$4,
  license: license$4,
  coreAPI: coreAPI$4,
  activationEvents: activationEvents$4,
  contributes: contributes$4
};
const $schema$3 = "../../../schemas/plugin-manifest.schema@1.1.0.json";
const id$3 = "kitchen-presets";
const name$3 = "Kitchen Presets";
const version$3 = "1.0.0";
const main$3 = "./index.js";
const route$3 = "/admin/kitchen-presets";
const license$3 = "MIT";
const coreAPI$3 = "1.0.0";
const activationEvents$3 = ["onStartupFinished", "onShiftChange"];
const contributes$3 = { "actions": [], "routes": [], "transforms": [], "shortcuts": [], "adminPanels": [{ "id": "kitchen-presets", "title": "Kitchen Presets", "requiredDomains": ["layout"], "requiredScopes": ["layout:write"], "latency": "interactive" }] };
const __vite_glob_0_1 = {
  $schema: $schema$3,
  id: id$3,
  name: name$3,
  version: version$3,
  main: main$3,
  route: route$3,
  license: license$3,
  coreAPI: coreAPI$3,
  activationEvents: activationEvents$3,
  contributes: contributes$3
};
const $schema$2 = "../../../schemas/plugin-manifest.schema@1.1.0.json";
const id$2 = "layout-admin";
const name$2 = "Layout Administration";
const version$2 = "1.0.0";
const main$2 = "./index.js";
const route$2 = "/admin/layouts";
const license$2 = "MIT";
const coreAPI$2 = "1.0.0";
const activationEvents$2 = ["onStartupFinished", "onShiftChange"];
const contributes$2 = { "actions": [], "routes": [], "transforms": [], "shortcuts": [], "adminPanels": [{ "id": "layouts", "title": "Layouts", "requiredDomains": ["layouts"], "requiredScopes": ["layouts:read", "layouts:write"], "latency": "interactive" }] };
const __vite_glob_0_2 = {
  $schema: $schema$2,
  id: id$2,
  name: name$2,
  version: version$2,
  main: main$2,
  route: route$2,
  license: license$2,
  coreAPI: coreAPI$2,
  activationEvents: activationEvents$2,
  contributes: contributes$2
};
const $schema$1 = "../../../schemas/plugin-manifest.schema@1.1.0.json";
const id$1 = "menu-admin";
const name$1 = "Menu Administration";
const version$1 = "1.0.0";
const main$1 = "./index.js";
const route$1 = "/admin/menu";
const license$1 = "MIT";
const coreAPI$1 = "1.0.0";
const activationEvents$1 = ["onStartupFinished", "onShiftChange"];
const contributes$1 = { "actions": [], "routes": [], "transforms": [], "shortcuts": [], "adminPanels": [{ "id": "menu", "title": "Menu", "requiredDomains": ["menu"], "requiredScopes": ["menu:read", "menu:write"], "latency": "interactive" }] };
const __vite_glob_0_3 = {
  $schema: $schema$1,
  id: id$1,
  name: name$1,
  version: version$1,
  main: main$1,
  route: route$1,
  license: license$1,
  coreAPI: coreAPI$1,
  activationEvents: activationEvents$1,
  contributes: contributes$1
};
const $schema = "../../../schemas/plugin-manifest.schema@1.1.0.json";
const id = "theme-editor";
const name = "Theme Editor";
const version = "1.0.0";
const main = "./index.js";
const route = "/admin/theme";
const license = "MIT";
const coreAPI = "1.0.0";
const activationEvents = ["onStartupFinished"];
const contributes = { "actions": [], "routes": [], "transforms": [], "shortcuts": [], "adminPanels": [{ "id": "theme-editor", "title": "Theme Editor", "requiredDomains": ["tokens"], "requiredScopes": ["tokens:write"], "latency": "interactive" }] };
const __vite_glob_0_4 = {
  $schema,
  id,
  name,
  version,
  main,
  route,
  license,
  coreAPI,
  activationEvents,
  contributes
};
async function loadPlugins() {
  const manifests = /* @__PURE__ */ Object.assign({
    "./feature-flags/plugin.json": __vite_glob_0_0,
    "./kitchen-presets/plugin.json": __vite_glob_0_1,
    "./layout-admin/plugin.json": __vite_glob_0_2,
    "./menu-admin/plugin.json": __vite_glob_0_3,
    "./theme-editor/plugin.json": __vite_glob_0_4
  });
  const plugins = await Promise.all(
    Object.entries(manifests).map(async ([path, manifest]) => {
      const base = path.replace(/plugin\.json$/, "");
      try {
        const mod = await __vitePreload(() => import(
          /* @vite-ignore */
          `${base}${manifest.main}`
        ), true ? [] : void 0);
        if (typeof mod.default !== "function") {
          console.error(
            `Plugin missing valid default export: ${manifest.id || path}`
          );
          return null;
        }
        const contributes2 = {
          actions: [],
          routes: [],
          transforms: [],
          shortcuts: [],
          adminPanels: [],
          ...manifest.contributes || {}
        };
        return {
          Component: mod.default,
          meta: { ...manifest, ...mod.meta || {}, contributes: contributes2 }
        };
      } catch (err) {
        console.error(`Failed to load plugin: ${manifest.id || path}`, err);
        return null;
      }
    })
  );
  return plugins.filter(Boolean);
}
const bus = new EventTarget();
function on(event, handler) {
  bus.addEventListener(event, handler);
  return () => bus.removeEventListener(event, handler);
}
const PluginContext = reactExports.createContext({ plugins: [], zones: {}, contributions: {} });
function PluginProvider({ children }) {
  const [plugins, setPlugins] = reactExports.useState([]);
  reactExports.useEffect(() => {
    let active = true;
    async function load() {
      try {
        const loaded = await loadPlugins();
        if (active) {
          setPlugins(loaded);
        }
      } catch (err) {
        console.error("Failed to load plugins", err);
        if (active) {
          setPlugins([]);
        }
      }
    }
    load();
    const unsub = on("config-updated", load);
    return () => {
      active = false;
      unsub();
    };
  }, []);
  const value = reactExports.useMemo(() => {
    const zones = {};
    const contributions = {
      actions: [],
      routes: [],
      transforms: [],
      shortcuts: [],
      adminPanels: []
    };
    plugins.forEach((plugin) => {
      const { meta } = plugin;
      const zone = meta?.zone;
      if (zone) {
        zones[zone] = zones[zone] || [];
        zones[zone].push(plugin);
      }
      const contrib = meta?.contributes || {};
      Object.keys(contributions).forEach((key) => {
        (contrib[key] || []).forEach((item) => contributions[key].push(item));
      });
    });
    return { plugins, zones, contributions };
  }, [plugins]);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(PluginContext.Provider, { value, children });
}
function usePlugins() {
  return reactExports.useContext(PluginContext);
}
function PluginZone({ zone }) {
  const { zones } = usePlugins();
  const zonePlugins = (zones[zone] || []).filter(
    (p) => p && typeof p.Component === "function" && p.meta && p.meta.id
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsx(jsxRuntimeExports.Fragment, { children: zonePlugins.map(({ Component, meta }) => /* @__PURE__ */ jsxRuntimeExports.jsx(Component, {}, meta.id)) });
}
function MenuPanel() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Menu administration panel" });
}
MenuPanel.meta = {
  id: "menu",
  title: "Menu",
  dataDomain: "menu",
  scopes: ["menu:read", "menu:write"],
  latencyClass: "interactive"
};
function StationsPanel() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Stations configuration panel" });
}
StationsPanel.meta = {
  id: "stations",
  title: "Stations",
  dataDomain: "stations",
  scopes: ["stations:read", "stations:write"],
  latencyClass: "deferred"
};
function InventoryPanel() {
  const [data, setData] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    let active = true;
    (async () => {
      try {
        const res = await fetch("/api/admin");
        if (!res.ok) throw new Error("Failed to load inventory");
        const json = await res.json();
        if (active) setData(json);
      } catch (e2) {
        if (active) setError(e2.message || "Error");
      } finally {
        if (active) setLoading(false);
      }
    })();
    return () => {
      active = false;
    };
  }, []);
  const summary = reactExports.useMemo(() => {
    if (!data) return [];
    return [
      { label: "Ingredients", value: data.ingredients?.length || 0 },
      { label: "Suppliers", value: data.suppliers?.length || 0 },
      { label: "Purchase Orders", value: data.orders?.length || 0 },
      { label: "Locations", value: data.locations?.length || 0 },
      { label: "Units", value: data.units?.length || 0 },
      { label: "Tags", value: data.tags?.length || 0 }
    ];
  }, [data]);
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  if (error) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error });
  if (!data) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-header", children: /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "m-0", children: "Inventory" }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-grid", children: summary.map((s3) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-surface p-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted small", children: s3.label }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fs-3 fw-bold", children: s3.value })
    ] }, s3.label)) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section mt-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "mb-3", children: "Ingredients" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm align-middle", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Name" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Qty" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Unit" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "SKU" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Cost" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: (data.ingredients || []).slice(0, 50).map((ing) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.quantity }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.unit }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.sku || "-" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.cost ?? "-" })
        ] }, ing.id)) })
      ] }) }),
      data.ingredients && data.ingredients.length > 50 && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted small", children: "Showing first 50…" })
    ] })
  ] });
}
InventoryPanel.meta = {
  id: "inventory",
  title: "Inventory",
  dataDomain: "inventory",
  scopes: ["inventory:read", "inventory:write"],
  latencyClass: "interactive"
};
const corePanels = {
  menu: MenuPanel,
  stations: StationsPanel,
  inventory: InventoryPanel
};
async function discoverPanels(plugins = []) {
  let data = {};
  try {
    const res = await fetch("/api/modules");
    if (res.ok) data = await res.json();
  } catch {
  }
  const groups = Array.isArray(data.groups) ? data.groups : [];
  const categories = groups.map((g) => ({ category: g.category, panels: [] }));
  const catMap = new Map(categories.map((g) => [g.category, g]));
  groups.forEach((g) => {
    g.modules.forEach((m3) => {
      const Component = corePanels[m3];
      const meta = Component?.meta;
      if (meta && typeof meta.dataDomain === "string" && Array.isArray(meta.scopes) && meta.latencyClass) {
        catMap.get(g.category).panels.push({
          id: meta.id || m3,
          title: meta.title || m3,
          Component
        });
      }
    });
  });
  plugins.forEach(({ meta }) => {
    const contrib = meta?.contributes?.adminPanels || [];
    contrib.forEach((p) => {
      const Component = p.Component;
      const meta2 = Component?.meta;
      if (!meta2 || typeof meta2.dataDomain !== "string" || !Array.isArray(meta2.scopes) || !meta2.latencyClass)
        return;
      const cat = p.category || "admin";
      let group = catMap.get(cat);
      if (!group) {
        group = { category: cat, panels: [] };
        catMap.set(cat, group);
        categories.push(group);
      }
      group.panels.push({
        id: p.id || meta2.id,
        title: p.title || meta2.title,
        Component,
        getProps: p.getProps,
        props: p.props
      });
    });
  });
  return categories;
}
function AdminPanels() {
  const { plugins } = usePlugins();
  const [groups, setGroups] = reactExports.useState([]);
  const [active, setActive] = reactExports.useState({});
  reactExports.useEffect(() => {
    async function load() {
      const g = await discoverPanels(plugins);
      setGroups(g);
      setActive((prev2) => {
        const next = { ...prev2 };
        g.forEach((group) => {
          if (!next[group.category] && group.panels.length) {
            next[group.category] = group.panels[0].id;
          }
        });
        return next;
      });
    }
    load();
    const off = on("config-updated", load);
    return off;
  }, [plugins]);
  const tabRefs = reactExports.useRef({});
  const onKeyDown = (e2, group, idx) => {
    if (e2.key === "ArrowRight" || e2.key === "ArrowLeft") {
      e2.preventDefault();
      const dir = e2.key === "ArrowRight" ? 1 : -1;
      const panels = group.panels;
      const newIndex = (idx + dir + panels.length) % panels.length;
      const newId = panels[newIndex].id;
      tabRefs.current[newId]?.focus();
      setActive((prev2) => ({ ...prev2, [group.category]: newId }));
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: groups.map((group) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mt-3 text-capitalize", children: group.category }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "tablist", "aria-label": `${group.category} panels`, children: group.panels.map((p, idx) => /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        ref: (el) => tabRefs.current[p.id] = el,
        role: "tab",
        className: "admin-panel-tab btn btn-outline-primary me-2",
        "aria-selected": active[group.category] === p.id,
        tabIndex: active[group.category] === p.id ? 0 : -1,
        onClick: () => setActive((prev2) => ({ ...prev2, [group.category]: p.id })),
        onKeyDown: (e2) => onKeyDown(e2, group, idx),
        children: p.title
      },
      p.id
    )) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { role: "tabpanel", tabIndex: 0, "aria-live": "polite", children: group.panels.map((p) => {
      if (active[group.category] !== p.id) return null;
      const props = typeof p.getProps === "function" ? p.getProps() : p.props || {};
      const Panel = p.Component;
      return /* @__PURE__ */ jsxRuntimeExports.jsx(Panel, { ...props }, p.id);
    }) })
  ] }, group.category)) });
}
function NavItem({ to, children }) {
  const loc = useLocation();
  const active = loc.pathname === to;
  return /* @__PURE__ */ jsxRuntimeExports.jsx("li", { className: "nav-item", children: /* @__PURE__ */ jsxRuntimeExports.jsx(NavLink$1, { className: `nav-link ${active ? "active" : ""}`, to, children }) });
}
function AdminShell() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "container-fluid", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("aside", { className: "col-12 col-md-3 col-lg-2 mb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-surface p-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "mb-3", children: "Admin" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("ul", { className: "nav flex-column", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin", children: "Overview" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/employees", children: "Employees" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/roles", children: "Roles & Access" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/inventory", children: "Inventory" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/suppliers", children: "Suppliers" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/purchase-orders", children: "Purchase Orders" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/menu", children: "Menu" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/stations", children: "Stations" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/schedule", children: "Schedule" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/timeclock", children: "Time Clock" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/payroll", children: "Payroll" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/theme", children: "Theme" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/layouts", children: "Layouts" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/flags", children: "Feature Flags" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(NavItem, { to: "/admin/backups", children: "Backups" })
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "col-12 col-md-9 col-lg-10", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Outlet, {}) })
  ] }) });
}
function RoleSelector({ roles = [], value, onChange }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsx(
    "select",
    {
      name: "role",
      value,
      onChange,
      className: "form-select form-select-sm",
      children: roles.map((r2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: r2, children: r2 }, r2))
    }
  );
}
function EmployeeForm({ form, roles, onChange, onSubmit, editIndex, resetForm }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit, className: "mb-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-2 mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "first_name",
          value: form.first_name,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "First Name"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "last_name",
          value: form.last_name,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Last Name"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "position",
          value: form.position,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Position"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "date",
          name: "start_date",
          value: form.start_date,
          onChange,
          className: "form-control form-control-sm"
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-2 mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "email",
          name: "email",
          value: form.email,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Email"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "phone",
          value: form.phone,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Phone"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "number",
          step: "0.01",
          name: "wage_rate",
          value: form.wage_rate,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Wage Rate"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "username",
          value: form.username,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Username"
        }
      ) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-2 mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "password",
          name: "password",
          value: form.password,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "Password",
          autoComplete: "new-password"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "password",
          name: "pin",
          value: form.pin,
          onChange,
          className: "form-control form-control-sm",
          placeholder: "PIN (4-6 digits)",
          autoComplete: "new-password",
          pattern: "\\d{4,6}"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-sm-4", children: /* @__PURE__ */ jsxRuntimeExports.jsx(RoleSelector, { roles, value: form.role, onChange }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", className: "btn btn-primary btn-sm", children: editIndex >= 0 ? "Update" : "Save" }),
    editIndex >= 0 && /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", className: "btn btn-secondary btn-sm ms-2", onClick: resetForm, children: "Cancel" })
  ] });
}
const EMPTY_FORM = {
  first_name: "",
  last_name: "",
  position: "",
  start_date: "",
  email: "",
  phone: "",
  wage_rate: "",
  username: "",
  password: "",
  pin: "",
  role: ""
};
function useEmployeeForm(defaultRole2 = "") {
  const [form, setForm] = reactExports.useState({ ...EMPTY_FORM, role: defaultRole2 });
  const [editIndex, setEditIndex] = reactExports.useState(-1);
  reactExports.useEffect(() => {
    setForm((f) => ({ ...f, role: defaultRole2 }));
  }, [defaultRole2]);
  const resetForm = reactExports.useCallback(() => {
    setForm({ ...EMPTY_FORM, role: defaultRole2 });
    setEditIndex(-1);
  }, [defaultRole2]);
  const handleChange = reactExports.useCallback((e2) => {
    const { name: name2, value } = e2.target;
    setForm((f) => ({ ...f, [name2]: value }));
  }, []);
  return { form, setForm, editIndex, setEditIndex, resetForm, handleChange };
}
function OnboardingApp() {
  const [roles, setRoles] = reactExports.useState([]);
  const [employees, setEmployees] = reactExports.useState([]);
  const { form, setForm, editIndex, setEditIndex, resetForm, handleChange } = useEmployeeForm(roles[0]);
  const [search, setSearch] = reactExports.useState("");
  reactExports.useEffect(() => {
    async function load() {
      try {
        const [empRes, roleRes] = await Promise.all([
          fetch("/api/employees"),
          fetch("/api/hierarchy")
        ]);
        const empJson = await empRes.json();
        const roleJson = await roleRes.json();
        setEmployees(empJson.employees || []);
        setRoles(roleJson.hierarchy || []);
      } catch (err) {
        console.error(err);
      }
    }
    load();
  }, []);
  const handleSubmit = async (e2) => {
    e2.preventDefault();
    const list = [...employees];
    if (editIndex >= 0) list[editIndex] = { ...list[editIndex], ...form };
    else list.push({ ...form, id: Date.now() });
    setEmployees(list);
    resetForm();
    try {
      await fetch("/api/employees", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ employees: list })
      });
    } catch (err) {
      console.error(err);
    }
  };
  const startEdit = (idx) => {
    const emp = employees[idx];
    if (!emp) return;
    setEditIndex(idx);
    setForm({
      first_name: emp.first_name || "",
      last_name: emp.last_name || "",
      position: emp.position || "",
      start_date: emp.start_date || "",
      email: emp.email || "",
      phone: emp.phone || "",
      wage_rate: emp.wage_rate || "",
      username: emp.username || "",
      password: "",
      pin: "",
      role: emp.role || roles[0] || ""
    });
  };
  const filtered = employees.filter((e2) => {
    const f = search.toLowerCase();
    return e2.first_name && e2.first_name.toLowerCase().includes(f) || e2.last_name && e2.last_name.toLowerCase().includes(f) || e2.position && e2.position.toLowerCase().includes(f) || e2.role && e2.role.toLowerCase().includes(f);
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      EmployeeForm,
      {
        form,
        roles,
        onChange: handleChange,
        onSubmit: handleSubmit,
        editIndex,
        resetForm
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-4", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { children: "Current Employees" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          value: search,
          onChange: (e2) => setSearch(e2.target.value),
          className: "form-control form-control-sm mb-2",
          placeholder: "Search employees"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "First" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Last" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Position" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Start" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Email" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Phone" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Wage" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Username" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Role" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", {})
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: filtered.map((e2, i2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.first_name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.last_name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.position }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.start_date }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.email }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.phone }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.wage_rate }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.username }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: e2.role }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: "btn btn-sm btn-outline-primary",
              onClick: () => startEdit(i2),
              children: "Edit"
            }
          ) })
        ] }, e2.id || i2)) })
      ] }) })
    ] })
  ] });
}
function EmployeesRoute() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Employees" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(OnboardingApp, {})
  ] });
}
function useCombinedRefs() {
  for (var _len = arguments.length, refs = new Array(_len), _key = 0; _key < _len; _key++) {
    refs[_key] = arguments[_key];
  }
  return reactExports.useMemo(
    () => (node) => {
      refs.forEach((ref) => ref(node));
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    refs
  );
}
const canUseDOM = typeof window !== "undefined" && typeof window.document !== "undefined" && typeof window.document.createElement !== "undefined";
function isWindow$1(element) {
  const elementString = Object.prototype.toString.call(element);
  return elementString === "[object Window]" || // In Electron context the Window object serializes to [object global]
  elementString === "[object global]";
}
function isNode(node) {
  return "nodeType" in node;
}
function getWindow(target) {
  var _target$ownerDocument, _target$ownerDocument2;
  if (!target) {
    return window;
  }
  if (isWindow$1(target)) {
    return target;
  }
  if (!isNode(target)) {
    return window;
  }
  return (_target$ownerDocument = (_target$ownerDocument2 = target.ownerDocument) == null ? void 0 : _target$ownerDocument2.defaultView) != null ? _target$ownerDocument : window;
}
function isDocument$1(node) {
  const {
    Document
  } = getWindow(node);
  return node instanceof Document;
}
function isHTMLElement$1(node) {
  if (isWindow$1(node)) {
    return false;
  }
  return node instanceof getWindow(node).HTMLElement;
}
function isSVGElement(node) {
  return node instanceof getWindow(node).SVGElement;
}
function getOwnerDocument(target) {
  if (!target) {
    return document;
  }
  if (isWindow$1(target)) {
    return target.document;
  }
  if (!isNode(target)) {
    return document;
  }
  if (isDocument$1(target)) {
    return target;
  }
  if (isHTMLElement$1(target) || isSVGElement(target)) {
    return target.ownerDocument;
  }
  return document;
}
const useIsomorphicLayoutEffect = canUseDOM ? reactExports.useLayoutEffect : reactExports.useEffect;
function useEvent(handler) {
  const handlerRef = reactExports.useRef(handler);
  useIsomorphicLayoutEffect(() => {
    handlerRef.current = handler;
  });
  return reactExports.useCallback(function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return handlerRef.current == null ? void 0 : handlerRef.current(...args);
  }, []);
}
function useInterval() {
  const intervalRef = reactExports.useRef(null);
  const set = reactExports.useCallback((listener, duration2) => {
    intervalRef.current = setInterval(listener, duration2);
  }, []);
  const clear2 = reactExports.useCallback(() => {
    if (intervalRef.current !== null) {
      clearInterval(intervalRef.current);
      intervalRef.current = null;
    }
  }, []);
  return [set, clear2];
}
function useLatestValue(value, dependencies) {
  if (dependencies === void 0) {
    dependencies = [value];
  }
  const valueRef = reactExports.useRef(value);
  useIsomorphicLayoutEffect(() => {
    if (valueRef.current !== value) {
      valueRef.current = value;
    }
  }, dependencies);
  return valueRef;
}
function useLazyMemo(callback, dependencies) {
  const valueRef = reactExports.useRef();
  return reactExports.useMemo(
    () => {
      const newValue = callback(valueRef.current);
      valueRef.current = newValue;
      return newValue;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...dependencies]
  );
}
function useNodeRef(onChange) {
  const onChangeHandler = useEvent(onChange);
  const node = reactExports.useRef(null);
  const setNodeRef = reactExports.useCallback(
    (element) => {
      if (element !== node.current) {
        onChangeHandler == null ? void 0 : onChangeHandler(element, node.current);
      }
      node.current = element;
    },
    //eslint-disable-next-line
    []
  );
  return [node, setNodeRef];
}
function usePrevious(value) {
  const ref = reactExports.useRef();
  reactExports.useEffect(() => {
    ref.current = value;
  }, [value]);
  return ref.current;
}
let ids = {};
function useUniqueId(prefix, value) {
  return reactExports.useMemo(() => {
    if (value) {
      return value;
    }
    const id2 = ids[prefix] == null ? 0 : ids[prefix] + 1;
    ids[prefix] = id2;
    return prefix + "-" + id2;
  }, [prefix, value]);
}
function createAdjustmentFn(modifier) {
  return function(object) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((accumulator, adjustment) => {
      const entries = Object.entries(adjustment);
      for (const [key, valueAdjustment] of entries) {
        const value = accumulator[key];
        if (value != null) {
          accumulator[key] = value + modifier * valueAdjustment;
        }
      }
      return accumulator;
    }, {
      ...object
    });
  };
}
const add$1 = /* @__PURE__ */ createAdjustmentFn(1);
const subtract$1 = /* @__PURE__ */ createAdjustmentFn(-1);
function hasViewportRelativeCoordinates(event) {
  return "clientX" in event && "clientY" in event;
}
function isKeyboardEvent(event) {
  if (!event) {
    return false;
  }
  const {
    KeyboardEvent
  } = getWindow(event.target);
  return KeyboardEvent && event instanceof KeyboardEvent;
}
function isTouchEvent(event) {
  if (!event) {
    return false;
  }
  const {
    TouchEvent
  } = getWindow(event.target);
  return TouchEvent && event instanceof TouchEvent;
}
function getEventCoordinates$1(event) {
  if (isTouchEvent(event)) {
    if (event.touches && event.touches.length) {
      const {
        clientX: x2,
        clientY: y3
      } = event.touches[0];
      return {
        x: x2,
        y: y3
      };
    } else if (event.changedTouches && event.changedTouches.length) {
      const {
        clientX: x2,
        clientY: y3
      } = event.changedTouches[0];
      return {
        x: x2,
        y: y3
      };
    }
  }
  if (hasViewportRelativeCoordinates(event)) {
    return {
      x: event.clientX,
      y: event.clientY
    };
  }
  return null;
}
const CSS = /* @__PURE__ */ Object.freeze({
  Translate: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        x: x2,
        y: y3
      } = transform;
      return "translate3d(" + (x2 ? Math.round(x2) : 0) + "px, " + (y3 ? Math.round(y3) : 0) + "px, 0)";
    }
  },
  Scale: {
    toString(transform) {
      if (!transform) {
        return;
      }
      const {
        scaleX,
        scaleY
      } = transform;
      return "scaleX(" + scaleX + ") scaleY(" + scaleY + ")";
    }
  },
  Transform: {
    toString(transform) {
      if (!transform) {
        return;
      }
      return [CSS.Translate.toString(transform), CSS.Scale.toString(transform)].join(" ");
    }
  },
  Transition: {
    toString(_ref) {
      let {
        property,
        duration: duration2,
        easing
      } = _ref;
      return property + " " + duration2 + "ms " + easing;
    }
  }
});
const SELECTOR = "a,frame,iframe,input:not([type=hidden]):not(:disabled),select:not(:disabled),textarea:not(:disabled),button:not(:disabled),*[tabindex]";
function findFirstFocusableNode(element) {
  if (element.matches(SELECTOR)) {
    return element;
  }
  return element.querySelector(SELECTOR);
}
const hiddenStyles = {
  display: "none"
};
function HiddenText(_ref) {
  let {
    id: id2,
    value
  } = _ref;
  return reactExports.createElement("div", {
    id: id2,
    style: hiddenStyles
  }, value);
}
function LiveRegion(_ref) {
  let {
    id: id2,
    announcement,
    ariaLiveType = "assertive"
  } = _ref;
  const visuallyHidden = {
    position: "fixed",
    top: 0,
    left: 0,
    width: 1,
    height: 1,
    margin: -1,
    border: 0,
    padding: 0,
    overflow: "hidden",
    clip: "rect(0 0 0 0)",
    clipPath: "inset(100%)",
    whiteSpace: "nowrap"
  };
  return reactExports.createElement("div", {
    id: id2,
    style: visuallyHidden,
    role: "status",
    "aria-live": ariaLiveType,
    "aria-atomic": true
  }, announcement);
}
function useAnnouncement() {
  const [announcement, setAnnouncement] = reactExports.useState("");
  const announce = reactExports.useCallback((value) => {
    if (value != null) {
      setAnnouncement(value);
    }
  }, []);
  return {
    announce,
    announcement
  };
}
const DndMonitorContext = /* @__PURE__ */ reactExports.createContext(null);
function useDndMonitor(listener) {
  const registerListener = reactExports.useContext(DndMonitorContext);
  reactExports.useEffect(() => {
    if (!registerListener) {
      throw new Error("useDndMonitor must be used within a children of <DndContext>");
    }
    const unsubscribe = registerListener(listener);
    return unsubscribe;
  }, [listener, registerListener]);
}
function useDndMonitorProvider() {
  const [listeners] = reactExports.useState(() => /* @__PURE__ */ new Set());
  const registerListener = reactExports.useCallback((listener) => {
    listeners.add(listener);
    return () => listeners.delete(listener);
  }, [listeners]);
  const dispatch = reactExports.useCallback((_ref) => {
    let {
      type,
      event
    } = _ref;
    listeners.forEach((listener) => {
      var _listener$type;
      return (_listener$type = listener[type]) == null ? void 0 : _listener$type.call(listener, event);
    });
  }, [listeners]);
  return [dispatch, registerListener];
}
const defaultScreenReaderInstructions = {
  draggable: "\n    To pick up a draggable item, press the space bar.\n    While dragging, use the arrow keys to move the item.\n    Press space again to drop the item in its new position, or press escape to cancel.\n  "
};
const defaultAnnouncements = {
  onDragStart(_ref) {
    let {
      active
    } = _ref;
    return "Picked up draggable item " + active.id + ".";
  },
  onDragOver(_ref2) {
    let {
      active,
      over
    } = _ref2;
    if (over) {
      return "Draggable item " + active.id + " was moved over droppable area " + over.id + ".";
    }
    return "Draggable item " + active.id + " is no longer over a droppable area.";
  },
  onDragEnd(_ref3) {
    let {
      active,
      over
    } = _ref3;
    if (over) {
      return "Draggable item " + active.id + " was dropped over droppable area " + over.id;
    }
    return "Draggable item " + active.id + " was dropped.";
  },
  onDragCancel(_ref4) {
    let {
      active
    } = _ref4;
    return "Dragging was cancelled. Draggable item " + active.id + " was dropped.";
  }
};
function Accessibility(_ref) {
  let {
    announcements = defaultAnnouncements,
    container,
    hiddenTextDescribedById,
    screenReaderInstructions = defaultScreenReaderInstructions
  } = _ref;
  const {
    announce,
    announcement
  } = useAnnouncement();
  const liveRegionId = useUniqueId("DndLiveRegion");
  const [mounted, setMounted] = reactExports.useState(false);
  reactExports.useEffect(() => {
    setMounted(true);
  }, []);
  useDndMonitor(reactExports.useMemo(() => ({
    onDragStart(_ref2) {
      let {
        active
      } = _ref2;
      announce(announcements.onDragStart({
        active
      }));
    },
    onDragMove(_ref3) {
      let {
        active,
        over
      } = _ref3;
      if (announcements.onDragMove) {
        announce(announcements.onDragMove({
          active,
          over
        }));
      }
    },
    onDragOver(_ref4) {
      let {
        active,
        over
      } = _ref4;
      announce(announcements.onDragOver({
        active,
        over
      }));
    },
    onDragEnd(_ref5) {
      let {
        active,
        over
      } = _ref5;
      announce(announcements.onDragEnd({
        active,
        over
      }));
    },
    onDragCancel(_ref6) {
      let {
        active,
        over
      } = _ref6;
      announce(announcements.onDragCancel({
        active,
        over
      }));
    }
  }), [announce, announcements]));
  if (!mounted) {
    return null;
  }
  const markup = reactExports.createElement(reactExports.Fragment, null, reactExports.createElement(HiddenText, {
    id: hiddenTextDescribedById,
    value: screenReaderInstructions.draggable
  }), reactExports.createElement(LiveRegion, {
    id: liveRegionId,
    announcement
  }));
  return container ? reactDomExports.createPortal(markup, container) : markup;
}
var Action;
(function(Action2) {
  Action2["DragStart"] = "dragStart";
  Action2["DragMove"] = "dragMove";
  Action2["DragEnd"] = "dragEnd";
  Action2["DragCancel"] = "dragCancel";
  Action2["DragOver"] = "dragOver";
  Action2["RegisterDroppable"] = "registerDroppable";
  Action2["SetDroppableDisabled"] = "setDroppableDisabled";
  Action2["UnregisterDroppable"] = "unregisterDroppable";
})(Action || (Action = {}));
function noop$1() {
}
function useSensor(sensor, options) {
  return reactExports.useMemo(
    () => ({
      sensor,
      options: options != null ? options : {}
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [sensor, options]
  );
}
function useSensors() {
  for (var _len = arguments.length, sensors = new Array(_len), _key = 0; _key < _len; _key++) {
    sensors[_key] = arguments[_key];
  }
  return reactExports.useMemo(
    () => [...sensors].filter((sensor) => sensor != null),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [...sensors]
  );
}
const defaultCoordinates = /* @__PURE__ */ Object.freeze({
  x: 0,
  y: 0
});
function distanceBetween(p1, p2) {
  return Math.sqrt(Math.pow(p1.x - p2.x, 2) + Math.pow(p1.y - p2.y, 2));
}
function sortCollisionsAsc(_ref, _ref2) {
  let {
    data: {
      value: a3
    }
  } = _ref;
  let {
    data: {
      value: b2
    }
  } = _ref2;
  return a3 - b2;
}
function sortCollisionsDesc(_ref3, _ref4) {
  let {
    data: {
      value: a3
    }
  } = _ref3;
  let {
    data: {
      value: b2
    }
  } = _ref4;
  return b2 - a3;
}
function getFirstCollision(collisions, property) {
  if (!collisions || collisions.length === 0) {
    return null;
  }
  const [firstCollision] = collisions;
  return firstCollision[property];
}
function centerOfRectangle(rect, left2, top2) {
  if (left2 === void 0) {
    left2 = rect.left;
  }
  if (top2 === void 0) {
    top2 = rect.top;
  }
  return {
    x: left2 + rect.width * 0.5,
    y: top2 + rect.height * 0.5
  };
}
const closestCenter = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const centerRect = centerOfRectangle(collisionRect, collisionRect.left, collisionRect.top);
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id: id2
    } = droppableContainer;
    const rect = droppableRects.get(id2);
    if (rect) {
      const distBetween = distanceBetween(centerOfRectangle(rect), centerRect);
      collisions.push({
        id: id2,
        data: {
          droppableContainer,
          value: distBetween
        }
      });
    }
  }
  return collisions.sort(sortCollisionsAsc);
};
function getIntersectionRatio(entry, target) {
  const top2 = Math.max(target.top, entry.top);
  const left2 = Math.max(target.left, entry.left);
  const right2 = Math.min(target.left + target.width, entry.left + entry.width);
  const bottom2 = Math.min(target.top + target.height, entry.top + entry.height);
  const width = right2 - left2;
  const height2 = bottom2 - top2;
  if (left2 < right2 && top2 < bottom2) {
    const targetArea = target.width * target.height;
    const entryArea = entry.width * entry.height;
    const intersectionArea = width * height2;
    const intersectionRatio = intersectionArea / (targetArea + entryArea - intersectionArea);
    return Number(intersectionRatio.toFixed(4));
  }
  return 0;
}
const rectIntersection = (_ref) => {
  let {
    collisionRect,
    droppableRects,
    droppableContainers
  } = _ref;
  const collisions = [];
  for (const droppableContainer of droppableContainers) {
    const {
      id: id2
    } = droppableContainer;
    const rect = droppableRects.get(id2);
    if (rect) {
      const intersectionRatio = getIntersectionRatio(rect, collisionRect);
      if (intersectionRatio > 0) {
        collisions.push({
          id: id2,
          data: {
            droppableContainer,
            value: intersectionRatio
          }
        });
      }
    }
  }
  return collisions.sort(sortCollisionsDesc);
};
function adjustScale(transform, rect1, rect2) {
  return {
    ...transform,
    scaleX: rect1 && rect2 ? rect1.width / rect2.width : 1,
    scaleY: rect1 && rect2 ? rect1.height / rect2.height : 1
  };
}
function getRectDelta(rect1, rect2) {
  return rect1 && rect2 ? {
    x: rect1.left - rect2.left,
    y: rect1.top - rect2.top
  } : defaultCoordinates;
}
function createRectAdjustmentFn(modifier) {
  return function adjustClientRect(rect) {
    for (var _len = arguments.length, adjustments = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      adjustments[_key - 1] = arguments[_key];
    }
    return adjustments.reduce((acc, adjustment) => ({
      ...acc,
      top: acc.top + modifier * adjustment.y,
      bottom: acc.bottom + modifier * adjustment.y,
      left: acc.left + modifier * adjustment.x,
      right: acc.right + modifier * adjustment.x
    }), {
      ...rect
    });
  };
}
const getAdjustedRect = /* @__PURE__ */ createRectAdjustmentFn(1);
function parseTransform(transform) {
  if (transform.startsWith("matrix3d(")) {
    const transformArray = transform.slice(9, -1).split(/, /);
    return {
      x: +transformArray[12],
      y: +transformArray[13],
      scaleX: +transformArray[0],
      scaleY: +transformArray[5]
    };
  } else if (transform.startsWith("matrix(")) {
    const transformArray = transform.slice(7, -1).split(/, /);
    return {
      x: +transformArray[4],
      y: +transformArray[5],
      scaleX: +transformArray[0],
      scaleY: +transformArray[3]
    };
  }
  return null;
}
function inverseTransform(rect, transform, transformOrigin) {
  const parsedTransform = parseTransform(transform);
  if (!parsedTransform) {
    return rect;
  }
  const {
    scaleX,
    scaleY,
    x: translateX,
    y: translateY
  } = parsedTransform;
  const x2 = rect.left - translateX - (1 - scaleX) * parseFloat(transformOrigin);
  const y3 = rect.top - translateY - (1 - scaleY) * parseFloat(transformOrigin.slice(transformOrigin.indexOf(" ") + 1));
  const w2 = scaleX ? rect.width / scaleX : rect.width;
  const h3 = scaleY ? rect.height / scaleY : rect.height;
  return {
    width: w2,
    height: h3,
    top: y3,
    right: x2 + w2,
    bottom: y3 + h3,
    left: x2
  };
}
const defaultOptions$1 = {
  ignoreTransform: false
};
function getClientRect(element, options) {
  if (options === void 0) {
    options = defaultOptions$1;
  }
  let rect = element.getBoundingClientRect();
  if (options.ignoreTransform) {
    const {
      transform,
      transformOrigin
    } = getWindow(element).getComputedStyle(element);
    if (transform) {
      rect = inverseTransform(rect, transform, transformOrigin);
    }
  }
  const {
    top: top2,
    left: left2,
    width,
    height: height2,
    bottom: bottom2,
    right: right2
  } = rect;
  return {
    top: top2,
    left: left2,
    width,
    height: height2,
    bottom: bottom2,
    right: right2
  };
}
function getTransformAgnosticClientRect(element) {
  return getClientRect(element, {
    ignoreTransform: true
  });
}
function getWindowClientRect(element) {
  const width = element.innerWidth;
  const height2 = element.innerHeight;
  return {
    top: 0,
    left: 0,
    right: width,
    bottom: height2,
    width,
    height: height2
  };
}
function isFixed(node, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(node).getComputedStyle(node);
  }
  return computedStyle.position === "fixed";
}
function isScrollable(element, computedStyle) {
  if (computedStyle === void 0) {
    computedStyle = getWindow(element).getComputedStyle(element);
  }
  const overflowRegex = /(auto|scroll|overlay)/;
  const properties2 = ["overflow", "overflowX", "overflowY"];
  return properties2.some((property) => {
    const value = computedStyle[property];
    return typeof value === "string" ? overflowRegex.test(value) : false;
  });
}
function getScrollableAncestors(element, limit) {
  const scrollParents = [];
  function findScrollableAncestors(node) {
    if (limit != null && scrollParents.length >= limit) {
      return scrollParents;
    }
    if (!node) {
      return scrollParents;
    }
    if (isDocument$1(node) && node.scrollingElement != null && !scrollParents.includes(node.scrollingElement)) {
      scrollParents.push(node.scrollingElement);
      return scrollParents;
    }
    if (!isHTMLElement$1(node) || isSVGElement(node)) {
      return scrollParents;
    }
    if (scrollParents.includes(node)) {
      return scrollParents;
    }
    const computedStyle = getWindow(element).getComputedStyle(node);
    if (node !== element) {
      if (isScrollable(node, computedStyle)) {
        scrollParents.push(node);
      }
    }
    if (isFixed(node, computedStyle)) {
      return scrollParents;
    }
    return findScrollableAncestors(node.parentNode);
  }
  if (!element) {
    return scrollParents;
  }
  return findScrollableAncestors(element);
}
function getFirstScrollableAncestor(node) {
  const [firstScrollableAncestor] = getScrollableAncestors(node, 1);
  return firstScrollableAncestor != null ? firstScrollableAncestor : null;
}
function getScrollableElement(element) {
  if (!canUseDOM || !element) {
    return null;
  }
  if (isWindow$1(element)) {
    return element;
  }
  if (!isNode(element)) {
    return null;
  }
  if (isDocument$1(element) || element === getOwnerDocument(element).scrollingElement) {
    return window;
  }
  if (isHTMLElement$1(element)) {
    return element;
  }
  return null;
}
function getScrollXCoordinate(element) {
  if (isWindow$1(element)) {
    return element.scrollX;
  }
  return element.scrollLeft;
}
function getScrollYCoordinate(element) {
  if (isWindow$1(element)) {
    return element.scrollY;
  }
  return element.scrollTop;
}
function getScrollCoordinates(element) {
  return {
    x: getScrollXCoordinate(element),
    y: getScrollYCoordinate(element)
  };
}
var Direction;
(function(Direction2) {
  Direction2[Direction2["Forward"] = 1] = "Forward";
  Direction2[Direction2["Backward"] = -1] = "Backward";
})(Direction || (Direction = {}));
function isDocumentScrollingElement(element) {
  if (!canUseDOM || !element) {
    return false;
  }
  return element === document.scrollingElement;
}
function getScrollPosition(scrollingContainer) {
  const minScroll = {
    x: 0,
    y: 0
  };
  const dimensions = isDocumentScrollingElement(scrollingContainer) ? {
    height: window.innerHeight,
    width: window.innerWidth
  } : {
    height: scrollingContainer.clientHeight,
    width: scrollingContainer.clientWidth
  };
  const maxScroll = {
    x: scrollingContainer.scrollWidth - dimensions.width,
    y: scrollingContainer.scrollHeight - dimensions.height
  };
  const isTop = scrollingContainer.scrollTop <= minScroll.y;
  const isLeft = scrollingContainer.scrollLeft <= minScroll.x;
  const isBottom = scrollingContainer.scrollTop >= maxScroll.y;
  const isRight = scrollingContainer.scrollLeft >= maxScroll.x;
  return {
    isTop,
    isLeft,
    isBottom,
    isRight,
    maxScroll,
    minScroll
  };
}
const defaultThreshold = {
  x: 0.2,
  y: 0.2
};
function getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, _ref, acceleration, thresholdPercentage) {
  let {
    top: top2,
    left: left2,
    right: right2,
    bottom: bottom2
  } = _ref;
  if (acceleration === void 0) {
    acceleration = 10;
  }
  if (thresholdPercentage === void 0) {
    thresholdPercentage = defaultThreshold;
  }
  const {
    isTop,
    isBottom,
    isLeft,
    isRight
  } = getScrollPosition(scrollContainer);
  const direction = {
    x: 0,
    y: 0
  };
  const speed = {
    x: 0,
    y: 0
  };
  const threshold = {
    height: scrollContainerRect.height * thresholdPercentage.y,
    width: scrollContainerRect.width * thresholdPercentage.x
  };
  if (!isTop && top2 <= scrollContainerRect.top + threshold.height) {
    direction.y = Direction.Backward;
    speed.y = acceleration * Math.abs((scrollContainerRect.top + threshold.height - top2) / threshold.height);
  } else if (!isBottom && bottom2 >= scrollContainerRect.bottom - threshold.height) {
    direction.y = Direction.Forward;
    speed.y = acceleration * Math.abs((scrollContainerRect.bottom - threshold.height - bottom2) / threshold.height);
  }
  if (!isRight && right2 >= scrollContainerRect.right - threshold.width) {
    direction.x = Direction.Forward;
    speed.x = acceleration * Math.abs((scrollContainerRect.right - threshold.width - right2) / threshold.width);
  } else if (!isLeft && left2 <= scrollContainerRect.left + threshold.width) {
    direction.x = Direction.Backward;
    speed.x = acceleration * Math.abs((scrollContainerRect.left + threshold.width - left2) / threshold.width);
  }
  return {
    direction,
    speed
  };
}
function getScrollElementRect(element) {
  if (element === document.scrollingElement) {
    const {
      innerWidth,
      innerHeight
    } = window;
    return {
      top: 0,
      left: 0,
      right: innerWidth,
      bottom: innerHeight,
      width: innerWidth,
      height: innerHeight
    };
  }
  const {
    top: top2,
    left: left2,
    right: right2,
    bottom: bottom2
  } = element.getBoundingClientRect();
  return {
    top: top2,
    left: left2,
    right: right2,
    bottom: bottom2,
    width: element.clientWidth,
    height: element.clientHeight
  };
}
function getScrollOffsets(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return add$1(acc, getScrollCoordinates(node));
  }, defaultCoordinates);
}
function getScrollXOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollXCoordinate(node);
  }, 0);
}
function getScrollYOffset(scrollableAncestors) {
  return scrollableAncestors.reduce((acc, node) => {
    return acc + getScrollYCoordinate(node);
  }, 0);
}
function scrollIntoViewIfNeeded(element, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  if (!element) {
    return;
  }
  const {
    top: top2,
    left: left2,
    bottom: bottom2,
    right: right2
  } = measure(element);
  const firstScrollableAncestor = getFirstScrollableAncestor(element);
  if (!firstScrollableAncestor) {
    return;
  }
  if (bottom2 <= 0 || right2 <= 0 || top2 >= window.innerHeight || left2 >= window.innerWidth) {
    element.scrollIntoView({
      block: "center",
      inline: "center"
    });
  }
}
const properties = [["x", ["left", "right"], getScrollXOffset], ["y", ["top", "bottom"], getScrollYOffset]];
class Rect {
  constructor(rect, element) {
    this.rect = void 0;
    this.width = void 0;
    this.height = void 0;
    this.top = void 0;
    this.bottom = void 0;
    this.right = void 0;
    this.left = void 0;
    const scrollableAncestors = getScrollableAncestors(element);
    const scrollOffsets = getScrollOffsets(scrollableAncestors);
    this.rect = {
      ...rect
    };
    this.width = rect.width;
    this.height = rect.height;
    for (const [axis, keys, getScrollOffset] of properties) {
      for (const key of keys) {
        Object.defineProperty(this, key, {
          get: () => {
            const currentOffsets = getScrollOffset(scrollableAncestors);
            const scrollOffsetsDeltla = scrollOffsets[axis] - currentOffsets;
            return this.rect[key] + scrollOffsetsDeltla;
          },
          enumerable: true
        });
      }
    }
    Object.defineProperty(this, "rect", {
      enumerable: false
    });
  }
}
class Listeners {
  constructor(target) {
    this.target = void 0;
    this.listeners = [];
    this.removeAll = () => {
      this.listeners.forEach((listener) => {
        var _this$target;
        return (_this$target = this.target) == null ? void 0 : _this$target.removeEventListener(...listener);
      });
    };
    this.target = target;
  }
  add(eventName, handler, options) {
    var _this$target2;
    (_this$target2 = this.target) == null ? void 0 : _this$target2.addEventListener(eventName, handler, options);
    this.listeners.push([eventName, handler, options]);
  }
}
function getEventListenerTarget(target) {
  const {
    EventTarget: EventTarget2
  } = getWindow(target);
  return target instanceof EventTarget2 ? target : getOwnerDocument(target);
}
function hasExceededDistance(delta, measurement) {
  const dx = Math.abs(delta.x);
  const dy = Math.abs(delta.y);
  if (typeof measurement === "number") {
    return Math.sqrt(dx ** 2 + dy ** 2) > measurement;
  }
  if ("x" in measurement && "y" in measurement) {
    return dx > measurement.x && dy > measurement.y;
  }
  if ("x" in measurement) {
    return dx > measurement.x;
  }
  if ("y" in measurement) {
    return dy > measurement.y;
  }
  return false;
}
var EventName;
(function(EventName2) {
  EventName2["Click"] = "click";
  EventName2["DragStart"] = "dragstart";
  EventName2["Keydown"] = "keydown";
  EventName2["ContextMenu"] = "contextmenu";
  EventName2["Resize"] = "resize";
  EventName2["SelectionChange"] = "selectionchange";
  EventName2["VisibilityChange"] = "visibilitychange";
})(EventName || (EventName = {}));
function preventDefault(event) {
  event.preventDefault();
}
function stopPropagation(event) {
  event.stopPropagation();
}
var KeyboardCode;
(function(KeyboardCode2) {
  KeyboardCode2["Space"] = "Space";
  KeyboardCode2["Down"] = "ArrowDown";
  KeyboardCode2["Right"] = "ArrowRight";
  KeyboardCode2["Left"] = "ArrowLeft";
  KeyboardCode2["Up"] = "ArrowUp";
  KeyboardCode2["Esc"] = "Escape";
  KeyboardCode2["Enter"] = "Enter";
  KeyboardCode2["Tab"] = "Tab";
})(KeyboardCode || (KeyboardCode = {}));
const defaultKeyboardCodes = {
  start: [KeyboardCode.Space, KeyboardCode.Enter],
  cancel: [KeyboardCode.Esc],
  end: [KeyboardCode.Space, KeyboardCode.Enter, KeyboardCode.Tab]
};
const defaultKeyboardCoordinateGetter = (event, _ref) => {
  let {
    currentCoordinates
  } = _ref;
  switch (event.code) {
    case KeyboardCode.Right:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x + 25
      };
    case KeyboardCode.Left:
      return {
        ...currentCoordinates,
        x: currentCoordinates.x - 25
      };
    case KeyboardCode.Down:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y + 25
      };
    case KeyboardCode.Up:
      return {
        ...currentCoordinates,
        y: currentCoordinates.y - 25
      };
  }
  return void 0;
};
class KeyboardSensor {
  constructor(props) {
    this.props = void 0;
    this.autoScrollEnabled = false;
    this.referenceCoordinates = void 0;
    this.listeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    const {
      event: {
        target
      }
    } = props;
    this.props = props;
    this.listeners = new Listeners(getOwnerDocument(target));
    this.windowListeners = new Listeners(getWindow(target));
    this.handleKeyDown = this.handleKeyDown.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.attach();
  }
  attach() {
    this.handleStart();
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    setTimeout(() => this.listeners.add(EventName.Keydown, this.handleKeyDown));
  }
  handleStart() {
    const {
      activeNode,
      onStart
    } = this.props;
    const node = activeNode.node.current;
    if (node) {
      scrollIntoViewIfNeeded(node);
    }
    onStart(defaultCoordinates);
  }
  handleKeyDown(event) {
    if (isKeyboardEvent(event)) {
      const {
        active,
        context: context2,
        options
      } = this.props;
      const {
        keyboardCodes = defaultKeyboardCodes,
        coordinateGetter = defaultKeyboardCoordinateGetter,
        scrollBehavior = "smooth"
      } = options;
      const {
        code
      } = event;
      if (keyboardCodes.end.includes(code)) {
        this.handleEnd(event);
        return;
      }
      if (keyboardCodes.cancel.includes(code)) {
        this.handleCancel(event);
        return;
      }
      const {
        collisionRect
      } = context2.current;
      const currentCoordinates = collisionRect ? {
        x: collisionRect.left,
        y: collisionRect.top
      } : defaultCoordinates;
      if (!this.referenceCoordinates) {
        this.referenceCoordinates = currentCoordinates;
      }
      const newCoordinates = coordinateGetter(event, {
        active,
        context: context2.current,
        currentCoordinates
      });
      if (newCoordinates) {
        const coordinatesDelta = subtract$1(newCoordinates, currentCoordinates);
        const scrollDelta = {
          x: 0,
          y: 0
        };
        const {
          scrollableAncestors
        } = context2.current;
        for (const scrollContainer of scrollableAncestors) {
          const direction = event.code;
          const {
            isTop,
            isRight,
            isLeft,
            isBottom,
            maxScroll,
            minScroll
          } = getScrollPosition(scrollContainer);
          const scrollElementRect = getScrollElementRect(scrollContainer);
          const clampedCoordinates = {
            x: Math.min(direction === KeyboardCode.Right ? scrollElementRect.right - scrollElementRect.width / 2 : scrollElementRect.right, Math.max(direction === KeyboardCode.Right ? scrollElementRect.left : scrollElementRect.left + scrollElementRect.width / 2, newCoordinates.x)),
            y: Math.min(direction === KeyboardCode.Down ? scrollElementRect.bottom - scrollElementRect.height / 2 : scrollElementRect.bottom, Math.max(direction === KeyboardCode.Down ? scrollElementRect.top : scrollElementRect.top + scrollElementRect.height / 2, newCoordinates.y))
          };
          const canScrollX = direction === KeyboardCode.Right && !isRight || direction === KeyboardCode.Left && !isLeft;
          const canScrollY = direction === KeyboardCode.Down && !isBottom || direction === KeyboardCode.Up && !isTop;
          if (canScrollX && clampedCoordinates.x !== newCoordinates.x) {
            const newScrollCoordinates = scrollContainer.scrollLeft + coordinatesDelta.x;
            const canScrollToNewCoordinates = direction === KeyboardCode.Right && newScrollCoordinates <= maxScroll.x || direction === KeyboardCode.Left && newScrollCoordinates >= minScroll.x;
            if (canScrollToNewCoordinates && !coordinatesDelta.y) {
              scrollContainer.scrollTo({
                left: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.x = scrollContainer.scrollLeft - newScrollCoordinates;
            } else {
              scrollDelta.x = direction === KeyboardCode.Right ? scrollContainer.scrollLeft - maxScroll.x : scrollContainer.scrollLeft - minScroll.x;
            }
            if (scrollDelta.x) {
              scrollContainer.scrollBy({
                left: -scrollDelta.x,
                behavior: scrollBehavior
              });
            }
            break;
          } else if (canScrollY && clampedCoordinates.y !== newCoordinates.y) {
            const newScrollCoordinates = scrollContainer.scrollTop + coordinatesDelta.y;
            const canScrollToNewCoordinates = direction === KeyboardCode.Down && newScrollCoordinates <= maxScroll.y || direction === KeyboardCode.Up && newScrollCoordinates >= minScroll.y;
            if (canScrollToNewCoordinates && !coordinatesDelta.x) {
              scrollContainer.scrollTo({
                top: newScrollCoordinates,
                behavior: scrollBehavior
              });
              return;
            }
            if (canScrollToNewCoordinates) {
              scrollDelta.y = scrollContainer.scrollTop - newScrollCoordinates;
            } else {
              scrollDelta.y = direction === KeyboardCode.Down ? scrollContainer.scrollTop - maxScroll.y : scrollContainer.scrollTop - minScroll.y;
            }
            if (scrollDelta.y) {
              scrollContainer.scrollBy({
                top: -scrollDelta.y,
                behavior: scrollBehavior
              });
            }
            break;
          }
        }
        this.handleMove(event, add$1(subtract$1(newCoordinates, this.referenceCoordinates), scrollDelta));
      }
    }
  }
  handleMove(event, coordinates) {
    const {
      onMove
    } = this.props;
    event.preventDefault();
    onMove(coordinates);
  }
  handleEnd(event) {
    const {
      onEnd
    } = this.props;
    event.preventDefault();
    this.detach();
    onEnd();
  }
  handleCancel(event) {
    const {
      onCancel
    } = this.props;
    event.preventDefault();
    this.detach();
    onCancel();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
  }
}
KeyboardSensor.activators = [{
  eventName: "onKeyDown",
  handler: (event, _ref, _ref2) => {
    let {
      keyboardCodes = defaultKeyboardCodes,
      onActivation
    } = _ref;
    let {
      active
    } = _ref2;
    const {
      code
    } = event.nativeEvent;
    if (keyboardCodes.start.includes(code)) {
      const activator = active.activatorNode.current;
      if (activator && event.target !== activator) {
        return false;
      }
      event.preventDefault();
      onActivation == null ? void 0 : onActivation({
        event: event.nativeEvent
      });
      return true;
    }
    return false;
  }
}];
function isDistanceConstraint(constraint) {
  return Boolean(constraint && "distance" in constraint);
}
function isDelayConstraint(constraint) {
  return Boolean(constraint && "delay" in constraint);
}
class AbstractPointerSensor {
  constructor(props, events2, listenerTarget) {
    var _getEventCoordinates;
    if (listenerTarget === void 0) {
      listenerTarget = getEventListenerTarget(props.event.target);
    }
    this.props = void 0;
    this.events = void 0;
    this.autoScrollEnabled = true;
    this.document = void 0;
    this.activated = false;
    this.initialCoordinates = void 0;
    this.timeoutId = null;
    this.listeners = void 0;
    this.documentListeners = void 0;
    this.windowListeners = void 0;
    this.props = props;
    this.events = events2;
    const {
      event
    } = props;
    const {
      target
    } = event;
    this.props = props;
    this.events = events2;
    this.document = getOwnerDocument(target);
    this.documentListeners = new Listeners(this.document);
    this.listeners = new Listeners(listenerTarget);
    this.windowListeners = new Listeners(getWindow(target));
    this.initialCoordinates = (_getEventCoordinates = getEventCoordinates$1(event)) != null ? _getEventCoordinates : defaultCoordinates;
    this.handleStart = this.handleStart.bind(this);
    this.handleMove = this.handleMove.bind(this);
    this.handleEnd = this.handleEnd.bind(this);
    this.handleCancel = this.handleCancel.bind(this);
    this.handleKeydown = this.handleKeydown.bind(this);
    this.removeTextSelection = this.removeTextSelection.bind(this);
    this.attach();
  }
  attach() {
    const {
      events: events2,
      props: {
        options: {
          activationConstraint,
          bypassActivationConstraint
        }
      }
    } = this;
    this.listeners.add(events2.move.name, this.handleMove, {
      passive: false
    });
    this.listeners.add(events2.end.name, this.handleEnd);
    if (events2.cancel) {
      this.listeners.add(events2.cancel.name, this.handleCancel);
    }
    this.windowListeners.add(EventName.Resize, this.handleCancel);
    this.windowListeners.add(EventName.DragStart, preventDefault);
    this.windowListeners.add(EventName.VisibilityChange, this.handleCancel);
    this.windowListeners.add(EventName.ContextMenu, preventDefault);
    this.documentListeners.add(EventName.Keydown, this.handleKeydown);
    if (activationConstraint) {
      if (bypassActivationConstraint != null && bypassActivationConstraint({
        event: this.props.event,
        activeNode: this.props.activeNode,
        options: this.props.options
      })) {
        return this.handleStart();
      }
      if (isDelayConstraint(activationConstraint)) {
        this.timeoutId = setTimeout(this.handleStart, activationConstraint.delay);
        this.handlePending(activationConstraint);
        return;
      }
      if (isDistanceConstraint(activationConstraint)) {
        this.handlePending(activationConstraint);
        return;
      }
    }
    this.handleStart();
  }
  detach() {
    this.listeners.removeAll();
    this.windowListeners.removeAll();
    setTimeout(this.documentListeners.removeAll, 50);
    if (this.timeoutId !== null) {
      clearTimeout(this.timeoutId);
      this.timeoutId = null;
    }
  }
  handlePending(constraint, offset2) {
    const {
      active,
      onPending
    } = this.props;
    onPending(active, constraint, this.initialCoordinates, offset2);
  }
  handleStart() {
    const {
      initialCoordinates
    } = this;
    const {
      onStart
    } = this.props;
    if (initialCoordinates) {
      this.activated = true;
      this.documentListeners.add(EventName.Click, stopPropagation, {
        capture: true
      });
      this.removeTextSelection();
      this.documentListeners.add(EventName.SelectionChange, this.removeTextSelection);
      onStart(initialCoordinates);
    }
  }
  handleMove(event) {
    var _getEventCoordinates2;
    const {
      activated,
      initialCoordinates,
      props
    } = this;
    const {
      onMove,
      options: {
        activationConstraint
      }
    } = props;
    if (!initialCoordinates) {
      return;
    }
    const coordinates = (_getEventCoordinates2 = getEventCoordinates$1(event)) != null ? _getEventCoordinates2 : defaultCoordinates;
    const delta = subtract$1(initialCoordinates, coordinates);
    if (!activated && activationConstraint) {
      if (isDistanceConstraint(activationConstraint)) {
        if (activationConstraint.tolerance != null && hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
        if (hasExceededDistance(delta, activationConstraint.distance)) {
          return this.handleStart();
        }
      }
      if (isDelayConstraint(activationConstraint)) {
        if (hasExceededDistance(delta, activationConstraint.tolerance)) {
          return this.handleCancel();
        }
      }
      this.handlePending(activationConstraint, delta);
      return;
    }
    if (event.cancelable) {
      event.preventDefault();
    }
    onMove(coordinates);
  }
  handleEnd() {
    const {
      onAbort,
      onEnd
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onEnd();
  }
  handleCancel() {
    const {
      onAbort,
      onCancel
    } = this.props;
    this.detach();
    if (!this.activated) {
      onAbort(this.props.active);
    }
    onCancel();
  }
  handleKeydown(event) {
    if (event.code === KeyboardCode.Esc) {
      this.handleCancel();
    }
  }
  removeTextSelection() {
    var _this$document$getSel;
    (_this$document$getSel = this.document.getSelection()) == null ? void 0 : _this$document$getSel.removeAllRanges();
  }
}
const events = {
  cancel: {
    name: "pointercancel"
  },
  move: {
    name: "pointermove"
  },
  end: {
    name: "pointerup"
  }
};
class PointerSensor extends AbstractPointerSensor {
  constructor(props) {
    const {
      event
    } = props;
    const listenerTarget = getOwnerDocument(event.target);
    super(props, events, listenerTarget);
  }
}
PointerSensor.activators = [{
  eventName: "onPointerDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (!event.isPrimary || event.button !== 0) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
const events$1 = {
  move: {
    name: "mousemove"
  },
  end: {
    name: "mouseup"
  }
};
var MouseButton;
(function(MouseButton2) {
  MouseButton2[MouseButton2["RightClick"] = 2] = "RightClick";
})(MouseButton || (MouseButton = {}));
class MouseSensor extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$1, getOwnerDocument(props.event.target));
  }
}
MouseSensor.activators = [{
  eventName: "onMouseDown",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    if (event.button === MouseButton.RightClick) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
const events$2 = {
  cancel: {
    name: "touchcancel"
  },
  move: {
    name: "touchmove"
  },
  end: {
    name: "touchend"
  }
};
class TouchSensor extends AbstractPointerSensor {
  constructor(props) {
    super(props, events$2);
  }
  static setup() {
    window.addEventListener(events$2.move.name, noop4, {
      capture: false,
      passive: false
    });
    return function teardown() {
      window.removeEventListener(events$2.move.name, noop4);
    };
    function noop4() {
    }
  }
}
TouchSensor.activators = [{
  eventName: "onTouchStart",
  handler: (_ref, _ref2) => {
    let {
      nativeEvent: event
    } = _ref;
    let {
      onActivation
    } = _ref2;
    const {
      touches
    } = event;
    if (touches.length > 1) {
      return false;
    }
    onActivation == null ? void 0 : onActivation({
      event
    });
    return true;
  }
}];
var AutoScrollActivator;
(function(AutoScrollActivator2) {
  AutoScrollActivator2[AutoScrollActivator2["Pointer"] = 0] = "Pointer";
  AutoScrollActivator2[AutoScrollActivator2["DraggableRect"] = 1] = "DraggableRect";
})(AutoScrollActivator || (AutoScrollActivator = {}));
var TraversalOrder;
(function(TraversalOrder2) {
  TraversalOrder2[TraversalOrder2["TreeOrder"] = 0] = "TreeOrder";
  TraversalOrder2[TraversalOrder2["ReversedTreeOrder"] = 1] = "ReversedTreeOrder";
})(TraversalOrder || (TraversalOrder = {}));
function useAutoScroller(_ref) {
  let {
    acceleration,
    activator = AutoScrollActivator.Pointer,
    canScroll,
    draggingRect,
    enabled,
    interval = 5,
    order: order2 = TraversalOrder.TreeOrder,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects,
    delta,
    threshold
  } = _ref;
  const scrollIntent = useScrollIntent({
    delta,
    disabled: !enabled
  });
  const [setAutoScrollInterval, clearAutoScrollInterval] = useInterval();
  const scrollSpeed = reactExports.useRef({
    x: 0,
    y: 0
  });
  const scrollDirection = reactExports.useRef({
    x: 0,
    y: 0
  });
  const rect = reactExports.useMemo(() => {
    switch (activator) {
      case AutoScrollActivator.Pointer:
        return pointerCoordinates ? {
          top: pointerCoordinates.y,
          bottom: pointerCoordinates.y,
          left: pointerCoordinates.x,
          right: pointerCoordinates.x
        } : null;
      case AutoScrollActivator.DraggableRect:
        return draggingRect;
    }
  }, [activator, draggingRect, pointerCoordinates]);
  const scrollContainerRef = reactExports.useRef(null);
  const autoScroll = reactExports.useCallback(() => {
    const scrollContainer = scrollContainerRef.current;
    if (!scrollContainer) {
      return;
    }
    const scrollLeft2 = scrollSpeed.current.x * scrollDirection.current.x;
    const scrollTop2 = scrollSpeed.current.y * scrollDirection.current.y;
    scrollContainer.scrollBy(scrollLeft2, scrollTop2);
  }, []);
  const sortedScrollableAncestors = reactExports.useMemo(() => order2 === TraversalOrder.TreeOrder ? [...scrollableAncestors].reverse() : scrollableAncestors, [order2, scrollableAncestors]);
  reactExports.useEffect(
    () => {
      if (!enabled || !scrollableAncestors.length || !rect) {
        clearAutoScrollInterval();
        return;
      }
      for (const scrollContainer of sortedScrollableAncestors) {
        if ((canScroll == null ? void 0 : canScroll(scrollContainer)) === false) {
          continue;
        }
        const index2 = scrollableAncestors.indexOf(scrollContainer);
        const scrollContainerRect = scrollableAncestorRects[index2];
        if (!scrollContainerRect) {
          continue;
        }
        const {
          direction,
          speed
        } = getScrollDirectionAndSpeed(scrollContainer, scrollContainerRect, rect, acceleration, threshold);
        for (const axis of ["x", "y"]) {
          if (!scrollIntent[axis][direction[axis]]) {
            speed[axis] = 0;
            direction[axis] = 0;
          }
        }
        if (speed.x > 0 || speed.y > 0) {
          clearAutoScrollInterval();
          scrollContainerRef.current = scrollContainer;
          setAutoScrollInterval(autoScroll, interval);
          scrollSpeed.current = speed;
          scrollDirection.current = direction;
          return;
        }
      }
      scrollSpeed.current = {
        x: 0,
        y: 0
      };
      scrollDirection.current = {
        x: 0,
        y: 0
      };
      clearAutoScrollInterval();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [
      acceleration,
      autoScroll,
      canScroll,
      clearAutoScrollInterval,
      enabled,
      interval,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(rect),
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(scrollIntent),
      setAutoScrollInterval,
      scrollableAncestors,
      sortedScrollableAncestors,
      scrollableAncestorRects,
      // eslint-disable-next-line react-hooks/exhaustive-deps
      JSON.stringify(threshold)
    ]
  );
}
const defaultScrollIntent = {
  x: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  },
  y: {
    [Direction.Backward]: false,
    [Direction.Forward]: false
  }
};
function useScrollIntent(_ref2) {
  let {
    delta,
    disabled
  } = _ref2;
  const previousDelta = usePrevious(delta);
  return useLazyMemo((previousIntent) => {
    if (disabled || !previousDelta || !previousIntent) {
      return defaultScrollIntent;
    }
    const direction = {
      x: Math.sign(delta.x - previousDelta.x),
      y: Math.sign(delta.y - previousDelta.y)
    };
    return {
      x: {
        [Direction.Backward]: previousIntent.x[Direction.Backward] || direction.x === -1,
        [Direction.Forward]: previousIntent.x[Direction.Forward] || direction.x === 1
      },
      y: {
        [Direction.Backward]: previousIntent.y[Direction.Backward] || direction.y === -1,
        [Direction.Forward]: previousIntent.y[Direction.Forward] || direction.y === 1
      }
    };
  }, [disabled, delta, previousDelta]);
}
function useCachedNode(draggableNodes, id2) {
  const draggableNode = id2 != null ? draggableNodes.get(id2) : void 0;
  const node = draggableNode ? draggableNode.node.current : null;
  return useLazyMemo((cachedNode) => {
    var _ref;
    if (id2 == null) {
      return null;
    }
    return (_ref = node != null ? node : cachedNode) != null ? _ref : null;
  }, [node, id2]);
}
function useCombineActivators(sensors, getSyntheticHandler) {
  return reactExports.useMemo(() => sensors.reduce((accumulator, sensor) => {
    const {
      sensor: Sensor
    } = sensor;
    const sensorActivators = Sensor.activators.map((activator) => ({
      eventName: activator.eventName,
      handler: getSyntheticHandler(activator.handler, sensor)
    }));
    return [...accumulator, ...sensorActivators];
  }, []), [sensors, getSyntheticHandler]);
}
var MeasuringStrategy;
(function(MeasuringStrategy2) {
  MeasuringStrategy2[MeasuringStrategy2["Always"] = 0] = "Always";
  MeasuringStrategy2[MeasuringStrategy2["BeforeDragging"] = 1] = "BeforeDragging";
  MeasuringStrategy2[MeasuringStrategy2["WhileDragging"] = 2] = "WhileDragging";
})(MeasuringStrategy || (MeasuringStrategy = {}));
var MeasuringFrequency;
(function(MeasuringFrequency2) {
  MeasuringFrequency2["Optimized"] = "optimized";
})(MeasuringFrequency || (MeasuringFrequency = {}));
const defaultValue = /* @__PURE__ */ new Map();
function useDroppableMeasuring(containers, _ref) {
  let {
    dragging,
    dependencies,
    config: config2
  } = _ref;
  const [queue, setQueue] = reactExports.useState(null);
  const {
    frequency,
    measure,
    strategy
  } = config2;
  const containersRef = reactExports.useRef(containers);
  const disabled = isDisabled();
  const disabledRef = useLatestValue(disabled);
  const measureDroppableContainers = reactExports.useCallback(function(ids2) {
    if (ids2 === void 0) {
      ids2 = [];
    }
    if (disabledRef.current) {
      return;
    }
    setQueue((value) => {
      if (value === null) {
        return ids2;
      }
      return value.concat(ids2.filter((id2) => !value.includes(id2)));
    });
  }, [disabledRef]);
  const timeoutId = reactExports.useRef(null);
  const droppableRects = useLazyMemo((previousValue) => {
    if (disabled && !dragging) {
      return defaultValue;
    }
    if (!previousValue || previousValue === defaultValue || containersRef.current !== containers || queue != null) {
      const map = /* @__PURE__ */ new Map();
      for (let container of containers) {
        if (!container) {
          continue;
        }
        if (queue && queue.length > 0 && !queue.includes(container.id) && container.rect.current) {
          map.set(container.id, container.rect.current);
          continue;
        }
        const node = container.node.current;
        const rect = node ? new Rect(measure(node), node) : null;
        container.rect.current = rect;
        if (rect) {
          map.set(container.id, rect);
        }
      }
      return map;
    }
    return previousValue;
  }, [containers, queue, dragging, disabled, measure]);
  reactExports.useEffect(() => {
    containersRef.current = containers;
  }, [containers]);
  reactExports.useEffect(
    () => {
      if (disabled) {
        return;
      }
      measureDroppableContainers();
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [dragging, disabled]
  );
  reactExports.useEffect(
    () => {
      if (queue && queue.length > 0) {
        setQueue(null);
      }
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [JSON.stringify(queue)]
  );
  reactExports.useEffect(
    () => {
      if (disabled || typeof frequency !== "number" || timeoutId.current !== null) {
        return;
      }
      timeoutId.current = setTimeout(() => {
        measureDroppableContainers();
        timeoutId.current = null;
      }, frequency);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [frequency, disabled, measureDroppableContainers, ...dependencies]
  );
  return {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled: queue != null
  };
  function isDisabled() {
    switch (strategy) {
      case MeasuringStrategy.Always:
        return false;
      case MeasuringStrategy.BeforeDragging:
        return dragging;
      default:
        return !dragging;
    }
  }
}
function useInitialValue(value, computeFn) {
  return useLazyMemo((previousValue) => {
    if (!value) {
      return null;
    }
    if (previousValue) {
      return previousValue;
    }
    return typeof computeFn === "function" ? computeFn(value) : value;
  }, [computeFn, value]);
}
function useInitialRect(node, measure) {
  return useInitialValue(node, measure);
}
function useMutationObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleMutations = useEvent(callback);
  const mutationObserver = reactExports.useMemo(() => {
    if (disabled || typeof window === "undefined" || typeof window.MutationObserver === "undefined") {
      return void 0;
    }
    const {
      MutationObserver: MutationObserver2
    } = window;
    return new MutationObserver2(handleMutations);
  }, [handleMutations, disabled]);
  reactExports.useEffect(() => {
    return () => mutationObserver == null ? void 0 : mutationObserver.disconnect();
  }, [mutationObserver]);
  return mutationObserver;
}
function useResizeObserver(_ref) {
  let {
    callback,
    disabled
  } = _ref;
  const handleResize = useEvent(callback);
  const resizeObserver = reactExports.useMemo(
    () => {
      if (disabled || typeof window === "undefined" || typeof window.ResizeObserver === "undefined") {
        return void 0;
      }
      const {
        ResizeObserver
      } = window;
      return new ResizeObserver(handleResize);
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [disabled]
  );
  reactExports.useEffect(() => {
    return () => resizeObserver == null ? void 0 : resizeObserver.disconnect();
  }, [resizeObserver]);
  return resizeObserver;
}
function defaultMeasure(element) {
  return new Rect(getClientRect(element), element);
}
function useRect(element, measure, fallbackRect) {
  if (measure === void 0) {
    measure = defaultMeasure;
  }
  const [rect, setRect] = reactExports.useState(null);
  function measureRect() {
    setRect((currentRect) => {
      if (!element) {
        return null;
      }
      if (element.isConnected === false) {
        var _ref;
        return (_ref = currentRect != null ? currentRect : fallbackRect) != null ? _ref : null;
      }
      const newRect = measure(element);
      if (JSON.stringify(currentRect) === JSON.stringify(newRect)) {
        return currentRect;
      }
      return newRect;
    });
  }
  const mutationObserver = useMutationObserver({
    callback(records) {
      if (!element) {
        return;
      }
      for (const record of records) {
        const {
          type,
          target
        } = record;
        if (type === "childList" && target instanceof HTMLElement && target.contains(element)) {
          measureRect();
          break;
        }
      }
    }
  });
  const resizeObserver = useResizeObserver({
    callback: measureRect
  });
  useIsomorphicLayoutEffect(() => {
    measureRect();
    if (element) {
      resizeObserver == null ? void 0 : resizeObserver.observe(element);
      mutationObserver == null ? void 0 : mutationObserver.observe(document.body, {
        childList: true,
        subtree: true
      });
    } else {
      resizeObserver == null ? void 0 : resizeObserver.disconnect();
      mutationObserver == null ? void 0 : mutationObserver.disconnect();
    }
  }, [element]);
  return rect;
}
function useRectDelta(rect) {
  const initialRect = useInitialValue(rect);
  return getRectDelta(rect, initialRect);
}
const defaultValue$1 = [];
function useScrollableAncestors(node) {
  const previousNode = reactExports.useRef(node);
  const ancestors = useLazyMemo((previousValue) => {
    if (!node) {
      return defaultValue$1;
    }
    if (previousValue && previousValue !== defaultValue$1 && node && previousNode.current && node.parentNode === previousNode.current.parentNode) {
      return previousValue;
    }
    return getScrollableAncestors(node);
  }, [node]);
  reactExports.useEffect(() => {
    previousNode.current = node;
  }, [node]);
  return ancestors;
}
function useScrollOffsets(elements) {
  const [scrollCoordinates, setScrollCoordinates] = reactExports.useState(null);
  const prevElements = reactExports.useRef(elements);
  const handleScroll = reactExports.useCallback((event) => {
    const scrollingElement = getScrollableElement(event.target);
    if (!scrollingElement) {
      return;
    }
    setScrollCoordinates((scrollCoordinates2) => {
      if (!scrollCoordinates2) {
        return null;
      }
      scrollCoordinates2.set(scrollingElement, getScrollCoordinates(scrollingElement));
      return new Map(scrollCoordinates2);
    });
  }, []);
  reactExports.useEffect(() => {
    const previousElements = prevElements.current;
    if (elements !== previousElements) {
      cleanup(previousElements);
      const entries = elements.map((element) => {
        const scrollableElement = getScrollableElement(element);
        if (scrollableElement) {
          scrollableElement.addEventListener("scroll", handleScroll, {
            passive: true
          });
          return [scrollableElement, getScrollCoordinates(scrollableElement)];
        }
        return null;
      }).filter((entry) => entry != null);
      setScrollCoordinates(entries.length ? new Map(entries) : null);
      prevElements.current = elements;
    }
    return () => {
      cleanup(elements);
      cleanup(previousElements);
    };
    function cleanup(elements2) {
      elements2.forEach((element) => {
        const scrollableElement = getScrollableElement(element);
        scrollableElement == null ? void 0 : scrollableElement.removeEventListener("scroll", handleScroll);
      });
    }
  }, [handleScroll, elements]);
  return reactExports.useMemo(() => {
    if (elements.length) {
      return scrollCoordinates ? Array.from(scrollCoordinates.values()).reduce((acc, coordinates) => add$1(acc, coordinates), defaultCoordinates) : getScrollOffsets(elements);
    }
    return defaultCoordinates;
  }, [elements, scrollCoordinates]);
}
function useScrollOffsetsDelta(scrollOffsets, dependencies) {
  if (dependencies === void 0) {
    dependencies = [];
  }
  const initialScrollOffsets = reactExports.useRef(null);
  reactExports.useEffect(
    () => {
      initialScrollOffsets.current = null;
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    dependencies
  );
  reactExports.useEffect(() => {
    const hasScrollOffsets = scrollOffsets !== defaultCoordinates;
    if (hasScrollOffsets && !initialScrollOffsets.current) {
      initialScrollOffsets.current = scrollOffsets;
    }
    if (!hasScrollOffsets && initialScrollOffsets.current) {
      initialScrollOffsets.current = null;
    }
  }, [scrollOffsets]);
  return initialScrollOffsets.current ? subtract$1(scrollOffsets, initialScrollOffsets.current) : defaultCoordinates;
}
function useSensorSetup(sensors) {
  reactExports.useEffect(
    () => {
      if (!canUseDOM) {
        return;
      }
      const teardownFns = sensors.map((_ref) => {
        let {
          sensor
        } = _ref;
        return sensor.setup == null ? void 0 : sensor.setup();
      });
      return () => {
        for (const teardown of teardownFns) {
          teardown == null ? void 0 : teardown();
        }
      };
    },
    // TO-DO: Sensors length could theoretically change which would not be a valid dependency
    // eslint-disable-next-line react-hooks/exhaustive-deps
    sensors.map((_ref2) => {
      let {
        sensor
      } = _ref2;
      return sensor;
    })
  );
}
function useSyntheticListeners(listeners, id2) {
  return reactExports.useMemo(() => {
    return listeners.reduce((acc, _ref) => {
      let {
        eventName,
        handler
      } = _ref;
      acc[eventName] = (event) => {
        handler(event, id2);
      };
      return acc;
    }, {});
  }, [listeners, id2]);
}
function useWindowRect(element) {
  return reactExports.useMemo(() => element ? getWindowClientRect(element) : null, [element]);
}
const defaultValue$2 = [];
function useRects(elements, measure) {
  if (measure === void 0) {
    measure = getClientRect;
  }
  const [firstElement] = elements;
  const windowRect = useWindowRect(firstElement ? getWindow(firstElement) : null);
  const [rects, setRects] = reactExports.useState(defaultValue$2);
  function measureRects() {
    setRects(() => {
      if (!elements.length) {
        return defaultValue$2;
      }
      return elements.map((element) => isDocumentScrollingElement(element) ? windowRect : new Rect(measure(element), element));
    });
  }
  const resizeObserver = useResizeObserver({
    callback: measureRects
  });
  useIsomorphicLayoutEffect(() => {
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    measureRects();
    elements.forEach((element) => resizeObserver == null ? void 0 : resizeObserver.observe(element));
  }, [elements]);
  return rects;
}
function getMeasurableNode(node) {
  if (!node) {
    return null;
  }
  if (node.children.length > 1) {
    return node;
  }
  const firstChild = node.children[0];
  return isHTMLElement$1(firstChild) ? firstChild : node;
}
function useDragOverlayMeasuring(_ref) {
  let {
    measure
  } = _ref;
  const [rect, setRect] = reactExports.useState(null);
  const handleResize = reactExports.useCallback((entries) => {
    for (const {
      target
    } of entries) {
      if (isHTMLElement$1(target)) {
        setRect((rect2) => {
          const newRect = measure(target);
          return rect2 ? {
            ...rect2,
            width: newRect.width,
            height: newRect.height
          } : newRect;
        });
        break;
      }
    }
  }, [measure]);
  const resizeObserver = useResizeObserver({
    callback: handleResize
  });
  const handleNodeChange = reactExports.useCallback((element) => {
    const node = getMeasurableNode(element);
    resizeObserver == null ? void 0 : resizeObserver.disconnect();
    if (node) {
      resizeObserver == null ? void 0 : resizeObserver.observe(node);
    }
    setRect(node ? measure(node) : null);
  }, [measure, resizeObserver]);
  const [nodeRef, setRef] = useNodeRef(handleNodeChange);
  return reactExports.useMemo(() => ({
    nodeRef,
    rect,
    setRef
  }), [rect, nodeRef, setRef]);
}
const defaultSensors = [{
  sensor: PointerSensor,
  options: {}
}, {
  sensor: KeyboardSensor,
  options: {}
}];
const defaultData = {
  current: {}
};
const defaultMeasuringConfiguration = {
  draggable: {
    measure: getTransformAgnosticClientRect
  },
  droppable: {
    measure: getTransformAgnosticClientRect,
    strategy: MeasuringStrategy.WhileDragging,
    frequency: MeasuringFrequency.Optimized
  },
  dragOverlay: {
    measure: getClientRect
  }
};
class DroppableContainersMap extends Map {
  get(id2) {
    var _super$get;
    return id2 != null ? (_super$get = super.get(id2)) != null ? _super$get : void 0 : void 0;
  }
  toArray() {
    return Array.from(this.values());
  }
  getEnabled() {
    return this.toArray().filter((_ref) => {
      let {
        disabled
      } = _ref;
      return !disabled;
    });
  }
  getNodeFor(id2) {
    var _this$get$node$curren, _this$get;
    return (_this$get$node$curren = (_this$get = this.get(id2)) == null ? void 0 : _this$get.node.current) != null ? _this$get$node$curren : void 0;
  }
}
const defaultPublicContext = {
  activatorEvent: null,
  active: null,
  activeNode: null,
  activeNodeRect: null,
  collisions: null,
  containerNodeRect: null,
  draggableNodes: /* @__PURE__ */ new Map(),
  droppableRects: /* @__PURE__ */ new Map(),
  droppableContainers: /* @__PURE__ */ new DroppableContainersMap(),
  over: null,
  dragOverlay: {
    nodeRef: {
      current: null
    },
    rect: null,
    setRef: noop$1
  },
  scrollableAncestors: [],
  scrollableAncestorRects: [],
  measuringConfiguration: defaultMeasuringConfiguration,
  measureDroppableContainers: noop$1,
  windowRect: null,
  measuringScheduled: false
};
const defaultInternalContext = {
  activatorEvent: null,
  activators: [],
  active: null,
  activeNodeRect: null,
  ariaDescribedById: {
    draggable: ""
  },
  dispatch: noop$1,
  draggableNodes: /* @__PURE__ */ new Map(),
  over: null,
  measureDroppableContainers: noop$1
};
const InternalContext = /* @__PURE__ */ reactExports.createContext(defaultInternalContext);
const PublicContext = /* @__PURE__ */ reactExports.createContext(defaultPublicContext);
function getInitialState() {
  return {
    draggable: {
      active: null,
      initialCoordinates: {
        x: 0,
        y: 0
      },
      nodes: /* @__PURE__ */ new Map(),
      translate: {
        x: 0,
        y: 0
      }
    },
    droppable: {
      containers: new DroppableContainersMap()
    }
  };
}
function reducer(state, action) {
  switch (action.type) {
    case Action.DragStart:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          initialCoordinates: action.initialCoordinates,
          active: action.active
        }
      };
    case Action.DragMove:
      if (state.draggable.active == null) {
        return state;
      }
      return {
        ...state,
        draggable: {
          ...state.draggable,
          translate: {
            x: action.coordinates.x - state.draggable.initialCoordinates.x,
            y: action.coordinates.y - state.draggable.initialCoordinates.y
          }
        }
      };
    case Action.DragEnd:
    case Action.DragCancel:
      return {
        ...state,
        draggable: {
          ...state.draggable,
          active: null,
          initialCoordinates: {
            x: 0,
            y: 0
          },
          translate: {
            x: 0,
            y: 0
          }
        }
      };
    case Action.RegisterDroppable: {
      const {
        element
      } = action;
      const {
        id: id2
      } = element;
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id2, element);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.SetDroppableDisabled: {
      const {
        id: id2,
        key,
        disabled
      } = action;
      const element = state.droppable.containers.get(id2);
      if (!element || key !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.set(id2, {
        ...element,
        disabled
      });
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    case Action.UnregisterDroppable: {
      const {
        id: id2,
        key
      } = action;
      const element = state.droppable.containers.get(id2);
      if (!element || key !== element.key) {
        return state;
      }
      const containers = new DroppableContainersMap(state.droppable.containers);
      containers.delete(id2);
      return {
        ...state,
        droppable: {
          ...state.droppable,
          containers
        }
      };
    }
    default: {
      return state;
    }
  }
}
function RestoreFocus(_ref) {
  let {
    disabled
  } = _ref;
  const {
    active,
    activatorEvent,
    draggableNodes
  } = reactExports.useContext(InternalContext);
  const previousActivatorEvent = usePrevious(activatorEvent);
  const previousActiveId = usePrevious(active == null ? void 0 : active.id);
  reactExports.useEffect(() => {
    if (disabled) {
      return;
    }
    if (!activatorEvent && previousActivatorEvent && previousActiveId != null) {
      if (!isKeyboardEvent(previousActivatorEvent)) {
        return;
      }
      if (document.activeElement === previousActivatorEvent.target) {
        return;
      }
      const draggableNode = draggableNodes.get(previousActiveId);
      if (!draggableNode) {
        return;
      }
      const {
        activatorNode,
        node
      } = draggableNode;
      if (!activatorNode.current && !node.current) {
        return;
      }
      requestAnimationFrame(() => {
        for (const element of [activatorNode.current, node.current]) {
          if (!element) {
            continue;
          }
          const focusableNode = findFirstFocusableNode(element);
          if (focusableNode) {
            focusableNode.focus();
            break;
          }
        }
      });
    }
  }, [activatorEvent, disabled, draggableNodes, previousActiveId, previousActivatorEvent]);
  return null;
}
function applyModifiers(modifiers, _ref) {
  let {
    transform,
    ...args
  } = _ref;
  return modifiers != null && modifiers.length ? modifiers.reduce((accumulator, modifier) => {
    return modifier({
      transform: accumulator,
      ...args
    });
  }, transform) : transform;
}
function useMeasuringConfiguration(config2) {
  return reactExports.useMemo(
    () => ({
      draggable: {
        ...defaultMeasuringConfiguration.draggable,
        ...config2 == null ? void 0 : config2.draggable
      },
      droppable: {
        ...defaultMeasuringConfiguration.droppable,
        ...config2 == null ? void 0 : config2.droppable
      },
      dragOverlay: {
        ...defaultMeasuringConfiguration.dragOverlay,
        ...config2 == null ? void 0 : config2.dragOverlay
      }
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [config2 == null ? void 0 : config2.draggable, config2 == null ? void 0 : config2.droppable, config2 == null ? void 0 : config2.dragOverlay]
  );
}
function useLayoutShiftScrollCompensation(_ref) {
  let {
    activeNode,
    measure,
    initialRect,
    config: config2 = true
  } = _ref;
  const initialized = reactExports.useRef(false);
  const {
    x: x2,
    y: y3
  } = typeof config2 === "boolean" ? {
    x: config2,
    y: config2
  } : config2;
  useIsomorphicLayoutEffect(() => {
    const disabled = !x2 && !y3;
    if (disabled || !activeNode) {
      initialized.current = false;
      return;
    }
    if (initialized.current || !initialRect) {
      return;
    }
    const node = activeNode == null ? void 0 : activeNode.node.current;
    if (!node || node.isConnected === false) {
      return;
    }
    const rect = measure(node);
    const rectDelta = getRectDelta(rect, initialRect);
    if (!x2) {
      rectDelta.x = 0;
    }
    if (!y3) {
      rectDelta.y = 0;
    }
    initialized.current = true;
    if (Math.abs(rectDelta.x) > 0 || Math.abs(rectDelta.y) > 0) {
      const firstScrollableAncestor = getFirstScrollableAncestor(node);
      if (firstScrollableAncestor) {
        firstScrollableAncestor.scrollBy({
          top: rectDelta.y,
          left: rectDelta.x
        });
      }
    }
  }, [activeNode, x2, y3, initialRect, measure]);
}
const ActiveDraggableContext = /* @__PURE__ */ reactExports.createContext({
  ...defaultCoordinates,
  scaleX: 1,
  scaleY: 1
});
var Status;
(function(Status2) {
  Status2[Status2["Uninitialized"] = 0] = "Uninitialized";
  Status2[Status2["Initializing"] = 1] = "Initializing";
  Status2[Status2["Initialized"] = 2] = "Initialized";
})(Status || (Status = {}));
const DndContext = /* @__PURE__ */ reactExports.memo(function DndContext2(_ref) {
  var _sensorContext$curren, _dragOverlay$nodeRef$, _dragOverlay$rect, _over$rect;
  let {
    id: id2,
    accessibility,
    autoScroll = true,
    children,
    sensors = defaultSensors,
    collisionDetection = rectIntersection,
    measuring,
    modifiers,
    ...props
  } = _ref;
  const store = reactExports.useReducer(reducer, void 0, getInitialState);
  const [state, dispatch] = store;
  const [dispatchMonitorEvent, registerMonitorListener] = useDndMonitorProvider();
  const [status, setStatus] = reactExports.useState(Status.Uninitialized);
  const isInitialized = status === Status.Initialized;
  const {
    draggable: {
      active: activeId,
      nodes: draggableNodes,
      translate
    },
    droppable: {
      containers: droppableContainers
    }
  } = state;
  const node = activeId != null ? draggableNodes.get(activeId) : null;
  const activeRects = reactExports.useRef({
    initial: null,
    translated: null
  });
  const active = reactExports.useMemo(() => {
    var _node$data;
    return activeId != null ? {
      id: activeId,
      // It's possible for the active node to unmount while dragging
      data: (_node$data = node == null ? void 0 : node.data) != null ? _node$data : defaultData,
      rect: activeRects
    } : null;
  }, [activeId, node]);
  const activeRef = reactExports.useRef(null);
  const [activeSensor, setActiveSensor] = reactExports.useState(null);
  const [activatorEvent, setActivatorEvent] = reactExports.useState(null);
  const latestProps = useLatestValue(props, Object.values(props));
  const draggableDescribedById = useUniqueId("DndDescribedBy", id2);
  const enabledDroppableContainers = reactExports.useMemo(() => droppableContainers.getEnabled(), [droppableContainers]);
  const measuringConfiguration = useMeasuringConfiguration(measuring);
  const {
    droppableRects,
    measureDroppableContainers,
    measuringScheduled
  } = useDroppableMeasuring(enabledDroppableContainers, {
    dragging: isInitialized,
    dependencies: [translate.x, translate.y],
    config: measuringConfiguration.droppable
  });
  const activeNode = useCachedNode(draggableNodes, activeId);
  const activationCoordinates = reactExports.useMemo(() => activatorEvent ? getEventCoordinates$1(activatorEvent) : null, [activatorEvent]);
  const autoScrollOptions = getAutoScrollerOptions();
  const initialActiveNodeRect = useInitialRect(activeNode, measuringConfiguration.draggable.measure);
  useLayoutShiftScrollCompensation({
    activeNode: activeId != null ? draggableNodes.get(activeId) : null,
    config: autoScrollOptions.layoutShiftCompensation,
    initialRect: initialActiveNodeRect,
    measure: measuringConfiguration.draggable.measure
  });
  const activeNodeRect = useRect(activeNode, measuringConfiguration.draggable.measure, initialActiveNodeRect);
  const containerNodeRect = useRect(activeNode ? activeNode.parentElement : null);
  const sensorContext = reactExports.useRef({
    activatorEvent: null,
    active: null,
    activeNode,
    collisionRect: null,
    collisions: null,
    droppableRects,
    draggableNodes,
    draggingNode: null,
    draggingNodeRect: null,
    droppableContainers,
    over: null,
    scrollableAncestors: [],
    scrollAdjustedTranslate: null
  });
  const overNode = droppableContainers.getNodeFor((_sensorContext$curren = sensorContext.current.over) == null ? void 0 : _sensorContext$curren.id);
  const dragOverlay = useDragOverlayMeasuring({
    measure: measuringConfiguration.dragOverlay.measure
  });
  const draggingNode = (_dragOverlay$nodeRef$ = dragOverlay.nodeRef.current) != null ? _dragOverlay$nodeRef$ : activeNode;
  const draggingNodeRect = isInitialized ? (_dragOverlay$rect = dragOverlay.rect) != null ? _dragOverlay$rect : activeNodeRect : null;
  const usesDragOverlay = Boolean(dragOverlay.nodeRef.current && dragOverlay.rect);
  const nodeRectDelta = useRectDelta(usesDragOverlay ? null : activeNodeRect);
  const windowRect = useWindowRect(draggingNode ? getWindow(draggingNode) : null);
  const scrollableAncestors = useScrollableAncestors(isInitialized ? overNode != null ? overNode : activeNode : null);
  const scrollableAncestorRects = useRects(scrollableAncestors);
  const modifiedTranslate = applyModifiers(modifiers, {
    transform: {
      x: translate.x - nodeRectDelta.x,
      y: translate.y - nodeRectDelta.y,
      scaleX: 1,
      scaleY: 1
    },
    activatorEvent,
    active,
    activeNodeRect,
    containerNodeRect,
    draggingNodeRect,
    over: sensorContext.current.over,
    overlayNodeRect: dragOverlay.rect,
    scrollableAncestors,
    scrollableAncestorRects,
    windowRect
  });
  const pointerCoordinates = activationCoordinates ? add$1(activationCoordinates, translate) : null;
  const scrollOffsets = useScrollOffsets(scrollableAncestors);
  const scrollAdjustment = useScrollOffsetsDelta(scrollOffsets);
  const activeNodeScrollDelta = useScrollOffsetsDelta(scrollOffsets, [activeNodeRect]);
  const scrollAdjustedTranslate = add$1(modifiedTranslate, scrollAdjustment);
  const collisionRect = draggingNodeRect ? getAdjustedRect(draggingNodeRect, modifiedTranslate) : null;
  const collisions = active && collisionRect ? collisionDetection({
    active,
    collisionRect,
    droppableRects,
    droppableContainers: enabledDroppableContainers,
    pointerCoordinates
  }) : null;
  const overId = getFirstCollision(collisions, "id");
  const [over, setOver] = reactExports.useState(null);
  const appliedTranslate = usesDragOverlay ? modifiedTranslate : add$1(modifiedTranslate, activeNodeScrollDelta);
  const transform = adjustScale(appliedTranslate, (_over$rect = over == null ? void 0 : over.rect) != null ? _over$rect : null, activeNodeRect);
  const activeSensorRef = reactExports.useRef(null);
  const instantiateSensor = reactExports.useCallback(
    (event, _ref2) => {
      let {
        sensor: Sensor,
        options
      } = _ref2;
      if (activeRef.current == null) {
        return;
      }
      const activeNode2 = draggableNodes.get(activeRef.current);
      if (!activeNode2) {
        return;
      }
      const activatorEvent2 = event.nativeEvent;
      const sensorInstance = new Sensor({
        active: activeRef.current,
        activeNode: activeNode2,
        event: activatorEvent2,
        options,
        // Sensors need to be instantiated with refs for arguments that change over time
        // otherwise they are frozen in time with the stale arguments
        context: sensorContext,
        onAbort(id3) {
          const draggableNode = draggableNodes.get(id3);
          if (!draggableNode) {
            return;
          }
          const {
            onDragAbort
          } = latestProps.current;
          const event2 = {
            id: id3
          };
          onDragAbort == null ? void 0 : onDragAbort(event2);
          dispatchMonitorEvent({
            type: "onDragAbort",
            event: event2
          });
        },
        onPending(id3, constraint, initialCoordinates, offset2) {
          const draggableNode = draggableNodes.get(id3);
          if (!draggableNode) {
            return;
          }
          const {
            onDragPending
          } = latestProps.current;
          const event2 = {
            id: id3,
            constraint,
            initialCoordinates,
            offset: offset2
          };
          onDragPending == null ? void 0 : onDragPending(event2);
          dispatchMonitorEvent({
            type: "onDragPending",
            event: event2
          });
        },
        onStart(initialCoordinates) {
          const id3 = activeRef.current;
          if (id3 == null) {
            return;
          }
          const draggableNode = draggableNodes.get(id3);
          if (!draggableNode) {
            return;
          }
          const {
            onDragStart
          } = latestProps.current;
          const event2 = {
            activatorEvent: activatorEvent2,
            active: {
              id: id3,
              data: draggableNode.data,
              rect: activeRects
            }
          };
          reactDomExports.unstable_batchedUpdates(() => {
            onDragStart == null ? void 0 : onDragStart(event2);
            setStatus(Status.Initializing);
            dispatch({
              type: Action.DragStart,
              initialCoordinates,
              active: id3
            });
            dispatchMonitorEvent({
              type: "onDragStart",
              event: event2
            });
            setActiveSensor(activeSensorRef.current);
            setActivatorEvent(activatorEvent2);
          });
        },
        onMove(coordinates) {
          dispatch({
            type: Action.DragMove,
            coordinates
          });
        },
        onEnd: createHandler(Action.DragEnd),
        onCancel: createHandler(Action.DragCancel)
      });
      activeSensorRef.current = sensorInstance;
      function createHandler(type) {
        return async function handler() {
          const {
            active: active2,
            collisions: collisions2,
            over: over2,
            scrollAdjustedTranslate: scrollAdjustedTranslate2
          } = sensorContext.current;
          let event2 = null;
          if (active2 && scrollAdjustedTranslate2) {
            const {
              cancelDrop
            } = latestProps.current;
            event2 = {
              activatorEvent: activatorEvent2,
              active: active2,
              collisions: collisions2,
              delta: scrollAdjustedTranslate2,
              over: over2
            };
            if (type === Action.DragEnd && typeof cancelDrop === "function") {
              const shouldCancel = await Promise.resolve(cancelDrop(event2));
              if (shouldCancel) {
                type = Action.DragCancel;
              }
            }
          }
          activeRef.current = null;
          reactDomExports.unstable_batchedUpdates(() => {
            dispatch({
              type
            });
            setStatus(Status.Uninitialized);
            setOver(null);
            setActiveSensor(null);
            setActivatorEvent(null);
            activeSensorRef.current = null;
            const eventName = type === Action.DragEnd ? "onDragEnd" : "onDragCancel";
            if (event2) {
              const handler2 = latestProps.current[eventName];
              handler2 == null ? void 0 : handler2(event2);
              dispatchMonitorEvent({
                type: eventName,
                event: event2
              });
            }
          });
        };
      }
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes]
  );
  const bindActivatorToSensorInstantiator = reactExports.useCallback((handler, sensor) => {
    return (event, active2) => {
      const nativeEvent = event.nativeEvent;
      const activeDraggableNode = draggableNodes.get(active2);
      if (
        // Another sensor is already instantiating
        activeRef.current !== null || // No active draggable
        !activeDraggableNode || // Event has already been captured
        nativeEvent.dndKit || nativeEvent.defaultPrevented
      ) {
        return;
      }
      const activationContext = {
        active: activeDraggableNode
      };
      const shouldActivate = handler(event, sensor.options, activationContext);
      if (shouldActivate === true) {
        nativeEvent.dndKit = {
          capturedBy: sensor.sensor
        };
        activeRef.current = active2;
        instantiateSensor(event, sensor);
      }
    };
  }, [draggableNodes, instantiateSensor]);
  const activators = useCombineActivators(sensors, bindActivatorToSensorInstantiator);
  useSensorSetup(sensors);
  useIsomorphicLayoutEffect(() => {
    if (activeNodeRect && status === Status.Initializing) {
      setStatus(Status.Initialized);
    }
  }, [activeNodeRect, status]);
  reactExports.useEffect(
    () => {
      const {
        onDragMove
      } = latestProps.current;
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        over: over2
      } = sensorContext.current;
      if (!active2 || !activatorEvent2) {
        return;
      }
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate.x,
          y: scrollAdjustedTranslate.y
        },
        over: over2
      };
      reactDomExports.unstable_batchedUpdates(() => {
        onDragMove == null ? void 0 : onDragMove(event);
        dispatchMonitorEvent({
          type: "onDragMove",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [scrollAdjustedTranslate.x, scrollAdjustedTranslate.y]
  );
  reactExports.useEffect(
    () => {
      const {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        droppableContainers: droppableContainers2,
        scrollAdjustedTranslate: scrollAdjustedTranslate2
      } = sensorContext.current;
      if (!active2 || activeRef.current == null || !activatorEvent2 || !scrollAdjustedTranslate2) {
        return;
      }
      const {
        onDragOver
      } = latestProps.current;
      const overContainer = droppableContainers2.get(overId);
      const over2 = overContainer && overContainer.rect.current ? {
        id: overContainer.id,
        rect: overContainer.rect.current,
        data: overContainer.data,
        disabled: overContainer.disabled
      } : null;
      const event = {
        active: active2,
        activatorEvent: activatorEvent2,
        collisions: collisions2,
        delta: {
          x: scrollAdjustedTranslate2.x,
          y: scrollAdjustedTranslate2.y
        },
        over: over2
      };
      reactDomExports.unstable_batchedUpdates(() => {
        setOver(over2);
        onDragOver == null ? void 0 : onDragOver(event);
        dispatchMonitorEvent({
          type: "onDragOver",
          event
        });
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [overId]
  );
  useIsomorphicLayoutEffect(() => {
    sensorContext.current = {
      activatorEvent,
      active,
      activeNode,
      collisionRect,
      collisions,
      droppableRects,
      draggableNodes,
      draggingNode,
      draggingNodeRect,
      droppableContainers,
      over,
      scrollableAncestors,
      scrollAdjustedTranslate
    };
    activeRects.current = {
      initial: draggingNodeRect,
      translated: collisionRect
    };
  }, [active, activeNode, collisions, collisionRect, draggableNodes, draggingNode, draggingNodeRect, droppableRects, droppableContainers, over, scrollableAncestors, scrollAdjustedTranslate]);
  useAutoScroller({
    ...autoScrollOptions,
    delta: translate,
    draggingRect: collisionRect,
    pointerCoordinates,
    scrollableAncestors,
    scrollableAncestorRects
  });
  const publicContext = reactExports.useMemo(() => {
    const context2 = {
      active,
      activeNode,
      activeNodeRect,
      activatorEvent,
      collisions,
      containerNodeRect,
      dragOverlay,
      draggableNodes,
      droppableContainers,
      droppableRects,
      over,
      measureDroppableContainers,
      scrollableAncestors,
      scrollableAncestorRects,
      measuringConfiguration,
      measuringScheduled,
      windowRect
    };
    return context2;
  }, [active, activeNode, activeNodeRect, activatorEvent, collisions, containerNodeRect, dragOverlay, draggableNodes, droppableContainers, droppableRects, over, measureDroppableContainers, scrollableAncestors, scrollableAncestorRects, measuringConfiguration, measuringScheduled, windowRect]);
  const internalContext = reactExports.useMemo(() => {
    const context2 = {
      activatorEvent,
      activators,
      active,
      activeNodeRect,
      ariaDescribedById: {
        draggable: draggableDescribedById
      },
      dispatch,
      draggableNodes,
      over,
      measureDroppableContainers
    };
    return context2;
  }, [activatorEvent, activators, active, activeNodeRect, dispatch, draggableDescribedById, draggableNodes, over, measureDroppableContainers]);
  return reactExports.createElement(DndMonitorContext.Provider, {
    value: registerMonitorListener
  }, reactExports.createElement(InternalContext.Provider, {
    value: internalContext
  }, reactExports.createElement(PublicContext.Provider, {
    value: publicContext
  }, reactExports.createElement(ActiveDraggableContext.Provider, {
    value: transform
  }, children)), reactExports.createElement(RestoreFocus, {
    disabled: (accessibility == null ? void 0 : accessibility.restoreFocus) === false
  })), reactExports.createElement(Accessibility, {
    ...accessibility,
    hiddenTextDescribedById: draggableDescribedById
  }));
  function getAutoScrollerOptions() {
    const activeSensorDisablesAutoscroll = (activeSensor == null ? void 0 : activeSensor.autoScrollEnabled) === false;
    const autoScrollGloballyDisabled = typeof autoScroll === "object" ? autoScroll.enabled === false : autoScroll === false;
    const enabled = isInitialized && !activeSensorDisablesAutoscroll && !autoScrollGloballyDisabled;
    if (typeof autoScroll === "object") {
      return {
        ...autoScroll,
        enabled
      };
    }
    return {
      enabled
    };
  }
});
const NullContext = /* @__PURE__ */ reactExports.createContext(null);
const defaultRole = "button";
const ID_PREFIX$1 = "Draggable";
function useDraggable(_ref) {
  let {
    id: id2,
    data,
    disabled = false,
    attributes
  } = _ref;
  const key = useUniqueId(ID_PREFIX$1);
  const {
    activators,
    activatorEvent,
    active,
    activeNodeRect,
    ariaDescribedById,
    draggableNodes,
    over
  } = reactExports.useContext(InternalContext);
  const {
    role = defaultRole,
    roleDescription = "draggable",
    tabIndex = 0
  } = attributes != null ? attributes : {};
  const isDragging = (active == null ? void 0 : active.id) === id2;
  const transform = reactExports.useContext(isDragging ? ActiveDraggableContext : NullContext);
  const [node, setNodeRef] = useNodeRef();
  const [activatorNode, setActivatorNodeRef] = useNodeRef();
  const listeners = useSyntheticListeners(activators, id2);
  const dataRef = useLatestValue(data);
  useIsomorphicLayoutEffect(
    () => {
      draggableNodes.set(id2, {
        id: id2,
        key,
        node,
        activatorNode,
        data: dataRef
      });
      return () => {
        const node2 = draggableNodes.get(id2);
        if (node2 && node2.key === key) {
          draggableNodes.delete(id2);
        }
      };
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [draggableNodes, id2]
  );
  const memoizedAttributes = reactExports.useMemo(() => ({
    role,
    tabIndex,
    "aria-disabled": disabled,
    "aria-pressed": isDragging && role === defaultRole ? true : void 0,
    "aria-roledescription": roleDescription,
    "aria-describedby": ariaDescribedById.draggable
  }), [disabled, role, tabIndex, isDragging, roleDescription, ariaDescribedById.draggable]);
  return {
    active,
    activatorEvent,
    activeNodeRect,
    attributes: memoizedAttributes,
    isDragging,
    listeners: disabled ? void 0 : listeners,
    node,
    over,
    setNodeRef,
    setActivatorNodeRef,
    transform
  };
}
function useDndContext() {
  return reactExports.useContext(PublicContext);
}
const ID_PREFIX$1$1 = "Droppable";
const defaultResizeObserverConfig = {
  timeout: 25
};
function useDroppable(_ref) {
  let {
    data,
    disabled = false,
    id: id2,
    resizeObserverConfig
  } = _ref;
  const key = useUniqueId(ID_PREFIX$1$1);
  const {
    active,
    dispatch,
    over,
    measureDroppableContainers
  } = reactExports.useContext(InternalContext);
  const previous = reactExports.useRef({
    disabled
  });
  const resizeObserverConnected = reactExports.useRef(false);
  const rect = reactExports.useRef(null);
  const callbackId = reactExports.useRef(null);
  const {
    disabled: resizeObserverDisabled,
    updateMeasurementsFor,
    timeout: resizeObserverTimeout
  } = {
    ...defaultResizeObserverConfig,
    ...resizeObserverConfig
  };
  const ids2 = useLatestValue(updateMeasurementsFor != null ? updateMeasurementsFor : id2);
  const handleResize = reactExports.useCallback(
    () => {
      if (!resizeObserverConnected.current) {
        resizeObserverConnected.current = true;
        return;
      }
      if (callbackId.current != null) {
        clearTimeout(callbackId.current);
      }
      callbackId.current = setTimeout(() => {
        measureDroppableContainers(Array.isArray(ids2.current) ? ids2.current : [ids2.current]);
        callbackId.current = null;
      }, resizeObserverTimeout);
    },
    //eslint-disable-next-line react-hooks/exhaustive-deps
    [resizeObserverTimeout]
  );
  const resizeObserver = useResizeObserver({
    callback: handleResize,
    disabled: resizeObserverDisabled || !active
  });
  const handleNodeChange = reactExports.useCallback((newElement, previousElement) => {
    if (!resizeObserver) {
      return;
    }
    if (previousElement) {
      resizeObserver.unobserve(previousElement);
      resizeObserverConnected.current = false;
    }
    if (newElement) {
      resizeObserver.observe(newElement);
    }
  }, [resizeObserver]);
  const [nodeRef, setNodeRef] = useNodeRef(handleNodeChange);
  const dataRef = useLatestValue(data);
  reactExports.useEffect(() => {
    if (!resizeObserver || !nodeRef.current) {
      return;
    }
    resizeObserver.disconnect();
    resizeObserverConnected.current = false;
    resizeObserver.observe(nodeRef.current);
  }, [nodeRef, resizeObserver]);
  reactExports.useEffect(
    () => {
      dispatch({
        type: Action.RegisterDroppable,
        element: {
          id: id2,
          key,
          disabled,
          node: nodeRef,
          rect,
          data: dataRef
        }
      });
      return () => dispatch({
        type: Action.UnregisterDroppable,
        key,
        id: id2
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [id2]
  );
  reactExports.useEffect(() => {
    if (disabled !== previous.current.disabled) {
      dispatch({
        type: Action.SetDroppableDisabled,
        id: id2,
        key,
        disabled
      });
      previous.current.disabled = disabled;
    }
  }, [id2, key, disabled, dispatch]);
  return {
    active,
    rect,
    isOver: (over == null ? void 0 : over.id) === id2,
    node: nodeRef,
    over,
    setNodeRef
  };
}
function arrayMove(array, from, to) {
  const newArray = array.slice();
  newArray.splice(to < 0 ? newArray.length + to : to, 0, newArray.splice(from, 1)[0]);
  return newArray;
}
function getSortedRects(items, rects) {
  return items.reduce((accumulator, id2, index2) => {
    const rect = rects.get(id2);
    if (rect) {
      accumulator[index2] = rect;
    }
    return accumulator;
  }, Array(items.length));
}
function isValidIndex(index2) {
  return index2 !== null && index2 >= 0;
}
function itemsEqual(a3, b2) {
  if (a3 === b2) {
    return true;
  }
  if (a3.length !== b2.length) {
    return false;
  }
  for (let i2 = 0; i2 < a3.length; i2++) {
    if (a3[i2] !== b2[i2]) {
      return false;
    }
  }
  return true;
}
function normalizeDisabled(disabled) {
  if (typeof disabled === "boolean") {
    return {
      draggable: disabled,
      droppable: disabled
    };
  }
  return disabled;
}
const rectSortingStrategy = (_ref) => {
  let {
    rects,
    activeIndex,
    overIndex,
    index: index2
  } = _ref;
  const newRects = arrayMove(rects, overIndex, activeIndex);
  const oldRect = rects[index2];
  const newRect = newRects[index2];
  if (!newRect || !oldRect) {
    return null;
  }
  return {
    x: newRect.left - oldRect.left,
    y: newRect.top - oldRect.top,
    scaleX: newRect.width / oldRect.width,
    scaleY: newRect.height / oldRect.height
  };
};
const defaultScale$1 = {
  scaleX: 1,
  scaleY: 1
};
const verticalListSortingStrategy = (_ref) => {
  var _rects$activeIndex;
  let {
    activeIndex,
    activeNodeRect: fallbackActiveRect,
    index: index2,
    rects,
    overIndex
  } = _ref;
  const activeNodeRect = (_rects$activeIndex = rects[activeIndex]) != null ? _rects$activeIndex : fallbackActiveRect;
  if (!activeNodeRect) {
    return null;
  }
  if (index2 === activeIndex) {
    const overIndexRect = rects[overIndex];
    if (!overIndexRect) {
      return null;
    }
    return {
      x: 0,
      y: activeIndex < overIndex ? overIndexRect.top + overIndexRect.height - (activeNodeRect.top + activeNodeRect.height) : overIndexRect.top - activeNodeRect.top,
      ...defaultScale$1
    };
  }
  const itemGap = getItemGap$1(rects, index2, activeIndex);
  if (index2 > activeIndex && index2 <= overIndex) {
    return {
      x: 0,
      y: -activeNodeRect.height - itemGap,
      ...defaultScale$1
    };
  }
  if (index2 < activeIndex && index2 >= overIndex) {
    return {
      x: 0,
      y: activeNodeRect.height + itemGap,
      ...defaultScale$1
    };
  }
  return {
    x: 0,
    y: 0,
    ...defaultScale$1
  };
};
function getItemGap$1(clientRects, index2, activeIndex) {
  const currentRect = clientRects[index2];
  const previousRect = clientRects[index2 - 1];
  const nextRect = clientRects[index2 + 1];
  if (!currentRect) {
    return 0;
  }
  if (activeIndex < index2) {
    return previousRect ? currentRect.top - (previousRect.top + previousRect.height) : nextRect ? nextRect.top - (currentRect.top + currentRect.height) : 0;
  }
  return nextRect ? nextRect.top - (currentRect.top + currentRect.height) : previousRect ? currentRect.top - (previousRect.top + previousRect.height) : 0;
}
const ID_PREFIX = "Sortable";
const Context = /* @__PURE__ */ reactExports.createContext({
  activeIndex: -1,
  containerId: ID_PREFIX,
  disableTransforms: false,
  items: [],
  overIndex: -1,
  useDragOverlay: false,
  sortedRects: [],
  strategy: rectSortingStrategy,
  disabled: {
    draggable: false,
    droppable: false
  }
});
function SortableContext(_ref) {
  let {
    children,
    id: id2,
    items: userDefinedItems,
    strategy = rectSortingStrategy,
    disabled: disabledProp = false
  } = _ref;
  const {
    active,
    dragOverlay,
    droppableRects,
    over,
    measureDroppableContainers
  } = useDndContext();
  const containerId = useUniqueId(ID_PREFIX, id2);
  const useDragOverlay = Boolean(dragOverlay.rect !== null);
  const items = reactExports.useMemo(() => userDefinedItems.map((item) => typeof item === "object" && "id" in item ? item.id : item), [userDefinedItems]);
  const isDragging = active != null;
  const activeIndex = active ? items.indexOf(active.id) : -1;
  const overIndex = over ? items.indexOf(over.id) : -1;
  const previousItemsRef = reactExports.useRef(items);
  const itemsHaveChanged = !itemsEqual(items, previousItemsRef.current);
  const disableTransforms = overIndex !== -1 && activeIndex === -1 || itemsHaveChanged;
  const disabled = normalizeDisabled(disabledProp);
  useIsomorphicLayoutEffect(() => {
    if (itemsHaveChanged && isDragging) {
      measureDroppableContainers(items);
    }
  }, [itemsHaveChanged, items, isDragging, measureDroppableContainers]);
  reactExports.useEffect(() => {
    previousItemsRef.current = items;
  }, [items]);
  const contextValue = reactExports.useMemo(
    () => ({
      activeIndex,
      containerId,
      disabled,
      disableTransforms,
      items,
      overIndex,
      useDragOverlay,
      sortedRects: getSortedRects(items, droppableRects),
      strategy
    }),
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [activeIndex, containerId, disabled.draggable, disabled.droppable, disableTransforms, items, overIndex, droppableRects, useDragOverlay, strategy]
  );
  return reactExports.createElement(Context.Provider, {
    value: contextValue
  }, children);
}
const defaultNewIndexGetter = (_ref) => {
  let {
    id: id2,
    items,
    activeIndex,
    overIndex
  } = _ref;
  return arrayMove(items, activeIndex, overIndex).indexOf(id2);
};
const defaultAnimateLayoutChanges = (_ref2) => {
  let {
    containerId,
    isSorting,
    wasDragging,
    index: index2,
    items,
    newIndex,
    previousItems,
    previousContainerId,
    transition
  } = _ref2;
  if (!transition || !wasDragging) {
    return false;
  }
  if (previousItems !== items && index2 === newIndex) {
    return false;
  }
  if (isSorting) {
    return true;
  }
  return newIndex !== index2 && containerId === previousContainerId;
};
const defaultTransition = {
  duration: 200,
  easing: "ease"
};
const transitionProperty = "transform";
const disabledTransition = /* @__PURE__ */ CSS.Transition.toString({
  property: transitionProperty,
  duration: 0,
  easing: "linear"
});
const defaultAttributes = {
  roleDescription: "sortable"
};
function useDerivedTransform(_ref) {
  let {
    disabled,
    index: index2,
    node,
    rect
  } = _ref;
  const [derivedTransform, setDerivedtransform] = reactExports.useState(null);
  const previousIndex = reactExports.useRef(index2);
  useIsomorphicLayoutEffect(() => {
    if (!disabled && index2 !== previousIndex.current && node.current) {
      const initial = rect.current;
      if (initial) {
        const current = getClientRect(node.current, {
          ignoreTransform: true
        });
        const delta = {
          x: initial.left - current.left,
          y: initial.top - current.top,
          scaleX: initial.width / current.width,
          scaleY: initial.height / current.height
        };
        if (delta.x || delta.y) {
          setDerivedtransform(delta);
        }
      }
    }
    if (index2 !== previousIndex.current) {
      previousIndex.current = index2;
    }
  }, [disabled, index2, node, rect]);
  reactExports.useEffect(() => {
    if (derivedTransform) {
      setDerivedtransform(null);
    }
  }, [derivedTransform]);
  return derivedTransform;
}
function useSortable(_ref) {
  let {
    animateLayoutChanges = defaultAnimateLayoutChanges,
    attributes: userDefinedAttributes,
    disabled: localDisabled,
    data: customData,
    getNewIndex = defaultNewIndexGetter,
    id: id2,
    strategy: localStrategy,
    resizeObserverConfig,
    transition = defaultTransition
  } = _ref;
  const {
    items,
    containerId,
    activeIndex,
    disabled: globalDisabled,
    disableTransforms,
    sortedRects,
    overIndex,
    useDragOverlay,
    strategy: globalStrategy
  } = reactExports.useContext(Context);
  const disabled = normalizeLocalDisabled(localDisabled, globalDisabled);
  const index2 = items.indexOf(id2);
  const data = reactExports.useMemo(() => ({
    sortable: {
      containerId,
      index: index2,
      items
    },
    ...customData
  }), [containerId, customData, index2, items]);
  const itemsAfterCurrentSortable = reactExports.useMemo(() => items.slice(items.indexOf(id2)), [items, id2]);
  const {
    rect,
    node,
    isOver,
    setNodeRef: setDroppableNodeRef
  } = useDroppable({
    id: id2,
    data,
    disabled: disabled.droppable,
    resizeObserverConfig: {
      updateMeasurementsFor: itemsAfterCurrentSortable,
      ...resizeObserverConfig
    }
  });
  const {
    active,
    activatorEvent,
    activeNodeRect,
    attributes,
    setNodeRef: setDraggableNodeRef,
    listeners,
    isDragging,
    over,
    setActivatorNodeRef,
    transform
  } = useDraggable({
    id: id2,
    data,
    attributes: {
      ...defaultAttributes,
      ...userDefinedAttributes
    },
    disabled: disabled.draggable
  });
  const setNodeRef = useCombinedRefs(setDroppableNodeRef, setDraggableNodeRef);
  const isSorting = Boolean(active);
  const displaceItem = isSorting && !disableTransforms && isValidIndex(activeIndex) && isValidIndex(overIndex);
  const shouldDisplaceDragSource = !useDragOverlay && isDragging;
  const dragSourceDisplacement = shouldDisplaceDragSource && displaceItem ? transform : null;
  const strategy = localStrategy != null ? localStrategy : globalStrategy;
  const finalTransform = displaceItem ? dragSourceDisplacement != null ? dragSourceDisplacement : strategy({
    rects: sortedRects,
    activeNodeRect,
    activeIndex,
    overIndex,
    index: index2
  }) : null;
  const newIndex = isValidIndex(activeIndex) && isValidIndex(overIndex) ? getNewIndex({
    id: id2,
    items,
    activeIndex,
    overIndex
  }) : index2;
  const activeId = active == null ? void 0 : active.id;
  const previous = reactExports.useRef({
    activeId,
    items,
    newIndex,
    containerId
  });
  const itemsHaveChanged = items !== previous.current.items;
  const shouldAnimateLayoutChanges = animateLayoutChanges({
    active,
    containerId,
    isDragging,
    isSorting,
    id: id2,
    index: index2,
    items,
    newIndex: previous.current.newIndex,
    previousItems: previous.current.items,
    previousContainerId: previous.current.containerId,
    transition,
    wasDragging: previous.current.activeId != null
  });
  const derivedTransform = useDerivedTransform({
    disabled: !shouldAnimateLayoutChanges,
    index: index2,
    node,
    rect
  });
  reactExports.useEffect(() => {
    if (isSorting && previous.current.newIndex !== newIndex) {
      previous.current.newIndex = newIndex;
    }
    if (containerId !== previous.current.containerId) {
      previous.current.containerId = containerId;
    }
    if (items !== previous.current.items) {
      previous.current.items = items;
    }
  }, [isSorting, newIndex, containerId, items]);
  reactExports.useEffect(() => {
    if (activeId === previous.current.activeId) {
      return;
    }
    if (activeId && !previous.current.activeId) {
      previous.current.activeId = activeId;
      return;
    }
    const timeoutId = setTimeout(() => {
      previous.current.activeId = activeId;
    }, 50);
    return () => clearTimeout(timeoutId);
  }, [activeId]);
  return {
    active,
    activeIndex,
    attributes,
    data,
    rect,
    index: index2,
    newIndex,
    items,
    isOver,
    isSorting,
    isDragging,
    listeners,
    node,
    overIndex,
    over,
    setNodeRef,
    setActivatorNodeRef,
    setDroppableNodeRef,
    setDraggableNodeRef,
    transform: derivedTransform != null ? derivedTransform : finalTransform,
    transition: getTransition()
  };
  function getTransition() {
    if (
      // Temporarily disable transitions for a single frame to set up derived transforms
      derivedTransform || // Or to prevent items jumping to back to their "new" position when items change
      itemsHaveChanged && previous.current.newIndex === index2
    ) {
      return disabledTransition;
    }
    if (shouldDisplaceDragSource && !isKeyboardEvent(activatorEvent) || !transition) {
      return void 0;
    }
    if (isSorting || shouldAnimateLayoutChanges) {
      return CSS.Transition.toString({
        ...transition,
        property: transitionProperty
      });
    }
    return void 0;
  }
}
function normalizeLocalDisabled(localDisabled, globalDisabled) {
  var _localDisabled$dragga, _localDisabled$droppa;
  if (typeof localDisabled === "boolean") {
    return {
      draggable: localDisabled,
      // Backwards compatibility
      droppable: false
    };
  }
  return {
    draggable: (_localDisabled$dragga = localDisabled == null ? void 0 : localDisabled.draggable) != null ? _localDisabled$dragga : globalDisabled.draggable,
    droppable: (_localDisabled$droppa = localDisabled == null ? void 0 : localDisabled.droppable) != null ? _localDisabled$droppa : globalDisabled.droppable
  };
}
[KeyboardCode.Down, KeyboardCode.Right, KeyboardCode.Up, KeyboardCode.Left];
function ModuleGroup({
  group,
  index: index2,
  collapsed,
  toggleGroup,
  role,
  permissions,
  togglePermission
}) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "module-group mb-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        type: "button",
        className: "btn btn-link btn-sm arrow-toggle",
        onClick: () => toggleGroup(index2),
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: `bi ${collapsed ? "bi-caret-right" : "bi-caret-down"}` })
      }
    ),
    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "ms-1 fw-semibold", children: group.category }),
    !collapsed && /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { className: "list-group mt-2", children: group.modules.map((m3) => /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "li",
      {
        className: "list-group-item d-flex justify-content-between align-items-center",
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("span", { children: m3.replace(/-/g, " ") }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "form-check form-switch", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              className: "form-check-input",
              type: "checkbox",
              checked: (permissions[role] || []).includes(m3),
              onChange: () => togglePermission(role, m3)
            }
          ) })
        ]
      },
      m3
    )) })
  ] });
}
function HierarchyApp() {
  const [roles, setRoles] = reactExports.useState([]);
  const [moduleGroups, setModuleGroups] = reactExports.useState([]);
  const [permissions, setPermissions] = reactExports.useState({});
  const [selectedRole, setSelectedRole] = reactExports.useState("");
  const [modulesCollapsed, setModulesCollapsed] = reactExports.useState(true);
  const [collapsedGroups, setCollapsedGroups] = reactExports.useState({});
  reactExports.useEffect(() => {
    async function load() {
      try {
        const [roleRes, permRes, modRes] = await Promise.all([
          fetch("/api/hierarchy"),
          fetch("/api/permissions"),
          fetch("/api/modules")
        ]);
        const roleJson = await roleRes.json();
        const permJson = await permRes.json();
        const modJson = await modRes.json();
        setRoles(roleJson.hierarchy || []);
        setPermissions(permJson.permissions || {});
        if (Array.isArray(modJson.groups)) {
          setModuleGroups(modJson.groups);
        } else if (Array.isArray(modJson.modules)) {
          setModuleGroups([{ category: "modules", modules: modJson.modules }]);
        } else {
          setModuleGroups([]);
        }
      } catch (err) {
        console.error(err);
      }
    }
    load();
  }, []);
  reactExports.useEffect(() => {
    if (roles.length && !selectedRole) setSelectedRole(roles[0]);
  }, [roles, selectedRole]);
  const saveHierarchy = async (arr) => {
    setRoles(arr);
    try {
      await fetch("/api/hierarchy", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ hierarchy: arr })
      });
    } catch (err) {
      console.error(err);
    }
  };
  const savePermissions = async (obj) => {
    setPermissions(obj);
    try {
      await fetch("/api/permissions", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ permissions: obj })
      });
    } catch (err) {
      console.error(err);
    }
  };
  const addRole = (e2) => {
    e2.preventDefault();
    const name2 = e2.target.role.value.trim();
    if (!name2 || roles.includes(name2)) return;
    const list = [...roles, name2];
    saveHierarchy(list);
    setPermissions((p) => ({ ...p, [name2]: [] }));
    e2.target.reset();
  };
  const sensors = useSensors(
    useSensor(PointerSensor, { activationConstraint: { distance: 5 } })
  );
  const handleDragEnd = (event) => {
    const { active, over } = event;
    if (over && active.id !== over.id) {
      const oldIndex = roles.indexOf(active.id);
      const newIndex = roles.indexOf(over.id);
      const list = arrayMove(roles, oldIndex, newIndex);
      saveHierarchy(list);
    }
  };
  const renameRole = (idx, newName) => {
    const old = roles[idx];
    if (!old || !newName || roles.includes(newName)) return;
    const list = roles.slice();
    list[idx] = newName;
    const perms = { ...permissions };
    if (perms[old]) {
      perms[newName] = perms[old];
      delete perms[old];
    }
    saveHierarchy(list);
    savePermissions(perms);
    if (selectedRole === old) setSelectedRole(newName);
  };
  const removeRole = (idx) => {
    if (idx === roles.length - 1) {
      alert("Cannot remove the highest role");
      return;
    }
    if (!window.confirm("Remove this role?")) return;
    const name2 = roles[idx];
    const list = roles.filter((_, i2) => i2 !== idx);
    const perms = { ...permissions };
    delete perms[name2];
    saveHierarchy(list);
    savePermissions(perms);
    if (selectedRole === name2) setSelectedRole(list[0] || "");
  };
  const togglePermission = (role, mod) => {
    const current = new Set(permissions[role] || []);
    if (current.has(mod)) current.delete(mod);
    else current.add(mod);
    savePermissions({ ...permissions, [role]: Array.from(current) });
  };
  const toggleGroup = (idx) => {
    setCollapsedGroups((c2) => ({ ...c2, [idx]: !c2[idx] }));
  };
  const RoleItem = ({ id: id2, index: index2, highest }) => {
    const {
      attributes,
      listeners,
      setNodeRef,
      transform,
      transition
    } = useSortable({ id: id2 });
    const style2 = {
      transform: CSS.Transform.toString(transform),
      transition
    };
    const [editing, setEditing] = reactExports.useState(false);
    const [name2, setName] = reactExports.useState(id2);
    const finishEdit = () => {
      setEditing(false);
      if (name2.trim() && name2.trim() !== id2) renameRole(index2, name2.trim());
      else setName(id2);
    };
    const onKeyDown = (e2) => {
      if (e2.key === "Enter") {
        e2.preventDefault();
        finishEdit();
      } else if (e2.key === "Escape") {
        e2.preventDefault();
        setName(id2);
        setEditing(false);
      }
    };
    return /* @__PURE__ */ jsxRuntimeExports.jsxs(
      "div",
      {
        ref: setNodeRef,
        style: style2,
        className: `role-card${id2 === selectedRole ? " selected" : ""}`,
        onClick: () => setSelectedRole(id2),
        children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex align-items-center flex-grow-1", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "drag-handle me-2", ...attributes, ...listeners, children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-grip-vertical" }) }),
            editing ? /* @__PURE__ */ jsxRuntimeExports.jsx(
              "input",
              {
                className: "form-control form-control-sm edit-input me-2",
                value: name2,
                onChange: (e2) => setName(e2.target.value),
                onBlur: finishEdit,
                onKeyDown,
                autoFocus: true
              }
            ) : /* @__PURE__ */ jsxRuntimeExports.jsxs(
              "span",
              {
                className: "role-name flex-grow-1",
                onDoubleClick: () => setEditing(true),
                children: [
                  id2,
                  highest && /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "badge bg-success ms-2", children: "Highest" })
                ]
              }
            )
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex align-items-center ms-2", children: [
            !editing && /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: "btn btn-sm btn-link p-0 me-2",
                onClick: () => setEditing(true),
                "aria-label": "Rename",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-pencil" })
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsx(
              "button",
              {
                className: "delete-btn",
                onClick: () => removeRole(index2),
                "aria-label": "Delete",
                children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-trash" })
              }
            )
          ] })
        ]
      }
    );
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "hierarchy-wrapper", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "roles-panel", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { children: "Roles" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-muted small", children: "Order roles from lowest to highest privilege. The last role has access to everything." }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(DndContext, { sensors, collisionDetection: closestCenter, onDragEnd: handleDragEnd, children: /* @__PURE__ */ jsxRuntimeExports.jsx(SortableContext, { items: roles, strategy: verticalListSortingStrategy, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "roles-list mb-2", children: roles.map((r2, i2) => /* @__PURE__ */ jsxRuntimeExports.jsx(RoleItem, { id: r2, index: i2, highest: i2 === roles.length - 1 }, r2)) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { onSubmit: addRole, className: "d-flex", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "input",
          {
            type: "text",
            name: "role",
            className: "form-control form-control-sm me-2",
            placeholder: "New role"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", className: "btn btn-sm btn-primary", children: "Add" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modules-panel", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("h5", { className: "d-flex align-items-center", children: [
        "Module Access",
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            className: "btn btn-link btn-sm ms-2 module-toggle",
            onClick: () => setModulesCollapsed(!modulesCollapsed),
            children: modulesCollapsed ? "Show" : "Hide"
          }
        )
      ] }),
      !modulesCollapsed && selectedRole && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: moduleGroups.map((grp, idx) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        ModuleGroup,
        {
          group: grp,
          index: idx,
          collapsed: collapsedGroups[idx],
          toggleGroup,
          role: selectedRole,
          permissions,
          togglePermission
        },
        grp.category || idx
      )) })
    ] })
  ] });
}
function RolesRoute() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Roles & Access" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(HierarchyApp, {})
  ] });
}
function PayrollRoute() {
  const [rows, setRows] = reactExports.useState([]);
  const [error, setError] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    let active = true;
    (async () => {
      try {
        const res = await fetch("/api/payroll");
        if (!res.ok) throw new Error("Failed to load payroll");
        const json = await res.json();
        if (active) setRows(json.payroll || []);
      } catch (e2) {
        if (active) setError(e2.message || "Error");
      } finally {
        if (active) setLoading(false);
      }
    })();
    return () => {
      active = false;
    };
  }, []);
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  if (error) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Payroll" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm align-middle", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Employee" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Period Start" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Hours" })
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: rows.map((r2, i2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: r2.employee_id || r2.username || "-" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: r2.period_start }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: Number(r2.hours).toFixed(2) })
      ] }, i2)) })
    ] }) })
  ] });
}
function useAdminData() {
  const [data, setData] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  const [error, setError] = reactExports.useState(null);
  const reload = async () => {
    setLoading(true);
    setError(null);
    try {
      const res = await fetch("/api/admin");
      if (!res.ok) throw new Error("Failed to load admin data");
      const json = await res.json();
      setData(json);
    } catch (e2) {
      setError(e2.message || "Error");
    } finally {
      setLoading(false);
    }
  };
  reactExports.useEffect(() => {
    reload();
  }, []);
  return { data, loading, error, reload };
}
function InventoryRoute() {
  const { data, loading, error, reload } = useAdminData();
  const [form, setForm] = reactExports.useState({});
  const [saving, setSaving] = reactExports.useState(false);
  const summary = reactExports.useMemo(() => {
    if (!data) return [];
    return [
      { label: "Ingredients", value: data.ingredients?.length || 0 },
      { label: "Suppliers", value: data.suppliers?.length || 0 },
      { label: "Purchase Orders", value: data.orders?.length || 0 },
      { label: "Locations", value: data.locations?.length || 0 },
      { label: "Units", value: data.units?.length || 0 },
      { label: "Tags", value: data.tags?.length || 0 }
    ];
  }, [data]);
  const setField = (k2, v) => setForm((f) => ({ ...f, [k2]: v }));
  const saveIngredient = async (e2) => {
    e2.preventDefault();
    setSaving(true);
    try {
      const body = new URLSearchParams();
      Object.entries(form).forEach(([k2, v]) => {
        if (v !== void 0 && v !== null) body.append(k2, String(v));
      });
      await fetch("/api/admin/ingredients", {
        method: "POST",
        headers: { "Content-Type": "application/x-www-form-urlencoded" },
        body,
        redirect: "follow"
      });
      setForm({});
      await reload();
    } catch {
    } finally {
      setSaving(false);
    }
  };
  const currentUnits = data?.units || [];
  const currentCats = data?.itemCategories || [];
  const currentTags = data?.tags || [];
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  if (error) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error });
  if (!data) return null;
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "m-0", children: "Inventory" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-outline-primary", onClick: reload, children: "Refresh" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-grid", children: summary.map((s3) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "card-surface p-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted small", children: s3.label }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "fs-3 fw-bold", children: s3.value })
    ] }, s3.label)) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section mt-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "mb-3", children: "Add / Edit Ingredient" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { className: "row g-2", onSubmit: saveIngredient, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", placeholder: "Name", value: form.name || "", onChange: (e2) => setField("name", e2.target.value), required: true }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", placeholder: "Quantity", type: "number", step: "any", value: form.quantity || "", onChange: (e2) => setField("quantity", e2.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { className: "form-select", value: form.unit_id || "", onChange: (e2) => setField("unit_id", e2.target.value), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "Unit" }),
          currentUnits.map((u2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: u2.id, children: u2.abbreviation || u2.name }, u2.id))
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { className: "form-select", value: form.category_id || "", onChange: (e2) => setField("category_id", e2.target.value), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "Category" }),
          currentCats.map((c2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: c2.id, children: c2.name }, c2.id))
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", placeholder: "SKU", value: form.sku || "", onChange: (e2) => setField("sku", e2.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", placeholder: "Total Cost", type: "number", step: "any", value: form.cost || "", onChange: (e2) => setField("cost", e2.target.value) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("select", { multiple: true, className: "form-select", value: form.tag_ids || [], onChange: (e2) => setField("tag_ids", Array.from(e2.target.selectedOptions).map((o) => o.value)), children: currentTags.map((t2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: t2.id, children: t2.name }, t2.id)) }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-3 d-flex align-items-center", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "form-check", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-check-input", type: "checkbox", id: "isPublic", checked: !!form.is_public, onChange: (e2) => setField("is_public", e2.target.checked ? 1 : 0) }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "isPublic", className: "form-check-label ms-1", children: "Public" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-md-2", children: /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-primary w-100", disabled: saving, children: "Save" }) })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section mt-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { className: "mb-2", children: "Ingredients" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm align-middle", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Name" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Qty" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Unit" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "SKU" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Cost" })
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: (data.ingredients || []).map((ing) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.name }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.quantity }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.unit }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.sku || "-" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: ing.cost ?? "-" })
        ] }, ing.id)) })
      ] }) })
    ] })
  ] });
}
function RowEditor({ row, onSave, saving }) {
  const [inVal, setInVal] = reactExports.useState(
    row.clock_in ? new Date(row.clock_in).toISOString().slice(0, 16) : ""
  );
  const [outVal, setOutVal] = reactExports.useState(
    row.clock_out ? new Date(row.clock_out).toISOString().slice(0, 16) : ""
  );
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: row.id }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: row.name }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { style: { minWidth: "220px" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        type: "datetime-local",
        className: "form-control form-control-sm",
        value: inVal,
        onChange: (e2) => setInVal(e2.target.value)
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { style: { minWidth: "220px" }, children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        type: "datetime-local",
        className: "form-control form-control-sm",
        value: outVal,
        onChange: (e2) => setOutVal(e2.target.value)
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "button",
      {
        className: "btn btn-sm btn-primary",
        disabled: saving,
        onClick: () => onSave(
          row.id,
          inVal ? new Date(inVal).toISOString() : null,
          outVal ? new Date(outVal).toISOString() : null
        ),
        children: "Save"
      }
    ) })
  ] });
}
function TimeClockRoute() {
  const [rows, setRows] = reactExports.useState([]);
  const [error, setError] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  const [savingId, setSavingId] = reactExports.useState(null);
  const load = async () => {
    setLoading(true);
    setError(null);
    try {
      const res = await fetch("/api/time-clock");
      if (!res.ok) throw new Error("Failed to load");
      const json = await res.json();
      setRows(json.records || []);
    } catch (e2) {
      setError(e2.message || "Error");
    } finally {
      setLoading(false);
    }
  };
  reactExports.useEffect(() => {
    load();
  }, []);
  const save = async (id2, clock_in, clock_out) => {
    setSavingId(id2);
    try {
      const res = await fetch(`/api/time-clock/${id2}`, {
        method: "PUT",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ clock_in, clock_out })
      });
      if (!res.ok) throw new Error("Save failed");
      await load();
    } catch (e2) {
      setError(e2.message || "Error");
    } finally {
      setSavingId(null);
    }
  };
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  if (error) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Time Clock" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm align-middle", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "ID" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Employee" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Clock In" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Clock Out" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", {})
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: rows.map((r2) => /* @__PURE__ */ jsxRuntimeExports.jsx(RowEditor, { row: r2, saving: savingId === r2.id, onSave: save }, r2.id)) })
    ] }) })
  ] });
}
function BackupsRoute() {
  const [list, setList] = reactExports.useState([]);
  const [status, setStatus] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [running, setRunning] = reactExports.useState(false);
  const [selected, setSelected] = reactExports.useState("");
  const [uploading, setUploading] = reactExports.useState(false);
  const load = async () => {
    try {
      const [ls, st] = await Promise.all([
        fetch("/api/admin/backups/list"),
        fetch("/api/admin/backups/status")
      ]);
      const lsJson = await ls.json();
      const stJson = await st.json();
      setList(lsJson.backups || []);
      setStatus(stJson || {});
    } catch (e2) {
      setError(e2.message || "Error");
    }
  };
  reactExports.useEffect(() => {
    load();
  }, []);
  reactExports.useEffect(() => {
    if (!status?.running) return;
    const t2 = setInterval(load, 2e3);
    return () => clearInterval(t2);
  }, [status?.running]);
  const createBackup = async () => {
    setRunning(true);
    try {
      await fetch("/api/admin/backups/create", { method: "POST" });
      await load();
    } finally {
      setRunning(false);
    }
  };
  const del = async (file) => {
    const body = new URLSearchParams();
    body.append("file", file);
    await fetch("/api/admin/backups/delete", { method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded" }, body });
    await load();
  };
  const restore = async () => {
    if (!selected) return;
    if (!confirm(`Restore backup ${selected}? This will overwrite current data.`)) return;
    const body = new URLSearchParams();
    body.append("file", selected);
    await fetch("/api/admin/backups/restore", { method: "POST", headers: { "Content-Type": "application/x-www-form-urlencoded" }, body });
    await load();
  };
  const upload = async (e2) => {
    const file = e2.target.files?.[0];
    if (!file) return;
    setUploading(true);
    try {
      const form = new FormData();
      form.append("backup", file);
      await fetch("/api/admin/backups/upload", { method: "POST", body: form });
      await load();
    } finally {
      setUploading(false);
      e2.target.value = "";
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex align-items-center mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "m-0", children: "Backups" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-primary ms-3", onClick: createBackup, disabled: running, children: "Create Backup" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-outline-secondary ms-2", onClick: load, children: "Refresh" })
    ] }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2 text-muted small", children: [
      "Status: ",
      status?.running ? "Running" : "Idle"
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx("pre", { className: "small", style: { maxHeight: 160, overflow: "auto", background: "#f8f9fa", padding: "0.5rem" }, children: (() => {
      const [log, setLog] = reactExports.useState("");
      reactExports.useEffect(() => {
        let mounted = true;
        const loadLog = async () => {
          try {
            const res = await fetch("/api/admin/backups/log");
            const text2 = await res.text();
            if (mounted) setLog(text2 || "");
          } catch {
          }
        };
        loadLog();
        if (status?.running) {
          const t2 = setInterval(loadLog, 2e3);
          return () => {
            mounted = false;
            clearInterval(t2);
          };
        }
        return () => {
          mounted = false;
        };
      }, [status?.running]);
      return log;
    })() }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex align-items-center gap-2 mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { className: "form-select w-auto", value: selected, onChange: (e2) => setSelected(e2.target.value), children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "", children: "Select backup to restore…" }),
        list.map((f) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: f, children: f }, f))
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-outline-warning", onClick: restore, disabled: !selected, children: "Restore" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "btn btn-outline-secondary mb-0", children: [
        "Upload",
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "file", className: "d-none", onChange: upload, disabled: uploading })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "table-responsive", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table table-sm align-middle", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "File" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", {})
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: list.map((f) => /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: f }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("td", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("a", { className: "btn btn-sm btn-outline-primary me-2", href: `/api/admin/backups/download?file=${encodeURIComponent(f)}`, children: "Download" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-outline-danger", onClick: () => del(f), children: "Delete" })
        ] })
      ] }, f)) })
    ] }) })
  ] });
}
function RecipeModal() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { id: "addRecipeModal", className: "modal custom-modal d-none", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modal-content bg-white text-dark", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Item Recipe" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "recipe-container mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label", children: "Recipe Steps" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "recipe-rows" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex gap-2 mb-2", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "text", className: "form-control recipe-input", placeholder: "Instruction" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", className: "btn btn-sm btn-secondary add-recipe-row", children: "Add Step" })
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "ingredient-container mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label", children: "Ingredients" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "ingredient-rows" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", className: "btn btn-sm btn-secondary add-ingredient-row", children: "Add Ingredient" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "text-end", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", id: "recipeSave", className: "btn btn-primary", children: "Save" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "button", id: "recipeCancel", className: "btn btn-secondary", children: "Cancel" })
    ] })
  ] }) });
}
const AdminMenuContext = reactExports.createContext();
function AdminMenuProvider({ ingredients = [], units = [], children }) {
  const [ingredientFields, setIngredientFields] = reactExports.useState([
    { name: "", unitId: "", amount: "" }
  ]);
  const [modifiers, setModifiers] = reactExports.useState({});
  const addIngredient = () => setIngredientFields((fields) => [
    ...fields,
    { name: "", unitId: "", amount: "" }
  ]);
  const removeIngredient = (index2) => setIngredientFields((fields) => fields.filter((_, i2) => i2 !== index2));
  const updateIngredient = (index2, field) => setIngredientFields(
    (fields) => fields.map((f, i2) => i2 === index2 ? { ...f, ...field } : f)
  );
  const toggleModifier = (id2, checked) => setModifiers((mods) => ({
    ...mods,
    [id2]: { ...mods[id2], checked }
  }));
  const setModifierReplace = (id2, replace) => setModifiers((mods) => ({
    ...mods,
    [id2]: { ...mods[id2], replace }
  }));
  const value = {
    ingredients,
    units,
    ingredientFields,
    addIngredient,
    removeIngredient,
    updateIngredient,
    modifiers,
    toggleModifier,
    setModifierReplace
  };
  return reactExports.createElement(AdminMenuContext.Provider, { value }, children);
}
function useAdminMenu() {
  return reactExports.useContext(AdminMenuContext);
}
function IngredientField({ index: index2 }) {
  const { ingredientFields, updateIngredient, removeIngredient, units } = useAdminMenu();
  const field = ingredientFields[index2];
  return reactExports.createElement(
    "div",
    { className: "ingredient-row" },
    reactExports.createElement("input", {
      placeholder: "Ingredient",
      value: field.name,
      onChange: (e2) => updateIngredient(index2, { name: e2.target.value })
    }),
    reactExports.createElement(
      "select",
      {
        value: field.unitId,
        onChange: (e2) => updateIngredient(index2, { unitId: e2.target.value })
      },
      reactExports.createElement("option", { value: "" }, ""),
      units.map(
        (u2) => reactExports.createElement("option", { key: u2.id, value: u2.id }, u2.abbreviation)
      )
    ),
    reactExports.createElement("input", {
      type: "number",
      value: field.amount,
      onChange: (e2) => updateIngredient(index2, { amount: e2.target.value })
    }),
    reactExports.createElement(
      "button",
      { type: "button", onClick: () => removeIngredient(index2) },
      "Remove"
    )
  );
}
function IngredientList() {
  const { ingredientFields, addIngredient } = useAdminMenu();
  return reactExports.createElement(
    reactExports.Fragment,
    null,
    ingredientFields.map(
      (_, idx) => reactExports.createElement(IngredientField, { key: idx, index: idx })
    ),
    reactExports.createElement(
      "button",
      { type: "button", onClick: addIngredient },
      "Add Ingredient"
    )
  );
}
function ModifierList({ modifiers = [] }) {
  const { toggleModifier, setModifierReplace, ingredientFields, modifiers: selected } = useAdminMenu();
  return reactExports.createElement(
    "div",
    null,
    modifiers.map(
      (mod) => reactExports.createElement(
        "div",
        { key: mod.id },
        reactExports.createElement(
          "label",
          null,
          reactExports.createElement("input", {
            type: "checkbox",
            checked: selected[mod.id]?.checked || false,
            onChange: (e2) => toggleModifier(mod.id, e2.target.checked)
          }),
          mod.name
        ),
        reactExports.createElement(
          "select",
          {
            "aria-label": `Replace ${mod.name}`,
            value: selected[mod.id]?.replace || "",
            onChange: (e2) => setModifierReplace(mod.id, e2.target.value),
            disabled: !selected[mod.id]?.checked
          },
          reactExports.createElement("option", { value: "" }, "Adds"),
          ingredientFields.map(
            (f, idx) => reactExports.createElement(
              "option",
              { key: idx, value: String(idx) },
              f.name || `Ingredient ${idx + 1}`
            )
          )
        )
      )
    )
  );
}
function MenuEditor({ ingredients = [], units = [], modifiers = [] }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(AdminMenuProvider, { ingredients, units, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(IngredientList, {}),
    /* @__PURE__ */ jsxRuntimeExports.jsx(ModifierList, { modifiers }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(RecipeModal, {})
  ] });
}
MenuEditor.meta = {
  id: "menu",
  title: "Menu",
  dataDomain: "menu",
  scopes: ["menu:read", "menu:write"],
  latencyClass: "interactive"
};
function MenuRoute() {
  const [data, setData] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [loading, setLoading] = reactExports.useState(true);
  reactExports.useEffect(() => {
    let active = true;
    (async () => {
      try {
        const res = await fetch("/api/admin");
        if (!res.ok) throw new Error("Failed to load admin data");
        const json = await res.json();
        if (active) setData(json);
      } catch (e2) {
        if (active) setError(e2.message || "Error");
      } finally {
        if (active) setLoading(false);
      }
    })();
    return () => {
      active = false;
    };
  }, []);
  if (loading) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  if (error) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error });
  const ingredients = data?.ingredients || [];
  const units = data?.units || [];
  const modifiers = data?.modifiers || [];
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Menu" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(MenuEditor, { ingredients, units, modifiers })
  ] });
}
var __defProp$1 = Object.defineProperty;
var __defProps$1 = Object.defineProperties;
var __getOwnPropDescs$1 = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols$1 = Object.getOwnPropertySymbols;
var __hasOwnProp$1 = Object.prototype.hasOwnProperty;
var __propIsEnum$1 = Object.prototype.propertyIsEnumerable;
var __defNormalProp$1 = (obj, key, value) => key in obj ? __defProp$1(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues$1 = (a3, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp$1.call(b2, prop))
      __defNormalProp$1(a3, prop, b2[prop]);
  if (__getOwnPropSymbols$1)
    for (var prop of __getOwnPropSymbols$1(b2)) {
      if (__propIsEnum$1.call(b2, prop))
        __defNormalProp$1(a3, prop, b2[prop]);
    }
  return a3;
};
var __spreadProps$1 = (a3, b2) => __defProps$1(a3, __getOwnPropDescs$1(b2));
var __async$1 = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var step = (x2) => x2.done ? resolve(x2.value) : Promise.resolve(x2.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var MapHookData = class {
  constructor() {
    this.data = /* @__PURE__ */ new Map();
  }
  set(key, value) {
    this.data.set(key, value);
  }
  get(key) {
    return this.data.get(key);
  }
  has(key) {
    return this.data.has(key);
  }
  delete(key) {
    return this.data.delete(key);
  }
  clear() {
    this.data.clear();
  }
};
var StandardResolutionReasons = {
  /**
   * The resolved value was the result of an error.
   *
   * Note: The `errorCode` and `errorMessage` fields may contain additional details of this error.
   */
  ERROR: "ERROR"
};
var ErrorCode = /* @__PURE__ */ ((ErrorCode2) => {
  ErrorCode2["PROVIDER_NOT_READY"] = "PROVIDER_NOT_READY";
  ErrorCode2["PROVIDER_FATAL"] = "PROVIDER_FATAL";
  ErrorCode2["FLAG_NOT_FOUND"] = "FLAG_NOT_FOUND";
  ErrorCode2["PARSE_ERROR"] = "PARSE_ERROR";
  ErrorCode2["TYPE_MISMATCH"] = "TYPE_MISMATCH";
  ErrorCode2["TARGETING_KEY_MISSING"] = "TARGETING_KEY_MISSING";
  ErrorCode2["INVALID_CONTEXT"] = "INVALID_CONTEXT";
  ErrorCode2["GENERAL"] = "GENERAL";
  return ErrorCode2;
})(ErrorCode || {});
var OpenFeatureError = class _OpenFeatureError extends Error {
  constructor(message, options) {
    super(message);
    Object.setPrototypeOf(this, _OpenFeatureError.prototype);
    this.name = "OpenFeatureError";
    this.cause = options == null ? void 0 : options.cause;
  }
};
var FlagNotFoundError = class _FlagNotFoundError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _FlagNotFoundError.prototype);
    this.name = "FlagNotFoundError";
    this.code = "FLAG_NOT_FOUND";
  }
};
var GeneralError = class _GeneralError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _GeneralError.prototype);
    this.name = "GeneralError";
    this.code = "GENERAL";
  }
};
var InvalidContextError = class _InvalidContextError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _InvalidContextError.prototype);
    this.name = "InvalidContextError";
    this.code = "INVALID_CONTEXT";
  }
};
var ParseError = class _ParseError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _ParseError.prototype);
    this.name = "ParseError";
    this.code = "PARSE_ERROR";
  }
};
var ProviderFatalError = class _ProviderFatalError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _ProviderFatalError.prototype);
    this.name = "ProviderFatalError";
    this.code = "PROVIDER_FATAL";
  }
};
var ProviderNotReadyError = class _ProviderNotReadyError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _ProviderNotReadyError.prototype);
    this.name = "ProviderNotReadyError";
    this.code = "PROVIDER_NOT_READY";
  }
};
var TargetingKeyMissingError = class _TargetingKeyMissingError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _TargetingKeyMissingError.prototype);
    this.name = "TargetingKeyMissingError";
    this.code = "TARGETING_KEY_MISSING";
  }
};
var TypeMismatchError = class _TypeMismatchError extends OpenFeatureError {
  constructor(message, options) {
    super(message, options);
    Object.setPrototypeOf(this, _TypeMismatchError.prototype);
    this.name = "TypeMismatchError";
    this.code = "TYPE_MISMATCH";
  }
};
var instantiateErrorByErrorCode = (errorCode, message) => {
  switch (errorCode) {
    case "FLAG_NOT_FOUND":
      return new FlagNotFoundError(message);
    case "PARSE_ERROR":
      return new ParseError(message);
    case "TYPE_MISMATCH":
      return new TypeMismatchError(message);
    case "TARGETING_KEY_MISSING":
      return new TargetingKeyMissingError(message);
    case "INVALID_CONTEXT":
      return new InvalidContextError(message);
    case "PROVIDER_NOT_READY":
      return new ProviderNotReadyError(message);
    case "PROVIDER_FATAL":
      return new ProviderFatalError(message);
    default:
      return new GeneralError(message);
  }
};
var ClientProviderStatus = /* @__PURE__ */ ((ClientProviderStatus2) => {
  ClientProviderStatus2["NOT_READY"] = "NOT_READY";
  ClientProviderStatus2["READY"] = "READY";
  ClientProviderStatus2["ERROR"] = "ERROR";
  ClientProviderStatus2["STALE"] = "STALE";
  ClientProviderStatus2["FATAL"] = "FATAL";
  ClientProviderStatus2["RECONCILING"] = "RECONCILING";
  return ClientProviderStatus2;
})(ClientProviderStatus || {});
var ClientProviderEvents = /* @__PURE__ */ ((ClientProviderEvents2) => {
  ClientProviderEvents2["Ready"] = "PROVIDER_READY";
  ClientProviderEvents2["Error"] = "PROVIDER_ERROR";
  ClientProviderEvents2["ConfigurationChanged"] = "PROVIDER_CONFIGURATION_CHANGED";
  ClientProviderEvents2["ContextChanged"] = "PROVIDER_CONTEXT_CHANGED";
  ClientProviderEvents2["Reconciling"] = "PROVIDER_RECONCILING";
  ClientProviderEvents2["Stale"] = "PROVIDER_STALE";
  return ClientProviderEvents2;
})(ClientProviderEvents || {});
var eventStatusMap = {
  [
    "READY"
    /* READY */
  ]: "PROVIDER_READY",
  [
    "ERROR"
    /* ERROR */
  ]: "PROVIDER_ERROR",
  [
    "FATAL"
    /* FATAL */
  ]: "PROVIDER_ERROR",
  [
    "STALE"
    /* STALE */
  ]: "PROVIDER_STALE",
  [
    "RECONCILING"
    /* RECONCILING */
  ]: "PROVIDER_RECONCILING",
  [
    "NOT_READY"
    /* NOT_READY */
  ]: void 0
};
var statusMatchesEvent = (event, status) => {
  return !status && event === "PROVIDER_READY" || eventStatusMap[status] === event;
};
var DefaultLogger = class {
  error(...args) {
    console.error(...args);
  }
  warn(...args) {
    console.warn(...args);
  }
  info() {
  }
  debug() {
  }
};
var LOG_LEVELS = ["error", "warn", "info", "debug"];
var SafeLogger = class {
  constructor(logger) {
    this.fallbackLogger = new DefaultLogger();
    try {
      for (const level of LOG_LEVELS) {
        if (!logger[level] || typeof logger[level] !== "function") {
          throw new Error(`The provided logger is missing the ${level} method.`);
        }
      }
      this.logger = logger;
    } catch (err) {
      console.error(err);
      console.error("Falling back to the default logger.");
      this.logger = this.fallbackLogger;
    }
  }
  error(...args) {
    this.log("error", ...args);
  }
  warn(...args) {
    this.log("warn", ...args);
  }
  info(...args) {
    this.log("info", ...args);
  }
  debug(...args) {
    this.log("debug", ...args);
  }
  log(level, ...args) {
    try {
      this.logger[level](...args);
    } catch (error) {
      this.fallbackLogger[level](...args);
    }
  }
};
var GenericEventEmitter = class {
  constructor(globalLogger) {
    this.globalLogger = globalLogger;
    this._handlers = {
      [
        "PROVIDER_CONFIGURATION_CHANGED"
        /* ConfigurationChanged */
      ]: /* @__PURE__ */ new WeakMap(),
      [
        "PROVIDER_CONTEXT_CHANGED"
        /* ContextChanged */
      ]: /* @__PURE__ */ new WeakMap(),
      [
        "PROVIDER_READY"
        /* Ready */
      ]: /* @__PURE__ */ new WeakMap(),
      [
        "PROVIDER_ERROR"
        /* Error */
      ]: /* @__PURE__ */ new WeakMap(),
      [
        "PROVIDER_STALE"
        /* Stale */
      ]: /* @__PURE__ */ new WeakMap(),
      [
        "PROVIDER_RECONCILING"
        /* Reconciling */
      ]: /* @__PURE__ */ new WeakMap()
    };
  }
  // here we use E, to restrict the events a provider can manually emit (PROVIDER_CONTEXT_CHANGED is emitted by the SDK)
  emit(eventType, context2) {
    this.eventEmitter.emit(eventType, context2);
  }
  addHandler(eventType, handler) {
    const asyncHandler = (details) => __async$1(this, null, function* () {
      var _a;
      try {
        yield handler(details);
      } catch (err) {
        (_a = this._logger) == null ? void 0 : _a.error("Error running event handler:", err);
      }
    });
    const existingAsyncHandlers = this._handlers[eventType].get(handler);
    this._handlers[eventType].set(handler, [...existingAsyncHandlers || [], asyncHandler]);
    this.eventEmitter.on(eventType, asyncHandler);
  }
  removeHandler(eventType, handler) {
    const existingAsyncHandlers = this._handlers[eventType].get(handler);
    if (existingAsyncHandlers) {
      const removedAsyncHandler = existingAsyncHandlers.pop();
      if (removedAsyncHandler) {
        this.eventEmitter.removeListener(eventType, removedAsyncHandler);
      }
    }
  }
  removeAllHandlers(eventType) {
    if (eventType) {
      this.eventEmitter.removeAllListeners(eventType);
    } else {
      this.eventEmitter.removeAllListeners();
    }
  }
  getHandlers(eventType) {
    return this.eventEmitter.listeners(eventType);
  }
  setLogger(logger) {
    this._eventLogger = new SafeLogger(logger);
    return this;
  }
  get _logger() {
    var _a, _b;
    return (_b = this._eventLogger) != null ? _b : (_a = this.globalLogger) == null ? void 0 : _a.call(this);
  }
};
function isString(value) {
  return typeof value === "string";
}
function stringOrUndefined(value) {
  return isString(value) ? value : void 0;
}
function isObject(value) {
  return typeof value === "object";
}
function objectOrUndefined(value) {
  return isObject(value) ? value : void 0;
}
function isDefined(input) {
  return typeof input !== "undefined" && input !== null;
}
var ProviderWrapper = class {
  constructor(_provider, _status, _statusEnumType) {
    this._provider = _provider;
    this._status = _status;
    this._pendingContextChanges = 0;
    var _a, _b, _c;
    (_a = _provider.events) == null ? void 0 : _a.addHandler("PROVIDER_READY", () => {
      this._status = _statusEnumType.READY;
    });
    (_b = _provider.events) == null ? void 0 : _b.addHandler("PROVIDER_STALE", () => {
      this._status = _statusEnumType.STALE;
    });
    (_c = _provider.events) == null ? void 0 : _c.addHandler("PROVIDER_ERROR", (details) => {
      if ((details == null ? void 0 : details.errorCode) === "PROVIDER_FATAL") {
        this._status = _statusEnumType.FATAL;
      } else {
        this._status = _statusEnumType.ERROR;
      }
    });
  }
  get provider() {
    return this._provider;
  }
  set provider(provider2) {
    this._provider = provider2;
  }
  get status() {
    return this._status;
  }
  set status(status) {
    this._status = status;
  }
  get allContextChangesSettled() {
    return this._pendingContextChanges === 0;
  }
  incrementPendingContextChanges() {
    this._pendingContextChanges++;
  }
  decrementPendingContextChanges() {
    this._pendingContextChanges--;
  }
};
var OpenFeatureCommonAPI = class {
  constructor(category) {
    this._hooks = [];
    this._context = {};
    this._logger = new DefaultLogger();
    this._clientEventHandlers = /* @__PURE__ */ new Map();
    this._domainScopedContext = /* @__PURE__ */ new Map();
    this._clientEvents = /* @__PURE__ */ new Map();
    this._runsOn = category;
  }
  addHooks(...hooks) {
    this._hooks = [...this._hooks, ...hooks];
    return this;
  }
  getHooks() {
    return this._hooks;
  }
  clearHooks() {
    this._hooks = [];
    return this;
  }
  setLogger(logger) {
    this._logger = new SafeLogger(logger);
    return this;
  }
  /**
   * Get metadata about the default provider.
   * @returns {ProviderMetadata} Provider Metadata
   */
  get providerMetadata() {
    return this.getProviderMetadata();
  }
  /**
   * Get metadata about a registered provider using the client name.
   * An unbound or empty client name will return metadata from the default provider.
   * @param {string} domain An identifier which logically binds clients with providers
   * @returns {ProviderMetadata} Provider Metadata
   */
  getProviderMetadata(domain) {
    return this.getProviderForClient(domain).metadata;
  }
  /**
   * Adds a handler for the given provider event type.
   * The handlers are called in the order they have been added.
   * API (global) events run for all providers.
   * @param {AnyProviderEvent} eventType The provider event type to listen to
   * @param {EventHandler} handler The handler to run on occurrence of the event type
   * @param {EventOptions} options Optional options such as signal for aborting
   */
  addHandler(eventType, handler, options) {
    [.../* @__PURE__ */ new Map([[void 0, this._defaultProvider]]), ...this._domainScopedProviders].forEach((keyProviderTuple) => {
      var _a;
      const domain = keyProviderTuple[0];
      const provider2 = keyProviderTuple[1].provider;
      const status = keyProviderTuple[1].status;
      const shouldRunNow = statusMatchesEvent(eventType, status);
      if (shouldRunNow) {
        try {
          handler({ domain, providerName: provider2.metadata.name });
        } catch (err) {
          (_a = this._logger) == null ? void 0 : _a.error("Error running event handler:", err);
        }
      }
    });
    this._apiEmitter.addHandler(eventType, handler);
    if ((options == null ? void 0 : options.signal) && typeof options.signal.addEventListener === "function") {
      options.signal.addEventListener("abort", () => {
        this.removeHandler(eventType, handler);
      });
    }
  }
  /**
   * Removes a handler for the given provider event type.
   * @param {AnyProviderEvent} eventType The provider event type to remove the listener for
   * @param {EventHandler} handler The handler to remove for the provider event type
   */
  removeHandler(eventType, handler) {
    this._apiEmitter.removeHandler(eventType, handler);
  }
  /**
   * Removes all event handlers.
   */
  clearHandlers() {
    this._apiEmitter.removeAllHandlers();
  }
  /**
   * Gets the current handlers for the given provider event type.
   * @param {AnyProviderEvent} eventType The provider event type to get the current handlers for
   * @returns {EventHandler[]} The handlers currently attached to the given provider event type
   */
  getHandlers(eventType) {
    return this._apiEmitter.getHandlers(eventType);
  }
  setAwaitableProvider(domainOrProvider, providerOrUndefined) {
    var _a, _b, _c, _d, _e, _f, _g, _h;
    const domain = stringOrUndefined(domainOrProvider);
    const provider2 = (_a = objectOrUndefined(domainOrProvider)) != null ? _a : objectOrUndefined(providerOrUndefined);
    if (!provider2) {
      this._logger.debug("No provider defined, ignoring setProvider call");
      return;
    }
    const oldProvider = this.getProviderForClient(domain);
    const providerName = provider2.metadata.name;
    if (oldProvider === provider2) {
      this._logger.debug("Provider is already set, ignoring setProvider call");
      return;
    }
    if (!provider2.runsOn) {
      this._logger.debug(`Provider '${provider2.metadata.name}' has not defined its intended use.`);
    } else if (provider2.runsOn !== this._runsOn) {
      throw new GeneralError(`Provider '${provider2.metadata.name}' is intended for use on the ${provider2.runsOn}.`);
    }
    const emitters = this.getAssociatedEventEmitters(domain);
    let initializationPromise = void 0;
    const wrappedProvider = new ProviderWrapper(
      provider2,
      this._statusEnumType.NOT_READY,
      this._statusEnumType
    );
    if (typeof provider2.initialize === "function" && !this.allProviders.includes(provider2)) {
      initializationPromise = (_e = (_d = (_c = provider2.initialize) == null ? void 0 : _c.call(provider2, domain ? (_b = this._domainScopedContext.get(domain)) != null ? _b : this._context : this._context)) == null ? void 0 : _d.then(() => {
        var _a2;
        wrappedProvider.status = this._statusEnumType.READY;
        this.getAssociatedEventEmitters(domain).forEach((emitter) => {
          emitter == null ? void 0 : emitter.emit("PROVIDER_READY", { clientName: domain, domain, providerName });
        });
        (_a2 = this._apiEmitter) == null ? void 0 : _a2.emit("PROVIDER_READY", { clientName: domain, domain, providerName });
      })) == null ? void 0 : _e.catch((error) => {
        var _a2;
        if ((error == null ? void 0 : error.code) === "PROVIDER_FATAL") {
          wrappedProvider.status = this._statusEnumType.FATAL;
        } else {
          wrappedProvider.status = this._statusEnumType.ERROR;
        }
        this.getAssociatedEventEmitters(domain).forEach((emitter) => {
          emitter == null ? void 0 : emitter.emit("PROVIDER_ERROR", {
            clientName: domain,
            domain,
            providerName,
            message: error == null ? void 0 : error.message
          });
        });
        (_a2 = this._apiEmitter) == null ? void 0 : _a2.emit("PROVIDER_ERROR", {
          clientName: domain,
          domain,
          providerName,
          message: error == null ? void 0 : error.message
        });
        throw error;
      });
    } else {
      wrappedProvider.status = this._statusEnumType.READY;
      emitters.forEach((emitter) => {
        emitter == null ? void 0 : emitter.emit("PROVIDER_READY", { clientName: domain, domain, providerName });
      });
      (_f = this._apiEmitter) == null ? void 0 : _f.emit("PROVIDER_READY", { clientName: domain, domain, providerName });
    }
    if (domain) {
      this._domainScopedProviders.set(domain, wrappedProvider);
    } else {
      this._defaultProvider = wrappedProvider;
    }
    this.transferListeners(oldProvider, provider2, domain, emitters);
    if (!this.allProviders.includes(oldProvider)) {
      (_h = (_g = oldProvider == null ? void 0 : oldProvider.onClose) == null ? void 0 : _g.call(oldProvider)) == null ? void 0 : _h.catch((err) => {
        this._logger.error(`error closing provider: ${err == null ? void 0 : err.message}, ${err == null ? void 0 : err.stack}`);
      });
    }
    return initializationPromise;
  }
  getProviderForClient(domain) {
    var _a, _b;
    if (!domain) {
      return this._defaultProvider.provider;
    }
    return (_b = (_a = this._domainScopedProviders.get(domain)) == null ? void 0 : _a.provider) != null ? _b : this._defaultProvider.provider;
  }
  buildAndCacheEventEmitterForClient(domain) {
    const emitter = this._clientEvents.get(domain);
    if (emitter) {
      return emitter;
    }
    const newEmitter = this._createEventEmitter();
    this._clientEvents.set(domain, newEmitter);
    const clientProvider = this.getProviderForClient(domain);
    Object.values(ClientProviderEvents).forEach(
      (eventType) => {
        var _a;
        return (_a = clientProvider.events) == null ? void 0 : _a.addHandler(eventType, (details) => __async$1(this, null, function* () {
          newEmitter.emit(eventType, __spreadProps$1(__spreadValues$1({}, details), {
            clientName: domain,
            domain,
            providerName: clientProvider.metadata.name
          }));
        }));
      }
    );
    return newEmitter;
  }
  getUnboundEmitters() {
    const domainScopedProviders = [...this._domainScopedProviders.keys()];
    const eventEmitterNames = [...this._clientEvents.keys()].filter(isDefined);
    const unboundEmitterNames = eventEmitterNames.filter((name2) => !domainScopedProviders.includes(name2));
    return [
      // all unbound, named emitters
      ...unboundEmitterNames.map((name2) => this._clientEvents.get(name2)),
      // the default emitter
      this._clientEvents.get(void 0)
    ].filter(isDefined);
  }
  getAssociatedEventEmitters(domain) {
    return domain ? [this.buildAndCacheEventEmitterForClient(domain)] : this.getUnboundEmitters();
  }
  transferListeners(oldProvider, newProvider, domain, emitters) {
    var _a;
    (_a = this._clientEventHandlers.get(domain)) == null ? void 0 : _a.forEach((eventHandler) => {
      var _a2;
      return (_a2 = oldProvider.events) == null ? void 0 : _a2.removeHandler(...eventHandler);
    });
    const newClientHandlers = Object.values(ClientProviderEvents).map((eventType) => {
      const handler = (details) => __async$1(this, null, function* () {
        emitters.forEach((emitter) => {
          emitter == null ? void 0 : emitter.emit(eventType, __spreadProps$1(__spreadValues$1({}, details), { clientName: domain, domain, providerName: newProvider.metadata.name }));
        });
        this._apiEmitter.emit(eventType, __spreadProps$1(__spreadValues$1({}, details), {
          clientName: domain,
          domain,
          providerName: newProvider.metadata.name
        }));
      });
      return [eventType, handler];
    });
    this._clientEventHandlers.set(domain, newClientHandlers);
    newClientHandlers.forEach((eventHandler) => {
      var _a2;
      return (_a2 = newProvider.events) == null ? void 0 : _a2.addHandler(...eventHandler);
    });
  }
  close() {
    return __async$1(this, null, function* () {
      var _a, _b;
      try {
        yield (_b = (_a = this == null ? void 0 : this._defaultProvider.provider) == null ? void 0 : _a.onClose) == null ? void 0 : _b.call(_a);
      } catch (err) {
        this.handleShutdownError(this._defaultProvider.provider, err);
      }
      const wrappers = Array.from(this._domainScopedProviders);
      yield Promise.all(
        wrappers.map((_0) => __async$1(this, [_0], function* ([, wrapper]) {
          var _a2, _b2;
          try {
            yield (_b2 = wrapper == null ? void 0 : (_a2 = wrapper.provider).onClose) == null ? void 0 : _b2.call(_a2);
          } catch (err) {
            this.handleShutdownError(wrapper == null ? void 0 : wrapper.provider, err);
          }
        }))
      );
    });
  }
  clearProvidersAndSetDefault(defaultProvider) {
    return __async$1(this, null, function* () {
      try {
        yield this.close();
      } catch (err) {
        this._logger.error("Unable to cleanly close providers. Resetting to the default configuration.");
      } finally {
        this._domainScopedProviders.clear();
        this._defaultProvider = new ProviderWrapper(
          defaultProvider,
          this._statusEnumType.NOT_READY,
          this._statusEnumType
        );
      }
    });
  }
  get allProviders() {
    return [
      ...[...this._domainScopedProviders.values()].map((wrappers) => wrappers.provider),
      this._defaultProvider.provider
    ];
  }
  handleShutdownError(provider2, err) {
    this._logger.error(`Error during shutdown of provider ${provider2.metadata.name}: ${err}`);
    this._logger.error(err == null ? void 0 : err.stack);
  }
};
var __create = Object.create;
var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __reflectGet = Reflect.get;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a3, b2) => {
  for (var prop in b2 || (b2 = {}))
    if (__hasOwnProp.call(b2, prop))
      __defNormalProp(a3, prop, b2[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b2)) {
      if (__propIsEnum.call(b2, prop))
        __defNormalProp(a3, prop, b2[prop]);
    }
  return a3;
};
var __spreadProps = (a3, b2) => __defProps(a3, __getOwnPropDescs(b2));
var __commonJS = (cb, mod) => function __require() {
  return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
  // If the importer is in node compatibility mode or this is not an ESM
  // file that has been converted to a CommonJS file using a Babel-
  // compatible transform (i.e. "__esModule" has not been set), then set
  // "default" to the CommonJS "module.exports" for node compatibility.
  __defProp(target, "default", { value: mod, enumerable: true }),
  mod
));
var __superGet = (cls, obj, key) => __reflectGet(__getProtoOf(cls), key, obj);
var __async = (__this, __arguments, generator) => {
  return new Promise((resolve, reject) => {
    var fulfilled = (value) => {
      try {
        step(generator.next(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var rejected = (value) => {
      try {
        step(generator.throw(value));
      } catch (e2) {
        reject(e2);
      }
    };
    var step = (x2) => x2.done ? resolve(x2.value) : Promise.resolve(x2.value).then(fulfilled, rejected);
    step((generator = generator.apply(__this, __arguments)).next());
  });
};
var require_eventemitter3 = __commonJS({
  "../../node_modules/eventemitter3/index.js"(exports, module) {
    var has = Object.prototype.hasOwnProperty;
    var prefix = "~";
    function Events() {
    }
    if (Object.create) {
      Events.prototype = /* @__PURE__ */ Object.create(null);
      if (!new Events().__proto__) prefix = false;
    }
    function EE(fn3, context2, once) {
      this.fn = fn3;
      this.context = context2;
      this.once = once || false;
    }
    function addListener(emitter, event, fn3, context2, once) {
      if (typeof fn3 !== "function") {
        throw new TypeError("The listener must be a function");
      }
      var listener = new EE(fn3, context2 || emitter, once), evt = prefix ? prefix + event : event;
      if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
      else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
      else emitter._events[evt] = [emitter._events[evt], listener];
      return emitter;
    }
    function clearEvent(emitter, evt) {
      if (--emitter._eventsCount === 0) emitter._events = new Events();
      else delete emitter._events[evt];
    }
    function EventEmitter2() {
      this._events = new Events();
      this._eventsCount = 0;
    }
    EventEmitter2.prototype.eventNames = function eventNames() {
      var names = [], events2, name2;
      if (this._eventsCount === 0) return names;
      for (name2 in events2 = this._events) {
        if (has.call(events2, name2)) names.push(prefix ? name2.slice(1) : name2);
      }
      if (Object.getOwnPropertySymbols) {
        return names.concat(Object.getOwnPropertySymbols(events2));
      }
      return names;
    };
    EventEmitter2.prototype.listeners = function listeners(event) {
      var evt = prefix ? prefix + event : event, handlers = this._events[evt];
      if (!handlers) return [];
      if (handlers.fn) return [handlers.fn];
      for (var i2 = 0, l = handlers.length, ee = new Array(l); i2 < l; i2++) {
        ee[i2] = handlers[i2].fn;
      }
      return ee;
    };
    EventEmitter2.prototype.listenerCount = function listenerCount(event) {
      var evt = prefix ? prefix + event : event, listeners = this._events[evt];
      if (!listeners) return 0;
      if (listeners.fn) return 1;
      return listeners.length;
    };
    EventEmitter2.prototype.emit = function emit(event, a1, a22, a3, a4, a5) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return false;
      var listeners = this._events[evt], len = arguments.length, args, i2;
      if (listeners.fn) {
        if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
        switch (len) {
          case 1:
            return listeners.fn.call(listeners.context), true;
          case 2:
            return listeners.fn.call(listeners.context, a1), true;
          case 3:
            return listeners.fn.call(listeners.context, a1, a22), true;
          case 4:
            return listeners.fn.call(listeners.context, a1, a22, a3), true;
          case 5:
            return listeners.fn.call(listeners.context, a1, a22, a3, a4), true;
          case 6:
            return listeners.fn.call(listeners.context, a1, a22, a3, a4, a5), true;
        }
        for (i2 = 1, args = new Array(len - 1); i2 < len; i2++) {
          args[i2 - 1] = arguments[i2];
        }
        listeners.fn.apply(listeners.context, args);
      } else {
        var length = listeners.length, j;
        for (i2 = 0; i2 < length; i2++) {
          if (listeners[i2].once) this.removeListener(event, listeners[i2].fn, void 0, true);
          switch (len) {
            case 1:
              listeners[i2].fn.call(listeners[i2].context);
              break;
            case 2:
              listeners[i2].fn.call(listeners[i2].context, a1);
              break;
            case 3:
              listeners[i2].fn.call(listeners[i2].context, a1, a22);
              break;
            case 4:
              listeners[i2].fn.call(listeners[i2].context, a1, a22, a3);
              break;
            default:
              if (!args) for (j = 1, args = new Array(len - 1); j < len; j++) {
                args[j - 1] = arguments[j];
              }
              listeners[i2].fn.apply(listeners[i2].context, args);
          }
        }
      }
      return true;
    };
    EventEmitter2.prototype.on = function on2(event, fn3, context2) {
      return addListener(this, event, fn3, context2, false);
    };
    EventEmitter2.prototype.once = function once(event, fn3, context2) {
      return addListener(this, event, fn3, context2, true);
    };
    EventEmitter2.prototype.removeListener = function removeListener(event, fn3, context2, once) {
      var evt = prefix ? prefix + event : event;
      if (!this._events[evt]) return this;
      if (!fn3) {
        clearEvent(this, evt);
        return this;
      }
      var listeners = this._events[evt];
      if (listeners.fn) {
        if (listeners.fn === fn3 && (!once || listeners.once) && (!context2 || listeners.context === context2)) {
          clearEvent(this, evt);
        }
      } else {
        for (var i2 = 0, events2 = [], length = listeners.length; i2 < length; i2++) {
          if (listeners[i2].fn !== fn3 || once && !listeners[i2].once || context2 && listeners[i2].context !== context2) {
            events2.push(listeners[i2]);
          }
        }
        if (events2.length) this._events[evt] = events2.length === 1 ? events2[0] : events2;
        else clearEvent(this, evt);
      }
      return this;
    };
    EventEmitter2.prototype.removeAllListeners = function removeAllListeners(event) {
      var evt;
      if (event) {
        evt = prefix ? prefix + event : event;
        if (this._events[evt]) clearEvent(this, evt);
      } else {
        this._events = new Events();
        this._eventsCount = 0;
      }
      return this;
    };
    EventEmitter2.prototype.off = EventEmitter2.prototype.removeListener;
    EventEmitter2.prototype.addListener = EventEmitter2.prototype.on;
    EventEmitter2.prefixed = prefix;
    EventEmitter2.EventEmitter = EventEmitter2;
    if ("undefined" !== typeof module) {
      module.exports = EventEmitter2;
    }
  }
});
var REASON_NO_OP = "No-op";
var NoopFeatureProvider = class {
  constructor() {
    this.metadata = {
      name: "No-op Provider"
    };
  }
  resolveBooleanEvaluation(_, defaultValue2) {
    return this.noOp(defaultValue2);
  }
  resolveStringEvaluation(_, defaultValue2) {
    return this.noOp(defaultValue2);
  }
  resolveNumberEvaluation(_, defaultValue2) {
    return this.noOp(defaultValue2);
  }
  resolveObjectEvaluation(_, defaultValue2) {
    return this.noOp(defaultValue2);
  }
  noOp(defaultValue2) {
    return {
      value: defaultValue2,
      reason: REASON_NO_OP
    };
  }
};
var NOOP_PROVIDER = new NoopFeatureProvider();
var import_index = __toESM(require_eventemitter3());
var OpenFeatureEventEmitter = class extends GenericEventEmitter {
  constructor() {
    super();
    this.eventEmitter = new import_index.default();
  }
};
var OpenFeatureClient = class {
  constructor(providerAccessor, providerStatusAccessor, emitterAccessor, apiContextAccessor, apiHooksAccessor, globalLogger, options) {
    this.providerAccessor = providerAccessor;
    this.providerStatusAccessor = providerStatusAccessor;
    this.emitterAccessor = emitterAccessor;
    this.apiContextAccessor = apiContextAccessor;
    this.apiHooksAccessor = apiHooksAccessor;
    this.globalLogger = globalLogger;
    this.options = options;
    this._hooks = [];
  }
  get metadata() {
    var _a, _b;
    return {
      // Use domain if name is not provided
      name: (_a = this.options.domain) != null ? _a : this.options.name,
      domain: (_b = this.options.domain) != null ? _b : this.options.name,
      version: this.options.version,
      providerMetadata: this.providerAccessor().metadata
    };
  }
  get providerStatus() {
    return this.providerStatusAccessor();
  }
  addHandler(eventType, handler, options) {
    var _a;
    this.emitterAccessor().addHandler(eventType, handler);
    const shouldRunNow = statusMatchesEvent(eventType, this.providerStatus);
    if (shouldRunNow) {
      try {
        handler({
          clientName: this.metadata.name,
          domain: this.metadata.domain,
          providerName: this._provider.metadata.name
        });
      } catch (err) {
        (_a = this._logger) == null ? void 0 : _a.error("Error running event handler:", err);
      }
    }
    if ((options == null ? void 0 : options.signal) && typeof options.signal.addEventListener === "function") {
      options.signal.addEventListener("abort", () => {
        this.removeHandler(eventType, handler);
      });
    }
  }
  removeHandler(notificationType, handler) {
    this.emitterAccessor().removeHandler(notificationType, handler);
  }
  getHandlers(eventType) {
    return this.emitterAccessor().getHandlers(eventType);
  }
  setLogger(logger) {
    this._clientLogger = new SafeLogger(logger);
    return this;
  }
  addHooks(...hooks) {
    this._hooks = [...this._hooks, ...hooks];
    return this;
  }
  getHooks() {
    return this._hooks;
  }
  clearHooks() {
    this._hooks = [];
    return this;
  }
  getBooleanValue(flagKey, defaultValue2, options) {
    return this.getBooleanDetails(flagKey, defaultValue2, options).value;
  }
  getBooleanDetails(flagKey, defaultValue2, options) {
    return this.evaluate(flagKey, this._provider.resolveBooleanEvaluation, defaultValue2, "boolean", options);
  }
  getStringValue(flagKey, defaultValue2, options) {
    return this.getStringDetails(flagKey, defaultValue2, options).value;
  }
  getStringDetails(flagKey, defaultValue2, options) {
    return this.evaluate(
      flagKey,
      // this isolates providers from our restricted string generic argument.
      this._provider.resolveStringEvaluation,
      defaultValue2,
      "string",
      options
    );
  }
  getNumberValue(flagKey, defaultValue2, options) {
    return this.getNumberDetails(flagKey, defaultValue2, options).value;
  }
  getNumberDetails(flagKey, defaultValue2, options) {
    return this.evaluate(
      flagKey,
      // this isolates providers from our restricted number generic argument.
      this._provider.resolveNumberEvaluation,
      defaultValue2,
      "number",
      options
    );
  }
  getObjectValue(flagKey, defaultValue2, options) {
    return this.getObjectDetails(flagKey, defaultValue2, options).value;
  }
  getObjectDetails(flagKey, defaultValue2, options) {
    return this.evaluate(flagKey, this._provider.resolveObjectEvaluation, defaultValue2, "object", options);
  }
  track(occurrenceKey, occurrenceDetails = {}) {
    var _a, _b, _c;
    try {
      this.shortCircuitIfNotReady();
      if (typeof this._provider.track === "function") {
        const frozenContext = Object.freeze(__spreadValues({}, this.apiContextAccessor((_a = this == null ? void 0 : this.options) == null ? void 0 : _a.domain)));
        return (_c = (_b = this._provider).track) == null ? void 0 : _c.call(_b, occurrenceKey, frozenContext, occurrenceDetails);
      } else {
        this._logger.debug("Provider does not support the track function; will no-op.");
      }
    } catch (err) {
      this._logger.debug("Error recording tracking event.", err);
    }
  }
  evaluate(flagKey, resolver, defaultValue2, flagType, options = {}) {
    var _a, _b;
    const allHooks = [
      ...this.apiHooksAccessor(),
      ...this.getHooks(),
      ...options.hooks || [],
      ...this._provider.hooks || []
    ];
    const allHooksReversed = [...allHooks].reverse();
    const context2 = __spreadValues({}, this.apiContextAccessor((_a = this == null ? void 0 : this.options) == null ? void 0 : _a.domain));
    const hookContexts = allHooksReversed.map(
      () => Object.freeze({
        flagKey,
        defaultValue: defaultValue2,
        flagValueType: flagType,
        clientMetadata: this.metadata,
        providerMetadata: this._provider.metadata,
        context: context2,
        logger: this._logger,
        hookData: new MapHookData()
      })
    );
    let evaluationDetails;
    try {
      this.beforeHooks(allHooks, hookContexts, options);
      this.shortCircuitIfNotReady();
      const resolution = resolver.call(this._provider, flagKey, defaultValue2, context2, this._logger);
      const resolutionDetails = __spreadProps(__spreadValues({}, resolution), {
        flagMetadata: Object.freeze((_b = resolution.flagMetadata) != null ? _b : {}),
        flagKey
      });
      if (resolutionDetails.errorCode) {
        const err = instantiateErrorByErrorCode(resolutionDetails.errorCode, resolutionDetails.errorMessage);
        this.errorHooks(allHooksReversed, hookContexts, err, options);
        evaluationDetails = this.getErrorEvaluationDetails(flagKey, defaultValue2, err, resolutionDetails.flagMetadata);
      } else {
        this.afterHooks(allHooksReversed, hookContexts, resolutionDetails, options);
        evaluationDetails = resolutionDetails;
      }
    } catch (err) {
      this.errorHooks(allHooksReversed, hookContexts, err, options);
      evaluationDetails = this.getErrorEvaluationDetails(flagKey, defaultValue2, err);
    }
    this.finallyHooks(allHooksReversed, hookContexts, evaluationDetails, options);
    return evaluationDetails;
  }
  beforeHooks(hooks, hookContexts, options) {
    var _a;
    for (const [index2, hook] of hooks.entries()) {
      const hookContextIndex = hooks.length - 1 - index2;
      const hookContext = hookContexts[hookContextIndex];
      Object.freeze(hookContext);
      Object.freeze(hookContext.context);
      (_a = hook == null ? void 0 : hook.before) == null ? void 0 : _a.call(hook, hookContext, Object.freeze(options.hookHints));
    }
  }
  afterHooks(hooks, hookContexts, evaluationDetails, options) {
    var _a;
    for (const [index2, hook] of hooks.entries()) {
      const hookContext = hookContexts[index2];
      (_a = hook == null ? void 0 : hook.after) == null ? void 0 : _a.call(hook, hookContext, evaluationDetails, options.hookHints);
    }
  }
  errorHooks(hooks, hookContexts, err, options) {
    var _a;
    for (const [index2, hook] of hooks.entries()) {
      try {
        const hookContext = hookContexts[index2];
        (_a = hook == null ? void 0 : hook.error) == null ? void 0 : _a.call(hook, hookContext, err, options.hookHints);
      } catch (err2) {
        this._logger.error(`Unhandled error during 'error' hook: ${err2}`);
        if (err2 instanceof Error) {
          this._logger.error(err2.stack);
        }
        this._logger.error(err2 == null ? void 0 : err2.stack);
      }
    }
  }
  finallyHooks(hooks, hookContexts, evaluationDetails, options) {
    var _a;
    for (const [index2, hook] of hooks.entries()) {
      try {
        const hookContext = hookContexts[index2];
        (_a = hook == null ? void 0 : hook.finally) == null ? void 0 : _a.call(hook, hookContext, evaluationDetails, options.hookHints);
      } catch (err) {
        this._logger.error(`Unhandled error during 'finally' hook: ${err}`);
        if (err instanceof Error) {
          this._logger.error(err.stack);
        }
        this._logger.error(err == null ? void 0 : err.stack);
      }
    }
  }
  get _provider() {
    return this.providerAccessor();
  }
  get _logger() {
    return this._clientLogger || this.globalLogger();
  }
  shortCircuitIfNotReady() {
    if (this.providerStatus === ClientProviderStatus.NOT_READY) {
      throw new ProviderNotReadyError("provider has not yet initialized");
    } else if (this.providerStatus === ClientProviderStatus.FATAL) {
      throw new ProviderFatalError("provider is in an irrecoverable error state");
    }
  }
  getErrorEvaluationDetails(flagKey, defaultValue2, err, flagMetadata = {}) {
    const errorMessage = err == null ? void 0 : err.message;
    const errorCode = (err == null ? void 0 : err.code) || ErrorCode.GENERAL;
    return {
      errorCode,
      errorMessage,
      value: defaultValue2,
      reason: StandardResolutionReasons.ERROR,
      flagMetadata: Object.freeze(flagMetadata),
      flagKey
    };
  }
};
var GLOBAL_OPENFEATURE_API_KEY = Symbol.for("@openfeature/web-sdk/api");
var _globalThis = globalThis;
var OpenFeatureAPI = class _OpenFeatureAPI extends OpenFeatureCommonAPI {
  constructor() {
    super("client");
    this._statusEnumType = ClientProviderStatus;
    this._apiEmitter = new OpenFeatureEventEmitter();
    this._defaultProvider = new ProviderWrapper(
      NOOP_PROVIDER,
      ClientProviderStatus.NOT_READY,
      this._statusEnumType
    );
    this._domainScopedProviders = /* @__PURE__ */ new Map();
    this._createEventEmitter = () => new OpenFeatureEventEmitter();
  }
  /**
   * Gets a singleton instance of the OpenFeature API.
   * @ignore
   * @returns {OpenFeatureAPI} OpenFeature API
   */
  static getInstance() {
    const globalApi = _globalThis[GLOBAL_OPENFEATURE_API_KEY];
    if (globalApi) {
      return globalApi;
    }
    const instance = new _OpenFeatureAPI();
    _globalThis[GLOBAL_OPENFEATURE_API_KEY] = instance;
    return instance;
  }
  getProviderStatus(domain) {
    var _a, _b;
    if (!domain) {
      return this._defaultProvider.status;
    }
    return (_b = (_a = this._domainScopedProviders.get(domain)) == null ? void 0 : _a.status) != null ? _b : this._defaultProvider.status;
  }
  setProviderAndWait(clientOrProvider, providerContextOrUndefined, contextOrUndefined) {
    return __async(this, null, function* () {
      const domain = stringOrUndefined(clientOrProvider);
      const provider2 = domain ? objectOrUndefined(providerContextOrUndefined) : objectOrUndefined(clientOrProvider);
      const context2 = domain ? objectOrUndefined(contextOrUndefined) : objectOrUndefined(providerContextOrUndefined);
      if (context2) {
        if (domain) {
          this._domainScopedContext.set(domain, context2);
        } else {
          this._context = context2;
        }
      }
      yield this.setAwaitableProvider(domain, provider2);
    });
  }
  setProvider(domainOrProvider, providerContextOrUndefined, contextOrUndefined) {
    const domain = stringOrUndefined(domainOrProvider);
    const provider2 = domain ? objectOrUndefined(providerContextOrUndefined) : objectOrUndefined(domainOrProvider);
    const context2 = domain ? objectOrUndefined(contextOrUndefined) : objectOrUndefined(providerContextOrUndefined);
    if (context2) {
      if (domain) {
        this._domainScopedContext.set(domain, context2);
      } else {
        this._context = context2;
      }
    }
    const maybePromise = this.setAwaitableProvider(domain, provider2);
    Promise.resolve(maybePromise).catch((err) => {
      this._logger.error("Error during provider initialization:", err);
    });
    return this;
  }
  getProvider(domain) {
    return this.getProviderForClient(domain);
  }
  setContext(domainOrContext, contextOrUndefined) {
    return __async(this, null, function* () {
      var _a, _b;
      const domain = stringOrUndefined(domainOrContext);
      const context2 = (_b = (_a = objectOrUndefined(domainOrContext)) != null ? _a : objectOrUndefined(contextOrUndefined)) != null ? _b : {};
      if (domain) {
        const wrapper = this._domainScopedProviders.get(domain);
        if (wrapper) {
          const oldContext = this.getContext(domain);
          this._domainScopedContext.set(domain, context2);
          yield this.runProviderContextChangeHandler(domain, wrapper, oldContext, context2);
        } else {
          this._domainScopedContext.set(domain, context2);
        }
      } else {
        const oldContext = this._context;
        this._context = context2;
        const unboundProviders = Array.from(this._domainScopedProviders.entries()).filter(([domain2]) => !this._domainScopedContext.has(domain2)).reduce((acc, [domain2, wrapper]) => {
          acc.push({ domain: domain2, wrapper });
          return acc;
        }, []);
        const allDomainRecords = [
          // add in the default (no domain)
          { domain: void 0, wrapper: this._defaultProvider },
          ...unboundProviders
        ];
        yield Promise.all(
          allDomainRecords.map((dm) => this.runProviderContextChangeHandler(dm.domain, dm.wrapper, oldContext, context2))
        );
      }
    });
  }
  getContext(domainOrUndefined) {
    const domain = stringOrUndefined(domainOrUndefined);
    if (domain) {
      const context2 = this._domainScopedContext.get(domain);
      if (context2) {
        return context2;
      } else {
        this._logger.debug(`Unable to find context for '${domain}'.`);
      }
    }
    return this._context;
  }
  clearContext(domainOrUndefined) {
    return __async(this, null, function* () {
      const domain = stringOrUndefined(domainOrUndefined);
      if (domain) {
        const wrapper = this._domainScopedProviders.get(domain);
        if (wrapper) {
          const oldContext = this.getContext(domain);
          this._domainScopedContext.delete(domain);
          const newContext = this.getContext();
          yield this.runProviderContextChangeHandler(domain, wrapper, oldContext, newContext);
        } else {
          this._domainScopedContext.delete(domain);
        }
      } else {
        return this.setContext({});
      }
    });
  }
  /**
   * Resets the global evaluation context and removes the evaluation context for
   * all domains.
   */
  clearContexts() {
    return __async(this, null, function* () {
      yield this.clearContext();
      yield Promise.allSettled(Array.from(this._domainScopedProviders.keys()).map((domain) => this.clearContext(domain)));
    });
  }
  /**
   * A factory function for creating new domain-scoped OpenFeature clients. Clients
   * can contain their own state (e.g. logger, hook, context). Multiple domains
   * can be used to segment feature flag configuration.
   *
   * If there is already a provider bound to this name via {@link this.setProvider setProvider}, this provider will be used.
   * Otherwise, the default provider is used until a provider is assigned to that name.
   * @param {string} domain An identifier which logically binds clients with providers
   * @param {string} version The version of the client (only used for metadata)
   * @returns {Client} OpenFeature Client
   */
  getClient(domain, version2) {
    return new OpenFeatureClient(
      // functions are passed here to make sure that these values are always up to date,
      // and so we don't have to make these public properties on the API class.
      () => this.getProviderForClient(domain),
      () => this.getProviderStatus(domain),
      () => this.buildAndCacheEventEmitterForClient(domain),
      (domain2) => this.getContext(domain2),
      () => this.getHooks(),
      () => this._logger,
      { domain, version: version2 }
    );
  }
  /**
   * Clears all registered providers and resets the default provider.
   * @returns {Promise<void>}
   */
  clearProviders() {
    return __async(this, null, function* () {
      yield __superGet(_OpenFeatureAPI.prototype, this, "clearProvidersAndSetDefault").call(this, NOOP_PROVIDER);
      this._domainScopedContext.clear();
    });
  }
  runProviderContextChangeHandler(domain, wrapper, oldContext, newContext) {
    return __async(this, null, function* () {
      var _a, _b, _c, _d, _e, _f;
      const providerName = ((_b = (_a = wrapper.provider) == null ? void 0 : _a.metadata) == null ? void 0 : _b.name) || "unnamed-provider";
      try {
        if (typeof wrapper.provider.onContextChange === "function") {
          const maybePromise = wrapper.provider.onContextChange(oldContext, newContext);
          if (typeof (maybePromise == null ? void 0 : maybePromise.then) === "function") {
            wrapper.incrementPendingContextChanges();
            wrapper.status = this._statusEnumType.RECONCILING;
            this.getAssociatedEventEmitters(domain).forEach((emitter) => {
              emitter == null ? void 0 : emitter.emit(ClientProviderEvents.Reconciling, { domain, providerName });
            });
            (_c = this._apiEmitter) == null ? void 0 : _c.emit(ClientProviderEvents.Reconciling, { domain, providerName });
            yield maybePromise;
            wrapper.decrementPendingContextChanges();
          }
        }
        wrapper.status = this._statusEnumType.READY;
        if (wrapper.allContextChangesSettled) {
          this.getAssociatedEventEmitters(domain).forEach((emitter) => {
            emitter == null ? void 0 : emitter.emit(ClientProviderEvents.ContextChanged, { clientName: domain, domain, providerName });
          });
          (_d = this._apiEmitter) == null ? void 0 : _d.emit(ClientProviderEvents.ContextChanged, { clientName: domain, domain, providerName });
        }
      } catch (err) {
        wrapper.decrementPendingContextChanges();
        wrapper.status = this._statusEnumType.ERROR;
        if (wrapper.allContextChangesSettled) {
          const error = err;
          const message = `Error running ${providerName}'s context change handler: ${error == null ? void 0 : error.message}`;
          (_e = this._logger) == null ? void 0 : _e.error(`${message}`, err);
          this.getAssociatedEventEmitters(domain).forEach((emitter) => {
            emitter == null ? void 0 : emitter.emit(ClientProviderEvents.Error, { clientName: domain, domain, providerName, message });
          });
          (_f = this._apiEmitter) == null ? void 0 : _f.emit(ClientProviderEvents.Error, { clientName: domain, domain, providerName, message });
        }
      }
    });
  }
};
var OpenFeature = OpenFeatureAPI.getInstance();
const global$1 = { "ui": { "sampleFeature": false }, "transport": {}, "perf": { "enableMetrics": false } };
const tenant = {};
const station = { "1": { "transport": { "type": "ws", "fallback": "sse" } } };
const screen = {};
const flags = {
  global: global$1,
  tenant,
  station,
  screen
};
const NAMESPACES$1 = ["ui", "transport", "perf"];
class JsonFileProvider {
  runsOn = "client";
  constructor(data) {
    this.data = data;
  }
  get metadata() {
    return { name: "json-file-provider" };
  }
  resolveEvaluation(flagKey, defaultValue2, context2) {
    const [namespace, key] = flagKey.split(".");
    if (!NAMESPACES$1.includes(namespace)) {
      return { value: defaultValue2, variant: "default" };
    }
    let value = this.data.global?.[namespace]?.[key];
    let variant = "global";
    const tenant2 = context2?.tenant;
    if (tenant2 && this.data.tenant?.[tenant2]?.[namespace]?.[key] !== void 0) {
      value = this.data.tenant[tenant2][namespace][key];
      variant = "tenant";
    }
    const station2 = context2?.station;
    if (station2 && this.data.station?.[station2]?.[namespace]?.[key] !== void 0) {
      value = this.data.station[station2][namespace][key];
      variant = "station";
    }
    const screen2 = context2?.screen;
    if (screen2 && this.data.screen?.[screen2]?.[namespace]?.[key] !== void 0) {
      value = this.data.screen[screen2][namespace][key];
      variant = "screen";
    }
    if (value === void 0) {
      return { value: defaultValue2, variant: "default" };
    }
    return { value, variant };
  }
  resolveBooleanEvaluation(flagKey, defaultValue2, context2, logger) {
    const { value, variant } = this.resolveEvaluation(
      flagKey,
      defaultValue2,
      context2
    );
    return Promise.resolve({ value, variant });
  }
  resolveStringEvaluation(flagKey, defaultValue2, context2, logger) {
    const { value, variant } = this.resolveEvaluation(
      flagKey,
      defaultValue2,
      context2
    );
    return Promise.resolve({ value, variant });
  }
  resolveNumberEvaluation(flagKey, defaultValue2, context2, logger) {
    const { value, variant } = this.resolveEvaluation(
      flagKey,
      defaultValue2,
      context2
    );
    return Promise.resolve({ value, variant });
  }
  resolveObjectEvaluation(flagKey, defaultValue2, context2, logger) {
    const { value, variant } = this.resolveEvaluation(
      flagKey,
      defaultValue2,
      context2
    );
    return Promise.resolve({ value, variant });
  }
}
const provider = new JsonFileProvider(flags);
OpenFeature.setProvider(provider);
function updateFeatureFlags(newFlags) {
  provider.data = newFlags;
}
const featureFlagClient = OpenFeature.getClient();
function useFeatureFlag(key, defaultValue2, context2 = {}) {
  const [state, setState] = reactExports.useState({ value: defaultValue2, variant: "default" });
  reactExports.useEffect(() => {
    let active = true;
    async function evaluate() {
      let result;
      switch (typeof defaultValue2) {
        case "string":
          result = await featureFlagClient.getStringDetails(
            key,
            defaultValue2,
            context2
          );
          break;
        case "number":
          result = await featureFlagClient.getNumberDetails(
            key,
            defaultValue2,
            context2
          );
          break;
        case "boolean":
          result = await featureFlagClient.getBooleanDetails(
            key,
            defaultValue2,
            context2
          );
          break;
        default:
          result = await featureFlagClient.getObjectDetails(
            key,
            defaultValue2,
            context2
          );
      }
      if (active) {
        setState({ value: result.value, variant: result.variant || "default" });
      }
    }
    evaluate();
    const interval = setInterval(evaluate, 1e3);
    return () => {
      active = false;
      clearInterval(interval);
    };
  }, [key, defaultValue2, context2]);
  return state;
}
const NAMESPACES = ["ui", "transport", "perf"];
function FlagRow({ flagKey, defaultValue: defaultValue2, context: context2, onSave }) {
  const { value, variant } = useFeatureFlag(flagKey, defaultValue2, context2);
  const [edit, setEdit] = reactExports.useState("");
  const handleSave = () => {
    onSave(flagKey, edit);
    setEdit("");
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: flagKey }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: /* @__PURE__ */ jsxRuntimeExports.jsx(
      "input",
      {
        className: "form-control",
        value: edit,
        placeholder: String(value),
        onChange: (e2) => setEdit(e2.target.value)
      }
    ) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: variant }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("td", { children: /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-primary", onClick: handleSave, children: "Save" }) })
  ] });
}
function FeatureFlagsAdminPanel() {
  const [tenantId, setTenantId] = reactExports.useState("");
  const [stationId, setStationId] = reactExports.useState("");
  const [screenId, setScreenId] = reactExports.useState("");
  const [flagsData, setFlagsData] = reactExports.useState(null);
  const context2 = {};
  if (tenantId) context2.tenant = tenantId;
  if (stationId) context2.station = stationId;
  if (screenId) context2.screen = screenId;
  async function loadFlags() {
    const res = await fetch("/api/flags");
    const data = await res.json();
    setFlagsData(data);
    updateFeatureFlags(data);
  }
  reactExports.useEffect(() => {
    loadFlags();
  }, []);
  const handleSave = async (flagKey, raw) => {
    let value;
    if (raw === "") {
      value = null;
    } else {
      try {
        value = JSON.parse(raw);
      } catch {
        value = raw;
      }
    }
    const [namespace, key] = flagKey.split(".");
    let level = "global";
    let id2;
    if (screenId) {
      level = "screen";
      id2 = screenId;
    } else if (stationId) {
      level = "station";
      id2 = stationId;
    } else if (tenantId) {
      level = "tenant";
      id2 = tenantId;
    }
    await fetch("/api/flags", {
      method: "PUT",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ level, id: id2, namespace, key, value })
    });
    await loadFlags();
  };
  if (!flagsData) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading..." });
  const rows = [];
  NAMESPACES.forEach((ns) => {
    const keySet = new Set(Object.keys(flagsData.global?.[ns] || {}));
    Object.values(flagsData.tenant || {}).forEach((scope) => {
      Object.keys(scope[ns] || {}).forEach((k2) => keySet.add(k2));
    });
    Object.values(flagsData.station || {}).forEach((scope) => {
      Object.keys(scope[ns] || {}).forEach((k2) => keySet.add(k2));
    });
    Object.values(flagsData.screen || {}).forEach((scope) => {
      Object.keys(scope[ns] || {}).forEach((k2) => keySet.add(k2));
    });
    keySet.forEach((key) => {
      const fullKey = `${ns}.${key}`;
      const defaultValue2 = flagsData.global?.[ns]?.[key] !== void 0 ? flagsData.global[ns][key] : false;
      rows.push(
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          FlagRow,
          {
            flagKey: fullKey,
            defaultValue: defaultValue2,
            context: context2,
            onSave: handleSave
          },
          fullKey
        )
      );
    });
  });
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label me-2", children: "Tenant" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          className: "form-control d-inline-block w-auto me-3",
          value: tenantId,
          onChange: (e2) => setTenantId(e2.target.value)
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label me-2", children: "Station" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          className: "form-control d-inline-block w-auto me-3",
          value: stationId,
          onChange: (e2) => setStationId(e2.target.value)
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label me-2", children: "Screen" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          className: "form-control d-inline-block w-auto",
          value: screenId,
          onChange: (e2) => setScreenId(e2.target.value)
        }
      )
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("table", { className: "table", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("thead", { children: /* @__PURE__ */ jsxRuntimeExports.jsxs("tr", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Flag" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Value" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", { children: "Source" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("th", {})
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("tbody", { children: rows })
    ] })
  ] });
}
FeatureFlagsAdminPanel.meta = {
  id: "feature-flags",
  title: "Feature Flags",
  dataDomain: "feature-flags",
  scopes: [],
  latencyClass: "interactive"
};
function ColorInput({ label, value, onChange }) {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label small d-block", children: label }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("input", { type: "color", className: "form-control form-control-color", value, onChange: (e2) => onChange(e2.target.value) })
  ] });
}
function ThemeEditorPro() {
  const [tokens, setTokens] = reactExports.useState(null);
  const [saveMsg, setSaveMsg] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [scope, setScope] = reactExports.useState("global");
  const [scopeId, setScopeId] = reactExports.useState("");
  reactExports.useEffect(() => {
    (async () => {
      try {
        const t2 = await resolveTokens();
        setTokens(t2);
      } catch (e2) {
        setError("Failed to load tokens");
      }
    })();
  }, []);
  if (!tokens) return /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: "Loading…" });
  const get = (p, def = "") => p.split(".").reduce((o, k2) => o?.[k2], tokens)?.$value ?? def;
  const set = (p, v) => {
    const parts = p.split(".");
    setTokens((t2) => {
      const next = { ...t2 };
      let cur = next;
      for (let i2 = 0; i2 < parts.length; i2++) {
        const k2 = parts[i2];
        if (i2 === parts.length - 1) {
          cur[k2] = { ...cur[k2] || {}, $value: v };
        } else {
          cur[k2] = { ...cur[k2] || {} };
          cur = cur[k2];
        }
      }
      return next;
    });
  };
  const save = async () => {
    setSaveMsg(null);
    setError(null);
    try {
      const body = JSON.stringify({ color: tokens.color, font: tokens.font, space: tokens.space, radius: tokens.radius });
      const qs = new URLSearchParams();
      if (scope === "station" && scopeId) qs.append("stationId", scopeId);
      if (scope === "screen" && scopeId) qs.append("screenId", scopeId);
      const res = await fetch(`/api/tokens${qs.toString() ? `?${qs}` : ""}`, { method: "POST", headers: { "Content-Type": "application/json" }, body });
      if (!res.ok) throw new Error("Save failed");
      clearTokenCache();
      setSaveMsg("Saved");
    } catch (e2) {
      setError("Save failed");
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { className: "mb-3", children: "Theme" }),
    error && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-danger", children: error }),
    saveMsg && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "alert alert-success", children: saveMsg }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section mb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-3 align-items-end", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label small", children: "Scope" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("select", { className: "form-select", value: scope, onChange: (e2) => setScope(e2.target.value), children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "global", children: "Global" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "station", children: "Station" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "screen", children: "Screen" })
        ] })
      ] }),
      (scope === "station" || scope === "screen") && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label small", children: scope === "station" ? "Station ID" : "Screen ID" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", value: scopeId, onChange: (e2) => setScopeId(e2.target.value), placeholder: "e.g. 1" })
      ] })
    ] }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(ColorInput, { label: "Surface", value: get("color.surface", "#f8f9fa"), onChange: (v) => set("color.surface", v) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ColorInput, { label: "Text", value: get("color.text", "#212529"), onChange: (v) => set("color.text", v) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ColorInput, { label: "Accent", value: get("color.accent", "#0d6efd"), onChange: (v) => set("color.accent", v) }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(ColorInput, { label: "Background", value: get("color.background", "#ffffff"), onChange: (v) => set("color.background", v) })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-primary mt-3", onClick: save, children: "Save Theme" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "admin-section mt-3", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h5", { children: "Typography & Spacing" }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "row g-3", children: [
        ["sm", "md", "lg", "xl"].map((k2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "form-label small", children: [
            "Font ",
            k2.toUpperCase()
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", value: get(`font.size.${k2}`, ""), onChange: (e2) => set(`font.size.${k2}`, e2.target.value), placeholder: "e.g. 1rem" })
        ] }, k2)),
        ["xs", "sm", "md", "lg", "xl"].map((k2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "form-label small", children: [
            "Space ",
            k2.toUpperCase()
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", value: get(`space.${k2}`, ""), onChange: (e2) => set(`space.${k2}`, e2.target.value), placeholder: "e.g. 0.5rem" })
        ] }, k2)),
        ["card", "button", "field"].map((k2) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "col-md-3", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "form-label small", children: [
            "Radius ",
            k2
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx("input", { className: "form-control", value: get(`radius.${k2}`, ""), onChange: (e2) => set(`radius.${k2}`, e2.target.value), placeholder: "e.g. 0.5rem" })
        ] }, k2))
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-primary mt-3", onClick: save, children: "Save Theme" })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-muted small mt-2", children: "Advanced editing is available in the legacy JSON editor." })
  ] });
}
function _typeof$1(o) {
  "@babel/helpers - typeof";
  return _typeof$1 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
    return typeof o2;
  } : function(o2) {
    return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
  }, _typeof$1(o);
}
function toPrimitive$1(t2, r2) {
  if ("object" != _typeof$1(t2) || !t2) return t2;
  var e2 = t2[Symbol.toPrimitive];
  if (void 0 !== e2) {
    var i2 = e2.call(t2, r2);
    if ("object" != _typeof$1(i2)) return i2;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r2 ? String : Number)(t2);
}
function toPropertyKey$1(t2) {
  var i2 = toPrimitive$1(t2, "string");
  return "symbol" == _typeof$1(i2) ? i2 : i2 + "";
}
function _defineProperty$1(e2, r2, t2) {
  return (r2 = toPropertyKey$1(r2)) in e2 ? Object.defineProperty(e2, r2, {
    value: t2,
    enumerable: true,
    configurable: true,
    writable: true
  }) : e2[r2] = t2, e2;
}
function ownKeys(e2, r2) {
  var t2 = Object.keys(e2);
  if (Object.getOwnPropertySymbols) {
    var o = Object.getOwnPropertySymbols(e2);
    r2 && (o = o.filter(function(r3) {
      return Object.getOwnPropertyDescriptor(e2, r3).enumerable;
    })), t2.push.apply(t2, o);
  }
  return t2;
}
function _objectSpread2(e2) {
  for (var r2 = 1; r2 < arguments.length; r2++) {
    var t2 = null != arguments[r2] ? arguments[r2] : {};
    r2 % 2 ? ownKeys(Object(t2), true).forEach(function(r3) {
      _defineProperty$1(e2, r3, t2[r3]);
    }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e2, Object.getOwnPropertyDescriptors(t2)) : ownKeys(Object(t2)).forEach(function(r3) {
      Object.defineProperty(e2, r3, Object.getOwnPropertyDescriptor(t2, r3));
    });
  }
  return e2;
}
function _objectWithoutProperties(e2, t2) {
  if (null == e2) return {};
  var o, r2, i2 = _objectWithoutPropertiesLoose$7(e2, t2);
  if (Object.getOwnPropertySymbols) {
    var n = Object.getOwnPropertySymbols(e2);
    for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t2.indexOf(o) && {}.propertyIsEnumerable.call(e2, o) && (i2[o] = e2[o]);
  }
  return i2;
}
function _classCallCheck(a3, n) {
  if (!(a3 instanceof n)) throw new TypeError("Cannot call a class as a function");
}
function _defineProperties(e2, r2) {
  for (var t2 = 0; t2 < r2.length; t2++) {
    var o = r2[t2];
    o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e2, toPropertyKey$1(o.key), o);
  }
}
function _createClass(e2, r2, t2) {
  return r2 && _defineProperties(e2.prototype, r2), t2 && _defineProperties(e2, t2), Object.defineProperty(e2, "prototype", {
    writable: false
  }), e2;
}
function _getPrototypeOf(t2) {
  return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t3) {
    return t3.__proto__ || Object.getPrototypeOf(t3);
  }, _getPrototypeOf(t2);
}
function _isNativeReflectConstruct() {
  try {
    var t2 = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch (t3) {
  }
  return (_isNativeReflectConstruct = function _isNativeReflectConstruct2() {
    return !!t2;
  })();
}
function _assertThisInitialized(e2) {
  if (void 0 === e2) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e2;
}
function _possibleConstructorReturn(t2, e2) {
  if (e2 && ("object" == _typeof$1(e2) || "function" == typeof e2)) return e2;
  if (void 0 !== e2) throw new TypeError("Derived constructors may only return object or undefined");
  return _assertThisInitialized(t2);
}
function _callSuper(t2, o, e2) {
  return o = _getPrototypeOf(o), _possibleConstructorReturn(t2, _isNativeReflectConstruct() ? Reflect.construct(o, e2 || [], _getPrototypeOf(t2).constructor) : o.apply(t2, e2));
}
function _inherits(t2, e2) {
  if ("function" != typeof e2 && null !== e2) throw new TypeError("Super expression must either be null or a function");
  t2.prototype = Object.create(e2 && e2.prototype, {
    constructor: {
      value: t2,
      writable: true,
      configurable: true
    }
  }), Object.defineProperty(t2, "prototype", {
    writable: false
  }), e2 && _setPrototypeOf(t2, e2);
}
function _arrayWithHoles(r2) {
  if (Array.isArray(r2)) return r2;
}
function _iterableToArrayLimit(r2, l) {
  var t2 = null == r2 ? null : "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
  if (null != t2) {
    var e2, n, i2, u2, a3 = [], f = true, o = false;
    try {
      if (i2 = (t2 = t2.call(r2)).next, 0 === l) ;
      else for (; !(f = (e2 = i2.call(t2)).done) && (a3.push(e2.value), a3.length !== l); f = true) ;
    } catch (r3) {
      o = true, n = r3;
    } finally {
      try {
        if (!f && null != t2["return"] && (u2 = t2["return"](), Object(u2) !== u2)) return;
      } finally {
        if (o) throw n;
      }
    }
    return a3;
  }
}
function _arrayLikeToArray(r2, a3) {
  (null == a3 || a3 > r2.length) && (a3 = r2.length);
  for (var e2 = 0, n = Array(a3); e2 < a3; e2++) n[e2] = r2[e2];
  return n;
}
function _unsupportedIterableToArray(r2, a3) {
  if (r2) {
    if ("string" == typeof r2) return _arrayLikeToArray(r2, a3);
    var t2 = {}.toString.call(r2).slice(8, -1);
    return "Object" === t2 && r2.constructor && (t2 = r2.constructor.name), "Map" === t2 || "Set" === t2 ? Array.from(r2) : "Arguments" === t2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t2) ? _arrayLikeToArray(r2, a3) : void 0;
  }
}
function _nonIterableRest() {
  throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _slicedToArray(r2, e2) {
  return _arrayWithHoles(r2) || _iterableToArrayLimit(r2, e2) || _unsupportedIterableToArray(r2, e2) || _nonIterableRest();
}
function r(e2) {
  var t2, f, n = "";
  if ("string" == typeof e2 || "number" == typeof e2) n += e2;
  else if ("object" == typeof e2) if (Array.isArray(e2)) for (t2 = 0; t2 < e2.length; t2++) e2[t2] && (f = r(e2[t2])) && (n && (n += " "), n += f);
  else for (t2 in e2) e2[t2] && (n && (n += " "), n += t2);
  return n;
}
function clsx() {
  for (var e2, t2, f = 0, n = ""; f < arguments.length; ) (e2 = arguments[f++]) && (t2 = r(e2)) && (n && (n += " "), n += t2);
  return n;
}
const clsx_m = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  clsx,
  default: clsx
}, Symbol.toStringTag, { value: "Module" }));
var MILI = "milliseconds", SECONDS = "seconds", MINUTES = "minutes", HOURS = "hours", DAY = "day", WEEK = "week", MONTH = "month", YEAR = "year", DECADE = "decade", CENTURY = "century";
var multiplierMilli = {
  "milliseconds": 1,
  "seconds": 1e3,
  "minutes": 60 * 1e3,
  "hours": 60 * 60 * 1e3,
  "day": 24 * 60 * 60 * 1e3,
  "week": 7 * 24 * 60 * 60 * 1e3
};
var multiplierMonth = {
  "month": 1,
  "year": 12,
  "decade": 10 * 12,
  "century": 100 * 12
};
function daysOf(year2) {
  return [31, daysInFeb(year2), 31, 30, 31, 30, 31, 31, 30, 31, 30, 31];
}
function daysInFeb(year2) {
  return year2 % 4 === 0 && year2 % 100 !== 0 || year2 % 400 === 0 ? 29 : 28;
}
function add(d3, num, unit) {
  d3 = new Date(d3);
  switch (unit) {
    case MILI:
    case SECONDS:
    case MINUTES:
    case HOURS:
    case DAY:
    case WEEK:
      return addMillis(d3, num * multiplierMilli[unit]);
    case MONTH:
    case YEAR:
    case DECADE:
    case CENTURY:
      return addMonths(d3, num * multiplierMonth[unit]);
  }
  throw new TypeError('Invalid units: "' + unit + '"');
}
function addMillis(d3, num) {
  var nextDate = new Date(+d3 + num);
  return solveDST(d3, nextDate);
}
function addMonths(d3, num) {
  var year2 = d3.getFullYear(), month2 = d3.getMonth(), day2 = d3.getDate(), totalMonths = year2 * 12 + month2 + num, nextYear = Math.trunc(totalMonths / 12), nextMonth = totalMonths % 12, nextDay = Math.min(day2, daysOf(nextYear)[nextMonth]);
  var nextDate = new Date(d3);
  nextDate.setFullYear(nextYear);
  nextDate.setDate(1);
  nextDate.setMonth(nextMonth);
  nextDate.setDate(nextDay);
  return nextDate;
}
function solveDST(currentDate, nextDate) {
  var currentOffset = currentDate.getTimezoneOffset(), nextOffset = nextDate.getTimezoneOffset();
  var diffMinutes = nextOffset - currentOffset;
  return new Date(+nextDate + diffMinutes * multiplierMilli["minutes"]);
}
function subtract(d3, num, unit) {
  return add(d3, -num, unit);
}
function startOf(d3, unit, firstOfWeek) {
  d3 = new Date(d3);
  switch (unit) {
    case CENTURY:
    case DECADE:
    case YEAR:
      d3 = month(d3, 0);
    case MONTH:
      d3 = date(d3, 1);
    case WEEK:
    case DAY:
      d3 = hours(d3, 0);
    case HOURS:
      d3 = minutes(d3, 0);
    case MINUTES:
      d3 = seconds(d3, 0);
    case SECONDS:
      d3 = milliseconds(d3, 0);
  }
  if (unit === DECADE)
    d3 = subtract(d3, year(d3) % 10, "year");
  if (unit === CENTURY)
    d3 = subtract(d3, year(d3) % 100, "year");
  if (unit === WEEK)
    d3 = weekday(d3, 0, firstOfWeek);
  return d3;
}
function endOf(d3, unit, firstOfWeek) {
  d3 = new Date(d3);
  d3 = startOf(d3, unit, firstOfWeek);
  switch (unit) {
    case CENTURY:
    case DECADE:
    case YEAR:
    case MONTH:
    case WEEK:
      d3 = add(d3, 1, unit);
      d3 = subtract(d3, 1, DAY);
      d3.setHours(23, 59, 59, 999);
      break;
    case DAY:
      d3.setHours(23, 59, 59, 999);
      break;
    case HOURS:
    case MINUTES:
    case SECONDS:
      d3 = add(d3, 1, unit);
      d3 = subtract(d3, 1, MILI);
  }
  return d3;
}
var eq = createComparer(function(a3, b2) {
  return a3 === b2;
});
var neq = createComparer(function(a3, b2) {
  return a3 !== b2;
});
var gt = createComparer(function(a3, b2) {
  return a3 > b2;
});
var gte = createComparer(function(a3, b2) {
  return a3 >= b2;
});
var lt = createComparer(function(a3, b2) {
  return a3 < b2;
});
var lte = createComparer(function(a3, b2) {
  return a3 <= b2;
});
function min() {
  return new Date(Math.min.apply(Math, arguments));
}
function max() {
  return new Date(Math.max.apply(Math, arguments));
}
function inRange$1(day2, min2, max2, unit) {
  unit = unit || "day";
  return (!min2 || gte(day2, min2, unit)) && (!max2 || lte(day2, max2, unit));
}
var milliseconds = createAccessor("Milliseconds");
var seconds = createAccessor("Seconds");
var minutes = createAccessor("Minutes");
var hours = createAccessor("Hours");
var day = createAccessor("Day");
var date = createAccessor("Date");
var month = createAccessor("Month");
var year = createAccessor("FullYear");
function decade(d3, val) {
  return val === void 0 ? year(startOf(d3, DECADE)) : add(d3, val + 10, YEAR);
}
function century(d3, val) {
  return val === void 0 ? year(startOf(d3, CENTURY)) : add(d3, val + 100, YEAR);
}
function weekday(d3, val, firstDay) {
  var w2 = (day(d3) + 7 - (firstDay || 0)) % 7;
  return val === void 0 ? w2 : add(d3, val - w2, DAY);
}
function diff$1(date1, date2, unit, asFloat) {
  var dividend, divisor, result;
  switch (unit) {
    case MILI:
    case SECONDS:
    case MINUTES:
    case HOURS:
    case DAY:
    case WEEK:
      dividend = date2.getTime() - date1.getTime();
      break;
    case MONTH:
    case YEAR:
    case DECADE:
    case CENTURY:
      dividend = (year(date2) - year(date1)) * 12 + month(date2) - month(date1);
      break;
    default:
      throw new TypeError('Invalid units: "' + unit + '"');
  }
  switch (unit) {
    case MILI:
      divisor = 1;
      break;
    case SECONDS:
      divisor = 1e3;
      break;
    case MINUTES:
      divisor = 1e3 * 60;
      break;
    case HOURS:
      divisor = 1e3 * 60 * 60;
      break;
    case DAY:
      divisor = 1e3 * 60 * 60 * 24;
      break;
    case WEEK:
      divisor = 1e3 * 60 * 60 * 24 * 7;
      break;
    case MONTH:
      divisor = 1;
      break;
    case YEAR:
      divisor = 12;
      break;
    case DECADE:
      divisor = 120;
      break;
    case CENTURY:
      divisor = 1200;
      break;
    default:
      throw new TypeError('Invalid units: "' + unit + '"');
  }
  result = dividend / divisor;
  return asFloat ? result : Math.round(result);
}
function createAccessor(method) {
  var hourLength = (function(method2) {
    switch (method2) {
      case "Milliseconds":
        return 36e5;
      case "Seconds":
        return 3600;
      case "Minutes":
        return 60;
      case "Hours":
        return 1;
      default:
        return null;
    }
  })(method);
  return function(d3, val) {
    if (val === void 0)
      return d3["get" + method]();
    var dateOut = new Date(d3);
    dateOut["set" + method](val);
    if (hourLength && dateOut["get" + method]() != val && (method === "Hours" || val >= hourLength && dateOut.getHours() - d3.getHours() < Math.floor(val / hourLength))) {
      dateOut["set" + method](val + hourLength);
    }
    return dateOut;
  };
}
function createComparer(operator) {
  return function(a3, b2, unit) {
    return operator(+startOf(a3, unit), +startOf(b2, unit));
  };
}
const dates = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  add,
  century,
  date,
  day,
  decade,
  diff: diff$1,
  endOf,
  eq,
  gt,
  gte,
  hours,
  inRange: inRange$1,
  lt,
  lte,
  max,
  milliseconds,
  min,
  minutes,
  month,
  neq,
  seconds,
  startOf,
  subtract,
  weekday,
  year
}, Symbol.toStringTag, { value: "Module" }));
function _arrayWithoutHoles(r2) {
  if (Array.isArray(r2)) return _arrayLikeToArray(r2);
}
function _iterableToArray(r2) {
  if ("undefined" != typeof Symbol && null != r2[Symbol.iterator] || null != r2["@@iterator"]) return Array.from(r2);
}
function _nonIterableSpread() {
  throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _toConsumableArray(r2) {
  return _arrayWithoutHoles(r2) || _iterableToArray(r2) || _unsupportedIterableToArray(r2) || _nonIterableSpread();
}
var _baseSlice;
var hasRequired_baseSlice;
function require_baseSlice() {
  if (hasRequired_baseSlice) return _baseSlice;
  hasRequired_baseSlice = 1;
  function baseSlice(array, start2, end2) {
    var index2 = -1, length = array.length;
    if (start2 < 0) {
      start2 = -start2 > length ? 0 : length + start2;
    }
    end2 = end2 > length ? length : end2;
    if (end2 < 0) {
      end2 += length;
    }
    length = start2 > end2 ? 0 : end2 - start2 >>> 0;
    start2 >>>= 0;
    var result = Array(length);
    while (++index2 < length) {
      result[index2] = array[index2 + start2];
    }
    return result;
  }
  _baseSlice = baseSlice;
  return _baseSlice;
}
var eq_1;
var hasRequiredEq;
function requireEq() {
  if (hasRequiredEq) return eq_1;
  hasRequiredEq = 1;
  function eq2(value, other) {
    return value === other || value !== value && other !== other;
  }
  eq_1 = eq2;
  return eq_1;
}
var _freeGlobal;
var hasRequired_freeGlobal;
function require_freeGlobal() {
  if (hasRequired_freeGlobal) return _freeGlobal;
  hasRequired_freeGlobal = 1;
  var freeGlobal = typeof commonjsGlobal == "object" && commonjsGlobal && commonjsGlobal.Object === Object && commonjsGlobal;
  _freeGlobal = freeGlobal;
  return _freeGlobal;
}
var _root;
var hasRequired_root;
function require_root() {
  if (hasRequired_root) return _root;
  hasRequired_root = 1;
  var freeGlobal = require_freeGlobal();
  var freeSelf = typeof self == "object" && self && self.Object === Object && self;
  var root = freeGlobal || freeSelf || Function("return this")();
  _root = root;
  return _root;
}
var _Symbol;
var hasRequired_Symbol;
function require_Symbol() {
  if (hasRequired_Symbol) return _Symbol;
  hasRequired_Symbol = 1;
  var root = require_root();
  var Symbol2 = root.Symbol;
  _Symbol = Symbol2;
  return _Symbol;
}
var _getRawTag;
var hasRequired_getRawTag;
function require_getRawTag() {
  if (hasRequired_getRawTag) return _getRawTag;
  hasRequired_getRawTag = 1;
  var Symbol2 = require_Symbol();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  var nativeObjectToString = objectProto.toString;
  var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
  function getRawTag(value) {
    var isOwn = hasOwnProperty.call(value, symToStringTag), tag = value[symToStringTag];
    try {
      value[symToStringTag] = void 0;
      var unmasked = true;
    } catch (e2) {
    }
    var result = nativeObjectToString.call(value);
    if (unmasked) {
      if (isOwn) {
        value[symToStringTag] = tag;
      } else {
        delete value[symToStringTag];
      }
    }
    return result;
  }
  _getRawTag = getRawTag;
  return _getRawTag;
}
var _objectToString;
var hasRequired_objectToString;
function require_objectToString() {
  if (hasRequired_objectToString) return _objectToString;
  hasRequired_objectToString = 1;
  var objectProto = Object.prototype;
  var nativeObjectToString = objectProto.toString;
  function objectToString(value) {
    return nativeObjectToString.call(value);
  }
  _objectToString = objectToString;
  return _objectToString;
}
var _baseGetTag;
var hasRequired_baseGetTag;
function require_baseGetTag() {
  if (hasRequired_baseGetTag) return _baseGetTag;
  hasRequired_baseGetTag = 1;
  var Symbol2 = require_Symbol(), getRawTag = require_getRawTag(), objectToString = require_objectToString();
  var nullTag = "[object Null]", undefinedTag = "[object Undefined]";
  var symToStringTag = Symbol2 ? Symbol2.toStringTag : void 0;
  function baseGetTag(value) {
    if (value == null) {
      return value === void 0 ? undefinedTag : nullTag;
    }
    return symToStringTag && symToStringTag in Object(value) ? getRawTag(value) : objectToString(value);
  }
  _baseGetTag = baseGetTag;
  return _baseGetTag;
}
var isObject_1;
var hasRequiredIsObject;
function requireIsObject() {
  if (hasRequiredIsObject) return isObject_1;
  hasRequiredIsObject = 1;
  function isObject2(value) {
    var type = typeof value;
    return value != null && (type == "object" || type == "function");
  }
  isObject_1 = isObject2;
  return isObject_1;
}
var isFunction_1;
var hasRequiredIsFunction;
function requireIsFunction() {
  if (hasRequiredIsFunction) return isFunction_1;
  hasRequiredIsFunction = 1;
  var baseGetTag = require_baseGetTag(), isObject2 = requireIsObject();
  var asyncTag = "[object AsyncFunction]", funcTag = "[object Function]", genTag = "[object GeneratorFunction]", proxyTag = "[object Proxy]";
  function isFunction2(value) {
    if (!isObject2(value)) {
      return false;
    }
    var tag = baseGetTag(value);
    return tag == funcTag || tag == genTag || tag == asyncTag || tag == proxyTag;
  }
  isFunction_1 = isFunction2;
  return isFunction_1;
}
var isLength_1;
var hasRequiredIsLength;
function requireIsLength() {
  if (hasRequiredIsLength) return isLength_1;
  hasRequiredIsLength = 1;
  var MAX_SAFE_INTEGER = 9007199254740991;
  function isLength(value) {
    return typeof value == "number" && value > -1 && value % 1 == 0 && value <= MAX_SAFE_INTEGER;
  }
  isLength_1 = isLength;
  return isLength_1;
}
var isArrayLike_1;
var hasRequiredIsArrayLike;
function requireIsArrayLike() {
  if (hasRequiredIsArrayLike) return isArrayLike_1;
  hasRequiredIsArrayLike = 1;
  var isFunction2 = requireIsFunction(), isLength = requireIsLength();
  function isArrayLike(value) {
    return value != null && isLength(value.length) && !isFunction2(value);
  }
  isArrayLike_1 = isArrayLike;
  return isArrayLike_1;
}
var _isIndex;
var hasRequired_isIndex;
function require_isIndex() {
  if (hasRequired_isIndex) return _isIndex;
  hasRequired_isIndex = 1;
  var MAX_SAFE_INTEGER = 9007199254740991;
  var reIsUint = /^(?:0|[1-9]\d*)$/;
  function isIndex(value, length) {
    var type = typeof value;
    length = length == null ? MAX_SAFE_INTEGER : length;
    return !!length && (type == "number" || type != "symbol" && reIsUint.test(value)) && (value > -1 && value % 1 == 0 && value < length);
  }
  _isIndex = isIndex;
  return _isIndex;
}
var _isIterateeCall;
var hasRequired_isIterateeCall;
function require_isIterateeCall() {
  if (hasRequired_isIterateeCall) return _isIterateeCall;
  hasRequired_isIterateeCall = 1;
  var eq2 = requireEq(), isArrayLike = requireIsArrayLike(), isIndex = require_isIndex(), isObject2 = requireIsObject();
  function isIterateeCall(value, index2, object) {
    if (!isObject2(object)) {
      return false;
    }
    var type = typeof index2;
    if (type == "number" ? isArrayLike(object) && isIndex(index2, object.length) : type == "string" && index2 in object) {
      return eq2(object[index2], value);
    }
    return false;
  }
  _isIterateeCall = isIterateeCall;
  return _isIterateeCall;
}
var _trimmedEndIndex;
var hasRequired_trimmedEndIndex;
function require_trimmedEndIndex() {
  if (hasRequired_trimmedEndIndex) return _trimmedEndIndex;
  hasRequired_trimmedEndIndex = 1;
  var reWhitespace = /\s/;
  function trimmedEndIndex(string) {
    var index2 = string.length;
    while (index2-- && reWhitespace.test(string.charAt(index2))) {
    }
    return index2;
  }
  _trimmedEndIndex = trimmedEndIndex;
  return _trimmedEndIndex;
}
var _baseTrim;
var hasRequired_baseTrim;
function require_baseTrim() {
  if (hasRequired_baseTrim) return _baseTrim;
  hasRequired_baseTrim = 1;
  var trimmedEndIndex = require_trimmedEndIndex();
  var reTrimStart = /^\s+/;
  function baseTrim(string) {
    return string ? string.slice(0, trimmedEndIndex(string) + 1).replace(reTrimStart, "") : string;
  }
  _baseTrim = baseTrim;
  return _baseTrim;
}
var isObjectLike_1;
var hasRequiredIsObjectLike;
function requireIsObjectLike() {
  if (hasRequiredIsObjectLike) return isObjectLike_1;
  hasRequiredIsObjectLike = 1;
  function isObjectLike(value) {
    return value != null && typeof value == "object";
  }
  isObjectLike_1 = isObjectLike;
  return isObjectLike_1;
}
var isSymbol_1;
var hasRequiredIsSymbol;
function requireIsSymbol() {
  if (hasRequiredIsSymbol) return isSymbol_1;
  hasRequiredIsSymbol = 1;
  var baseGetTag = require_baseGetTag(), isObjectLike = requireIsObjectLike();
  var symbolTag = "[object Symbol]";
  function isSymbol(value) {
    return typeof value == "symbol" || isObjectLike(value) && baseGetTag(value) == symbolTag;
  }
  isSymbol_1 = isSymbol;
  return isSymbol_1;
}
var toNumber_1;
var hasRequiredToNumber;
function requireToNumber() {
  if (hasRequiredToNumber) return toNumber_1;
  hasRequiredToNumber = 1;
  var baseTrim = require_baseTrim(), isObject2 = requireIsObject(), isSymbol = requireIsSymbol();
  var NAN = 0 / 0;
  var reIsBadHex = /^[-+]0x[0-9a-f]+$/i;
  var reIsBinary = /^0b[01]+$/i;
  var reIsOctal = /^0o[0-7]+$/i;
  var freeParseInt = parseInt;
  function toNumber(value) {
    if (typeof value == "number") {
      return value;
    }
    if (isSymbol(value)) {
      return NAN;
    }
    if (isObject2(value)) {
      var other = typeof value.valueOf == "function" ? value.valueOf() : value;
      value = isObject2(other) ? other + "" : other;
    }
    if (typeof value != "string") {
      return value === 0 ? value : +value;
    }
    value = baseTrim(value);
    var isBinary = reIsBinary.test(value);
    return isBinary || reIsOctal.test(value) ? freeParseInt(value.slice(2), isBinary ? 2 : 8) : reIsBadHex.test(value) ? NAN : +value;
  }
  toNumber_1 = toNumber;
  return toNumber_1;
}
var toFinite_1;
var hasRequiredToFinite;
function requireToFinite() {
  if (hasRequiredToFinite) return toFinite_1;
  hasRequiredToFinite = 1;
  var toNumber = requireToNumber();
  var INFINITY = 1 / 0, MAX_INTEGER = 17976931348623157e292;
  function toFinite(value) {
    if (!value) {
      return value === 0 ? value : 0;
    }
    value = toNumber(value);
    if (value === INFINITY || value === -INFINITY) {
      var sign = value < 0 ? -1 : 1;
      return sign * MAX_INTEGER;
    }
    return value === value ? value : 0;
  }
  toFinite_1 = toFinite;
  return toFinite_1;
}
var toInteger_1;
var hasRequiredToInteger;
function requireToInteger() {
  if (hasRequiredToInteger) return toInteger_1;
  hasRequiredToInteger = 1;
  var toFinite = requireToFinite();
  function toInteger2(value) {
    var result = toFinite(value), remainder = result % 1;
    return result === result ? remainder ? result - remainder : result : 0;
  }
  toInteger_1 = toInteger2;
  return toInteger_1;
}
var chunk_1;
var hasRequiredChunk;
function requireChunk() {
  if (hasRequiredChunk) return chunk_1;
  hasRequiredChunk = 1;
  var baseSlice = require_baseSlice(), isIterateeCall = require_isIterateeCall(), toInteger2 = requireToInteger();
  var nativeCeil = Math.ceil, nativeMax = Math.max;
  function chunk(array, size2, guard) {
    if (guard ? isIterateeCall(array, size2, guard) : size2 === void 0) {
      size2 = 1;
    } else {
      size2 = nativeMax(toInteger2(size2), 0);
    }
    var length = array == null ? 0 : array.length;
    if (!length || size2 < 1) {
      return [];
    }
    var index2 = 0, resIndex = 0, result = Array(nativeCeil(length / size2));
    while (index2 < length) {
      result[resIndex++] = baseSlice(array, index2, index2 += size2);
    }
    return result;
  }
  chunk_1 = chunk;
  return chunk_1;
}
var chunkExports = requireChunk();
function isDocument(element) {
  return "nodeType" in element && element.nodeType === document.DOCUMENT_NODE;
}
function isWindow(node) {
  if ("window" in node && node.window === node) return node;
  if (isDocument(node)) return node.defaultView || false;
  return false;
}
function getscrollAccessor(offset2) {
  var prop = offset2 === "pageXOffset" ? "scrollLeft" : "scrollTop";
  function scrollAccessor(node, val) {
    var win = isWindow(node);
    if (val === void 0) {
      return win ? win[offset2] : node[prop];
    }
    if (win) {
      win.scrollTo(win[offset2], val);
    } else {
      node[prop] = val;
    }
  }
  return scrollAccessor;
}
const scrollLeft = getscrollAccessor("pageXOffset");
const scrollTop = getscrollAccessor("pageYOffset");
function offset(node) {
  var doc = ownerDocument$1(node);
  var box = {
    top: 0,
    left: 0,
    height: 0,
    width: 0
  };
  var docElem = doc && doc.documentElement;
  if (!docElem || !contains(docElem, node)) return box;
  if (node.getBoundingClientRect !== void 0) box = node.getBoundingClientRect();
  box = {
    top: box.top + scrollTop(docElem) - (docElem.clientTop || 0),
    left: box.left + scrollLeft(docElem) - (docElem.clientLeft || 0),
    width: box.width,
    height: box.height
  };
  return box;
}
var isHTMLElement = function isHTMLElement2(e2) {
  return !!e2 && "offsetParent" in e2;
};
function offsetParent(node) {
  var doc = ownerDocument$1(node);
  var parent = node && node.offsetParent;
  while (isHTMLElement(parent) && parent.nodeName !== "HTML" && style(parent, "position") === "static") {
    parent = parent.offsetParent;
  }
  return parent || doc.documentElement;
}
var nodeName = function nodeName2(node) {
  return node.nodeName && node.nodeName.toLowerCase();
};
function position(node, offsetParent$1) {
  var parentOffset = {
    top: 0,
    left: 0
  };
  var offset$12;
  if (style(node, "position") === "fixed") {
    offset$12 = node.getBoundingClientRect();
  } else {
    var parent = offsetParent$1 || offsetParent(node);
    offset$12 = offset(node);
    if (nodeName(parent) !== "html") parentOffset = offset(parent);
    var borderTop = String(style(parent, "borderTopWidth") || 0);
    parentOffset.top += parseInt(borderTop, 10) - scrollTop(parent) || 0;
    var borderLeft = String(style(parent, "borderLeftWidth") || 0);
    parentOffset.left += parseInt(borderLeft, 10) - scrollLeft(parent) || 0;
  }
  var marginTop = String(style(node, "marginTop") || 0);
  var marginLeft = String(style(node, "marginLeft") || 0);
  return _extends({}, offset$12, {
    top: offset$12.top - parentOffset.top - (parseInt(marginTop, 10) || 0),
    left: offset$12.left - parentOffset.left - (parseInt(marginLeft, 10) || 0)
  });
}
var prev = (/* @__PURE__ */ new Date()).getTime();
function fallback(fn3) {
  var curr = (/* @__PURE__ */ new Date()).getTime();
  var ms = Math.max(0, 16 - (curr - prev));
  var handle = setTimeout(fn3, ms);
  prev = curr;
  return handle;
}
var vendors = ["", "webkit", "moz", "o", "ms"];
var cancelMethod = "clearTimeout";
var rafImpl = fallback;
var getKey$1 = function getKey(vendor, k2) {
  return vendor + (!vendor ? k2 : k2[0].toUpperCase() + k2.substr(1)) + "AnimationFrame";
};
if (canUseDOM$1) {
  vendors.some(function(vendor) {
    var rafMethod = getKey$1(vendor, "request");
    if (rafMethod in window) {
      cancelMethod = getKey$1(vendor, "cancel");
      rafImpl = function rafImpl2(cb) {
        return window[rafMethod](cb);
      };
    }
    return !!rafImpl;
  });
}
var cancel = function cancel2(id2) {
  if (typeof window[cancelMethod] === "function") window[cancelMethod](id2);
};
var request = rafImpl;
var matchesImpl;
function matches(node, selector) {
  if (!matchesImpl) {
    var body = document.body;
    var nativeMatch = body.matches || body.matchesSelector || body.webkitMatchesSelector || body.mozMatchesSelector || body.msMatchesSelector;
    matchesImpl = function matchesImpl2(n, s3) {
      return nativeMatch.call(n, s3);
    };
  }
  return matchesImpl(node, selector);
}
function useSafeState(state) {
  const isMounted = useMounted();
  return [state[0], reactExports.useCallback((nextState) => {
    if (!isMounted()) return;
    return state[1](nextState);
  }, [isMounted, state[1]])];
}
var createPopper = popperGenerator({
  defaultModifiers: [hide$1, popperOffsets$1, computeStyles$1, eventListeners, offset$2, flip$1, preventOverflow$1, arrow$1]
});
var initialPopperStyles = function initialPopperStyles2(position2) {
  return {
    position: position2,
    top: "0",
    left: "0",
    opacity: "0",
    pointerEvents: "none"
  };
};
var disabledApplyStylesModifier = {
  name: "applyStyles",
  enabled: false
};
var ariaDescribedByModifier = {
  name: "ariaDescribedBy",
  enabled: true,
  phase: "afterWrite",
  effect: function effect2(_ref) {
    var state = _ref.state;
    return function() {
      var _state$elements = state.elements, reference2 = _state$elements.reference, popper2 = _state$elements.popper;
      if ("removeAttribute" in reference2) {
        var ids2 = (reference2.getAttribute("aria-describedby") || "").split(",").filter(function(id2) {
          return id2.trim() !== popper2.id;
        });
        if (!ids2.length) reference2.removeAttribute("aria-describedby");
        else reference2.setAttribute("aria-describedby", ids2.join(","));
      }
    };
  },
  fn: function fn2(_ref2) {
    var _popper$getAttribute;
    var state = _ref2.state;
    var _state$elements2 = state.elements, popper2 = _state$elements2.popper, reference2 = _state$elements2.reference;
    var role = (_popper$getAttribute = popper2.getAttribute("role")) == null ? void 0 : _popper$getAttribute.toLowerCase();
    if (popper2.id && role === "tooltip" && "setAttribute" in reference2) {
      var ids2 = reference2.getAttribute("aria-describedby");
      if (ids2 && ids2.split(",").indexOf(popper2.id) !== -1) {
        return;
      }
      reference2.setAttribute("aria-describedby", ids2 ? ids2 + "," + popper2.id : popper2.id);
    }
  }
};
var EMPTY_MODIFIERS = [];
function usePopper(referenceElement, popperElement, _temp) {
  var _ref3 = _temp === void 0 ? {} : _temp, _ref3$enabled = _ref3.enabled, enabled = _ref3$enabled === void 0 ? true : _ref3$enabled, _ref3$placement = _ref3.placement, placement = _ref3$placement === void 0 ? "bottom" : _ref3$placement, _ref3$strategy = _ref3.strategy, strategy = _ref3$strategy === void 0 ? "absolute" : _ref3$strategy, _ref3$modifiers = _ref3.modifiers, modifiers = _ref3$modifiers === void 0 ? EMPTY_MODIFIERS : _ref3$modifiers, config2 = _objectWithoutPropertiesLoose$7(_ref3, ["enabled", "placement", "strategy", "modifiers"]);
  var popperInstanceRef = reactExports.useRef();
  var update = reactExports.useCallback(function() {
    var _popperInstanceRef$cu;
    (_popperInstanceRef$cu = popperInstanceRef.current) == null ? void 0 : _popperInstanceRef$cu.update();
  }, []);
  var forceUpdate = reactExports.useCallback(function() {
    var _popperInstanceRef$cu2;
    (_popperInstanceRef$cu2 = popperInstanceRef.current) == null ? void 0 : _popperInstanceRef$cu2.forceUpdate();
  }, []);
  var _useSafeState = useSafeState(reactExports.useState({
    placement,
    update,
    forceUpdate,
    attributes: {},
    styles: {
      popper: initialPopperStyles(strategy),
      arrow: {}
    }
  })), popperState = _useSafeState[0], setState = _useSafeState[1];
  var updateModifier = reactExports.useMemo(function() {
    return {
      name: "updateStateModifier",
      enabled: true,
      phase: "write",
      requires: ["computeStyles"],
      fn: function fn3(_ref4) {
        var state = _ref4.state;
        var styles = {};
        var attributes = {};
        Object.keys(state.elements).forEach(function(element) {
          styles[element] = state.styles[element];
          attributes[element] = state.attributes[element];
        });
        setState({
          state,
          styles,
          attributes,
          update,
          forceUpdate,
          placement: state.placement
        });
      }
    };
  }, [update, forceUpdate, setState]);
  reactExports.useEffect(function() {
    if (!popperInstanceRef.current || !enabled) return;
    popperInstanceRef.current.setOptions({
      placement,
      strategy,
      modifiers: [].concat(modifiers, [updateModifier, disabledApplyStylesModifier])
    });
  }, [strategy, placement, updateModifier, enabled]);
  reactExports.useEffect(function() {
    if (!enabled || referenceElement == null || popperElement == null) {
      return void 0;
    }
    popperInstanceRef.current = createPopper(referenceElement, popperElement, _extends({}, config2, {
      placement,
      strategy,
      modifiers: [].concat(modifiers, [ariaDescribedByModifier, updateModifier])
    }));
    return function() {
      if (popperInstanceRef.current != null) {
        popperInstanceRef.current.destroy();
        popperInstanceRef.current = void 0;
        setState(function(s3) {
          return _extends({}, s3, {
            attributes: {},
            styles: {
              popper: initialPopperStyles(strategy)
            }
          });
        });
      }
    };
  }, [enabled, referenceElement, popperElement]);
  return popperState;
}
function safeFindDOMNode(componentOrElement) {
  if (componentOrElement && "setState" in componentOrElement) {
    return reactDomExports.findDOMNode(componentOrElement);
  }
  return componentOrElement != null ? componentOrElement : null;
}
const ownerDocument = (function(componentOrElement) {
  return ownerDocument$1(safeFindDOMNode(componentOrElement));
});
var escapeKeyCode = 27;
var noop = function noop3() {
};
function isLeftClickEvent(event) {
  return event.button === 0;
}
function isModifiedEvent(event) {
  return !!(event.metaKey || event.altKey || event.ctrlKey || event.shiftKey);
}
var getRefTarget = function getRefTarget2(ref) {
  return ref && ("current" in ref ? ref.current : ref);
};
function useRootClose(ref, onRootClose, _temp) {
  var _ref = _temp === void 0 ? {} : _temp, disabled = _ref.disabled, _ref$clickTrigger = _ref.clickTrigger, clickTrigger = _ref$clickTrigger === void 0 ? "click" : _ref$clickTrigger;
  var preventMouseRootCloseRef = reactExports.useRef(false);
  var onClose = onRootClose || noop;
  var handleMouseCapture = reactExports.useCallback(function(e2) {
    var _e$composedPath$;
    var currentTarget = getRefTarget(ref);
    warningExports(!!currentTarget, "RootClose captured a close event but does not have a ref to compare it to. useRootClose(), should be passed a ref that resolves to a DOM node");
    preventMouseRootCloseRef.current = !currentTarget || isModifiedEvent(e2) || !isLeftClickEvent(e2) || !!contains(currentTarget, (_e$composedPath$ = e2.composedPath == null ? void 0 : e2.composedPath()[0]) != null ? _e$composedPath$ : e2.target);
  }, [ref]);
  var handleMouse = useEventCallback$1(function(e2) {
    if (!preventMouseRootCloseRef.current) {
      onClose(e2);
    }
  });
  var handleKeyUp = useEventCallback$1(function(e2) {
    if (e2.keyCode === escapeKeyCode) {
      onClose(e2);
    }
  });
  reactExports.useEffect(function() {
    if (disabled || ref == null) return void 0;
    var currentEvent = window.event;
    var doc = ownerDocument(getRefTarget(ref));
    var removeMouseCaptureListener = listen(doc, clickTrigger, handleMouseCapture, true);
    var removeMouseListener = listen(doc, clickTrigger, function(e2) {
      if (e2 === currentEvent) {
        currentEvent = void 0;
        return;
      }
      handleMouse(e2);
    });
    var removeKeyupListener = listen(doc, "keyup", function(e2) {
      if (e2 === currentEvent) {
        currentEvent = void 0;
        return;
      }
      handleKeyUp(e2);
    });
    var mobileSafariHackListeners = [];
    if ("ontouchstart" in doc.documentElement) {
      mobileSafariHackListeners = [].slice.call(doc.body.children).map(function(el) {
        return listen(el, "mousemove", noop);
      });
    }
    return function() {
      removeMouseCaptureListener();
      removeMouseListener();
      removeKeyupListener();
      mobileSafariHackListeners.forEach(function(remove2) {
        return remove2();
      });
    };
  }, [ref, disabled, clickTrigger, handleMouseCapture, handleMouse, handleKeyUp]);
}
function toModifierMap(modifiers) {
  var result = {};
  if (!Array.isArray(modifiers)) {
    return modifiers || result;
  }
  modifiers == null ? void 0 : modifiers.forEach(function(m3) {
    result[m3.name] = m3;
  });
  return result;
}
function toModifierArray(map) {
  if (map === void 0) {
    map = {};
  }
  if (Array.isArray(map)) return map;
  return Object.keys(map).map(function(k2) {
    map[k2].name = k2;
    return map[k2];
  });
}
function mergeOptionsWithPopperConfig(_ref) {
  var _modifiers$preventOve, _modifiers$preventOve2, _modifiers$offset, _modifiers$arrow;
  var enabled = _ref.enabled, enableEvents = _ref.enableEvents, placement = _ref.placement, flip2 = _ref.flip, offset2 = _ref.offset, fixed = _ref.fixed, containerPadding = _ref.containerPadding, arrowElement = _ref.arrowElement, _ref$popperConfig = _ref.popperConfig, popperConfig = _ref$popperConfig === void 0 ? {} : _ref$popperConfig;
  var modifiers = toModifierMap(popperConfig.modifiers);
  return _extends({}, popperConfig, {
    placement,
    enabled,
    strategy: fixed ? "fixed" : popperConfig.strategy,
    modifiers: toModifierArray(_extends({}, modifiers, {
      eventListeners: {
        enabled: enableEvents
      },
      preventOverflow: _extends({}, modifiers.preventOverflow, {
        options: containerPadding ? _extends({
          padding: containerPadding
        }, (_modifiers$preventOve = modifiers.preventOverflow) == null ? void 0 : _modifiers$preventOve.options) : (_modifiers$preventOve2 = modifiers.preventOverflow) == null ? void 0 : _modifiers$preventOve2.options
      }),
      offset: {
        options: _extends({
          offset: offset2
        }, (_modifiers$offset = modifiers.offset) == null ? void 0 : _modifiers$offset.options)
      },
      arrow: _extends({}, modifiers.arrow, {
        enabled: !!arrowElement,
        options: _extends({}, (_modifiers$arrow = modifiers.arrow) == null ? void 0 : _modifiers$arrow.options, {
          element: arrowElement
        })
      }),
      flip: _extends({
        enabled: !!flip2
      }, modifiers.flip)
    }))
  });
}
var resolveContainerRef = function resolveContainerRef2(ref) {
  var _ref;
  if (typeof document === "undefined") return null;
  if (ref == null) return ownerDocument$1().body;
  if (typeof ref === "function") ref = ref();
  if (ref && "current" in ref) ref = ref.current;
  if ((_ref = ref) != null && _ref.nodeType) return ref || null;
  return null;
};
function useWaitForDOMRef(ref, onResolved) {
  var _useState = reactExports.useState(function() {
    return resolveContainerRef(ref);
  }), resolvedRef = _useState[0], setRef = _useState[1];
  if (!resolvedRef) {
    var earlyRef = resolveContainerRef(ref);
    if (earlyRef) setRef(earlyRef);
  }
  reactExports.useEffect(function() {
  }, [onResolved, resolvedRef]);
  reactExports.useEffect(function() {
    var nextRef = resolveContainerRef(ref);
    if (nextRef !== resolvedRef) {
      setRef(nextRef);
    }
  }, [ref, resolvedRef]);
  return resolvedRef;
}
var Overlay = /* @__PURE__ */ reactExports.forwardRef(function(props, outerRef) {
  var flip2 = props.flip, offset2 = props.offset, placement = props.placement, _props$containerPaddi = props.containerPadding, containerPadding = _props$containerPaddi === void 0 ? 5 : _props$containerPaddi, _props$popperConfig = props.popperConfig, popperConfig = _props$popperConfig === void 0 ? {} : _props$popperConfig, Transition2 = props.transition;
  var _useCallbackRef = useCallbackRef(), rootElement = _useCallbackRef[0], attachRef = _useCallbackRef[1];
  var _useCallbackRef2 = useCallbackRef(), arrowElement = _useCallbackRef2[0], attachArrowRef = _useCallbackRef2[1];
  var mergedRef = useMergedRefs$1(attachRef, outerRef);
  var container = useWaitForDOMRef(props.container);
  var target = useWaitForDOMRef(props.target);
  var _useState = reactExports.useState(!props.show), exited = _useState[0], setExited = _useState[1];
  var _usePopper = usePopper(target, rootElement, mergeOptionsWithPopperConfig({
    placement,
    enableEvents: !!props.show,
    containerPadding: containerPadding || 5,
    flip: flip2,
    offset: offset2,
    arrowElement,
    popperConfig
  })), styles = _usePopper.styles, attributes = _usePopper.attributes, popper2 = _objectWithoutPropertiesLoose$7(_usePopper, ["styles", "attributes"]);
  if (props.show) {
    if (exited) setExited(false);
  } else if (!props.transition && !exited) {
    setExited(true);
  }
  var handleHidden = function handleHidden2() {
    setExited(true);
    if (props.onExited) {
      props.onExited.apply(props, arguments);
    }
  };
  var mountOverlay = props.show || Transition2 && !exited;
  useRootClose(rootElement, props.onHide, {
    disabled: !props.rootClose || props.rootCloseDisabled,
    clickTrigger: props.rootCloseEvent
  });
  if (!mountOverlay) {
    return null;
  }
  var child = props.children(_extends({}, popper2, {
    show: !!props.show,
    props: _extends({}, attributes.popper, {
      style: styles.popper,
      ref: mergedRef
    }),
    arrowProps: _extends({}, attributes.arrow, {
      style: styles.arrow,
      ref: attachArrowRef
    })
  }));
  if (Transition2) {
    var onExit = props.onExit, onExiting = props.onExiting, onEnter = props.onEnter, onEntering = props.onEntering, onEntered = props.onEntered;
    child = /* @__PURE__ */ reactExports.createElement(Transition2, {
      "in": props.show,
      appear: true,
      onExit,
      onExiting,
      onExited: handleHidden,
      onEnter,
      onEntering,
      onEntered
    }, child);
  }
  return container ? /* @__PURE__ */ reactDomExports.createPortal(child, container) : null;
});
Overlay.displayName = "Overlay";
Overlay.propTypes = {
  /**
   * Set the visibility of the Overlay
   */
  show: propTypesExports.bool,
  /** Specify where the overlay element is positioned in relation to the target element */
  placement: propTypesExports.oneOf(placements),
  /**
   * A DOM Element, Ref to an element, or function that returns either. The `target` element is where
   * the overlay is positioned relative to.
   */
  target: propTypesExports.any,
  /**
   * A DOM Element, Ref to an element, or function that returns either. The `container` will have the Portal children
   * appended to it.
   */
  container: propTypesExports.any,
  /**
   * Enables the Popper.js `flip` modifier, allowing the Overlay to
   * automatically adjust it's placement in case of overlap with the viewport or toggle.
   * Refer to the [flip docs](https://popper.js.org/popper-documentation.html#modifiers..flip.enabled) for more info
   */
  flip: propTypesExports.bool,
  /**
   * A render prop that returns an element to overlay and position. See
   * the [react-popper documentation](https://github.com/FezVrasta/react-popper#children) for more info.
   *
   * @type {Function ({
   *   show: boolean,
   *   placement: Placement,
   *   update: () => void,
   *   forceUpdate: () => void,
   *   props: {
   *     ref: (?HTMLElement) => void,
   *     style: { [string]: string | number },
   *     aria-labelledby: ?string
   *     [string]: string | number,
   *   },
   *   arrowProps: {
   *     ref: (?HTMLElement) => void,
   *     style: { [string]: string | number },
   *     [string]: string | number,
   *   },
   * }) => React.Element}
   */
  children: propTypesExports.func.isRequired,
  /**
   * Control how much space there is between the edge of the boundary element and overlay.
   * A convenience shortcut to setting `popperConfig.modfiers.preventOverflow.padding`
   */
  containerPadding: propTypesExports.number,
  /**
   * A set of popper options and props passed directly to react-popper's Popper component.
   */
  popperConfig: propTypesExports.object,
  /**
   * Specify whether the overlay should trigger `onHide` when the user clicks outside the overlay
   */
  rootClose: propTypesExports.bool,
  /**
   * Specify event for toggling overlay
   */
  rootCloseEvent: propTypesExports.oneOf(["click", "mousedown"]),
  /**
   * Specify disabled for disable RootCloseWrapper
   */
  rootCloseDisabled: propTypesExports.bool,
  /**
   * A Callback fired by the Overlay when it wishes to be hidden.
   *
   * __required__ when `rootClose` is `true`.
   *
   * @type func
   */
  onHide: function onHide(props) {
    for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
      args[_key - 1] = arguments[_key];
    }
    if (props.rootClose) {
      var _PropTypes$func;
      return (_PropTypes$func = propTypesExports.func).isRequired.apply(_PropTypes$func, [props].concat(args));
    }
    return propTypesExports.func.apply(propTypesExports, [props].concat(args));
  },
  /**
   * A `react-transition-group@2.0.0` `<Transition/>` component
   * used to animate the overlay as it changes visibility.
   */
  // @ts-ignore
  transition: propTypesExports.elementType,
  /**
   * Callback fired before the Overlay transitions in
   */
  onEnter: propTypesExports.func,
  /**
   * Callback fired as the Overlay begins to transition in
   */
  onEntering: propTypesExports.func,
  /**
   * Callback fired after the Overlay finishes transitioning in
   */
  onEntered: propTypesExports.func,
  /**
   * Callback fired right before the Overlay transitions out
   */
  onExit: propTypesExports.func,
  /**
   * Callback fired as the Overlay begins to transition out
   */
  onExiting: propTypesExports.func,
  /**
   * Callback fired after the Overlay finishes transitioning out
   */
  onExited: propTypesExports.func
};
var _listCacheClear;
var hasRequired_listCacheClear;
function require_listCacheClear() {
  if (hasRequired_listCacheClear) return _listCacheClear;
  hasRequired_listCacheClear = 1;
  function listCacheClear() {
    this.__data__ = [];
    this.size = 0;
  }
  _listCacheClear = listCacheClear;
  return _listCacheClear;
}
var _assocIndexOf;
var hasRequired_assocIndexOf;
function require_assocIndexOf() {
  if (hasRequired_assocIndexOf) return _assocIndexOf;
  hasRequired_assocIndexOf = 1;
  var eq2 = requireEq();
  function assocIndexOf(array, key) {
    var length = array.length;
    while (length--) {
      if (eq2(array[length][0], key)) {
        return length;
      }
    }
    return -1;
  }
  _assocIndexOf = assocIndexOf;
  return _assocIndexOf;
}
var _listCacheDelete;
var hasRequired_listCacheDelete;
function require_listCacheDelete() {
  if (hasRequired_listCacheDelete) return _listCacheDelete;
  hasRequired_listCacheDelete = 1;
  var assocIndexOf = require_assocIndexOf();
  var arrayProto = Array.prototype;
  var splice = arrayProto.splice;
  function listCacheDelete(key) {
    var data = this.__data__, index2 = assocIndexOf(data, key);
    if (index2 < 0) {
      return false;
    }
    var lastIndex = data.length - 1;
    if (index2 == lastIndex) {
      data.pop();
    } else {
      splice.call(data, index2, 1);
    }
    --this.size;
    return true;
  }
  _listCacheDelete = listCacheDelete;
  return _listCacheDelete;
}
var _listCacheGet;
var hasRequired_listCacheGet;
function require_listCacheGet() {
  if (hasRequired_listCacheGet) return _listCacheGet;
  hasRequired_listCacheGet = 1;
  var assocIndexOf = require_assocIndexOf();
  function listCacheGet(key) {
    var data = this.__data__, index2 = assocIndexOf(data, key);
    return index2 < 0 ? void 0 : data[index2][1];
  }
  _listCacheGet = listCacheGet;
  return _listCacheGet;
}
var _listCacheHas;
var hasRequired_listCacheHas;
function require_listCacheHas() {
  if (hasRequired_listCacheHas) return _listCacheHas;
  hasRequired_listCacheHas = 1;
  var assocIndexOf = require_assocIndexOf();
  function listCacheHas(key) {
    return assocIndexOf(this.__data__, key) > -1;
  }
  _listCacheHas = listCacheHas;
  return _listCacheHas;
}
var _listCacheSet;
var hasRequired_listCacheSet;
function require_listCacheSet() {
  if (hasRequired_listCacheSet) return _listCacheSet;
  hasRequired_listCacheSet = 1;
  var assocIndexOf = require_assocIndexOf();
  function listCacheSet(key, value) {
    var data = this.__data__, index2 = assocIndexOf(data, key);
    if (index2 < 0) {
      ++this.size;
      data.push([key, value]);
    } else {
      data[index2][1] = value;
    }
    return this;
  }
  _listCacheSet = listCacheSet;
  return _listCacheSet;
}
var _ListCache;
var hasRequired_ListCache;
function require_ListCache() {
  if (hasRequired_ListCache) return _ListCache;
  hasRequired_ListCache = 1;
  var listCacheClear = require_listCacheClear(), listCacheDelete = require_listCacheDelete(), listCacheGet = require_listCacheGet(), listCacheHas = require_listCacheHas(), listCacheSet = require_listCacheSet();
  function ListCache(entries) {
    var index2 = -1, length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index2 < length) {
      var entry = entries[index2];
      this.set(entry[0], entry[1]);
    }
  }
  ListCache.prototype.clear = listCacheClear;
  ListCache.prototype["delete"] = listCacheDelete;
  ListCache.prototype.get = listCacheGet;
  ListCache.prototype.has = listCacheHas;
  ListCache.prototype.set = listCacheSet;
  _ListCache = ListCache;
  return _ListCache;
}
var _stackClear;
var hasRequired_stackClear;
function require_stackClear() {
  if (hasRequired_stackClear) return _stackClear;
  hasRequired_stackClear = 1;
  var ListCache = require_ListCache();
  function stackClear() {
    this.__data__ = new ListCache();
    this.size = 0;
  }
  _stackClear = stackClear;
  return _stackClear;
}
var _stackDelete;
var hasRequired_stackDelete;
function require_stackDelete() {
  if (hasRequired_stackDelete) return _stackDelete;
  hasRequired_stackDelete = 1;
  function stackDelete(key) {
    var data = this.__data__, result = data["delete"](key);
    this.size = data.size;
    return result;
  }
  _stackDelete = stackDelete;
  return _stackDelete;
}
var _stackGet;
var hasRequired_stackGet;
function require_stackGet() {
  if (hasRequired_stackGet) return _stackGet;
  hasRequired_stackGet = 1;
  function stackGet(key) {
    return this.__data__.get(key);
  }
  _stackGet = stackGet;
  return _stackGet;
}
var _stackHas;
var hasRequired_stackHas;
function require_stackHas() {
  if (hasRequired_stackHas) return _stackHas;
  hasRequired_stackHas = 1;
  function stackHas(key) {
    return this.__data__.has(key);
  }
  _stackHas = stackHas;
  return _stackHas;
}
var _coreJsData;
var hasRequired_coreJsData;
function require_coreJsData() {
  if (hasRequired_coreJsData) return _coreJsData;
  hasRequired_coreJsData = 1;
  var root = require_root();
  var coreJsData = root["__core-js_shared__"];
  _coreJsData = coreJsData;
  return _coreJsData;
}
var _isMasked;
var hasRequired_isMasked;
function require_isMasked() {
  if (hasRequired_isMasked) return _isMasked;
  hasRequired_isMasked = 1;
  var coreJsData = require_coreJsData();
  var maskSrcKey = (function() {
    var uid = /[^.]+$/.exec(coreJsData && coreJsData.keys && coreJsData.keys.IE_PROTO || "");
    return uid ? "Symbol(src)_1." + uid : "";
  })();
  function isMasked(func) {
    return !!maskSrcKey && maskSrcKey in func;
  }
  _isMasked = isMasked;
  return _isMasked;
}
var _toSource;
var hasRequired_toSource;
function require_toSource() {
  if (hasRequired_toSource) return _toSource;
  hasRequired_toSource = 1;
  var funcProto = Function.prototype;
  var funcToString = funcProto.toString;
  function toSource(func) {
    if (func != null) {
      try {
        return funcToString.call(func);
      } catch (e2) {
      }
      try {
        return func + "";
      } catch (e2) {
      }
    }
    return "";
  }
  _toSource = toSource;
  return _toSource;
}
var _baseIsNative;
var hasRequired_baseIsNative;
function require_baseIsNative() {
  if (hasRequired_baseIsNative) return _baseIsNative;
  hasRequired_baseIsNative = 1;
  var isFunction2 = requireIsFunction(), isMasked = require_isMasked(), isObject2 = requireIsObject(), toSource = require_toSource();
  var reRegExpChar = /[\\^$.*+?()[\]{}|]/g;
  var reIsHostCtor = /^\[object .+?Constructor\]$/;
  var funcProto = Function.prototype, objectProto = Object.prototype;
  var funcToString = funcProto.toString;
  var hasOwnProperty = objectProto.hasOwnProperty;
  var reIsNative = RegExp(
    "^" + funcToString.call(hasOwnProperty).replace(reRegExpChar, "\\$&").replace(/hasOwnProperty|(function).*?(?=\\\()| for .+?(?=\\\])/g, "$1.*?") + "$"
  );
  function baseIsNative(value) {
    if (!isObject2(value) || isMasked(value)) {
      return false;
    }
    var pattern = isFunction2(value) ? reIsNative : reIsHostCtor;
    return pattern.test(toSource(value));
  }
  _baseIsNative = baseIsNative;
  return _baseIsNative;
}
var _getValue;
var hasRequired_getValue;
function require_getValue() {
  if (hasRequired_getValue) return _getValue;
  hasRequired_getValue = 1;
  function getValue(object, key) {
    return object == null ? void 0 : object[key];
  }
  _getValue = getValue;
  return _getValue;
}
var _getNative;
var hasRequired_getNative;
function require_getNative() {
  if (hasRequired_getNative) return _getNative;
  hasRequired_getNative = 1;
  var baseIsNative = require_baseIsNative(), getValue = require_getValue();
  function getNative(object, key) {
    var value = getValue(object, key);
    return baseIsNative(value) ? value : void 0;
  }
  _getNative = getNative;
  return _getNative;
}
var _Map;
var hasRequired_Map;
function require_Map() {
  if (hasRequired_Map) return _Map;
  hasRequired_Map = 1;
  var getNative = require_getNative(), root = require_root();
  var Map2 = getNative(root, "Map");
  _Map = Map2;
  return _Map;
}
var _nativeCreate;
var hasRequired_nativeCreate;
function require_nativeCreate() {
  if (hasRequired_nativeCreate) return _nativeCreate;
  hasRequired_nativeCreate = 1;
  var getNative = require_getNative();
  var nativeCreate = getNative(Object, "create");
  _nativeCreate = nativeCreate;
  return _nativeCreate;
}
var _hashClear;
var hasRequired_hashClear;
function require_hashClear() {
  if (hasRequired_hashClear) return _hashClear;
  hasRequired_hashClear = 1;
  var nativeCreate = require_nativeCreate();
  function hashClear() {
    this.__data__ = nativeCreate ? nativeCreate(null) : {};
    this.size = 0;
  }
  _hashClear = hashClear;
  return _hashClear;
}
var _hashDelete;
var hasRequired_hashDelete;
function require_hashDelete() {
  if (hasRequired_hashDelete) return _hashDelete;
  hasRequired_hashDelete = 1;
  function hashDelete(key) {
    var result = this.has(key) && delete this.__data__[key];
    this.size -= result ? 1 : 0;
    return result;
  }
  _hashDelete = hashDelete;
  return _hashDelete;
}
var _hashGet;
var hasRequired_hashGet;
function require_hashGet() {
  if (hasRequired_hashGet) return _hashGet;
  hasRequired_hashGet = 1;
  var nativeCreate = require_nativeCreate();
  var HASH_UNDEFINED = "__lodash_hash_undefined__";
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function hashGet(key) {
    var data = this.__data__;
    if (nativeCreate) {
      var result = data[key];
      return result === HASH_UNDEFINED ? void 0 : result;
    }
    return hasOwnProperty.call(data, key) ? data[key] : void 0;
  }
  _hashGet = hashGet;
  return _hashGet;
}
var _hashHas;
var hasRequired_hashHas;
function require_hashHas() {
  if (hasRequired_hashHas) return _hashHas;
  hasRequired_hashHas = 1;
  var nativeCreate = require_nativeCreate();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function hashHas(key) {
    var data = this.__data__;
    return nativeCreate ? data[key] !== void 0 : hasOwnProperty.call(data, key);
  }
  _hashHas = hashHas;
  return _hashHas;
}
var _hashSet;
var hasRequired_hashSet;
function require_hashSet() {
  if (hasRequired_hashSet) return _hashSet;
  hasRequired_hashSet = 1;
  var nativeCreate = require_nativeCreate();
  var HASH_UNDEFINED = "__lodash_hash_undefined__";
  function hashSet(key, value) {
    var data = this.__data__;
    this.size += this.has(key) ? 0 : 1;
    data[key] = nativeCreate && value === void 0 ? HASH_UNDEFINED : value;
    return this;
  }
  _hashSet = hashSet;
  return _hashSet;
}
var _Hash;
var hasRequired_Hash;
function require_Hash() {
  if (hasRequired_Hash) return _Hash;
  hasRequired_Hash = 1;
  var hashClear = require_hashClear(), hashDelete = require_hashDelete(), hashGet = require_hashGet(), hashHas = require_hashHas(), hashSet = require_hashSet();
  function Hash(entries) {
    var index2 = -1, length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index2 < length) {
      var entry = entries[index2];
      this.set(entry[0], entry[1]);
    }
  }
  Hash.prototype.clear = hashClear;
  Hash.prototype["delete"] = hashDelete;
  Hash.prototype.get = hashGet;
  Hash.prototype.has = hashHas;
  Hash.prototype.set = hashSet;
  _Hash = Hash;
  return _Hash;
}
var _mapCacheClear;
var hasRequired_mapCacheClear;
function require_mapCacheClear() {
  if (hasRequired_mapCacheClear) return _mapCacheClear;
  hasRequired_mapCacheClear = 1;
  var Hash = require_Hash(), ListCache = require_ListCache(), Map2 = require_Map();
  function mapCacheClear() {
    this.size = 0;
    this.__data__ = {
      "hash": new Hash(),
      "map": new (Map2 || ListCache)(),
      "string": new Hash()
    };
  }
  _mapCacheClear = mapCacheClear;
  return _mapCacheClear;
}
var _isKeyable;
var hasRequired_isKeyable;
function require_isKeyable() {
  if (hasRequired_isKeyable) return _isKeyable;
  hasRequired_isKeyable = 1;
  function isKeyable(value) {
    var type = typeof value;
    return type == "string" || type == "number" || type == "symbol" || type == "boolean" ? value !== "__proto__" : value === null;
  }
  _isKeyable = isKeyable;
  return _isKeyable;
}
var _getMapData;
var hasRequired_getMapData;
function require_getMapData() {
  if (hasRequired_getMapData) return _getMapData;
  hasRequired_getMapData = 1;
  var isKeyable = require_isKeyable();
  function getMapData(map, key) {
    var data = map.__data__;
    return isKeyable(key) ? data[typeof key == "string" ? "string" : "hash"] : data.map;
  }
  _getMapData = getMapData;
  return _getMapData;
}
var _mapCacheDelete;
var hasRequired_mapCacheDelete;
function require_mapCacheDelete() {
  if (hasRequired_mapCacheDelete) return _mapCacheDelete;
  hasRequired_mapCacheDelete = 1;
  var getMapData = require_getMapData();
  function mapCacheDelete(key) {
    var result = getMapData(this, key)["delete"](key);
    this.size -= result ? 1 : 0;
    return result;
  }
  _mapCacheDelete = mapCacheDelete;
  return _mapCacheDelete;
}
var _mapCacheGet;
var hasRequired_mapCacheGet;
function require_mapCacheGet() {
  if (hasRequired_mapCacheGet) return _mapCacheGet;
  hasRequired_mapCacheGet = 1;
  var getMapData = require_getMapData();
  function mapCacheGet(key) {
    return getMapData(this, key).get(key);
  }
  _mapCacheGet = mapCacheGet;
  return _mapCacheGet;
}
var _mapCacheHas;
var hasRequired_mapCacheHas;
function require_mapCacheHas() {
  if (hasRequired_mapCacheHas) return _mapCacheHas;
  hasRequired_mapCacheHas = 1;
  var getMapData = require_getMapData();
  function mapCacheHas(key) {
    return getMapData(this, key).has(key);
  }
  _mapCacheHas = mapCacheHas;
  return _mapCacheHas;
}
var _mapCacheSet;
var hasRequired_mapCacheSet;
function require_mapCacheSet() {
  if (hasRequired_mapCacheSet) return _mapCacheSet;
  hasRequired_mapCacheSet = 1;
  var getMapData = require_getMapData();
  function mapCacheSet(key, value) {
    var data = getMapData(this, key), size2 = data.size;
    data.set(key, value);
    this.size += data.size == size2 ? 0 : 1;
    return this;
  }
  _mapCacheSet = mapCacheSet;
  return _mapCacheSet;
}
var _MapCache;
var hasRequired_MapCache;
function require_MapCache() {
  if (hasRequired_MapCache) return _MapCache;
  hasRequired_MapCache = 1;
  var mapCacheClear = require_mapCacheClear(), mapCacheDelete = require_mapCacheDelete(), mapCacheGet = require_mapCacheGet(), mapCacheHas = require_mapCacheHas(), mapCacheSet = require_mapCacheSet();
  function MapCache(entries) {
    var index2 = -1, length = entries == null ? 0 : entries.length;
    this.clear();
    while (++index2 < length) {
      var entry = entries[index2];
      this.set(entry[0], entry[1]);
    }
  }
  MapCache.prototype.clear = mapCacheClear;
  MapCache.prototype["delete"] = mapCacheDelete;
  MapCache.prototype.get = mapCacheGet;
  MapCache.prototype.has = mapCacheHas;
  MapCache.prototype.set = mapCacheSet;
  _MapCache = MapCache;
  return _MapCache;
}
var _stackSet;
var hasRequired_stackSet;
function require_stackSet() {
  if (hasRequired_stackSet) return _stackSet;
  hasRequired_stackSet = 1;
  var ListCache = require_ListCache(), Map2 = require_Map(), MapCache = require_MapCache();
  var LARGE_ARRAY_SIZE = 200;
  function stackSet(key, value) {
    var data = this.__data__;
    if (data instanceof ListCache) {
      var pairs = data.__data__;
      if (!Map2 || pairs.length < LARGE_ARRAY_SIZE - 1) {
        pairs.push([key, value]);
        this.size = ++data.size;
        return this;
      }
      data = this.__data__ = new MapCache(pairs);
    }
    data.set(key, value);
    this.size = data.size;
    return this;
  }
  _stackSet = stackSet;
  return _stackSet;
}
var _Stack;
var hasRequired_Stack;
function require_Stack() {
  if (hasRequired_Stack) return _Stack;
  hasRequired_Stack = 1;
  var ListCache = require_ListCache(), stackClear = require_stackClear(), stackDelete = require_stackDelete(), stackGet = require_stackGet(), stackHas = require_stackHas(), stackSet = require_stackSet();
  function Stack(entries) {
    var data = this.__data__ = new ListCache(entries);
    this.size = data.size;
  }
  Stack.prototype.clear = stackClear;
  Stack.prototype["delete"] = stackDelete;
  Stack.prototype.get = stackGet;
  Stack.prototype.has = stackHas;
  Stack.prototype.set = stackSet;
  _Stack = Stack;
  return _Stack;
}
var _setCacheAdd;
var hasRequired_setCacheAdd;
function require_setCacheAdd() {
  if (hasRequired_setCacheAdd) return _setCacheAdd;
  hasRequired_setCacheAdd = 1;
  var HASH_UNDEFINED = "__lodash_hash_undefined__";
  function setCacheAdd(value) {
    this.__data__.set(value, HASH_UNDEFINED);
    return this;
  }
  _setCacheAdd = setCacheAdd;
  return _setCacheAdd;
}
var _setCacheHas;
var hasRequired_setCacheHas;
function require_setCacheHas() {
  if (hasRequired_setCacheHas) return _setCacheHas;
  hasRequired_setCacheHas = 1;
  function setCacheHas(value) {
    return this.__data__.has(value);
  }
  _setCacheHas = setCacheHas;
  return _setCacheHas;
}
var _SetCache;
var hasRequired_SetCache;
function require_SetCache() {
  if (hasRequired_SetCache) return _SetCache;
  hasRequired_SetCache = 1;
  var MapCache = require_MapCache(), setCacheAdd = require_setCacheAdd(), setCacheHas = require_setCacheHas();
  function SetCache(values) {
    var index2 = -1, length = values == null ? 0 : values.length;
    this.__data__ = new MapCache();
    while (++index2 < length) {
      this.add(values[index2]);
    }
  }
  SetCache.prototype.add = SetCache.prototype.push = setCacheAdd;
  SetCache.prototype.has = setCacheHas;
  _SetCache = SetCache;
  return _SetCache;
}
var _arraySome;
var hasRequired_arraySome;
function require_arraySome() {
  if (hasRequired_arraySome) return _arraySome;
  hasRequired_arraySome = 1;
  function arraySome(array, predicate) {
    var index2 = -1, length = array == null ? 0 : array.length;
    while (++index2 < length) {
      if (predicate(array[index2], index2, array)) {
        return true;
      }
    }
    return false;
  }
  _arraySome = arraySome;
  return _arraySome;
}
var _cacheHas;
var hasRequired_cacheHas;
function require_cacheHas() {
  if (hasRequired_cacheHas) return _cacheHas;
  hasRequired_cacheHas = 1;
  function cacheHas(cache, key) {
    return cache.has(key);
  }
  _cacheHas = cacheHas;
  return _cacheHas;
}
var _equalArrays;
var hasRequired_equalArrays;
function require_equalArrays() {
  if (hasRequired_equalArrays) return _equalArrays;
  hasRequired_equalArrays = 1;
  var SetCache = require_SetCache(), arraySome = require_arraySome(), cacheHas = require_cacheHas();
  var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
  function equalArrays(array, other, bitmask, customizer, equalFunc, stack) {
    var isPartial = bitmask & COMPARE_PARTIAL_FLAG, arrLength = array.length, othLength = other.length;
    if (arrLength != othLength && !(isPartial && othLength > arrLength)) {
      return false;
    }
    var arrStacked = stack.get(array);
    var othStacked = stack.get(other);
    if (arrStacked && othStacked) {
      return arrStacked == other && othStacked == array;
    }
    var index2 = -1, result = true, seen2 = bitmask & COMPARE_UNORDERED_FLAG ? new SetCache() : void 0;
    stack.set(array, other);
    stack.set(other, array);
    while (++index2 < arrLength) {
      var arrValue = array[index2], othValue = other[index2];
      if (customizer) {
        var compared = isPartial ? customizer(othValue, arrValue, index2, other, array, stack) : customizer(arrValue, othValue, index2, array, other, stack);
      }
      if (compared !== void 0) {
        if (compared) {
          continue;
        }
        result = false;
        break;
      }
      if (seen2) {
        if (!arraySome(other, function(othValue2, othIndex) {
          if (!cacheHas(seen2, othIndex) && (arrValue === othValue2 || equalFunc(arrValue, othValue2, bitmask, customizer, stack))) {
            return seen2.push(othIndex);
          }
        })) {
          result = false;
          break;
        }
      } else if (!(arrValue === othValue || equalFunc(arrValue, othValue, bitmask, customizer, stack))) {
        result = false;
        break;
      }
    }
    stack["delete"](array);
    stack["delete"](other);
    return result;
  }
  _equalArrays = equalArrays;
  return _equalArrays;
}
var _Uint8Array;
var hasRequired_Uint8Array;
function require_Uint8Array() {
  if (hasRequired_Uint8Array) return _Uint8Array;
  hasRequired_Uint8Array = 1;
  var root = require_root();
  var Uint8Array = root.Uint8Array;
  _Uint8Array = Uint8Array;
  return _Uint8Array;
}
var _mapToArray;
var hasRequired_mapToArray;
function require_mapToArray() {
  if (hasRequired_mapToArray) return _mapToArray;
  hasRequired_mapToArray = 1;
  function mapToArray(map) {
    var index2 = -1, result = Array(map.size);
    map.forEach(function(value, key) {
      result[++index2] = [key, value];
    });
    return result;
  }
  _mapToArray = mapToArray;
  return _mapToArray;
}
var _setToArray;
var hasRequired_setToArray;
function require_setToArray() {
  if (hasRequired_setToArray) return _setToArray;
  hasRequired_setToArray = 1;
  function setToArray(set) {
    var index2 = -1, result = Array(set.size);
    set.forEach(function(value) {
      result[++index2] = value;
    });
    return result;
  }
  _setToArray = setToArray;
  return _setToArray;
}
var _equalByTag;
var hasRequired_equalByTag;
function require_equalByTag() {
  if (hasRequired_equalByTag) return _equalByTag;
  hasRequired_equalByTag = 1;
  var Symbol2 = require_Symbol(), Uint8Array = require_Uint8Array(), eq2 = requireEq(), equalArrays = require_equalArrays(), mapToArray = require_mapToArray(), setToArray = require_setToArray();
  var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
  var boolTag = "[object Boolean]", dateTag = "[object Date]", errorTag = "[object Error]", mapTag = "[object Map]", numberTag = "[object Number]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]";
  var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]";
  var symbolProto = Symbol2 ? Symbol2.prototype : void 0, symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
  function equalByTag(object, other, tag, bitmask, customizer, equalFunc, stack) {
    switch (tag) {
      case dataViewTag:
        if (object.byteLength != other.byteLength || object.byteOffset != other.byteOffset) {
          return false;
        }
        object = object.buffer;
        other = other.buffer;
      case arrayBufferTag:
        if (object.byteLength != other.byteLength || !equalFunc(new Uint8Array(object), new Uint8Array(other))) {
          return false;
        }
        return true;
      case boolTag:
      case dateTag:
      case numberTag:
        return eq2(+object, +other);
      case errorTag:
        return object.name == other.name && object.message == other.message;
      case regexpTag:
      case stringTag:
        return object == other + "";
      case mapTag:
        var convert = mapToArray;
      case setTag:
        var isPartial = bitmask & COMPARE_PARTIAL_FLAG;
        convert || (convert = setToArray);
        if (object.size != other.size && !isPartial) {
          return false;
        }
        var stacked = stack.get(object);
        if (stacked) {
          return stacked == other;
        }
        bitmask |= COMPARE_UNORDERED_FLAG;
        stack.set(object, other);
        var result = equalArrays(convert(object), convert(other), bitmask, customizer, equalFunc, stack);
        stack["delete"](object);
        return result;
      case symbolTag:
        if (symbolValueOf) {
          return symbolValueOf.call(object) == symbolValueOf.call(other);
        }
    }
    return false;
  }
  _equalByTag = equalByTag;
  return _equalByTag;
}
var _arrayPush;
var hasRequired_arrayPush;
function require_arrayPush() {
  if (hasRequired_arrayPush) return _arrayPush;
  hasRequired_arrayPush = 1;
  function arrayPush(array, values) {
    var index2 = -1, length = values.length, offset2 = array.length;
    while (++index2 < length) {
      array[offset2 + index2] = values[index2];
    }
    return array;
  }
  _arrayPush = arrayPush;
  return _arrayPush;
}
var isArray_1;
var hasRequiredIsArray;
function requireIsArray() {
  if (hasRequiredIsArray) return isArray_1;
  hasRequiredIsArray = 1;
  var isArray = Array.isArray;
  isArray_1 = isArray;
  return isArray_1;
}
var _baseGetAllKeys;
var hasRequired_baseGetAllKeys;
function require_baseGetAllKeys() {
  if (hasRequired_baseGetAllKeys) return _baseGetAllKeys;
  hasRequired_baseGetAllKeys = 1;
  var arrayPush = require_arrayPush(), isArray = requireIsArray();
  function baseGetAllKeys(object, keysFunc, symbolsFunc) {
    var result = keysFunc(object);
    return isArray(object) ? result : arrayPush(result, symbolsFunc(object));
  }
  _baseGetAllKeys = baseGetAllKeys;
  return _baseGetAllKeys;
}
var _arrayFilter;
var hasRequired_arrayFilter;
function require_arrayFilter() {
  if (hasRequired_arrayFilter) return _arrayFilter;
  hasRequired_arrayFilter = 1;
  function arrayFilter(array, predicate) {
    var index2 = -1, length = array == null ? 0 : array.length, resIndex = 0, result = [];
    while (++index2 < length) {
      var value = array[index2];
      if (predicate(value, index2, array)) {
        result[resIndex++] = value;
      }
    }
    return result;
  }
  _arrayFilter = arrayFilter;
  return _arrayFilter;
}
var stubArray_1;
var hasRequiredStubArray;
function requireStubArray() {
  if (hasRequiredStubArray) return stubArray_1;
  hasRequiredStubArray = 1;
  function stubArray() {
    return [];
  }
  stubArray_1 = stubArray;
  return stubArray_1;
}
var _getSymbols;
var hasRequired_getSymbols;
function require_getSymbols() {
  if (hasRequired_getSymbols) return _getSymbols;
  hasRequired_getSymbols = 1;
  var arrayFilter = require_arrayFilter(), stubArray = requireStubArray();
  var objectProto = Object.prototype;
  var propertyIsEnumerable = objectProto.propertyIsEnumerable;
  var nativeGetSymbols = Object.getOwnPropertySymbols;
  var getSymbols = !nativeGetSymbols ? stubArray : function(object) {
    if (object == null) {
      return [];
    }
    object = Object(object);
    return arrayFilter(nativeGetSymbols(object), function(symbol) {
      return propertyIsEnumerable.call(object, symbol);
    });
  };
  _getSymbols = getSymbols;
  return _getSymbols;
}
var _baseTimes;
var hasRequired_baseTimes;
function require_baseTimes() {
  if (hasRequired_baseTimes) return _baseTimes;
  hasRequired_baseTimes = 1;
  function baseTimes(n, iteratee) {
    var index2 = -1, result = Array(n);
    while (++index2 < n) {
      result[index2] = iteratee(index2);
    }
    return result;
  }
  _baseTimes = baseTimes;
  return _baseTimes;
}
var _baseIsArguments;
var hasRequired_baseIsArguments;
function require_baseIsArguments() {
  if (hasRequired_baseIsArguments) return _baseIsArguments;
  hasRequired_baseIsArguments = 1;
  var baseGetTag = require_baseGetTag(), isObjectLike = requireIsObjectLike();
  var argsTag = "[object Arguments]";
  function baseIsArguments(value) {
    return isObjectLike(value) && baseGetTag(value) == argsTag;
  }
  _baseIsArguments = baseIsArguments;
  return _baseIsArguments;
}
var isArguments_1;
var hasRequiredIsArguments;
function requireIsArguments() {
  if (hasRequiredIsArguments) return isArguments_1;
  hasRequiredIsArguments = 1;
  var baseIsArguments = require_baseIsArguments(), isObjectLike = requireIsObjectLike();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  var propertyIsEnumerable = objectProto.propertyIsEnumerable;
  var isArguments = baseIsArguments(/* @__PURE__ */ (function() {
    return arguments;
  })()) ? baseIsArguments : function(value) {
    return isObjectLike(value) && hasOwnProperty.call(value, "callee") && !propertyIsEnumerable.call(value, "callee");
  };
  isArguments_1 = isArguments;
  return isArguments_1;
}
var isBuffer = { exports: {} };
var stubFalse_1;
var hasRequiredStubFalse;
function requireStubFalse() {
  if (hasRequiredStubFalse) return stubFalse_1;
  hasRequiredStubFalse = 1;
  function stubFalse() {
    return false;
  }
  stubFalse_1 = stubFalse;
  return stubFalse_1;
}
isBuffer.exports;
var hasRequiredIsBuffer;
function requireIsBuffer() {
  if (hasRequiredIsBuffer) return isBuffer.exports;
  hasRequiredIsBuffer = 1;
  (function(module, exports) {
    var root = require_root(), stubFalse = requireStubFalse();
    var freeExports = exports && !exports.nodeType && exports;
    var freeModule = freeExports && true && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0;
    var nativeIsBuffer = Buffer ? Buffer.isBuffer : void 0;
    var isBuffer2 = nativeIsBuffer || stubFalse;
    module.exports = isBuffer2;
  })(isBuffer, isBuffer.exports);
  return isBuffer.exports;
}
var _baseIsTypedArray;
var hasRequired_baseIsTypedArray;
function require_baseIsTypedArray() {
  if (hasRequired_baseIsTypedArray) return _baseIsTypedArray;
  hasRequired_baseIsTypedArray = 1;
  var baseGetTag = require_baseGetTag(), isLength = requireIsLength(), isObjectLike = requireIsObjectLike();
  var argsTag = "[object Arguments]", arrayTag = "[object Array]", boolTag = "[object Boolean]", dateTag = "[object Date]", errorTag = "[object Error]", funcTag = "[object Function]", mapTag = "[object Map]", numberTag = "[object Number]", objectTag = "[object Object]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", weakMapTag = "[object WeakMap]";
  var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
  var typedArrayTags = {};
  typedArrayTags[float32Tag] = typedArrayTags[float64Tag] = typedArrayTags[int8Tag] = typedArrayTags[int16Tag] = typedArrayTags[int32Tag] = typedArrayTags[uint8Tag] = typedArrayTags[uint8ClampedTag] = typedArrayTags[uint16Tag] = typedArrayTags[uint32Tag] = true;
  typedArrayTags[argsTag] = typedArrayTags[arrayTag] = typedArrayTags[arrayBufferTag] = typedArrayTags[boolTag] = typedArrayTags[dataViewTag] = typedArrayTags[dateTag] = typedArrayTags[errorTag] = typedArrayTags[funcTag] = typedArrayTags[mapTag] = typedArrayTags[numberTag] = typedArrayTags[objectTag] = typedArrayTags[regexpTag] = typedArrayTags[setTag] = typedArrayTags[stringTag] = typedArrayTags[weakMapTag] = false;
  function baseIsTypedArray(value) {
    return isObjectLike(value) && isLength(value.length) && !!typedArrayTags[baseGetTag(value)];
  }
  _baseIsTypedArray = baseIsTypedArray;
  return _baseIsTypedArray;
}
var _baseUnary;
var hasRequired_baseUnary;
function require_baseUnary() {
  if (hasRequired_baseUnary) return _baseUnary;
  hasRequired_baseUnary = 1;
  function baseUnary(func) {
    return function(value) {
      return func(value);
    };
  }
  _baseUnary = baseUnary;
  return _baseUnary;
}
var _nodeUtil = { exports: {} };
_nodeUtil.exports;
var hasRequired_nodeUtil;
function require_nodeUtil() {
  if (hasRequired_nodeUtil) return _nodeUtil.exports;
  hasRequired_nodeUtil = 1;
  (function(module, exports) {
    var freeGlobal = require_freeGlobal();
    var freeExports = exports && !exports.nodeType && exports;
    var freeModule = freeExports && true && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var freeProcess = moduleExports && freeGlobal.process;
    var nodeUtil = (function() {
      try {
        var types = freeModule && freeModule.require && freeModule.require("util").types;
        if (types) {
          return types;
        }
        return freeProcess && freeProcess.binding && freeProcess.binding("util");
      } catch (e2) {
      }
    })();
    module.exports = nodeUtil;
  })(_nodeUtil, _nodeUtil.exports);
  return _nodeUtil.exports;
}
var isTypedArray_1;
var hasRequiredIsTypedArray;
function requireIsTypedArray() {
  if (hasRequiredIsTypedArray) return isTypedArray_1;
  hasRequiredIsTypedArray = 1;
  var baseIsTypedArray = require_baseIsTypedArray(), baseUnary = require_baseUnary(), nodeUtil = require_nodeUtil();
  var nodeIsTypedArray = nodeUtil && nodeUtil.isTypedArray;
  var isTypedArray = nodeIsTypedArray ? baseUnary(nodeIsTypedArray) : baseIsTypedArray;
  isTypedArray_1 = isTypedArray;
  return isTypedArray_1;
}
var _arrayLikeKeys;
var hasRequired_arrayLikeKeys;
function require_arrayLikeKeys() {
  if (hasRequired_arrayLikeKeys) return _arrayLikeKeys;
  hasRequired_arrayLikeKeys = 1;
  var baseTimes = require_baseTimes(), isArguments = requireIsArguments(), isArray = requireIsArray(), isBuffer2 = requireIsBuffer(), isIndex = require_isIndex(), isTypedArray = requireIsTypedArray();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function arrayLikeKeys(value, inherited) {
    var isArr = isArray(value), isArg = !isArr && isArguments(value), isBuff = !isArr && !isArg && isBuffer2(value), isType = !isArr && !isArg && !isBuff && isTypedArray(value), skipIndexes = isArr || isArg || isBuff || isType, result = skipIndexes ? baseTimes(value.length, String) : [], length = result.length;
    for (var key in value) {
      if ((inherited || hasOwnProperty.call(value, key)) && !(skipIndexes && // Safari 9 has enumerable `arguments.length` in strict mode.
      (key == "length" || // Node.js 0.10 has enumerable non-index properties on buffers.
      isBuff && (key == "offset" || key == "parent") || // PhantomJS 2 has enumerable non-index properties on typed arrays.
      isType && (key == "buffer" || key == "byteLength" || key == "byteOffset") || // Skip index properties.
      isIndex(key, length)))) {
        result.push(key);
      }
    }
    return result;
  }
  _arrayLikeKeys = arrayLikeKeys;
  return _arrayLikeKeys;
}
var _isPrototype;
var hasRequired_isPrototype;
function require_isPrototype() {
  if (hasRequired_isPrototype) return _isPrototype;
  hasRequired_isPrototype = 1;
  var objectProto = Object.prototype;
  function isPrototype(value) {
    var Ctor = value && value.constructor, proto = typeof Ctor == "function" && Ctor.prototype || objectProto;
    return value === proto;
  }
  _isPrototype = isPrototype;
  return _isPrototype;
}
var _overArg;
var hasRequired_overArg;
function require_overArg() {
  if (hasRequired_overArg) return _overArg;
  hasRequired_overArg = 1;
  function overArg(func, transform) {
    return function(arg) {
      return func(transform(arg));
    };
  }
  _overArg = overArg;
  return _overArg;
}
var _nativeKeys;
var hasRequired_nativeKeys;
function require_nativeKeys() {
  if (hasRequired_nativeKeys) return _nativeKeys;
  hasRequired_nativeKeys = 1;
  var overArg = require_overArg();
  var nativeKeys = overArg(Object.keys, Object);
  _nativeKeys = nativeKeys;
  return _nativeKeys;
}
var _baseKeys;
var hasRequired_baseKeys;
function require_baseKeys() {
  if (hasRequired_baseKeys) return _baseKeys;
  hasRequired_baseKeys = 1;
  var isPrototype = require_isPrototype(), nativeKeys = require_nativeKeys();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function baseKeys(object) {
    if (!isPrototype(object)) {
      return nativeKeys(object);
    }
    var result = [];
    for (var key in Object(object)) {
      if (hasOwnProperty.call(object, key) && key != "constructor") {
        result.push(key);
      }
    }
    return result;
  }
  _baseKeys = baseKeys;
  return _baseKeys;
}
var keys_1;
var hasRequiredKeys;
function requireKeys() {
  if (hasRequiredKeys) return keys_1;
  hasRequiredKeys = 1;
  var arrayLikeKeys = require_arrayLikeKeys(), baseKeys = require_baseKeys(), isArrayLike = requireIsArrayLike();
  function keys(object) {
    return isArrayLike(object) ? arrayLikeKeys(object) : baseKeys(object);
  }
  keys_1 = keys;
  return keys_1;
}
var _getAllKeys;
var hasRequired_getAllKeys;
function require_getAllKeys() {
  if (hasRequired_getAllKeys) return _getAllKeys;
  hasRequired_getAllKeys = 1;
  var baseGetAllKeys = require_baseGetAllKeys(), getSymbols = require_getSymbols(), keys = requireKeys();
  function getAllKeys(object) {
    return baseGetAllKeys(object, keys, getSymbols);
  }
  _getAllKeys = getAllKeys;
  return _getAllKeys;
}
var _equalObjects;
var hasRequired_equalObjects;
function require_equalObjects() {
  if (hasRequired_equalObjects) return _equalObjects;
  hasRequired_equalObjects = 1;
  var getAllKeys = require_getAllKeys();
  var COMPARE_PARTIAL_FLAG = 1;
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function equalObjects(object, other, bitmask, customizer, equalFunc, stack) {
    var isPartial = bitmask & COMPARE_PARTIAL_FLAG, objProps = getAllKeys(object), objLength = objProps.length, othProps = getAllKeys(other), othLength = othProps.length;
    if (objLength != othLength && !isPartial) {
      return false;
    }
    var index2 = objLength;
    while (index2--) {
      var key = objProps[index2];
      if (!(isPartial ? key in other : hasOwnProperty.call(other, key))) {
        return false;
      }
    }
    var objStacked = stack.get(object);
    var othStacked = stack.get(other);
    if (objStacked && othStacked) {
      return objStacked == other && othStacked == object;
    }
    var result = true;
    stack.set(object, other);
    stack.set(other, object);
    var skipCtor = isPartial;
    while (++index2 < objLength) {
      key = objProps[index2];
      var objValue = object[key], othValue = other[key];
      if (customizer) {
        var compared = isPartial ? customizer(othValue, objValue, key, other, object, stack) : customizer(objValue, othValue, key, object, other, stack);
      }
      if (!(compared === void 0 ? objValue === othValue || equalFunc(objValue, othValue, bitmask, customizer, stack) : compared)) {
        result = false;
        break;
      }
      skipCtor || (skipCtor = key == "constructor");
    }
    if (result && !skipCtor) {
      var objCtor = object.constructor, othCtor = other.constructor;
      if (objCtor != othCtor && ("constructor" in object && "constructor" in other) && !(typeof objCtor == "function" && objCtor instanceof objCtor && typeof othCtor == "function" && othCtor instanceof othCtor)) {
        result = false;
      }
    }
    stack["delete"](object);
    stack["delete"](other);
    return result;
  }
  _equalObjects = equalObjects;
  return _equalObjects;
}
var _DataView;
var hasRequired_DataView;
function require_DataView() {
  if (hasRequired_DataView) return _DataView;
  hasRequired_DataView = 1;
  var getNative = require_getNative(), root = require_root();
  var DataView = getNative(root, "DataView");
  _DataView = DataView;
  return _DataView;
}
var _Promise;
var hasRequired_Promise;
function require_Promise() {
  if (hasRequired_Promise) return _Promise;
  hasRequired_Promise = 1;
  var getNative = require_getNative(), root = require_root();
  var Promise2 = getNative(root, "Promise");
  _Promise = Promise2;
  return _Promise;
}
var _Set;
var hasRequired_Set;
function require_Set() {
  if (hasRequired_Set) return _Set;
  hasRequired_Set = 1;
  var getNative = require_getNative(), root = require_root();
  var Set2 = getNative(root, "Set");
  _Set = Set2;
  return _Set;
}
var _WeakMap;
var hasRequired_WeakMap;
function require_WeakMap() {
  if (hasRequired_WeakMap) return _WeakMap;
  hasRequired_WeakMap = 1;
  var getNative = require_getNative(), root = require_root();
  var WeakMap2 = getNative(root, "WeakMap");
  _WeakMap = WeakMap2;
  return _WeakMap;
}
var _getTag;
var hasRequired_getTag;
function require_getTag() {
  if (hasRequired_getTag) return _getTag;
  hasRequired_getTag = 1;
  var DataView = require_DataView(), Map2 = require_Map(), Promise2 = require_Promise(), Set2 = require_Set(), WeakMap2 = require_WeakMap(), baseGetTag = require_baseGetTag(), toSource = require_toSource();
  var mapTag = "[object Map]", objectTag = "[object Object]", promiseTag = "[object Promise]", setTag = "[object Set]", weakMapTag = "[object WeakMap]";
  var dataViewTag = "[object DataView]";
  var dataViewCtorString = toSource(DataView), mapCtorString = toSource(Map2), promiseCtorString = toSource(Promise2), setCtorString = toSource(Set2), weakMapCtorString = toSource(WeakMap2);
  var getTag = baseGetTag;
  if (DataView && getTag(new DataView(new ArrayBuffer(1))) != dataViewTag || Map2 && getTag(new Map2()) != mapTag || Promise2 && getTag(Promise2.resolve()) != promiseTag || Set2 && getTag(new Set2()) != setTag || WeakMap2 && getTag(new WeakMap2()) != weakMapTag) {
    getTag = function(value) {
      var result = baseGetTag(value), Ctor = result == objectTag ? value.constructor : void 0, ctorString = Ctor ? toSource(Ctor) : "";
      if (ctorString) {
        switch (ctorString) {
          case dataViewCtorString:
            return dataViewTag;
          case mapCtorString:
            return mapTag;
          case promiseCtorString:
            return promiseTag;
          case setCtorString:
            return setTag;
          case weakMapCtorString:
            return weakMapTag;
        }
      }
      return result;
    };
  }
  _getTag = getTag;
  return _getTag;
}
var _baseIsEqualDeep;
var hasRequired_baseIsEqualDeep;
function require_baseIsEqualDeep() {
  if (hasRequired_baseIsEqualDeep) return _baseIsEqualDeep;
  hasRequired_baseIsEqualDeep = 1;
  var Stack = require_Stack(), equalArrays = require_equalArrays(), equalByTag = require_equalByTag(), equalObjects = require_equalObjects(), getTag = require_getTag(), isArray = requireIsArray(), isBuffer2 = requireIsBuffer(), isTypedArray = requireIsTypedArray();
  var COMPARE_PARTIAL_FLAG = 1;
  var argsTag = "[object Arguments]", arrayTag = "[object Array]", objectTag = "[object Object]";
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function baseIsEqualDeep(object, other, bitmask, customizer, equalFunc, stack) {
    var objIsArr = isArray(object), othIsArr = isArray(other), objTag = objIsArr ? arrayTag : getTag(object), othTag = othIsArr ? arrayTag : getTag(other);
    objTag = objTag == argsTag ? objectTag : objTag;
    othTag = othTag == argsTag ? objectTag : othTag;
    var objIsObj = objTag == objectTag, othIsObj = othTag == objectTag, isSameTag = objTag == othTag;
    if (isSameTag && isBuffer2(object)) {
      if (!isBuffer2(other)) {
        return false;
      }
      objIsArr = true;
      objIsObj = false;
    }
    if (isSameTag && !objIsObj) {
      stack || (stack = new Stack());
      return objIsArr || isTypedArray(object) ? equalArrays(object, other, bitmask, customizer, equalFunc, stack) : equalByTag(object, other, objTag, bitmask, customizer, equalFunc, stack);
    }
    if (!(bitmask & COMPARE_PARTIAL_FLAG)) {
      var objIsWrapped = objIsObj && hasOwnProperty.call(object, "__wrapped__"), othIsWrapped = othIsObj && hasOwnProperty.call(other, "__wrapped__");
      if (objIsWrapped || othIsWrapped) {
        var objUnwrapped = objIsWrapped ? object.value() : object, othUnwrapped = othIsWrapped ? other.value() : other;
        stack || (stack = new Stack());
        return equalFunc(objUnwrapped, othUnwrapped, bitmask, customizer, stack);
      }
    }
    if (!isSameTag) {
      return false;
    }
    stack || (stack = new Stack());
    return equalObjects(object, other, bitmask, customizer, equalFunc, stack);
  }
  _baseIsEqualDeep = baseIsEqualDeep;
  return _baseIsEqualDeep;
}
var _baseIsEqual;
var hasRequired_baseIsEqual;
function require_baseIsEqual() {
  if (hasRequired_baseIsEqual) return _baseIsEqual;
  hasRequired_baseIsEqual = 1;
  var baseIsEqualDeep = require_baseIsEqualDeep(), isObjectLike = requireIsObjectLike();
  function baseIsEqual(value, other, bitmask, customizer, stack) {
    if (value === other) {
      return true;
    }
    if (value == null || other == null || !isObjectLike(value) && !isObjectLike(other)) {
      return value !== value && other !== other;
    }
    return baseIsEqualDeep(value, other, bitmask, customizer, baseIsEqual, stack);
  }
  _baseIsEqual = baseIsEqual;
  return _baseIsEqual;
}
var isEqual_1;
var hasRequiredIsEqual;
function requireIsEqual() {
  if (hasRequiredIsEqual) return isEqual_1;
  hasRequiredIsEqual = 1;
  var baseIsEqual = require_baseIsEqual();
  function isEqual3(value, other) {
    return baseIsEqual(value, other);
  }
  isEqual_1 = isEqual3;
  return isEqual_1;
}
var isEqualExports = requireIsEqual();
function height(node, client2) {
  var win = isWindow(node);
  return win ? win.innerHeight : client2 ? node.clientHeight : offset(node).height;
}
function closest(node, selector, stopAt) {
  if (node.closest && !stopAt) node.closest(selector);
  var nextNode = node;
  do {
    if (matches(nextNode, selector)) return nextNode;
    nextNode = nextNode.parentElement;
  } while (nextNode && nextNode !== stopAt && nextNode.nodeType === document.ELEMENT_NODE);
  return null;
}
var _baseFindIndex;
var hasRequired_baseFindIndex;
function require_baseFindIndex() {
  if (hasRequired_baseFindIndex) return _baseFindIndex;
  hasRequired_baseFindIndex = 1;
  function baseFindIndex(array, predicate, fromIndex, fromRight) {
    var length = array.length, index2 = fromIndex + (fromRight ? 1 : -1);
    while (fromRight ? index2-- : ++index2 < length) {
      if (predicate(array[index2], index2, array)) {
        return index2;
      }
    }
    return -1;
  }
  _baseFindIndex = baseFindIndex;
  return _baseFindIndex;
}
var _baseIsMatch;
var hasRequired_baseIsMatch;
function require_baseIsMatch() {
  if (hasRequired_baseIsMatch) return _baseIsMatch;
  hasRequired_baseIsMatch = 1;
  var Stack = require_Stack(), baseIsEqual = require_baseIsEqual();
  var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
  function baseIsMatch(object, source, matchData, customizer) {
    var index2 = matchData.length, length = index2, noCustomizer = !customizer;
    if (object == null) {
      return !length;
    }
    object = Object(object);
    while (index2--) {
      var data = matchData[index2];
      if (noCustomizer && data[2] ? data[1] !== object[data[0]] : !(data[0] in object)) {
        return false;
      }
    }
    while (++index2 < length) {
      data = matchData[index2];
      var key = data[0], objValue = object[key], srcValue = data[1];
      if (noCustomizer && data[2]) {
        if (objValue === void 0 && !(key in object)) {
          return false;
        }
      } else {
        var stack = new Stack();
        if (customizer) {
          var result = customizer(objValue, srcValue, key, object, source, stack);
        }
        if (!(result === void 0 ? baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG, customizer, stack) : result)) {
          return false;
        }
      }
    }
    return true;
  }
  _baseIsMatch = baseIsMatch;
  return _baseIsMatch;
}
var _isStrictComparable;
var hasRequired_isStrictComparable;
function require_isStrictComparable() {
  if (hasRequired_isStrictComparable) return _isStrictComparable;
  hasRequired_isStrictComparable = 1;
  var isObject2 = requireIsObject();
  function isStrictComparable(value) {
    return value === value && !isObject2(value);
  }
  _isStrictComparable = isStrictComparable;
  return _isStrictComparable;
}
var _getMatchData;
var hasRequired_getMatchData;
function require_getMatchData() {
  if (hasRequired_getMatchData) return _getMatchData;
  hasRequired_getMatchData = 1;
  var isStrictComparable = require_isStrictComparable(), keys = requireKeys();
  function getMatchData(object) {
    var result = keys(object), length = result.length;
    while (length--) {
      var key = result[length], value = object[key];
      result[length] = [key, value, isStrictComparable(value)];
    }
    return result;
  }
  _getMatchData = getMatchData;
  return _getMatchData;
}
var _matchesStrictComparable;
var hasRequired_matchesStrictComparable;
function require_matchesStrictComparable() {
  if (hasRequired_matchesStrictComparable) return _matchesStrictComparable;
  hasRequired_matchesStrictComparable = 1;
  function matchesStrictComparable(key, srcValue) {
    return function(object) {
      if (object == null) {
        return false;
      }
      return object[key] === srcValue && (srcValue !== void 0 || key in Object(object));
    };
  }
  _matchesStrictComparable = matchesStrictComparable;
  return _matchesStrictComparable;
}
var _baseMatches;
var hasRequired_baseMatches;
function require_baseMatches() {
  if (hasRequired_baseMatches) return _baseMatches;
  hasRequired_baseMatches = 1;
  var baseIsMatch = require_baseIsMatch(), getMatchData = require_getMatchData(), matchesStrictComparable = require_matchesStrictComparable();
  function baseMatches(source) {
    var matchData = getMatchData(source);
    if (matchData.length == 1 && matchData[0][2]) {
      return matchesStrictComparable(matchData[0][0], matchData[0][1]);
    }
    return function(object) {
      return object === source || baseIsMatch(object, source, matchData);
    };
  }
  _baseMatches = baseMatches;
  return _baseMatches;
}
var _isKey;
var hasRequired_isKey;
function require_isKey() {
  if (hasRequired_isKey) return _isKey;
  hasRequired_isKey = 1;
  var isArray = requireIsArray(), isSymbol = requireIsSymbol();
  var reIsDeepProp = /\.|\[(?:[^[\]]*|(["'])(?:(?!\1)[^\\]|\\.)*?\1)\]/, reIsPlainProp = /^\w*$/;
  function isKey(value, object) {
    if (isArray(value)) {
      return false;
    }
    var type = typeof value;
    if (type == "number" || type == "symbol" || type == "boolean" || value == null || isSymbol(value)) {
      return true;
    }
    return reIsPlainProp.test(value) || !reIsDeepProp.test(value) || object != null && value in Object(object);
  }
  _isKey = isKey;
  return _isKey;
}
var memoize_1;
var hasRequiredMemoize;
function requireMemoize() {
  if (hasRequiredMemoize) return memoize_1;
  hasRequiredMemoize = 1;
  var MapCache = require_MapCache();
  var FUNC_ERROR_TEXT = "Expected a function";
  function memoize(func, resolver) {
    if (typeof func != "function" || resolver != null && typeof resolver != "function") {
      throw new TypeError(FUNC_ERROR_TEXT);
    }
    var memoized = function() {
      var args = arguments, key = resolver ? resolver.apply(this, args) : args[0], cache = memoized.cache;
      if (cache.has(key)) {
        return cache.get(key);
      }
      var result = func.apply(this, args);
      memoized.cache = cache.set(key, result) || cache;
      return result;
    };
    memoized.cache = new (memoize.Cache || MapCache)();
    return memoized;
  }
  memoize.Cache = MapCache;
  memoize_1 = memoize;
  return memoize_1;
}
var _memoizeCapped;
var hasRequired_memoizeCapped;
function require_memoizeCapped() {
  if (hasRequired_memoizeCapped) return _memoizeCapped;
  hasRequired_memoizeCapped = 1;
  var memoize = requireMemoize();
  var MAX_MEMOIZE_SIZE = 500;
  function memoizeCapped(func) {
    var result = memoize(func, function(key) {
      if (cache.size === MAX_MEMOIZE_SIZE) {
        cache.clear();
      }
      return key;
    });
    var cache = result.cache;
    return result;
  }
  _memoizeCapped = memoizeCapped;
  return _memoizeCapped;
}
var _stringToPath;
var hasRequired_stringToPath;
function require_stringToPath() {
  if (hasRequired_stringToPath) return _stringToPath;
  hasRequired_stringToPath = 1;
  var memoizeCapped = require_memoizeCapped();
  var rePropName = /[^.[\]]+|\[(?:(-?\d+(?:\.\d+)?)|(["'])((?:(?!\2)[^\\]|\\.)*?)\2)\]|(?=(?:\.|\[\])(?:\.|\[\]|$))/g;
  var reEscapeChar = /\\(\\)?/g;
  var stringToPath = memoizeCapped(function(string) {
    var result = [];
    if (string.charCodeAt(0) === 46) {
      result.push("");
    }
    string.replace(rePropName, function(match2, number, quote, subString) {
      result.push(quote ? subString.replace(reEscapeChar, "$1") : number || match2);
    });
    return result;
  });
  _stringToPath = stringToPath;
  return _stringToPath;
}
var _arrayMap;
var hasRequired_arrayMap;
function require_arrayMap() {
  if (hasRequired_arrayMap) return _arrayMap;
  hasRequired_arrayMap = 1;
  function arrayMap(array, iteratee) {
    var index2 = -1, length = array == null ? 0 : array.length, result = Array(length);
    while (++index2 < length) {
      result[index2] = iteratee(array[index2], index2, array);
    }
    return result;
  }
  _arrayMap = arrayMap;
  return _arrayMap;
}
var _baseToString;
var hasRequired_baseToString;
function require_baseToString() {
  if (hasRequired_baseToString) return _baseToString;
  hasRequired_baseToString = 1;
  var Symbol2 = require_Symbol(), arrayMap = require_arrayMap(), isArray = requireIsArray(), isSymbol = requireIsSymbol();
  var symbolProto = Symbol2 ? Symbol2.prototype : void 0, symbolToString = symbolProto ? symbolProto.toString : void 0;
  function baseToString(value) {
    if (typeof value == "string") {
      return value;
    }
    if (isArray(value)) {
      return arrayMap(value, baseToString) + "";
    }
    if (isSymbol(value)) {
      return symbolToString ? symbolToString.call(value) : "";
    }
    var result = value + "";
    return result == "0" && 1 / value == -Infinity ? "-0" : result;
  }
  _baseToString = baseToString;
  return _baseToString;
}
var toString_1;
var hasRequiredToString;
function requireToString() {
  if (hasRequiredToString) return toString_1;
  hasRequiredToString = 1;
  var baseToString = require_baseToString();
  function toString(value) {
    return value == null ? "" : baseToString(value);
  }
  toString_1 = toString;
  return toString_1;
}
var _castPath;
var hasRequired_castPath;
function require_castPath() {
  if (hasRequired_castPath) return _castPath;
  hasRequired_castPath = 1;
  var isArray = requireIsArray(), isKey = require_isKey(), stringToPath = require_stringToPath(), toString = requireToString();
  function castPath(value, object) {
    if (isArray(value)) {
      return value;
    }
    return isKey(value, object) ? [value] : stringToPath(toString(value));
  }
  _castPath = castPath;
  return _castPath;
}
var _toKey;
var hasRequired_toKey;
function require_toKey() {
  if (hasRequired_toKey) return _toKey;
  hasRequired_toKey = 1;
  var isSymbol = requireIsSymbol();
  function toKey(value) {
    if (typeof value == "string" || isSymbol(value)) {
      return value;
    }
    var result = value + "";
    return result == "0" && 1 / value == -Infinity ? "-0" : result;
  }
  _toKey = toKey;
  return _toKey;
}
var _baseGet;
var hasRequired_baseGet;
function require_baseGet() {
  if (hasRequired_baseGet) return _baseGet;
  hasRequired_baseGet = 1;
  var castPath = require_castPath(), toKey = require_toKey();
  function baseGet(object, path) {
    path = castPath(path, object);
    var index2 = 0, length = path.length;
    while (object != null && index2 < length) {
      object = object[toKey(path[index2++])];
    }
    return index2 && index2 == length ? object : void 0;
  }
  _baseGet = baseGet;
  return _baseGet;
}
var get_1;
var hasRequiredGet;
function requireGet() {
  if (hasRequiredGet) return get_1;
  hasRequiredGet = 1;
  var baseGet = require_baseGet();
  function get(object, path, defaultValue2) {
    var result = object == null ? void 0 : baseGet(object, path);
    return result === void 0 ? defaultValue2 : result;
  }
  get_1 = get;
  return get_1;
}
var _baseHasIn;
var hasRequired_baseHasIn;
function require_baseHasIn() {
  if (hasRequired_baseHasIn) return _baseHasIn;
  hasRequired_baseHasIn = 1;
  function baseHasIn(object, key) {
    return object != null && key in Object(object);
  }
  _baseHasIn = baseHasIn;
  return _baseHasIn;
}
var _hasPath;
var hasRequired_hasPath;
function require_hasPath() {
  if (hasRequired_hasPath) return _hasPath;
  hasRequired_hasPath = 1;
  var castPath = require_castPath(), isArguments = requireIsArguments(), isArray = requireIsArray(), isIndex = require_isIndex(), isLength = requireIsLength(), toKey = require_toKey();
  function hasPath(object, path, hasFunc) {
    path = castPath(path, object);
    var index2 = -1, length = path.length, result = false;
    while (++index2 < length) {
      var key = toKey(path[index2]);
      if (!(result = object != null && hasFunc(object, key))) {
        break;
      }
      object = object[key];
    }
    if (result || ++index2 != length) {
      return result;
    }
    length = object == null ? 0 : object.length;
    return !!length && isLength(length) && isIndex(key, length) && (isArray(object) || isArguments(object));
  }
  _hasPath = hasPath;
  return _hasPath;
}
var hasIn_1;
var hasRequiredHasIn;
function requireHasIn() {
  if (hasRequiredHasIn) return hasIn_1;
  hasRequiredHasIn = 1;
  var baseHasIn = require_baseHasIn(), hasPath = require_hasPath();
  function hasIn(object, path) {
    return object != null && hasPath(object, path, baseHasIn);
  }
  hasIn_1 = hasIn;
  return hasIn_1;
}
var _baseMatchesProperty;
var hasRequired_baseMatchesProperty;
function require_baseMatchesProperty() {
  if (hasRequired_baseMatchesProperty) return _baseMatchesProperty;
  hasRequired_baseMatchesProperty = 1;
  var baseIsEqual = require_baseIsEqual(), get = requireGet(), hasIn = requireHasIn(), isKey = require_isKey(), isStrictComparable = require_isStrictComparable(), matchesStrictComparable = require_matchesStrictComparable(), toKey = require_toKey();
  var COMPARE_PARTIAL_FLAG = 1, COMPARE_UNORDERED_FLAG = 2;
  function baseMatchesProperty(path, srcValue) {
    if (isKey(path) && isStrictComparable(srcValue)) {
      return matchesStrictComparable(toKey(path), srcValue);
    }
    return function(object) {
      var objValue = get(object, path);
      return objValue === void 0 && objValue === srcValue ? hasIn(object, path) : baseIsEqual(srcValue, objValue, COMPARE_PARTIAL_FLAG | COMPARE_UNORDERED_FLAG);
    };
  }
  _baseMatchesProperty = baseMatchesProperty;
  return _baseMatchesProperty;
}
var identity_1;
var hasRequiredIdentity;
function requireIdentity() {
  if (hasRequiredIdentity) return identity_1;
  hasRequiredIdentity = 1;
  function identity(value) {
    return value;
  }
  identity_1 = identity;
  return identity_1;
}
var _baseProperty;
var hasRequired_baseProperty;
function require_baseProperty() {
  if (hasRequired_baseProperty) return _baseProperty;
  hasRequired_baseProperty = 1;
  function baseProperty(key) {
    return function(object) {
      return object == null ? void 0 : object[key];
    };
  }
  _baseProperty = baseProperty;
  return _baseProperty;
}
var _basePropertyDeep;
var hasRequired_basePropertyDeep;
function require_basePropertyDeep() {
  if (hasRequired_basePropertyDeep) return _basePropertyDeep;
  hasRequired_basePropertyDeep = 1;
  var baseGet = require_baseGet();
  function basePropertyDeep(path) {
    return function(object) {
      return baseGet(object, path);
    };
  }
  _basePropertyDeep = basePropertyDeep;
  return _basePropertyDeep;
}
var property_1;
var hasRequiredProperty;
function requireProperty() {
  if (hasRequiredProperty) return property_1;
  hasRequiredProperty = 1;
  var baseProperty = require_baseProperty(), basePropertyDeep = require_basePropertyDeep(), isKey = require_isKey(), toKey = require_toKey();
  function property(path) {
    return isKey(path) ? baseProperty(toKey(path)) : basePropertyDeep(path);
  }
  property_1 = property;
  return property_1;
}
var _baseIteratee;
var hasRequired_baseIteratee;
function require_baseIteratee() {
  if (hasRequired_baseIteratee) return _baseIteratee;
  hasRequired_baseIteratee = 1;
  var baseMatches = require_baseMatches(), baseMatchesProperty = require_baseMatchesProperty(), identity = requireIdentity(), isArray = requireIsArray(), property = requireProperty();
  function baseIteratee(value) {
    if (typeof value == "function") {
      return value;
    }
    if (value == null) {
      return identity;
    }
    if (typeof value == "object") {
      return isArray(value) ? baseMatchesProperty(value[0], value[1]) : baseMatches(value);
    }
    return property(value);
  }
  _baseIteratee = baseIteratee;
  return _baseIteratee;
}
var findIndex_1;
var hasRequiredFindIndex;
function requireFindIndex() {
  if (hasRequiredFindIndex) return findIndex_1;
  hasRequiredFindIndex = 1;
  var baseFindIndex = require_baseFindIndex(), baseIteratee = require_baseIteratee(), toInteger2 = requireToInteger();
  var nativeMax = Math.max;
  function findIndex2(array, predicate, fromIndex) {
    var length = array == null ? 0 : array.length;
    if (!length) {
      return -1;
    }
    var index2 = fromIndex == null ? 0 : toInteger2(fromIndex);
    if (index2 < 0) {
      index2 = nativeMax(length + index2, 0);
    }
    return baseFindIndex(array, baseIteratee(predicate, 3), index2);
  }
  findIndex_1 = findIndex2;
  return findIndex_1;
}
var findIndexExports = requireFindIndex();
var _baseRange;
var hasRequired_baseRange;
function require_baseRange() {
  if (hasRequired_baseRange) return _baseRange;
  hasRequired_baseRange = 1;
  var nativeCeil = Math.ceil, nativeMax = Math.max;
  function baseRange(start2, end2, step, fromRight) {
    var index2 = -1, length = nativeMax(nativeCeil((end2 - start2) / (step || 1)), 0), result = Array(length);
    while (length--) {
      result[fromRight ? length : ++index2] = start2;
      start2 += step;
    }
    return result;
  }
  _baseRange = baseRange;
  return _baseRange;
}
var _createRange;
var hasRequired_createRange;
function require_createRange() {
  if (hasRequired_createRange) return _createRange;
  hasRequired_createRange = 1;
  var baseRange = require_baseRange(), isIterateeCall = require_isIterateeCall(), toFinite = requireToFinite();
  function createRange(fromRight) {
    return function(start2, end2, step) {
      if (step && typeof step != "number" && isIterateeCall(start2, end2, step)) {
        end2 = step = void 0;
      }
      start2 = toFinite(start2);
      if (end2 === void 0) {
        end2 = start2;
        start2 = 0;
      } else {
        end2 = toFinite(end2);
      }
      step = step === void 0 ? start2 < end2 ? 1 : -1 : toFinite(step);
      return baseRange(start2, end2, step, fromRight);
    };
  }
  _createRange = createRange;
  return _createRange;
}
var range_1;
var hasRequiredRange;
function requireRange() {
  if (hasRequiredRange) return range_1;
  hasRequiredRange = 1;
  var createRange = require_createRange();
  var range2 = createRange();
  range_1 = range2;
  return range_1;
}
var rangeExports = requireRange();
var safeIsNaN = Number.isNaN || function ponyfill(value) {
  return typeof value === "number" && value !== value;
};
function isEqual$1(first, second) {
  if (first === second) {
    return true;
  }
  if (safeIsNaN(first) && safeIsNaN(second)) {
    return true;
  }
  return false;
}
function areInputsEqual(newInputs, lastInputs) {
  if (newInputs.length !== lastInputs.length) {
    return false;
  }
  for (var i2 = 0; i2 < newInputs.length; i2++) {
    if (!isEqual$1(newInputs[i2], lastInputs[i2])) {
      return false;
    }
  }
  return true;
}
function memoizeOne(resultFn, isEqual3) {
  if (isEqual3 === void 0) {
    isEqual3 = areInputsEqual;
  }
  var cache = null;
  function memoized() {
    var newArgs = [];
    for (var _i = 0; _i < arguments.length; _i++) {
      newArgs[_i] = arguments[_i];
    }
    if (cache && cache.lastThis === this && isEqual3(newArgs, cache.lastArgs)) {
      return cache.lastResult;
    }
    var lastResult = resultFn.apply(this, newArgs);
    cache = {
      lastResult,
      lastArgs: newArgs,
      lastThis: this
    };
    return lastResult;
  }
  memoized.clear = function clear2() {
    cache = null;
  };
  return memoized;
}
function getWidth(node, client2) {
  var win = isWindow(node);
  return win ? win.innerWidth : client2 ? node.clientWidth : offset(node).width;
}
var _isFlattenable;
var hasRequired_isFlattenable;
function require_isFlattenable() {
  if (hasRequired_isFlattenable) return _isFlattenable;
  hasRequired_isFlattenable = 1;
  var Symbol2 = require_Symbol(), isArguments = requireIsArguments(), isArray = requireIsArray();
  var spreadableSymbol = Symbol2 ? Symbol2.isConcatSpreadable : void 0;
  function isFlattenable(value) {
    return isArray(value) || isArguments(value) || !!(spreadableSymbol && value && value[spreadableSymbol]);
  }
  _isFlattenable = isFlattenable;
  return _isFlattenable;
}
var _baseFlatten;
var hasRequired_baseFlatten;
function require_baseFlatten() {
  if (hasRequired_baseFlatten) return _baseFlatten;
  hasRequired_baseFlatten = 1;
  var arrayPush = require_arrayPush(), isFlattenable = require_isFlattenable();
  function baseFlatten(array, depth, predicate, isStrict, result) {
    var index2 = -1, length = array.length;
    predicate || (predicate = isFlattenable);
    result || (result = []);
    while (++index2 < length) {
      var value = array[index2];
      if (depth > 0 && predicate(value)) {
        if (depth > 1) {
          baseFlatten(value, depth - 1, predicate, isStrict, result);
        } else {
          arrayPush(result, value);
        }
      } else if (!isStrict) {
        result[result.length] = value;
      }
    }
    return result;
  }
  _baseFlatten = baseFlatten;
  return _baseFlatten;
}
var _createBaseFor;
var hasRequired_createBaseFor;
function require_createBaseFor() {
  if (hasRequired_createBaseFor) return _createBaseFor;
  hasRequired_createBaseFor = 1;
  function createBaseFor(fromRight) {
    return function(object, iteratee, keysFunc) {
      var index2 = -1, iterable = Object(object), props = keysFunc(object), length = props.length;
      while (length--) {
        var key = props[fromRight ? length : ++index2];
        if (iteratee(iterable[key], key, iterable) === false) {
          break;
        }
      }
      return object;
    };
  }
  _createBaseFor = createBaseFor;
  return _createBaseFor;
}
var _baseFor;
var hasRequired_baseFor;
function require_baseFor() {
  if (hasRequired_baseFor) return _baseFor;
  hasRequired_baseFor = 1;
  var createBaseFor = require_createBaseFor();
  var baseFor = createBaseFor();
  _baseFor = baseFor;
  return _baseFor;
}
var _baseForOwn;
var hasRequired_baseForOwn;
function require_baseForOwn() {
  if (hasRequired_baseForOwn) return _baseForOwn;
  hasRequired_baseForOwn = 1;
  var baseFor = require_baseFor(), keys = requireKeys();
  function baseForOwn(object, iteratee) {
    return object && baseFor(object, iteratee, keys);
  }
  _baseForOwn = baseForOwn;
  return _baseForOwn;
}
var _createBaseEach;
var hasRequired_createBaseEach;
function require_createBaseEach() {
  if (hasRequired_createBaseEach) return _createBaseEach;
  hasRequired_createBaseEach = 1;
  var isArrayLike = requireIsArrayLike();
  function createBaseEach(eachFunc, fromRight) {
    return function(collection, iteratee) {
      if (collection == null) {
        return collection;
      }
      if (!isArrayLike(collection)) {
        return eachFunc(collection, iteratee);
      }
      var length = collection.length, index2 = fromRight ? length : -1, iterable = Object(collection);
      while (fromRight ? index2-- : ++index2 < length) {
        if (iteratee(iterable[index2], index2, iterable) === false) {
          break;
        }
      }
      return collection;
    };
  }
  _createBaseEach = createBaseEach;
  return _createBaseEach;
}
var _baseEach;
var hasRequired_baseEach;
function require_baseEach() {
  if (hasRequired_baseEach) return _baseEach;
  hasRequired_baseEach = 1;
  var baseForOwn = require_baseForOwn(), createBaseEach = require_createBaseEach();
  var baseEach = createBaseEach(baseForOwn);
  _baseEach = baseEach;
  return _baseEach;
}
var _baseMap;
var hasRequired_baseMap;
function require_baseMap() {
  if (hasRequired_baseMap) return _baseMap;
  hasRequired_baseMap = 1;
  var baseEach = require_baseEach(), isArrayLike = requireIsArrayLike();
  function baseMap(collection, iteratee) {
    var index2 = -1, result = isArrayLike(collection) ? Array(collection.length) : [];
    baseEach(collection, function(value, key, collection2) {
      result[++index2] = iteratee(value, key, collection2);
    });
    return result;
  }
  _baseMap = baseMap;
  return _baseMap;
}
var _baseSortBy;
var hasRequired_baseSortBy;
function require_baseSortBy() {
  if (hasRequired_baseSortBy) return _baseSortBy;
  hasRequired_baseSortBy = 1;
  function baseSortBy(array, comparer) {
    var length = array.length;
    array.sort(comparer);
    while (length--) {
      array[length] = array[length].value;
    }
    return array;
  }
  _baseSortBy = baseSortBy;
  return _baseSortBy;
}
var _compareAscending;
var hasRequired_compareAscending;
function require_compareAscending() {
  if (hasRequired_compareAscending) return _compareAscending;
  hasRequired_compareAscending = 1;
  var isSymbol = requireIsSymbol();
  function compareAscending(value, other) {
    if (value !== other) {
      var valIsDefined = value !== void 0, valIsNull = value === null, valIsReflexive = value === value, valIsSymbol = isSymbol(value);
      var othIsDefined = other !== void 0, othIsNull = other === null, othIsReflexive = other === other, othIsSymbol = isSymbol(other);
      if (!othIsNull && !othIsSymbol && !valIsSymbol && value > other || valIsSymbol && othIsDefined && othIsReflexive && !othIsNull && !othIsSymbol || valIsNull && othIsDefined && othIsReflexive || !valIsDefined && othIsReflexive || !valIsReflexive) {
        return 1;
      }
      if (!valIsNull && !valIsSymbol && !othIsSymbol && value < other || othIsSymbol && valIsDefined && valIsReflexive && !valIsNull && !valIsSymbol || othIsNull && valIsDefined && valIsReflexive || !othIsDefined && valIsReflexive || !othIsReflexive) {
        return -1;
      }
    }
    return 0;
  }
  _compareAscending = compareAscending;
  return _compareAscending;
}
var _compareMultiple;
var hasRequired_compareMultiple;
function require_compareMultiple() {
  if (hasRequired_compareMultiple) return _compareMultiple;
  hasRequired_compareMultiple = 1;
  var compareAscending = require_compareAscending();
  function compareMultiple(object, other, orders) {
    var index2 = -1, objCriteria = object.criteria, othCriteria = other.criteria, length = objCriteria.length, ordersLength = orders.length;
    while (++index2 < length) {
      var result = compareAscending(objCriteria[index2], othCriteria[index2]);
      if (result) {
        if (index2 >= ordersLength) {
          return result;
        }
        var order2 = orders[index2];
        return result * (order2 == "desc" ? -1 : 1);
      }
    }
    return object.index - other.index;
  }
  _compareMultiple = compareMultiple;
  return _compareMultiple;
}
var _baseOrderBy;
var hasRequired_baseOrderBy;
function require_baseOrderBy() {
  if (hasRequired_baseOrderBy) return _baseOrderBy;
  hasRequired_baseOrderBy = 1;
  var arrayMap = require_arrayMap(), baseGet = require_baseGet(), baseIteratee = require_baseIteratee(), baseMap = require_baseMap(), baseSortBy = require_baseSortBy(), baseUnary = require_baseUnary(), compareMultiple = require_compareMultiple(), identity = requireIdentity(), isArray = requireIsArray();
  function baseOrderBy(collection, iteratees, orders) {
    if (iteratees.length) {
      iteratees = arrayMap(iteratees, function(iteratee) {
        if (isArray(iteratee)) {
          return function(value) {
            return baseGet(value, iteratee.length === 1 ? iteratee[0] : iteratee);
          };
        }
        return iteratee;
      });
    } else {
      iteratees = [identity];
    }
    var index2 = -1;
    iteratees = arrayMap(iteratees, baseUnary(baseIteratee));
    var result = baseMap(collection, function(value, key, collection2) {
      var criteria = arrayMap(iteratees, function(iteratee) {
        return iteratee(value);
      });
      return { "criteria": criteria, "index": ++index2, "value": value };
    });
    return baseSortBy(result, function(object, other) {
      return compareMultiple(object, other, orders);
    });
  }
  _baseOrderBy = baseOrderBy;
  return _baseOrderBy;
}
var _apply;
var hasRequired_apply;
function require_apply() {
  if (hasRequired_apply) return _apply;
  hasRequired_apply = 1;
  function apply(func, thisArg, args) {
    switch (args.length) {
      case 0:
        return func.call(thisArg);
      case 1:
        return func.call(thisArg, args[0]);
      case 2:
        return func.call(thisArg, args[0], args[1]);
      case 3:
        return func.call(thisArg, args[0], args[1], args[2]);
    }
    return func.apply(thisArg, args);
  }
  _apply = apply;
  return _apply;
}
var _overRest;
var hasRequired_overRest;
function require_overRest() {
  if (hasRequired_overRest) return _overRest;
  hasRequired_overRest = 1;
  var apply = require_apply();
  var nativeMax = Math.max;
  function overRest(func, start2, transform) {
    start2 = nativeMax(start2 === void 0 ? func.length - 1 : start2, 0);
    return function() {
      var args = arguments, index2 = -1, length = nativeMax(args.length - start2, 0), array = Array(length);
      while (++index2 < length) {
        array[index2] = args[start2 + index2];
      }
      index2 = -1;
      var otherArgs = Array(start2 + 1);
      while (++index2 < start2) {
        otherArgs[index2] = args[index2];
      }
      otherArgs[start2] = transform(array);
      return apply(func, this, otherArgs);
    };
  }
  _overRest = overRest;
  return _overRest;
}
var constant_1;
var hasRequiredConstant;
function requireConstant() {
  if (hasRequiredConstant) return constant_1;
  hasRequiredConstant = 1;
  function constant(value) {
    return function() {
      return value;
    };
  }
  constant_1 = constant;
  return constant_1;
}
var _defineProperty;
var hasRequired_defineProperty;
function require_defineProperty() {
  if (hasRequired_defineProperty) return _defineProperty;
  hasRequired_defineProperty = 1;
  var getNative = require_getNative();
  var defineProperty2 = (function() {
    try {
      var func = getNative(Object, "defineProperty");
      func({}, "", {});
      return func;
    } catch (e2) {
    }
  })();
  _defineProperty = defineProperty2;
  return _defineProperty;
}
var _baseSetToString;
var hasRequired_baseSetToString;
function require_baseSetToString() {
  if (hasRequired_baseSetToString) return _baseSetToString;
  hasRequired_baseSetToString = 1;
  var constant = requireConstant(), defineProperty2 = require_defineProperty(), identity = requireIdentity();
  var baseSetToString = !defineProperty2 ? identity : function(func, string) {
    return defineProperty2(func, "toString", {
      "configurable": true,
      "enumerable": false,
      "value": constant(string),
      "writable": true
    });
  };
  _baseSetToString = baseSetToString;
  return _baseSetToString;
}
var _shortOut;
var hasRequired_shortOut;
function require_shortOut() {
  if (hasRequired_shortOut) return _shortOut;
  hasRequired_shortOut = 1;
  var HOT_COUNT = 800, HOT_SPAN = 16;
  var nativeNow = Date.now;
  function shortOut(func) {
    var count = 0, lastCalled = 0;
    return function() {
      var stamp = nativeNow(), remaining = HOT_SPAN - (stamp - lastCalled);
      lastCalled = stamp;
      if (remaining > 0) {
        if (++count >= HOT_COUNT) {
          return arguments[0];
        }
      } else {
        count = 0;
      }
      return func.apply(void 0, arguments);
    };
  }
  _shortOut = shortOut;
  return _shortOut;
}
var _setToString;
var hasRequired_setToString;
function require_setToString() {
  if (hasRequired_setToString) return _setToString;
  hasRequired_setToString = 1;
  var baseSetToString = require_baseSetToString(), shortOut = require_shortOut();
  var setToString = shortOut(baseSetToString);
  _setToString = setToString;
  return _setToString;
}
var _baseRest;
var hasRequired_baseRest;
function require_baseRest() {
  if (hasRequired_baseRest) return _baseRest;
  hasRequired_baseRest = 1;
  var identity = requireIdentity(), overRest = require_overRest(), setToString = require_setToString();
  function baseRest(func, start2) {
    return setToString(overRest(func, start2, identity), func + "");
  }
  _baseRest = baseRest;
  return _baseRest;
}
var sortBy_1;
var hasRequiredSortBy;
function requireSortBy() {
  if (hasRequiredSortBy) return sortBy_1;
  hasRequiredSortBy = 1;
  var baseFlatten = require_baseFlatten(), baseOrderBy = require_baseOrderBy(), baseRest = require_baseRest(), isIterateeCall = require_isIterateeCall();
  var sortBy = baseRest(function(collection, iteratees) {
    if (collection == null) {
      return [];
    }
    var length = iteratees.length;
    if (length > 1 && isIterateeCall(collection, iteratees[0], iteratees[1])) {
      iteratees = [];
    } else if (length > 2 && isIterateeCall(iteratees[0], iteratees[1], iteratees[2])) {
      iteratees = [iteratees[0]];
    }
    return baseOrderBy(collection, baseFlatten(iteratees, 1), []);
  });
  sortBy_1 = sortBy;
  return sortBy_1;
}
var sortByExports = requireSortBy();
function _toArray(r2) {
  return _arrayWithHoles(r2) || _iterableToArray(r2) || _unsupportedIterableToArray(r2) || _nonIterableRest();
}
var _nativeKeysIn;
var hasRequired_nativeKeysIn;
function require_nativeKeysIn() {
  if (hasRequired_nativeKeysIn) return _nativeKeysIn;
  hasRequired_nativeKeysIn = 1;
  function nativeKeysIn(object) {
    var result = [];
    if (object != null) {
      for (var key in Object(object)) {
        result.push(key);
      }
    }
    return result;
  }
  _nativeKeysIn = nativeKeysIn;
  return _nativeKeysIn;
}
var _baseKeysIn;
var hasRequired_baseKeysIn;
function require_baseKeysIn() {
  if (hasRequired_baseKeysIn) return _baseKeysIn;
  hasRequired_baseKeysIn = 1;
  var isObject2 = requireIsObject(), isPrototype = require_isPrototype(), nativeKeysIn = require_nativeKeysIn();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function baseKeysIn(object) {
    if (!isObject2(object)) {
      return nativeKeysIn(object);
    }
    var isProto = isPrototype(object), result = [];
    for (var key in object) {
      if (!(key == "constructor" && (isProto || !hasOwnProperty.call(object, key)))) {
        result.push(key);
      }
    }
    return result;
  }
  _baseKeysIn = baseKeysIn;
  return _baseKeysIn;
}
var keysIn_1;
var hasRequiredKeysIn;
function requireKeysIn() {
  if (hasRequiredKeysIn) return keysIn_1;
  hasRequiredKeysIn = 1;
  var arrayLikeKeys = require_arrayLikeKeys(), baseKeysIn = require_baseKeysIn(), isArrayLike = requireIsArrayLike();
  function keysIn(object) {
    return isArrayLike(object) ? arrayLikeKeys(object, true) : baseKeysIn(object);
  }
  keysIn_1 = keysIn;
  return keysIn_1;
}
var defaults_1;
var hasRequiredDefaults;
function requireDefaults() {
  if (hasRequiredDefaults) return defaults_1;
  hasRequiredDefaults = 1;
  var baseRest = require_baseRest(), eq2 = requireEq(), isIterateeCall = require_isIterateeCall(), keysIn = requireKeysIn();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  var defaults = baseRest(function(object, sources) {
    object = Object(object);
    var index2 = -1;
    var length = sources.length;
    var guard = length > 2 ? sources[2] : void 0;
    if (guard && isIterateeCall(sources[0], sources[1], guard)) {
      length = 1;
    }
    while (++index2 < length) {
      var source = sources[index2];
      var props = keysIn(source);
      var propsIndex = -1;
      var propsLength = props.length;
      while (++propsIndex < propsLength) {
        var key = props[propsIndex];
        var value = object[key];
        if (value === void 0 || eq2(value, objectProto[key]) && !hasOwnProperty.call(object, key)) {
          object[key] = source[key];
        }
      }
    }
    return object;
  });
  defaults_1 = defaults;
  return defaults_1;
}
var defaultsExports = requireDefaults();
var _baseAssignValue;
var hasRequired_baseAssignValue;
function require_baseAssignValue() {
  if (hasRequired_baseAssignValue) return _baseAssignValue;
  hasRequired_baseAssignValue = 1;
  var defineProperty2 = require_defineProperty();
  function baseAssignValue(object, key, value) {
    if (key == "__proto__" && defineProperty2) {
      defineProperty2(object, key, {
        "configurable": true,
        "enumerable": true,
        "value": value,
        "writable": true
      });
    } else {
      object[key] = value;
    }
  }
  _baseAssignValue = baseAssignValue;
  return _baseAssignValue;
}
var mapValues_1;
var hasRequiredMapValues;
function requireMapValues() {
  if (hasRequiredMapValues) return mapValues_1;
  hasRequiredMapValues = 1;
  var baseAssignValue = require_baseAssignValue(), baseForOwn = require_baseForOwn(), baseIteratee = require_baseIteratee();
  function mapValues(object, iteratee) {
    var result = {};
    iteratee = baseIteratee(iteratee, 3);
    baseForOwn(object, function(value, key, object2) {
      baseAssignValue(result, key, iteratee(value, key, object2));
    });
    return result;
  }
  mapValues_1 = mapValues;
  return mapValues_1;
}
var mapValuesExports = requireMapValues();
var _arrayEach;
var hasRequired_arrayEach;
function require_arrayEach() {
  if (hasRequired_arrayEach) return _arrayEach;
  hasRequired_arrayEach = 1;
  function arrayEach(array, iteratee) {
    var index2 = -1, length = array == null ? 0 : array.length;
    while (++index2 < length) {
      if (iteratee(array[index2], index2, array) === false) {
        break;
      }
    }
    return array;
  }
  _arrayEach = arrayEach;
  return _arrayEach;
}
var _assignValue;
var hasRequired_assignValue;
function require_assignValue() {
  if (hasRequired_assignValue) return _assignValue;
  hasRequired_assignValue = 1;
  var baseAssignValue = require_baseAssignValue(), eq2 = requireEq();
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function assignValue(object, key, value) {
    var objValue = object[key];
    if (!(hasOwnProperty.call(object, key) && eq2(objValue, value)) || value === void 0 && !(key in object)) {
      baseAssignValue(object, key, value);
    }
  }
  _assignValue = assignValue;
  return _assignValue;
}
var _copyObject;
var hasRequired_copyObject;
function require_copyObject() {
  if (hasRequired_copyObject) return _copyObject;
  hasRequired_copyObject = 1;
  var assignValue = require_assignValue(), baseAssignValue = require_baseAssignValue();
  function copyObject(source, props, object, customizer) {
    var isNew = !object;
    object || (object = {});
    var index2 = -1, length = props.length;
    while (++index2 < length) {
      var key = props[index2];
      var newValue = customizer ? customizer(object[key], source[key], key, object, source) : void 0;
      if (newValue === void 0) {
        newValue = source[key];
      }
      if (isNew) {
        baseAssignValue(object, key, newValue);
      } else {
        assignValue(object, key, newValue);
      }
    }
    return object;
  }
  _copyObject = copyObject;
  return _copyObject;
}
var _baseAssign;
var hasRequired_baseAssign;
function require_baseAssign() {
  if (hasRequired_baseAssign) return _baseAssign;
  hasRequired_baseAssign = 1;
  var copyObject = require_copyObject(), keys = requireKeys();
  function baseAssign(object, source) {
    return object && copyObject(source, keys(source), object);
  }
  _baseAssign = baseAssign;
  return _baseAssign;
}
var _baseAssignIn;
var hasRequired_baseAssignIn;
function require_baseAssignIn() {
  if (hasRequired_baseAssignIn) return _baseAssignIn;
  hasRequired_baseAssignIn = 1;
  var copyObject = require_copyObject(), keysIn = requireKeysIn();
  function baseAssignIn(object, source) {
    return object && copyObject(source, keysIn(source), object);
  }
  _baseAssignIn = baseAssignIn;
  return _baseAssignIn;
}
var _cloneBuffer = { exports: {} };
_cloneBuffer.exports;
var hasRequired_cloneBuffer;
function require_cloneBuffer() {
  if (hasRequired_cloneBuffer) return _cloneBuffer.exports;
  hasRequired_cloneBuffer = 1;
  (function(module, exports) {
    var root = require_root();
    var freeExports = exports && !exports.nodeType && exports;
    var freeModule = freeExports && true && module && !module.nodeType && module;
    var moduleExports = freeModule && freeModule.exports === freeExports;
    var Buffer = moduleExports ? root.Buffer : void 0, allocUnsafe = Buffer ? Buffer.allocUnsafe : void 0;
    function cloneBuffer(buffer, isDeep) {
      if (isDeep) {
        return buffer.slice();
      }
      var length = buffer.length, result = allocUnsafe ? allocUnsafe(length) : new buffer.constructor(length);
      buffer.copy(result);
      return result;
    }
    module.exports = cloneBuffer;
  })(_cloneBuffer, _cloneBuffer.exports);
  return _cloneBuffer.exports;
}
var _copyArray;
var hasRequired_copyArray;
function require_copyArray() {
  if (hasRequired_copyArray) return _copyArray;
  hasRequired_copyArray = 1;
  function copyArray(source, array) {
    var index2 = -1, length = source.length;
    array || (array = Array(length));
    while (++index2 < length) {
      array[index2] = source[index2];
    }
    return array;
  }
  _copyArray = copyArray;
  return _copyArray;
}
var _copySymbols;
var hasRequired_copySymbols;
function require_copySymbols() {
  if (hasRequired_copySymbols) return _copySymbols;
  hasRequired_copySymbols = 1;
  var copyObject = require_copyObject(), getSymbols = require_getSymbols();
  function copySymbols(source, object) {
    return copyObject(source, getSymbols(source), object);
  }
  _copySymbols = copySymbols;
  return _copySymbols;
}
var _getPrototype;
var hasRequired_getPrototype;
function require_getPrototype() {
  if (hasRequired_getPrototype) return _getPrototype;
  hasRequired_getPrototype = 1;
  var overArg = require_overArg();
  var getPrototype = overArg(Object.getPrototypeOf, Object);
  _getPrototype = getPrototype;
  return _getPrototype;
}
var _getSymbolsIn;
var hasRequired_getSymbolsIn;
function require_getSymbolsIn() {
  if (hasRequired_getSymbolsIn) return _getSymbolsIn;
  hasRequired_getSymbolsIn = 1;
  var arrayPush = require_arrayPush(), getPrototype = require_getPrototype(), getSymbols = require_getSymbols(), stubArray = requireStubArray();
  var nativeGetSymbols = Object.getOwnPropertySymbols;
  var getSymbolsIn = !nativeGetSymbols ? stubArray : function(object) {
    var result = [];
    while (object) {
      arrayPush(result, getSymbols(object));
      object = getPrototype(object);
    }
    return result;
  };
  _getSymbolsIn = getSymbolsIn;
  return _getSymbolsIn;
}
var _copySymbolsIn;
var hasRequired_copySymbolsIn;
function require_copySymbolsIn() {
  if (hasRequired_copySymbolsIn) return _copySymbolsIn;
  hasRequired_copySymbolsIn = 1;
  var copyObject = require_copyObject(), getSymbolsIn = require_getSymbolsIn();
  function copySymbolsIn(source, object) {
    return copyObject(source, getSymbolsIn(source), object);
  }
  _copySymbolsIn = copySymbolsIn;
  return _copySymbolsIn;
}
var _getAllKeysIn;
var hasRequired_getAllKeysIn;
function require_getAllKeysIn() {
  if (hasRequired_getAllKeysIn) return _getAllKeysIn;
  hasRequired_getAllKeysIn = 1;
  var baseGetAllKeys = require_baseGetAllKeys(), getSymbolsIn = require_getSymbolsIn(), keysIn = requireKeysIn();
  function getAllKeysIn(object) {
    return baseGetAllKeys(object, keysIn, getSymbolsIn);
  }
  _getAllKeysIn = getAllKeysIn;
  return _getAllKeysIn;
}
var _initCloneArray;
var hasRequired_initCloneArray;
function require_initCloneArray() {
  if (hasRequired_initCloneArray) return _initCloneArray;
  hasRequired_initCloneArray = 1;
  var objectProto = Object.prototype;
  var hasOwnProperty = objectProto.hasOwnProperty;
  function initCloneArray(array) {
    var length = array.length, result = new array.constructor(length);
    if (length && typeof array[0] == "string" && hasOwnProperty.call(array, "index")) {
      result.index = array.index;
      result.input = array.input;
    }
    return result;
  }
  _initCloneArray = initCloneArray;
  return _initCloneArray;
}
var _cloneArrayBuffer;
var hasRequired_cloneArrayBuffer;
function require_cloneArrayBuffer() {
  if (hasRequired_cloneArrayBuffer) return _cloneArrayBuffer;
  hasRequired_cloneArrayBuffer = 1;
  var Uint8Array = require_Uint8Array();
  function cloneArrayBuffer(arrayBuffer) {
    var result = new arrayBuffer.constructor(arrayBuffer.byteLength);
    new Uint8Array(result).set(new Uint8Array(arrayBuffer));
    return result;
  }
  _cloneArrayBuffer = cloneArrayBuffer;
  return _cloneArrayBuffer;
}
var _cloneDataView;
var hasRequired_cloneDataView;
function require_cloneDataView() {
  if (hasRequired_cloneDataView) return _cloneDataView;
  hasRequired_cloneDataView = 1;
  var cloneArrayBuffer = require_cloneArrayBuffer();
  function cloneDataView(dataView, isDeep) {
    var buffer = isDeep ? cloneArrayBuffer(dataView.buffer) : dataView.buffer;
    return new dataView.constructor(buffer, dataView.byteOffset, dataView.byteLength);
  }
  _cloneDataView = cloneDataView;
  return _cloneDataView;
}
var _cloneRegExp;
var hasRequired_cloneRegExp;
function require_cloneRegExp() {
  if (hasRequired_cloneRegExp) return _cloneRegExp;
  hasRequired_cloneRegExp = 1;
  var reFlags = /\w*$/;
  function cloneRegExp(regexp) {
    var result = new regexp.constructor(regexp.source, reFlags.exec(regexp));
    result.lastIndex = regexp.lastIndex;
    return result;
  }
  _cloneRegExp = cloneRegExp;
  return _cloneRegExp;
}
var _cloneSymbol;
var hasRequired_cloneSymbol;
function require_cloneSymbol() {
  if (hasRequired_cloneSymbol) return _cloneSymbol;
  hasRequired_cloneSymbol = 1;
  var Symbol2 = require_Symbol();
  var symbolProto = Symbol2 ? Symbol2.prototype : void 0, symbolValueOf = symbolProto ? symbolProto.valueOf : void 0;
  function cloneSymbol(symbol) {
    return symbolValueOf ? Object(symbolValueOf.call(symbol)) : {};
  }
  _cloneSymbol = cloneSymbol;
  return _cloneSymbol;
}
var _cloneTypedArray;
var hasRequired_cloneTypedArray;
function require_cloneTypedArray() {
  if (hasRequired_cloneTypedArray) return _cloneTypedArray;
  hasRequired_cloneTypedArray = 1;
  var cloneArrayBuffer = require_cloneArrayBuffer();
  function cloneTypedArray(typedArray, isDeep) {
    var buffer = isDeep ? cloneArrayBuffer(typedArray.buffer) : typedArray.buffer;
    return new typedArray.constructor(buffer, typedArray.byteOffset, typedArray.length);
  }
  _cloneTypedArray = cloneTypedArray;
  return _cloneTypedArray;
}
var _initCloneByTag;
var hasRequired_initCloneByTag;
function require_initCloneByTag() {
  if (hasRequired_initCloneByTag) return _initCloneByTag;
  hasRequired_initCloneByTag = 1;
  var cloneArrayBuffer = require_cloneArrayBuffer(), cloneDataView = require_cloneDataView(), cloneRegExp = require_cloneRegExp(), cloneSymbol = require_cloneSymbol(), cloneTypedArray = require_cloneTypedArray();
  var boolTag = "[object Boolean]", dateTag = "[object Date]", mapTag = "[object Map]", numberTag = "[object Number]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]";
  var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
  function initCloneByTag(object, tag, isDeep) {
    var Ctor = object.constructor;
    switch (tag) {
      case arrayBufferTag:
        return cloneArrayBuffer(object);
      case boolTag:
      case dateTag:
        return new Ctor(+object);
      case dataViewTag:
        return cloneDataView(object, isDeep);
      case float32Tag:
      case float64Tag:
      case int8Tag:
      case int16Tag:
      case int32Tag:
      case uint8Tag:
      case uint8ClampedTag:
      case uint16Tag:
      case uint32Tag:
        return cloneTypedArray(object, isDeep);
      case mapTag:
        return new Ctor();
      case numberTag:
      case stringTag:
        return new Ctor(object);
      case regexpTag:
        return cloneRegExp(object);
      case setTag:
        return new Ctor();
      case symbolTag:
        return cloneSymbol(object);
    }
  }
  _initCloneByTag = initCloneByTag;
  return _initCloneByTag;
}
var _baseCreate;
var hasRequired_baseCreate;
function require_baseCreate() {
  if (hasRequired_baseCreate) return _baseCreate;
  hasRequired_baseCreate = 1;
  var isObject2 = requireIsObject();
  var objectCreate = Object.create;
  var baseCreate = /* @__PURE__ */ (function() {
    function object() {
    }
    return function(proto) {
      if (!isObject2(proto)) {
        return {};
      }
      if (objectCreate) {
        return objectCreate(proto);
      }
      object.prototype = proto;
      var result = new object();
      object.prototype = void 0;
      return result;
    };
  })();
  _baseCreate = baseCreate;
  return _baseCreate;
}
var _initCloneObject;
var hasRequired_initCloneObject;
function require_initCloneObject() {
  if (hasRequired_initCloneObject) return _initCloneObject;
  hasRequired_initCloneObject = 1;
  var baseCreate = require_baseCreate(), getPrototype = require_getPrototype(), isPrototype = require_isPrototype();
  function initCloneObject(object) {
    return typeof object.constructor == "function" && !isPrototype(object) ? baseCreate(getPrototype(object)) : {};
  }
  _initCloneObject = initCloneObject;
  return _initCloneObject;
}
var _baseIsMap;
var hasRequired_baseIsMap;
function require_baseIsMap() {
  if (hasRequired_baseIsMap) return _baseIsMap;
  hasRequired_baseIsMap = 1;
  var getTag = require_getTag(), isObjectLike = requireIsObjectLike();
  var mapTag = "[object Map]";
  function baseIsMap(value) {
    return isObjectLike(value) && getTag(value) == mapTag;
  }
  _baseIsMap = baseIsMap;
  return _baseIsMap;
}
var isMap_1;
var hasRequiredIsMap;
function requireIsMap() {
  if (hasRequiredIsMap) return isMap_1;
  hasRequiredIsMap = 1;
  var baseIsMap = require_baseIsMap(), baseUnary = require_baseUnary(), nodeUtil = require_nodeUtil();
  var nodeIsMap = nodeUtil && nodeUtil.isMap;
  var isMap = nodeIsMap ? baseUnary(nodeIsMap) : baseIsMap;
  isMap_1 = isMap;
  return isMap_1;
}
var _baseIsSet;
var hasRequired_baseIsSet;
function require_baseIsSet() {
  if (hasRequired_baseIsSet) return _baseIsSet;
  hasRequired_baseIsSet = 1;
  var getTag = require_getTag(), isObjectLike = requireIsObjectLike();
  var setTag = "[object Set]";
  function baseIsSet(value) {
    return isObjectLike(value) && getTag(value) == setTag;
  }
  _baseIsSet = baseIsSet;
  return _baseIsSet;
}
var isSet_1;
var hasRequiredIsSet;
function requireIsSet() {
  if (hasRequiredIsSet) return isSet_1;
  hasRequiredIsSet = 1;
  var baseIsSet = require_baseIsSet(), baseUnary = require_baseUnary(), nodeUtil = require_nodeUtil();
  var nodeIsSet = nodeUtil && nodeUtil.isSet;
  var isSet = nodeIsSet ? baseUnary(nodeIsSet) : baseIsSet;
  isSet_1 = isSet;
  return isSet_1;
}
var _baseClone;
var hasRequired_baseClone;
function require_baseClone() {
  if (hasRequired_baseClone) return _baseClone;
  hasRequired_baseClone = 1;
  var Stack = require_Stack(), arrayEach = require_arrayEach(), assignValue = require_assignValue(), baseAssign = require_baseAssign(), baseAssignIn = require_baseAssignIn(), cloneBuffer = require_cloneBuffer(), copyArray = require_copyArray(), copySymbols = require_copySymbols(), copySymbolsIn = require_copySymbolsIn(), getAllKeys = require_getAllKeys(), getAllKeysIn = require_getAllKeysIn(), getTag = require_getTag(), initCloneArray = require_initCloneArray(), initCloneByTag = require_initCloneByTag(), initCloneObject = require_initCloneObject(), isArray = requireIsArray(), isBuffer2 = requireIsBuffer(), isMap = requireIsMap(), isObject2 = requireIsObject(), isSet = requireIsSet(), keys = requireKeys(), keysIn = requireKeysIn();
  var CLONE_DEEP_FLAG = 1, CLONE_FLAT_FLAG = 2, CLONE_SYMBOLS_FLAG = 4;
  var argsTag = "[object Arguments]", arrayTag = "[object Array]", boolTag = "[object Boolean]", dateTag = "[object Date]", errorTag = "[object Error]", funcTag = "[object Function]", genTag = "[object GeneratorFunction]", mapTag = "[object Map]", numberTag = "[object Number]", objectTag = "[object Object]", regexpTag = "[object RegExp]", setTag = "[object Set]", stringTag = "[object String]", symbolTag = "[object Symbol]", weakMapTag = "[object WeakMap]";
  var arrayBufferTag = "[object ArrayBuffer]", dataViewTag = "[object DataView]", float32Tag = "[object Float32Array]", float64Tag = "[object Float64Array]", int8Tag = "[object Int8Array]", int16Tag = "[object Int16Array]", int32Tag = "[object Int32Array]", uint8Tag = "[object Uint8Array]", uint8ClampedTag = "[object Uint8ClampedArray]", uint16Tag = "[object Uint16Array]", uint32Tag = "[object Uint32Array]";
  var cloneableTags = {};
  cloneableTags[argsTag] = cloneableTags[arrayTag] = cloneableTags[arrayBufferTag] = cloneableTags[dataViewTag] = cloneableTags[boolTag] = cloneableTags[dateTag] = cloneableTags[float32Tag] = cloneableTags[float64Tag] = cloneableTags[int8Tag] = cloneableTags[int16Tag] = cloneableTags[int32Tag] = cloneableTags[mapTag] = cloneableTags[numberTag] = cloneableTags[objectTag] = cloneableTags[regexpTag] = cloneableTags[setTag] = cloneableTags[stringTag] = cloneableTags[symbolTag] = cloneableTags[uint8Tag] = cloneableTags[uint8ClampedTag] = cloneableTags[uint16Tag] = cloneableTags[uint32Tag] = true;
  cloneableTags[errorTag] = cloneableTags[funcTag] = cloneableTags[weakMapTag] = false;
  function baseClone(value, bitmask, customizer, key, object, stack) {
    var result, isDeep = bitmask & CLONE_DEEP_FLAG, isFlat = bitmask & CLONE_FLAT_FLAG, isFull = bitmask & CLONE_SYMBOLS_FLAG;
    if (customizer) {
      result = object ? customizer(value, key, object, stack) : customizer(value);
    }
    if (result !== void 0) {
      return result;
    }
    if (!isObject2(value)) {
      return value;
    }
    var isArr = isArray(value);
    if (isArr) {
      result = initCloneArray(value);
      if (!isDeep) {
        return copyArray(value, result);
      }
    } else {
      var tag = getTag(value), isFunc = tag == funcTag || tag == genTag;
      if (isBuffer2(value)) {
        return cloneBuffer(value, isDeep);
      }
      if (tag == objectTag || tag == argsTag || isFunc && !object) {
        result = isFlat || isFunc ? {} : initCloneObject(value);
        if (!isDeep) {
          return isFlat ? copySymbolsIn(value, baseAssignIn(result, value)) : copySymbols(value, baseAssign(result, value));
        }
      } else {
        if (!cloneableTags[tag]) {
          return object ? value : {};
        }
        result = initCloneByTag(value, tag, isDeep);
      }
    }
    stack || (stack = new Stack());
    var stacked = stack.get(value);
    if (stacked) {
      return stacked;
    }
    stack.set(value, result);
    if (isSet(value)) {
      value.forEach(function(subValue) {
        result.add(baseClone(subValue, bitmask, customizer, subValue, value, stack));
      });
    } else if (isMap(value)) {
      value.forEach(function(subValue, key2) {
        result.set(key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
      });
    }
    var keysFunc = isFull ? isFlat ? getAllKeysIn : getAllKeys : isFlat ? keysIn : keys;
    var props = isArr ? void 0 : keysFunc(value);
    arrayEach(props || value, function(subValue, key2) {
      if (props) {
        key2 = subValue;
        subValue = value[key2];
      }
      assignValue(result, key2, baseClone(subValue, bitmask, customizer, key2, value, stack));
    });
    return result;
  }
  _baseClone = baseClone;
  return _baseClone;
}
var last_1;
var hasRequiredLast;
function requireLast() {
  if (hasRequiredLast) return last_1;
  hasRequiredLast = 1;
  function last(array) {
    var length = array == null ? 0 : array.length;
    return length ? array[length - 1] : void 0;
  }
  last_1 = last;
  return last_1;
}
var _parent;
var hasRequired_parent;
function require_parent() {
  if (hasRequired_parent) return _parent;
  hasRequired_parent = 1;
  var baseGet = require_baseGet(), baseSlice = require_baseSlice();
  function parent(object, path) {
    return path.length < 2 ? object : baseGet(object, baseSlice(path, 0, -1));
  }
  _parent = parent;
  return _parent;
}
var _baseUnset;
var hasRequired_baseUnset;
function require_baseUnset() {
  if (hasRequired_baseUnset) return _baseUnset;
  hasRequired_baseUnset = 1;
  var castPath = require_castPath(), last = requireLast(), parent = require_parent(), toKey = require_toKey();
  function baseUnset(object, path) {
    path = castPath(path, object);
    object = parent(object, path);
    return object == null || delete object[toKey(last(path))];
  }
  _baseUnset = baseUnset;
  return _baseUnset;
}
var isPlainObject_1;
var hasRequiredIsPlainObject;
function requireIsPlainObject() {
  if (hasRequiredIsPlainObject) return isPlainObject_1;
  hasRequiredIsPlainObject = 1;
  var baseGetTag = require_baseGetTag(), getPrototype = require_getPrototype(), isObjectLike = requireIsObjectLike();
  var objectTag = "[object Object]";
  var funcProto = Function.prototype, objectProto = Object.prototype;
  var funcToString = funcProto.toString;
  var hasOwnProperty = objectProto.hasOwnProperty;
  var objectCtorString = funcToString.call(Object);
  function isPlainObject(value) {
    if (!isObjectLike(value) || baseGetTag(value) != objectTag) {
      return false;
    }
    var proto = getPrototype(value);
    if (proto === null) {
      return true;
    }
    var Ctor = hasOwnProperty.call(proto, "constructor") && proto.constructor;
    return typeof Ctor == "function" && Ctor instanceof Ctor && funcToString.call(Ctor) == objectCtorString;
  }
  isPlainObject_1 = isPlainObject;
  return isPlainObject_1;
}
var _customOmitClone;
var hasRequired_customOmitClone;
function require_customOmitClone() {
  if (hasRequired_customOmitClone) return _customOmitClone;
  hasRequired_customOmitClone = 1;
  var isPlainObject = requireIsPlainObject();
  function customOmitClone(value) {
    return isPlainObject(value) ? void 0 : value;
  }
  _customOmitClone = customOmitClone;
  return _customOmitClone;
}
var flatten_1;
var hasRequiredFlatten;
function requireFlatten() {
  if (hasRequiredFlatten) return flatten_1;
  hasRequiredFlatten = 1;
  var baseFlatten = require_baseFlatten();
  function flatten(array) {
    var length = array == null ? 0 : array.length;
    return length ? baseFlatten(array, 1) : [];
  }
  flatten_1 = flatten;
  return flatten_1;
}
var _flatRest;
var hasRequired_flatRest;
function require_flatRest() {
  if (hasRequired_flatRest) return _flatRest;
  hasRequired_flatRest = 1;
  var flatten = requireFlatten(), overRest = require_overRest(), setToString = require_setToString();
  function flatRest(func) {
    return setToString(overRest(func, void 0, flatten), func + "");
  }
  _flatRest = flatRest;
  return _flatRest;
}
var omit_1;
var hasRequiredOmit;
function requireOmit() {
  if (hasRequiredOmit) return omit_1;
  hasRequiredOmit = 1;
  var arrayMap = require_arrayMap(), baseClone = require_baseClone(), baseUnset = require_baseUnset(), castPath = require_castPath(), copyObject = require_copyObject(), customOmitClone = require_customOmitClone(), flatRest = require_flatRest(), getAllKeysIn = require_getAllKeysIn();
  var CLONE_DEEP_FLAG = 1, CLONE_FLAT_FLAG = 2, CLONE_SYMBOLS_FLAG = 4;
  var omit = flatRest(function(object, paths) {
    var result = {};
    if (object == null) {
      return result;
    }
    var isDeep = false;
    paths = arrayMap(paths, function(path) {
      path = castPath(path, object);
      isDeep || (isDeep = path.length > 1);
      return path;
    });
    copyObject(object, getAllKeysIn(object), result);
    if (isDeep) {
      result = baseClone(result, CLONE_DEEP_FLAG | CLONE_FLAT_FLAG | CLONE_SYMBOLS_FLAG, customOmitClone);
    }
    var length = paths.length;
    while (length--) {
      baseUnset(result, paths[length]);
    }
    return result;
  });
  omit_1 = omit;
  return omit_1;
}
var omitExports = requireOmit();
var transform_1;
var hasRequiredTransform;
function requireTransform() {
  if (hasRequiredTransform) return transform_1;
  hasRequiredTransform = 1;
  var arrayEach = require_arrayEach(), baseCreate = require_baseCreate(), baseForOwn = require_baseForOwn(), baseIteratee = require_baseIteratee(), getPrototype = require_getPrototype(), isArray = requireIsArray(), isBuffer2 = requireIsBuffer(), isFunction2 = requireIsFunction(), isObject2 = requireIsObject(), isTypedArray = requireIsTypedArray();
  function transform(object, iteratee, accumulator) {
    var isArr = isArray(object), isArrLike = isArr || isBuffer2(object) || isTypedArray(object);
    iteratee = baseIteratee(iteratee, 4);
    if (accumulator == null) {
      var Ctor = object && object.constructor;
      if (isArrLike) {
        accumulator = isArr ? new Ctor() : [];
      } else if (isObject2(object)) {
        accumulator = isFunction2(Ctor) ? baseCreate(getPrototype(object)) : {};
      } else {
        accumulator = {};
      }
    }
    (isArrLike ? arrayEach : baseForOwn)(object, function(value, index2, object2) {
      return iteratee(accumulator, value, index2, object2);
    });
    return accumulator;
  }
  transform_1 = transform;
  return transform_1;
}
var transformExports = requireTransform();
var isBetween$1 = { exports: {} };
var isBetween = isBetween$1.exports;
var hasRequiredIsBetween;
function requireIsBetween() {
  if (hasRequiredIsBetween) return isBetween$1.exports;
  hasRequiredIsBetween = 1;
  (function(module, exports) {
    !(function(e2, i2) {
      module.exports = i2();
    })(isBetween, (function() {
      return function(e2, i2, t2) {
        i2.prototype.isBetween = function(e3, i3, s3, f) {
          var n = t2(e3), o = t2(i3), r2 = "(" === (f = f || "()")[0], u2 = ")" === f[1];
          return (r2 ? this.isAfter(n, s3) : !this.isBefore(n, s3)) && (u2 ? this.isBefore(o, s3) : !this.isAfter(o, s3)) || (r2 ? this.isBefore(n, s3) : !this.isAfter(n, s3)) && (u2 ? this.isAfter(o, s3) : !this.isBefore(o, s3));
        };
      };
    }));
  })(isBetween$1);
  return isBetween$1.exports;
}
requireIsBetween();
var isSameOrAfter$1 = { exports: {} };
var isSameOrAfter = isSameOrAfter$1.exports;
var hasRequiredIsSameOrAfter;
function requireIsSameOrAfter() {
  if (hasRequiredIsSameOrAfter) return isSameOrAfter$1.exports;
  hasRequiredIsSameOrAfter = 1;
  (function(module, exports) {
    !(function(e2, t2) {
      module.exports = t2();
    })(isSameOrAfter, (function() {
      return function(e2, t2) {
        t2.prototype.isSameOrAfter = function(e3, t3) {
          return this.isSame(e3, t3) || this.isAfter(e3, t3);
        };
      };
    }));
  })(isSameOrAfter$1);
  return isSameOrAfter$1.exports;
}
requireIsSameOrAfter();
var isSameOrBefore$1 = { exports: {} };
var isSameOrBefore = isSameOrBefore$1.exports;
var hasRequiredIsSameOrBefore;
function requireIsSameOrBefore() {
  if (hasRequiredIsSameOrBefore) return isSameOrBefore$1.exports;
  hasRequiredIsSameOrBefore = 1;
  (function(module, exports) {
    !(function(e2, i2) {
      module.exports = i2();
    })(isSameOrBefore, (function() {
      return function(e2, i2) {
        i2.prototype.isSameOrBefore = function(e3, i3) {
          return this.isSame(e3, i3) || this.isBefore(e3, i3);
        };
      };
    }));
  })(isSameOrBefore$1);
  return isSameOrBefore$1.exports;
}
requireIsSameOrBefore();
var localeData$1 = { exports: {} };
var localeData = localeData$1.exports;
var hasRequiredLocaleData;
function requireLocaleData() {
  if (hasRequiredLocaleData) return localeData$1.exports;
  hasRequiredLocaleData = 1;
  (function(module, exports) {
    !(function(n, e2) {
      module.exports = e2();
    })(localeData, (function() {
      return function(n, e2, t2) {
        var r2 = e2.prototype, o = function(n2) {
          return n2 && (n2.indexOf ? n2 : n2.s);
        }, u2 = function(n2, e3, t3, r3, u3) {
          var i3 = n2.name ? n2 : n2.$locale(), a4 = o(i3[e3]), s4 = o(i3[t3]), f = a4 || s4.map((function(n3) {
            return n3.slice(0, r3);
          }));
          if (!u3) return f;
          var d3 = i3.weekStart;
          return f.map((function(n3, e4) {
            return f[(e4 + (d3 || 0)) % 7];
          }));
        }, i2 = function() {
          return t2.Ls[t2.locale()];
        }, a3 = function(n2, e3) {
          return n2.formats[e3] || (function(n3) {
            return n3.replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, (function(n4, e4, t3) {
              return e4 || t3.slice(1);
            }));
          })(n2.formats[e3.toUpperCase()]);
        }, s3 = function() {
          var n2 = this;
          return { months: function(e3) {
            return e3 ? e3.format("MMMM") : u2(n2, "months");
          }, monthsShort: function(e3) {
            return e3 ? e3.format("MMM") : u2(n2, "monthsShort", "months", 3);
          }, firstDayOfWeek: function() {
            return n2.$locale().weekStart || 0;
          }, weekdays: function(e3) {
            return e3 ? e3.format("dddd") : u2(n2, "weekdays");
          }, weekdaysMin: function(e3) {
            return e3 ? e3.format("dd") : u2(n2, "weekdaysMin", "weekdays", 2);
          }, weekdaysShort: function(e3) {
            return e3 ? e3.format("ddd") : u2(n2, "weekdaysShort", "weekdays", 3);
          }, longDateFormat: function(e3) {
            return a3(n2.$locale(), e3);
          }, meridiem: this.$locale().meridiem, ordinal: this.$locale().ordinal };
        };
        r2.localeData = function() {
          return s3.bind(this)();
        }, t2.localeData = function() {
          var n2 = i2();
          return { firstDayOfWeek: function() {
            return n2.weekStart || 0;
          }, weekdays: function() {
            return t2.weekdays();
          }, weekdaysShort: function() {
            return t2.weekdaysShort();
          }, weekdaysMin: function() {
            return t2.weekdaysMin();
          }, months: function() {
            return t2.months();
          }, monthsShort: function() {
            return t2.monthsShort();
          }, longDateFormat: function(e3) {
            return a3(n2, e3);
          }, meridiem: n2.meridiem, ordinal: n2.ordinal };
        }, t2.months = function() {
          return u2(i2(), "months");
        }, t2.monthsShort = function() {
          return u2(i2(), "monthsShort", "months", 3);
        }, t2.weekdays = function(n2) {
          return u2(i2(), "weekdays", null, null, n2);
        }, t2.weekdaysShort = function(n2) {
          return u2(i2(), "weekdaysShort", "weekdays", 3, n2);
        }, t2.weekdaysMin = function(n2) {
          return u2(i2(), "weekdaysMin", "weekdays", 2, n2);
        };
      };
    }));
  })(localeData$1);
  return localeData$1.exports;
}
requireLocaleData();
var localizedFormat$1 = { exports: {} };
var localizedFormat = localizedFormat$1.exports;
var hasRequiredLocalizedFormat;
function requireLocalizedFormat() {
  if (hasRequiredLocalizedFormat) return localizedFormat$1.exports;
  hasRequiredLocalizedFormat = 1;
  (function(module, exports) {
    !(function(e2, t2) {
      module.exports = t2();
    })(localizedFormat, (function() {
      var e2 = { LTS: "h:mm:ss A", LT: "h:mm A", L: "MM/DD/YYYY", LL: "MMMM D, YYYY", LLL: "MMMM D, YYYY h:mm A", LLLL: "dddd, MMMM D, YYYY h:mm A" };
      return function(t2, o, n) {
        var r2 = o.prototype, i2 = r2.format;
        n.en.formats = e2, r2.format = function(t3) {
          void 0 === t3 && (t3 = "YYYY-MM-DDTHH:mm:ssZ");
          var o2 = this.$locale().formats, n2 = (function(t4, o3) {
            return t4.replace(/(\[[^\]]+])|(LTS?|l{1,4}|L{1,4})/g, (function(t5, n3, r3) {
              var i3 = r3 && r3.toUpperCase();
              return n3 || o3[r3] || e2[r3] || o3[i3].replace(/(\[[^\]]+])|(MMMM|MM|DD|dddd)/g, (function(e3, t6, o4) {
                return t6 || o4.slice(1);
              }));
            }));
          })(t3, void 0 === o2 ? {} : o2);
          return i2.call(this, n2);
        };
      };
    }));
  })(localizedFormat$1);
  return localizedFormat$1.exports;
}
requireLocalizedFormat();
var minMax$1 = { exports: {} };
var minMax = minMax$1.exports;
var hasRequiredMinMax;
function requireMinMax() {
  if (hasRequiredMinMax) return minMax$1.exports;
  hasRequiredMinMax = 1;
  (function(module, exports) {
    !(function(e2, n) {
      module.exports = n();
    })(minMax, (function() {
      return function(e2, n, t2) {
        var i2 = function(e3, n2) {
          if (!n2 || !n2.length || 1 === n2.length && !n2[0] || 1 === n2.length && Array.isArray(n2[0]) && !n2[0].length) return null;
          var t3;
          1 === n2.length && n2[0].length > 0 && (n2 = n2[0]);
          t3 = (n2 = n2.filter((function(e4) {
            return e4;
          })))[0];
          for (var i3 = 1; i3 < n2.length; i3 += 1) n2[i3].isValid() && !n2[i3][e3](t3) || (t3 = n2[i3]);
          return t3;
        };
        t2.max = function() {
          var e3 = [].slice.call(arguments, 0);
          return i2("isAfter", e3);
        }, t2.min = function() {
          var e3 = [].slice.call(arguments, 0);
          return i2("isBefore", e3);
        };
      };
    }));
  })(minMax$1);
  return minMax$1.exports;
}
requireMinMax();
var utc$1 = { exports: {} };
var utc = utc$1.exports;
var hasRequiredUtc;
function requireUtc() {
  if (hasRequiredUtc) return utc$1.exports;
  hasRequiredUtc = 1;
  (function(module, exports) {
    !(function(t2, i2) {
      module.exports = i2();
    })(utc, (function() {
      var t2 = "minute", i2 = /[+-]\d\d(?::?\d\d)?/g, e2 = /([+-]|\d\d)/g;
      return function(s3, f, n) {
        var u2 = f.prototype;
        n.utc = function(t3) {
          var i3 = { date: t3, utc: true, args: arguments };
          return new f(i3);
        }, u2.utc = function(i3) {
          var e3 = n(this.toDate(), { locale: this.$L, utc: true });
          return i3 ? e3.add(this.utcOffset(), t2) : e3;
        }, u2.local = function() {
          return n(this.toDate(), { locale: this.$L, utc: false });
        };
        var o = u2.parse;
        u2.parse = function(t3) {
          t3.utc && (this.$u = true), this.$utils().u(t3.$offset) || (this.$offset = t3.$offset), o.call(this, t3);
        };
        var r2 = u2.init;
        u2.init = function() {
          if (this.$u) {
            var t3 = this.$d;
            this.$y = t3.getUTCFullYear(), this.$M = t3.getUTCMonth(), this.$D = t3.getUTCDate(), this.$W = t3.getUTCDay(), this.$H = t3.getUTCHours(), this.$m = t3.getUTCMinutes(), this.$s = t3.getUTCSeconds(), this.$ms = t3.getUTCMilliseconds();
          } else r2.call(this);
        };
        var a3 = u2.utcOffset;
        u2.utcOffset = function(s4, f2) {
          var n2 = this.$utils().u;
          if (n2(s4)) return this.$u ? 0 : n2(this.$offset) ? a3.call(this) : this.$offset;
          if ("string" == typeof s4 && (s4 = (function(t3) {
            void 0 === t3 && (t3 = "");
            var s5 = t3.match(i2);
            if (!s5) return null;
            var f3 = ("" + s5[0]).match(e2) || ["-", 0, 0], n3 = f3[0], u4 = 60 * +f3[1] + +f3[2];
            return 0 === u4 ? 0 : "+" === n3 ? u4 : -u4;
          })(s4), null === s4)) return this;
          var u3 = Math.abs(s4) <= 16 ? 60 * s4 : s4, o2 = this;
          if (f2) return o2.$offset = u3, o2.$u = 0 === s4, o2;
          if (0 !== s4) {
            var r3 = this.$u ? this.toDate().getTimezoneOffset() : -1 * this.utcOffset();
            (o2 = this.local().add(u3 + r3, t2)).$offset = u3, o2.$x.$localOffset = r3;
          } else o2 = this.utc();
          return o2;
        };
        var h3 = u2.format;
        u2.format = function(t3) {
          var i3 = t3 || (this.$u ? "YYYY-MM-DDTHH:mm:ss[Z]" : "");
          return h3.call(this, i3);
        }, u2.valueOf = function() {
          var t3 = this.$utils().u(this.$offset) ? 0 : this.$offset + (this.$x.$localOffset || this.$d.getTimezoneOffset());
          return this.$d.valueOf() - 6e4 * t3;
        }, u2.isUTC = function() {
          return !!this.$u;
        }, u2.toISOString = function() {
          return this.toDate().toISOString();
        }, u2.toString = function() {
          return this.toDate().toUTCString();
        };
        var l = u2.toDate;
        u2.toDate = function(t3) {
          return "s" === t3 && this.$offset ? n(this.format("YYYY-MM-DD HH:mm:ss:SSS")).toDate() : l.call(this);
        };
        var c2 = u2.diff;
        u2.diff = function(t3, i3, e3) {
          if (t3 && this.$u === t3.$u) return c2.call(this, t3, i3, e3);
          var s4 = this.local(), f2 = n(t3).local();
          return c2.call(s4, f2, i3, e3);
        };
      };
    }));
  })(utc$1);
  return utc$1.exports;
}
requireUtc();
var isLeapYear$1 = { exports: {} };
var isLeapYear = isLeapYear$1.exports;
var hasRequiredIsLeapYear;
function requireIsLeapYear() {
  if (hasRequiredIsLeapYear) return isLeapYear$1.exports;
  hasRequiredIsLeapYear = 1;
  (function(module, exports) {
    !(function(e2, t2) {
      module.exports = t2();
    })(isLeapYear, (function() {
      return function(e2, t2) {
        t2.prototype.isLeapYear = function() {
          return this.$y % 4 == 0 && this.$y % 100 != 0 || this.$y % 400 == 0;
        };
      };
    }));
  })(isLeapYear$1);
  return isLeapYear$1.exports;
}
requireIsLeapYear();
function NoopWrapper(props) {
  return props.children;
}
var navigate = {
  PREVIOUS: "PREV",
  NEXT: "NEXT",
  TODAY: "TODAY",
  DATE: "DATE"
};
var views = {
  MONTH: "month",
  WEEK: "week",
  WORK_WEEK: "work_week",
  DAY: "day",
  AGENDA: "agenda"
};
var viewNames$1 = Object.keys(views).map(function(k2) {
  return views[k2];
});
propTypesExports.oneOfType([propTypesExports.string, propTypesExports.func]);
propTypesExports.any;
propTypesExports.func;
propTypesExports.oneOfType([propTypesExports.arrayOf(propTypesExports.oneOf(viewNames$1)), propTypesExports.objectOf(function(prop, key) {
  var isBuiltinView = viewNames$1.indexOf(key) !== -1 && typeof prop[key] === "boolean";
  if (isBuiltinView) {
    return null;
  } else {
    for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
      args[_key - 2] = arguments[_key];
    }
    return propTypesExports.elementType.apply(propTypesExports, [prop, key].concat(args));
  }
})]);
propTypesExports.oneOfType([propTypesExports.oneOf(["overlap", "no-overlap"]), propTypesExports.func]);
var MILLI = {
  seconds: 1e3,
  minutes: 1e3 * 60,
  hours: 1e3 * 60 * 60,
  day: 1e3 * 60 * 60 * 24
};
function firstVisibleDay(date2, localizer2) {
  var firstOfMonth = startOf(date2, "month");
  return startOf(firstOfMonth, "week", localizer2.startOfWeek());
}
function lastVisibleDay(date2, localizer2) {
  var endOfMonth = endOf(date2, "month");
  return endOf(endOfMonth, "week", localizer2.startOfWeek());
}
function visibleDays(date2, localizer2) {
  var current = firstVisibleDay(date2, localizer2), last = lastVisibleDay(date2, localizer2), days = [];
  while (lte(current, last, "day")) {
    days.push(current);
    current = add(current, 1, "day");
  }
  return days;
}
function ceil(date2, unit) {
  var floor = startOf(date2, unit);
  return eq(floor, date2) ? floor : add(floor, 1, unit);
}
function range(start2, end2) {
  var unit = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : "day";
  var current = start2, days = [];
  while (lte(current, end2, unit)) {
    days.push(current);
    current = add(current, 1, unit);
  }
  return days;
}
function merge(date2, time) {
  if (time == null && date2 == null) return null;
  if (time == null) time = /* @__PURE__ */ new Date();
  if (date2 == null) date2 = /* @__PURE__ */ new Date();
  date2 = startOf(date2, "day");
  date2 = hours(date2, hours(time));
  date2 = minutes(date2, minutes(time));
  date2 = seconds(date2, seconds(time));
  return milliseconds(date2, milliseconds(time));
}
function isJustDate(date2) {
  return hours(date2) === 0 && minutes(date2) === 0 && seconds(date2) === 0 && milliseconds(date2) === 0;
}
function duration(start2, end2, unit, firstOfWeek) {
  if (unit === "day") unit = "date";
  return Math.abs(
    // eslint-disable-next-line import/namespace
    dates[unit](start2, void 0, firstOfWeek) - // eslint-disable-next-line import/namespace
    dates[unit](end2, void 0, firstOfWeek)
  );
}
function diff(dateA, dateB, unit) {
  if (!unit || unit === "milliseconds") return Math.abs(+dateA - +dateB);
  return Math.round(Math.abs(+startOf(dateA, unit) / MILLI[unit] - +startOf(dateB, unit) / MILLI[unit]));
}
var localePropType = propTypesExports.oneOfType([propTypesExports.string, propTypesExports.func]);
function _format(localizer2, formatter, value, format2, culture) {
  var result = typeof format2 === "function" ? format2(value, culture, localizer2) : formatter.call(localizer2, value, format2, culture);
  browserExports(result == null || typeof result === "string", "`localizer format(..)` must return a string, null, or undefined");
  return result;
}
function getSlotDate(dt, minutesFromMidnight, offset2) {
  return new Date(dt.getFullYear(), dt.getMonth(), dt.getDate(), 0, minutesFromMidnight + offset2, 0, 0);
}
function getDstOffset(start2, end2) {
  return start2.getTimezoneOffset() - end2.getTimezoneOffset();
}
function getTotalMin(start2, end2) {
  return diff(start2, end2, "minutes") + getDstOffset(start2, end2);
}
function getMinutesFromMidnight(start2) {
  var daystart = startOf(start2, "day");
  return diff(daystart, start2, "minutes") + getDstOffset(daystart, start2);
}
function continuesPrior(start2, first) {
  return lt(start2, first, "day");
}
function continuesAfter(start2, end2, last) {
  var singleDayDuration = eq(start2, end2, "minutes");
  return singleDayDuration ? gte(end2, last, "minutes") : gt(end2, last, "minutes");
}
function daySpan(start2, end2) {
  return duration(start2, end2, "day");
}
function sortEvents$1(_ref) {
  var _ref$evtA = _ref.evtA, aStart = _ref$evtA.start, aEnd = _ref$evtA.end, aAllDay = _ref$evtA.allDay, _ref$evtB = _ref.evtB, bStart = _ref$evtB.start, bEnd = _ref$evtB.end, bAllDay = _ref$evtB.allDay;
  var startSort = +startOf(aStart, "day") - +startOf(bStart, "day");
  var durA = daySpan(aStart, aEnd);
  var durB = daySpan(bStart, bEnd);
  return startSort || // sort by start Day first
  durB - durA || // events spanning multiple days go first
  !!bAllDay - !!aAllDay || // then allDay single day events
  +aStart - +bStart || // then sort by start time
  +aEnd - +bEnd;
}
function inEventRange(_ref2) {
  var _ref2$event = _ref2.event, start2 = _ref2$event.start, end2 = _ref2$event.end, _ref2$range = _ref2.range, rangeStart = _ref2$range.start, rangeEnd = _ref2$range.end;
  var eStart = startOf(start2, "day");
  var startsBeforeEnd = lte(eStart, rangeEnd, "day");
  var sameMin = neq(eStart, end2, "minutes");
  var endsAfterStart = sameMin ? gt(end2, rangeStart, "minutes") : gte(end2, rangeStart, "minutes");
  return startsBeforeEnd && endsAfterStart;
}
function isSameDate(date1, date2) {
  return eq(date1, date2, "day");
}
function startAndEndAreDateOnly(start2, end2) {
  return isJustDate(start2) && isJustDate(end2);
}
var DateLocalizer = /* @__PURE__ */ _createClass(function DateLocalizer2(spec) {
  var _this = this;
  _classCallCheck(this, DateLocalizer2);
  browserExports(typeof spec.format === "function", "date localizer `format(..)` must be a function");
  browserExports(typeof spec.firstOfWeek === "function", "date localizer `firstOfWeek(..)` must be a function");
  this.propType = spec.propType || localePropType;
  this.formats = spec.formats;
  this.format = function() {
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    return _format.apply(void 0, [_this, spec.format].concat(args));
  };
  this.startOfWeek = spec.firstOfWeek;
  this.merge = spec.merge || merge;
  this.inRange = spec.inRange || inRange$1;
  this.lt = spec.lt || lt;
  this.lte = spec.lte || lte;
  this.gt = spec.gt || gt;
  this.gte = spec.gte || gte;
  this.eq = spec.eq || eq;
  this.neq = spec.neq || neq;
  this.startOf = spec.startOf || startOf;
  this.endOf = spec.endOf || endOf;
  this.add = spec.add || add;
  this.range = spec.range || range;
  this.diff = spec.diff || diff;
  this.ceil = spec.ceil || ceil;
  this.min = spec.min || min;
  this.max = spec.max || max;
  this.minutes = spec.minutes || minutes;
  this.daySpan = spec.daySpan || daySpan;
  this.firstVisibleDay = spec.firstVisibleDay || firstVisibleDay;
  this.lastVisibleDay = spec.lastVisibleDay || lastVisibleDay;
  this.visibleDays = spec.visibleDays || visibleDays;
  this.getSlotDate = spec.getSlotDate || getSlotDate;
  this.getTimezoneOffset = spec.getTimezoneOffset || function(value) {
    return value.getTimezoneOffset();
  };
  this.getDstOffset = spec.getDstOffset || getDstOffset;
  this.getTotalMin = spec.getTotalMin || getTotalMin;
  this.getMinutesFromMidnight = spec.getMinutesFromMidnight || getMinutesFromMidnight;
  this.continuesPrior = spec.continuesPrior || continuesPrior;
  this.continuesAfter = spec.continuesAfter || continuesAfter;
  this.sortEvents = spec.sortEvents || sortEvents$1;
  this.inEventRange = spec.inEventRange || inEventRange;
  this.isSameDate = spec.isSameDate || isSameDate;
  this.startAndEndAreDateOnly = spec.startAndEndAreDateOnly || startAndEndAreDateOnly;
  this.segmentOffset = spec.browserTZOffset ? spec.browserTZOffset() : 0;
});
function mergeWithDefaults(localizer2, culture, formatOverrides, messages2) {
  var formats2 = _objectSpread2(_objectSpread2({}, localizer2.formats), formatOverrides);
  return _objectSpread2(_objectSpread2({}, localizer2), {}, {
    messages: messages2,
    startOfWeek: function startOfWeek2() {
      return localizer2.startOfWeek(culture);
    },
    format: function format2(value, _format2) {
      return localizer2.format(value, formats2[_format2] || _format2, culture);
    }
  });
}
var Toolbar = /* @__PURE__ */ (function(_React$Component) {
  function Toolbar2() {
    var _this;
    _classCallCheck(this, Toolbar2);
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _callSuper(this, Toolbar2, [].concat(args));
    _this.navigate = function(action) {
      _this.props.onNavigate(action);
    };
    _this.view = function(view) {
      _this.props.onView(view);
    };
    return _this;
  }
  _inherits(Toolbar2, _React$Component);
  return _createClass(Toolbar2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, messages2 = _this$props.localizer.messages, label = _this$props.label;
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-toolbar"
      }, /* @__PURE__ */ reactExports.createElement("span", {
        className: "rbc-btn-group"
      }, /* @__PURE__ */ reactExports.createElement("button", {
        type: "button",
        onClick: this.navigate.bind(null, navigate.TODAY)
      }, messages2.today), /* @__PURE__ */ reactExports.createElement("button", {
        type: "button",
        onClick: this.navigate.bind(null, navigate.PREVIOUS)
      }, messages2.previous), /* @__PURE__ */ reactExports.createElement("button", {
        type: "button",
        onClick: this.navigate.bind(null, navigate.NEXT)
      }, messages2.next)), /* @__PURE__ */ reactExports.createElement("span", {
        className: "rbc-toolbar-label"
      }, label), /* @__PURE__ */ reactExports.createElement("span", {
        className: "rbc-btn-group"
      }, this.viewNamesGroup(messages2)));
    }
  }, {
    key: "viewNamesGroup",
    value: function viewNamesGroup(messages2) {
      var _this2 = this;
      var viewNames2 = this.props.views;
      var view = this.props.view;
      if (viewNames2.length > 1) {
        return viewNames2.map(function(name2) {
          return /* @__PURE__ */ reactExports.createElement("button", {
            type: "button",
            key: name2,
            className: clsx({
              "rbc-active": view === name2
            }),
            onClick: _this2.view.bind(null, name2)
          }, messages2[name2]);
        });
      }
    }
  }]);
})(reactExports.Component);
function notify(handler, args) {
  handler && handler.apply(null, [].concat(args));
}
var defaultMessages = {
  date: "Date",
  time: "Time",
  event: "Event",
  allDay: "All Day",
  week: "Week",
  work_week: "Work Week",
  day: "Day",
  month: "Month",
  previous: "Back",
  next: "Next",
  yesterday: "Yesterday",
  tomorrow: "Tomorrow",
  today: "Today",
  agenda: "Agenda",
  noEventsInRange: "There are no events in this range.",
  showMore: function showMore(total) {
    return "+".concat(total, " more");
  }
};
function messages(msgs) {
  return _objectSpread2(_objectSpread2({}, defaultMessages), msgs);
}
function useClickOutside(_ref) {
  var ref = _ref.ref, callback = _ref.callback;
  reactExports.useEffect(function() {
    var handleClickOutside = function handleClickOutside2(e2) {
      if (ref.current && !ref.current.contains(e2.target)) {
        callback();
      }
    };
    document.addEventListener("mousedown", handleClickOutside);
    return function() {
      document.removeEventListener("mousedown", handleClickOutside);
    };
  }, [ref, callback]);
}
var _excluded$7 = ["style", "className", "event", "selected", "isAllDay", "onSelect", "onDoubleClick", "onKeyPress", "localizer", "continuesPrior", "continuesAfter", "accessors", "getters", "children", "components", "slotStart", "slotEnd"];
var EventCell$1 = /* @__PURE__ */ (function(_React$Component) {
  function EventCell2() {
    _classCallCheck(this, EventCell2);
    return _callSuper(this, EventCell2, arguments);
  }
  _inherits(EventCell2, _React$Component);
  return _createClass(EventCell2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, style2 = _this$props.style, className = _this$props.className, event = _this$props.event, selected = _this$props.selected, isAllDay = _this$props.isAllDay, onSelect = _this$props.onSelect, _onDoubleClick = _this$props.onDoubleClick, onKeyPress = _this$props.onKeyPress, localizer2 = _this$props.localizer, continuesPrior2 = _this$props.continuesPrior, continuesAfter2 = _this$props.continuesAfter, accessors2 = _this$props.accessors, getters = _this$props.getters, children = _this$props.children, _this$props$component = _this$props.components, Event2 = _this$props$component.event, EventWrapper2 = _this$props$component.eventWrapper, slotStart = _this$props.slotStart, slotEnd = _this$props.slotEnd, props = _objectWithoutProperties(_this$props, _excluded$7);
      delete props.resizable;
      var title = accessors2.title(event);
      var tooltip = accessors2.tooltip(event);
      var end2 = accessors2.end(event);
      var start2 = accessors2.start(event);
      var allDay = accessors2.allDay(event);
      var showAsAllDay = isAllDay || allDay || localizer2.diff(start2, localizer2.ceil(end2, "day"), "day") > 1;
      var userProps = getters.eventProp(event, start2, end2, selected);
      var content = /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-event-content",
        title: tooltip || void 0
      }, Event2 ? /* @__PURE__ */ reactExports.createElement(Event2, {
        event,
        continuesPrior: continuesPrior2,
        continuesAfter: continuesAfter2,
        title,
        isAllDay: allDay,
        localizer: localizer2,
        slotStart,
        slotEnd
      }) : title);
      return /* @__PURE__ */ reactExports.createElement(EventWrapper2, Object.assign({}, this.props, {
        type: "date"
      }), /* @__PURE__ */ reactExports.createElement("div", Object.assign({}, props, {
        style: _objectSpread2(_objectSpread2({}, userProps.style), style2),
        className: clsx("rbc-event", className, userProps.className, {
          "rbc-selected": selected,
          "rbc-event-allday": showAsAllDay,
          "rbc-event-continues-prior": continuesPrior2,
          "rbc-event-continues-after": continuesAfter2
        }),
        onClick: function onClick(e2) {
          return onSelect && onSelect(event, e2);
        },
        onDoubleClick: function onDoubleClick(e2) {
          return _onDoubleClick && _onDoubleClick(event, e2);
        },
        onKeyDown: function onKeyDown(e2) {
          return onKeyPress && onKeyPress(event, e2);
        }
      }), typeof children === "function" ? children(content) : content));
    }
  }]);
})(reactExports.Component);
function isSelected(event, selected) {
  if (!event || selected == null) return false;
  return isEqualExports(event, selected);
}
function slotWidth(rowBox, slots) {
  var rowWidth = rowBox.right - rowBox.left;
  var cellWidth = rowWidth / slots;
  return cellWidth;
}
function getSlotAtX(rowBox, x2, rtl, slots) {
  var cellWidth = slotWidth(rowBox, slots);
  return rtl ? slots - 1 - Math.floor((x2 - rowBox.left) / cellWidth) : Math.floor((x2 - rowBox.left) / cellWidth);
}
function pointInBox(box, _ref) {
  var x2 = _ref.x, y3 = _ref.y;
  return y3 >= box.top && y3 <= box.bottom && x2 >= box.left && x2 <= box.right;
}
function dateCellSelection(start2, rowBox, box, slots, rtl) {
  var startIdx = -1;
  var endIdx = -1;
  var lastSlotIdx = slots - 1;
  var cellWidth = slotWidth(rowBox, slots);
  var currentSlot = getSlotAtX(rowBox, box.x, rtl, slots);
  var isCurrentRow = rowBox.top < box.y && rowBox.bottom > box.y;
  var isStartRow = rowBox.top < start2.y && rowBox.bottom > start2.y;
  var isAboveStart = start2.y > rowBox.bottom;
  var isBelowStart = rowBox.top > start2.y;
  var isBetween2 = box.top < rowBox.top && box.bottom > rowBox.bottom;
  if (isBetween2) {
    startIdx = 0;
    endIdx = lastSlotIdx;
  }
  if (isCurrentRow) {
    if (isBelowStart) {
      startIdx = 0;
      endIdx = currentSlot;
    } else if (isAboveStart) {
      startIdx = currentSlot;
      endIdx = lastSlotIdx;
    }
  }
  if (isStartRow) {
    startIdx = endIdx = rtl ? lastSlotIdx - Math.floor((start2.x - rowBox.left) / cellWidth) : Math.floor((start2.x - rowBox.left) / cellWidth);
    if (isCurrentRow) {
      if (currentSlot < startIdx) startIdx = currentSlot;
      else endIdx = currentSlot;
    } else if (start2.y < box.y) {
      endIdx = lastSlotIdx;
    } else {
      startIdx = 0;
    }
  }
  return {
    startIdx,
    endIdx
  };
}
function getPosition(_ref) {
  var target = _ref.target, offset$12 = _ref.offset, container = _ref.container, box = _ref.box;
  var _getOffset = offset(target), top2 = _getOffset.top, left2 = _getOffset.left, width = _getOffset.width, height2 = _getOffset.height;
  var _getOffset2 = offset(container), cTop = _getOffset2.top, cLeft = _getOffset2.left, cWidth = _getOffset2.width, cHeight = _getOffset2.height;
  var _getOffset3 = offset(box), bWidth = _getOffset3.width, bHeight = _getOffset3.height;
  var viewBottom = cTop + cHeight;
  var viewRight = cLeft + cWidth;
  var bottom2 = top2 + bHeight;
  var right2 = left2 + bWidth;
  var x2 = offset$12.x, y3 = offset$12.y;
  var topOffset = bottom2 > viewBottom ? top2 - bHeight - y3 : top2 + y3 + height2;
  var leftOffset = right2 > viewRight ? left2 + x2 - bWidth + width : left2 + x2;
  return {
    topOffset,
    leftOffset
  };
}
function Pop(_ref2) {
  var containerRef = _ref2.containerRef, accessors2 = _ref2.accessors, getters = _ref2.getters, selected = _ref2.selected, components2 = _ref2.components, localizer2 = _ref2.localizer, position2 = _ref2.position, show = _ref2.show, events2 = _ref2.events, slotStart = _ref2.slotStart, slotEnd = _ref2.slotEnd, onSelect = _ref2.onSelect, onDoubleClick = _ref2.onDoubleClick, onKeyPress = _ref2.onKeyPress, handleDragStart = _ref2.handleDragStart, popperRef = _ref2.popperRef, target = _ref2.target, offset2 = _ref2.offset;
  useClickOutside({
    ref: popperRef,
    callback: show
  });
  reactExports.useLayoutEffect(function() {
    var _getPosition = getPosition({
      target,
      offset: offset2,
      container: containerRef.current,
      box: popperRef.current
    }), topOffset = _getPosition.topOffset, leftOffset = _getPosition.leftOffset;
    popperRef.current.style.top = "".concat(topOffset, "px");
    popperRef.current.style.left = "".concat(leftOffset, "px");
  }, [offset2.x, offset2.y, target]);
  var width = position2.width;
  var style2 = {
    minWidth: width + width / 2
  };
  return /* @__PURE__ */ reactExports.createElement("div", {
    style: style2,
    className: "rbc-overlay",
    ref: popperRef
  }, /* @__PURE__ */ reactExports.createElement("div", {
    className: "rbc-overlay-header"
  }, localizer2.format(slotStart, "dayHeaderFormat")), events2.map(function(event, idx) {
    return /* @__PURE__ */ reactExports.createElement(EventCell$1, {
      key: idx,
      type: "popup",
      localizer: localizer2,
      event,
      getters,
      onSelect,
      accessors: accessors2,
      components: components2,
      onDoubleClick,
      onKeyPress,
      continuesPrior: localizer2.lt(accessors2.end(event), slotStart, "day"),
      continuesAfter: localizer2.gte(accessors2.start(event), slotEnd, "day"),
      slotStart,
      slotEnd,
      selected: isSelected(event, selected),
      draggable: true,
      onDragStart: function onDragStart() {
        return handleDragStart(event);
      },
      onDragEnd: function onDragEnd() {
        return show();
      }
    });
  }));
}
var Popup = /* @__PURE__ */ reactExports.forwardRef(function(props, ref) {
  return /* @__PURE__ */ reactExports.createElement(Pop, Object.assign({}, props, {
    popperRef: ref
  }));
});
Popup.propTypes = {
  accessors: propTypesExports.object.isRequired,
  getters: propTypesExports.object.isRequired,
  selected: propTypesExports.object,
  components: propTypesExports.object.isRequired,
  localizer: propTypesExports.object.isRequired,
  position: propTypesExports.object.isRequired,
  show: propTypesExports.func.isRequired,
  events: propTypesExports.array.isRequired,
  slotStart: propTypesExports.instanceOf(Date).isRequired,
  slotEnd: propTypesExports.instanceOf(Date),
  onSelect: propTypesExports.func,
  onDoubleClick: propTypesExports.func,
  onKeyPress: propTypesExports.func,
  handleDragStart: propTypesExports.func,
  style: propTypesExports.object,
  offset: propTypesExports.shape({
    x: propTypesExports.number,
    y: propTypesExports.number
  })
};
function CalOverlay(_ref) {
  var containerRef = _ref.containerRef, _ref$popupOffset = _ref.popupOffset, popupOffset = _ref$popupOffset === void 0 ? 5 : _ref$popupOffset, overlay = _ref.overlay, accessors2 = _ref.accessors, localizer2 = _ref.localizer, components2 = _ref.components, getters = _ref.getters, selected = _ref.selected, handleSelectEvent = _ref.handleSelectEvent, handleDoubleClickEvent = _ref.handleDoubleClickEvent, handleKeyPressEvent = _ref.handleKeyPressEvent, handleDragStart = _ref.handleDragStart, onHide2 = _ref.onHide, overlayDisplay = _ref.overlayDisplay;
  var popperRef = reactExports.useRef(null);
  if (!overlay.position) return null;
  var offset2 = popupOffset;
  if (!isNaN(popupOffset)) {
    offset2 = {
      x: popupOffset,
      y: popupOffset
    };
  }
  var position2 = overlay.position, events2 = overlay.events, date2 = overlay.date, end2 = overlay.end;
  return /* @__PURE__ */ reactExports.createElement(Overlay, {
    rootClose: true,
    flip: true,
    show: true,
    placement: "bottom",
    onHide: onHide2,
    target: overlay.target
  }, function(_ref2) {
    var props = _ref2.props;
    return /* @__PURE__ */ reactExports.createElement(Popup, Object.assign({}, props, {
      containerRef,
      ref: popperRef,
      target: overlay.target,
      offset: offset2,
      accessors: accessors2,
      getters,
      selected,
      components: components2,
      localizer: localizer2,
      position: position2,
      show: overlayDisplay,
      events: events2,
      slotStart: date2,
      slotEnd: end2,
      onSelect: handleSelectEvent,
      onDoubleClick: handleDoubleClickEvent,
      onKeyPress: handleKeyPressEvent,
      handleDragStart
    }));
  });
}
var PopOverlay = /* @__PURE__ */ reactExports.forwardRef(function(props, ref) {
  return /* @__PURE__ */ reactExports.createElement(CalOverlay, Object.assign({}, props, {
    containerRef: ref
  }));
});
PopOverlay.propTypes = {
  popupOffset: propTypesExports.oneOfType([propTypesExports.number, propTypesExports.shape({
    x: propTypesExports.number,
    y: propTypesExports.number
  })]),
  overlay: propTypesExports.shape({
    position: propTypesExports.object,
    events: propTypesExports.array,
    date: propTypesExports.instanceOf(Date),
    end: propTypesExports.instanceOf(Date)
  }),
  accessors: propTypesExports.object.isRequired,
  localizer: propTypesExports.object.isRequired,
  components: propTypesExports.object.isRequired,
  getters: propTypesExports.object.isRequired,
  selected: propTypesExports.object,
  handleSelectEvent: propTypesExports.func,
  handleDoubleClickEvent: propTypesExports.func,
  handleKeyPressEvent: propTypesExports.func,
  handleDragStart: propTypesExports.func,
  onHide: propTypesExports.func,
  overlayDisplay: propTypesExports.func
};
function addEventListener(type, handler) {
  var target = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : document;
  return listen(target, type, handler, {
    passive: false
  });
}
function isOverContainer(container, x2, y3) {
  return !container || contains(container, document.elementFromPoint(x2, y3));
}
function getEventNodeFromPoint(node, _ref) {
  var clientX = _ref.clientX, clientY = _ref.clientY;
  var target = document.elementFromPoint(clientX, clientY);
  return closest(target, ".rbc-event", node);
}
function getShowMoreNodeFromPoint(node, _ref2) {
  var clientX = _ref2.clientX, clientY = _ref2.clientY;
  var target = document.elementFromPoint(clientX, clientY);
  return closest(target, ".rbc-show-more", node);
}
function isEvent(node, bounds) {
  return !!getEventNodeFromPoint(node, bounds);
}
function isShowMore(node, bounds) {
  return !!getShowMoreNodeFromPoint(node, bounds);
}
function getEventCoordinates(e2) {
  var target = e2;
  if (e2.touches && e2.touches.length) {
    target = e2.touches[0];
  }
  return {
    clientX: target.clientX,
    clientY: target.clientY,
    pageX: target.pageX,
    pageY: target.pageY
  };
}
var clickTolerance = 5;
var clickInterval = 250;
var Selection$1 = /* @__PURE__ */ (function() {
  function Selection2(node) {
    var _ref3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref3$global = _ref3.global, global2 = _ref3$global === void 0 ? false : _ref3$global, _ref3$longPressThresh = _ref3.longPressThreshold, longPressThreshold = _ref3$longPressThresh === void 0 ? 250 : _ref3$longPressThresh, _ref3$validContainers = _ref3.validContainers, validContainers = _ref3$validContainers === void 0 ? [] : _ref3$validContainers;
    _classCallCheck(this, Selection2);
    this._initialEvent = null;
    this.selecting = false;
    this.isDetached = false;
    this.container = node;
    this.globalMouse = !node || global2;
    this.longPressThreshold = longPressThreshold;
    this.validContainers = validContainers;
    this._listeners = /* @__PURE__ */ Object.create(null);
    this._handleInitialEvent = this._handleInitialEvent.bind(this);
    this._handleMoveEvent = this._handleMoveEvent.bind(this);
    this._handleTerminatingEvent = this._handleTerminatingEvent.bind(this);
    this._keyListener = this._keyListener.bind(this);
    this._dropFromOutsideListener = this._dropFromOutsideListener.bind(this);
    this._dragOverFromOutsideListener = this._dragOverFromOutsideListener.bind(this);
    this._removeTouchMoveWindowListener = addEventListener("touchmove", function() {
    }, window);
    this._removeKeyDownListener = addEventListener("keydown", this._keyListener);
    this._removeKeyUpListener = addEventListener("keyup", this._keyListener);
    this._removeDropFromOutsideListener = addEventListener("drop", this._dropFromOutsideListener);
    this._removeDragOverFromOutsideListener = addEventListener("dragover", this._dragOverFromOutsideListener);
    this._addInitialEventListener();
  }
  return _createClass(Selection2, [{
    key: "on",
    value: function on2(type, handler) {
      var handlers = this._listeners[type] || (this._listeners[type] = []);
      handlers.push(handler);
      return {
        remove: function remove2() {
          var idx = handlers.indexOf(handler);
          if (idx !== -1) handlers.splice(idx, 1);
        }
      };
    }
  }, {
    key: "emit",
    value: function emit(type) {
      for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
        args[_key - 1] = arguments[_key];
      }
      var result;
      var handlers = this._listeners[type] || [];
      handlers.forEach(function(fn3) {
        if (result === void 0) result = fn3.apply(void 0, args);
      });
      return result;
    }
  }, {
    key: "teardown",
    value: function teardown() {
      this._initialEvent = null;
      this._initialEventData = null;
      this._selectRect = null;
      this.selecting = false;
      this._lastClickData = null;
      this.isDetached = true;
      this._listeners = /* @__PURE__ */ Object.create(null);
      this._removeTouchMoveWindowListener && this._removeTouchMoveWindowListener();
      this._removeInitialEventListener && this._removeInitialEventListener();
      this._removeEndListener && this._removeEndListener();
      this._onEscListener && this._onEscListener();
      this._removeMoveListener && this._removeMoveListener();
      this._removeKeyUpListener && this._removeKeyUpListener();
      this._removeKeyDownListener && this._removeKeyDownListener();
      this._removeDropFromOutsideListener && this._removeDropFromOutsideListener();
      this._removeDragOverFromOutsideListener && this._removeDragOverFromOutsideListener();
    }
  }, {
    key: "isSelected",
    value: function isSelected2(node) {
      var box = this._selectRect;
      if (!box || !this.selecting) return false;
      return objectsCollide(box, getBoundsForNode(node));
    }
  }, {
    key: "filter",
    value: function filter(items) {
      var box = this._selectRect;
      if (!box || !this.selecting) return [];
      return items.filter(this.isSelected, this);
    }
    // Adds a listener that will call the handler only after the user has pressed on the screen
    // without moving their finger for 250ms.
  }, {
    key: "_addLongPressListener",
    value: function _addLongPressListener(handler, initialEvent) {
      var _this = this;
      var timer = null;
      var removeTouchMoveListener = null;
      var removeTouchEndListener = null;
      var handleTouchStart = function handleTouchStart2(initialEvent2) {
        timer = setTimeout(function() {
          cleanup();
          handler(initialEvent2);
        }, _this.longPressThreshold);
        removeTouchMoveListener = addEventListener("touchmove", function() {
          return cleanup();
        });
        removeTouchEndListener = addEventListener("touchend", function() {
          return cleanup();
        });
      };
      var removeTouchStartListener = addEventListener("touchstart", handleTouchStart);
      var cleanup = function cleanup2() {
        if (timer) {
          clearTimeout(timer);
        }
        if (removeTouchMoveListener) {
          removeTouchMoveListener();
        }
        if (removeTouchEndListener) {
          removeTouchEndListener();
        }
        timer = null;
        removeTouchMoveListener = null;
        removeTouchEndListener = null;
      };
      if (initialEvent) {
        handleTouchStart(initialEvent);
      }
      return function() {
        cleanup();
        removeTouchStartListener();
      };
    }
    // Listen for mousedown and touchstart events. When one is received, disable the other and setup
    // future event handling based on the type of event.
  }, {
    key: "_addInitialEventListener",
    value: function _addInitialEventListener() {
      var _this2 = this;
      var removeMouseDownListener = addEventListener("mousedown", function(e2) {
        _this2._removeInitialEventListener();
        _this2._handleInitialEvent(e2);
        _this2._removeInitialEventListener = addEventListener("mousedown", _this2._handleInitialEvent);
      });
      var removeTouchStartListener = addEventListener("touchstart", function(e2) {
        _this2._removeInitialEventListener();
        _this2._removeInitialEventListener = _this2._addLongPressListener(_this2._handleInitialEvent, e2);
      });
      this._removeInitialEventListener = function() {
        removeMouseDownListener();
        removeTouchStartListener();
      };
    }
  }, {
    key: "_dropFromOutsideListener",
    value: function _dropFromOutsideListener(e2) {
      var _getEventCoordinates = getEventCoordinates(e2), pageX = _getEventCoordinates.pageX, pageY = _getEventCoordinates.pageY, clientX = _getEventCoordinates.clientX, clientY = _getEventCoordinates.clientY;
      this.emit("dropFromOutside", {
        x: pageX,
        y: pageY,
        clientX,
        clientY
      });
      e2.preventDefault();
    }
  }, {
    key: "_dragOverFromOutsideListener",
    value: function _dragOverFromOutsideListener(e2) {
      var _getEventCoordinates2 = getEventCoordinates(e2), pageX = _getEventCoordinates2.pageX, pageY = _getEventCoordinates2.pageY, clientX = _getEventCoordinates2.clientX, clientY = _getEventCoordinates2.clientY;
      this.emit("dragOverFromOutside", {
        x: pageX,
        y: pageY,
        clientX,
        clientY
      });
      e2.preventDefault();
    }
  }, {
    key: "_handleInitialEvent",
    value: function _handleInitialEvent(e2) {
      this._initialEvent = e2;
      if (this.isDetached) {
        return;
      }
      var _getEventCoordinates3 = getEventCoordinates(e2), clientX = _getEventCoordinates3.clientX, clientY = _getEventCoordinates3.clientY, pageX = _getEventCoordinates3.pageX, pageY = _getEventCoordinates3.pageY;
      var node = this.container(), collides, offsetData;
      if (e2.which === 3 || e2.button === 2 || !isOverContainer(node, clientX, clientY)) return;
      if (!this.globalMouse && node && !contains(node, e2.target)) {
        var _normalizeDistance = normalizeDistance(0), top2 = _normalizeDistance.top, left2 = _normalizeDistance.left, bottom2 = _normalizeDistance.bottom, right2 = _normalizeDistance.right;
        offsetData = getBoundsForNode(node);
        collides = objectsCollide({
          top: offsetData.top - top2,
          left: offsetData.left - left2,
          bottom: offsetData.bottom + bottom2,
          right: offsetData.right + right2
        }, {
          top: pageY,
          left: pageX
        });
        if (!collides) return;
      }
      var result = this.emit("beforeSelect", this._initialEventData = {
        isTouch: /^touch/.test(e2.type),
        x: pageX,
        y: pageY,
        clientX,
        clientY
      });
      if (result === false) return;
      switch (e2.type) {
        case "mousedown":
          this._removeEndListener = addEventListener("mouseup", this._handleTerminatingEvent);
          this._onEscListener = addEventListener("keydown", this._handleTerminatingEvent);
          this._removeMoveListener = addEventListener("mousemove", this._handleMoveEvent);
          break;
        case "touchstart":
          this._handleMoveEvent(e2);
          this._removeEndListener = addEventListener("touchend", this._handleTerminatingEvent);
          this._removeMoveListener = addEventListener("touchmove", this._handleMoveEvent);
          break;
      }
    }
    // Check whether provided event target element
    // - is contained within a valid container
  }, {
    key: "_isWithinValidContainer",
    value: function _isWithinValidContainer(e2) {
      var eventTarget = e2.target;
      var containers = this.validContainers;
      if (!containers || !containers.length || !eventTarget) {
        return true;
      }
      return containers.some(function(target) {
        return !!eventTarget.closest(target);
      });
    }
  }, {
    key: "_handleTerminatingEvent",
    value: function _handleTerminatingEvent(e2) {
      var selecting = this.selecting;
      var bounds = this._selectRect;
      if (!selecting && e2.type.includes("key")) {
        e2 = this._initialEvent;
      }
      this.selecting = false;
      this._removeEndListener && this._removeEndListener();
      this._removeMoveListener && this._removeMoveListener();
      this._selectRect = null;
      this._initialEvent = null;
      this._initialEventData = null;
      if (!e2) return;
      var inRoot = !this.container || contains(this.container(), e2.target);
      var isWithinValidContainer = this._isWithinValidContainer(e2);
      if (e2.key === "Escape" || !isWithinValidContainer) {
        return this.emit("reset");
      }
      if (!selecting && inRoot) {
        return this._handleClickEvent(e2);
      }
      if (selecting) return this.emit("select", bounds);
      return this.emit("reset");
    }
  }, {
    key: "_handleClickEvent",
    value: function _handleClickEvent(e2) {
      var _getEventCoordinates4 = getEventCoordinates(e2), pageX = _getEventCoordinates4.pageX, pageY = _getEventCoordinates4.pageY, clientX = _getEventCoordinates4.clientX, clientY = _getEventCoordinates4.clientY;
      var now = (/* @__PURE__ */ new Date()).getTime();
      if (this._lastClickData && now - this._lastClickData.timestamp < clickInterval) {
        this._lastClickData = null;
        return this.emit("doubleClick", {
          x: pageX,
          y: pageY,
          clientX,
          clientY
        });
      }
      this._lastClickData = {
        timestamp: now
      };
      return this.emit("click", {
        x: pageX,
        y: pageY,
        clientX,
        clientY
      });
    }
  }, {
    key: "_handleMoveEvent",
    value: function _handleMoveEvent(e2) {
      if (this._initialEventData === null || this.isDetached) {
        return;
      }
      var _this$_initialEventDa = this._initialEventData, x2 = _this$_initialEventDa.x, y3 = _this$_initialEventDa.y;
      var _getEventCoordinates5 = getEventCoordinates(e2), pageX = _getEventCoordinates5.pageX, pageY = _getEventCoordinates5.pageY;
      var w2 = Math.abs(x2 - pageX);
      var h3 = Math.abs(y3 - pageY);
      var left2 = Math.min(pageX, x2), top2 = Math.min(pageY, y3), old = this.selecting;
      var click = this.isClick(pageX, pageY);
      if (click && !old && !(w2 || h3)) {
        return;
      }
      if (!old && !click) {
        this.emit("selectStart", this._initialEventData);
      }
      if (!click) {
        this.selecting = true;
        this._selectRect = {
          top: top2,
          left: left2,
          x: pageX,
          y: pageY,
          right: left2 + w2,
          bottom: top2 + h3
        };
        this.emit("selecting", this._selectRect);
      }
      e2.preventDefault();
    }
  }, {
    key: "_keyListener",
    value: function _keyListener(e2) {
      this.ctrl = e2.metaKey || e2.ctrlKey;
    }
  }, {
    key: "isClick",
    value: function isClick(pageX, pageY) {
      var _this$_initialEventDa2 = this._initialEventData, x2 = _this$_initialEventDa2.x, y3 = _this$_initialEventDa2.y, isTouch = _this$_initialEventDa2.isTouch;
      return !isTouch && Math.abs(pageX - x2) <= clickTolerance && Math.abs(pageY - y3) <= clickTolerance;
    }
  }]);
})();
function normalizeDistance() {
  var distance = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
  if (_typeof$1(distance) !== "object") distance = {
    top: distance,
    left: distance,
    right: distance,
    bottom: distance
  };
  return distance;
}
function objectsCollide(nodeA, nodeB) {
  var tolerance = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
  var _getBoundsForNode = getBoundsForNode(nodeA), aTop = _getBoundsForNode.top, aLeft = _getBoundsForNode.left, _getBoundsForNode$rig = _getBoundsForNode.right, aRight = _getBoundsForNode$rig === void 0 ? aLeft : _getBoundsForNode$rig, _getBoundsForNode$bot = _getBoundsForNode.bottom, aBottom = _getBoundsForNode$bot === void 0 ? aTop : _getBoundsForNode$bot;
  var _getBoundsForNode2 = getBoundsForNode(nodeB), bTop = _getBoundsForNode2.top, bLeft = _getBoundsForNode2.left, _getBoundsForNode2$ri = _getBoundsForNode2.right, bRight = _getBoundsForNode2$ri === void 0 ? bLeft : _getBoundsForNode2$ri, _getBoundsForNode2$bo = _getBoundsForNode2.bottom, bBottom = _getBoundsForNode2$bo === void 0 ? bTop : _getBoundsForNode2$bo;
  return !// 'a' bottom doesn't touch 'b' top
  (aBottom - tolerance < bTop || // 'a' top doesn't touch 'b' bottom
  aTop + tolerance > bBottom || // 'a' right doesn't touch 'b' left
  aRight - tolerance < bLeft || // 'a' left doesn't touch 'b' right
  aLeft + tolerance > bRight);
}
function getBoundsForNode(node) {
  if (!node.getBoundingClientRect) return node;
  var rect = node.getBoundingClientRect(), left2 = rect.left + pageOffset("left"), top2 = rect.top + pageOffset("top");
  return {
    top: top2,
    left: left2,
    right: (node.offsetWidth || 0) + left2,
    bottom: (node.offsetHeight || 0) + top2
  };
}
function pageOffset(dir) {
  if (dir === "left") return window.pageXOffset || document.body.scrollLeft || 0;
  if (dir === "top") return window.pageYOffset || document.body.scrollTop || 0;
}
var BackgroundCells = /* @__PURE__ */ (function(_React$Component) {
  function BackgroundCells2(props, context2) {
    var _this;
    _classCallCheck(this, BackgroundCells2);
    _this = _callSuper(this, BackgroundCells2, [props, context2]);
    _this.state = {
      selecting: false
    };
    _this.containerRef = /* @__PURE__ */ reactExports.createRef();
    return _this;
  }
  _inherits(BackgroundCells2, _React$Component);
  return _createClass(BackgroundCells2, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      this.props.selectable && this._selectable();
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      this._teardownSelectable();
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate(prevProps) {
      if (!prevProps.selectable && this.props.selectable) this._selectable();
      if (prevProps.selectable && !this.props.selectable) this._teardownSelectable();
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props = this.props, range2 = _this$props.range, getNow2 = _this$props.getNow, getters = _this$props.getters, currentDate = _this$props.date, Wrapper = _this$props.components.dateCellWrapper, localizer2 = _this$props.localizer;
      var _this$state = this.state, selecting = _this$state.selecting, startIdx = _this$state.startIdx, endIdx = _this$state.endIdx;
      var current = getNow2();
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row-bg",
        ref: this.containerRef
      }, range2.map(function(date2, index2) {
        var selected = selecting && index2 >= startIdx && index2 <= endIdx;
        var _getters$dayProp = getters.dayProp(date2), className = _getters$dayProp.className, style2 = _getters$dayProp.style;
        return /* @__PURE__ */ reactExports.createElement(Wrapper, {
          key: index2,
          value: date2,
          range: range2
        }, /* @__PURE__ */ reactExports.createElement("div", {
          style: style2,
          className: clsx("rbc-day-bg", className, selected && "rbc-selected-cell", localizer2.isSameDate(date2, current) && "rbc-today", currentDate && localizer2.neq(currentDate, date2, "month") && "rbc-off-range-bg")
        }));
      }));
    }
  }, {
    key: "_selectable",
    value: function _selectable() {
      var _this2 = this;
      var node = this.containerRef.current;
      var selector = this._selector = new Selection$1(this.props.container, {
        longPressThreshold: this.props.longPressThreshold
      });
      var selectorClicksHandler = function selectorClicksHandler2(point, actionType) {
        if (!isEvent(node, point) && !isShowMore(node, point)) {
          var rowBox = getBoundsForNode(node);
          var _this2$props = _this2.props, range2 = _this2$props.range, rtl = _this2$props.rtl;
          if (pointInBox(rowBox, point)) {
            var currentCell = getSlotAtX(rowBox, point.x, rtl, range2.length);
            _this2._selectSlot({
              startIdx: currentCell,
              endIdx: currentCell,
              action: actionType,
              box: point
            });
          }
        }
        _this2._initial = {};
        _this2.setState({
          selecting: false
        });
      };
      selector.on("selecting", function(box) {
        var _this2$props2 = _this2.props, range2 = _this2$props2.range, rtl = _this2$props2.rtl;
        var startIdx = -1;
        var endIdx = -1;
        if (!_this2.state.selecting) {
          notify(_this2.props.onSelectStart, [box]);
          _this2._initial = {
            x: box.x,
            y: box.y
          };
        }
        if (selector.isSelected(node)) {
          var nodeBox = getBoundsForNode(node);
          var _dateCellSelection = dateCellSelection(_this2._initial, nodeBox, box, range2.length, rtl);
          startIdx = _dateCellSelection.startIdx;
          endIdx = _dateCellSelection.endIdx;
        }
        _this2.setState({
          selecting: true,
          startIdx,
          endIdx
        });
      });
      selector.on("beforeSelect", function(box) {
        if (_this2.props.selectable !== "ignoreEvents") return;
        return !isEvent(_this2.containerRef.current, box);
      });
      selector.on("click", function(point) {
        return selectorClicksHandler(point, "click");
      });
      selector.on("doubleClick", function(point) {
        return selectorClicksHandler(point, "doubleClick");
      });
      selector.on("select", function(bounds) {
        _this2._selectSlot(_objectSpread2(_objectSpread2({}, _this2.state), {}, {
          action: "select",
          bounds
        }));
        _this2._initial = {};
        _this2.setState({
          selecting: false
        });
        notify(_this2.props.onSelectEnd, [_this2.state]);
      });
    }
  }, {
    key: "_teardownSelectable",
    value: function _teardownSelectable() {
      if (!this._selector) return;
      this._selector.teardown();
      this._selector = null;
    }
  }, {
    key: "_selectSlot",
    value: function _selectSlot(_ref) {
      var endIdx = _ref.endIdx, startIdx = _ref.startIdx, action = _ref.action, bounds = _ref.bounds, box = _ref.box;
      if (endIdx !== -1 && startIdx !== -1) this.props.onSelectSlot && this.props.onSelectSlot({
        start: startIdx,
        end: endIdx,
        action,
        bounds,
        box,
        resourceId: this.props.resourceId
      });
    }
  }]);
})(reactExports.Component);
var EventRowMixin$1 = {
  propTypes: {
    slotMetrics: propTypesExports.object.isRequired,
    selected: propTypesExports.object,
    isAllDay: propTypesExports.bool,
    accessors: propTypesExports.object.isRequired,
    localizer: propTypesExports.object.isRequired,
    components: propTypesExports.object.isRequired,
    getters: propTypesExports.object.isRequired,
    onSelect: propTypesExports.func,
    onDoubleClick: propTypesExports.func,
    onKeyPress: propTypesExports.func
  },
  defaultProps: {
    segments: [],
    selected: {}
  },
  renderEvent: function renderEvent(props, event) {
    var selected = props.selected;
    props.isAllDay;
    var accessors2 = props.accessors, getters = props.getters, onSelect = props.onSelect, onDoubleClick = props.onDoubleClick, onKeyPress = props.onKeyPress, localizer2 = props.localizer, slotMetrics = props.slotMetrics, components2 = props.components, resizable = props.resizable;
    var continuesPrior2 = slotMetrics.continuesPrior(event);
    var continuesAfter2 = slotMetrics.continuesAfter(event);
    return /* @__PURE__ */ reactExports.createElement(EventCell$1, {
      event,
      getters,
      localizer: localizer2,
      accessors: accessors2,
      components: components2,
      onSelect,
      onDoubleClick,
      onKeyPress,
      continuesPrior: continuesPrior2,
      continuesAfter: continuesAfter2,
      slotStart: slotMetrics.first,
      slotEnd: slotMetrics.last,
      selected: isSelected(event, selected),
      resizable
    });
  },
  renderSpan: function renderSpan(slots, len, key) {
    var content = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : " ";
    var per = Math.abs(len) / slots * 100 + "%";
    return /* @__PURE__ */ reactExports.createElement("div", {
      key,
      className: "rbc-row-segment",
      style: {
        WebkitFlexBasis: per,
        flexBasis: per,
        maxWidth: per
      }
    }, content);
  }
};
var EventRow$1 = /* @__PURE__ */ (function(_React$Component) {
  function EventRow2() {
    _classCallCheck(this, EventRow2);
    return _callSuper(this, EventRow2, arguments);
  }
  _inherits(EventRow2, _React$Component);
  return _createClass(EventRow2, [{
    key: "render",
    value: function render() {
      var _this = this;
      var _this$props = this.props, segments = _this$props.segments, slots = _this$props.slotMetrics.slots, className = _this$props.className;
      var lastEnd = 1;
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx(className, "rbc-row")
      }, segments.reduce(function(row, _ref, li) {
        var event = _ref.event, left2 = _ref.left, right2 = _ref.right, span = _ref.span;
        var key = "_lvl_" + li;
        var gap = left2 - lastEnd;
        var content = EventRowMixin$1.renderEvent(_this.props, event);
        if (gap) row.push(EventRowMixin$1.renderSpan(slots, gap, "".concat(key, "_gap")));
        row.push(EventRowMixin$1.renderSpan(slots, span, key, content));
        lastEnd = right2 + 1;
        return row;
      }, []));
    }
  }]);
})(reactExports.Component);
EventRow$1.defaultProps = _objectSpread2({}, EventRowMixin$1.defaultProps);
function endOfRange(_ref) {
  var dateRange = _ref.dateRange, _ref$unit = _ref.unit, unit = _ref$unit === void 0 ? "day" : _ref$unit, localizer2 = _ref.localizer;
  return {
    first: dateRange[0],
    last: localizer2.add(dateRange[dateRange.length - 1], 1, unit)
  };
}
function eventSegments(event, range2, accessors2, localizer2) {
  var _endOfRange = endOfRange({
    dateRange: range2,
    localizer: localizer2
  }), first = _endOfRange.first, last = _endOfRange.last;
  var slots = localizer2.diff(first, last, "day");
  var start2 = localizer2.max(localizer2.startOf(accessors2.start(event), "day"), first);
  var end2 = localizer2.min(localizer2.ceil(accessors2.end(event), "day"), last);
  var padding = findIndexExports(range2, function(x2) {
    return localizer2.isSameDate(x2, start2);
  });
  var span = localizer2.diff(start2, end2, "day");
  span = Math.min(span, slots);
  span = Math.max(span - localizer2.segmentOffset, 1);
  return {
    event,
    span,
    left: padding + 1,
    right: Math.max(padding + span, 1)
  };
}
function eventLevels$1(rowSegments) {
  var limit = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Infinity;
  var i2, j, seg, levels = [], extra = [];
  for (i2 = 0; i2 < rowSegments.length; i2++) {
    seg = rowSegments[i2];
    for (j = 0; j < levels.length; j++) if (!segsOverlap(seg, levels[j])) break;
    if (j >= limit) {
      extra.push(seg);
    } else {
      (levels[j] || (levels[j] = [])).push(seg);
    }
  }
  for (i2 = 0; i2 < levels.length; i2++) {
    levels[i2].sort(function(a3, b2) {
      return a3.left - b2.left;
    });
  }
  return {
    levels,
    extra
  };
}
function inRange(e2, start2, end2, accessors2, localizer2) {
  var event = {
    start: accessors2.start(e2),
    end: accessors2.end(e2)
  };
  var range2 = {
    start: start2,
    end: end2
  };
  return localizer2.inEventRange({
    event,
    range: range2
  });
}
function segsOverlap(seg, otherSegs) {
  return otherSegs.some(function(otherSeg) {
    return otherSeg.left <= seg.right && otherSeg.right >= seg.left;
  });
}
function sortWeekEvents(events2, accessors2, localizer2) {
  var base = _toConsumableArray(events2);
  var multiDayEvents = [];
  var standardEvents = [];
  base.forEach(function(event) {
    var startCheck = accessors2.start(event);
    var endCheck = accessors2.end(event);
    if (localizer2.daySpan(startCheck, endCheck) > 1) {
      multiDayEvents.push(event);
    } else {
      standardEvents.push(event);
    }
  });
  var multiSorted = multiDayEvents.sort(function(a3, b2) {
    return sortEvents(a3, b2, accessors2, localizer2);
  });
  var standardSorted = standardEvents.sort(function(a3, b2) {
    return sortEvents(a3, b2, accessors2, localizer2);
  });
  return [].concat(_toConsumableArray(multiSorted), _toConsumableArray(standardSorted));
}
function sortEvents(eventA, eventB, accessors2, localizer2) {
  var evtA = {
    start: accessors2.start(eventA),
    end: accessors2.end(eventA),
    allDay: accessors2.allDay(eventA)
  };
  var evtB = {
    start: accessors2.start(eventB),
    end: accessors2.end(eventB),
    allDay: accessors2.allDay(eventB)
  };
  return localizer2.sortEvents({
    evtA,
    evtB
  });
}
var isSegmentInSlot$1 = function isSegmentInSlot(seg, slot) {
  return seg.left <= slot && seg.right >= slot;
};
var eventsInSlot = function eventsInSlot2(segments, slot) {
  return segments.filter(function(seg) {
    return isSegmentInSlot$1(seg, slot);
  }).map(function(seg) {
    return seg.event;
  });
};
var EventEndingRow = /* @__PURE__ */ (function(_React$Component) {
  function EventEndingRow2() {
    _classCallCheck(this, EventEndingRow2);
    return _callSuper(this, EventEndingRow2, arguments);
  }
  _inherits(EventEndingRow2, _React$Component);
  return _createClass(EventEndingRow2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, segments = _this$props.segments, slots = _this$props.slotMetrics.slots;
      var rowSegments = eventLevels$1(segments).levels[0];
      var current = 1, lastEnd = 1, row = [];
      while (current <= slots) {
        var key = "_lvl_" + current;
        var _ref = rowSegments.filter(function(seg) {
          return isSegmentInSlot$1(seg, current);
        })[0] || {}, event = _ref.event, left2 = _ref.left, right2 = _ref.right, span = _ref.span;
        if (!event) {
          var hiddenEvents = this.getHiddenEventsForSlot(segments, current);
          if (hiddenEvents.length > 0) {
            var _gap = current - lastEnd;
            if (_gap) {
              row.push(EventRowMixin$1.renderSpan(slots, _gap, key + "_gap"));
            }
            row.push(EventRowMixin$1.renderSpan(slots, 1, key, this.renderShowMore(segments, current)));
            lastEnd = current = current + 1;
            continue;
          }
          current++;
          continue;
        }
        var gap = Math.max(0, left2 - lastEnd);
        if (this.canRenderSlotEvent(left2, span)) {
          var content = EventRowMixin$1.renderEvent(this.props, event);
          if (gap) {
            row.push(EventRowMixin$1.renderSpan(slots, gap, key + "_gap"));
          }
          row.push(EventRowMixin$1.renderSpan(slots, span, key, content));
          lastEnd = current = right2 + 1;
        } else {
          if (gap) {
            row.push(EventRowMixin$1.renderSpan(slots, gap, key + "_gap"));
          }
          row.push(EventRowMixin$1.renderSpan(slots, 1, key, this.renderShowMore(segments, current)));
          lastEnd = current = current + 1;
        }
      }
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row"
      }, row);
    }
    // New helper method to find hidden events for a slot
  }, {
    key: "getHiddenEventsForSlot",
    value: function getHiddenEventsForSlot(segments, slot) {
      var allEventsInSlot = eventsInSlot(segments, slot);
      var rowSegments = eventLevels$1(segments).levels[0];
      var visibleEventsInSlot = rowSegments.filter(function(seg) {
        return isSegmentInSlot$1(seg, slot);
      }).map(function(seg) {
        return seg.event;
      });
      return allEventsInSlot.filter(function(event) {
        return !visibleEventsInSlot.some(function(visEvent) {
          return visEvent === event;
        });
      });
    }
  }, {
    key: "canRenderSlotEvent",
    value: function canRenderSlotEvent(slot, span) {
      var segments = this.props.segments;
      return rangeExports(slot, slot + span).every(function(s3) {
        var count = eventsInSlot(segments, s3).length;
        return count === 1;
      });
    }
  }, {
    key: "renderShowMore",
    value: function renderShowMore(segments, slot) {
      var _this = this;
      var _this$props2 = this.props, localizer2 = _this$props2.localizer, slotMetrics = _this$props2.slotMetrics, components2 = _this$props2.components;
      var events2 = slotMetrics.getEventsForSlot(slot);
      var remainingEvents = eventsInSlot(segments, slot);
      var count = remainingEvents.length;
      if (components2 !== null && components2 !== void 0 && components2.showMore) {
        var ShowMore = components2.showMore;
        var slotDate = slotMetrics.getDateForSlot(slot - 1);
        return count ? /* @__PURE__ */ reactExports.createElement(ShowMore, {
          localizer: localizer2,
          slotDate,
          slot,
          count,
          events: events2,
          remainingEvents
        }) : false;
      }
      return count ? /* @__PURE__ */ reactExports.createElement("button", {
        type: "button",
        key: "sm_" + slot,
        className: clsx("rbc-button-link", "rbc-show-more"),
        onClick: function onClick(e2) {
          return _this.showMore(slot, e2);
        }
      }, localizer2.messages.showMore(count, remainingEvents, events2)) : false;
    }
  }, {
    key: "showMore",
    value: function showMore2(slot, e2) {
      e2.preventDefault();
      e2.stopPropagation();
      this.props.onShowMore(slot, e2.target);
    }
  }]);
})(reactExports.Component);
EventEndingRow.defaultProps = _objectSpread2({}, EventRowMixin$1.defaultProps);
var ScrollableWeekWrapper = function ScrollableWeekWrapper2(_ref) {
  var children = _ref.children;
  return /* @__PURE__ */ reactExports.createElement("div", {
    className: "rbc-row-content-scroll-container"
  }, children);
};
var isSegmentInSlot2 = function isSegmentInSlot3(seg, slot) {
  return seg.left <= slot && seg.right >= slot;
};
var isEqual = function isEqual2(a3, b2) {
  return a3[0].range === b2[0].range && a3[0].events === b2[0].events;
};
function getSlotMetrics$1() {
  return memoizeOne(function(options) {
    var range2 = options.range, events2 = options.events, maxRows = options.maxRows, minRows = options.minRows, accessors2 = options.accessors, localizer2 = options.localizer;
    var _endOfRange = endOfRange({
      dateRange: range2,
      localizer: localizer2
    }), first = _endOfRange.first, last = _endOfRange.last;
    var segments = events2.map(function(evt) {
      return eventSegments(evt, range2, accessors2, localizer2);
    });
    var _eventLevels = eventLevels$1(segments, Math.max(maxRows - 1, 1)), levels = _eventLevels.levels, extra = _eventLevels.extra;
    var minEventRows = extra.length > 0 ? minRows - 1 : minRows;
    while (levels.length < minEventRows) levels.push([]);
    return {
      first,
      last,
      levels,
      extra,
      range: range2,
      slots: range2.length,
      clone: function clone(args) {
        var metrics = getSlotMetrics$1();
        return metrics(_objectSpread2(_objectSpread2({}, options), args));
      },
      getDateForSlot: function getDateForSlot(slotNumber) {
        return range2[slotNumber];
      },
      getSlotForDate: function getSlotForDate(date2) {
        return range2.find(function(r2) {
          return localizer2.isSameDate(r2, date2);
        });
      },
      getEventsForSlot: function getEventsForSlot(slot) {
        return segments.filter(function(seg) {
          return isSegmentInSlot2(seg, slot);
        }).map(function(seg) {
          return seg.event;
        });
      },
      continuesPrior: function continuesPrior2(event) {
        return localizer2.continuesPrior(accessors2.start(event), first);
      },
      continuesAfter: function continuesAfter2(event) {
        var start2 = accessors2.start(event);
        var end2 = accessors2.end(event);
        return localizer2.continuesAfter(start2, end2, last);
      }
    };
  }, isEqual);
}
var DateContentRow = /* @__PURE__ */ (function(_React$Component) {
  function DateContentRow2() {
    var _this;
    _classCallCheck(this, DateContentRow2);
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _callSuper(this, DateContentRow2, [].concat(args));
    _this.handleSelectSlot = function(slot) {
      var _this$props = _this.props, range2 = _this$props.range, onSelectSlot = _this$props.onSelectSlot;
      onSelectSlot(range2.slice(slot.start, slot.end + 1), slot);
    };
    _this.handleShowMore = function(slot, target) {
      var _this$props2 = _this.props, range2 = _this$props2.range, onShowMore = _this$props2.onShowMore;
      var metrics = _this.slotMetrics(_this.props);
      var row = qsa(_this.containerRef.current, ".rbc-row-bg")[0];
      var cell;
      if (row) cell = row.children[slot - 1];
      var events2 = metrics.getEventsForSlot(slot);
      onShowMore(events2, range2[slot - 1], cell, slot, target);
    };
    _this.getContainer = function() {
      var container = _this.props.container;
      return container ? container() : _this.containerRef.current;
    };
    _this.renderHeadingCell = function(date2, index2) {
      var _this$props3 = _this.props, renderHeader = _this$props3.renderHeader, getNow2 = _this$props3.getNow, localizer2 = _this$props3.localizer;
      return renderHeader({
        date: date2,
        key: "header_".concat(index2),
        className: clsx("rbc-date-cell", localizer2.isSameDate(date2, getNow2()) && "rbc-now")
      });
    };
    _this.renderDummy = function() {
      var _this$props4 = _this.props, className = _this$props4.className, range2 = _this$props4.range, renderHeader = _this$props4.renderHeader, showAllEvents = _this$props4.showAllEvents;
      return /* @__PURE__ */ reactExports.createElement("div", {
        className,
        ref: _this.containerRef
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx("rbc-row-content", showAllEvents && "rbc-row-content-scrollable")
      }, renderHeader && /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row",
        ref: _this.headingRowRef
      }, range2.map(_this.renderHeadingCell)), /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row",
        ref: _this.eventRowRef
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row-segment"
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-event"
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-event-content"
      }, " "))))));
    };
    _this.containerRef = /* @__PURE__ */ reactExports.createRef();
    _this.headingRowRef = /* @__PURE__ */ reactExports.createRef();
    _this.eventRowRef = /* @__PURE__ */ reactExports.createRef();
    _this.slotMetrics = getSlotMetrics$1();
    return _this;
  }
  _inherits(DateContentRow2, _React$Component);
  return _createClass(DateContentRow2, [{
    key: "getRowLimit",
    value: function getRowLimit() {
      var _this$headingRowRef;
      var eventHeight = height(this.eventRowRef.current);
      var headingHeight = (_this$headingRowRef = this.headingRowRef) !== null && _this$headingRowRef !== void 0 && _this$headingRowRef.current ? height(this.headingRowRef.current) : 0;
      var eventSpace = height(this.containerRef.current) - headingHeight;
      return Math.max(Math.floor(eventSpace / eventHeight), 1);
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props5 = this.props, date2 = _this$props5.date, rtl = _this$props5.rtl, range2 = _this$props5.range, className = _this$props5.className, selected = _this$props5.selected, selectable = _this$props5.selectable, renderForMeasure = _this$props5.renderForMeasure, accessors2 = _this$props5.accessors, getters = _this$props5.getters, components2 = _this$props5.components, getNow2 = _this$props5.getNow, renderHeader = _this$props5.renderHeader, onSelect = _this$props5.onSelect, localizer2 = _this$props5.localizer, onSelectStart = _this$props5.onSelectStart, onSelectEnd = _this$props5.onSelectEnd, onDoubleClick = _this$props5.onDoubleClick, onKeyPress = _this$props5.onKeyPress, resourceId = _this$props5.resourceId, longPressThreshold = _this$props5.longPressThreshold, isAllDay = _this$props5.isAllDay, resizable = _this$props5.resizable, showAllEvents = _this$props5.showAllEvents;
      if (renderForMeasure) return this.renderDummy();
      var metrics = this.slotMetrics(this.props);
      var levels = metrics.levels, extra = metrics.extra;
      var ScrollableWeekComponent = showAllEvents ? ScrollableWeekWrapper : NoopWrapper;
      var WeekWrapper2 = components2.weekWrapper;
      var eventRowProps = {
        selected,
        accessors: accessors2,
        getters,
        localizer: localizer2,
        components: components2,
        onSelect,
        onDoubleClick,
        onKeyPress,
        resourceId,
        slotMetrics: metrics,
        resizable
      };
      return /* @__PURE__ */ reactExports.createElement("div", {
        className,
        role: "rowgroup",
        ref: this.containerRef
      }, /* @__PURE__ */ reactExports.createElement(BackgroundCells, {
        localizer: localizer2,
        date: date2,
        getNow: getNow2,
        rtl,
        range: range2,
        selectable,
        container: this.getContainer,
        getters,
        onSelectStart,
        onSelectEnd,
        onSelectSlot: this.handleSelectSlot,
        components: components2,
        longPressThreshold,
        resourceId
      }), /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx("rbc-row-content", showAllEvents && "rbc-row-content-scrollable"),
        role: "row"
      }, renderHeader && /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row ",
        ref: this.headingRowRef
      }, range2.map(this.renderHeadingCell)), /* @__PURE__ */ reactExports.createElement(ScrollableWeekComponent, null, /* @__PURE__ */ reactExports.createElement(WeekWrapper2, Object.assign({
        isAllDay
      }, eventRowProps, {
        rtl: this.props.rtl
      }), levels.map(function(segs, idx) {
        return /* @__PURE__ */ reactExports.createElement(EventRow$1, Object.assign({
          key: idx,
          segments: segs
        }, eventRowProps));
      }), !!extra.length && /* @__PURE__ */ reactExports.createElement(EventEndingRow, Object.assign({
        segments: extra,
        onShowMore: this.handleShowMore
      }, eventRowProps))))));
    }
  }]);
})(reactExports.Component);
DateContentRow.defaultProps = {
  minRows: 0,
  maxRows: Infinity
};
var Header = function Header2(_ref) {
  var label = _ref.label;
  return /* @__PURE__ */ reactExports.createElement("span", {
    role: "columnheader",
    "aria-sort": "none"
  }, label);
};
var DateHeader = function DateHeader2(_ref) {
  var label = _ref.label, drilldownView = _ref.drilldownView, onDrillDown = _ref.onDrillDown;
  if (!drilldownView) {
    return /* @__PURE__ */ reactExports.createElement("span", null, label);
  }
  return /* @__PURE__ */ reactExports.createElement("button", {
    type: "button",
    className: "rbc-button-link",
    onClick: onDrillDown
  }, label);
};
var _excluded$6 = ["date", "className"];
var eventsForWeek = function eventsForWeek2(evts, start2, end2, accessors2, localizer2) {
  return evts.filter(function(e2) {
    return inRange(e2, start2, end2, accessors2, localizer2);
  });
};
var MonthView = /* @__PURE__ */ (function(_React$Component) {
  function MonthView2() {
    var _this;
    _classCallCheck(this, MonthView2);
    for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
      _args[_key] = arguments[_key];
    }
    _this = _callSuper(this, MonthView2, [].concat(_args));
    _this.getContainer = function() {
      return _this.containerRef.current;
    };
    _this.renderWeek = function(week, weekIdx) {
      var _this$props = _this.props, events2 = _this$props.events, components2 = _this$props.components, selectable = _this$props.selectable, getNow2 = _this$props.getNow, selected = _this$props.selected, date2 = _this$props.date, localizer2 = _this$props.localizer, longPressThreshold = _this$props.longPressThreshold, accessors2 = _this$props.accessors, getters = _this$props.getters, showAllEvents = _this$props.showAllEvents;
      var _this$state = _this.state, needLimitMeasure = _this$state.needLimitMeasure, rowLimit = _this$state.rowLimit;
      var weeksEvents = eventsForWeek(_toConsumableArray(events2), week[0], week[week.length - 1], accessors2, localizer2);
      var sorted = sortWeekEvents(weeksEvents, accessors2, localizer2);
      return /* @__PURE__ */ reactExports.createElement(DateContentRow, {
        key: weekIdx,
        ref: weekIdx === 0 ? _this.slotRowRef : void 0,
        container: _this.getContainer,
        className: "rbc-month-row",
        getNow: getNow2,
        date: date2,
        range: week,
        events: sorted,
        maxRows: showAllEvents ? Infinity : rowLimit,
        selected,
        selectable,
        components: components2,
        accessors: accessors2,
        getters,
        localizer: localizer2,
        renderHeader: _this.readerDateHeading,
        renderForMeasure: needLimitMeasure,
        onShowMore: _this.handleShowMore,
        onSelect: _this.handleSelectEvent,
        onDoubleClick: _this.handleDoubleClickEvent,
        onKeyPress: _this.handleKeyPressEvent,
        onSelectSlot: _this.handleSelectSlot,
        longPressThreshold,
        rtl: _this.props.rtl,
        resizable: _this.props.resizable,
        showAllEvents
      });
    };
    _this.readerDateHeading = function(_ref) {
      var date2 = _ref.date, className = _ref.className, props = _objectWithoutProperties(_ref, _excluded$6);
      var _this$props2 = _this.props, currentDate = _this$props2.date, getDrilldownView = _this$props2.getDrilldownView, localizer2 = _this$props2.localizer;
      var isOffRange = localizer2.neq(currentDate, date2, "month");
      var isCurrent = localizer2.isSameDate(date2, currentDate);
      var drilldownView = getDrilldownView(date2);
      var label = localizer2.format(date2, "dateFormat");
      var DateHeaderComponent = _this.props.components.dateHeader || DateHeader;
      return /* @__PURE__ */ reactExports.createElement("div", Object.assign({}, props, {
        className: clsx(className, isOffRange && "rbc-off-range", isCurrent && "rbc-current"),
        role: "cell"
      }), /* @__PURE__ */ reactExports.createElement(DateHeaderComponent, {
        label,
        date: date2,
        drilldownView,
        isOffRange,
        onDrillDown: function onDrillDown(e2) {
          return _this.handleHeadingClick(date2, drilldownView, e2);
        }
      }));
    };
    _this.handleSelectSlot = function(range2, slotInfo) {
      _this._pendingSelection = _this._pendingSelection.concat(range2);
      clearTimeout(_this._selectTimer);
      _this._selectTimer = setTimeout(function() {
        return _this.selectDates(slotInfo);
      });
    };
    _this.handleHeadingClick = function(date2, view, e2) {
      e2.preventDefault();
      _this.clearSelection();
      notify(_this.props.onDrillDown, [date2, view]);
    };
    _this.handleSelectEvent = function() {
      _this.clearSelection();
      for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      notify(_this.props.onSelectEvent, args);
    };
    _this.handleDoubleClickEvent = function() {
      _this.clearSelection();
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      notify(_this.props.onDoubleClickEvent, args);
    };
    _this.handleKeyPressEvent = function() {
      _this.clearSelection();
      for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        args[_key4] = arguments[_key4];
      }
      notify(_this.props.onKeyPressEvent, args);
    };
    _this.handleShowMore = function(events2, date2, cell, slot, target) {
      var _this$props3 = _this.props, popup = _this$props3.popup, onDrillDown = _this$props3.onDrillDown, onShowMore = _this$props3.onShowMore, getDrilldownView = _this$props3.getDrilldownView, doShowMoreDrillDown = _this$props3.doShowMoreDrillDown;
      _this.clearSelection();
      if (popup) {
        var position$1 = position(cell, _this.containerRef.current);
        _this.setState({
          overlay: {
            date: date2,
            events: events2,
            position: position$1,
            target
          }
        });
      } else if (doShowMoreDrillDown) {
        notify(onDrillDown, [date2, getDrilldownView(date2) || views.DAY]);
      }
      notify(onShowMore, [events2, date2, slot]);
    };
    _this.overlayDisplay = function() {
      _this.setState({
        overlay: null
      });
    };
    _this.state = {
      rowLimit: 5,
      needLimitMeasure: true,
      date: null
    };
    _this.containerRef = /* @__PURE__ */ reactExports.createRef();
    _this.slotRowRef = /* @__PURE__ */ reactExports.createRef();
    _this._bgRows = [];
    _this._pendingSelection = [];
    return _this;
  }
  _inherits(MonthView2, _React$Component);
  return _createClass(MonthView2, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      var _this2 = this;
      var running;
      if (this.state.needLimitMeasure) this.measureRowLimit(this.props);
      window.addEventListener("resize", this._resizeListener = function() {
        if (!running) {
          request(function() {
            running = false;
            _this2.setState({
              needLimitMeasure: true
            });
          });
        }
      }, false);
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate() {
      if (this.state.needLimitMeasure) this.measureRowLimit(this.props);
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      window.removeEventListener("resize", this._resizeListener, false);
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props4 = this.props, date2 = _this$props4.date, localizer2 = _this$props4.localizer, className = _this$props4.className, month2 = localizer2.visibleDays(date2, localizer2), weeks = chunkExports(month2, 7);
      this._weekCount = weeks.length;
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx("rbc-month-view", className),
        role: "table",
        "aria-label": "Month View",
        ref: this.containerRef
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-row rbc-month-header",
        role: "row"
      }, this.renderHeaders(weeks[0])), weeks.map(this.renderWeek), this.props.popup && this.renderOverlay());
    }
  }, {
    key: "renderHeaders",
    value: function renderHeaders(row) {
      var _this$props5 = this.props, localizer2 = _this$props5.localizer, components2 = _this$props5.components;
      var first = row[0];
      var last = row[row.length - 1];
      var HeaderComponent = components2.header || Header;
      return localizer2.range(first, last, "day").map(function(day2, idx) {
        return /* @__PURE__ */ reactExports.createElement("div", {
          key: "header_" + idx,
          className: "rbc-header"
        }, /* @__PURE__ */ reactExports.createElement(HeaderComponent, {
          date: day2,
          localizer: localizer2,
          label: localizer2.format(day2, "weekdayFormat")
        }));
      });
    }
  }, {
    key: "renderOverlay",
    value: function renderOverlay() {
      var _this$state$overlay, _this$state2, _this3 = this;
      var overlay = (_this$state$overlay = (_this$state2 = this.state) === null || _this$state2 === void 0 ? void 0 : _this$state2.overlay) !== null && _this$state$overlay !== void 0 ? _this$state$overlay : {};
      var _this$props6 = this.props, accessors2 = _this$props6.accessors, localizer2 = _this$props6.localizer, components2 = _this$props6.components, getters = _this$props6.getters, selected = _this$props6.selected, popupOffset = _this$props6.popupOffset, handleDragStart = _this$props6.handleDragStart;
      var onHide2 = function onHide22() {
        return _this3.setState({
          overlay: null
        });
      };
      return /* @__PURE__ */ reactExports.createElement(PopOverlay, {
        overlay,
        accessors: accessors2,
        localizer: localizer2,
        components: components2,
        getters,
        selected,
        popupOffset,
        ref: this.containerRef,
        handleKeyPressEvent: this.handleKeyPressEvent,
        handleSelectEvent: this.handleSelectEvent,
        handleDoubleClickEvent: this.handleDoubleClickEvent,
        handleDragStart,
        show: !!overlay.position,
        overlayDisplay: this.overlayDisplay,
        onHide: onHide2
      });
    }
  }, {
    key: "measureRowLimit",
    value: function measureRowLimit() {
      this.setState({
        needLimitMeasure: false,
        rowLimit: this.slotRowRef.current.getRowLimit()
      });
    }
  }, {
    key: "selectDates",
    value: function selectDates(slotInfo) {
      var slots = this._pendingSelection.slice();
      this._pendingSelection = [];
      slots.sort(function(a3, b2) {
        return +a3 - +b2;
      });
      var start2 = new Date(slots[0]);
      var end2 = new Date(slots[slots.length - 1]);
      end2.setDate(slots[slots.length - 1].getDate() + 1);
      notify(this.props.onSelectSlot, {
        slots,
        start: start2,
        end: end2,
        action: slotInfo.action,
        bounds: slotInfo.bounds,
        box: slotInfo.box
      });
    }
  }, {
    key: "clearSelection",
    value: function clearSelection() {
      clearTimeout(this._selectTimer);
      this._pendingSelection = [];
    }
  }], [{
    key: "getDerivedStateFromProps",
    value: function getDerivedStateFromProps(_ref2, state) {
      var date2 = _ref2.date, localizer2 = _ref2.localizer;
      return {
        date: date2,
        needLimitMeasure: localizer2.neq(date2, state.date, "month")
      };
    }
  }]);
})(reactExports.Component);
MonthView.range = function(date2, _ref3) {
  var localizer2 = _ref3.localizer;
  var start2 = localizer2.firstVisibleDay(date2, localizer2);
  var end2 = localizer2.lastVisibleDay(date2, localizer2);
  return {
    start: start2,
    end: end2
  };
};
MonthView.navigate = function(date2, action, _ref4) {
  var localizer2 = _ref4.localizer;
  switch (action) {
    case navigate.PREVIOUS:
      return localizer2.add(date2, -1, "month");
    case navigate.NEXT:
      return localizer2.add(date2, 1, "month");
    default:
      return date2;
  }
};
MonthView.title = function(date2, _ref5) {
  var localizer2 = _ref5.localizer;
  return localizer2.format(date2, "monthHeaderFormat");
};
var getKey2 = function getKey22(_ref) {
  var min2 = _ref.min, max2 = _ref.max, step = _ref.step, slots = _ref.slots, localizer2 = _ref.localizer;
  return "".concat(+localizer2.startOf(min2, "minutes")) + "".concat(+localizer2.startOf(max2, "minutes")) + "".concat(step, "-").concat(slots);
};
function getSlotMetrics(_ref2) {
  var start2 = _ref2.min, end2 = _ref2.max, step = _ref2.step, timeslots = _ref2.timeslots, localizer2 = _ref2.localizer;
  var key = getKey2({
    step,
    localizer: localizer2
  });
  var totalMin = 1 + localizer2.getTotalMin(start2, end2);
  var minutesFromMidnight = localizer2.getMinutesFromMidnight(start2);
  var numGroups = Math.ceil((totalMin - 1) / (step * timeslots));
  var numSlots = numGroups * timeslots;
  var groups = new Array(numGroups);
  var slots = new Array(numSlots);
  for (var grp = 0; grp < numGroups; grp++) {
    groups[grp] = new Array(timeslots);
    for (var slot = 0; slot < timeslots; slot++) {
      var slotIdx = grp * timeslots + slot;
      var minFromStart = slotIdx * step;
      slots[slotIdx] = groups[grp][slot] = localizer2.getSlotDate(start2, minutesFromMidnight, minFromStart);
    }
  }
  var lastSlotMinFromStart = slots.length * step;
  slots.push(localizer2.getSlotDate(start2, minutesFromMidnight, lastSlotMinFromStart));
  function positionFromDate(date2) {
    var diff2 = localizer2.diff(start2, date2, "minutes") + localizer2.getDstOffset(start2, date2);
    return Math.min(diff2, totalMin);
  }
  return {
    groups,
    update: function update(args) {
      if (getKey2(args) !== key) return getSlotMetrics(args);
      return this;
    },
    dateIsInGroup: function dateIsInGroup(date2, groupIndex) {
      var nextGroup = groups[groupIndex + 1];
      return localizer2.inRange(date2, groups[groupIndex][0], nextGroup ? nextGroup[0] : end2, "minutes");
    },
    nextSlot: function nextSlot(slot2) {
      var next = slots[Math.min(slots.findIndex(function(s3) {
        return s3 === slot2 || localizer2.eq(s3, slot2);
      }) + 1, slots.length - 1)];
      if (localizer2.eq(next, slot2)) next = localizer2.add(slot2, step, "minutes");
      return next;
    },
    closestSlotToPosition: function closestSlotToPosition(percent) {
      var slot2 = Math.min(slots.length - 1, Math.max(0, Math.floor(percent * numSlots)));
      return slots[slot2];
    },
    closestSlotFromPoint: function closestSlotFromPoint(point, boundaryRect) {
      var range2 = Math.abs(boundaryRect.top - boundaryRect.bottom);
      return this.closestSlotToPosition((point.y - boundaryRect.top) / range2);
    },
    closestSlotFromDate: function closestSlotFromDate(date2) {
      var offset2 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : 0;
      if (localizer2.lt(date2, start2, "minutes")) return slots[0];
      if (localizer2.gt(date2, end2, "minutes")) return slots[slots.length - 1];
      var diffMins = localizer2.diff(start2, date2, "minutes");
      return slots[(diffMins - diffMins % step) / step + offset2];
    },
    startsBeforeDay: function startsBeforeDay(date2) {
      return localizer2.lt(date2, start2, "day");
    },
    startsAfterDay: function startsAfterDay(date2) {
      return localizer2.gt(date2, end2, "day");
    },
    startsBefore: function startsBefore(date2) {
      return localizer2.lt(localizer2.merge(start2, date2), start2, "minutes");
    },
    startsAfter: function startsAfter(date2) {
      return localizer2.gt(localizer2.merge(end2, date2), end2, "minutes");
    },
    getRange: function getRange(rangeStart, rangeEnd, ignoreMin, ignoreMax) {
      if (!ignoreMin) rangeStart = localizer2.min(end2, localizer2.max(start2, rangeStart));
      if (!ignoreMax) rangeEnd = localizer2.min(end2, localizer2.max(start2, rangeEnd));
      var rangeStartMin = positionFromDate(rangeStart);
      var rangeEndMin = positionFromDate(rangeEnd);
      var top2 = rangeEndMin > step * numSlots && !localizer2.eq(end2, rangeEnd) ? (rangeStartMin - step) / (step * numSlots) * 100 : rangeStartMin / (step * numSlots) * 100;
      return {
        top: top2,
        height: rangeEndMin / (step * numSlots) * 100 - top2,
        start: positionFromDate(rangeStart),
        startDate: rangeStart,
        end: positionFromDate(rangeEnd),
        endDate: rangeEnd
      };
    },
    getCurrentTimePosition: function getCurrentTimePosition(rangeStart) {
      var rangeStartMin = positionFromDate(rangeStart);
      var top2 = rangeStartMin / (step * numSlots) * 100;
      return top2;
    }
  };
}
var Event$1 = /* @__PURE__ */ (function() {
  function Event2(data, _ref) {
    var accessors2 = _ref.accessors, slotMetrics = _ref.slotMetrics;
    _classCallCheck(this, Event2);
    var _slotMetrics$getRange = slotMetrics.getRange(accessors2.start(data), accessors2.end(data)), start2 = _slotMetrics$getRange.start, startDate = _slotMetrics$getRange.startDate, end2 = _slotMetrics$getRange.end, endDate = _slotMetrics$getRange.endDate, top2 = _slotMetrics$getRange.top, height2 = _slotMetrics$getRange.height;
    this.start = start2;
    this.end = end2;
    this.startMs = +startDate;
    this.endMs = +endDate;
    this.top = top2;
    this.height = height2;
    this.data = data;
  }
  return _createClass(Event2, [{
    key: "_width",
    get: function get() {
      if (this.rows) {
        var columns = this.rows.reduce(
          function(max2, row) {
            return Math.max(max2, row.leaves.length + 1);
          },
          // add itself
          0
        ) + 1;
        return 100 / columns;
      }
      if (this.leaves) {
        var availableWidth = 100 - this.container._width;
        return availableWidth / (this.leaves.length + 1);
      }
      return this.row._width;
    }
    /**
     * The event's calculated width, possibly with extra width added for
     * overlapping effect.
     */
  }, {
    key: "width",
    get: function get() {
      var noOverlap2 = this._width;
      var overlap = Math.min(100, this._width * 1.7);
      if (this.rows) {
        return overlap;
      }
      if (this.leaves) {
        return this.leaves.length > 0 ? overlap : noOverlap2;
      }
      var leaves = this.row.leaves;
      var index2 = leaves.indexOf(this);
      return index2 === leaves.length - 1 ? noOverlap2 : overlap;
    }
  }, {
    key: "xOffset",
    get: function get() {
      if (this.rows) return 0;
      if (this.leaves) return this.container._width;
      var _this$row = this.row, leaves = _this$row.leaves, xOffset = _this$row.xOffset, _width = _this$row._width;
      var index2 = leaves.indexOf(this) + 1;
      return xOffset + index2 * _width;
    }
  }]);
})();
function onSameRow(a3, b2, minimumStartDifference) {
  return (
    // Occupies the same start slot.
    Math.abs(b2.start - a3.start) < minimumStartDifference || // A's start slot overlaps with b's end slot.
    b2.start > a3.start && b2.start < a3.end
  );
}
function sortByRender(events2) {
  var sortedByTime = sortByExports(events2, ["startMs", function(e2) {
    return -e2.endMs;
  }]);
  var sorted = [];
  while (sortedByTime.length > 0) {
    var event = sortedByTime.shift();
    sorted.push(event);
    for (var i2 = 0; i2 < sortedByTime.length; i2++) {
      var test = sortedByTime[i2];
      if (event.endMs > test.startMs) continue;
      if (i2 > 0) {
        var _event = sortedByTime.splice(i2, 1)[0];
        sorted.push(_event);
      }
      break;
    }
  }
  return sorted;
}
function getStyledEvents$1(_ref2) {
  var events2 = _ref2.events, minimumStartDifference = _ref2.minimumStartDifference, slotMetrics = _ref2.slotMetrics, accessors2 = _ref2.accessors;
  var proxies = events2.map(function(event) {
    return new Event$1(event, {
      slotMetrics,
      accessors: accessors2
    });
  });
  var eventsInRenderOrder = sortByRender(proxies);
  var containerEvents = [];
  var _loop = function _loop2() {
    var event = eventsInRenderOrder[i2];
    var container = containerEvents.find(function(c2) {
      return c2.end > event.start || Math.abs(event.start - c2.start) < minimumStartDifference;
    });
    if (!container) {
      event.rows = [];
      containerEvents.push(event);
      return 1;
    }
    event.container = container;
    var row = null;
    for (var j = container.rows.length - 1; !row && j >= 0; j--) {
      if (onSameRow(container.rows[j], event, minimumStartDifference)) {
        row = container.rows[j];
      }
    }
    if (row) {
      row.leaves.push(event);
      event.row = row;
    } else {
      event.leaves = [];
      container.rows.push(event);
    }
  };
  for (var i2 = 0; i2 < eventsInRenderOrder.length; i2++) {
    if (_loop()) continue;
  }
  return eventsInRenderOrder.map(function(event) {
    return {
      event: event.data,
      style: {
        top: event.top,
        height: event.height,
        width: event.width,
        xOffset: Math.max(0, event.xOffset)
      }
    };
  });
}
function getMaxIdxDFS(node, maxIdx, visited) {
  for (var i2 = 0; i2 < node.friends.length; ++i2) {
    if (visited.indexOf(node.friends[i2]) > -1) continue;
    maxIdx = maxIdx > node.friends[i2].idx ? maxIdx : node.friends[i2].idx;
    visited.push(node.friends[i2]);
    var newIdx = getMaxIdxDFS(node.friends[i2], maxIdx, visited);
    maxIdx = maxIdx > newIdx ? maxIdx : newIdx;
  }
  return maxIdx;
}
function noOverlap(_ref) {
  var events2 = _ref.events, minimumStartDifference = _ref.minimumStartDifference, slotMetrics = _ref.slotMetrics, accessors2 = _ref.accessors;
  var styledEvents = getStyledEvents$1({
    events: events2,
    minimumStartDifference,
    slotMetrics,
    accessors: accessors2
  });
  styledEvents.sort(function(a3, b2) {
    a3 = a3.style;
    b2 = b2.style;
    if (a3.top !== b2.top) return a3.top > b2.top ? 1 : -1;
    else if (a3.height !== b2.height) return a3.top + a3.height < b2.top + b2.height ? 1 : -1;
    else return 0;
  });
  for (var i2 = 0; i2 < styledEvents.length; ++i2) {
    styledEvents[i2].friends = [];
    delete styledEvents[i2].style.left;
    delete styledEvents[i2].style.left;
    delete styledEvents[i2].idx;
    delete styledEvents[i2].size;
  }
  for (var _i2 = 0; _i2 < styledEvents.length - 1; ++_i2) {
    var se1 = styledEvents[_i2];
    var y1 = se1.style.top;
    var y22 = se1.style.top + se1.style.height;
    for (var j = _i2 + 1; j < styledEvents.length; ++j) {
      var se2 = styledEvents[j];
      var y3 = se2.style.top;
      var y4 = se2.style.top + se2.style.height;
      if (y3 >= y1 && y4 <= y22 || y4 > y1 && y4 <= y22 || y3 >= y1 && y3 < y22) {
        se1.friends.push(se2);
        se2.friends.push(se1);
      }
    }
  }
  for (var _i4 = 0; _i4 < styledEvents.length; ++_i4) {
    var se = styledEvents[_i4];
    var bitmap = [];
    for (var _j2 = 0; _j2 < 100; ++_j2) bitmap.push(1);
    for (var _j4 = 0; _j4 < se.friends.length; ++_j4) if (se.friends[_j4].idx !== void 0) bitmap[se.friends[_j4].idx] = 0;
    se.idx = bitmap.indexOf(1);
  }
  for (var _i6 = 0; _i6 < styledEvents.length; ++_i6) {
    var size2 = 0;
    if (styledEvents[_i6].size) continue;
    var allFriends = [];
    var maxIdx = getMaxIdxDFS(styledEvents[_i6], 0, allFriends);
    size2 = 100 / (maxIdx + 1);
    styledEvents[_i6].size = size2;
    for (var _j6 = 0; _j6 < allFriends.length; ++_j6) allFriends[_j6].size = size2;
  }
  for (var _i8 = 0; _i8 < styledEvents.length; ++_i8) {
    var e2 = styledEvents[_i8];
    e2.style.left = e2.idx * e2.size;
    var _maxIdx = 0;
    for (var _j8 = 0; _j8 < e2.friends.length; ++_j8) {
      var idx = e2.friends[_j8].idx;
      _maxIdx = _maxIdx > idx ? _maxIdx : idx;
    }
    if (_maxIdx <= e2.idx) e2.size = 100 - e2.idx * e2.size;
    var padding = e2.idx === 0 ? 0 : 3;
    e2.style.width = "calc(".concat(e2.size, "% - ").concat(padding, "px)");
    e2.style.height = "calc(".concat(e2.style.height, "% - 2px)");
    e2.style.xOffset = "calc(".concat(e2.style.left, "% + ").concat(padding, "px)");
  }
  return styledEvents;
}
var DefaultAlgorithms = {
  overlap: getStyledEvents$1,
  "no-overlap": noOverlap
};
function isFunction(a3) {
  return !!(a3 && a3.constructor && a3.call && a3.apply);
}
function getStyledEvents(_ref) {
  _ref.events;
  _ref.minimumStartDifference;
  _ref.slotMetrics;
  _ref.accessors;
  var dayLayoutAlgorithm = _ref.dayLayoutAlgorithm;
  var algorithm = dayLayoutAlgorithm;
  if (dayLayoutAlgorithm in DefaultAlgorithms) algorithm = DefaultAlgorithms[dayLayoutAlgorithm];
  if (!isFunction(algorithm)) {
    return [];
  }
  return algorithm.apply(this, arguments);
}
var TimeSlotGroup = /* @__PURE__ */ (function(_Component) {
  function TimeSlotGroup2() {
    _classCallCheck(this, TimeSlotGroup2);
    return _callSuper(this, TimeSlotGroup2, arguments);
  }
  _inherits(TimeSlotGroup2, _Component);
  return _createClass(TimeSlotGroup2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, renderSlot = _this$props.renderSlot, resource = _this$props.resource, group = _this$props.group, getters = _this$props.getters, _this$props$component = _this$props.components, _this$props$component2 = _this$props$component === void 0 ? {} : _this$props$component, _this$props$component3 = _this$props$component2.timeSlotWrapper, Wrapper = _this$props$component3 === void 0 ? NoopWrapper : _this$props$component3;
      var groupProps = getters ? getters.slotGroupProp(group) : {};
      return /* @__PURE__ */ reactExports.createElement("div", Object.assign({
        className: "rbc-timeslot-group"
      }, groupProps), group.map(function(value, idx) {
        var slotProps = getters ? getters.slotProp(value, resource) : {};
        return /* @__PURE__ */ reactExports.createElement(Wrapper, {
          key: idx,
          value,
          resource
        }, /* @__PURE__ */ reactExports.createElement("div", Object.assign({}, slotProps, {
          className: clsx("rbc-time-slot", slotProps.className)
        }), renderSlot && renderSlot(value, idx)));
      }));
    }
  }]);
})(reactExports.Component);
function stringifyPercent(v) {
  return typeof v === "string" ? v : v + "%";
}
function TimeGridEvent$1(props) {
  var style2 = props.style, className = props.className, event = props.event, accessors2 = props.accessors, rtl = props.rtl, selected = props.selected, label = props.label, continuesPrior2 = props.continuesPrior, continuesAfter2 = props.continuesAfter, getters = props.getters, onClick = props.onClick, onDoubleClick = props.onDoubleClick, isBackgroundEvent = props.isBackgroundEvent, onKeyPress = props.onKeyPress, _props$components = props.components, Event2 = _props$components.event, EventWrapper2 = _props$components.eventWrapper;
  var title = accessors2.title(event);
  var tooltip = accessors2.tooltip(event);
  var end2 = accessors2.end(event);
  var start2 = accessors2.start(event);
  var userProps = getters.eventProp(event, start2, end2, selected);
  var inner = [/* @__PURE__ */ reactExports.createElement("div", {
    key: "1",
    className: "rbc-event-label"
  }, label), /* @__PURE__ */ reactExports.createElement("div", {
    key: "2",
    className: "rbc-event-content"
  }, Event2 ? /* @__PURE__ */ reactExports.createElement(Event2, {
    event,
    title
  }) : title)];
  var height2 = style2.height, top2 = style2.top, width = style2.width, xOffset = style2.xOffset;
  var eventStyle = _objectSpread2(_objectSpread2({}, userProps.style), {}, _defineProperty$1({
    top: stringifyPercent(top2),
    height: stringifyPercent(height2),
    width: stringifyPercent(width)
  }, rtl ? "right" : "left", stringifyPercent(xOffset)));
  return /* @__PURE__ */ reactExports.createElement(EventWrapper2, Object.assign({
    type: "time"
  }, props), /* @__PURE__ */ reactExports.createElement("div", {
    role: "button",
    tabIndex: 0,
    onClick,
    onDoubleClick,
    style: eventStyle,
    onKeyDown: onKeyPress,
    title: tooltip ? (typeof label === "string" ? label + ": " : "") + tooltip : void 0,
    className: clsx(isBackgroundEvent ? "rbc-background-event" : "rbc-event", className, userProps.className, {
      "rbc-selected": selected,
      "rbc-event-continues-earlier": continuesPrior2,
      "rbc-event-continues-later": continuesAfter2
    })
  }, inner));
}
var DayColumnWrapper = function DayColumnWrapper2(_ref) {
  var children = _ref.children, className = _ref.className, style2 = _ref.style, innerRef = _ref.innerRef;
  return /* @__PURE__ */ reactExports.createElement("div", {
    className,
    style: style2,
    ref: innerRef
  }, children);
};
var DayColumnWrapper$1 = /* @__PURE__ */ reactExports.forwardRef(function(props, ref) {
  return /* @__PURE__ */ reactExports.createElement(DayColumnWrapper, Object.assign({}, props, {
    innerRef: ref
  }));
});
var _excluded$5 = ["dayProp"], _excluded2$1 = ["eventContainerWrapper", "timeIndicatorWrapper"];
var DayColumn = /* @__PURE__ */ (function(_React$Component) {
  function DayColumn2() {
    var _this;
    _classCallCheck(this, DayColumn2);
    for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
      _args[_key] = arguments[_key];
    }
    _this = _callSuper(this, DayColumn2, [].concat(_args));
    _this.state = {
      selecting: false,
      timeIndicatorPosition: null
    };
    _this.intervalTriggered = false;
    _this.renderEvents = function(_ref) {
      var events2 = _ref.events, isBackgroundEvent = _ref.isBackgroundEvent;
      var _this$props = _this.props, rtl = _this$props.rtl, selected = _this$props.selected, accessors2 = _this$props.accessors, localizer2 = _this$props.localizer, getters = _this$props.getters, components2 = _this$props.components, step = _this$props.step, timeslots = _this$props.timeslots, dayLayoutAlgorithm = _this$props.dayLayoutAlgorithm, resizable = _this$props.resizable;
      var _this2 = _this, slotMetrics = _this2.slotMetrics;
      var messages2 = localizer2.messages;
      var styledEvents = getStyledEvents({
        events: events2,
        accessors: accessors2,
        slotMetrics,
        minimumStartDifference: Math.ceil(step * timeslots / 2),
        dayLayoutAlgorithm
      });
      return styledEvents.map(function(_ref2, idx) {
        var _accessors$eventId;
        var event = _ref2.event, style2 = _ref2.style;
        var end2 = accessors2.end(event);
        var start2 = accessors2.start(event);
        var key = (_accessors$eventId = accessors2.eventId(event)) !== null && _accessors$eventId !== void 0 ? _accessors$eventId : "evt_" + idx;
        var format2 = "eventTimeRangeFormat";
        var label;
        var startsBeforeDay = slotMetrics.startsBeforeDay(start2);
        var startsAfterDay = slotMetrics.startsAfterDay(end2);
        if (startsBeforeDay) format2 = "eventTimeRangeEndFormat";
        else if (startsAfterDay) format2 = "eventTimeRangeStartFormat";
        if (startsBeforeDay && startsAfterDay) label = messages2.allDay;
        else label = localizer2.format({
          start: start2,
          end: end2
        }, format2);
        var continuesPrior2 = startsBeforeDay || slotMetrics.startsBefore(start2);
        var continuesAfter2 = startsAfterDay || slotMetrics.startsAfter(end2);
        return /* @__PURE__ */ reactExports.createElement(TimeGridEvent$1, {
          style: style2,
          event,
          label,
          key,
          getters,
          rtl,
          components: components2,
          continuesPrior: continuesPrior2,
          continuesAfter: continuesAfter2,
          accessors: accessors2,
          resource: _this.props.resource,
          selected: isSelected(event, selected),
          onClick: function onClick(e2) {
            return _this._select(_objectSpread2(_objectSpread2(_objectSpread2({}, event), _this.props.resource && {
              sourceResource: _this.props.resource
            }), isBackgroundEvent && {
              isBackgroundEvent: true
            }), e2);
          },
          onDoubleClick: function onDoubleClick(e2) {
            return _this._doubleClick(event, e2);
          },
          isBackgroundEvent,
          onKeyPress: function onKeyPress(e2) {
            return _this._keyPress(event, e2);
          },
          resizable
        });
      });
    };
    _this._selectable = function() {
      var node = _this.containerRef.current;
      var _this$props2 = _this.props, longPressThreshold = _this$props2.longPressThreshold, localizer2 = _this$props2.localizer;
      var selector = _this._selector = new Selection$1(function() {
        return node;
      }, {
        longPressThreshold
      });
      var maybeSelect = function maybeSelect2(box) {
        var onSelecting = _this.props.onSelecting;
        var current = _this.state || {};
        var state = selectionState(box);
        var start2 = state.startDate, end2 = state.endDate;
        if (onSelecting) {
          if (localizer2.eq(current.startDate, start2, "minutes") && localizer2.eq(current.endDate, end2, "minutes") || onSelecting({
            start: start2,
            end: end2,
            resourceId: _this.props.resource
          }) === false) return;
        }
        if (_this.state.start !== state.start || _this.state.end !== state.end || _this.state.selecting !== state.selecting) {
          _this.setState(state);
        }
      };
      var selectionState = function selectionState2(point) {
        var currentSlot = _this.slotMetrics.closestSlotFromPoint(point, getBoundsForNode(node));
        if (!_this.state.selecting) {
          _this._initialSlot = currentSlot;
        }
        var initialSlot = _this._initialSlot;
        if (localizer2.lte(initialSlot, currentSlot)) {
          currentSlot = _this.slotMetrics.nextSlot(currentSlot);
        } else if (localizer2.gt(initialSlot, currentSlot)) {
          initialSlot = _this.slotMetrics.nextSlot(initialSlot);
        }
        var selectRange = _this.slotMetrics.getRange(localizer2.min(initialSlot, currentSlot), localizer2.max(initialSlot, currentSlot));
        return _objectSpread2(_objectSpread2({}, selectRange), {}, {
          selecting: true,
          top: "".concat(selectRange.top, "%"),
          height: "".concat(selectRange.height, "%")
        });
      };
      var selectorClicksHandler = function selectorClicksHandler2(box, actionType) {
        if (!isEvent(_this.containerRef.current, box)) {
          var _selectionState = selectionState(box), startDate = _selectionState.startDate, endDate = _selectionState.endDate;
          _this._selectSlot({
            startDate,
            endDate,
            action: actionType,
            box
          });
        }
        _this.setState({
          selecting: false
        });
      };
      selector.on("selecting", maybeSelect);
      selector.on("selectStart", maybeSelect);
      selector.on("beforeSelect", function(box) {
        if (_this.props.selectable !== "ignoreEvents") return;
        return !isEvent(_this.containerRef.current, box);
      });
      selector.on("click", function(box) {
        return selectorClicksHandler(box, "click");
      });
      selector.on("doubleClick", function(box) {
        return selectorClicksHandler(box, "doubleClick");
      });
      selector.on("select", function(bounds) {
        if (_this.state.selecting) {
          _this._selectSlot(_objectSpread2(_objectSpread2({}, _this.state), {}, {
            action: "select",
            bounds
          }));
          _this.setState({
            selecting: false
          });
        }
      });
      selector.on("reset", function() {
        if (_this.state.selecting) {
          _this.setState({
            selecting: false
          });
        }
      });
    };
    _this._teardownSelectable = function() {
      if (!_this._selector) return;
      _this._selector.teardown();
      _this._selector = null;
    };
    _this._selectSlot = function(_ref3) {
      var startDate = _ref3.startDate, endDate = _ref3.endDate, action = _ref3.action, bounds = _ref3.bounds, box = _ref3.box;
      var current = startDate, slots = [];
      while (_this.props.localizer.lte(current, endDate)) {
        slots.push(current);
        current = new Date(+current + _this.props.step * 60 * 1e3);
      }
      notify(_this.props.onSelectSlot, {
        slots,
        start: startDate,
        end: endDate,
        resourceId: _this.props.resource,
        action,
        bounds,
        box
      });
    };
    _this._select = function() {
      for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      notify(_this.props.onSelectEvent, args);
    };
    _this._doubleClick = function() {
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      notify(_this.props.onDoubleClickEvent, args);
    };
    _this._keyPress = function() {
      for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        args[_key4] = arguments[_key4];
      }
      notify(_this.props.onKeyPressEvent, args);
    };
    _this.slotMetrics = getSlotMetrics(_this.props);
    _this.containerRef = /* @__PURE__ */ reactExports.createRef();
    return _this;
  }
  _inherits(DayColumn2, _React$Component);
  return _createClass(DayColumn2, [{
    key: "componentDidMount",
    value: function componentDidMount() {
      this.props.selectable && this._selectable();
      if (this.props.isNow) {
        this.setTimeIndicatorPositionUpdateInterval();
      }
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      this._teardownSelectable();
      this.clearTimeIndicatorInterval();
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate(prevProps, prevState) {
      if (this.props.selectable && !prevProps.selectable) this._selectable();
      if (!this.props.selectable && prevProps.selectable) this._teardownSelectable();
      var _this$props3 = this.props, getNow2 = _this$props3.getNow, isNow = _this$props3.isNow, localizer2 = _this$props3.localizer, date2 = _this$props3.date, min2 = _this$props3.min, max2 = _this$props3.max;
      var getNowChanged = localizer2.neq(prevProps.getNow(), getNow2(), "minutes");
      if (prevProps.isNow !== isNow || getNowChanged) {
        this.clearTimeIndicatorInterval();
        if (isNow) {
          var tail = !getNowChanged && localizer2.eq(prevProps.date, date2, "minutes") && prevState.timeIndicatorPosition === this.state.timeIndicatorPosition;
          this.setTimeIndicatorPositionUpdateInterval(tail);
        }
      } else if (isNow && (localizer2.neq(prevProps.min, min2, "minutes") || localizer2.neq(prevProps.max, max2, "minutes"))) {
        this.positionTimeIndicator();
      }
    }
    /**
     * @param tail {Boolean} - whether `positionTimeIndicator` call should be
     *   deferred or called upon setting interval (`true` - if deferred);
     */
  }, {
    key: "setTimeIndicatorPositionUpdateInterval",
    value: function setTimeIndicatorPositionUpdateInterval() {
      var _this3 = this;
      var tail = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : false;
      if (!this.intervalTriggered && !tail) {
        this.positionTimeIndicator();
      }
      this._timeIndicatorTimeout = window.setTimeout(function() {
        _this3.intervalTriggered = true;
        _this3.positionTimeIndicator();
        _this3.setTimeIndicatorPositionUpdateInterval();
      }, 6e4);
    }
  }, {
    key: "clearTimeIndicatorInterval",
    value: function clearTimeIndicatorInterval() {
      this.intervalTriggered = false;
      window.clearTimeout(this._timeIndicatorTimeout);
    }
  }, {
    key: "positionTimeIndicator",
    value: function positionTimeIndicator() {
      var _this$props4 = this.props, min2 = _this$props4.min, max2 = _this$props4.max, getNow2 = _this$props4.getNow;
      var current = getNow2();
      if (current >= min2 && current <= max2) {
        var top2 = this.slotMetrics.getCurrentTimePosition(current);
        this.intervalTriggered = true;
        this.setState({
          timeIndicatorPosition: top2
        });
      } else {
        this.clearTimeIndicatorInterval();
      }
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props5 = this.props, date2 = _this$props5.date, max2 = _this$props5.max, rtl = _this$props5.rtl, isNow = _this$props5.isNow, resource = _this$props5.resource, accessors2 = _this$props5.accessors, localizer2 = _this$props5.localizer, _this$props5$getters = _this$props5.getters, dayProp = _this$props5$getters.dayProp, getters = _objectWithoutProperties(_this$props5$getters, _excluded$5), _this$props5$componen = _this$props5.components, EventContainer = _this$props5$componen.eventContainerWrapper, TimeIndicatorWrapper = _this$props5$componen.timeIndicatorWrapper, components2 = _objectWithoutProperties(_this$props5$componen, _excluded2$1);
      this.slotMetrics = this.slotMetrics.update(this.props);
      var slotMetrics = this.slotMetrics;
      var _this$state = this.state, selecting = _this$state.selecting, top2 = _this$state.top, height2 = _this$state.height, startDate = _this$state.startDate, endDate = _this$state.endDate;
      var selectDates = {
        start: startDate,
        end: endDate
      };
      var _dayProp = dayProp(max2, resource), className = _dayProp.className, style2 = _dayProp.style;
      var timeIndicatorProps = {
        className: "rbc-current-time-indicator",
        style: {
          top: "".concat(this.state.timeIndicatorPosition, "%")
        }
      };
      var DayColumnWrapperComponent = components2.dayColumnWrapper || DayColumnWrapper$1;
      return /* @__PURE__ */ reactExports.createElement(DayColumnWrapperComponent, {
        ref: this.containerRef,
        date: date2,
        style: style2,
        className: clsx(
          className,
          "rbc-day-slot",
          "rbc-time-column",
          isNow && "rbc-now",
          isNow && "rbc-today",
          // WHY
          selecting && "rbc-slot-selecting"
        ),
        slotMetrics,
        resource
      }, slotMetrics.groups.map(function(grp, idx) {
        return /* @__PURE__ */ reactExports.createElement(TimeSlotGroup, {
          key: idx,
          group: grp,
          resource,
          getters,
          components: components2
        });
      }), /* @__PURE__ */ reactExports.createElement(EventContainer, {
        localizer: localizer2,
        resource,
        accessors: accessors2,
        getters,
        components: components2,
        slotMetrics
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx("rbc-events-container", rtl && "rtl")
      }, this.renderEvents({
        events: this.props.backgroundEvents,
        isBackgroundEvent: true
      }), this.renderEvents({
        events: this.props.events
      }))), selecting && /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-slot-selection",
        style: {
          top: top2,
          height: height2
        }
      }, /* @__PURE__ */ reactExports.createElement("span", null, localizer2.format(selectDates, "selectRangeFormat"))), isNow && this.intervalTriggered && /* @__PURE__ */ reactExports.createElement(TimeIndicatorWrapper, timeIndicatorProps, /* @__PURE__ */ reactExports.createElement("div", timeIndicatorProps)));
    }
  }]);
})(reactExports.Component);
DayColumn.defaultProps = {
  dragThroughEvents: true,
  timeslots: 2
};
var ResourceHeader = function ResourceHeader2(_ref) {
  var label = _ref.label;
  return /* @__PURE__ */ reactExports.createElement(reactExports.Fragment, null, label);
};
var TimeGridHeader = /* @__PURE__ */ (function(_React$Component) {
  function TimeGridHeader2() {
    var _this;
    _classCallCheck(this, TimeGridHeader2);
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _callSuper(this, TimeGridHeader2, [].concat(args));
    _this.handleHeaderClick = function(date2, view, e2) {
      e2.preventDefault();
      notify(_this.props.onDrillDown, [date2, view]);
    };
    _this.renderRow = function(resource) {
      var _this$props = _this.props, events2 = _this$props.events, rtl = _this$props.rtl, selectable = _this$props.selectable, getNow2 = _this$props.getNow, range2 = _this$props.range, getters = _this$props.getters, localizer2 = _this$props.localizer, accessors2 = _this$props.accessors, components2 = _this$props.components, resizable = _this$props.resizable;
      var resourceId = accessors2.resourceId(resource);
      var eventsToDisplay = resource ? events2.filter(function(event) {
        return accessors2.resource(event) === resourceId;
      }) : events2;
      return /* @__PURE__ */ reactExports.createElement(DateContentRow, {
        isAllDay: true,
        rtl,
        getNow: getNow2,
        minRows: 2,
        maxRows: _this.props.allDayMaxRows + 1,
        range: range2,
        events: eventsToDisplay,
        resourceId,
        className: "rbc-allday-cell",
        selectable,
        selected: _this.props.selected,
        components: components2,
        accessors: accessors2,
        getters,
        localizer: localizer2,
        onSelect: _this.props.onSelectEvent,
        onShowMore: _this.props.onShowMore,
        onDoubleClick: _this.props.onDoubleClickEvent,
        onKeyPress: _this.props.onKeyPressEvent,
        onSelectSlot: _this.props.onSelectSlot,
        longPressThreshold: _this.props.longPressThreshold,
        resizable
      });
    };
    return _this;
  }
  _inherits(TimeGridHeader2, _React$Component);
  return _createClass(TimeGridHeader2, [{
    key: "renderHeaderCells",
    value: function renderHeaderCells(range2) {
      var _this2 = this;
      var _this$props2 = this.props, localizer2 = _this$props2.localizer, getDrilldownView = _this$props2.getDrilldownView, getNow2 = _this$props2.getNow, dayProp = _this$props2.getters.dayProp, _this$props2$componen = _this$props2.components.header, HeaderComponent = _this$props2$componen === void 0 ? Header : _this$props2$componen;
      var today = getNow2();
      return range2.map(function(date2, i2) {
        var drilldownView = getDrilldownView(date2);
        var label = localizer2.format(date2, "dayFormat");
        var _dayProp = dayProp(date2), className = _dayProp.className, style2 = _dayProp.style;
        var header = /* @__PURE__ */ reactExports.createElement(HeaderComponent, {
          date: date2,
          label,
          localizer: localizer2
        });
        return /* @__PURE__ */ reactExports.createElement("div", {
          key: i2,
          style: style2,
          className: clsx("rbc-header", className, localizer2.isSameDate(date2, today) && "rbc-today")
        }, drilldownView ? /* @__PURE__ */ reactExports.createElement("button", {
          type: "button",
          className: "rbc-button-link",
          onClick: function onClick(e2) {
            return _this2.handleHeaderClick(date2, drilldownView, e2);
          }
        }, header) : /* @__PURE__ */ reactExports.createElement("span", null, header));
      });
    }
  }, {
    key: "render",
    value: function render() {
      var _this3 = this;
      var _this$props3 = this.props, width = _this$props3.width, rtl = _this$props3.rtl, resources = _this$props3.resources, range2 = _this$props3.range, events2 = _this$props3.events, getNow2 = _this$props3.getNow, accessors2 = _this$props3.accessors, selectable = _this$props3.selectable, components2 = _this$props3.components, getters = _this$props3.getters, scrollRef = _this$props3.scrollRef, localizer2 = _this$props3.localizer, isOverflowing = _this$props3.isOverflowing, _this$props3$componen = _this$props3.components, TimeGutterHeader = _this$props3$componen.timeGutterHeader, _this$props3$componen2 = _this$props3$componen.resourceHeader, ResourceHeaderComponent = _this$props3$componen2 === void 0 ? ResourceHeader : _this$props3$componen2, resizable = _this$props3.resizable;
      var style2 = {};
      if (isOverflowing) {
        style2[rtl ? "marginLeft" : "marginRight"] = "".concat(scrollbarSize() - 1, "px");
      }
      var groupedEvents = resources.groupEvents(events2);
      return /* @__PURE__ */ reactExports.createElement("div", {
        style: style2,
        ref: scrollRef,
        className: clsx("rbc-time-header", isOverflowing && "rbc-overflowing")
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-label rbc-time-header-gutter",
        style: {
          width,
          minWidth: width,
          maxWidth: width
        }
      }, TimeGutterHeader && /* @__PURE__ */ reactExports.createElement(TimeGutterHeader, null)), resources.map(function(_ref, idx) {
        var _ref2 = _slicedToArray(_ref, 2), id2 = _ref2[0], resource = _ref2[1];
        return /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-time-header-content",
          key: id2 || idx
        }, resource && /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-row rbc-row-resource",
          key: "resource_".concat(idx)
        }, /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-header"
        }, /* @__PURE__ */ reactExports.createElement(ResourceHeaderComponent, {
          index: idx,
          label: accessors2.resourceTitle(resource),
          resource
        }))), /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-row rbc-time-header-cell".concat(range2.length <= 1 ? " rbc-time-header-cell-single-day" : "")
        }, _this3.renderHeaderCells(range2)), /* @__PURE__ */ reactExports.createElement(DateContentRow, {
          isAllDay: true,
          rtl,
          getNow: getNow2,
          minRows: 2,
          maxRows: _this3.props.allDayMaxRows + 1,
          range: range2,
          events: groupedEvents.get(id2) || [],
          resourceId: resource && id2,
          className: "rbc-allday-cell",
          selectable,
          selected: _this3.props.selected,
          components: components2,
          accessors: accessors2,
          getters,
          localizer: localizer2,
          onSelect: _this3.props.onSelectEvent,
          onShowMore: _this3.props.onShowMore,
          onDoubleClick: _this3.props.onDoubleClickEvent,
          onKeyDown: _this3.props.onKeyPressEvent,
          onSelectSlot: _this3.props.onSelectSlot,
          longPressThreshold: _this3.props.longPressThreshold,
          resizable
        }));
      }));
    }
  }]);
})(reactExports.Component);
var TimeGridHeaderResources = /* @__PURE__ */ (function(_React$Component) {
  function TimeGridHeaderResources2() {
    var _this;
    _classCallCheck(this, TimeGridHeaderResources2);
    for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
      args[_key] = arguments[_key];
    }
    _this = _callSuper(this, TimeGridHeaderResources2, [].concat(args));
    _this.handleHeaderClick = function(date2, view, e2) {
      e2.preventDefault();
      notify(_this.props.onDrillDown, [date2, view]);
    };
    return _this;
  }
  _inherits(TimeGridHeaderResources2, _React$Component);
  return _createClass(TimeGridHeaderResources2, [{
    key: "renderHeaderCells",
    value: function renderHeaderCells(range2) {
      var _this2 = this;
      var _this$props = this.props, localizer2 = _this$props.localizer, getDrilldownView = _this$props.getDrilldownView, getNow2 = _this$props.getNow, dayProp = _this$props.getters.dayProp, _this$props$component = _this$props.components, _this$props$component2 = _this$props$component.header, HeaderComponent = _this$props$component2 === void 0 ? Header : _this$props$component2, _this$props$component3 = _this$props$component.resourceHeader, ResourceHeaderComponent = _this$props$component3 === void 0 ? ResourceHeader : _this$props$component3, resources = _this$props.resources, accessors2 = _this$props.accessors, events2 = _this$props.events, rtl = _this$props.rtl, selectable = _this$props.selectable, components2 = _this$props.components, getters = _this$props.getters, resizable = _this$props.resizable;
      var today = getNow2();
      var groupedEvents = resources.groupEvents(events2);
      return range2.map(function(date2, idx) {
        var drilldownView = getDrilldownView(date2);
        var label = localizer2.format(date2, "dayFormat");
        var _dayProp = dayProp(date2), className = _dayProp.className, style2 = _dayProp.style;
        var header = /* @__PURE__ */ reactExports.createElement(HeaderComponent, {
          date: date2,
          label,
          localizer: localizer2
        });
        return /* @__PURE__ */ reactExports.createElement("div", {
          key: idx,
          className: "rbc-time-header-content rbc-resource-grouping"
        }, /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-row rbc-time-header-cell".concat(range2.length <= 1 ? " rbc-time-header-cell-single-day" : "")
        }, /* @__PURE__ */ reactExports.createElement("div", {
          style: style2,
          className: clsx("rbc-header", className, localizer2.isSameDate(date2, today) && "rbc-today")
        }, drilldownView ? /* @__PURE__ */ reactExports.createElement("button", {
          type: "button",
          className: "rbc-button-link",
          onClick: function onClick(e2) {
            return _this2.handleHeaderClick(date2, drilldownView, e2);
          }
        }, header) : /* @__PURE__ */ reactExports.createElement("span", null, header))), /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-row"
        }, resources.map(function(_ref, idx2) {
          var _ref2 = _slicedToArray(_ref, 2), id2 = _ref2[0], resource = _ref2[1];
          return /* @__PURE__ */ reactExports.createElement("div", {
            key: "resource_".concat(id2, "_").concat(idx2),
            className: clsx("rbc-header", className, localizer2.isSameDate(date2, today) && "rbc-today")
          }, /* @__PURE__ */ reactExports.createElement(ResourceHeaderComponent, {
            index: idx2,
            label: accessors2.resourceTitle(resource),
            resource
          }));
        })), /* @__PURE__ */ reactExports.createElement("div", {
          className: "rbc-row rbc-m-b-negative-3 rbc-h-full"
        }, resources.map(function(_ref3, idx2) {
          var _ref4 = _slicedToArray(_ref3, 2), id2 = _ref4[0], resource = _ref4[1];
          var filteredEvents = (groupedEvents.get(id2) || []).filter(function(event) {
            return localizer2.isSameDate(event.start, date2) || localizer2.isSameDate(event.end, date2);
          });
          return /* @__PURE__ */ reactExports.createElement(DateContentRow, {
            key: "resource_".concat(id2, "_").concat(idx2),
            isAllDay: true,
            rtl,
            getNow: getNow2,
            minRows: 2,
            maxRows: _this2.props.allDayMaxRows + 1,
            range: [date2],
            events: filteredEvents,
            resourceId: resource && id2,
            className: "rbc-allday-cell",
            selectable,
            selected: _this2.props.selected,
            components: components2,
            accessors: accessors2,
            getters,
            localizer: localizer2,
            onSelect: _this2.props.onSelectEvent,
            onShowMore: _this2.props.onShowMore,
            onDoubleClick: _this2.props.onDoubleClickEvent,
            onKeyDown: _this2.props.onKeyPressEvent,
            onSelectSlot: _this2.props.onSelectSlot,
            longPressThreshold: _this2.props.longPressThreshold,
            resizable
          });
        })));
      });
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props2 = this.props, width = _this$props2.width, rtl = _this$props2.rtl, range2 = _this$props2.range, scrollRef = _this$props2.scrollRef, isOverflowing = _this$props2.isOverflowing, TimeGutterHeader = _this$props2.components.timeGutterHeader;
      var style2 = {};
      if (isOverflowing) {
        style2[rtl ? "marginLeft" : "marginRight"] = "".concat(scrollbarSize() - 1, "px");
      }
      return /* @__PURE__ */ reactExports.createElement("div", {
        style: style2,
        ref: scrollRef,
        className: clsx("rbc-time-header", isOverflowing && "rbc-overflowing")
      }, /* @__PURE__ */ reactExports.createElement("div", {
        className: "rbc-label rbc-time-header-gutter",
        style: {
          width,
          minWidth: width,
          maxWidth: width
        }
      }, TimeGutterHeader && /* @__PURE__ */ reactExports.createElement(TimeGutterHeader, null)), this.renderHeaderCells(range2));
    }
  }]);
})(reactExports.Component);
function adjustForDST(_ref) {
  var min2 = _ref.min, max2 = _ref.max, localizer2 = _ref.localizer;
  if (localizer2.getTimezoneOffset(min2) !== localizer2.getTimezoneOffset(max2)) {
    return {
      start: localizer2.add(min2, -1, "day"),
      end: localizer2.add(max2, -1, "day")
    };
  }
  return {
    start: min2,
    end: max2
  };
}
var TimeGutter = function TimeGutter2(_ref2) {
  var min2 = _ref2.min, max2 = _ref2.max, timeslots = _ref2.timeslots, step = _ref2.step, localizer2 = _ref2.localizer, getNow2 = _ref2.getNow, resource = _ref2.resource, components2 = _ref2.components, getters = _ref2.getters, gutterRef = _ref2.gutterRef;
  var TimeGutterWrapper = components2.timeGutterWrapper;
  var _useMemo = reactExports.useMemo(
    function() {
      return adjustForDST({
        min: min2,
        max: max2,
        localizer: localizer2
      });
    },
    // eslint-disable-next-line react-hooks/exhaustive-deps
    [min2 === null || min2 === void 0 ? void 0 : min2.toISOString(), max2 === null || max2 === void 0 ? void 0 : max2.toISOString(), localizer2]
  ), start2 = _useMemo.start, end2 = _useMemo.end;
  var _useState = reactExports.useState(getSlotMetrics({
    min: start2,
    max: end2,
    timeslots,
    step,
    localizer: localizer2
  })), _useState2 = _slicedToArray(_useState, 2), slotMetrics = _useState2[0], setSlotMetrics = _useState2[1];
  reactExports.useEffect(function() {
    if (slotMetrics) {
      setSlotMetrics(slotMetrics.update({
        min: start2,
        max: end2,
        timeslots,
        step,
        localizer: localizer2
      }));
    }
  }, [start2 === null || start2 === void 0 ? void 0 : start2.toISOString(), end2 === null || end2 === void 0 ? void 0 : end2.toISOString(), timeslots, step]);
  var renderSlot = reactExports.useCallback(function(value, idx) {
    if (idx) return null;
    var isNow = slotMetrics.dateIsInGroup(getNow2(), idx);
    return /* @__PURE__ */ reactExports.createElement("span", {
      className: clsx("rbc-label", isNow && "rbc-now")
    }, localizer2.format(value, "timeGutterFormat"));
  }, [slotMetrics, localizer2, getNow2]);
  return /* @__PURE__ */ reactExports.createElement(TimeGutterWrapper, {
    slotMetrics
  }, /* @__PURE__ */ reactExports.createElement("div", {
    className: "rbc-time-gutter rbc-time-column",
    ref: gutterRef
  }, slotMetrics.groups.map(function(grp, idx) {
    return /* @__PURE__ */ reactExports.createElement(TimeSlotGroup, {
      key: idx,
      group: grp,
      resource,
      components: components2,
      renderSlot,
      getters
    });
  })));
};
var TimeGutter$1 = /* @__PURE__ */ reactExports.forwardRef(function(props, ref) {
  return /* @__PURE__ */ reactExports.createElement(TimeGutter, Object.assign({
    gutterRef: ref
  }, props));
});
var NONE = {};
function Resources(resources, accessors2) {
  return {
    map: function map(fn3) {
      if (!resources) return [fn3([NONE, null], 0)];
      return resources.map(function(resource, idx) {
        return fn3([accessors2.resourceId(resource), resource], idx);
      });
    },
    groupEvents: function groupEvents(events2) {
      var eventsByResource = /* @__PURE__ */ new Map();
      if (!resources) {
        eventsByResource.set(NONE, events2);
        return eventsByResource;
      }
      events2.forEach(function(event) {
        var id2 = accessors2.resource(event) || NONE;
        if (Array.isArray(id2)) {
          id2.forEach(function(item) {
            var resourceEvents2 = eventsByResource.get(item) || [];
            resourceEvents2.push(event);
            eventsByResource.set(item, resourceEvents2);
          });
        } else {
          var resourceEvents = eventsByResource.get(id2) || [];
          resourceEvents.push(event);
          eventsByResource.set(id2, resourceEvents);
        }
      });
      return eventsByResource;
    }
  };
}
var TimeGrid = /* @__PURE__ */ (function(_Component) {
  function TimeGrid2(props) {
    var _this;
    _classCallCheck(this, TimeGrid2);
    _this = _callSuper(this, TimeGrid2, [props]);
    _this.handleScroll = function(e2) {
      if (_this.scrollRef.current) {
        _this.scrollRef.current.scrollLeft = e2.target.scrollLeft;
      }
    };
    _this.handleResize = function() {
      cancel(_this.rafHandle);
      _this.rafHandle = request(_this.checkOverflow);
    };
    _this.handleKeyPressEvent = function() {
      _this.clearSelection();
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      notify(_this.props.onKeyPressEvent, args);
    };
    _this.handleSelectEvent = function() {
      _this.clearSelection();
      for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      notify(_this.props.onSelectEvent, args);
    };
    _this.handleDoubleClickEvent = function() {
      _this.clearSelection();
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      notify(_this.props.onDoubleClickEvent, args);
    };
    _this.handleShowMore = function(events2, date2, cell, slot, target) {
      var _this$props = _this.props, popup = _this$props.popup, onDrillDown = _this$props.onDrillDown, onShowMore = _this$props.onShowMore, getDrilldownView = _this$props.getDrilldownView, doShowMoreDrillDown = _this$props.doShowMoreDrillDown;
      _this.clearSelection();
      if (popup) {
        var position$1 = position(cell, _this.containerRef.current);
        _this.setState({
          overlay: {
            date: date2,
            events: events2,
            position: _objectSpread2(_objectSpread2({}, position$1), {}, {
              width: "200px"
            }),
            target
          }
        });
      } else if (doShowMoreDrillDown) {
        notify(onDrillDown, [date2, getDrilldownView(date2) || views.DAY]);
      }
      notify(onShowMore, [events2, date2, slot]);
    };
    _this.handleSelectAllDaySlot = function(slots, slotInfo) {
      var onSelectSlot = _this.props.onSelectSlot;
      var start2 = new Date(slots[0]);
      var end2 = new Date(slots[slots.length - 1]);
      end2.setDate(slots[slots.length - 1].getDate() + 1);
      notify(onSelectSlot, {
        slots,
        start: start2,
        end: end2,
        action: slotInfo.action,
        resourceId: slotInfo.resourceId
      });
    };
    _this.overlayDisplay = function() {
      _this.setState({
        overlay: null
      });
    };
    _this.checkOverflow = function() {
      if (_this._updatingOverflow) return;
      var content = _this.contentRef.current;
      if (!(content !== null && content !== void 0 && content.scrollHeight)) return;
      var isOverflowing = content.scrollHeight > content.clientHeight;
      if (_this.state.isOverflowing !== isOverflowing) {
        _this._updatingOverflow = true;
        _this.setState({
          isOverflowing
        }, function() {
          _this._updatingOverflow = false;
        });
      }
    };
    _this.memoizedResources = memoizeOne(function(resources, accessors2) {
      return Resources(resources, accessors2);
    });
    _this.state = {
      gutterWidth: void 0,
      isOverflowing: null
    };
    _this.scrollRef = /* @__PURE__ */ reactExports.createRef();
    _this.contentRef = /* @__PURE__ */ reactExports.createRef();
    _this.containerRef = /* @__PURE__ */ reactExports.createRef();
    _this._scrollRatio = null;
    _this.gutterRef = /* @__PURE__ */ reactExports.createRef();
    return _this;
  }
  _inherits(TimeGrid2, _Component);
  return _createClass(TimeGrid2, [{
    key: "getSnapshotBeforeUpdate",
    value: function getSnapshotBeforeUpdate() {
      this.checkOverflow();
      return null;
    }
  }, {
    key: "componentDidMount",
    value: function componentDidMount() {
      if (this.props.width == null) {
        this.measureGutter();
      }
      this.calculateScroll();
      this.applyScroll();
      window.addEventListener("resize", this.handleResize);
    }
  }, {
    key: "componentWillUnmount",
    value: function componentWillUnmount() {
      window.removeEventListener("resize", this.handleResize);
      cancel(this.rafHandle);
      if (this.measureGutterAnimationFrameRequest) {
        window.cancelAnimationFrame(this.measureGutterAnimationFrameRequest);
      }
    }
  }, {
    key: "componentDidUpdate",
    value: function componentDidUpdate() {
      this.applyScroll();
    }
  }, {
    key: "renderDayColumn",
    value: function renderDayColumn(date2, id2, resource, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, components2, dayLayoutAlgorithm, now) {
      var _this$props2 = this.props, min2 = _this$props2.min, max2 = _this$props2.max;
      var daysEvents = (groupedEvents.get(id2) || []).filter(function(event) {
        return localizer2.inRange(date2, accessors2.start(event), accessors2.end(event), "day");
      });
      var daysBackgroundEvents = (groupedBackgroundEvents.get(id2) || []).filter(function(event) {
        return localizer2.inRange(date2, accessors2.start(event), accessors2.end(event), "day");
      });
      return /* @__PURE__ */ reactExports.createElement(DayColumn, Object.assign({}, this.props, {
        localizer: localizer2,
        min: localizer2.merge(date2, min2),
        max: localizer2.merge(date2, max2),
        resource: resource && id2,
        components: components2,
        isNow: localizer2.isSameDate(date2, now),
        key: "".concat(id2, "-").concat(date2),
        date: date2,
        events: daysEvents,
        backgroundEvents: daysBackgroundEvents,
        dayLayoutAlgorithm
      }));
    }
  }, {
    key: "renderResourcesFirst",
    value: function renderResourcesFirst(range2, resources, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, now, components2, dayLayoutAlgorithm) {
      var _this2 = this;
      return resources.map(function(_ref) {
        var _ref2 = _slicedToArray(_ref, 2), id2 = _ref2[0], resource = _ref2[1];
        return range2.map(function(date2) {
          return _this2.renderDayColumn(date2, id2, resource, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, components2, dayLayoutAlgorithm, now);
        });
      });
    }
  }, {
    key: "renderRangeFirst",
    value: function renderRangeFirst(range2, resources, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, now, components2, dayLayoutAlgorithm) {
      var _this3 = this;
      return range2.map(function(date2) {
        return /* @__PURE__ */ reactExports.createElement("div", {
          style: {
            display: "flex",
            minHeight: "100%",
            flex: 1
          },
          key: date2
        }, resources.map(function(_ref3) {
          var _ref4 = _slicedToArray(_ref3, 2), id2 = _ref4[0], resource = _ref4[1];
          return /* @__PURE__ */ reactExports.createElement("div", {
            style: {
              flex: 1
            },
            key: accessors2.resourceId(resource)
          }, _this3.renderDayColumn(date2, id2, resource, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, components2, dayLayoutAlgorithm, now));
        }));
      });
    }
  }, {
    key: "renderEvents",
    value: function renderEvents(range2, events2, backgroundEvents, now) {
      var _this$props3 = this.props, accessors2 = _this$props3.accessors, localizer2 = _this$props3.localizer, resourceGroupingLayout = _this$props3.resourceGroupingLayout, components2 = _this$props3.components, dayLayoutAlgorithm = _this$props3.dayLayoutAlgorithm;
      var resources = this.memoizedResources(this.props.resources, accessors2);
      var groupedEvents = resources.groupEvents(events2);
      var groupedBackgroundEvents = resources.groupEvents(backgroundEvents);
      if (!resourceGroupingLayout) {
        return this.renderResourcesFirst(range2, resources, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, now, components2, dayLayoutAlgorithm);
      } else {
        return this.renderRangeFirst(range2, resources, groupedEvents, groupedBackgroundEvents, localizer2, accessors2, now, components2, dayLayoutAlgorithm);
      }
    }
  }, {
    key: "render",
    value: function render() {
      var _this$props$allDayMax;
      var _this$props4 = this.props, events2 = _this$props4.events, backgroundEvents = _this$props4.backgroundEvents, range2 = _this$props4.range, width = _this$props4.width, rtl = _this$props4.rtl, selected = _this$props4.selected, getNow2 = _this$props4.getNow, resources = _this$props4.resources, components2 = _this$props4.components, accessors2 = _this$props4.accessors, getters = _this$props4.getters, localizer2 = _this$props4.localizer, min2 = _this$props4.min, max2 = _this$props4.max, showMultiDayTimes = _this$props4.showMultiDayTimes, longPressThreshold = _this$props4.longPressThreshold, resizable = _this$props4.resizable, resourceGroupingLayout = _this$props4.resourceGroupingLayout;
      width = width || this.state.gutterWidth;
      var start2 = range2[0], end2 = range2[range2.length - 1];
      this.slots = range2.length;
      var allDayEvents = [], rangeEvents = [], rangeBackgroundEvents = [];
      events2.forEach(function(event) {
        if (inRange(event, start2, end2, accessors2, localizer2)) {
          var eStart = accessors2.start(event), eEnd = accessors2.end(event);
          if (accessors2.allDay(event) || localizer2.startAndEndAreDateOnly(eStart, eEnd) || !showMultiDayTimes && !localizer2.isSameDate(eStart, eEnd)) {
            allDayEvents.push(event);
          } else {
            rangeEvents.push(event);
          }
        }
      });
      backgroundEvents.forEach(function(event) {
        if (inRange(event, start2, end2, accessors2, localizer2)) {
          rangeBackgroundEvents.push(event);
        }
      });
      allDayEvents.sort(function(a3, b2) {
        return sortEvents(a3, b2, accessors2, localizer2);
      });
      var headerProps = {
        range: range2,
        events: allDayEvents,
        width,
        rtl,
        getNow: getNow2,
        localizer: localizer2,
        selected,
        allDayMaxRows: this.props.showAllEvents ? Infinity : (_this$props$allDayMax = this.props.allDayMaxRows) !== null && _this$props$allDayMax !== void 0 ? _this$props$allDayMax : Infinity,
        resources: this.memoizedResources(resources, accessors2),
        selectable: this.props.selectable,
        accessors: accessors2,
        getters,
        components: components2,
        scrollRef: this.scrollRef,
        isOverflowing: this.state.isOverflowing,
        longPressThreshold,
        onSelectSlot: this.handleSelectAllDaySlot,
        onSelectEvent: this.handleSelectEvent,
        onShowMore: this.handleShowMore,
        onDoubleClickEvent: this.props.onDoubleClickEvent,
        onKeyPressEvent: this.props.onKeyPressEvent,
        onDrillDown: this.props.onDrillDown,
        getDrilldownView: this.props.getDrilldownView,
        resizable
      };
      return /* @__PURE__ */ reactExports.createElement("div", {
        className: clsx("rbc-time-view", resources && "rbc-time-view-resources"),
        ref: this.containerRef
      }, resources && resources.length > 1 && resourceGroupingLayout ? /* @__PURE__ */ reactExports.createElement(TimeGridHeaderResources, headerProps) : /* @__PURE__ */ reactExports.createElement(TimeGridHeader, headerProps), this.props.popup && this.renderOverlay(), /* @__PURE__ */ reactExports.createElement("div", {
        ref: this.contentRef,
        className: "rbc-time-content",
        onScroll: this.handleScroll
      }, /* @__PURE__ */ reactExports.createElement(TimeGutter$1, {
        date: start2,
        ref: this.gutterRef,
        localizer: localizer2,
        min: localizer2.merge(start2, min2),
        max: localizer2.merge(start2, max2),
        step: this.props.step,
        getNow: this.props.getNow,
        timeslots: this.props.timeslots,
        components: components2,
        className: "rbc-time-gutter",
        getters
      }), this.renderEvents(range2, rangeEvents, rangeBackgroundEvents, getNow2())));
    }
  }, {
    key: "renderOverlay",
    value: function renderOverlay() {
      var _this$state$overlay, _this$state, _this4 = this;
      var overlay = (_this$state$overlay = (_this$state = this.state) === null || _this$state === void 0 ? void 0 : _this$state.overlay) !== null && _this$state$overlay !== void 0 ? _this$state$overlay : {};
      var _this$props5 = this.props, accessors2 = _this$props5.accessors, localizer2 = _this$props5.localizer, components2 = _this$props5.components, getters = _this$props5.getters, selected = _this$props5.selected, popupOffset = _this$props5.popupOffset, handleDragStart = _this$props5.handleDragStart;
      var onHide2 = function onHide22() {
        return _this4.setState({
          overlay: null
        });
      };
      return /* @__PURE__ */ reactExports.createElement(PopOverlay, {
        overlay,
        accessors: accessors2,
        localizer: localizer2,
        components: components2,
        getters,
        selected,
        popupOffset,
        ref: this.containerRef,
        handleKeyPressEvent: this.handleKeyPressEvent,
        handleSelectEvent: this.handleSelectEvent,
        handleDoubleClickEvent: this.handleDoubleClickEvent,
        handleDragStart,
        show: !!overlay.position,
        overlayDisplay: this.overlayDisplay,
        onHide: onHide2
      });
    }
  }, {
    key: "clearSelection",
    value: function clearSelection() {
      clearTimeout(this._selectTimer);
      this._pendingSelection = [];
    }
  }, {
    key: "measureGutter",
    value: function measureGutter() {
      var _this5 = this;
      if (this.measureGutterAnimationFrameRequest) {
        window.cancelAnimationFrame(this.measureGutterAnimationFrameRequest);
      }
      this.measureGutterAnimationFrameRequest = window.requestAnimationFrame(function() {
        var _this5$gutterRef;
        var width = (_this5$gutterRef = _this5.gutterRef) !== null && _this5$gutterRef !== void 0 && _this5$gutterRef.current ? getWidth(_this5.gutterRef.current) : void 0;
        if (width && _this5.state.gutterWidth !== width) {
          _this5.setState({
            gutterWidth: width
          });
        }
      });
    }
  }, {
    key: "applyScroll",
    value: function applyScroll() {
      if (this._scrollRatio != null && this.props.enableAutoScroll === true) {
        var content = this.contentRef.current;
        content.scrollTop = content.scrollHeight * this._scrollRatio;
        this._scrollRatio = null;
      }
    }
  }, {
    key: "calculateScroll",
    value: function calculateScroll() {
      var props = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : this.props;
      var min2 = props.min, max2 = props.max, scrollToTime = props.scrollToTime, localizer2 = props.localizer;
      var diffMillis = localizer2.diff(localizer2.merge(scrollToTime, min2), scrollToTime, "milliseconds");
      var totalMillis = localizer2.diff(min2, max2, "milliseconds");
      this._scrollRatio = diffMillis / totalMillis;
    }
  }]);
})(reactExports.Component);
TimeGrid.defaultProps = {
  step: 30,
  timeslots: 2,
  // To be compatible with old versions, default as `false`.
  resourceGroupingLayout: false
};
var _excluded$4 = ["date", "localizer", "min", "max", "scrollToTime", "enableAutoScroll"];
var Day = /* @__PURE__ */ (function(_React$Component) {
  function Day2() {
    _classCallCheck(this, Day2);
    return _callSuper(this, Day2, arguments);
  }
  _inherits(Day2, _React$Component);
  return _createClass(Day2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, date2 = _this$props.date, localizer2 = _this$props.localizer, _this$props$min = _this$props.min, min2 = _this$props$min === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$min, _this$props$max = _this$props.max, max2 = _this$props$max === void 0 ? localizer2.endOf(/* @__PURE__ */ new Date(), "day") : _this$props$max, _this$props$scrollToT = _this$props.scrollToTime, scrollToTime = _this$props$scrollToT === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$scrollToT, _this$props$enableAut = _this$props.enableAutoScroll, enableAutoScroll = _this$props$enableAut === void 0 ? true : _this$props$enableAut, props = _objectWithoutProperties(_this$props, _excluded$4);
      var range2 = Day2.range(date2, {
        localizer: localizer2
      });
      return /* @__PURE__ */ reactExports.createElement(TimeGrid, Object.assign({}, props, {
        range: range2,
        eventOffset: 10,
        localizer: localizer2,
        min: min2,
        max: max2,
        scrollToTime,
        enableAutoScroll
      }));
    }
  }]);
})(reactExports.Component);
Day.range = function(date2, _ref) {
  var localizer2 = _ref.localizer;
  return [localizer2.startOf(date2, "day")];
};
Day.navigate = function(date2, action, _ref2) {
  var localizer2 = _ref2.localizer;
  switch (action) {
    case navigate.PREVIOUS:
      return localizer2.add(date2, -1, "day");
    case navigate.NEXT:
      return localizer2.add(date2, 1, "day");
    default:
      return date2;
  }
};
Day.title = function(date2, _ref3) {
  var localizer2 = _ref3.localizer;
  return localizer2.format(date2, "dayHeaderFormat");
};
var _excluded$3 = ["date", "localizer", "min", "max", "scrollToTime", "enableAutoScroll"];
var Week = /* @__PURE__ */ (function(_React$Component) {
  function Week2() {
    _classCallCheck(this, Week2);
    return _callSuper(this, Week2, arguments);
  }
  _inherits(Week2, _React$Component);
  return _createClass(Week2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, date2 = _this$props.date, localizer2 = _this$props.localizer, _this$props$min = _this$props.min, min2 = _this$props$min === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$min, _this$props$max = _this$props.max, max2 = _this$props$max === void 0 ? localizer2.endOf(/* @__PURE__ */ new Date(), "day") : _this$props$max, _this$props$scrollToT = _this$props.scrollToTime, scrollToTime = _this$props$scrollToT === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$scrollToT, _this$props$enableAut = _this$props.enableAutoScroll, enableAutoScroll = _this$props$enableAut === void 0 ? true : _this$props$enableAut, props = _objectWithoutProperties(_this$props, _excluded$3);
      var range2 = Week2.range(date2, this.props);
      return /* @__PURE__ */ reactExports.createElement(TimeGrid, Object.assign({}, props, {
        range: range2,
        eventOffset: 15,
        localizer: localizer2,
        min: min2,
        max: max2,
        scrollToTime,
        enableAutoScroll
      }));
    }
  }]);
})(reactExports.Component);
Week.defaultProps = TimeGrid.defaultProps;
Week.navigate = function(date2, action, _ref) {
  var localizer2 = _ref.localizer;
  switch (action) {
    case navigate.PREVIOUS:
      return localizer2.add(date2, -1, "week");
    case navigate.NEXT:
      return localizer2.add(date2, 1, "week");
    default:
      return date2;
  }
};
Week.range = function(date2, _ref2) {
  var localizer2 = _ref2.localizer;
  var firstOfWeek = localizer2.startOfWeek();
  var start2 = localizer2.startOf(date2, "week", firstOfWeek);
  var end2 = localizer2.endOf(date2, "week", firstOfWeek);
  return localizer2.range(start2, end2);
};
Week.title = function(date2, _ref3) {
  var localizer2 = _ref3.localizer;
  var _Week$range = Week.range(date2, {
    localizer: localizer2
  }), _Week$range2 = _toArray(_Week$range), start2 = _Week$range2[0], rest = _Week$range2.slice(1);
  return localizer2.format({
    start: start2,
    end: rest.pop()
  }, "dayRangeHeaderFormat");
};
var _excluded$2 = ["date", "localizer", "min", "max", "scrollToTime", "enableAutoScroll"];
function workWeekRange(date2, options) {
  return Week.range(date2, options).filter(function(d3) {
    return [6, 0].indexOf(d3.getDay()) === -1;
  });
}
var WorkWeek = /* @__PURE__ */ (function(_React$Component) {
  function WorkWeek2() {
    _classCallCheck(this, WorkWeek2);
    return _callSuper(this, WorkWeek2, arguments);
  }
  _inherits(WorkWeek2, _React$Component);
  return _createClass(WorkWeek2, [{
    key: "render",
    value: function render() {
      var _this$props = this.props, date2 = _this$props.date, localizer2 = _this$props.localizer, _this$props$min = _this$props.min, min2 = _this$props$min === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$min, _this$props$max = _this$props.max, max2 = _this$props$max === void 0 ? localizer2.endOf(/* @__PURE__ */ new Date(), "day") : _this$props$max, _this$props$scrollToT = _this$props.scrollToTime, scrollToTime = _this$props$scrollToT === void 0 ? localizer2.startOf(/* @__PURE__ */ new Date(), "day") : _this$props$scrollToT, _this$props$enableAut = _this$props.enableAutoScroll, enableAutoScroll = _this$props$enableAut === void 0 ? true : _this$props$enableAut, props = _objectWithoutProperties(_this$props, _excluded$2);
      var range2 = workWeekRange(date2, this.props);
      return /* @__PURE__ */ reactExports.createElement(TimeGrid, Object.assign({}, props, {
        range: range2,
        eventOffset: 15,
        localizer: localizer2,
        min: min2,
        max: max2,
        scrollToTime,
        enableAutoScroll
      }));
    }
  }]);
})(reactExports.Component);
WorkWeek.defaultProps = TimeGrid.defaultProps;
WorkWeek.range = workWeekRange;
WorkWeek.navigate = Week.navigate;
WorkWeek.title = function(date2, _ref) {
  var localizer2 = _ref.localizer;
  var _workWeekRange = workWeekRange(date2, {
    localizer: localizer2
  }), _workWeekRange2 = _toArray(_workWeekRange), start2 = _workWeekRange2[0], rest = _workWeekRange2.slice(1);
  return localizer2.format({
    start: start2,
    end: rest.pop()
  }, "dayRangeHeaderFormat");
};
var DEFAULT_LENGTH = 30;
function Agenda(_ref) {
  var accessors2 = _ref.accessors, components2 = _ref.components, date2 = _ref.date, events2 = _ref.events, getters = _ref.getters, _ref$length = _ref.length, length = _ref$length === void 0 ? DEFAULT_LENGTH : _ref$length, localizer2 = _ref.localizer, onDoubleClickEvent = _ref.onDoubleClickEvent, onSelectEvent = _ref.onSelectEvent, selected = _ref.selected;
  var headerRef = reactExports.useRef(null);
  var dateColRef = reactExports.useRef(null);
  var timeColRef = reactExports.useRef(null);
  var contentRef = reactExports.useRef(null);
  var tbodyRef = reactExports.useRef(null);
  reactExports.useEffect(function() {
    _adjustHeader();
  });
  var renderDay = function renderDay2(day2, events22, dayKey) {
    var Event2 = components2.event, AgendaDate = components2.date;
    events22 = events22.filter(function(e2) {
      return inRange(e2, localizer2.startOf(day2, "day"), localizer2.endOf(day2, "day"), accessors2, localizer2);
    });
    return events22.map(function(event, idx) {
      var title = accessors2.title(event);
      var end22 = accessors2.end(event);
      var start2 = accessors2.start(event);
      var userProps = getters.eventProp(event, start2, end22, isSelected(event, selected));
      var dateLabel = idx === 0 && localizer2.format(day2, "agendaDateFormat");
      var first = idx === 0 ? /* @__PURE__ */ reactExports.createElement("td", {
        rowSpan: events22.length,
        className: "rbc-agenda-date-cell"
      }, AgendaDate ? /* @__PURE__ */ reactExports.createElement(AgendaDate, {
        day: day2,
        label: dateLabel
      }) : dateLabel) : false;
      return /* @__PURE__ */ reactExports.createElement("tr", {
        key: dayKey + "_" + idx,
        className: userProps.className,
        style: userProps.style
      }, first, /* @__PURE__ */ reactExports.createElement("td", {
        className: "rbc-agenda-time-cell"
      }, timeRangeLabel(day2, event)), /* @__PURE__ */ reactExports.createElement("td", {
        className: "rbc-agenda-event-cell",
        onClick: function onClick(e2) {
          return onSelectEvent && onSelectEvent(event, e2);
        },
        onDoubleClick: function onDoubleClick(e2) {
          return onDoubleClickEvent && onDoubleClickEvent(event, e2);
        }
      }, Event2 ? /* @__PURE__ */ reactExports.createElement(Event2, {
        event,
        title
      }) : title));
    }, []);
  };
  var timeRangeLabel = function timeRangeLabel2(day2, event) {
    var labelClass = "", TimeComponent = components2.time, label = localizer2.messages.allDay;
    var end22 = accessors2.end(event);
    var start2 = accessors2.start(event);
    if (!accessors2.allDay(event)) {
      if (localizer2.eq(start2, end22)) {
        label = localizer2.format(start2, "agendaTimeFormat");
      } else if (localizer2.isSameDate(start2, end22)) {
        label = localizer2.format({
          start: start2,
          end: end22
        }, "agendaTimeRangeFormat");
      } else if (localizer2.isSameDate(day2, start2)) {
        label = localizer2.format(start2, "agendaTimeFormat");
      } else if (localizer2.isSameDate(day2, end22)) {
        label = localizer2.format(end22, "agendaTimeFormat");
      }
    }
    if (localizer2.gt(day2, start2, "day")) labelClass = "rbc-continues-prior";
    if (localizer2.lt(day2, end22, "day")) labelClass += " rbc-continues-after";
    return /* @__PURE__ */ reactExports.createElement("span", {
      className: labelClass.trim()
    }, TimeComponent ? /* @__PURE__ */ reactExports.createElement(TimeComponent, {
      event,
      day: day2,
      label
    }) : label);
  };
  var _adjustHeader = function _adjustHeader2() {
    if (!tbodyRef.current) return;
    var header = headerRef.current;
    var firstRow = tbodyRef.current.firstChild;
    if (!firstRow) return;
    var isOverflowing = contentRef.current.scrollHeight > contentRef.current.clientHeight;
    var _widths = [];
    var widths = _widths;
    _widths = [getWidth(firstRow.children[0]), getWidth(firstRow.children[1])];
    if (widths[0] !== _widths[0] || widths[1] !== _widths[1]) {
      dateColRef.current.style.width = _widths[0] + "px";
      timeColRef.current.style.width = _widths[1] + "px";
    }
    if (isOverflowing) {
      addClass(header, "rbc-header-overflowing");
      header.style.marginRight = scrollbarSize() + "px";
    } else {
      removeClass(header, "rbc-header-overflowing");
    }
  };
  var messages2 = localizer2.messages;
  var end2 = localizer2.add(date2, length, "day");
  var range2 = localizer2.range(date2, end2, "day");
  events2 = events2.filter(function(event) {
    return inRange(event, localizer2.startOf(date2, "day"), localizer2.endOf(end2, "day"), accessors2, localizer2);
  });
  events2.sort(function(a3, b2) {
    return +accessors2.start(a3) - +accessors2.start(b2);
  });
  return /* @__PURE__ */ reactExports.createElement("div", {
    className: "rbc-agenda-view"
  }, events2.length !== 0 ? /* @__PURE__ */ reactExports.createElement(reactExports.Fragment, null, /* @__PURE__ */ reactExports.createElement("table", {
    ref: headerRef,
    className: "rbc-agenda-table"
  }, /* @__PURE__ */ reactExports.createElement("thead", null, /* @__PURE__ */ reactExports.createElement("tr", null, /* @__PURE__ */ reactExports.createElement("th", {
    className: "rbc-header",
    ref: dateColRef
  }, messages2.date), /* @__PURE__ */ reactExports.createElement("th", {
    className: "rbc-header",
    ref: timeColRef
  }, messages2.time), /* @__PURE__ */ reactExports.createElement("th", {
    className: "rbc-header"
  }, messages2.event)))), /* @__PURE__ */ reactExports.createElement("div", {
    className: "rbc-agenda-content",
    ref: contentRef
  }, /* @__PURE__ */ reactExports.createElement("table", {
    className: "rbc-agenda-table"
  }, /* @__PURE__ */ reactExports.createElement("tbody", {
    ref: tbodyRef
  }, range2.map(function(day2, idx) {
    return renderDay(day2, events2, idx);
  }))))) : /* @__PURE__ */ reactExports.createElement("span", {
    className: "rbc-agenda-empty"
  }, messages2.noEventsInRange));
}
Agenda.range = function(start2, _ref2) {
  var _ref2$length = _ref2.length, length = _ref2$length === void 0 ? DEFAULT_LENGTH : _ref2$length, localizer2 = _ref2.localizer;
  var end2 = localizer2.add(start2, length, "day");
  return {
    start: start2,
    end: end2
  };
};
Agenda.navigate = function(date2, action, _ref3) {
  var _ref3$length = _ref3.length, length = _ref3$length === void 0 ? DEFAULT_LENGTH : _ref3$length, localizer2 = _ref3.localizer;
  switch (action) {
    case navigate.PREVIOUS:
      return localizer2.add(date2, -length, "day");
    case navigate.NEXT:
      return localizer2.add(date2, length, "day");
    default:
      return date2;
  }
};
Agenda.title = function(start2, _ref4) {
  var _ref4$length = _ref4.length, length = _ref4$length === void 0 ? DEFAULT_LENGTH : _ref4$length, localizer2 = _ref4.localizer;
  var end2 = localizer2.add(start2, length, "day");
  return localizer2.format({
    start: start2,
    end: end2
  }, "agendaHeaderFormat");
};
var VIEWS = _defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1(_defineProperty$1({}, views.MONTH, MonthView), views.WEEK, Week), views.WORK_WEEK, WorkWeek), views.DAY, Day), views.AGENDA, Agenda);
var _excluded$1 = ["action", "date", "today"];
function moveDate(View, _ref) {
  var action = _ref.action, date2 = _ref.date, today = _ref.today, props = _objectWithoutProperties(_ref, _excluded$1);
  View = typeof View === "string" ? VIEWS[View] : View;
  switch (action) {
    case navigate.TODAY:
      date2 = today || /* @__PURE__ */ new Date();
      break;
    case navigate.DATE:
      break;
    default:
      browserExports(View && typeof View.navigate === "function", "Calendar View components must implement a static `.navigate(date, action)` method.s");
      date2 = View.navigate(date2, action, props);
  }
  return date2;
}
function accessor(data, field) {
  var value = null;
  if (typeof field === "function") value = field(data);
  else if (typeof field === "string" && _typeof$1(data) === "object" && data != null && field in data) value = data[field];
  return value;
}
var wrapAccessor = function wrapAccessor2(acc) {
  return function(data) {
    return accessor(data, acc);
  };
};
var _excluded = ["view", "date", "getNow", "onNavigate"], _excluded2 = ["view", "toolbar", "events", "backgroundEvents", "resourceGroupingLayout", "style", "className", "elementProps", "date", "getNow", "length", "showMultiDayTimes", "onShowMore", "doShowMoreDrillDown", "components", "formats", "messages", "culture"];
function viewNames(_views) {
  if (Array.isArray(_views)) {
    return _views;
  }
  var views2 = [];
  for (var _i = 0, _Object$entries = Object.entries(_views); _i < _Object$entries.length; _i++) {
    var _Object$entries$_i = _slicedToArray(_Object$entries[_i], 2), key = _Object$entries$_i[0], value = _Object$entries$_i[1];
    if (value) {
      views2.push(key);
    }
  }
  return views2;
}
function isValidView(view, _ref) {
  var _views = _ref.views;
  var names = viewNames(_views);
  return names.indexOf(view) !== -1;
}
var Calendar = /* @__PURE__ */ (function(_React$Component) {
  function Calendar2() {
    var _this;
    _classCallCheck(this, Calendar2);
    for (var _len = arguments.length, _args = new Array(_len), _key = 0; _key < _len; _key++) {
      _args[_key] = arguments[_key];
    }
    _this = _callSuper(this, Calendar2, [].concat(_args));
    _this.getViews = function() {
      var views2 = _this.props.views;
      if (Array.isArray(views2)) {
        return transformExports(views2, function(obj, name2) {
          return obj[name2] = VIEWS[name2];
        }, {});
      }
      if (_typeof$1(views2) === "object") {
        return mapValuesExports(views2, function(value, key) {
          if (value === true) {
            return VIEWS[key];
          }
          return value;
        });
      }
      return VIEWS;
    };
    _this.getView = function() {
      var views2 = _this.getViews();
      return views2[_this.props.view];
    };
    _this.getDrilldownView = function(date2) {
      var _this$props = _this.props, view = _this$props.view, drilldownView = _this$props.drilldownView, getDrilldownView = _this$props.getDrilldownView;
      if (!getDrilldownView) return drilldownView;
      return getDrilldownView(date2, view, Object.keys(_this.getViews()));
    };
    _this.handleRangeChange = function(date2, viewComponent, view) {
      var _this$props2 = _this.props, onRangeChange = _this$props2.onRangeChange, localizer2 = _this$props2.localizer;
      if (onRangeChange) {
        if (viewComponent.range) {
          onRangeChange(viewComponent.range(date2, {
            localizer: localizer2
          }), view);
        }
      }
    };
    _this.handleNavigate = function(action, newDate) {
      var _this$props3 = _this.props, view = _this$props3.view, date2 = _this$props3.date, getNow2 = _this$props3.getNow, onNavigate = _this$props3.onNavigate, props = _objectWithoutProperties(_this$props3, _excluded);
      var ViewComponent = _this.getView();
      var today = getNow2();
      date2 = moveDate(ViewComponent, _objectSpread2(_objectSpread2({}, props), {}, {
        action,
        date: newDate || date2 || today,
        today
      }));
      onNavigate(date2, view, action);
      _this.handleRangeChange(date2, ViewComponent);
    };
    _this.handleViewChange = function(view) {
      if (view !== _this.props.view && isValidView(view, _this.props)) {
        _this.props.onView(view);
      }
      var views2 = _this.getViews();
      _this.handleRangeChange(_this.props.date || _this.props.getNow(), views2[view], view);
    };
    _this.handleSelectEvent = function() {
      for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
        args[_key2] = arguments[_key2];
      }
      notify(_this.props.onSelectEvent, args);
    };
    _this.handleDoubleClickEvent = function() {
      for (var _len3 = arguments.length, args = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
        args[_key3] = arguments[_key3];
      }
      notify(_this.props.onDoubleClickEvent, args);
    };
    _this.handleKeyPressEvent = function() {
      for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
        args[_key4] = arguments[_key4];
      }
      notify(_this.props.onKeyPressEvent, args);
    };
    _this.handleSelectSlot = function(slotInfo) {
      notify(_this.props.onSelectSlot, slotInfo);
    };
    _this.handleDrillDown = function(date2, view) {
      var onDrillDown = _this.props.onDrillDown;
      if (onDrillDown) {
        onDrillDown(date2, view, _this.drilldownView);
        return;
      }
      if (view) _this.handleViewChange(view);
      _this.handleNavigate(navigate.DATE, date2);
    };
    _this.state = {
      context: Calendar2.getContext(_this.props)
    };
    return _this;
  }
  _inherits(Calendar2, _React$Component);
  return _createClass(Calendar2, [{
    key: "render",
    value: function render() {
      var _this$props4 = this.props, view = _this$props4.view, toolbar = _this$props4.toolbar, events2 = _this$props4.events, backgroundEvents = _this$props4.backgroundEvents, resourceGroupingLayout = _this$props4.resourceGroupingLayout, style2 = _this$props4.style, className = _this$props4.className, elementProps = _this$props4.elementProps, current = _this$props4.date, getNow2 = _this$props4.getNow, length = _this$props4.length, showMultiDayTimes = _this$props4.showMultiDayTimes, onShowMore = _this$props4.onShowMore, doShowMoreDrillDown = _this$props4.doShowMoreDrillDown;
      _this$props4.components;
      _this$props4.formats;
      _this$props4.messages;
      _this$props4.culture;
      var props = _objectWithoutProperties(_this$props4, _excluded2);
      current = current || getNow2();
      var View = this.getView();
      var _this$state$context = this.state.context, accessors2 = _this$state$context.accessors, components2 = _this$state$context.components, getters = _this$state$context.getters, localizer2 = _this$state$context.localizer, viewNames2 = _this$state$context.viewNames;
      var CalToolbar = components2.toolbar || Toolbar;
      var label = View.title(current, {
        localizer: localizer2,
        length
      });
      return /* @__PURE__ */ reactExports.createElement("div", Object.assign({}, elementProps, {
        className: clsx(className, "rbc-calendar", props.rtl && "rbc-rtl"),
        style: style2
      }), toolbar && /* @__PURE__ */ reactExports.createElement(CalToolbar, {
        date: current,
        view,
        views: viewNames2,
        label,
        onView: this.handleViewChange,
        onNavigate: this.handleNavigate,
        localizer: localizer2
      }), /* @__PURE__ */ reactExports.createElement(View, Object.assign({}, props, {
        events: events2,
        backgroundEvents,
        date: current,
        getNow: getNow2,
        length,
        localizer: localizer2,
        getters,
        components: components2,
        accessors: accessors2,
        showMultiDayTimes,
        getDrilldownView: this.getDrilldownView,
        onNavigate: this.handleNavigate,
        onDrillDown: this.handleDrillDown,
        onSelectEvent: this.handleSelectEvent,
        onDoubleClickEvent: this.handleDoubleClickEvent,
        onKeyPressEvent: this.handleKeyPressEvent,
        onSelectSlot: this.handleSelectSlot,
        onShowMore,
        doShowMoreDrillDown,
        resourceGroupingLayout
      })));
    }
  }], [{
    key: "getDerivedStateFromProps",
    value: function getDerivedStateFromProps(nextProps) {
      return {
        context: Calendar2.getContext(nextProps)
      };
    }
  }, {
    key: "getContext",
    value: function getContext(_ref2) {
      var startAccessor = _ref2.startAccessor, endAccessor = _ref2.endAccessor, allDayAccessor = _ref2.allDayAccessor, tooltipAccessor = _ref2.tooltipAccessor, titleAccessor = _ref2.titleAccessor, resourceAccessor = _ref2.resourceAccessor, resourceIdAccessor = _ref2.resourceIdAccessor, resourceTitleAccessor = _ref2.resourceTitleAccessor, eventIdAccessor = _ref2.eventIdAccessor, eventPropGetter = _ref2.eventPropGetter, backgroundEventPropGetter = _ref2.backgroundEventPropGetter, slotPropGetter = _ref2.slotPropGetter, slotGroupPropGetter = _ref2.slotGroupPropGetter, dayPropGetter = _ref2.dayPropGetter, view = _ref2.view, views2 = _ref2.views, localizer2 = _ref2.localizer, culture = _ref2.culture, _ref2$messages = _ref2.messages, messages$1 = _ref2$messages === void 0 ? {} : _ref2$messages, _ref2$components = _ref2.components, components2 = _ref2$components === void 0 ? {} : _ref2$components, _ref2$formats = _ref2.formats, formats2 = _ref2$formats === void 0 ? {} : _ref2$formats;
      var names = viewNames(views2);
      var msgs = messages(messages$1);
      return {
        viewNames: names,
        localizer: mergeWithDefaults(localizer2, culture, formats2, msgs),
        getters: {
          eventProp: function eventProp() {
            return eventPropGetter && eventPropGetter.apply(void 0, arguments) || {};
          },
          backgroundEventProp: function backgroundEventProp() {
            return backgroundEventPropGetter && backgroundEventPropGetter.apply(void 0, arguments) || {};
          },
          slotProp: function slotProp() {
            return slotPropGetter && slotPropGetter.apply(void 0, arguments) || {};
          },
          slotGroupProp: function slotGroupProp() {
            return slotGroupPropGetter && slotGroupPropGetter.apply(void 0, arguments) || {};
          },
          dayProp: function dayProp() {
            return dayPropGetter && dayPropGetter.apply(void 0, arguments) || {};
          }
        },
        components: defaultsExports(components2[view] || {}, omitExports(components2, names), {
          eventWrapper: NoopWrapper,
          backgroundEventWrapper: NoopWrapper,
          eventContainerWrapper: NoopWrapper,
          dateCellWrapper: NoopWrapper,
          weekWrapper: NoopWrapper,
          timeSlotWrapper: NoopWrapper,
          timeGutterWrapper: NoopWrapper,
          timeIndicatorWrapper: NoopWrapper
        }),
        accessors: {
          start: wrapAccessor(startAccessor),
          end: wrapAccessor(endAccessor),
          allDay: wrapAccessor(allDayAccessor),
          tooltip: wrapAccessor(tooltipAccessor),
          title: wrapAccessor(titleAccessor),
          resource: wrapAccessor(resourceAccessor),
          resourceId: wrapAccessor(resourceIdAccessor),
          resourceTitle: wrapAccessor(resourceTitleAccessor),
          eventId: wrapAccessor(eventIdAccessor)
        }
      };
    }
  }]);
})(reactExports.Component);
Calendar.defaultProps = {
  events: [],
  backgroundEvents: [],
  elementProps: {},
  popup: false,
  toolbar: true,
  view: views.MONTH,
  views: [views.MONTH, views.WEEK, views.DAY, views.AGENDA],
  step: 30,
  length: 30,
  allDayMaxRows: Infinity,
  doShowMoreDrillDown: true,
  drilldownView: views.DAY,
  titleAccessor: "title",
  tooltipAccessor: "title",
  allDayAccessor: "allDay",
  startAccessor: "start",
  endAccessor: "end",
  resourceAccessor: "resourceId",
  resourceIdAccessor: "id",
  resourceTitleAccessor: "title",
  eventIdAccessor: "id",
  longPressThreshold: 250,
  getNow: function getNow() {
    return /* @__PURE__ */ new Date();
  },
  dayLayoutAlgorithm: "overlap"
};
var Calendar$1 = uncontrollable(Calendar, {
  view: "onView",
  date: "onNavigate",
  selected: "onSelectEvent"
});
var dateRangeFormat$1 = function dateRangeFormat5(_ref, culture, local) {
  var start2 = _ref.start, end2 = _ref.end;
  return "".concat(local.format(start2, "P", culture), " – ").concat(local.format(end2, "P", culture));
};
var timeRangeFormat$1 = function timeRangeFormat5(_ref2, culture, local) {
  var start2 = _ref2.start, end2 = _ref2.end;
  return "".concat(local.format(start2, "p", culture), " – ").concat(local.format(end2, "p", culture));
};
var timeRangeStartFormat$1 = function timeRangeStartFormat5(_ref3, culture, local) {
  var start2 = _ref3.start;
  return "".concat(local.format(start2, "h:mma", culture), " – ");
};
var timeRangeEndFormat$1 = function timeRangeEndFormat5(_ref4, culture, local) {
  var end2 = _ref4.end;
  return " – ".concat(local.format(end2, "h:mma", culture));
};
var weekRangeFormat$1 = function weekRangeFormat5(_ref5, culture, local) {
  var start2 = _ref5.start, end2 = _ref5.end;
  return "".concat(local.format(start2, "MMMM dd", culture), " – ").concat(local.format(end2, eq(start2, end2, "month") ? "dd" : "MMMM dd", culture));
};
var formats$1 = {
  dateFormat: "dd",
  dayFormat: "dd eee",
  weekdayFormat: "ccc",
  selectRangeFormat: timeRangeFormat$1,
  eventTimeRangeFormat: timeRangeFormat$1,
  eventTimeRangeStartFormat: timeRangeStartFormat$1,
  eventTimeRangeEndFormat: timeRangeEndFormat$1,
  timeGutterFormat: "p",
  monthHeaderFormat: "MMMM yyyy",
  dayHeaderFormat: "cccc MMM dd",
  dayRangeHeaderFormat: weekRangeFormat$1,
  agendaHeaderFormat: dateRangeFormat$1,
  agendaDateFormat: "ccc MMM dd",
  agendaTimeFormat: "p",
  agendaTimeRangeFormat: timeRangeFormat$1
};
var dateFnsLocalizer = function dateFnsLocalizer2(_ref6) {
  var startOfWeek2 = _ref6.startOfWeek, getDay2 = _ref6.getDay, _format2 = _ref6.format, locales2 = _ref6.locales;
  return new DateLocalizer({
    formats: formats$1,
    firstOfWeek: function firstOfWeek(culture) {
      return getDay2(startOfWeek2(/* @__PURE__ */ new Date(), {
        locale: locales2[culture]
      }));
    },
    format: function format2(value, formatString, culture) {
      return _format2(new Date(value), formatString, {
        locale: locales2[culture]
      });
    }
  });
};
var dragAndDrop = {};
var interopRequireDefault = { exports: {} };
var hasRequiredInteropRequireDefault;
function requireInteropRequireDefault() {
  if (hasRequiredInteropRequireDefault) return interopRequireDefault.exports;
  hasRequiredInteropRequireDefault = 1;
  (function(module) {
    function _interopRequireDefault(e2) {
      return e2 && e2.__esModule ? e2 : {
        "default": e2
      };
    }
    module.exports = _interopRequireDefault, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(interopRequireDefault);
  return interopRequireDefault.exports;
}
var withDragAndDrop = {};
var objectSpread2 = { exports: {} };
var defineProperty = { exports: {} };
var toPropertyKey = { exports: {} };
var _typeof = { exports: {} };
var hasRequired_typeof;
function require_typeof() {
  if (hasRequired_typeof) return _typeof.exports;
  hasRequired_typeof = 1;
  (function(module) {
    function _typeof2(o) {
      "@babel/helpers - typeof";
      return module.exports = _typeof2 = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function(o2) {
        return typeof o2;
      } : function(o2) {
        return o2 && "function" == typeof Symbol && o2.constructor === Symbol && o2 !== Symbol.prototype ? "symbol" : typeof o2;
      }, module.exports.__esModule = true, module.exports["default"] = module.exports, _typeof2(o);
    }
    module.exports = _typeof2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(_typeof);
  return _typeof.exports;
}
var toPrimitive = { exports: {} };
var hasRequiredToPrimitive;
function requireToPrimitive() {
  if (hasRequiredToPrimitive) return toPrimitive.exports;
  hasRequiredToPrimitive = 1;
  (function(module) {
    var _typeof2 = require_typeof()["default"];
    function toPrimitive2(t2, r2) {
      if ("object" != _typeof2(t2) || !t2) return t2;
      var e2 = t2[Symbol.toPrimitive];
      if (void 0 !== e2) {
        var i2 = e2.call(t2, r2 || "default");
        if ("object" != _typeof2(i2)) return i2;
        throw new TypeError("@@toPrimitive must return a primitive value.");
      }
      return ("string" === r2 ? String : Number)(t2);
    }
    module.exports = toPrimitive2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(toPrimitive);
  return toPrimitive.exports;
}
var hasRequiredToPropertyKey;
function requireToPropertyKey() {
  if (hasRequiredToPropertyKey) return toPropertyKey.exports;
  hasRequiredToPropertyKey = 1;
  (function(module) {
    var _typeof2 = require_typeof()["default"];
    var toPrimitive2 = requireToPrimitive();
    function toPropertyKey2(t2) {
      var i2 = toPrimitive2(t2, "string");
      return "symbol" == _typeof2(i2) ? i2 : i2 + "";
    }
    module.exports = toPropertyKey2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(toPropertyKey);
  return toPropertyKey.exports;
}
var hasRequiredDefineProperty;
function requireDefineProperty() {
  if (hasRequiredDefineProperty) return defineProperty.exports;
  hasRequiredDefineProperty = 1;
  (function(module) {
    var toPropertyKey2 = requireToPropertyKey();
    function _defineProperty2(e2, r2, t2) {
      return (r2 = toPropertyKey2(r2)) in e2 ? Object.defineProperty(e2, r2, {
        value: t2,
        enumerable: true,
        configurable: true,
        writable: true
      }) : e2[r2] = t2, e2;
    }
    module.exports = _defineProperty2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(defineProperty);
  return defineProperty.exports;
}
var hasRequiredObjectSpread2;
function requireObjectSpread2() {
  if (hasRequiredObjectSpread2) return objectSpread2.exports;
  hasRequiredObjectSpread2 = 1;
  (function(module) {
    var defineProperty2 = requireDefineProperty();
    function ownKeys2(e2, r2) {
      var t2 = Object.keys(e2);
      if (Object.getOwnPropertySymbols) {
        var o = Object.getOwnPropertySymbols(e2);
        r2 && (o = o.filter(function(r3) {
          return Object.getOwnPropertyDescriptor(e2, r3).enumerable;
        })), t2.push.apply(t2, o);
      }
      return t2;
    }
    function _objectSpread22(e2) {
      for (var r2 = 1; r2 < arguments.length; r2++) {
        var t2 = null != arguments[r2] ? arguments[r2] : {};
        r2 % 2 ? ownKeys2(Object(t2), true).forEach(function(r3) {
          defineProperty2(e2, r3, t2[r3]);
        }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e2, Object.getOwnPropertyDescriptors(t2)) : ownKeys2(Object(t2)).forEach(function(r3) {
          Object.defineProperty(e2, r3, Object.getOwnPropertyDescriptor(t2, r3));
        });
      }
      return e2;
    }
    module.exports = _objectSpread22, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(objectSpread2);
  return objectSpread2.exports;
}
var objectWithoutProperties = { exports: {} };
var objectWithoutPropertiesLoose = { exports: {} };
var hasRequiredObjectWithoutPropertiesLoose;
function requireObjectWithoutPropertiesLoose() {
  if (hasRequiredObjectWithoutPropertiesLoose) return objectWithoutPropertiesLoose.exports;
  hasRequiredObjectWithoutPropertiesLoose = 1;
  (function(module) {
    function _objectWithoutPropertiesLoose2(r2, e2) {
      if (null == r2) return {};
      var t2 = {};
      for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
        if (-1 !== e2.indexOf(n)) continue;
        t2[n] = r2[n];
      }
      return t2;
    }
    module.exports = _objectWithoutPropertiesLoose2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(objectWithoutPropertiesLoose);
  return objectWithoutPropertiesLoose.exports;
}
var hasRequiredObjectWithoutProperties;
function requireObjectWithoutProperties() {
  if (hasRequiredObjectWithoutProperties) return objectWithoutProperties.exports;
  hasRequiredObjectWithoutProperties = 1;
  (function(module) {
    var objectWithoutPropertiesLoose2 = requireObjectWithoutPropertiesLoose();
    function _objectWithoutProperties2(e2, t2) {
      if (null == e2) return {};
      var o, r2, i2 = objectWithoutPropertiesLoose2(e2, t2);
      if (Object.getOwnPropertySymbols) {
        var n = Object.getOwnPropertySymbols(e2);
        for (r2 = 0; r2 < n.length; r2++) o = n[r2], -1 === t2.indexOf(o) && {}.propertyIsEnumerable.call(e2, o) && (i2[o] = e2[o]);
      }
      return i2;
    }
    module.exports = _objectWithoutProperties2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(objectWithoutProperties);
  return objectWithoutProperties.exports;
}
var classCallCheck = { exports: {} };
var hasRequiredClassCallCheck;
function requireClassCallCheck() {
  if (hasRequiredClassCallCheck) return classCallCheck.exports;
  hasRequiredClassCallCheck = 1;
  (function(module) {
    function _classCallCheck2(a3, n) {
      if (!(a3 instanceof n)) throw new TypeError("Cannot call a class as a function");
    }
    module.exports = _classCallCheck2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(classCallCheck);
  return classCallCheck.exports;
}
var createClass = { exports: {} };
var hasRequiredCreateClass;
function requireCreateClass() {
  if (hasRequiredCreateClass) return createClass.exports;
  hasRequiredCreateClass = 1;
  (function(module) {
    var toPropertyKey2 = requireToPropertyKey();
    function _defineProperties2(e2, r2) {
      for (var t2 = 0; t2 < r2.length; t2++) {
        var o = r2[t2];
        o.enumerable = o.enumerable || false, o.configurable = true, "value" in o && (o.writable = true), Object.defineProperty(e2, toPropertyKey2(o.key), o);
      }
    }
    function _createClass2(e2, r2, t2) {
      return r2 && _defineProperties2(e2.prototype, r2), t2 && _defineProperties2(e2, t2), Object.defineProperty(e2, "prototype", {
        writable: false
      }), e2;
    }
    module.exports = _createClass2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(createClass);
  return createClass.exports;
}
var callSuper = { exports: {} };
var getPrototypeOf = { exports: {} };
var hasRequiredGetPrototypeOf;
function requireGetPrototypeOf() {
  if (hasRequiredGetPrototypeOf) return getPrototypeOf.exports;
  hasRequiredGetPrototypeOf = 1;
  (function(module) {
    function _getPrototypeOf2(t2) {
      return module.exports = _getPrototypeOf2 = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t3) {
        return t3.__proto__ || Object.getPrototypeOf(t3);
      }, module.exports.__esModule = true, module.exports["default"] = module.exports, _getPrototypeOf2(t2);
    }
    module.exports = _getPrototypeOf2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(getPrototypeOf);
  return getPrototypeOf.exports;
}
var isNativeReflectConstruct = { exports: {} };
var hasRequiredIsNativeReflectConstruct;
function requireIsNativeReflectConstruct() {
  if (hasRequiredIsNativeReflectConstruct) return isNativeReflectConstruct.exports;
  hasRequiredIsNativeReflectConstruct = 1;
  (function(module) {
    function _isNativeReflectConstruct2() {
      try {
        var t2 = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
        }));
      } catch (t3) {
      }
      return (module.exports = _isNativeReflectConstruct2 = function _isNativeReflectConstruct3() {
        return !!t2;
      }, module.exports.__esModule = true, module.exports["default"] = module.exports)();
    }
    module.exports = _isNativeReflectConstruct2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(isNativeReflectConstruct);
  return isNativeReflectConstruct.exports;
}
var possibleConstructorReturn = { exports: {} };
var assertThisInitialized = { exports: {} };
var hasRequiredAssertThisInitialized;
function requireAssertThisInitialized() {
  if (hasRequiredAssertThisInitialized) return assertThisInitialized.exports;
  hasRequiredAssertThisInitialized = 1;
  (function(module) {
    function _assertThisInitialized2(e2) {
      if (void 0 === e2) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
      return e2;
    }
    module.exports = _assertThisInitialized2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(assertThisInitialized);
  return assertThisInitialized.exports;
}
var hasRequiredPossibleConstructorReturn;
function requirePossibleConstructorReturn() {
  if (hasRequiredPossibleConstructorReturn) return possibleConstructorReturn.exports;
  hasRequiredPossibleConstructorReturn = 1;
  (function(module) {
    var _typeof2 = require_typeof()["default"];
    var assertThisInitialized2 = requireAssertThisInitialized();
    function _possibleConstructorReturn2(t2, e2) {
      if (e2 && ("object" == _typeof2(e2) || "function" == typeof e2)) return e2;
      if (void 0 !== e2) throw new TypeError("Derived constructors may only return object or undefined");
      return assertThisInitialized2(t2);
    }
    module.exports = _possibleConstructorReturn2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(possibleConstructorReturn);
  return possibleConstructorReturn.exports;
}
var hasRequiredCallSuper;
function requireCallSuper() {
  if (hasRequiredCallSuper) return callSuper.exports;
  hasRequiredCallSuper = 1;
  (function(module) {
    var getPrototypeOf2 = requireGetPrototypeOf();
    var isNativeReflectConstruct2 = requireIsNativeReflectConstruct();
    var possibleConstructorReturn2 = requirePossibleConstructorReturn();
    function _callSuper2(t2, o, e2) {
      return o = getPrototypeOf2(o), possibleConstructorReturn2(t2, isNativeReflectConstruct2() ? Reflect.construct(o, e2 || [], getPrototypeOf2(t2).constructor) : o.apply(t2, e2));
    }
    module.exports = _callSuper2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(callSuper);
  return callSuper.exports;
}
var inherits = { exports: {} };
var setPrototypeOf = { exports: {} };
var hasRequiredSetPrototypeOf;
function requireSetPrototypeOf() {
  if (hasRequiredSetPrototypeOf) return setPrototypeOf.exports;
  hasRequiredSetPrototypeOf = 1;
  (function(module) {
    function _setPrototypeOf2(t2, e2) {
      return module.exports = _setPrototypeOf2 = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t3, e3) {
        return t3.__proto__ = e3, t3;
      }, module.exports.__esModule = true, module.exports["default"] = module.exports, _setPrototypeOf2(t2, e2);
    }
    module.exports = _setPrototypeOf2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(setPrototypeOf);
  return setPrototypeOf.exports;
}
var hasRequiredInherits;
function requireInherits() {
  if (hasRequiredInherits) return inherits.exports;
  hasRequiredInherits = 1;
  (function(module) {
    var setPrototypeOf2 = requireSetPrototypeOf();
    function _inherits2(t2, e2) {
      if ("function" != typeof e2 && null !== e2) throw new TypeError("Super expression must either be null or a function");
      t2.prototype = Object.create(e2 && e2.prototype, {
        constructor: {
          value: t2,
          writable: true,
          configurable: true
        }
      }), Object.defineProperty(t2, "prototype", {
        writable: false
      }), e2 && setPrototypeOf2(t2, e2);
    }
    module.exports = _inherits2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(inherits);
  return inherits.exports;
}
var propTypes = {};
var constants = {};
var hasRequiredConstants;
function requireConstants() {
  if (hasRequiredConstants) return constants;
  hasRequiredConstants = 1;
  Object.defineProperty(constants, "__esModule", {
    value: true
  });
  constants.views = constants.navigate = void 0;
  constants.navigate = {
    PREVIOUS: "PREV",
    NEXT: "NEXT",
    TODAY: "TODAY",
    DATE: "DATE"
  };
  constants.views = {
    MONTH: "month",
    WEEK: "week",
    WORK_WEEK: "work_week",
    DAY: "day",
    AGENDA: "agenda"
  };
  return constants;
}
var hasRequiredPropTypes;
function requirePropTypes() {
  if (hasRequiredPropTypes) return propTypes;
  hasRequiredPropTypes = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(propTypes, "__esModule", {
    value: true
  });
  propTypes.views = propTypes.dateRangeFormat = propTypes.dateFormat = propTypes.accessor = propTypes.DayLayoutAlgorithmPropType = void 0;
  var _propTypes = _interopRequireDefault(/* @__PURE__ */ requirePropTypes$1());
  var _constants = requireConstants();
  var viewNames2 = Object.keys(_constants.views).map(function(k2) {
    return _constants.views[k2];
  });
  propTypes.accessor = _propTypes.default.oneOfType([_propTypes.default.string, _propTypes.default.func]);
  propTypes.dateFormat = _propTypes.default.any;
  propTypes.dateRangeFormat = _propTypes.default.func;
  propTypes.views = _propTypes.default.oneOfType([_propTypes.default.arrayOf(_propTypes.default.oneOf(viewNames2)), _propTypes.default.objectOf(function(prop, key) {
    var isBuiltinView = viewNames2.indexOf(key) !== -1 && typeof prop[key] === "boolean";
    if (isBuiltinView) {
      return null;
    } else {
      for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
        args[_key - 2] = arguments[_key];
      }
      return _propTypes.default.elementType.apply(_propTypes.default, [prop, key].concat(args));
    }
  })]);
  propTypes.DayLayoutAlgorithmPropType = _propTypes.default.oneOfType([_propTypes.default.oneOf(["overlap", "no-overlap"]), _propTypes.default.func]);
  return propTypes;
}
var EventWrapper = {};
var accessors = {};
var hasRequiredAccessors;
function requireAccessors() {
  if (hasRequiredAccessors) return accessors;
  hasRequiredAccessors = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(accessors, "__esModule", {
    value: true
  });
  accessors.accessor = accessor2;
  accessors.wrapAccessor = void 0;
  var _typeof2 = _interopRequireDefault(require_typeof());
  function accessor2(data, field) {
    var value = null;
    if (typeof field === "function") value = field(data);
    else if (typeof field === "string" && (0, _typeof2.default)(data) === "object" && data != null && field in data) value = data[field];
    return value;
  }
  accessors.wrapAccessor = function wrapAccessor3(acc) {
    return function(data) {
      return accessor2(data, acc);
    };
  };
  return accessors;
}
var DnDContext = {};
var hasRequiredDnDContext;
function requireDnDContext() {
  if (hasRequiredDnDContext) return DnDContext;
  hasRequiredDnDContext = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(DnDContext, "__esModule", {
    value: true
  });
  DnDContext.DnDContext = void 0;
  var _react = _interopRequireDefault(requireReact());
  DnDContext.DnDContext = /* @__PURE__ */ _react.default.createContext();
  return DnDContext;
}
var hasRequiredEventWrapper;
function requireEventWrapper() {
  if (hasRequiredEventWrapper) return EventWrapper;
  hasRequiredEventWrapper = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(EventWrapper, "__esModule", {
    value: true
  });
  EventWrapper.default = void 0;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _react = _interopRequireDefault(requireReact());
  var _clsx = _interopRequireDefault(clsx_m);
  var _accessors = requireAccessors();
  var _DnDContext = requireDnDContext();
  var EventWrapper$1 = /* @__PURE__ */ (function(_React$Component) {
    function EventWrapper2() {
      var _this;
      (0, _classCallCheck2.default)(this, EventWrapper2);
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      _this = (0, _callSuper2.default)(this, EventWrapper2, [].concat(args));
      _this.handleResizeUp = function(e2) {
        if (e2.button !== 0) return;
        _this.context.draggable.onBeginAction(_this.props.event, "resize", "UP");
      };
      _this.handleResizeDown = function(e2) {
        if (e2.button !== 0) return;
        _this.context.draggable.onBeginAction(_this.props.event, "resize", "DOWN");
      };
      _this.handleResizeLeft = function(e2) {
        if (e2.button !== 0) return;
        _this.context.draggable.onBeginAction(_this.props.event, "resize", "LEFT");
      };
      _this.handleResizeRight = function(e2) {
        if (e2.button !== 0) return;
        _this.context.draggable.onBeginAction(_this.props.event, "resize", "RIGHT");
      };
      _this.handleStartDragging = function(e2) {
        var _e$target$getAttribut;
        if (e2.button !== 0) return;
        var isResizeHandle = (_e$target$getAttribut = e2.target.getAttribute("class")) === null || _e$target$getAttribut === void 0 ? void 0 : _e$target$getAttribut.includes("rbc-addons-dnd-resize");
        if (!isResizeHandle) {
          var extendedEvent = (0, _objectSpread22.default)({}, _this.props.event);
          extendedEvent.sourceResource = _this.props.resource;
          _this.context.draggable.onBeginAction(_this.props.event, "move");
        }
      };
      return _this;
    }
    (0, _inherits2.default)(EventWrapper2, _React$Component);
    return (0, _createClass2.default)(EventWrapper2, [{
      key: "renderAnchor",
      value: function renderAnchor(direction) {
        var cls = direction === "Up" || direction === "Down" ? "ns" : "ew";
        return /* @__PURE__ */ _react.default.createElement("div", {
          className: "rbc-addons-dnd-resize-".concat(cls, "-anchor"),
          onMouseDown: this["handleResize".concat(direction)]
        }, /* @__PURE__ */ _react.default.createElement("div", {
          className: "rbc-addons-dnd-resize-".concat(cls, "-icon")
        }));
      }
    }, {
      key: "render",
      value: function render() {
        var _this$props = this.props, event = _this$props.event, type = _this$props.type, continuesPrior2 = _this$props.continuesPrior, continuesAfter2 = _this$props.continuesAfter, resizable = _this$props.resizable;
        var children = this.props.children;
        if (event.__isPreview) return /* @__PURE__ */ _react.default.cloneElement(children, {
          className: (0, _clsx.default)(children.props.className, "rbc-addons-dnd-drag-preview")
        });
        var draggable = this.context.draggable;
        var draggableAccessor = draggable.draggableAccessor, resizableAccessor = draggable.resizableAccessor;
        var isDraggable = draggableAccessor ? !!(0, _accessors.accessor)(event, draggableAccessor) : true;
        if (!isDraggable) {
          return children;
        }
        var isResizable = resizable && (resizableAccessor ? !!(0, _accessors.accessor)(event, resizableAccessor) : true);
        if (isResizable || isDraggable) {
          var newProps = {
            onMouseDown: this.handleStartDragging,
            onTouchStart: this.handleStartDragging
          };
          if (isResizable) {
            var StartAnchor = null;
            var EndAnchor = null;
            if (type === "date") {
              StartAnchor = !continuesPrior2 && this.renderAnchor("Left");
              EndAnchor = !continuesAfter2 && this.renderAnchor("Right");
            } else {
              StartAnchor = !continuesPrior2 && this.renderAnchor("Up");
              EndAnchor = !continuesAfter2 && this.renderAnchor("Down");
            }
            newProps.children = /* @__PURE__ */ _react.default.createElement("div", {
              className: "rbc-addons-dnd-resizable"
            }, StartAnchor, children.props.children, EndAnchor);
          }
          if (draggable.dragAndDropAction.interacting && // if an event is being dragged right now
          draggable.dragAndDropAction.event === event) {
            newProps.className = (0, _clsx.default)(children.props.className, "rbc-addons-dnd-dragged-event");
          }
          children = /* @__PURE__ */ _react.default.cloneElement(children, newProps);
        }
        return children;
      }
    }]);
  })(_react.default.Component);
  EventWrapper$1.contextType = _DnDContext.DnDContext;
  EventWrapper.default = EventWrapper$1;
  return EventWrapper;
}
var EventContainerWrapper = {};
var interopRequireWildcard = { exports: {} };
var hasRequiredInteropRequireWildcard;
function requireInteropRequireWildcard() {
  if (hasRequiredInteropRequireWildcard) return interopRequireWildcard.exports;
  hasRequiredInteropRequireWildcard = 1;
  (function(module) {
    var _typeof2 = require_typeof()["default"];
    function _interopRequireWildcard(e2, t2) {
      if ("function" == typeof WeakMap) var r2 = /* @__PURE__ */ new WeakMap(), n = /* @__PURE__ */ new WeakMap();
      return (module.exports = _interopRequireWildcard = function _interopRequireWildcard2(e3, t3) {
        if (!t3 && e3 && e3.__esModule) return e3;
        var o, i2, f = {
          __proto__: null,
          "default": e3
        };
        if (null === e3 || "object" != _typeof2(e3) && "function" != typeof e3) return f;
        if (o = t3 ? n : r2) {
          if (o.has(e3)) return o.get(e3);
          o.set(e3, f);
        }
        for (var _t in e3) "default" !== _t && {}.hasOwnProperty.call(e3, _t) && ((i2 = (o = Object.defineProperty) && Object.getOwnPropertyDescriptor(e3, _t)) && (i2.get || i2.set) ? o(f, _t, i2) : f[_t] = e3[_t]);
        return f;
      }, module.exports.__esModule = true, module.exports["default"] = module.exports)(e2, t2);
    }
    module.exports = _interopRequireWildcard, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(interopRequireWildcard);
  return interopRequireWildcard.exports;
}
var reset = {
  transition: "",
  "transition-duration": "",
  "transition-delay": "",
  "transition-timing-function": ""
};
function _animate(_ref) {
  var node = _ref.node, properties2 = _ref.properties, _ref$duration = _ref.duration, duration2 = _ref$duration === void 0 ? 200 : _ref$duration, easing = _ref.easing, callback = _ref.callback;
  var cssProperties = [];
  var cssValues = {};
  var transforms = "";
  Object.keys(properties2).forEach(function(key) {
    var value = properties2[key];
    if (isTransform(key)) transforms += key + "(" + value + ") ";
    else {
      cssValues[key] = value;
      cssProperties.push(hyphenate(key));
    }
  });
  if (transforms) {
    cssValues.transform = transforms;
    cssProperties.push("transform");
  }
  function done(event) {
    if (event.target !== event.currentTarget) return;
    style(node, reset);
    if (callback) callback.call(this, event);
  }
  if (duration2 > 0) {
    cssValues.transition = cssProperties.join(", ");
    cssValues["transition-duration"] = duration2 / 1e3 + "s";
    cssValues["transition-delay"] = "0s";
    cssValues["transition-timing-function"] = easing || "linear";
  }
  var removeListener = transitionEnd(node, done, duration2);
  node.clientLeft;
  style(node, cssValues);
  return {
    cancel: function cancel3() {
      removeListener();
      style(node, reset);
    }
  };
}
function animate(nodeOrOptions, properties2, duration2, easing, callback) {
  if (!("nodeType" in nodeOrOptions)) {
    return _animate(nodeOrOptions);
  }
  if (!properties2) {
    throw new Error("must include properties to animate");
  }
  if (typeof easing === "function") {
    callback = easing;
    easing = "";
  }
  return _animate({
    node: nodeOrOptions,
    properties: properties2,
    duration: duration2,
    easing,
    callback
  });
}
function attribute(node, attr, val) {
  if (node) {
    if (typeof val === "undefined") {
      return node.getAttribute(attr);
    }
    if (!val && val !== "") {
      node.removeAttribute(attr);
    } else {
      node.setAttribute(attr, String(val));
    }
  }
}
function childElements(node) {
  return node ? Array.from(node.children) : [];
}
function clear(node) {
  if (node) {
    while (node.firstChild) {
      node.removeChild(node.firstChild);
    }
    return node;
  }
  return null;
}
var toArray = Function.prototype.bind.call(Function.prototype.call, [].slice);
function childNodes(node) {
  return node ? toArray(node.childNodes) : [];
}
function filterEvents(selector, handler) {
  return function filterHandler(e2) {
    var top2 = e2.currentTarget;
    var target = e2.target;
    var matches2 = qsa(top2, selector);
    if (matches2.some(function(match2) {
      return contains(match2, target);
    })) handler.call(this, e2);
  };
}
function insertAfter(node, refNode) {
  if (node && refNode && refNode.parentNode) {
    if (refNode.nextSibling) {
      refNode.parentNode.insertBefore(node, refNode.nextSibling);
    } else {
      refNode.parentNode.appendChild(node);
    }
    return node;
  }
  return null;
}
var regExpInputs = /^(?:input|select|textarea|button)$/i;
function isInput(node) {
  return node ? regExpInputs.test(node.nodeName) : false;
}
function isVisible(node) {
  return node ? !!(node.offsetWidth || node.offsetHeight || node.getClientRects().length) : false;
}
function collectSiblings(node, refNode, selector) {
  if (refNode === void 0) {
    refNode = null;
  }
  if (selector === void 0) {
    selector = null;
  }
  var siblings2 = [];
  for (; node; node = node.nextElementSibling) {
    if (node !== refNode) {
      if (selector && matches(node, selector)) {
        break;
      }
      siblings2.push(node);
    }
  }
  return siblings2;
}
function nextUntil(node, selector) {
  return collectSiblings(node, node, selector);
}
function collectElements(node, direction) {
  var nextNode = null;
  var nodes = [];
  nextNode = node ? node[direction] : null;
  while (nextNode && nextNode.nodeType !== 9) {
    nodes.push(nextNode);
    nextNode = nextNode[direction] || null;
  }
  return nodes;
}
function parents(node) {
  return collectElements(node, "parentElement");
}
function prepend(node, parent) {
  if (node && parent) {
    if (parent.firstElementChild) {
      parent.insertBefore(node, parent.firstElementChild);
    } else {
      parent.appendChild(node);
    }
    return node;
  }
  return null;
}
function remove(node) {
  if (node && node.parentNode) {
    node.parentNode.removeChild(node);
    return node;
  }
  return null;
}
function scrollParent(element, firstPossible) {
  var position2 = style(element, "position");
  var excludeStatic = position2 === "absolute";
  var ownerDoc = element.ownerDocument;
  if (position2 === "fixed") return ownerDoc || document;
  while ((element = element.parentNode) && !isDocument(element)) {
    var isStatic = excludeStatic && style(element, "position") === "static";
    var style$1 = (style(element, "overflow") || "") + (style(element, "overflow-y") || "") + style(element, "overflow-x");
    if (isStatic) continue;
    if (/(auto|scroll)/.test(style$1) && (firstPossible || height(element) < element.scrollHeight)) {
      return element;
    }
  }
  return ownerDoc || document;
}
function scrollTo(selected, scrollParent$1) {
  var offset$12 = offset(selected);
  var poff = {
    top: 0,
    left: 0
  };
  if (!selected) return void 0;
  var list = scrollParent$1 || scrollParent(selected);
  var isWin = isWindow(list);
  var listScrollTop = scrollTop(list);
  var listHeight = height(list, true);
  if (!isWin) poff = offset(list);
  offset$12 = {
    top: offset$12.top - poff.top,
    left: offset$12.left - poff.left,
    height: offset$12.height,
    width: offset$12.width
  };
  var selectedHeight = offset$12.height;
  var selectedTop = offset$12.top + (isWin ? 0 : listScrollTop);
  var bottom2 = selectedTop + selectedHeight;
  listScrollTop = listScrollTop > selectedTop ? selectedTop : bottom2 > listScrollTop + listHeight ? bottom2 - listHeight : listScrollTop;
  var id2 = request(function() {
    return scrollTop(list, listScrollTop);
  });
  return function() {
    return cancel(id2);
  };
}
function siblings(node) {
  return collectSiblings(node && node.parentElement ? node.parentElement.firstElementChild : null, node);
}
var regExpNbspEntity = /&nbsp;/gi;
var regExpNbspHex = /\xA0/g;
var regExpSpaces = /\s+([^\s])/gm;
function text(node, trim, singleSpaces) {
  if (trim === void 0) {
    trim = true;
  }
  if (singleSpaces === void 0) {
    singleSpaces = true;
  }
  var elementText = "";
  if (node) {
    elementText = (node.textContent || "").replace(regExpNbspEntity, " ").replace(regExpNbspHex, " ");
    if (trim) {
      elementText = elementText.trim();
    }
    if (singleSpaces) {
      elementText = elementText.replace(regExpSpaces, " $1");
    }
  }
  return elementText;
}
function toggleClass(element, className) {
  if (element.classList) element.classList.toggle(className);
  else if (hasClass(element, className)) removeClass(element, className);
  else addClass(element, className);
}
const index = {
  addEventListener: addEventListener$1,
  removeEventListener,
  triggerEvent,
  animate,
  filter: filterEvents,
  listen,
  style,
  getComputedStyle: getComputedStyle$1,
  attribute,
  activeElement,
  ownerDocument: ownerDocument$1,
  ownerWindow,
  requestAnimationFrame: request,
  cancelAnimationFrame: cancel,
  matches,
  height,
  width: getWidth,
  offset,
  offsetParent,
  position,
  contains,
  scrollbarSize,
  scrollLeft,
  scrollParent,
  scrollTo,
  scrollTop,
  querySelectorAll: qsa,
  closest,
  addClass,
  removeClass,
  hasClass,
  toggleClass,
  transitionEnd,
  childNodes,
  childElements,
  nextUntil,
  parents,
  siblings,
  clear,
  insertAfter,
  isInput,
  isVisible,
  prepend,
  remove,
  text
};
const esm = /* @__PURE__ */ Object.freeze(/* @__PURE__ */ Object.defineProperty({
  __proto__: null,
  activeElement,
  addClass,
  addEventListener: addEventListener$1,
  animate,
  attribute,
  cancelAnimationFrame: cancel,
  childElements,
  childNodes,
  clear,
  closest,
  contains,
  default: index,
  filter: filterEvents,
  getComputedStyle: getComputedStyle$1,
  hasClass,
  height,
  insertAfter,
  isInput,
  isVisible,
  listen,
  matches,
  nextUntil,
  offset,
  offsetParent,
  ownerDocument: ownerDocument$1,
  ownerWindow,
  parents,
  position,
  prepend,
  querySelectorAll: qsa,
  remove,
  removeClass,
  removeEventListener,
  requestAnimationFrame: request,
  scrollLeft,
  scrollParent,
  scrollTo,
  scrollTop,
  scrollbarSize,
  siblings,
  style,
  text,
  toggleClass,
  transitionEnd,
  triggerEvent,
  width: getWidth
}, Symbol.toStringTag, { value: "Module" }));
var querySelectorAll = { exports: {} };
var hasRequiredQuerySelectorAll;
function requireQuerySelectorAll() {
  if (hasRequiredQuerySelectorAll) return querySelectorAll.exports;
  hasRequiredQuerySelectorAll = 1;
  (function(module, exports) {
    exports.__esModule = true;
    exports.default = qsa2;
    var toArray2 = Function.prototype.bind.call(Function.prototype.call, [].slice);
    function qsa2(element, selector) {
      return toArray2(element.querySelectorAll(selector));
    }
    module.exports = exports["default"];
  })(querySelectorAll, querySelectorAll.exports);
  return querySelectorAll.exports;
}
var Selection = {};
var hasRequiredSelection$1;
function requireSelection$1() {
  if (hasRequiredSelection$1) return Selection;
  hasRequiredSelection$1 = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(Selection, "__esModule", {
    value: true
  });
  Selection.default = void 0;
  Selection.getBoundsForNode = getBoundsForNode2;
  Selection.getEventNodeFromPoint = getEventNodeFromPoint2;
  Selection.getShowMoreNodeFromPoint = getShowMoreNodeFromPoint2;
  Selection.isEvent = isEvent2;
  Selection.isShowMore = isShowMore2;
  Selection.objectsCollide = objectsCollide2;
  var _typeof2 = _interopRequireDefault(require_typeof());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _contains = _interopRequireDefault(contains);
  var _closest = _interopRequireDefault(closest);
  var _listen = _interopRequireDefault(listen);
  function addEventListener2(type, handler) {
    var target = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : document;
    return (0, _listen.default)(target, type, handler, {
      passive: false
    });
  }
  function isOverContainer2(container, x2, y3) {
    return !container || (0, _contains.default)(container, document.elementFromPoint(x2, y3));
  }
  function getEventNodeFromPoint2(node, _ref) {
    var clientX = _ref.clientX, clientY = _ref.clientY;
    var target = document.elementFromPoint(clientX, clientY);
    return (0, _closest.default)(target, ".rbc-event", node);
  }
  function getShowMoreNodeFromPoint2(node, _ref2) {
    var clientX = _ref2.clientX, clientY = _ref2.clientY;
    var target = document.elementFromPoint(clientX, clientY);
    return (0, _closest.default)(target, ".rbc-show-more", node);
  }
  function isEvent2(node, bounds) {
    return !!getEventNodeFromPoint2(node, bounds);
  }
  function isShowMore2(node, bounds) {
    return !!getShowMoreNodeFromPoint2(node, bounds);
  }
  function getEventCoordinates2(e2) {
    var target = e2;
    if (e2.touches && e2.touches.length) {
      target = e2.touches[0];
    }
    return {
      clientX: target.clientX,
      clientY: target.clientY,
      pageX: target.pageX,
      pageY: target.pageY
    };
  }
  var clickTolerance2 = 5;
  var clickInterval2 = 250;
  var Selection$12 = /* @__PURE__ */ (function() {
    function Selection2(node) {
      var _ref3 = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref3$global = _ref3.global, global2 = _ref3$global === void 0 ? false : _ref3$global, _ref3$longPressThresh = _ref3.longPressThreshold, longPressThreshold = _ref3$longPressThresh === void 0 ? 250 : _ref3$longPressThresh, _ref3$validContainers = _ref3.validContainers, validContainers = _ref3$validContainers === void 0 ? [] : _ref3$validContainers;
      (0, _classCallCheck2.default)(this, Selection2);
      this._initialEvent = null;
      this.selecting = false;
      this.isDetached = false;
      this.container = node;
      this.globalMouse = !node || global2;
      this.longPressThreshold = longPressThreshold;
      this.validContainers = validContainers;
      this._listeners = /* @__PURE__ */ Object.create(null);
      this._handleInitialEvent = this._handleInitialEvent.bind(this);
      this._handleMoveEvent = this._handleMoveEvent.bind(this);
      this._handleTerminatingEvent = this._handleTerminatingEvent.bind(this);
      this._keyListener = this._keyListener.bind(this);
      this._dropFromOutsideListener = this._dropFromOutsideListener.bind(this);
      this._dragOverFromOutsideListener = this._dragOverFromOutsideListener.bind(this);
      this._removeTouchMoveWindowListener = addEventListener2("touchmove", function() {
      }, window);
      this._removeKeyDownListener = addEventListener2("keydown", this._keyListener);
      this._removeKeyUpListener = addEventListener2("keyup", this._keyListener);
      this._removeDropFromOutsideListener = addEventListener2("drop", this._dropFromOutsideListener);
      this._removeDragOverFromOutsideListener = addEventListener2("dragover", this._dragOverFromOutsideListener);
      this._addInitialEventListener();
    }
    return (0, _createClass2.default)(Selection2, [{
      key: "on",
      value: function on2(type, handler) {
        var handlers = this._listeners[type] || (this._listeners[type] = []);
        handlers.push(handler);
        return {
          remove: function remove2() {
            var idx = handlers.indexOf(handler);
            if (idx !== -1) handlers.splice(idx, 1);
          }
        };
      }
    }, {
      key: "emit",
      value: function emit(type) {
        for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
          args[_key - 1] = arguments[_key];
        }
        var result;
        var handlers = this._listeners[type] || [];
        handlers.forEach(function(fn3) {
          if (result === void 0) result = fn3.apply(void 0, args);
        });
        return result;
      }
    }, {
      key: "teardown",
      value: function teardown() {
        this._initialEvent = null;
        this._initialEventData = null;
        this._selectRect = null;
        this.selecting = false;
        this._lastClickData = null;
        this.isDetached = true;
        this._listeners = /* @__PURE__ */ Object.create(null);
        this._removeTouchMoveWindowListener && this._removeTouchMoveWindowListener();
        this._removeInitialEventListener && this._removeInitialEventListener();
        this._removeEndListener && this._removeEndListener();
        this._onEscListener && this._onEscListener();
        this._removeMoveListener && this._removeMoveListener();
        this._removeKeyUpListener && this._removeKeyUpListener();
        this._removeKeyDownListener && this._removeKeyDownListener();
        this._removeDropFromOutsideListener && this._removeDropFromOutsideListener();
        this._removeDragOverFromOutsideListener && this._removeDragOverFromOutsideListener();
      }
    }, {
      key: "isSelected",
      value: function isSelected2(node) {
        var box = this._selectRect;
        if (!box || !this.selecting) return false;
        return objectsCollide2(box, getBoundsForNode2(node));
      }
    }, {
      key: "filter",
      value: function filter(items) {
        var box = this._selectRect;
        if (!box || !this.selecting) return [];
        return items.filter(this.isSelected, this);
      }
      // Adds a listener that will call the handler only after the user has pressed on the screen
      // without moving their finger for 250ms.
    }, {
      key: "_addLongPressListener",
      value: function _addLongPressListener(handler, initialEvent) {
        var _this = this;
        var timer = null;
        var removeTouchMoveListener = null;
        var removeTouchEndListener = null;
        var handleTouchStart = function handleTouchStart2(initialEvent2) {
          timer = setTimeout(function() {
            cleanup();
            handler(initialEvent2);
          }, _this.longPressThreshold);
          removeTouchMoveListener = addEventListener2("touchmove", function() {
            return cleanup();
          });
          removeTouchEndListener = addEventListener2("touchend", function() {
            return cleanup();
          });
        };
        var removeTouchStartListener = addEventListener2("touchstart", handleTouchStart);
        var cleanup = function cleanup2() {
          if (timer) {
            clearTimeout(timer);
          }
          if (removeTouchMoveListener) {
            removeTouchMoveListener();
          }
          if (removeTouchEndListener) {
            removeTouchEndListener();
          }
          timer = null;
          removeTouchMoveListener = null;
          removeTouchEndListener = null;
        };
        if (initialEvent) {
          handleTouchStart(initialEvent);
        }
        return function() {
          cleanup();
          removeTouchStartListener();
        };
      }
      // Listen for mousedown and touchstart events. When one is received, disable the other and setup
      // future event handling based on the type of event.
    }, {
      key: "_addInitialEventListener",
      value: function _addInitialEventListener() {
        var _this2 = this;
        var removeMouseDownListener = addEventListener2("mousedown", function(e2) {
          _this2._removeInitialEventListener();
          _this2._handleInitialEvent(e2);
          _this2._removeInitialEventListener = addEventListener2("mousedown", _this2._handleInitialEvent);
        });
        var removeTouchStartListener = addEventListener2("touchstart", function(e2) {
          _this2._removeInitialEventListener();
          _this2._removeInitialEventListener = _this2._addLongPressListener(_this2._handleInitialEvent, e2);
        });
        this._removeInitialEventListener = function() {
          removeMouseDownListener();
          removeTouchStartListener();
        };
      }
    }, {
      key: "_dropFromOutsideListener",
      value: function _dropFromOutsideListener(e2) {
        var _getEventCoordinates = getEventCoordinates2(e2), pageX = _getEventCoordinates.pageX, pageY = _getEventCoordinates.pageY, clientX = _getEventCoordinates.clientX, clientY = _getEventCoordinates.clientY;
        this.emit("dropFromOutside", {
          x: pageX,
          y: pageY,
          clientX,
          clientY
        });
        e2.preventDefault();
      }
    }, {
      key: "_dragOverFromOutsideListener",
      value: function _dragOverFromOutsideListener(e2) {
        var _getEventCoordinates2 = getEventCoordinates2(e2), pageX = _getEventCoordinates2.pageX, pageY = _getEventCoordinates2.pageY, clientX = _getEventCoordinates2.clientX, clientY = _getEventCoordinates2.clientY;
        this.emit("dragOverFromOutside", {
          x: pageX,
          y: pageY,
          clientX,
          clientY
        });
        e2.preventDefault();
      }
    }, {
      key: "_handleInitialEvent",
      value: function _handleInitialEvent(e2) {
        this._initialEvent = e2;
        if (this.isDetached) {
          return;
        }
        var _getEventCoordinates3 = getEventCoordinates2(e2), clientX = _getEventCoordinates3.clientX, clientY = _getEventCoordinates3.clientY, pageX = _getEventCoordinates3.pageX, pageY = _getEventCoordinates3.pageY;
        var node = this.container(), collides, offsetData;
        if (e2.which === 3 || e2.button === 2 || !isOverContainer2(node, clientX, clientY)) return;
        if (!this.globalMouse && node && !(0, _contains.default)(node, e2.target)) {
          var _normalizeDistance = normalizeDistance2(0), top2 = _normalizeDistance.top, left2 = _normalizeDistance.left, bottom2 = _normalizeDistance.bottom, right2 = _normalizeDistance.right;
          offsetData = getBoundsForNode2(node);
          collides = objectsCollide2({
            top: offsetData.top - top2,
            left: offsetData.left - left2,
            bottom: offsetData.bottom + bottom2,
            right: offsetData.right + right2
          }, {
            top: pageY,
            left: pageX
          });
          if (!collides) return;
        }
        var result = this.emit("beforeSelect", this._initialEventData = {
          isTouch: /^touch/.test(e2.type),
          x: pageX,
          y: pageY,
          clientX,
          clientY
        });
        if (result === false) return;
        switch (e2.type) {
          case "mousedown":
            this._removeEndListener = addEventListener2("mouseup", this._handleTerminatingEvent);
            this._onEscListener = addEventListener2("keydown", this._handleTerminatingEvent);
            this._removeMoveListener = addEventListener2("mousemove", this._handleMoveEvent);
            break;
          case "touchstart":
            this._handleMoveEvent(e2);
            this._removeEndListener = addEventListener2("touchend", this._handleTerminatingEvent);
            this._removeMoveListener = addEventListener2("touchmove", this._handleMoveEvent);
            break;
        }
      }
      // Check whether provided event target element
      // - is contained within a valid container
    }, {
      key: "_isWithinValidContainer",
      value: function _isWithinValidContainer(e2) {
        var eventTarget = e2.target;
        var containers = this.validContainers;
        if (!containers || !containers.length || !eventTarget) {
          return true;
        }
        return containers.some(function(target) {
          return !!eventTarget.closest(target);
        });
      }
    }, {
      key: "_handleTerminatingEvent",
      value: function _handleTerminatingEvent(e2) {
        var selecting = this.selecting;
        var bounds = this._selectRect;
        if (!selecting && e2.type.includes("key")) {
          e2 = this._initialEvent;
        }
        this.selecting = false;
        this._removeEndListener && this._removeEndListener();
        this._removeMoveListener && this._removeMoveListener();
        this._selectRect = null;
        this._initialEvent = null;
        this._initialEventData = null;
        if (!e2) return;
        var inRoot = !this.container || (0, _contains.default)(this.container(), e2.target);
        var isWithinValidContainer = this._isWithinValidContainer(e2);
        if (e2.key === "Escape" || !isWithinValidContainer) {
          return this.emit("reset");
        }
        if (!selecting && inRoot) {
          return this._handleClickEvent(e2);
        }
        if (selecting) return this.emit("select", bounds);
        return this.emit("reset");
      }
    }, {
      key: "_handleClickEvent",
      value: function _handleClickEvent(e2) {
        var _getEventCoordinates4 = getEventCoordinates2(e2), pageX = _getEventCoordinates4.pageX, pageY = _getEventCoordinates4.pageY, clientX = _getEventCoordinates4.clientX, clientY = _getEventCoordinates4.clientY;
        var now = (/* @__PURE__ */ new Date()).getTime();
        if (this._lastClickData && now - this._lastClickData.timestamp < clickInterval2) {
          this._lastClickData = null;
          return this.emit("doubleClick", {
            x: pageX,
            y: pageY,
            clientX,
            clientY
          });
        }
        this._lastClickData = {
          timestamp: now
        };
        return this.emit("click", {
          x: pageX,
          y: pageY,
          clientX,
          clientY
        });
      }
    }, {
      key: "_handleMoveEvent",
      value: function _handleMoveEvent(e2) {
        if (this._initialEventData === null || this.isDetached) {
          return;
        }
        var _this$_initialEventDa = this._initialEventData, x2 = _this$_initialEventDa.x, y3 = _this$_initialEventDa.y;
        var _getEventCoordinates5 = getEventCoordinates2(e2), pageX = _getEventCoordinates5.pageX, pageY = _getEventCoordinates5.pageY;
        var w2 = Math.abs(x2 - pageX);
        var h3 = Math.abs(y3 - pageY);
        var left2 = Math.min(pageX, x2), top2 = Math.min(pageY, y3), old = this.selecting;
        var click = this.isClick(pageX, pageY);
        if (click && !old && !(w2 || h3)) {
          return;
        }
        if (!old && !click) {
          this.emit("selectStart", this._initialEventData);
        }
        if (!click) {
          this.selecting = true;
          this._selectRect = {
            top: top2,
            left: left2,
            x: pageX,
            y: pageY,
            right: left2 + w2,
            bottom: top2 + h3
          };
          this.emit("selecting", this._selectRect);
        }
        e2.preventDefault();
      }
    }, {
      key: "_keyListener",
      value: function _keyListener(e2) {
        this.ctrl = e2.metaKey || e2.ctrlKey;
      }
    }, {
      key: "isClick",
      value: function isClick(pageX, pageY) {
        var _this$_initialEventDa2 = this._initialEventData, x2 = _this$_initialEventDa2.x, y3 = _this$_initialEventDa2.y, isTouch = _this$_initialEventDa2.isTouch;
        return !isTouch && Math.abs(pageX - x2) <= clickTolerance2 && Math.abs(pageY - y3) <= clickTolerance2;
      }
    }]);
  })();
  function normalizeDistance2() {
    var distance = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : 0;
    if ((0, _typeof2.default)(distance) !== "object") distance = {
      top: distance,
      left: distance,
      right: distance,
      bottom: distance
    };
    return distance;
  }
  function objectsCollide2(nodeA, nodeB) {
    var tolerance = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : 0;
    var _getBoundsForNode = getBoundsForNode2(nodeA), aTop = _getBoundsForNode.top, aLeft = _getBoundsForNode.left, _getBoundsForNode$rig = _getBoundsForNode.right, aRight = _getBoundsForNode$rig === void 0 ? aLeft : _getBoundsForNode$rig, _getBoundsForNode$bot = _getBoundsForNode.bottom, aBottom = _getBoundsForNode$bot === void 0 ? aTop : _getBoundsForNode$bot;
    var _getBoundsForNode2 = getBoundsForNode2(nodeB), bTop = _getBoundsForNode2.top, bLeft = _getBoundsForNode2.left, _getBoundsForNode2$ri = _getBoundsForNode2.right, bRight = _getBoundsForNode2$ri === void 0 ? bLeft : _getBoundsForNode2$ri, _getBoundsForNode2$bo = _getBoundsForNode2.bottom, bBottom = _getBoundsForNode2$bo === void 0 ? bTop : _getBoundsForNode2$bo;
    return !// 'a' bottom doesn't touch 'b' top
    (aBottom - tolerance < bTop || // 'a' top doesn't touch 'b' bottom
    aTop + tolerance > bBottom || // 'a' right doesn't touch 'b' left
    aRight - tolerance < bLeft || // 'a' left doesn't touch 'b' right
    aLeft + tolerance > bRight);
  }
  function getBoundsForNode2(node) {
    if (!node.getBoundingClientRect) return node;
    var rect = node.getBoundingClientRect(), left2 = rect.left + pageOffset2("left"), top2 = rect.top + pageOffset2("top");
    return {
      top: top2,
      left: left2,
      right: (node.offsetWidth || 0) + left2,
      bottom: (node.offsetHeight || 0) + top2
    };
  }
  function pageOffset2(dir) {
    if (dir === "left") return window.pageXOffset || document.body.scrollLeft || 0;
    if (dir === "top") return window.pageYOffset || document.body.scrollTop || 0;
  }
  Selection.default = Selection$12;
  return Selection;
}
var TimeGridEvent = {};
var hasRequiredTimeGridEvent;
function requireTimeGridEvent() {
  if (hasRequiredTimeGridEvent) return TimeGridEvent;
  hasRequiredTimeGridEvent = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(TimeGridEvent, "__esModule", {
    value: true
  });
  TimeGridEvent.default = void 0;
  var _defineProperty2 = _interopRequireDefault(requireDefineProperty());
  var _objectSpread3 = _interopRequireDefault(requireObjectSpread2());
  var _clsx = _interopRequireDefault(clsx_m);
  var _react = _interopRequireDefault(requireReact());
  function stringifyPercent2(v) {
    return typeof v === "string" ? v : v + "%";
  }
  function TimeGridEvent$12(props) {
    var style2 = props.style, className = props.className, event = props.event, accessors2 = props.accessors, rtl = props.rtl, selected = props.selected, label = props.label, continuesPrior2 = props.continuesPrior, continuesAfter2 = props.continuesAfter, getters = props.getters, onClick = props.onClick, onDoubleClick = props.onDoubleClick, isBackgroundEvent = props.isBackgroundEvent, onKeyPress = props.onKeyPress, _props$components = props.components, Event2 = _props$components.event, EventWrapper2 = _props$components.eventWrapper;
    var title = accessors2.title(event);
    var tooltip = accessors2.tooltip(event);
    var end2 = accessors2.end(event);
    var start2 = accessors2.start(event);
    var userProps = getters.eventProp(event, start2, end2, selected);
    var inner = [/* @__PURE__ */ _react.default.createElement("div", {
      key: "1",
      className: "rbc-event-label"
    }, label), /* @__PURE__ */ _react.default.createElement("div", {
      key: "2",
      className: "rbc-event-content"
    }, Event2 ? /* @__PURE__ */ _react.default.createElement(Event2, {
      event,
      title
    }) : title)];
    var height2 = style2.height, top2 = style2.top, width = style2.width, xOffset = style2.xOffset;
    var eventStyle = (0, _objectSpread3.default)((0, _objectSpread3.default)({}, userProps.style), {}, (0, _defineProperty2.default)({
      top: stringifyPercent2(top2),
      height: stringifyPercent2(height2),
      width: stringifyPercent2(width)
    }, rtl ? "right" : "left", stringifyPercent2(xOffset)));
    return /* @__PURE__ */ _react.default.createElement(EventWrapper2, Object.assign({
      type: "time"
    }, props), /* @__PURE__ */ _react.default.createElement("div", {
      role: "button",
      tabIndex: 0,
      onClick,
      onDoubleClick,
      style: eventStyle,
      onKeyDown: onKeyPress,
      title: tooltip ? (typeof label === "string" ? label + ": " : "") + tooltip : void 0,
      className: (0, _clsx.default)(isBackgroundEvent ? "rbc-background-event" : "rbc-event", className, userProps.className, {
        "rbc-selected": selected,
        "rbc-event-continues-earlier": continuesPrior2,
        "rbc-event-continues-later": continuesAfter2
      })
    }, inner));
  }
  TimeGridEvent.default = TimeGridEvent$12;
  return TimeGridEvent;
}
var common = {};
var hasRequiredCommon;
function requireCommon() {
  if (hasRequiredCommon) return common;
  hasRequiredCommon = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(common, "__esModule", {
    value: true
  });
  common.dragAccessors = void 0;
  common.eventTimes = eventTimes;
  common.mergeComponents = mergeComponents;
  common.pointInColumn = pointInColumn;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _objectWithoutProperties2 = _interopRequireDefault(requireObjectWithoutProperties());
  var _accessors = requireAccessors();
  var _react = requireReact();
  var _excluded3 = ["children"];
  common.dragAccessors = {
    start: (0, _accessors.wrapAccessor)(function(e2) {
      return e2.start;
    }),
    end: (0, _accessors.wrapAccessor)(function(e2) {
      return e2.end;
    })
  };
  function nest() {
    for (var _len = arguments.length, Components = new Array(_len), _key = 0; _key < _len; _key++) {
      Components[_key] = arguments[_key];
    }
    var Nest = function Nest2(_ref) {
      var children = _ref.children, props = (0, _objectWithoutProperties2.default)(_ref, _excluded3);
      return Components.filter(Boolean).reduceRight(function(child, Component) {
        return /* @__PURE__ */ (0, _react.createElement)(Component, props, child);
      }, children);
    };
    return Nest;
  }
  function mergeComponents() {
    var components = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    var addons = arguments.length > 1 ? arguments[1] : void 0;
    var keys = Object.keys(addons);
    var result = (0, _objectSpread22.default)({}, components);
    keys.forEach(function(key) {
      result[key] = components[key] ? nest(components[key], addons[key]) : addons[key];
    });
    return result;
  }
  function pointInColumn(bounds, point) {
    var left2 = bounds.left, right2 = bounds.right, top2 = bounds.top;
    var x2 = point.x, y3 = point.y;
    return x2 < right2 + 10 && x2 > left2 && y3 > top2;
  }
  function eventTimes(event, accessors2, localizer2) {
    var start2 = accessors2.start(event);
    var end2 = accessors2.end(event);
    var isZeroDuration = localizer2.eq(start2, end2, "minutes") && localizer2.diff(start2, end2, "minutes") === 0;
    if (isZeroDuration) end2 = localizer2.add(end2, 1, "day");
    var duration2 = localizer2.diff(start2, end2, "milliseconds");
    return {
      start: start2,
      end: end2,
      duration: duration2
    };
  }
  return common;
}
var hasRequiredEventContainerWrapper;
function requireEventContainerWrapper() {
  if (hasRequiredEventContainerWrapper) return EventContainerWrapper;
  hasRequiredEventContainerWrapper = 1;
  var _interopRequireWildcard = requireInteropRequireWildcard().default;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(EventContainerWrapper, "__esModule", {
    value: true
  });
  EventContainerWrapper.default = void 0;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _domHelpers = esm;
  var _querySelectorAll = _interopRequireDefault(requireQuerySelectorAll());
  var _react = _interopRequireDefault(requireReact());
  var _DnDContext = requireDnDContext();
  var _Selection = _interopRequireWildcard(requireSelection$1());
  var _TimeGridEvent = _interopRequireDefault(requireTimeGridEvent());
  var _common = requireCommon();
  var EventContainerWrapper$1 = /* @__PURE__ */ (function(_React$Component) {
    function EventContainerWrapper2() {
      var _this;
      (0, _classCallCheck2.default)(this, EventContainerWrapper2);
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      _this = (0, _callSuper2.default)(this, EventContainerWrapper2, [].concat(args));
      _this.handleMove = function(point, bounds) {
        if (!(0, _common.pointInColumn)(bounds, point)) return _this.reset();
        var event = _this.context.draggable.dragAndDropAction.event;
        var _this$props = _this.props, accessors2 = _this$props.accessors, slotMetrics = _this$props.slotMetrics;
        var newSlot = slotMetrics.closestSlotFromPoint({
          y: point.y - _this.eventOffsetTop,
          x: point.x
        }, bounds);
        var _eventTimes = (0, _common.eventTimes)(event, accessors2, _this.props.localizer), duration2 = _eventTimes.duration;
        var newEnd = _this.props.localizer.add(newSlot, duration2, "milliseconds");
        _this.update(event, slotMetrics.getRange(newSlot, newEnd, false, true));
      };
      _this.handleDropFromOutside = function(point, boundaryBox) {
        var _this$props2 = _this.props, slotMetrics = _this$props2.slotMetrics, resource = _this$props2.resource;
        var start2 = slotMetrics.closestSlotFromPoint({
          y: point.y,
          x: point.x
        }, boundaryBox);
        var end2 = _this._calculateDnDEnd(start2);
        _this.context.draggable.onDropFromOutside({
          start: start2,
          end: end2,
          allDay: false,
          resource
        });
        _this.reset();
      };
      _this.handleDragOverFromOutside = function(point, bounds) {
        var slotMetrics = _this.props.slotMetrics;
        var start2 = slotMetrics.closestSlotFromPoint({
          y: point.y,
          x: point.x
        }, bounds);
        var end2 = _this._calculateDnDEnd(start2);
        var event = _this.context.draggable.dragFromOutsideItem();
        _this.update(event, slotMetrics.getRange(start2, end2, false, true));
      };
      _this._calculateDnDEnd = function(start2) {
        var _this$props3 = _this.props, accessors2 = _this$props3.accessors, slotMetrics = _this$props3.slotMetrics, localizer2 = _this$props3.localizer;
        var event = _this.context.draggable.dragFromOutsideItem();
        var _eventTimes2 = (0, _common.eventTimes)(event, accessors2, localizer2), eventDuration = _eventTimes2.duration;
        var end2 = slotMetrics.nextSlot(start2);
        var eventHasDuration = !isNaN(eventDuration);
        if (eventHasDuration) {
          var eventEndSlot = localizer2.add(start2, eventDuration, "milliseconds");
          end2 = new Date(Math.max(eventEndSlot, end2));
        }
        return end2;
      };
      _this.updateParentScroll = function(parent, node) {
        setTimeout(function() {
          var draggedEl = (0, _querySelectorAll.default)(node, ".rbc-addons-dnd-drag-preview")[0];
          if (draggedEl) {
            if (draggedEl.offsetTop < parent.scrollTop) {
              (0, _domHelpers.scrollTop)(parent, Math.max(draggedEl.offsetTop, 0));
            } else if (draggedEl.offsetTop + draggedEl.offsetHeight > parent.scrollTop + parent.clientHeight) {
              (0, _domHelpers.scrollTop)(parent, Math.min(draggedEl.offsetTop - parent.offsetHeight + draggedEl.offsetHeight, parent.scrollHeight));
            }
          }
        });
      };
      _this._selectable = function() {
        var wrapper = _this.ref.current;
        var node = wrapper.children[0];
        var isBeingDragged = false;
        var selector = _this._selector = new _Selection.default(function() {
          return wrapper.closest(".rbc-time-view");
        });
        var parent = (0, _domHelpers.scrollParent)(wrapper);
        selector.on("beforeSelect", function(point) {
          var dragAndDropAction = _this.context.draggable.dragAndDropAction;
          if (!dragAndDropAction.action) return false;
          if (dragAndDropAction.action === "resize") {
            return (0, _common.pointInColumn)((0, _Selection.getBoundsForNode)(node), point);
          }
          var eventNode = (0, _Selection.getEventNodeFromPoint)(node, point);
          if (!eventNode) return false;
          _this.eventOffsetTop = point.y - (0, _Selection.getBoundsForNode)(eventNode).top;
        });
        selector.on("selecting", function(box) {
          var bounds = (0, _Selection.getBoundsForNode)(node);
          var dragAndDropAction = _this.context.draggable.dragAndDropAction;
          if (dragAndDropAction.action === "move") {
            _this.updateParentScroll(parent, node);
            _this.handleMove(box, bounds);
          }
          if (dragAndDropAction.action === "resize") {
            _this.updateParentScroll(parent, node);
            _this.handleResize(box, bounds);
          }
        });
        selector.on("dropFromOutside", function(point) {
          if (!_this.context.draggable.onDropFromOutside) return;
          var bounds = (0, _Selection.getBoundsForNode)(node);
          if (!(0, _common.pointInColumn)(bounds, point)) return;
          _this.handleDropFromOutside(point, bounds);
        });
        selector.on("dragOverFromOutside", function(point) {
          var item = _this.context.draggable.dragFromOutsideItem ? _this.context.draggable.dragFromOutsideItem() : null;
          if (!item) return;
          var bounds = (0, _Selection.getBoundsForNode)(node);
          if (!(0, _common.pointInColumn)(bounds, point)) return _this.reset();
          _this.handleDragOverFromOutside(point, bounds);
        });
        selector.on("selectStart", function() {
          isBeingDragged = true;
          _this.context.draggable.onStart();
        });
        selector.on("select", function(point) {
          var bounds = (0, _Selection.getBoundsForNode)(node);
          isBeingDragged = false;
          var dragAndDropAction = _this.context.draggable.dragAndDropAction;
          if (dragAndDropAction.action === "resize") {
            _this.handleInteractionEnd();
          } else if (!_this.state.event || !(0, _common.pointInColumn)(bounds, point)) {
            return;
          } else {
            _this.handleInteractionEnd();
          }
        });
        selector.on("click", function() {
          if (isBeingDragged) _this.reset();
          _this.context.draggable.onEnd(null);
        });
        selector.on("reset", function() {
          _this.reset();
          _this.context.draggable.onEnd(null);
        });
      };
      _this.handleInteractionEnd = function() {
        var resource = _this.props.resource;
        var event = _this.state.event;
        _this.reset();
        _this.context.draggable.onEnd({
          start: event.start,
          end: event.end,
          resourceId: resource
        });
      };
      _this._teardownSelectable = function() {
        if (!_this._selector) return;
        _this._selector.teardown();
        _this._selector = null;
      };
      _this.state = {};
      _this.ref = /* @__PURE__ */ _react.default.createRef();
      return _this;
    }
    (0, _inherits2.default)(EventContainerWrapper2, _React$Component);
    return (0, _createClass2.default)(EventContainerWrapper2, [{
      key: "componentDidMount",
      value: function componentDidMount() {
        this._selectable();
      }
    }, {
      key: "componentWillUnmount",
      value: function componentWillUnmount() {
        this._teardownSelectable();
      }
    }, {
      key: "reset",
      value: function reset2() {
        if (this.state.event) this.setState({
          event: null,
          top: null,
          height: null
        });
      }
    }, {
      key: "update",
      value: function update(event, _ref) {
        var startDate = _ref.startDate, endDate = _ref.endDate, top2 = _ref.top, height2 = _ref.height;
        var lastEvent = this.state.event;
        if (lastEvent && startDate === lastEvent.start && endDate === lastEvent.end) {
          return;
        }
        this.setState({
          top: top2,
          height: height2,
          event: (0, _objectSpread22.default)((0, _objectSpread22.default)({}, event), {}, {
            start: startDate,
            end: endDate
          })
        });
      }
    }, {
      key: "handleResize",
      value: function handleResize(point, bounds) {
        var _this$props4 = this.props, accessors2 = _this$props4.accessors, slotMetrics = _this$props4.slotMetrics, localizer2 = _this$props4.localizer;
        var _this$context$draggab = this.context.draggable.dragAndDropAction, event = _this$context$draggab.event, direction = _this$context$draggab.direction;
        var newTime = slotMetrics.closestSlotFromPoint(point, bounds);
        var _eventTimes3 = (0, _common.eventTimes)(event, accessors2, localizer2), start2 = _eventTimes3.start, end2 = _eventTimes3.end;
        var newRange;
        if (direction === "UP") {
          var newStart = localizer2.min(newTime, slotMetrics.closestSlotFromDate(end2, -1));
          newRange = slotMetrics.getRange(newStart, end2);
          newRange = (0, _objectSpread22.default)((0, _objectSpread22.default)({}, newRange), {}, {
            endDate: end2
          });
        } else if (direction === "DOWN") {
          var newEnd = localizer2.max(newTime, slotMetrics.closestSlotFromDate(start2));
          newRange = slotMetrics.getRange(start2, newEnd);
          newRange = (0, _objectSpread22.default)((0, _objectSpread22.default)({}, newRange), {}, {
            startDate: start2
          });
        }
        this.update(event, newRange);
      }
    }, {
      key: "renderContent",
      value: function renderContent() {
        var _this$props5 = this.props, children = _this$props5.children, accessors2 = _this$props5.accessors, components = _this$props5.components, getters = _this$props5.getters, slotMetrics = _this$props5.slotMetrics, localizer2 = _this$props5.localizer;
        var _this$state = this.state, event = _this$state.event, top2 = _this$state.top, height2 = _this$state.height;
        if (!event) return children;
        var events2 = children.props.children;
        var start2 = event.start, end2 = event.end;
        var label;
        var format2 = "eventTimeRangeFormat";
        var startsBeforeDay = slotMetrics.startsBeforeDay(start2);
        var startsAfterDay = slotMetrics.startsAfterDay(end2);
        if (startsBeforeDay) format2 = "eventTimeRangeEndFormat";
        else if (startsAfterDay) format2 = "eventTimeRangeStartFormat";
        if (startsBeforeDay && startsAfterDay) label = localizer2.messages.allDay;
        else label = localizer2.format({
          start: start2,
          end: end2
        }, format2);
        return /* @__PURE__ */ _react.default.cloneElement(children, {
          children: /* @__PURE__ */ _react.default.createElement(_react.default.Fragment, null, events2, event && /* @__PURE__ */ _react.default.createElement(_TimeGridEvent.default, {
            event,
            label,
            className: "rbc-addons-dnd-drag-preview",
            style: {
              top: top2,
              height: height2,
              width: 100
            },
            getters,
            components,
            accessors: (0, _objectSpread22.default)((0, _objectSpread22.default)({}, accessors2), _common.dragAccessors),
            continuesPrior: startsBeforeDay,
            continuesAfter: startsAfterDay
          }))
        });
      }
    }, {
      key: "render",
      value: function render() {
        return /* @__PURE__ */ _react.default.createElement("div", {
          ref: this.ref
        }, this.renderContent());
      }
    }]);
  })(_react.default.Component);
  EventContainerWrapper$1.contextType = _DnDContext.DnDContext;
  EventContainerWrapper.default = EventContainerWrapper$1;
  return EventContainerWrapper;
}
var WeekWrapper = {};
var toConsumableArray = { exports: {} };
var arrayWithoutHoles = { exports: {} };
var arrayLikeToArray = { exports: {} };
var hasRequiredArrayLikeToArray;
function requireArrayLikeToArray() {
  if (hasRequiredArrayLikeToArray) return arrayLikeToArray.exports;
  hasRequiredArrayLikeToArray = 1;
  (function(module) {
    function _arrayLikeToArray2(r2, a3) {
      (null == a3 || a3 > r2.length) && (a3 = r2.length);
      for (var e2 = 0, n = Array(a3); e2 < a3; e2++) n[e2] = r2[e2];
      return n;
    }
    module.exports = _arrayLikeToArray2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(arrayLikeToArray);
  return arrayLikeToArray.exports;
}
var hasRequiredArrayWithoutHoles;
function requireArrayWithoutHoles() {
  if (hasRequiredArrayWithoutHoles) return arrayWithoutHoles.exports;
  hasRequiredArrayWithoutHoles = 1;
  (function(module) {
    var arrayLikeToArray2 = requireArrayLikeToArray();
    function _arrayWithoutHoles2(r2) {
      if (Array.isArray(r2)) return arrayLikeToArray2(r2);
    }
    module.exports = _arrayWithoutHoles2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(arrayWithoutHoles);
  return arrayWithoutHoles.exports;
}
var iterableToArray = { exports: {} };
var hasRequiredIterableToArray;
function requireIterableToArray() {
  if (hasRequiredIterableToArray) return iterableToArray.exports;
  hasRequiredIterableToArray = 1;
  (function(module) {
    function _iterableToArray2(r2) {
      if ("undefined" != typeof Symbol && null != r2[Symbol.iterator] || null != r2["@@iterator"]) return Array.from(r2);
    }
    module.exports = _iterableToArray2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(iterableToArray);
  return iterableToArray.exports;
}
var unsupportedIterableToArray = { exports: {} };
var hasRequiredUnsupportedIterableToArray;
function requireUnsupportedIterableToArray() {
  if (hasRequiredUnsupportedIterableToArray) return unsupportedIterableToArray.exports;
  hasRequiredUnsupportedIterableToArray = 1;
  (function(module) {
    var arrayLikeToArray2 = requireArrayLikeToArray();
    function _unsupportedIterableToArray2(r2, a3) {
      if (r2) {
        if ("string" == typeof r2) return arrayLikeToArray2(r2, a3);
        var t2 = {}.toString.call(r2).slice(8, -1);
        return "Object" === t2 && r2.constructor && (t2 = r2.constructor.name), "Map" === t2 || "Set" === t2 ? Array.from(r2) : "Arguments" === t2 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t2) ? arrayLikeToArray2(r2, a3) : void 0;
      }
    }
    module.exports = _unsupportedIterableToArray2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(unsupportedIterableToArray);
  return unsupportedIterableToArray.exports;
}
var nonIterableSpread = { exports: {} };
var hasRequiredNonIterableSpread;
function requireNonIterableSpread() {
  if (hasRequiredNonIterableSpread) return nonIterableSpread.exports;
  hasRequiredNonIterableSpread = 1;
  (function(module) {
    function _nonIterableSpread2() {
      throw new TypeError("Invalid attempt to spread non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
    }
    module.exports = _nonIterableSpread2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(nonIterableSpread);
  return nonIterableSpread.exports;
}
var hasRequiredToConsumableArray;
function requireToConsumableArray() {
  if (hasRequiredToConsumableArray) return toConsumableArray.exports;
  hasRequiredToConsumableArray = 1;
  (function(module) {
    var arrayWithoutHoles2 = requireArrayWithoutHoles();
    var iterableToArray2 = requireIterableToArray();
    var unsupportedIterableToArray2 = requireUnsupportedIterableToArray();
    var nonIterableSpread2 = requireNonIterableSpread();
    function _toConsumableArray2(r2) {
      return arrayWithoutHoles2(r2) || iterableToArray2(r2) || unsupportedIterableToArray2(r2) || nonIterableSpread2();
    }
    module.exports = _toConsumableArray2, module.exports.__esModule = true, module.exports["default"] = module.exports;
  })(toConsumableArray);
  return toConsumableArray.exports;
}
var EventRow = {};
var EventRowMixin = {};
var EventCell = {};
var hasRequiredEventCell;
function requireEventCell() {
  if (hasRequiredEventCell) return EventCell;
  hasRequiredEventCell = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(EventCell, "__esModule", {
    value: true
  });
  EventCell.default = void 0;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _objectWithoutProperties2 = _interopRequireDefault(requireObjectWithoutProperties());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _react = _interopRequireDefault(requireReact());
  var _clsx = _interopRequireDefault(clsx_m);
  var _excluded3 = ["style", "className", "event", "selected", "isAllDay", "onSelect", "onDoubleClick", "onKeyPress", "localizer", "continuesPrior", "continuesAfter", "accessors", "getters", "children", "components", "slotStart", "slotEnd"];
  var EventCell$12 = /* @__PURE__ */ (function(_React$Component) {
    function EventCell2() {
      (0, _classCallCheck2.default)(this, EventCell2);
      return (0, _callSuper2.default)(this, EventCell2, arguments);
    }
    (0, _inherits2.default)(EventCell2, _React$Component);
    return (0, _createClass2.default)(EventCell2, [{
      key: "render",
      value: function render() {
        var _this$props = this.props, style2 = _this$props.style, className = _this$props.className, event = _this$props.event, selected = _this$props.selected, isAllDay = _this$props.isAllDay, onSelect = _this$props.onSelect, _onDoubleClick = _this$props.onDoubleClick, onKeyPress = _this$props.onKeyPress, localizer2 = _this$props.localizer, continuesPrior2 = _this$props.continuesPrior, continuesAfter2 = _this$props.continuesAfter, accessors2 = _this$props.accessors, getters = _this$props.getters, children = _this$props.children, _this$props$component = _this$props.components, Event2 = _this$props$component.event, EventWrapper2 = _this$props$component.eventWrapper, slotStart = _this$props.slotStart, slotEnd = _this$props.slotEnd, props = (0, _objectWithoutProperties2.default)(_this$props, _excluded3);
        delete props.resizable;
        var title = accessors2.title(event);
        var tooltip = accessors2.tooltip(event);
        var end2 = accessors2.end(event);
        var start2 = accessors2.start(event);
        var allDay = accessors2.allDay(event);
        var showAsAllDay = isAllDay || allDay || localizer2.diff(start2, localizer2.ceil(end2, "day"), "day") > 1;
        var userProps = getters.eventProp(event, start2, end2, selected);
        var content = /* @__PURE__ */ _react.default.createElement("div", {
          className: "rbc-event-content",
          title: tooltip || void 0
        }, Event2 ? /* @__PURE__ */ _react.default.createElement(Event2, {
          event,
          continuesPrior: continuesPrior2,
          continuesAfter: continuesAfter2,
          title,
          isAllDay: allDay,
          localizer: localizer2,
          slotStart,
          slotEnd
        }) : title);
        return /* @__PURE__ */ _react.default.createElement(EventWrapper2, Object.assign({}, this.props, {
          type: "date"
        }), /* @__PURE__ */ _react.default.createElement("div", Object.assign({}, props, {
          style: (0, _objectSpread22.default)((0, _objectSpread22.default)({}, userProps.style), style2),
          className: (0, _clsx.default)("rbc-event", className, userProps.className, {
            "rbc-selected": selected,
            "rbc-event-allday": showAsAllDay,
            "rbc-event-continues-prior": continuesPrior2,
            "rbc-event-continues-after": continuesAfter2
          }),
          onClick: function onClick(e2) {
            return onSelect && onSelect(event, e2);
          },
          onDoubleClick: function onDoubleClick(e2) {
            return _onDoubleClick && _onDoubleClick(event, e2);
          },
          onKeyDown: function onKeyDown(e2) {
            return onKeyPress && onKeyPress(event, e2);
          }
        }), typeof children === "function" ? children(content) : content));
      }
    }]);
  })(_react.default.Component);
  EventCell.default = EventCell$12;
  return EventCell;
}
var selection = {};
var hasRequiredSelection;
function requireSelection() {
  if (hasRequiredSelection) return selection;
  hasRequiredSelection = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(selection, "__esModule", {
    value: true
  });
  selection.dateCellSelection = dateCellSelection2;
  selection.getSlotAtX = getSlotAtX2;
  selection.isSelected = isSelected2;
  selection.pointInBox = pointInBox2;
  selection.slotWidth = slotWidth2;
  var _isEqual = _interopRequireDefault(requireIsEqual());
  function isSelected2(event, selected) {
    if (!event || selected == null) return false;
    return (0, _isEqual.default)(event, selected);
  }
  function slotWidth2(rowBox, slots) {
    var rowWidth = rowBox.right - rowBox.left;
    var cellWidth = rowWidth / slots;
    return cellWidth;
  }
  function getSlotAtX2(rowBox, x2, rtl, slots) {
    var cellWidth = slotWidth2(rowBox, slots);
    return rtl ? slots - 1 - Math.floor((x2 - rowBox.left) / cellWidth) : Math.floor((x2 - rowBox.left) / cellWidth);
  }
  function pointInBox2(box, _ref) {
    var x2 = _ref.x, y3 = _ref.y;
    return y3 >= box.top && y3 <= box.bottom && x2 >= box.left && x2 <= box.right;
  }
  function dateCellSelection2(start2, rowBox, box, slots, rtl) {
    var startIdx = -1;
    var endIdx = -1;
    var lastSlotIdx = slots - 1;
    var cellWidth = slotWidth2(rowBox, slots);
    var currentSlot = getSlotAtX2(rowBox, box.x, rtl, slots);
    var isCurrentRow = rowBox.top < box.y && rowBox.bottom > box.y;
    var isStartRow = rowBox.top < start2.y && rowBox.bottom > start2.y;
    var isAboveStart = start2.y > rowBox.bottom;
    var isBelowStart = rowBox.top > start2.y;
    var isBetween2 = box.top < rowBox.top && box.bottom > rowBox.bottom;
    if (isBetween2) {
      startIdx = 0;
      endIdx = lastSlotIdx;
    }
    if (isCurrentRow) {
      if (isBelowStart) {
        startIdx = 0;
        endIdx = currentSlot;
      } else if (isAboveStart) {
        startIdx = currentSlot;
        endIdx = lastSlotIdx;
      }
    }
    if (isStartRow) {
      startIdx = endIdx = rtl ? lastSlotIdx - Math.floor((start2.x - rowBox.left) / cellWidth) : Math.floor((start2.x - rowBox.left) / cellWidth);
      if (isCurrentRow) {
        if (currentSlot < startIdx) startIdx = currentSlot;
        else endIdx = currentSlot;
      } else if (start2.y < box.y) {
        endIdx = lastSlotIdx;
      } else {
        startIdx = 0;
      }
    }
    return {
      startIdx,
      endIdx
    };
  }
  return selection;
}
var hasRequiredEventRowMixin;
function requireEventRowMixin() {
  if (hasRequiredEventRowMixin) return EventRowMixin;
  hasRequiredEventRowMixin = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(EventRowMixin, "__esModule", {
    value: true
  });
  EventRowMixin.default = void 0;
  var _propTypes = _interopRequireDefault(/* @__PURE__ */ requirePropTypes$1());
  var _react = _interopRequireDefault(requireReact());
  var _EventCell = _interopRequireDefault(requireEventCell());
  var _selection = requireSelection();
  EventRowMixin.default = {
    propTypes: {
      slotMetrics: _propTypes.default.object.isRequired,
      selected: _propTypes.default.object,
      isAllDay: _propTypes.default.bool,
      accessors: _propTypes.default.object.isRequired,
      localizer: _propTypes.default.object.isRequired,
      components: _propTypes.default.object.isRequired,
      getters: _propTypes.default.object.isRequired,
      onSelect: _propTypes.default.func,
      onDoubleClick: _propTypes.default.func,
      onKeyPress: _propTypes.default.func
    },
    defaultProps: {
      segments: [],
      selected: {}
    },
    renderEvent: function renderEvent2(props, event) {
      var selected = props.selected;
      props.isAllDay;
      var accessors2 = props.accessors, getters = props.getters, onSelect = props.onSelect, onDoubleClick = props.onDoubleClick, onKeyPress = props.onKeyPress, localizer2 = props.localizer, slotMetrics = props.slotMetrics, components = props.components, resizable = props.resizable;
      var continuesPrior2 = slotMetrics.continuesPrior(event);
      var continuesAfter2 = slotMetrics.continuesAfter(event);
      return /* @__PURE__ */ _react.default.createElement(_EventCell.default, {
        event,
        getters,
        localizer: localizer2,
        accessors: accessors2,
        components,
        onSelect,
        onDoubleClick,
        onKeyPress,
        continuesPrior: continuesPrior2,
        continuesAfter: continuesAfter2,
        slotStart: slotMetrics.first,
        slotEnd: slotMetrics.last,
        selected: (0, _selection.isSelected)(event, selected),
        resizable
      });
    },
    renderSpan: function renderSpan2(slots, len, key) {
      var content = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : " ";
      var per = Math.abs(len) / slots * 100 + "%";
      return /* @__PURE__ */ _react.default.createElement("div", {
        key,
        className: "rbc-row-segment",
        style: {
          WebkitFlexBasis: per,
          flexBasis: per,
          maxWidth: per
        }
      }, content);
    }
  };
  return EventRowMixin;
}
var hasRequiredEventRow;
function requireEventRow() {
  if (hasRequiredEventRow) return EventRow;
  hasRequiredEventRow = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(EventRow, "__esModule", {
    value: true
  });
  EventRow.default = void 0;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _clsx = _interopRequireDefault(clsx_m);
  var _react = _interopRequireDefault(requireReact());
  var _EventRowMixin = _interopRequireDefault(requireEventRowMixin());
  var EventRow$12 = /* @__PURE__ */ (function(_React$Component) {
    function EventRow2() {
      (0, _classCallCheck2.default)(this, EventRow2);
      return (0, _callSuper2.default)(this, EventRow2, arguments);
    }
    (0, _inherits2.default)(EventRow2, _React$Component);
    return (0, _createClass2.default)(EventRow2, [{
      key: "render",
      value: function render() {
        var _this = this;
        var _this$props = this.props, segments = _this$props.segments, slots = _this$props.slotMetrics.slots, className = _this$props.className;
        var lastEnd = 1;
        return /* @__PURE__ */ _react.default.createElement("div", {
          className: (0, _clsx.default)(className, "rbc-row")
        }, segments.reduce(function(row, _ref, li) {
          var event = _ref.event, left2 = _ref.left, right2 = _ref.right, span = _ref.span;
          var key = "_lvl_" + li;
          var gap = left2 - lastEnd;
          var content = _EventRowMixin.default.renderEvent(_this.props, event);
          if (gap) row.push(_EventRowMixin.default.renderSpan(slots, gap, "".concat(key, "_gap")));
          row.push(_EventRowMixin.default.renderSpan(slots, span, key, content));
          lastEnd = right2 + 1;
          return row;
        }, []));
      }
    }]);
  })(_react.default.Component);
  EventRow$12.defaultProps = (0, _objectSpread22.default)({}, _EventRowMixin.default.defaultProps);
  EventRow.default = EventRow$12;
  return EventRow;
}
var eventLevels = {};
var hasRequiredEventLevels;
function requireEventLevels() {
  if (hasRequiredEventLevels) return eventLevels;
  hasRequiredEventLevels = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(eventLevels, "__esModule", {
    value: true
  });
  eventLevels.endOfRange = endOfRange2;
  eventLevels.eventLevels = eventLevels$12;
  eventLevels.eventSegments = eventSegments2;
  eventLevels.inRange = inRange2;
  eventLevels.segsOverlap = segsOverlap2;
  eventLevels.sortEvents = sortEvents2;
  eventLevels.sortWeekEvents = sortWeekEvents2;
  var _toConsumableArray2 = _interopRequireDefault(requireToConsumableArray());
  var _findIndex = _interopRequireDefault(requireFindIndex());
  function endOfRange2(_ref) {
    var dateRange = _ref.dateRange, _ref$unit = _ref.unit, unit = _ref$unit === void 0 ? "day" : _ref$unit, localizer2 = _ref.localizer;
    return {
      first: dateRange[0],
      last: localizer2.add(dateRange[dateRange.length - 1], 1, unit)
    };
  }
  function eventSegments2(event, range2, accessors2, localizer2) {
    var _endOfRange = endOfRange2({
      dateRange: range2,
      localizer: localizer2
    }), first = _endOfRange.first, last = _endOfRange.last;
    var slots = localizer2.diff(first, last, "day");
    var start2 = localizer2.max(localizer2.startOf(accessors2.start(event), "day"), first);
    var end2 = localizer2.min(localizer2.ceil(accessors2.end(event), "day"), last);
    var padding = (0, _findIndex.default)(range2, function(x2) {
      return localizer2.isSameDate(x2, start2);
    });
    var span = localizer2.diff(start2, end2, "day");
    span = Math.min(span, slots);
    span = Math.max(span - localizer2.segmentOffset, 1);
    return {
      event,
      span,
      left: padding + 1,
      right: Math.max(padding + span, 1)
    };
  }
  function eventLevels$12(rowSegments) {
    var limit = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : Infinity;
    var i2, j, seg, levels = [], extra = [];
    for (i2 = 0; i2 < rowSegments.length; i2++) {
      seg = rowSegments[i2];
      for (j = 0; j < levels.length; j++) if (!segsOverlap2(seg, levels[j])) break;
      if (j >= limit) {
        extra.push(seg);
      } else {
        (levels[j] || (levels[j] = [])).push(seg);
      }
    }
    for (i2 = 0; i2 < levels.length; i2++) {
      levels[i2].sort(function(a3, b2) {
        return a3.left - b2.left;
      });
    }
    return {
      levels,
      extra
    };
  }
  function inRange2(e2, start2, end2, accessors2, localizer2) {
    var event = {
      start: accessors2.start(e2),
      end: accessors2.end(e2)
    };
    var range2 = {
      start: start2,
      end: end2
    };
    return localizer2.inEventRange({
      event,
      range: range2
    });
  }
  function segsOverlap2(seg, otherSegs) {
    return otherSegs.some(function(otherSeg) {
      return otherSeg.left <= seg.right && otherSeg.right >= seg.left;
    });
  }
  function sortWeekEvents2(events2, accessors2, localizer2) {
    var base = (0, _toConsumableArray2.default)(events2);
    var multiDayEvents = [];
    var standardEvents = [];
    base.forEach(function(event) {
      var startCheck = accessors2.start(event);
      var endCheck = accessors2.end(event);
      if (localizer2.daySpan(startCheck, endCheck) > 1) {
        multiDayEvents.push(event);
      } else {
        standardEvents.push(event);
      }
    });
    var multiSorted = multiDayEvents.sort(function(a3, b2) {
      return sortEvents2(a3, b2, accessors2, localizer2);
    });
    var standardSorted = standardEvents.sort(function(a3, b2) {
      return sortEvents2(a3, b2, accessors2, localizer2);
    });
    return [].concat((0, _toConsumableArray2.default)(multiSorted), (0, _toConsumableArray2.default)(standardSorted));
  }
  function sortEvents2(eventA, eventB, accessors2, localizer2) {
    var evtA = {
      start: accessors2.start(eventA),
      end: accessors2.end(eventA),
      allDay: accessors2.allDay(eventA)
    };
    var evtB = {
      start: accessors2.start(eventB),
      end: accessors2.end(eventB),
      allDay: accessors2.allDay(eventB)
    };
    return localizer2.sortEvents({
      evtA,
      evtB
    });
  }
  return eventLevels;
}
var hasRequiredWeekWrapper;
function requireWeekWrapper() {
  if (hasRequiredWeekWrapper) return WeekWrapper;
  hasRequiredWeekWrapper = 1;
  var _interopRequireWildcard = requireInteropRequireWildcard().default;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(WeekWrapper, "__esModule", {
    value: true
  });
  WeekWrapper.default = void 0;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _toConsumableArray2 = _interopRequireDefault(requireToConsumableArray());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _react = _interopRequireDefault(requireReact());
  var _EventRow = _interopRequireDefault(requireEventRow());
  var _Selection = _interopRequireWildcard(requireSelection$1());
  var _eventLevels = requireEventLevels();
  var _selection = requireSelection();
  var _common = requireCommon();
  var _DnDContext = requireDnDContext();
  var WeekWrapper$1 = /* @__PURE__ */ (function(_React$Component) {
    function WeekWrapper2() {
      var _this;
      (0, _classCallCheck2.default)(this, WeekWrapper2);
      for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
        args[_key] = arguments[_key];
      }
      _this = (0, _callSuper2.default)(this, WeekWrapper2, [].concat(args));
      _this.handleMove = function(point, bounds, draggedEvent) {
        if (!(0, _selection.pointInBox)(bounds, point)) return _this.reset();
        var event = _this.context.draggable.dragAndDropAction.event || draggedEvent;
        var _this$props = _this.props, accessors2 = _this$props.accessors, slotMetrics = _this$props.slotMetrics, rtl = _this$props.rtl, localizer2 = _this$props.localizer;
        var slot = (0, _selection.getSlotAtX)(bounds, point.x, rtl, slotMetrics.slots);
        var date2 = slotMetrics.getDateForSlot(slot);
        var _eventTimes = (0, _common.eventTimes)(event, accessors2, localizer2), start2 = _eventTimes.start, duration2 = _eventTimes.duration;
        start2 = localizer2.merge(date2, start2);
        var end2 = localizer2.add(start2, duration2, "milliseconds");
        _this.update(event, start2, end2);
      };
      _this.handleDropFromOutside = function(point, bounds) {
        if (!_this.context.draggable.onDropFromOutside) return;
        var _this$props2 = _this.props, slotMetrics = _this$props2.slotMetrics, rtl = _this$props2.rtl, localizer2 = _this$props2.localizer;
        var slot = (0, _selection.getSlotAtX)(bounds, point.x, rtl, slotMetrics.slots);
        var start2 = slotMetrics.getDateForSlot(slot);
        _this.context.draggable.onDropFromOutside({
          start: start2,
          end: localizer2.add(start2, 1, "day"),
          allDay: false
        });
      };
      _this.handleDragOverFromOutside = function(point, node) {
        var item = _this.context.draggable.dragFromOutsideItem ? _this.context.draggable.dragFromOutsideItem() : null;
        if (!item) return;
        _this.handleMove(point, node, item);
      };
      _this._selectable = function() {
        var node = _this.ref.current.closest(".rbc-month-row, .rbc-allday-cell");
        var container = node.closest(".rbc-month-view, .rbc-time-view");
        var isMonthRow = node.classList.contains("rbc-month-row");
        var selector = _this._selector = new _Selection.default(function() {
          return container;
        }, {
          validContainers: (0, _toConsumableArray2.default)(!isMonthRow ? [".rbc-day-slot", ".rbc-allday-cell"] : [])
        });
        selector.on("beforeSelect", function(point) {
          var isAllDay = _this.props.isAllDay;
          var action = _this.context.draggable.dragAndDropAction.action;
          var bounds = (0, _Selection.getBoundsForNode)(node);
          var isInBox = (0, _selection.pointInBox)(bounds, point);
          return action === "move" || action === "resize" && (!isAllDay || isInBox);
        });
        selector.on("selecting", function(box) {
          var bounds = (0, _Selection.getBoundsForNode)(node);
          var dragAndDropAction = _this.context.draggable.dragAndDropAction;
          if (dragAndDropAction.action === "move") _this.handleMove(box, bounds);
          if (dragAndDropAction.action === "resize") _this.handleResize(box, bounds);
        });
        selector.on("selectStart", function() {
          return _this.context.draggable.onStart();
        });
        selector.on("select", function(point) {
          var bounds = (0, _Selection.getBoundsForNode)(node);
          if (!_this.state.segment) return;
          if (!(0, _selection.pointInBox)(bounds, point)) {
            _this.reset();
          } else {
            _this.handleInteractionEnd();
          }
        });
        selector.on("dropFromOutside", function(point) {
          if (!_this.context.draggable.onDropFromOutside) return;
          var bounds = (0, _Selection.getBoundsForNode)(node);
          if (!(0, _selection.pointInBox)(bounds, point)) return;
          _this.handleDropFromOutside(point, bounds);
        });
        selector.on("dragOverFromOutside", function(point) {
          if (!_this.context.draggable.dragFromOutsideItem) return;
          var bounds = (0, _Selection.getBoundsForNode)(node);
          _this.handleDragOverFromOutside(point, bounds);
        });
        selector.on("click", function() {
          return _this.context.draggable.onEnd(null);
        });
        selector.on("reset", function() {
          _this.reset();
          _this.context.draggable.onEnd(null);
        });
      };
      _this.handleInteractionEnd = function() {
        var _this$props3 = _this.props, resourceId = _this$props3.resourceId, isAllDay = _this$props3.isAllDay;
        var event = _this.state.segment.event;
        _this.reset();
        _this.context.draggable.onEnd({
          start: event.start,
          end: event.end,
          resourceId,
          isAllDay
        });
      };
      _this._teardownSelectable = function() {
        if (!_this._selector) return;
        _this._selector.teardown();
        _this._selector = null;
      };
      _this.state = {};
      _this.ref = /* @__PURE__ */ _react.default.createRef();
      return _this;
    }
    (0, _inherits2.default)(WeekWrapper2, _React$Component);
    return (0, _createClass2.default)(WeekWrapper2, [{
      key: "componentDidMount",
      value: function componentDidMount() {
        this._selectable();
      }
    }, {
      key: "componentWillUnmount",
      value: function componentWillUnmount() {
        this._teardownSelectable();
      }
    }, {
      key: "reset",
      value: function reset2() {
        if (this.state.segment) this.setState({
          segment: null
        });
      }
    }, {
      key: "update",
      value: function update(event, start2, end2) {
        var segment = (0, _eventLevels.eventSegments)((0, _objectSpread22.default)((0, _objectSpread22.default)({}, event), {}, {
          end: end2,
          start: start2,
          __isPreview: true
        }), this.props.slotMetrics.range, _common.dragAccessors, this.props.localizer);
        var lastSegment = this.state.segment;
        if (lastSegment && segment.span === lastSegment.span && segment.left === lastSegment.left && segment.right === lastSegment.right) {
          return;
        }
        this.setState({
          segment
        });
      }
    }, {
      key: "handleResize",
      value: function handleResize(point, bounds) {
        var _this$context$draggab = this.context.draggable.dragAndDropAction, event = _this$context$draggab.event, direction = _this$context$draggab.direction;
        var _this$props4 = this.props, accessors2 = _this$props4.accessors, slotMetrics = _this$props4.slotMetrics, rtl = _this$props4.rtl, localizer2 = _this$props4.localizer;
        var _eventTimes2 = (0, _common.eventTimes)(event, accessors2, localizer2), start2 = _eventTimes2.start, end2 = _eventTimes2.end;
        var slot = (0, _selection.getSlotAtX)(bounds, point.x, rtl, slotMetrics.slots);
        var date2 = slotMetrics.getDateForSlot(slot);
        var cursorInRow = (0, _selection.pointInBox)(bounds, point);
        if (direction === "RIGHT") {
          if (cursorInRow) {
            if (slotMetrics.last < start2) return this.reset();
            if (localizer2.eq(localizer2.startOf(end2, "day"), end2)) end2 = localizer2.add(date2, 1, "day");
            else end2 = date2;
          } else if (localizer2.inRange(start2, slotMetrics.first, slotMetrics.last) || bounds.bottom < point.y && +slotMetrics.first > +start2) {
            end2 = localizer2.add(slotMetrics.last, 1, "milliseconds");
          } else {
            this.setState({
              segment: null
            });
            return;
          }
          var originalEnd = accessors2.end(event);
          end2 = localizer2.merge(end2, originalEnd);
          if (localizer2.lt(end2, start2)) {
            end2 = originalEnd;
          }
        } else if (direction === "LEFT") {
          if (cursorInRow) {
            if (slotMetrics.first > end2) return this.reset();
            start2 = date2;
          } else if (localizer2.inRange(end2, slotMetrics.first, slotMetrics.last) || bounds.top > point.y && localizer2.lt(slotMetrics.last, end2)) {
            start2 = localizer2.add(slotMetrics.first, -1, "milliseconds");
          } else {
            this.reset();
            return;
          }
          var originalStart = accessors2.start(event);
          start2 = localizer2.merge(start2, originalStart);
          if (localizer2.gt(start2, end2)) {
            start2 = originalStart;
          }
        }
        this.update(event, start2, end2);
      }
    }, {
      key: "render",
      value: function render() {
        var _this$props5 = this.props, children = _this$props5.children, accessors2 = _this$props5.accessors;
        var segment = this.state.segment;
        return /* @__PURE__ */ _react.default.createElement("div", {
          ref: this.ref,
          className: "rbc-addons-dnd-row-body"
        }, children, segment && /* @__PURE__ */ _react.default.createElement(_EventRow.default, Object.assign({}, this.props, {
          selected: null,
          className: "rbc-addons-dnd-drag-row",
          segments: [segment],
          accessors: (0, _objectSpread22.default)((0, _objectSpread22.default)({}, accessors2), _common.dragAccessors)
        })));
      }
    }]);
  })(_react.default.Component);
  WeekWrapper$1.contextType = _DnDContext.DnDContext;
  WeekWrapper.default = WeekWrapper$1;
  return WeekWrapper;
}
var hasRequiredWithDragAndDrop;
function requireWithDragAndDrop() {
  if (hasRequiredWithDragAndDrop) return withDragAndDrop;
  hasRequiredWithDragAndDrop = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(withDragAndDrop, "__esModule", {
    value: true
  });
  withDragAndDrop.default = withDragAndDrop$1;
  var _objectSpread22 = _interopRequireDefault(requireObjectSpread2());
  var _objectWithoutProperties2 = _interopRequireDefault(requireObjectWithoutProperties());
  var _classCallCheck2 = _interopRequireDefault(requireClassCallCheck());
  var _createClass2 = _interopRequireDefault(requireCreateClass());
  var _callSuper2 = _interopRequireDefault(requireCallSuper());
  var _inherits2 = _interopRequireDefault(requireInherits());
  var _react = _interopRequireDefault(requireReact());
  var _clsx = _interopRequireDefault(clsx_m);
  requirePropTypes();
  var _EventWrapper = _interopRequireDefault(requireEventWrapper());
  var _EventContainerWrapper = _interopRequireDefault(requireEventContainerWrapper());
  var _WeekWrapper = _interopRequireDefault(requireWeekWrapper());
  var _common = requireCommon();
  var _DnDContext = requireDnDContext();
  var _excluded3 = ["selectable", "elementProps", "components"];
  function withDragAndDrop$1(Calendar2) {
    var DragAndDropCalendar = /* @__PURE__ */ (function(_React$Component) {
      function DragAndDropCalendar2() {
        var _this;
        (0, _classCallCheck2.default)(this, DragAndDropCalendar2);
        for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
          args[_key] = arguments[_key];
        }
        _this = (0, _callSuper2.default)(this, DragAndDropCalendar2, [].concat(args));
        _this.defaultOnDragOver = function(event) {
          event.preventDefault();
        };
        _this.handleBeginAction = function(event, action, direction) {
          _this.setState({
            event,
            action,
            direction
          });
          var onDragStart = _this.props.onDragStart;
          if (onDragStart) onDragStart({
            event,
            action,
            direction
          });
        };
        _this.handleInteractionStart = function() {
          if (_this.state.interacting === false) _this.setState({
            interacting: true
          });
        };
        _this.handleInteractionEnd = function(interactionInfo) {
          var _this$state = _this.state, action = _this$state.action, event = _this$state.event;
          if (!action) return;
          _this.setState({
            action: null,
            event: null,
            interacting: false,
            direction: null
          });
          if (interactionInfo == null) return;
          interactionInfo.event = event;
          var _this$props = _this.props, onEventDrop = _this$props.onEventDrop, onEventResize = _this$props.onEventResize;
          if (action === "move" && onEventDrop) onEventDrop(interactionInfo);
          if (action === "resize" && onEventResize) onEventResize(interactionInfo);
        };
        _this.state = {
          interacting: false
        };
        return _this;
      }
      (0, _inherits2.default)(DragAndDropCalendar2, _React$Component);
      return (0, _createClass2.default)(DragAndDropCalendar2, [{
        key: "getDnDContextValue",
        value: function getDnDContextValue() {
          return {
            draggable: {
              onStart: this.handleInteractionStart,
              onEnd: this.handleInteractionEnd,
              onBeginAction: this.handleBeginAction,
              onDropFromOutside: this.props.onDropFromOutside,
              dragFromOutsideItem: this.props.dragFromOutsideItem,
              draggableAccessor: this.props.draggableAccessor,
              resizableAccessor: this.props.resizableAccessor,
              dragAndDropAction: this.state
            }
          };
        }
      }, {
        key: "render",
        value: function render() {
          var _this$props2 = this.props, selectable = _this$props2.selectable, elementProps = _this$props2.elementProps, components = _this$props2.components, props = (0, _objectWithoutProperties2.default)(_this$props2, _excluded3);
          var interacting = this.state.interacting;
          delete props.onEventDrop;
          delete props.onEventResize;
          props.selectable = selectable ? "ignoreEvents" : false;
          this.components = (0, _common.mergeComponents)(components, {
            eventWrapper: _EventWrapper.default,
            eventContainerWrapper: _EventContainerWrapper.default,
            weekWrapper: _WeekWrapper.default
          });
          var elementPropsWithDropFromOutside = this.props.onDropFromOutside ? (0, _objectSpread22.default)((0, _objectSpread22.default)({}, elementProps), {}, {
            onDragOver: this.props.onDragOver || this.defaultOnDragOver
          }) : elementProps;
          props.className = (0, _clsx.default)(props.className, "rbc-addons-dnd", !!interacting && "rbc-addons-dnd-is-dragging");
          var context2 = this.getDnDContextValue();
          return /* @__PURE__ */ _react.default.createElement(_DnDContext.DnDContext.Provider, {
            value: context2
          }, /* @__PURE__ */ _react.default.createElement(Calendar2, Object.assign({}, props, {
            elementProps: elementPropsWithDropFromOutside,
            components: this.components
          })));
        }
      }]);
    })(_react.default.Component);
    DragAndDropCalendar.defaultProps = (0, _objectSpread22.default)((0, _objectSpread22.default)({}, Calendar2.defaultProps), {}, {
      draggableAccessor: null,
      resizableAccessor: null,
      resizable: true
    });
    return DragAndDropCalendar;
  }
  return withDragAndDrop;
}
var hasRequiredDragAndDrop;
function requireDragAndDrop() {
  if (hasRequiredDragAndDrop) return dragAndDrop;
  hasRequiredDragAndDrop = 1;
  var _interopRequireDefault = requireInteropRequireDefault().default;
  Object.defineProperty(dragAndDrop, "__esModule", {
    value: true
  });
  dragAndDrop.default = void 0;
  var _withDragAndDrop = _interopRequireDefault(requireWithDragAndDrop());
  dragAndDrop.default = _withDragAndDrop.default;
  return dragAndDrop;
}
var dragAndDropExports = requireDragAndDrop();
const dnd = /* @__PURE__ */ _mergeNamespaces({
  __proto__: null,
  default: dragAndDropExports
}, [dragAndDropExports]);
function toInteger(dirtyNumber) {
  if (dirtyNumber === null || dirtyNumber === true || dirtyNumber === false) {
    return NaN;
  }
  var number = Number(dirtyNumber);
  if (isNaN(number)) {
    return number;
  }
  return number < 0 ? Math.ceil(number) : Math.floor(number);
}
function requiredArgs(required, args) {
  if (args.length < required) {
    throw new TypeError(required + " argument" + (required > 1 ? "s" : "") + " required, but only " + args.length + " present");
  }
}
function toDate(argument) {
  requiredArgs(1, arguments);
  var argStr = Object.prototype.toString.call(argument);
  if (argument instanceof Date || _typeof$1(argument) === "object" && argStr === "[object Date]") {
    return new Date(argument.getTime());
  } else if (typeof argument === "number" || argStr === "[object Number]") {
    return new Date(argument);
  } else {
    if ((typeof argument === "string" || argStr === "[object String]") && typeof console !== "undefined") {
      console.warn("Starting with v2.0.0-beta.1 date-fns doesn't accept strings as date arguments. Please use `parseISO` to parse strings. See: https://github.com/date-fns/date-fns/blob/master/docs/upgradeGuide.md#string-arguments");
      console.warn(new Error().stack);
    }
    return /* @__PURE__ */ new Date(NaN);
  }
}
function addMilliseconds(dirtyDate, dirtyAmount) {
  requiredArgs(2, arguments);
  var timestamp = toDate(dirtyDate).getTime();
  var amount = toInteger(dirtyAmount);
  return new Date(timestamp + amount);
}
var defaultOptions = {};
function getDefaultOptions() {
  return defaultOptions;
}
function startOfWeek(dirtyDate, options) {
  var _ref, _ref2, _ref3, _options$weekStartsOn, _options$locale, _options$locale$optio, _defaultOptions$local, _defaultOptions$local2;
  requiredArgs(1, arguments);
  var defaultOptions2 = getDefaultOptions();
  var weekStartsOn = toInteger((_ref = (_ref2 = (_ref3 = (_options$weekStartsOn = options === null || options === void 0 ? void 0 : options.weekStartsOn) !== null && _options$weekStartsOn !== void 0 ? _options$weekStartsOn : options === null || options === void 0 ? void 0 : (_options$locale = options.locale) === null || _options$locale === void 0 ? void 0 : (_options$locale$optio = _options$locale.options) === null || _options$locale$optio === void 0 ? void 0 : _options$locale$optio.weekStartsOn) !== null && _ref3 !== void 0 ? _ref3 : defaultOptions2.weekStartsOn) !== null && _ref2 !== void 0 ? _ref2 : (_defaultOptions$local = defaultOptions2.locale) === null || _defaultOptions$local === void 0 ? void 0 : (_defaultOptions$local2 = _defaultOptions$local.options) === null || _defaultOptions$local2 === void 0 ? void 0 : _defaultOptions$local2.weekStartsOn) !== null && _ref !== void 0 ? _ref : 0);
  if (!(weekStartsOn >= 0 && weekStartsOn <= 6)) {
    throw new RangeError("weekStartsOn must be between 0 and 6 inclusively");
  }
  var date2 = toDate(dirtyDate);
  var day2 = date2.getDay();
  var diff2 = (day2 < weekStartsOn ? 7 : 0) + day2 - weekStartsOn;
  date2.setDate(date2.getDate() - diff2);
  date2.setHours(0, 0, 0, 0);
  return date2;
}
function getTimezoneOffsetInMilliseconds(date2) {
  var utcDate = new Date(Date.UTC(date2.getFullYear(), date2.getMonth(), date2.getDate(), date2.getHours(), date2.getMinutes(), date2.getSeconds(), date2.getMilliseconds()));
  utcDate.setUTCFullYear(date2.getFullYear());
  return date2.getTime() - utcDate.getTime();
}
function isDate(value) {
  requiredArgs(1, arguments);
  return value instanceof Date || _typeof$1(value) === "object" && Object.prototype.toString.call(value) === "[object Date]";
}
function isValid(dirtyDate) {
  requiredArgs(1, arguments);
  if (!isDate(dirtyDate) && typeof dirtyDate !== "number") {
    return false;
  }
  var date2 = toDate(dirtyDate);
  return !isNaN(Number(date2));
}
function subMilliseconds(dirtyDate, dirtyAmount) {
  requiredArgs(2, arguments);
  var amount = toInteger(dirtyAmount);
  return addMilliseconds(dirtyDate, -amount);
}
var MILLISECONDS_IN_DAY = 864e5;
function getUTCDayOfYear(dirtyDate) {
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var timestamp = date2.getTime();
  date2.setUTCMonth(0, 1);
  date2.setUTCHours(0, 0, 0, 0);
  var startOfYearTimestamp = date2.getTime();
  var difference = timestamp - startOfYearTimestamp;
  return Math.floor(difference / MILLISECONDS_IN_DAY) + 1;
}
function startOfUTCISOWeek(dirtyDate) {
  requiredArgs(1, arguments);
  var weekStartsOn = 1;
  var date2 = toDate(dirtyDate);
  var day2 = date2.getUTCDay();
  var diff2 = (day2 < weekStartsOn ? 7 : 0) + day2 - weekStartsOn;
  date2.setUTCDate(date2.getUTCDate() - diff2);
  date2.setUTCHours(0, 0, 0, 0);
  return date2;
}
function getUTCISOWeekYear(dirtyDate) {
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var year2 = date2.getUTCFullYear();
  var fourthOfJanuaryOfNextYear = /* @__PURE__ */ new Date(0);
  fourthOfJanuaryOfNextYear.setUTCFullYear(year2 + 1, 0, 4);
  fourthOfJanuaryOfNextYear.setUTCHours(0, 0, 0, 0);
  var startOfNextYear = startOfUTCISOWeek(fourthOfJanuaryOfNextYear);
  var fourthOfJanuaryOfThisYear = /* @__PURE__ */ new Date(0);
  fourthOfJanuaryOfThisYear.setUTCFullYear(year2, 0, 4);
  fourthOfJanuaryOfThisYear.setUTCHours(0, 0, 0, 0);
  var startOfThisYear = startOfUTCISOWeek(fourthOfJanuaryOfThisYear);
  if (date2.getTime() >= startOfNextYear.getTime()) {
    return year2 + 1;
  } else if (date2.getTime() >= startOfThisYear.getTime()) {
    return year2;
  } else {
    return year2 - 1;
  }
}
function startOfUTCISOWeekYear(dirtyDate) {
  requiredArgs(1, arguments);
  var year2 = getUTCISOWeekYear(dirtyDate);
  var fourthOfJanuary = /* @__PURE__ */ new Date(0);
  fourthOfJanuary.setUTCFullYear(year2, 0, 4);
  fourthOfJanuary.setUTCHours(0, 0, 0, 0);
  var date2 = startOfUTCISOWeek(fourthOfJanuary);
  return date2;
}
var MILLISECONDS_IN_WEEK$1 = 6048e5;
function getUTCISOWeek(dirtyDate) {
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var diff2 = startOfUTCISOWeek(date2).getTime() - startOfUTCISOWeekYear(date2).getTime();
  return Math.round(diff2 / MILLISECONDS_IN_WEEK$1) + 1;
}
function startOfUTCWeek(dirtyDate, options) {
  var _ref, _ref2, _ref3, _options$weekStartsOn, _options$locale, _options$locale$optio, _defaultOptions$local, _defaultOptions$local2;
  requiredArgs(1, arguments);
  var defaultOptions2 = getDefaultOptions();
  var weekStartsOn = toInteger((_ref = (_ref2 = (_ref3 = (_options$weekStartsOn = options === null || options === void 0 ? void 0 : options.weekStartsOn) !== null && _options$weekStartsOn !== void 0 ? _options$weekStartsOn : options === null || options === void 0 ? void 0 : (_options$locale = options.locale) === null || _options$locale === void 0 ? void 0 : (_options$locale$optio = _options$locale.options) === null || _options$locale$optio === void 0 ? void 0 : _options$locale$optio.weekStartsOn) !== null && _ref3 !== void 0 ? _ref3 : defaultOptions2.weekStartsOn) !== null && _ref2 !== void 0 ? _ref2 : (_defaultOptions$local = defaultOptions2.locale) === null || _defaultOptions$local === void 0 ? void 0 : (_defaultOptions$local2 = _defaultOptions$local.options) === null || _defaultOptions$local2 === void 0 ? void 0 : _defaultOptions$local2.weekStartsOn) !== null && _ref !== void 0 ? _ref : 0);
  if (!(weekStartsOn >= 0 && weekStartsOn <= 6)) {
    throw new RangeError("weekStartsOn must be between 0 and 6 inclusively");
  }
  var date2 = toDate(dirtyDate);
  var day2 = date2.getUTCDay();
  var diff2 = (day2 < weekStartsOn ? 7 : 0) + day2 - weekStartsOn;
  date2.setUTCDate(date2.getUTCDate() - diff2);
  date2.setUTCHours(0, 0, 0, 0);
  return date2;
}
function getUTCWeekYear(dirtyDate, options) {
  var _ref, _ref2, _ref3, _options$firstWeekCon, _options$locale, _options$locale$optio, _defaultOptions$local, _defaultOptions$local2;
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var year2 = date2.getUTCFullYear();
  var defaultOptions2 = getDefaultOptions();
  var firstWeekContainsDate = toInteger((_ref = (_ref2 = (_ref3 = (_options$firstWeekCon = options === null || options === void 0 ? void 0 : options.firstWeekContainsDate) !== null && _options$firstWeekCon !== void 0 ? _options$firstWeekCon : options === null || options === void 0 ? void 0 : (_options$locale = options.locale) === null || _options$locale === void 0 ? void 0 : (_options$locale$optio = _options$locale.options) === null || _options$locale$optio === void 0 ? void 0 : _options$locale$optio.firstWeekContainsDate) !== null && _ref3 !== void 0 ? _ref3 : defaultOptions2.firstWeekContainsDate) !== null && _ref2 !== void 0 ? _ref2 : (_defaultOptions$local = defaultOptions2.locale) === null || _defaultOptions$local === void 0 ? void 0 : (_defaultOptions$local2 = _defaultOptions$local.options) === null || _defaultOptions$local2 === void 0 ? void 0 : _defaultOptions$local2.firstWeekContainsDate) !== null && _ref !== void 0 ? _ref : 1);
  if (!(firstWeekContainsDate >= 1 && firstWeekContainsDate <= 7)) {
    throw new RangeError("firstWeekContainsDate must be between 1 and 7 inclusively");
  }
  var firstWeekOfNextYear = /* @__PURE__ */ new Date(0);
  firstWeekOfNextYear.setUTCFullYear(year2 + 1, 0, firstWeekContainsDate);
  firstWeekOfNextYear.setUTCHours(0, 0, 0, 0);
  var startOfNextYear = startOfUTCWeek(firstWeekOfNextYear, options);
  var firstWeekOfThisYear = /* @__PURE__ */ new Date(0);
  firstWeekOfThisYear.setUTCFullYear(year2, 0, firstWeekContainsDate);
  firstWeekOfThisYear.setUTCHours(0, 0, 0, 0);
  var startOfThisYear = startOfUTCWeek(firstWeekOfThisYear, options);
  if (date2.getTime() >= startOfNextYear.getTime()) {
    return year2 + 1;
  } else if (date2.getTime() >= startOfThisYear.getTime()) {
    return year2;
  } else {
    return year2 - 1;
  }
}
function startOfUTCWeekYear(dirtyDate, options) {
  var _ref, _ref2, _ref3, _options$firstWeekCon, _options$locale, _options$locale$optio, _defaultOptions$local, _defaultOptions$local2;
  requiredArgs(1, arguments);
  var defaultOptions2 = getDefaultOptions();
  var firstWeekContainsDate = toInteger((_ref = (_ref2 = (_ref3 = (_options$firstWeekCon = options === null || options === void 0 ? void 0 : options.firstWeekContainsDate) !== null && _options$firstWeekCon !== void 0 ? _options$firstWeekCon : options === null || options === void 0 ? void 0 : (_options$locale = options.locale) === null || _options$locale === void 0 ? void 0 : (_options$locale$optio = _options$locale.options) === null || _options$locale$optio === void 0 ? void 0 : _options$locale$optio.firstWeekContainsDate) !== null && _ref3 !== void 0 ? _ref3 : defaultOptions2.firstWeekContainsDate) !== null && _ref2 !== void 0 ? _ref2 : (_defaultOptions$local = defaultOptions2.locale) === null || _defaultOptions$local === void 0 ? void 0 : (_defaultOptions$local2 = _defaultOptions$local.options) === null || _defaultOptions$local2 === void 0 ? void 0 : _defaultOptions$local2.firstWeekContainsDate) !== null && _ref !== void 0 ? _ref : 1);
  var year2 = getUTCWeekYear(dirtyDate, options);
  var firstWeek = /* @__PURE__ */ new Date(0);
  firstWeek.setUTCFullYear(year2, 0, firstWeekContainsDate);
  firstWeek.setUTCHours(0, 0, 0, 0);
  var date2 = startOfUTCWeek(firstWeek, options);
  return date2;
}
var MILLISECONDS_IN_WEEK = 6048e5;
function getUTCWeek(dirtyDate, options) {
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var diff2 = startOfUTCWeek(date2, options).getTime() - startOfUTCWeekYear(date2, options).getTime();
  return Math.round(diff2 / MILLISECONDS_IN_WEEK) + 1;
}
function addLeadingZeros(number, targetLength) {
  var sign = number < 0 ? "-" : "";
  var output = Math.abs(number).toString();
  while (output.length < targetLength) {
    output = "0" + output;
  }
  return sign + output;
}
var formatters$1 = {
  // Year
  y: function y(date2, token) {
    var signedYear = date2.getUTCFullYear();
    var year2 = signedYear > 0 ? signedYear : 1 - signedYear;
    return addLeadingZeros(token === "yy" ? year2 % 100 : year2, token.length);
  },
  // Month
  M: function M(date2, token) {
    var month2 = date2.getUTCMonth();
    return token === "M" ? String(month2 + 1) : addLeadingZeros(month2 + 1, 2);
  },
  // Day of the month
  d: function d(date2, token) {
    return addLeadingZeros(date2.getUTCDate(), token.length);
  },
  // AM or PM
  a: function a(date2, token) {
    var dayPeriodEnumValue = date2.getUTCHours() / 12 >= 1 ? "pm" : "am";
    switch (token) {
      case "a":
      case "aa":
        return dayPeriodEnumValue.toUpperCase();
      case "aaa":
        return dayPeriodEnumValue;
      case "aaaaa":
        return dayPeriodEnumValue[0];
      case "aaaa":
      default:
        return dayPeriodEnumValue === "am" ? "a.m." : "p.m.";
    }
  },
  // Hour [1-12]
  h: function h(date2, token) {
    return addLeadingZeros(date2.getUTCHours() % 12 || 12, token.length);
  },
  // Hour [0-23]
  H: function H(date2, token) {
    return addLeadingZeros(date2.getUTCHours(), token.length);
  },
  // Minute
  m: function m(date2, token) {
    return addLeadingZeros(date2.getUTCMinutes(), token.length);
  },
  // Second
  s: function s(date2, token) {
    return addLeadingZeros(date2.getUTCSeconds(), token.length);
  },
  // Fraction of second
  S: function S(date2, token) {
    var numberOfDigits = token.length;
    var milliseconds2 = date2.getUTCMilliseconds();
    var fractionalSeconds = Math.floor(milliseconds2 * Math.pow(10, numberOfDigits - 3));
    return addLeadingZeros(fractionalSeconds, token.length);
  }
};
var dayPeriodEnum = {
  midnight: "midnight",
  noon: "noon",
  morning: "morning",
  afternoon: "afternoon",
  evening: "evening",
  night: "night"
};
var formatters = {
  // Era
  G: function G(date2, token, localize2) {
    var era = date2.getUTCFullYear() > 0 ? 1 : 0;
    switch (token) {
      // AD, BC
      case "G":
      case "GG":
      case "GGG":
        return localize2.era(era, {
          width: "abbreviated"
        });
      // A, B
      case "GGGGG":
        return localize2.era(era, {
          width: "narrow"
        });
      // Anno Domini, Before Christ
      case "GGGG":
      default:
        return localize2.era(era, {
          width: "wide"
        });
    }
  },
  // Year
  y: function y2(date2, token, localize2) {
    if (token === "yo") {
      var signedYear = date2.getUTCFullYear();
      var year2 = signedYear > 0 ? signedYear : 1 - signedYear;
      return localize2.ordinalNumber(year2, {
        unit: "year"
      });
    }
    return formatters$1.y(date2, token);
  },
  // Local week-numbering year
  Y: function Y(date2, token, localize2, options) {
    var signedWeekYear = getUTCWeekYear(date2, options);
    var weekYear = signedWeekYear > 0 ? signedWeekYear : 1 - signedWeekYear;
    if (token === "YY") {
      var twoDigitYear = weekYear % 100;
      return addLeadingZeros(twoDigitYear, 2);
    }
    if (token === "Yo") {
      return localize2.ordinalNumber(weekYear, {
        unit: "year"
      });
    }
    return addLeadingZeros(weekYear, token.length);
  },
  // ISO week-numbering year
  R: function R(date2, token) {
    var isoWeekYear = getUTCISOWeekYear(date2);
    return addLeadingZeros(isoWeekYear, token.length);
  },
  // Extended year. This is a single number designating the year of this calendar system.
  // The main difference between `y` and `u` localizers are B.C. years:
  // | Year | `y` | `u` |
  // |------|-----|-----|
  // | AC 1 |   1 |   1 |
  // | BC 1 |   1 |   0 |
  // | BC 2 |   2 |  -1 |
  // Also `yy` always returns the last two digits of a year,
  // while `uu` pads single digit years to 2 characters and returns other years unchanged.
  u: function u(date2, token) {
    var year2 = date2.getUTCFullYear();
    return addLeadingZeros(year2, token.length);
  },
  // Quarter
  Q: function Q(date2, token, localize2) {
    var quarter = Math.ceil((date2.getUTCMonth() + 1) / 3);
    switch (token) {
      // 1, 2, 3, 4
      case "Q":
        return String(quarter);
      // 01, 02, 03, 04
      case "QQ":
        return addLeadingZeros(quarter, 2);
      // 1st, 2nd, 3rd, 4th
      case "Qo":
        return localize2.ordinalNumber(quarter, {
          unit: "quarter"
        });
      // Q1, Q2, Q3, Q4
      case "QQQ":
        return localize2.quarter(quarter, {
          width: "abbreviated",
          context: "formatting"
        });
      // 1, 2, 3, 4 (narrow quarter; could be not numerical)
      case "QQQQQ":
        return localize2.quarter(quarter, {
          width: "narrow",
          context: "formatting"
        });
      // 1st quarter, 2nd quarter, ...
      case "QQQQ":
      default:
        return localize2.quarter(quarter, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // Stand-alone quarter
  q: function q(date2, token, localize2) {
    var quarter = Math.ceil((date2.getUTCMonth() + 1) / 3);
    switch (token) {
      // 1, 2, 3, 4
      case "q":
        return String(quarter);
      // 01, 02, 03, 04
      case "qq":
        return addLeadingZeros(quarter, 2);
      // 1st, 2nd, 3rd, 4th
      case "qo":
        return localize2.ordinalNumber(quarter, {
          unit: "quarter"
        });
      // Q1, Q2, Q3, Q4
      case "qqq":
        return localize2.quarter(quarter, {
          width: "abbreviated",
          context: "standalone"
        });
      // 1, 2, 3, 4 (narrow quarter; could be not numerical)
      case "qqqqq":
        return localize2.quarter(quarter, {
          width: "narrow",
          context: "standalone"
        });
      // 1st quarter, 2nd quarter, ...
      case "qqqq":
      default:
        return localize2.quarter(quarter, {
          width: "wide",
          context: "standalone"
        });
    }
  },
  // Month
  M: function M2(date2, token, localize2) {
    var month2 = date2.getUTCMonth();
    switch (token) {
      case "M":
      case "MM":
        return formatters$1.M(date2, token);
      // 1st, 2nd, ..., 12th
      case "Mo":
        return localize2.ordinalNumber(month2 + 1, {
          unit: "month"
        });
      // Jan, Feb, ..., Dec
      case "MMM":
        return localize2.month(month2, {
          width: "abbreviated",
          context: "formatting"
        });
      // J, F, ..., D
      case "MMMMM":
        return localize2.month(month2, {
          width: "narrow",
          context: "formatting"
        });
      // January, February, ..., December
      case "MMMM":
      default:
        return localize2.month(month2, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // Stand-alone month
  L: function L(date2, token, localize2) {
    var month2 = date2.getUTCMonth();
    switch (token) {
      // 1, 2, ..., 12
      case "L":
        return String(month2 + 1);
      // 01, 02, ..., 12
      case "LL":
        return addLeadingZeros(month2 + 1, 2);
      // 1st, 2nd, ..., 12th
      case "Lo":
        return localize2.ordinalNumber(month2 + 1, {
          unit: "month"
        });
      // Jan, Feb, ..., Dec
      case "LLL":
        return localize2.month(month2, {
          width: "abbreviated",
          context: "standalone"
        });
      // J, F, ..., D
      case "LLLLL":
        return localize2.month(month2, {
          width: "narrow",
          context: "standalone"
        });
      // January, February, ..., December
      case "LLLL":
      default:
        return localize2.month(month2, {
          width: "wide",
          context: "standalone"
        });
    }
  },
  // Local week of year
  w: function w(date2, token, localize2, options) {
    var week = getUTCWeek(date2, options);
    if (token === "wo") {
      return localize2.ordinalNumber(week, {
        unit: "week"
      });
    }
    return addLeadingZeros(week, token.length);
  },
  // ISO week of year
  I: function I(date2, token, localize2) {
    var isoWeek = getUTCISOWeek(date2);
    if (token === "Io") {
      return localize2.ordinalNumber(isoWeek, {
        unit: "week"
      });
    }
    return addLeadingZeros(isoWeek, token.length);
  },
  // Day of the month
  d: function d2(date2, token, localize2) {
    if (token === "do") {
      return localize2.ordinalNumber(date2.getUTCDate(), {
        unit: "date"
      });
    }
    return formatters$1.d(date2, token);
  },
  // Day of year
  D: function D(date2, token, localize2) {
    var dayOfYear = getUTCDayOfYear(date2);
    if (token === "Do") {
      return localize2.ordinalNumber(dayOfYear, {
        unit: "dayOfYear"
      });
    }
    return addLeadingZeros(dayOfYear, token.length);
  },
  // Day of week
  E: function E(date2, token, localize2) {
    var dayOfWeek = date2.getUTCDay();
    switch (token) {
      // Tue
      case "E":
      case "EE":
      case "EEE":
        return localize2.day(dayOfWeek, {
          width: "abbreviated",
          context: "formatting"
        });
      // T
      case "EEEEE":
        return localize2.day(dayOfWeek, {
          width: "narrow",
          context: "formatting"
        });
      // Tu
      case "EEEEEE":
        return localize2.day(dayOfWeek, {
          width: "short",
          context: "formatting"
        });
      // Tuesday
      case "EEEE":
      default:
        return localize2.day(dayOfWeek, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // Local day of week
  e: function e(date2, token, localize2, options) {
    var dayOfWeek = date2.getUTCDay();
    var localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
    switch (token) {
      // Numerical value (Nth day of week with current locale or weekStartsOn)
      case "e":
        return String(localDayOfWeek);
      // Padded numerical value
      case "ee":
        return addLeadingZeros(localDayOfWeek, 2);
      // 1st, 2nd, ..., 7th
      case "eo":
        return localize2.ordinalNumber(localDayOfWeek, {
          unit: "day"
        });
      case "eee":
        return localize2.day(dayOfWeek, {
          width: "abbreviated",
          context: "formatting"
        });
      // T
      case "eeeee":
        return localize2.day(dayOfWeek, {
          width: "narrow",
          context: "formatting"
        });
      // Tu
      case "eeeeee":
        return localize2.day(dayOfWeek, {
          width: "short",
          context: "formatting"
        });
      // Tuesday
      case "eeee":
      default:
        return localize2.day(dayOfWeek, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // Stand-alone local day of week
  c: function c(date2, token, localize2, options) {
    var dayOfWeek = date2.getUTCDay();
    var localDayOfWeek = (dayOfWeek - options.weekStartsOn + 8) % 7 || 7;
    switch (token) {
      // Numerical value (same as in `e`)
      case "c":
        return String(localDayOfWeek);
      // Padded numerical value
      case "cc":
        return addLeadingZeros(localDayOfWeek, token.length);
      // 1st, 2nd, ..., 7th
      case "co":
        return localize2.ordinalNumber(localDayOfWeek, {
          unit: "day"
        });
      case "ccc":
        return localize2.day(dayOfWeek, {
          width: "abbreviated",
          context: "standalone"
        });
      // T
      case "ccccc":
        return localize2.day(dayOfWeek, {
          width: "narrow",
          context: "standalone"
        });
      // Tu
      case "cccccc":
        return localize2.day(dayOfWeek, {
          width: "short",
          context: "standalone"
        });
      // Tuesday
      case "cccc":
      default:
        return localize2.day(dayOfWeek, {
          width: "wide",
          context: "standalone"
        });
    }
  },
  // ISO day of week
  i: function i(date2, token, localize2) {
    var dayOfWeek = date2.getUTCDay();
    var isoDayOfWeek = dayOfWeek === 0 ? 7 : dayOfWeek;
    switch (token) {
      // 2
      case "i":
        return String(isoDayOfWeek);
      // 02
      case "ii":
        return addLeadingZeros(isoDayOfWeek, token.length);
      // 2nd
      case "io":
        return localize2.ordinalNumber(isoDayOfWeek, {
          unit: "day"
        });
      // Tue
      case "iii":
        return localize2.day(dayOfWeek, {
          width: "abbreviated",
          context: "formatting"
        });
      // T
      case "iiiii":
        return localize2.day(dayOfWeek, {
          width: "narrow",
          context: "formatting"
        });
      // Tu
      case "iiiiii":
        return localize2.day(dayOfWeek, {
          width: "short",
          context: "formatting"
        });
      // Tuesday
      case "iiii":
      default:
        return localize2.day(dayOfWeek, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // AM or PM
  a: function a2(date2, token, localize2) {
    var hours2 = date2.getUTCHours();
    var dayPeriodEnumValue = hours2 / 12 >= 1 ? "pm" : "am";
    switch (token) {
      case "a":
      case "aa":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "abbreviated",
          context: "formatting"
        });
      case "aaa":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "abbreviated",
          context: "formatting"
        }).toLowerCase();
      case "aaaaa":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "narrow",
          context: "formatting"
        });
      case "aaaa":
      default:
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // AM, PM, midnight, noon
  b: function b(date2, token, localize2) {
    var hours2 = date2.getUTCHours();
    var dayPeriodEnumValue;
    if (hours2 === 12) {
      dayPeriodEnumValue = dayPeriodEnum.noon;
    } else if (hours2 === 0) {
      dayPeriodEnumValue = dayPeriodEnum.midnight;
    } else {
      dayPeriodEnumValue = hours2 / 12 >= 1 ? "pm" : "am";
    }
    switch (token) {
      case "b":
      case "bb":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "abbreviated",
          context: "formatting"
        });
      case "bbb":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "abbreviated",
          context: "formatting"
        }).toLowerCase();
      case "bbbbb":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "narrow",
          context: "formatting"
        });
      case "bbbb":
      default:
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // in the morning, in the afternoon, in the evening, at night
  B: function B(date2, token, localize2) {
    var hours2 = date2.getUTCHours();
    var dayPeriodEnumValue;
    if (hours2 >= 17) {
      dayPeriodEnumValue = dayPeriodEnum.evening;
    } else if (hours2 >= 12) {
      dayPeriodEnumValue = dayPeriodEnum.afternoon;
    } else if (hours2 >= 4) {
      dayPeriodEnumValue = dayPeriodEnum.morning;
    } else {
      dayPeriodEnumValue = dayPeriodEnum.night;
    }
    switch (token) {
      case "B":
      case "BB":
      case "BBB":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "abbreviated",
          context: "formatting"
        });
      case "BBBBB":
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "narrow",
          context: "formatting"
        });
      case "BBBB":
      default:
        return localize2.dayPeriod(dayPeriodEnumValue, {
          width: "wide",
          context: "formatting"
        });
    }
  },
  // Hour [1-12]
  h: function h2(date2, token, localize2) {
    if (token === "ho") {
      var hours2 = date2.getUTCHours() % 12;
      if (hours2 === 0) hours2 = 12;
      return localize2.ordinalNumber(hours2, {
        unit: "hour"
      });
    }
    return formatters$1.h(date2, token);
  },
  // Hour [0-23]
  H: function H2(date2, token, localize2) {
    if (token === "Ho") {
      return localize2.ordinalNumber(date2.getUTCHours(), {
        unit: "hour"
      });
    }
    return formatters$1.H(date2, token);
  },
  // Hour [0-11]
  K: function K(date2, token, localize2) {
    var hours2 = date2.getUTCHours() % 12;
    if (token === "Ko") {
      return localize2.ordinalNumber(hours2, {
        unit: "hour"
      });
    }
    return addLeadingZeros(hours2, token.length);
  },
  // Hour [1-24]
  k: function k(date2, token, localize2) {
    var hours2 = date2.getUTCHours();
    if (hours2 === 0) hours2 = 24;
    if (token === "ko") {
      return localize2.ordinalNumber(hours2, {
        unit: "hour"
      });
    }
    return addLeadingZeros(hours2, token.length);
  },
  // Minute
  m: function m2(date2, token, localize2) {
    if (token === "mo") {
      return localize2.ordinalNumber(date2.getUTCMinutes(), {
        unit: "minute"
      });
    }
    return formatters$1.m(date2, token);
  },
  // Second
  s: function s2(date2, token, localize2) {
    if (token === "so") {
      return localize2.ordinalNumber(date2.getUTCSeconds(), {
        unit: "second"
      });
    }
    return formatters$1.s(date2, token);
  },
  // Fraction of second
  S: function S2(date2, token) {
    return formatters$1.S(date2, token);
  },
  // Timezone (ISO-8601. If offset is 0, output is always `'Z'`)
  X: function X(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timezoneOffset = originalDate.getTimezoneOffset();
    if (timezoneOffset === 0) {
      return "Z";
    }
    switch (token) {
      // Hours and optional minutes
      case "X":
        return formatTimezoneWithOptionalMinutes(timezoneOffset);
      // Hours, minutes and optional seconds without `:` delimiter
      // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
      // so this token always has the same output as `XX`
      case "XXXX":
      case "XX":
        return formatTimezone(timezoneOffset);
      // Hours, minutes and optional seconds with `:` delimiter
      // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
      // so this token always has the same output as `XXX`
      case "XXXXX":
      case "XXX":
      // Hours and minutes with `:` delimiter
      default:
        return formatTimezone(timezoneOffset, ":");
    }
  },
  // Timezone (ISO-8601. If offset is 0, output is `'+00:00'` or equivalent)
  x: function x(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timezoneOffset = originalDate.getTimezoneOffset();
    switch (token) {
      // Hours and optional minutes
      case "x":
        return formatTimezoneWithOptionalMinutes(timezoneOffset);
      // Hours, minutes and optional seconds without `:` delimiter
      // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
      // so this token always has the same output as `xx`
      case "xxxx":
      case "xx":
        return formatTimezone(timezoneOffset);
      // Hours, minutes and optional seconds with `:` delimiter
      // Note: neither ISO-8601 nor JavaScript supports seconds in timezone offsets
      // so this token always has the same output as `xxx`
      case "xxxxx":
      case "xxx":
      // Hours and minutes with `:` delimiter
      default:
        return formatTimezone(timezoneOffset, ":");
    }
  },
  // Timezone (GMT)
  O: function O(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timezoneOffset = originalDate.getTimezoneOffset();
    switch (token) {
      // Short
      case "O":
      case "OO":
      case "OOO":
        return "GMT" + formatTimezoneShort(timezoneOffset, ":");
      // Long
      case "OOOO":
      default:
        return "GMT" + formatTimezone(timezoneOffset, ":");
    }
  },
  // Timezone (specific non-location)
  z: function z(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timezoneOffset = originalDate.getTimezoneOffset();
    switch (token) {
      // Short
      case "z":
      case "zz":
      case "zzz":
        return "GMT" + formatTimezoneShort(timezoneOffset, ":");
      // Long
      case "zzzz":
      default:
        return "GMT" + formatTimezone(timezoneOffset, ":");
    }
  },
  // Seconds timestamp
  t: function t(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timestamp = Math.floor(originalDate.getTime() / 1e3);
    return addLeadingZeros(timestamp, token.length);
  },
  // Milliseconds timestamp
  T: function T(date2, token, _localize, options) {
    var originalDate = options._originalDate || date2;
    var timestamp = originalDate.getTime();
    return addLeadingZeros(timestamp, token.length);
  }
};
function formatTimezoneShort(offset2, dirtyDelimiter) {
  var sign = offset2 > 0 ? "-" : "+";
  var absOffset = Math.abs(offset2);
  var hours2 = Math.floor(absOffset / 60);
  var minutes2 = absOffset % 60;
  if (minutes2 === 0) {
    return sign + String(hours2);
  }
  var delimiter = dirtyDelimiter;
  return sign + String(hours2) + delimiter + addLeadingZeros(minutes2, 2);
}
function formatTimezoneWithOptionalMinutes(offset2, dirtyDelimiter) {
  if (offset2 % 60 === 0) {
    var sign = offset2 > 0 ? "-" : "+";
    return sign + addLeadingZeros(Math.abs(offset2) / 60, 2);
  }
  return formatTimezone(offset2, dirtyDelimiter);
}
function formatTimezone(offset2, dirtyDelimiter) {
  var delimiter = dirtyDelimiter || "";
  var sign = offset2 > 0 ? "-" : "+";
  var absOffset = Math.abs(offset2);
  var hours2 = addLeadingZeros(Math.floor(absOffset / 60), 2);
  var minutes2 = addLeadingZeros(absOffset % 60, 2);
  return sign + hours2 + delimiter + minutes2;
}
var dateLongFormatter = function dateLongFormatter2(pattern, formatLong2) {
  switch (pattern) {
    case "P":
      return formatLong2.date({
        width: "short"
      });
    case "PP":
      return formatLong2.date({
        width: "medium"
      });
    case "PPP":
      return formatLong2.date({
        width: "long"
      });
    case "PPPP":
    default:
      return formatLong2.date({
        width: "full"
      });
  }
};
var timeLongFormatter = function timeLongFormatter2(pattern, formatLong2) {
  switch (pattern) {
    case "p":
      return formatLong2.time({
        width: "short"
      });
    case "pp":
      return formatLong2.time({
        width: "medium"
      });
    case "ppp":
      return formatLong2.time({
        width: "long"
      });
    case "pppp":
    default:
      return formatLong2.time({
        width: "full"
      });
  }
};
var dateTimeLongFormatter = function dateTimeLongFormatter2(pattern, formatLong2) {
  var matchResult = pattern.match(/(P+)(p+)?/) || [];
  var datePattern = matchResult[1];
  var timePattern = matchResult[2];
  if (!timePattern) {
    return dateLongFormatter(pattern, formatLong2);
  }
  var dateTimeFormat;
  switch (datePattern) {
    case "P":
      dateTimeFormat = formatLong2.dateTime({
        width: "short"
      });
      break;
    case "PP":
      dateTimeFormat = formatLong2.dateTime({
        width: "medium"
      });
      break;
    case "PPP":
      dateTimeFormat = formatLong2.dateTime({
        width: "long"
      });
      break;
    case "PPPP":
    default:
      dateTimeFormat = formatLong2.dateTime({
        width: "full"
      });
      break;
  }
  return dateTimeFormat.replace("{{date}}", dateLongFormatter(datePattern, formatLong2)).replace("{{time}}", timeLongFormatter(timePattern, formatLong2));
};
var longFormatters = {
  p: timeLongFormatter,
  P: dateTimeLongFormatter
};
var protectedDayOfYearTokens = ["D", "DD"];
var protectedWeekYearTokens = ["YY", "YYYY"];
function isProtectedDayOfYearToken(token) {
  return protectedDayOfYearTokens.indexOf(token) !== -1;
}
function isProtectedWeekYearToken(token) {
  return protectedWeekYearTokens.indexOf(token) !== -1;
}
function throwProtectedError(token, format2, input) {
  if (token === "YYYY") {
    throw new RangeError("Use `yyyy` instead of `YYYY` (in `".concat(format2, "`) for formatting years to the input `").concat(input, "`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md"));
  } else if (token === "YY") {
    throw new RangeError("Use `yy` instead of `YY` (in `".concat(format2, "`) for formatting years to the input `").concat(input, "`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md"));
  } else if (token === "D") {
    throw new RangeError("Use `d` instead of `D` (in `".concat(format2, "`) for formatting days of the month to the input `").concat(input, "`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md"));
  } else if (token === "DD") {
    throw new RangeError("Use `dd` instead of `DD` (in `".concat(format2, "`) for formatting days of the month to the input `").concat(input, "`; see: https://github.com/date-fns/date-fns/blob/master/docs/unicodeTokens.md"));
  }
}
var formatDistanceLocale = {
  lessThanXSeconds: {
    one: "less than a second",
    other: "less than {{count}} seconds"
  },
  xSeconds: {
    one: "1 second",
    other: "{{count}} seconds"
  },
  halfAMinute: "half a minute",
  lessThanXMinutes: {
    one: "less than a minute",
    other: "less than {{count}} minutes"
  },
  xMinutes: {
    one: "1 minute",
    other: "{{count}} minutes"
  },
  aboutXHours: {
    one: "about 1 hour",
    other: "about {{count}} hours"
  },
  xHours: {
    one: "1 hour",
    other: "{{count}} hours"
  },
  xDays: {
    one: "1 day",
    other: "{{count}} days"
  },
  aboutXWeeks: {
    one: "about 1 week",
    other: "about {{count}} weeks"
  },
  xWeeks: {
    one: "1 week",
    other: "{{count}} weeks"
  },
  aboutXMonths: {
    one: "about 1 month",
    other: "about {{count}} months"
  },
  xMonths: {
    one: "1 month",
    other: "{{count}} months"
  },
  aboutXYears: {
    one: "about 1 year",
    other: "about {{count}} years"
  },
  xYears: {
    one: "1 year",
    other: "{{count}} years"
  },
  overXYears: {
    one: "over 1 year",
    other: "over {{count}} years"
  },
  almostXYears: {
    one: "almost 1 year",
    other: "almost {{count}} years"
  }
};
var formatDistance = function formatDistance2(token, count, options) {
  var result;
  var tokenValue = formatDistanceLocale[token];
  if (typeof tokenValue === "string") {
    result = tokenValue;
  } else if (count === 1) {
    result = tokenValue.one;
  } else {
    result = tokenValue.other.replace("{{count}}", count.toString());
  }
  if (options !== null && options !== void 0 && options.addSuffix) {
    if (options.comparison && options.comparison > 0) {
      return "in " + result;
    } else {
      return result + " ago";
    }
  }
  return result;
};
function buildFormatLongFn(args) {
  return function() {
    var options = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
    var width = options.width ? String(options.width) : args.defaultWidth;
    var format2 = args.formats[width] || args.formats[args.defaultWidth];
    return format2;
  };
}
var dateFormats = {
  full: "EEEE, MMMM do, y",
  long: "MMMM do, y",
  medium: "MMM d, y",
  short: "MM/dd/yyyy"
};
var timeFormats = {
  full: "h:mm:ss a zzzz",
  long: "h:mm:ss a z",
  medium: "h:mm:ss a",
  short: "h:mm a"
};
var dateTimeFormats = {
  full: "{{date}} 'at' {{time}}",
  long: "{{date}} 'at' {{time}}",
  medium: "{{date}}, {{time}}",
  short: "{{date}}, {{time}}"
};
var formatLong = {
  date: buildFormatLongFn({
    formats: dateFormats,
    defaultWidth: "full"
  }),
  time: buildFormatLongFn({
    formats: timeFormats,
    defaultWidth: "full"
  }),
  dateTime: buildFormatLongFn({
    formats: dateTimeFormats,
    defaultWidth: "full"
  })
};
var formatRelativeLocale = {
  lastWeek: "'last' eeee 'at' p",
  yesterday: "'yesterday at' p",
  today: "'today at' p",
  tomorrow: "'tomorrow at' p",
  nextWeek: "eeee 'at' p",
  other: "P"
};
var formatRelative = function formatRelative2(token, _date, _baseDate, _options) {
  return formatRelativeLocale[token];
};
function buildLocalizeFn(args) {
  return function(dirtyIndex, options) {
    var context2 = options !== null && options !== void 0 && options.context ? String(options.context) : "standalone";
    var valuesArray;
    if (context2 === "formatting" && args.formattingValues) {
      var defaultWidth = args.defaultFormattingWidth || args.defaultWidth;
      var width = options !== null && options !== void 0 && options.width ? String(options.width) : defaultWidth;
      valuesArray = args.formattingValues[width] || args.formattingValues[defaultWidth];
    } else {
      var _defaultWidth = args.defaultWidth;
      var _width = options !== null && options !== void 0 && options.width ? String(options.width) : args.defaultWidth;
      valuesArray = args.values[_width] || args.values[_defaultWidth];
    }
    var index2 = args.argumentCallback ? args.argumentCallback(dirtyIndex) : dirtyIndex;
    return valuesArray[index2];
  };
}
var eraValues = {
  narrow: ["B", "A"],
  abbreviated: ["BC", "AD"],
  wide: ["Before Christ", "Anno Domini"]
};
var quarterValues = {
  narrow: ["1", "2", "3", "4"],
  abbreviated: ["Q1", "Q2", "Q3", "Q4"],
  wide: ["1st quarter", "2nd quarter", "3rd quarter", "4th quarter"]
};
var monthValues = {
  narrow: ["J", "F", "M", "A", "M", "J", "J", "A", "S", "O", "N", "D"],
  abbreviated: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],
  wide: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"]
};
var dayValues = {
  narrow: ["S", "M", "T", "W", "T", "F", "S"],
  short: ["Su", "Mo", "Tu", "We", "Th", "Fr", "Sa"],
  abbreviated: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  wide: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"]
};
var dayPeriodValues = {
  narrow: {
    am: "a",
    pm: "p",
    midnight: "mi",
    noon: "n",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  },
  abbreviated: {
    am: "AM",
    pm: "PM",
    midnight: "midnight",
    noon: "noon",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  },
  wide: {
    am: "a.m.",
    pm: "p.m.",
    midnight: "midnight",
    noon: "noon",
    morning: "morning",
    afternoon: "afternoon",
    evening: "evening",
    night: "night"
  }
};
var formattingDayPeriodValues = {
  narrow: {
    am: "a",
    pm: "p",
    midnight: "mi",
    noon: "n",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  },
  abbreviated: {
    am: "AM",
    pm: "PM",
    midnight: "midnight",
    noon: "noon",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  },
  wide: {
    am: "a.m.",
    pm: "p.m.",
    midnight: "midnight",
    noon: "noon",
    morning: "in the morning",
    afternoon: "in the afternoon",
    evening: "in the evening",
    night: "at night"
  }
};
var ordinalNumber = function ordinalNumber2(dirtyNumber, _options) {
  var number = Number(dirtyNumber);
  var rem100 = number % 100;
  if (rem100 > 20 || rem100 < 10) {
    switch (rem100 % 10) {
      case 1:
        return number + "st";
      case 2:
        return number + "nd";
      case 3:
        return number + "rd";
    }
  }
  return number + "th";
};
var localize = {
  ordinalNumber,
  era: buildLocalizeFn({
    values: eraValues,
    defaultWidth: "wide"
  }),
  quarter: buildLocalizeFn({
    values: quarterValues,
    defaultWidth: "wide",
    argumentCallback: function argumentCallback(quarter) {
      return quarter - 1;
    }
  }),
  month: buildLocalizeFn({
    values: monthValues,
    defaultWidth: "wide"
  }),
  day: buildLocalizeFn({
    values: dayValues,
    defaultWidth: "wide"
  }),
  dayPeriod: buildLocalizeFn({
    values: dayPeriodValues,
    defaultWidth: "wide",
    formattingValues: formattingDayPeriodValues,
    defaultFormattingWidth: "wide"
  })
};
function buildMatchFn(args) {
  return function(string) {
    var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var width = options.width;
    var matchPattern = width && args.matchPatterns[width] || args.matchPatterns[args.defaultMatchWidth];
    var matchResult = string.match(matchPattern);
    if (!matchResult) {
      return null;
    }
    var matchedString = matchResult[0];
    var parsePatterns = width && args.parsePatterns[width] || args.parsePatterns[args.defaultParseWidth];
    var key = Array.isArray(parsePatterns) ? findIndex(parsePatterns, function(pattern) {
      return pattern.test(matchedString);
    }) : findKey(parsePatterns, function(pattern) {
      return pattern.test(matchedString);
    });
    var value;
    value = args.valueCallback ? args.valueCallback(key) : key;
    value = options.valueCallback ? options.valueCallback(value) : value;
    var rest = string.slice(matchedString.length);
    return {
      value,
      rest
    };
  };
}
function findKey(object, predicate) {
  for (var key in object) {
    if (object.hasOwnProperty(key) && predicate(object[key])) {
      return key;
    }
  }
  return void 0;
}
function findIndex(array, predicate) {
  for (var key = 0; key < array.length; key++) {
    if (predicate(array[key])) {
      return key;
    }
  }
  return void 0;
}
function buildMatchPatternFn(args) {
  return function(string) {
    var options = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
    var matchResult = string.match(args.matchPattern);
    if (!matchResult) return null;
    var matchedString = matchResult[0];
    var parseResult = string.match(args.parsePattern);
    if (!parseResult) return null;
    var value = args.valueCallback ? args.valueCallback(parseResult[0]) : parseResult[0];
    value = options.valueCallback ? options.valueCallback(value) : value;
    var rest = string.slice(matchedString.length);
    return {
      value,
      rest
    };
  };
}
var matchOrdinalNumberPattern = /^(\d+)(th|st|nd|rd)?/i;
var parseOrdinalNumberPattern = /\d+/i;
var matchEraPatterns = {
  narrow: /^(b|a)/i,
  abbreviated: /^(b\.?\s?c\.?|b\.?\s?c\.?\s?e\.?|a\.?\s?d\.?|c\.?\s?e\.?)/i,
  wide: /^(before christ|before common era|anno domini|common era)/i
};
var parseEraPatterns = {
  any: [/^b/i, /^(a|c)/i]
};
var matchQuarterPatterns = {
  narrow: /^[1234]/i,
  abbreviated: /^q[1234]/i,
  wide: /^[1234](th|st|nd|rd)? quarter/i
};
var parseQuarterPatterns = {
  any: [/1/i, /2/i, /3/i, /4/i]
};
var matchMonthPatterns = {
  narrow: /^[jfmasond]/i,
  abbreviated: /^(jan|feb|mar|apr|may|jun|jul|aug|sep|oct|nov|dec)/i,
  wide: /^(january|february|march|april|may|june|july|august|september|october|november|december)/i
};
var parseMonthPatterns = {
  narrow: [/^j/i, /^f/i, /^m/i, /^a/i, /^m/i, /^j/i, /^j/i, /^a/i, /^s/i, /^o/i, /^n/i, /^d/i],
  any: [/^ja/i, /^f/i, /^mar/i, /^ap/i, /^may/i, /^jun/i, /^jul/i, /^au/i, /^s/i, /^o/i, /^n/i, /^d/i]
};
var matchDayPatterns = {
  narrow: /^[smtwf]/i,
  short: /^(su|mo|tu|we|th|fr|sa)/i,
  abbreviated: /^(sun|mon|tue|wed|thu|fri|sat)/i,
  wide: /^(sunday|monday|tuesday|wednesday|thursday|friday|saturday)/i
};
var parseDayPatterns = {
  narrow: [/^s/i, /^m/i, /^t/i, /^w/i, /^t/i, /^f/i, /^s/i],
  any: [/^su/i, /^m/i, /^tu/i, /^w/i, /^th/i, /^f/i, /^sa/i]
};
var matchDayPeriodPatterns = {
  narrow: /^(a|p|mi|n|(in the|at) (morning|afternoon|evening|night))/i,
  any: /^([ap]\.?\s?m\.?|midnight|noon|(in the|at) (morning|afternoon|evening|night))/i
};
var parseDayPeriodPatterns = {
  any: {
    am: /^a/i,
    pm: /^p/i,
    midnight: /^mi/i,
    noon: /^no/i,
    morning: /morning/i,
    afternoon: /afternoon/i,
    evening: /evening/i,
    night: /night/i
  }
};
var match = {
  ordinalNumber: buildMatchPatternFn({
    matchPattern: matchOrdinalNumberPattern,
    parsePattern: parseOrdinalNumberPattern,
    valueCallback: function valueCallback(value) {
      return parseInt(value, 10);
    }
  }),
  era: buildMatchFn({
    matchPatterns: matchEraPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseEraPatterns,
    defaultParseWidth: "any"
  }),
  quarter: buildMatchFn({
    matchPatterns: matchQuarterPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseQuarterPatterns,
    defaultParseWidth: "any",
    valueCallback: function valueCallback2(index2) {
      return index2 + 1;
    }
  }),
  month: buildMatchFn({
    matchPatterns: matchMonthPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseMonthPatterns,
    defaultParseWidth: "any"
  }),
  day: buildMatchFn({
    matchPatterns: matchDayPatterns,
    defaultMatchWidth: "wide",
    parsePatterns: parseDayPatterns,
    defaultParseWidth: "any"
  }),
  dayPeriod: buildMatchFn({
    matchPatterns: matchDayPeriodPatterns,
    defaultMatchWidth: "any",
    parsePatterns: parseDayPeriodPatterns,
    defaultParseWidth: "any"
  })
};
var locale = {
  code: "en-US",
  formatDistance,
  formatLong,
  formatRelative,
  localize,
  match,
  options: {
    weekStartsOn: 0,
    firstWeekContainsDate: 1
  }
};
var formattingTokensRegExp = /[yYQqMLwIdDecihHKkms]o|(\w)\1*|''|'(''|[^'])+('|$)|./g;
var longFormattingTokensRegExp = /P+p+|P+|p+|''|'(''|[^'])+('|$)|./g;
var escapedStringRegExp = /^'([^]*?)'?$/;
var doubleQuoteRegExp = /''/g;
var unescapedLatinCharacterRegExp = /[a-zA-Z]/;
function format(dirtyDate, dirtyFormatStr, options) {
  var _ref, _options$locale, _ref2, _ref3, _ref4, _options$firstWeekCon, _options$locale2, _options$locale2$opti, _defaultOptions$local, _defaultOptions$local2, _ref5, _ref6, _ref7, _options$weekStartsOn, _options$locale3, _options$locale3$opti, _defaultOptions$local3, _defaultOptions$local4;
  requiredArgs(2, arguments);
  var formatStr = String(dirtyFormatStr);
  var defaultOptions2 = getDefaultOptions();
  var locale$1 = (_ref = (_options$locale = options === null || options === void 0 ? void 0 : options.locale) !== null && _options$locale !== void 0 ? _options$locale : defaultOptions2.locale) !== null && _ref !== void 0 ? _ref : locale;
  var firstWeekContainsDate = toInteger((_ref2 = (_ref3 = (_ref4 = (_options$firstWeekCon = options === null || options === void 0 ? void 0 : options.firstWeekContainsDate) !== null && _options$firstWeekCon !== void 0 ? _options$firstWeekCon : options === null || options === void 0 ? void 0 : (_options$locale2 = options.locale) === null || _options$locale2 === void 0 ? void 0 : (_options$locale2$opti = _options$locale2.options) === null || _options$locale2$opti === void 0 ? void 0 : _options$locale2$opti.firstWeekContainsDate) !== null && _ref4 !== void 0 ? _ref4 : defaultOptions2.firstWeekContainsDate) !== null && _ref3 !== void 0 ? _ref3 : (_defaultOptions$local = defaultOptions2.locale) === null || _defaultOptions$local === void 0 ? void 0 : (_defaultOptions$local2 = _defaultOptions$local.options) === null || _defaultOptions$local2 === void 0 ? void 0 : _defaultOptions$local2.firstWeekContainsDate) !== null && _ref2 !== void 0 ? _ref2 : 1);
  if (!(firstWeekContainsDate >= 1 && firstWeekContainsDate <= 7)) {
    throw new RangeError("firstWeekContainsDate must be between 1 and 7 inclusively");
  }
  var weekStartsOn = toInteger((_ref5 = (_ref6 = (_ref7 = (_options$weekStartsOn = options === null || options === void 0 ? void 0 : options.weekStartsOn) !== null && _options$weekStartsOn !== void 0 ? _options$weekStartsOn : options === null || options === void 0 ? void 0 : (_options$locale3 = options.locale) === null || _options$locale3 === void 0 ? void 0 : (_options$locale3$opti = _options$locale3.options) === null || _options$locale3$opti === void 0 ? void 0 : _options$locale3$opti.weekStartsOn) !== null && _ref7 !== void 0 ? _ref7 : defaultOptions2.weekStartsOn) !== null && _ref6 !== void 0 ? _ref6 : (_defaultOptions$local3 = defaultOptions2.locale) === null || _defaultOptions$local3 === void 0 ? void 0 : (_defaultOptions$local4 = _defaultOptions$local3.options) === null || _defaultOptions$local4 === void 0 ? void 0 : _defaultOptions$local4.weekStartsOn) !== null && _ref5 !== void 0 ? _ref5 : 0);
  if (!(weekStartsOn >= 0 && weekStartsOn <= 6)) {
    throw new RangeError("weekStartsOn must be between 0 and 6 inclusively");
  }
  if (!locale$1.localize) {
    throw new RangeError("locale must contain localize property");
  }
  if (!locale$1.formatLong) {
    throw new RangeError("locale must contain formatLong property");
  }
  var originalDate = toDate(dirtyDate);
  if (!isValid(originalDate)) {
    throw new RangeError("Invalid time value");
  }
  var timezoneOffset = getTimezoneOffsetInMilliseconds(originalDate);
  var utcDate = subMilliseconds(originalDate, timezoneOffset);
  var formatterOptions = {
    firstWeekContainsDate,
    weekStartsOn,
    locale: locale$1,
    _originalDate: originalDate
  };
  var result = formatStr.match(longFormattingTokensRegExp).map(function(substring) {
    var firstCharacter = substring[0];
    if (firstCharacter === "p" || firstCharacter === "P") {
      var longFormatter = longFormatters[firstCharacter];
      return longFormatter(substring, locale$1.formatLong);
    }
    return substring;
  }).join("").match(formattingTokensRegExp).map(function(substring) {
    if (substring === "''") {
      return "'";
    }
    var firstCharacter = substring[0];
    if (firstCharacter === "'") {
      return cleanEscapedString(substring);
    }
    var formatter = formatters[firstCharacter];
    if (formatter) {
      if (!(options !== null && options !== void 0 && options.useAdditionalWeekYearTokens) && isProtectedWeekYearToken(substring)) {
        throwProtectedError(substring, dirtyFormatStr, String(dirtyDate));
      }
      if (!(options !== null && options !== void 0 && options.useAdditionalDayOfYearTokens) && isProtectedDayOfYearToken(substring)) {
        throwProtectedError(substring, dirtyFormatStr, String(dirtyDate));
      }
      return formatter(utcDate, substring, locale$1.localize, formatterOptions);
    }
    if (firstCharacter.match(unescapedLatinCharacterRegExp)) {
      throw new RangeError("Format string contains an unescaped latin alphabet character `" + firstCharacter + "`");
    }
    return substring;
  }).join("");
  return result;
}
function cleanEscapedString(input) {
  var matched = input.match(escapedStringRegExp);
  if (!matched) {
    return input;
  }
  return matched[1].replace(doubleQuoteRegExp, "'");
}
function getDay(dirtyDate) {
  requiredArgs(1, arguments);
  var date2 = toDate(dirtyDate);
  var day2 = date2.getDay();
  return day2;
}
const locales = {};
const localizer = dateFnsLocalizer({
  format,
  startOfWeek: () => startOfWeek(/* @__PURE__ */ new Date(), { weekStartsOn: 1 }),
  getDay,
  locales
});
const withDragAndDropFn = typeof dnd === "function" && dnd || dnd && typeof dragAndDropExports === "function" && dragAndDropExports || dnd && typeof dragAndDropExports.withDragAndDrop === "function" && dragAndDropExports.withDragAndDrop || null;
const DnDCalendar = withDragAndDropFn ? withDragAndDropFn(Calendar$1) : Calendar$1;
function ScheduleApp() {
  const [employees, setEmployees] = reactExports.useState([]);
  const [events2, setEvents] = reactExports.useState([]);
  const [loading, setLoading] = reactExports.useState(true);
  const viewKey = "scheduleView";
  const [view, setView] = reactExports.useState(() => {
    try {
      const stored = localStorage.getItem(viewKey);
      if (stored && Object.values(views).includes(stored)) return stored;
    } catch {
    }
    return views.WEEK;
  });
  const changeView = (val) => {
    setView(val);
    try {
      localStorage.setItem(viewKey, val);
    } catch {
    }
  };
  const [saving, setSaving] = reactExports.useState(false);
  const [error, setError] = reactExports.useState(null);
  const [undoStack, setUndoStack] = reactExports.useState([]);
  const [copied, setCopied] = reactExports.useState(null);
  const [dirty, setDirty] = reactExports.useState(false);
  const [roleFilter, setRoleFilter] = reactExports.useState("All");
  const timeKey = "scheduleTimeRange";
  const defaultRange = { start: "08:00", end: "18:00" };
  const [timeRange, setTimeRange] = reactExports.useState(() => {
    try {
      return JSON.parse(localStorage.getItem(timeKey)) || defaultRange;
    } catch {
      return defaultRange;
    }
  });
  const parseTime = (val) => {
    const [h3 = 0, m3 = 0] = (val || "0:0").split(":");
    const d3 = /* @__PURE__ */ new Date();
    d3.setHours(parseInt(h3, 10) || 0, parseInt(m3, 10) || 0, 0, 0);
    return d3;
  };
  reactExports.useEffect(() => {
    const t2 = setTimeout(() => {
      window.dispatchEvent(new Event("resize"));
    }, 0);
    return () => clearTimeout(t2);
  }, []);
  const roles = reactExports.useMemo(() => {
    const set = new Set(employees.map((e2) => e2.role));
    return ["All", ...Array.from(set)];
  }, [employees]);
  const filteredEmployees = reactExports.useMemo(() => {
    return roleFilter === "All" ? employees : employees.filter((e2) => e2.role === roleFilter);
  }, [employees, roleFilter]);
  const displayEvents = reactExports.useMemo(() => {
    if (roleFilter === "All") return events2;
    const allowed = new Set(
      employees.filter((e2) => e2.role === roleFilter).map((e2) => e2.id)
    );
    return events2.filter((e2) => allowed.has(e2.employee_id));
  }, [events2, employees, roleFilter]);
  const externalEmp = reactExports.useRef(null);
  const getEventStyle = reactExports.useCallback(
    (event) => {
      const emp = employees.find((e2) => e2.id === event.employee_id);
      const color = emp?.color || "#0d6efd";
      return { style: { backgroundColor: color, borderColor: color } };
    },
    [employees]
  );
  const hasOverlap = (evts) => {
    const byEmp = {};
    for (const e2 of evts) {
      if (!byEmp[e2.employee_id]) byEmp[e2.employee_id] = [];
      byEmp[e2.employee_id].push(e2);
    }
    for (const list of Object.values(byEmp)) {
      list.sort((a3, b2) => a3.start - b2.start);
      for (let i2 = 1; i2 < list.length; i2++) {
        if (list[i2].start < list[i2 - 1].end) return true;
      }
    }
    return false;
  };
  const loadData = reactExports.useCallback(async () => {
    setLoading(true);
    try {
      const [empRes, schRes] = await Promise.all([
        fetch("/api/employees"),
        fetch("/api/schedule")
      ]);
      if (!empRes.ok || !schRes.ok) throw new Error("load");
      const empJson = await empRes.json();
      const schJson = await schRes.json();
      setEmployees(empJson.employees || []);
      const evts = (schJson.schedule || []).map((s3) => ({
        ...s3,
        start: new Date(s3.start_time),
        end: new Date(s3.end_time),
        title: empJson.employees.find((e2) => e2.id === s3.employee_id)?.name || "Shift"
      }));
      setEvents(evts);
      setDirty(false);
    } catch (err) {
      console.error(err);
      setError("Failed to load schedule");
    } finally {
      setLoading(false);
    }
  }, []);
  reactExports.useEffect(() => {
    loadData();
  }, [loadData]);
  reactExports.useEffect(() => {
    try {
      localStorage.setItem(timeKey, JSON.stringify(timeRange));
    } catch {
    }
  }, [timeRange]);
  const save = reactExports.useCallback(async (evts) => {
    setSaving(true);
    try {
      if (evts.some((e2) => e2.end <= e2.start)) {
        setError("Shift end must be after start");
        setSaving(false);
        return;
      }
      if (hasOverlap(evts)) {
        setError("Employees have overlapping shifts");
        setSaving(false);
        return;
      }
      const plain = evts.map((e2) => ({
        id: e2.id,
        employee_id: e2.employee_id,
        start_time: e2.start.toISOString(),
        end_time: e2.end.toISOString(),
        week_key: e2.week_key
      }));
      const res = await fetch("/api/schedule", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ schedule: plain })
      });
      if (!res.ok) throw new Error("save");
      setError(null);
      setDirty(false);
    } catch (err) {
      console.error(err);
      setError("Failed to save schedule");
    } finally {
      setSaving(false);
    }
  }, []);
  const pushUndo = (next) => {
    setUndoStack((u2) => [...u2, events2]);
    setEvents(next);
    setDirty(true);
  };
  const handleDrop = ({ event, start: start2, end: end2 }) => {
    const wk = start2.toISOString().split("T")[0];
    pushUndo(
      events2.map((e2) => e2 === event ? { ...e2, start: start2, end: end2, week_key: wk } : e2)
    );
  };
  const handleResize = ({ event, start: start2, end: end2 }) => {
    const wk = start2.toISOString().split("T")[0];
    pushUndo(
      events2.map((e2) => e2 === event ? { ...e2, start: start2, end: end2, week_key: wk } : e2)
    );
  };
  const onDropFromOutside = ({ start: start2, end: end2 }) => {
    if (externalEmp.current) {
      const empId = externalEmp.current;
      const wk = start2.toISOString().split("T")[0];
      pushUndo([
        ...events2,
        {
          id: Date.now(),
          employee_id: empId,
          start: start2,
          end: end2,
          week_key: wk,
          title: employees.find((e2) => e2.id === empId)?.name || "Shift"
        }
      ]);
      externalEmp.current = null;
    }
  };
  const undo = () => {
    setUndoStack((u2) => {
      if (!u2.length) return u2;
      const last = u2[u2.length - 1];
      setEvents(last);
      return u2.slice(0, -1);
    });
  };
  const copyEvent = (event) => {
    setCopied(event);
  };
  const pasteEvent = ({ start: start2, end: end2 }) => {
    if (copied) {
      const newEnd = new Date(start2.getTime() + (copied.end - copied.start));
      onDropFromOutside({ start: start2, end: newEnd });
    } else {
      const opts = employees.map((e2) => `${e2.id}: ${e2.name}`).join("\n");
      const input = prompt(`Assign employee ID:
${opts}`);
      const empId = parseInt(input, 10);
      if (empId && employees.find((e2) => e2.id === empId)) {
        externalEmp.current = empId;
        onDropFromOutside({ start: start2, end: end2 });
      }
    }
  };
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    error && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "alert alert-danger", children: [
      error,
      " ",
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { className: "btn btn-sm btn-light", onClick: () => save(events2), children: "Retry" })
    ] }),
    loading ? /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-center w-100 p-5", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "spinner-border", role: "status", children: /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "visually-hidden", children: "Loading..." }) }) }) : /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { className: "list-group me-3", style: { width: "200px" }, children: filteredEmployees.map((e2) => /* @__PURE__ */ jsxRuntimeExports.jsx(
        "li",
        {
          className: "list-group-item",
          draggable: true,
          onDragStart: () => externalEmp.current = e2.id,
          children: e2.name
        },
        e2.id
      )) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { flex: 1 }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2 d-flex", style: { gap: "0.5rem" }, children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label mb-0", children: "From" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              type: "time",
              className: "form-control form-control-sm",
              style: { width: "110px" },
              value: timeRange.start,
              onChange: (e2) => setTimeRange((r2) => ({ ...r2, start: e2.target.value }))
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label mb-0 ms-2", children: "To" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              type: "time",
              className: "form-control form-control-sm",
              style: { width: "110px" },
              value: timeRange.end,
              onChange: (e2) => setTimeRange((r2) => ({ ...r2, end: e2.target.value }))
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "select",
            {
              className: "form-select form-select-sm ms-2",
              style: { width: "150px" },
              value: roleFilter,
              onChange: (e2) => setRoleFilter(e2.target.value),
              children: roles.map((r2) => /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: r2, children: r2 }, r2))
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          DnDCalendar,
          {
            localizer,
            events: displayEvents,
            defaultView: view,
            views: [views.DAY, views.WEEK, views.MONTH],
            onView: changeView,
            dayLayoutAlgorithm: "no-overlap",
            onEventDrop: handleDrop,
            onEventResize: handleResize,
            resizable: true,
            eventPropGetter: getEventStyle,
            onDropFromOutside,
            selectable: true,
            onSelectEvent: copyEvent,
            onSelectSlot: pasteEvent,
            min: parseTime(timeRange.start),
            max: parseTime(timeRange.end),
            step: 15,
            timeslots: 4,
            longPressThreshold: 10,
            style: { height: 600 }
          }
        )
      ] })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-2", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          className: "btn btn-sm btn-outline-secondary me-2",
          onClick: undo,
          disabled: !undoStack.length,
          children: "Undo"
        }
      ),
      /* @__PURE__ */ jsxRuntimeExports.jsxs(
        "button",
        {
          className: "btn btn-sm btn-primary",
          onClick: () => save(events2),
          disabled: saving || !dirty,
          children: [
            "Save",
            saving && /* @__PURE__ */ jsxRuntimeExports.jsx(
              "span",
              {
                className: "spinner-border spinner-border-sm ms-2",
                role: "status"
              }
            )
          ]
        }
      )
    ] })
  ] });
}
function FlashMessage({ message, error, detail }) {
  const [show, setShow] = reactExports.useState(Boolean(message || error));
  reactExports.useEffect(() => {
    if (show) {
      const url = new URL(window.location);
      url.searchParams.delete("msg");
      url.searchParams.delete("err");
      url.searchParams.delete("detail");
      window.history.replaceState({}, "", url);
    }
  }, [show]);
  if (!show) return null;
  const bg = error ? "danger" : "success";
  return /* @__PURE__ */ jsxRuntimeExports.jsx(ToastContainer, { position: "top-end", className: "p-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(Toast$1, { bg, onClose: () => setShow(false), show, delay: 3e3, autohide: true, children: /* @__PURE__ */ jsxRuntimeExports.jsxs(Toast$1.Body, { children: [
    error || message,
    detail && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "small text-muted", children: detail })
  ] }) }) });
}
function LoginPage() {
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "login-page u-center-screen", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "login-container text-center", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "auth-card card shadow-sm p-4", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "mb-4", children: "Employee Login" }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("form", { method: "post", action: "/api/login", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "text",
          name: "username",
          placeholder: "Username",
          required: true,
          className: "form-control"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mb-3", children: /* @__PURE__ */ jsxRuntimeExports.jsx(
        "input",
        {
          type: "password",
          name: "password",
          placeholder: "Password",
          required: true,
          className: "form-control"
        }
      ) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("button", { type: "submit", className: "btn btn-primary w-100", children: "Log In" })
    ] })
  ] }) }) });
}
function OrderEntryPage() {
  const [categories, setCategories] = reactExports.useState([]);
  const [modGroups, setModGroups] = reactExports.useState([]);
  const [table, setTable] = reactExports.useState(null);
  const [orderType, setOrderType] = reactExports.useState("DINE-IN");
  const [cart, setCart] = reactExports.useState([]);
  const [message, setMessage] = reactExports.useState(null);
  const [currentItem, setCurrentItem] = reactExports.useState(null);
  const [selectedMods, setSelectedMods] = reactExports.useState([]);
  const [instructions, setInstructions] = reactExports.useState("");
  const [allergy, setAllergy] = reactExports.useState(false);
  const [allergyDetails, setAllergyDetails] = reactExports.useState("");
  reactExports.useEffect(() => {
    fetch("/order").then((res) => res.json()).then((data) => {
      setCategories(data.categories || []);
      setModGroups(data.modGroups || []);
      setTable(data.table || null);
    }).catch(() => {
    });
  }, []);
  const itemMap = reactExports.useMemo(() => {
    const map = {};
    categories.forEach((cat) => {
      (cat.items || []).forEach((it) => {
        map[it.id] = it;
      });
    });
    return map;
  }, [categories]);
  const total = reactExports.useMemo(
    () => cart.reduce((sum, c2) => sum + c2.quantity * (c2.price || 0), 0),
    [cart]
  );
  function openItem(itemId) {
    const it = itemMap[itemId];
    if (!it) return;
    setCurrentItem(it);
    setSelectedMods([]);
    setInstructions("");
    setAllergy(false);
    setAllergyDetails("");
  }
  function toggleMod(id2) {
    setSelectedMods(
      (mods) => mods.includes(id2) ? mods.filter((m3) => m3 !== id2) : [...mods, id2]
    );
  }
  function confirmItem() {
    if (!currentItem) return;
    const modNames = selectedMods.map((id2) => {
      const m3 = currentItem.modifiers.find((mod) => mod.id === id2);
      return m3 ? m3.name : null;
    }).filter(Boolean);
    setCart((c2) => [
      ...c2,
      {
        itemId: currentItem.id,
        name: currentItem.name,
        price: currentItem.price,
        modifierIds: selectedMods,
        modifierNames: modNames,
        quantity: 1,
        instructions,
        allergy,
        allergyDetails
      }
    ]);
    setCurrentItem(null);
  }
  function inc(idx) {
    setCart(
      (c2) => c2.map(
        (item, i2) => i2 === idx ? { ...item, quantity: item.quantity + 1 } : item
      )
    );
  }
  function dec(idx) {
    setCart(
      (c2) => c2.map(
        (item, i2) => i2 === idx ? { ...item, quantity: item.quantity - 1 } : item
      ).filter((item) => item.quantity > 0)
    );
  }
  function remove2(idx) {
    setCart((c2) => c2.filter((_, i2) => i2 !== idx));
  }
  function submitOrder() {
    const payload = {
      order_number: table || null,
      order_type: orderType,
      items: cart.map((c2) => ({
        menu_item_id: c2.itemId,
        quantity: c2.quantity,
        modifier_ids: c2.modifierIds,
        special_instructions: [c2.instructions, c2.allergyDetails].filter(Boolean).join(" | ") || null,
        allergy: !!c2.allergy
      }))
    };
    fetch("/api/orders", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    }).then((res) => res.json()).then(() => {
      setCart([]);
      setMessage({ text: "Order placed!", error: false });
    }).catch(() => {
      setMessage({ text: "Error sending order", error: true });
    });
  }
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: `menu-layout-grid`, children: [
    /* @__PURE__ */ jsxRuntimeExports.jsxs("header", { className: "menu-header", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "d-flex flex-column", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "brand-name fw-bold" }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "menu-title m-0", children: "Menu" })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "header-controls", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mt-1 order-type-select", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("label", { htmlFor: "orderType", className: "fw-bold me-2", children: "Order Type:" }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs(
          "select",
          {
            id: "orderType",
            className: "form-select d-inline-block w-auto",
            value: orderType,
            onChange: (e2) => setOrderType(e2.target.value),
            children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "DINE-IN", children: "DINE-IN" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "TO-GO", children: "TO-GO" }),
              /* @__PURE__ */ jsxRuntimeExports.jsx("option", { value: "CATERING", children: "CATERING" })
            ]
          }
        )
      ] }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("nav", { id: "categoryNav", children: categories.map((cat) => /* @__PURE__ */ jsxRuntimeExports.jsx("a", { href: `#cat-${cat.id}`, children: cat.name }, cat.id)) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("main", { id: "menu", className: "menu-content container py-3", children: categories.map((cat) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "category", id: `cat-${cat.id}`, children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h2", { children: cat.name }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "item-grid", children: (cat.items || []).map((item) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "item-card", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "img",
          {
            src: item.image_url || "/no-image-150.png",
            alt: item.name
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "item-name", children: item.name }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "item-price", children: [
          "$",
          Number(item.price).toFixed(2)
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            className: "btn btn-primary",
            onClick: () => openItem(item.id),
            children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-plus-lg" })
          }
        )
      ] }, item.id)) })
    ] }, cat.id)) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "cart", id: "cart", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("h3", { children: "Your Order" }),
      /* @__PURE__ */ jsxRuntimeExports.jsx("ul", { id: "cartItems", children: cart.map((c2, idx) => /* @__PURE__ */ jsxRuntimeExports.jsxs("li", { children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          c2.quantity,
          "× ",
          c2.name,
          c2.modifierNames.length > 0 && /* @__PURE__ */ jsxRuntimeExports.jsxs(jsxRuntimeExports.Fragment, { children: [
            " (",
            c2.modifierNames.join(", "),
            ")"
          ] }),
          c2.allergy && " [ALLERGY]",
          c2.allergyDetails && ` (${c2.allergyDetails})`,
          c2.instructions && ` - ${c2.instructions}`
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: "btn btn-primary btn-sm ms-1",
              onClick: () => dec(idx),
              children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-dash-lg" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: "btn btn-primary btn-sm ms-1",
              onClick: () => inc(idx),
              children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-plus-lg" })
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "button",
            {
              className: "btn btn-danger btn-sm ms-1",
              onClick: () => remove2(idx),
              children: /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-trash" })
            }
          )
        ] })
      ] }, idx)) }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { id: "cartTotal", className: "cart-total", children: [
        "Total: $",
        total.toFixed(2)
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(
        "button",
        {
          id: "submitBtn",
          className: "btn btn-success",
          onClick: submitOrder,
          disabled: cart.length === 0,
          children: "Submit Order"
        }
      )
    ] }),
    currentItem && /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "modal d-block", tabIndex: "-1", role: "dialog", children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "modal-dialog modal-dialog-centered", role: "document", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modal-content modal-small bg-white text-dark", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modal-header", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsxs("h5", { className: "modal-title", children: [
          "Add ",
          currentItem.name
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            type: "button",
            className: "btn-close",
            onClick: () => setCurrentItem(null)
          }
        )
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modal-body p-3", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("div", { children: (() => {
          const groups = {};
          currentItem.modifiers.forEach((m3) => {
            const gid = m3.group_id || "extras";
            if (!groups[gid]) groups[gid] = [];
            groups[gid].push(m3);
          });
          return Object.keys(groups).map((gid) => /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "mod-group-label", children: gid === "extras" ? "Extras" : modGroups.find((g) => g.id === parseInt(gid))?.name || "Options" }),
            groups[gid].map((m3) => /* @__PURE__ */ jsxRuntimeExports.jsxs("label", { className: "d-block", children: [
              /* @__PURE__ */ jsxRuntimeExports.jsx(
                "input",
                {
                  type: "checkbox",
                  value: m3.id,
                  checked: selectedMods.includes(m3.id),
                  onChange: () => toggleMod(m3.id)
                }
              ),
              " ",
              m3.name
            ] }, m3.id))
          ] }, gid));
        })() }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label", children: "Special Instructions:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "textarea",
            {
              rows: "2",
              className: "form-control",
              value: instructions,
              onChange: (e2) => setInstructions(e2.target.value)
            }
          )
        ] }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2 form-check", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              type: "checkbox",
              className: "form-check-input",
              id: "modAllergy",
              checked: allergy,
              onChange: (e2) => setAllergy(e2.target.checked)
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-check-label", htmlFor: "modAllergy", children: "Allergy" })
        ] }),
        allergy && /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "mb-2", id: "modAllergyDetailDiv", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsx("label", { className: "form-label", children: "Allergy Details:" }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            "input",
            {
              type: "text",
              className: "form-control",
              value: allergyDetails,
              onChange: (e2) => setAllergyDetails(e2.target.value)
            }
          )
        ] })
      ] }),
      /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "modal-footer", children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            id: "modConfirm",
            className: "btn btn-success",
            onClick: confirmItem,
            children: "Add Item"
          }
        ),
        /* @__PURE__ */ jsxRuntimeExports.jsx(
          "button",
          {
            id: "modCancel",
            className: "btn btn-secondary",
            onClick: () => setCurrentItem(null),
            children: "Cancel"
          }
        )
      ] })
    ] }) }) }),
    message && /* @__PURE__ */ jsxRuntimeExports.jsx(
      "div",
      {
        className: `toast show position-fixed bottom-0 end-0 m-3 text-bg-${message.error ? "danger" : "success"}`,
        children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "toast-body", children: message.text })
      }
    )
  ] });
}
function StationsPage() {
  const [stations, setStations] = reactExports.useState([]);
  reactExports.useEffect(() => {
    fetch("/stations").then((res) => res.json()).then((data) => setStations(data.stations || [])).catch(() => {
    });
  }, []);
  return /* @__PURE__ */ jsxRuntimeExports.jsx("main", { className: "login-page home-page d-flex align-items-center justify-content-center", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "login-card card shadow-sm p-4 text-center", children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { className: "mb-4", children: "Choose Station" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "row g-4 justify-content-center", children: stations.map((st) => /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "col-12 col-sm-6 col-md-4", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("a", { href: `/station/${st.id}`, className: "btn btn-primary dash-btn w-100", children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx("i", { className: "bi bi-display-fill me-2", "aria-hidden": "true" }),
      st.name,
      st.type && /* @__PURE__ */ jsxRuntimeExports.jsx("small", { className: "d-block text-uppercase", children: st.type })
    ] }) }, st.id)) })
  ] }) });
}
const SuppliersRoute = reactExports.lazy(() => __vitePreload(() => import("./Suppliers-CbKrIJiN.js"), true ? [] : void 0));
const PurchaseOrdersRoute = reactExports.lazy(() => __vitePreload(() => import("./PurchaseOrders-ncSkMyeg.js"), true ? [] : void 0));
const PurchaseOrderDetailRoute = reactExports.lazy(() => __vitePreload(() => import("./PurchaseOrderDetail-BRXH7do0.js"), true ? [] : void 0));
function Home() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { children: "Home" }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(PluginZone, { zone: "dashboard" })
  ] });
}
function RoutesConfig() {
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(Routes, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "/login", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LoginPage, {}) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "/", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutWrap, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(Home, {}) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "/order", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutWrap, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(OrderEntryPage, {}) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "/stations", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutWrap, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(StationsPage, {}) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "/foh/order", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutWrap, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(OrderEntryPage, {}) }) }),
    /* @__PURE__ */ jsxRuntimeExports.jsxs(Route, { path: "/admin", element: /* @__PURE__ */ jsxRuntimeExports.jsx(LayoutWrap, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(AdminShell, {}) }), children: [
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { index: true, element: /* @__PURE__ */ jsxRuntimeExports.jsx(AdminPanels, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "employees", element: /* @__PURE__ */ jsxRuntimeExports.jsx(EmployeesRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "roles", element: /* @__PURE__ */ jsxRuntimeExports.jsx(RolesRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "payroll", element: /* @__PURE__ */ jsxRuntimeExports.jsx(PayrollRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "inventory", element: /* @__PURE__ */ jsxRuntimeExports.jsx(InventoryRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "suppliers", element: /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Suspense, { fallback: null, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(SuppliersRoute, {}) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "purchase-orders", element: /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Suspense, { fallback: null, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(PurchaseOrdersRoute, {}) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "purchase-orders/:id", element: /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.Suspense, { fallback: null, children: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(PurchaseOrderDetailRoute, {}) }) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "timeclock", element: /* @__PURE__ */ jsxRuntimeExports.jsx(TimeClockRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "backups", element: /* @__PURE__ */ jsxRuntimeExports.jsx(BackupsRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "schedule", element: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ScheduleApp, {}) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "menu", element: /* @__PURE__ */ jsxRuntimeExports.jsx(MenuRoute, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "stations", element: /* @__PURE__ */ jsxRuntimeExports.jsx(AdminPanels, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "theme", element: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(ThemeEditorPro, {}) }) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "layouts", element: /* @__PURE__ */ jsxRuntimeExports.jsx(AdminPanels, {}) }),
      /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "flags", element: /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "admin-section", children: /* @__PURE__ */ jsxRuntimeExports.jsx(FeatureFlagsAdminPanel, {}) }) })
    ] }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(Route, { path: "*", element: /* @__PURE__ */ jsxRuntimeExports.jsx(Navigate, { to: "/login", replace: true }) })
  ] });
}
function App() {
  const params = new URLSearchParams(window.location.search);
  const msg = params.get("msg");
  const err = params.get("err");
  const detail = params.get("detail");
  return /* @__PURE__ */ jsxRuntimeExports.jsxs(BrowserRouter, { children: [
    /* @__PURE__ */ jsxRuntimeExports.jsx(FlashMessage, { message: msg, error: err, detail }),
    /* @__PURE__ */ jsxRuntimeExports.jsx(RoutesConfig, {})
  ] });
}
class AppErrorBoundary extends reactExports.Component {
  constructor(props) {
    super(props);
    this.state = { hasError: false, error: null, errorInfo: null };
  }
  static getDerivedStateFromError() {
    return { hasError: true };
  }
  componentDidCatch(error, errorInfo) {
    console.error("App crashed:", error, errorInfo);
    this.setState({ error, errorInfo });
    try {
      fetch("/api/log", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({
          message: error?.message || error?.toString() || "App crash",
          stack: error?.stack || errorInfo?.componentStack,
          componentStack: errorInfo?.componentStack
        })
      });
    } catch (loggingError) {
      console.error("Logging failed:", loggingError);
    }
  }
  handleReload = () => {
    window.location.reload();
  };
  render() {
    if (this.state.hasError) {
      return /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { style: { padding: "2rem", textAlign: "center" }, children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx("h1", { children: "Something went wrong." }),
        /* @__PURE__ */ jsxRuntimeExports.jsx("p", { children: "We're sorry, but the application encountered an unexpected error." }),
        false,
        /* @__PURE__ */ jsxRuntimeExports.jsx("button", { onClick: this.handleReload, style: { marginTop: "1rem" }, children: "Reload page" })
      ] });
    }
    return this.props.children;
  }
}
function ensureRoot() {
  let el = document.getElementById("root");
  if (!el) {
    el = document.createElement("div");
    el.id = "root";
    document.body.appendChild(el);
  }
  return el;
}
const rootEl = ensureRoot();
try {
  console.log("[FreeKDS] Bootstrapping app…");
  clientExports.createRoot(rootEl).render(
    /* @__PURE__ */ jsxRuntimeExports.jsx(reactExports.StrictMode, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(AppErrorBoundary, { children: /* @__PURE__ */ jsxRuntimeExports.jsx(App, {}) }) })
  );
} catch (err) {
  console.error("[FreeKDS] Bootstrap failed", err);
  try {
    fetch("/api/log", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ message: "Bootstrap error", stack: err?.stack || String(err) })
    });
  } catch {
  }
  try {
    rootEl.innerHTML = '<pre style="padding:1rem">Bootstrap error. See console/server logs.</pre>';
  } catch {
  }
}
export {
  Link as L,
  useParams as a,
  jsxRuntimeExports as j,
  reactExports as r,
  useNavigate as u
};
//# sourceMappingURL=app-z9U9ikwo.js.map
