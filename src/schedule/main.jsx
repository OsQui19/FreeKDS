import React from 'react';
import ReactDOM from 'react-dom/client';
import ScheduleApp from './ScheduleApp.jsx';

ReactDOM.createRoot(document.getElementById('scheduleApp')).render(
  <React.StrictMode>
    <ScheduleApp />
  </React.StrictMode>
);
