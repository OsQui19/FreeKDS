<div class="nav-buttons d-flex gap-2">
  <% if (typeof hasAccess === 'function' && hasAccess('order')) { %>
    <a href="/order" class="btn btn-secondary btn-sm" aria-label="Order">Order</a>
  <% } %>
  <% if (typeof hasAccess === 'function' && hasAccess('stations')) { %>
    <a href="/stations" class="btn btn-secondary btn-sm" aria-label="KDS">KDS</a>
  <% } %>
  <% if (
    typeof hasAccess === 'function' &&
    (hasAccess('menu') ||
      hasAccess('inventory') ||
      hasAccess('suppliers') ||
      hasAccess('purchase-orders') ||
      hasAccess('reports') ||
      hasAccess('employees') ||
      hasAccess('theme') ||
      hasAccess('locations'))
  ) { %>
    <a href="/admin?tab=stations" id="adminNavBtn" class="btn btn-secondary btn-sm" aria-label="Admin">Admin</a>
  <% } %>
  <a href="/clock/dashboard" class="btn btn-secondary btn-sm" aria-label="Clock">Clock</a>
  <form action="/logout" method="post" class="m-0">
    <button type="submit" class="btn btn-danger btn-sm" aria-label="Log Out">
      Log Out
    </button>
  </form>
</div>
