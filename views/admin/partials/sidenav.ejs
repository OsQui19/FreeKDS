<%
  const moduleInfo = {
    stations: ["bi-hdd-network", "Stations"],
    menu: ["bi-list-ul", "Menu"],
    theme: ["bi-palette", "Theme"],
    inventory: ["bi-box-seam", "Inventory"],
    employees: ["bi-people", "Employees"],
    reports: ["bi-bar-chart", "Reports"],
    backup: ["bi-cloud-arrow-down", "Backups"],
    updates: ["bi-arrow-clockwise", "Updates"],
  };
  const mods = Array.isArray(allowedModules) ? allowedModules : [];
%>
<div id="sideNav" class="side-nav">
  <% mods.forEach(function(m){ const info = moduleInfo[m]; if(info){ %>
    <a href="#" class="nav-link open-tab" data-type="<%= m %>" title="<%= info[1] %>">
      <i class="bi <%= info[0] %>"></i><span><%= info[1] %></span>
    </a>
  <% }}); %>
</div>
